# 태깅 학습 루프 설계

목표: **수동 편집이 "그때 한 건만" 고치는 게 아니라 로직을 영구히 개선하게 만든다.**
(2026-08-31 작성. Fable 자문 + 실DB 실측을 합쳐 정리. 실행은 `TAGGING.md`의 규칙을 따른다.)

---

## 0. 목표 재정의 — 두 단어만 바꾼다

사용자 표현은 "수동 편집 1% 미만 · 실시간 최적화"였다. 방향은 맞지만 그대로 두면 측정도 안 되고 위험하다.

| 원래 | 바꾼 것 | 이유 |
|---|---|---|
| 전체의 1% 미만 | **주간 신규 유입 중 수동 편집이 필요한 비율** | 누적 38만 건이 분모면 현재 오류율조차 모른다. 지금까지 찾아낸 오염(1,450·508·263건)은 **발견 건수**지 비율이 아니다 |
| 실시간 최적화 | **주 1회 배치 + 사람 승인** | 자동 반영은 사고의 통로다. 2026-08-20에 정상 영상 35,168건이 한 번에 숨겨졌다 |

첫 작업은 **기준선 채점**이다. 최근 유입분에서 무작위 300건을 뽑아 맞음/틀림을 손으로 매긴다.
이게 없으면 "나아졌다"를 영원히 말할 수 없다.

---

## 1. 지금 있는 것 / 없는 것

**있다**
- `tag_edit_log` — 수동 편집 before/after (2026-08-31 가동, 배선 7곳)
- `tools/m2_harness.js` — 배포 매처를 Node에서 그대로 실행
- `tools/name_pollution_probe.mjs` / `name_collision_audit.mjs` — 이름 오염 실측·정적 스캔
- 보류 탭 3,363건 — 확정 오답셋
- `tags_manual=true` — 사람이 확정한 정답셋
- `groups.json`의 `debut` — **268개 그룹 전부** 파싱 가능(실측), `projectShow` 33건 / `projectRing` 39건

**없다 (이번 설계가 채울 것)**
1. **매처가 "왜"를 안 남긴다.** `_m2ParseTitle`은 `confidence: strong/weak/ambiguous` 셋만 돌려준다. 사람이 고친 결과가 쌓여도 그 오류를 *어느 규칙* 탓으로 돌릴 수가 없다. 학습 루프의 전제 조건.
2. **재생(replay) 스크립트가 없다.** "고쳤는데 나아졌나"를 못 잰다. 사고 이력 5건 중 3건이 "고쳤더니 다른 게 깨짐"이다.
3. **데뷔 이전 게이트가 없다.** 탈퇴·해체 컷오프는 있는데 "영상이 그룹 데뷔보다 한참 전이면 그 그룹일 수 없다"는 규칙이 없다.
4. **오디션 데이터가 없다.** 그룹→프로그램(`projectShow`) 33건뿐, 참가자 단위 매핑이 없다.

---

## 2. ⚠️ 실측으로 정정된 전제

설계를 시작하기 전에 짚어야 할 것들. **전부 추측이 아니라 DB에서 직접 잰 값이다.**

**① "2000년대 초반 영상이 끌려온다" — published_at 기준으로는 그런 영상이 없다**
```
published_at < 2005 :      0건   (유튜브 자체가 2005년 시작)
published_at < 2010 :    114건
published_at < 2013 :  5,157건
published_at < 2016 : 12,536건
```
진짜 문제는 "2000년대 업로드"가 아니라 **2008~2015년 영상이 엉뚱한 그룹에 배정된 것**이다.

**② 데뷔 게이트의 실제 효과 (2016년 이전 12,536건에 적용)**
```
데뷔 − 0년 이전 → 783건 (기처리 201)
데뷔 − 1년 이전 → 682건 (기처리 189)
데뷔 − 2년 이전 → 581건 (기처리 186)
데뷔 − 3년 이전 → 508건 (기처리 151)   ← 권장
데뷔 − 5년 이전 → 374건 (기처리 103)
데뷔 −10년 이전 → 126건 (기처리  37)
```
**−10년(원래 아이디어)은 126건밖에 못 잡는다. −3년이면 508건**이고, 오디션 예외(대부분 데뷔 2년 이내, 최장 3년: SM루키즈→NCT)를 다 덮는다.

**③ 게이트의 진짜 가치는 "옛날 영상 거르기"가 아니라 오배정 탐지다**
−3년 기준 미플래그 357건의 그룹별 1위가 이거다:
```
97건  아이들 (2018 데뷔)  ← 전부 제국의 아이들(ZE:A) 영상
30건  이즈나 (2024 데뷔)  ← M4·채연 등 2011~2012 무대
19건  온리원오프          ← 9초(NINE SECOND) 커버 영상
10건  엔하이픈            ← 박재범 4가지쇼(2014)
 6건  더보이즈            ← 이효리 2008
```
**`아이들` ⊂ `제국의 아이들`** — `B.I` ⊂ `B.I.G`와 **똑같은 부분문자열 충돌**이다. 즉 데뷔 게이트는
연도 필터가 아니라 **부분문자열·동명이인 오배정을 값싸게 잡아내는 그물**이다. 이게 이 게이트를
1순위로 두는 이유.

**④ `tag_edit_log`는 anon 키로 못 읽는다** (RLS가 어드민 이메일 기반). 그래서 재생 스크립트를
"node에서 DB 직접 조회"로 만들 수 없다. → **브라우저(어드민 세션)에서 JSON 내보내기 → node harness가
재생** 구조여야 한다. Fable 제안에서 빠져 있던 제약.

---

## 3. 루프 구조

```
① 수집    수동 편집·무관·보류 처리가 tag_edit_log에 쌓인다              [가동 중]
      ↓
② 재생    골든셋에 현재 매처를 다시 돌려 필드별 일치율을 낸다
      ↓    불일치를 trace(어느 규칙이 결정했나)·채널·연도로 묶는다
③ 제안    반복 패턴을 후보로 승격 — 흔한단어 / 신규 그룹 / 오디션 매핑
      ↓
④ 승인    사람이 후보만 yes/no                                    ← 유일한 수동 단계
      ↓
⑤ 고정    승인된 사례를 tests/golden.json에 넣어 회귀 테스트로 전환
```

**⑤가 핵심이다.** 수동 편집 하나가 테스트 케이스 하나가 되어야 "학습"이 축적된다. 안 그러면
같은 오류를 몇 달 뒤에 다시 만든다(실제로 사고 이력 3건이 그랬다).

②~③은 **전부 읽기 전용**이다. 쓰기는 ④ 승인 뒤에만 일어나고, 언제나 스냅샷을 남긴다.

---

## 4. 작업 순서

효과가 확실하고 작은 것부터. 앞 단계가 뒤 단계의 토대가 되도록 배열했다.

### P0 · 데뷔 이전 게이트 — 측정 완료, 바로 가능
- 위치: **`_m2ParseTitle` 안**. `publishedAt`이 이미 파라미터로 들어오고 동기화·스윕이 같은 함수를
  타므로 여기가 유일한 판정점이다. `_extBuildRows`에 넣으면 스윕과 갈라진다(TAGGING §2).
- 규칙: 매칭된 그룹 중 `published_at 연도 < debut 연도 − 3`인 그룹을 primary 후보에서 제외.
  남는 그룹이 있으면 그걸로, 없으면 `null` → 기존 무매칭 경로(**보류**, '무관' 아님 — 이전 그룹이
  나중에 등록되면 재판정 대상이 되어야 하므로).
- owner 채널(idol/fans tier)은 `group_ko`가 고정이라 대상 아님.
- 기존 오염은 읽기 전용 미리보기(그룹별 건수 + 표본 40건) → 스냅샷 → 반영.
- 회귀: "2012년 영상 + 아이들(2018 데뷔) → 아이들 배정 안 됨" 케이스 추가.

### P1 · 매처가 "왜"를 남기게 — `trace`
- `_m2ParseTitle`이 `trace: [{rule, token, effect}]`를 같이 반환한다. **DB 변경 없음** — harness와
  재생 스크립트에서만 읽는다(런타임 비용은 배열 push 정도).
- 남길 규칙 이름 예: `debut_gate` · `songtitle_strip` · `hashtag_only` · `surname_match` ·
  `desc_noise` · `initialism_boundary` · `infer_exclude` · `dual_normalize`.
- 이게 있어야 ③ 제안 단계가 "이 오류는 어느 규칙 탓"이라고 말할 수 있다.

### P2 · 재생 스크립트 + 골든셋
- 어드민에 **"학습 로그 내보내기"** 버튼 → `tag_edit_log` + `tags_manual=true` 표본을 JSON 파일로.
  (④ 제약 때문에 브라우저에서 뽑아야 한다.)
- `tools/replay.mjs` — 그 JSON에 현재 매처를 돌려 `group_ko / members / with_* / cover_of_*` **필드별
  일치율**과 불일치 목록(trace 포함)을 출력.
- `tests/golden.json` — 승인된 사례를 케이스로 고정. `tests/golden.test.js`가 매번 돌린다.

### P3 · 흔한단어·동명이인을 목록에서 통계로
지금 방어는 **사람이 채우는 목록** 6종이라 새 사례를 영원히 못 따라간다(TAGGING §4 각주).
- 이름 토큰별로 **"그 이름이 등장한 영상 중 근거(그룹명·해시태그·영문명)가 같이 있는 비율"** 을
  코퍼스 전체에서 계산 → 비율이 낮으면 자동으로 해시태그-전용 후보에 올린다.
- `name_pollution_probe.mjs`가 이미 표본 기준으로 이걸 한다. **전수로 올리고 임계값을 붙이면** 된다.
- 승인된 것만 `name_match_whitelist`에 들어간다(기존 ⚑ 경로 재사용).

### P4 · 보류 탭 → 신규 그룹 등록 후보
- 보류 3,363건에서 GROUPS/ARTISTS에 없는 반복 토큰(해시태그·괄호 영문명·병기표기)을 빈도 집계.
- 실측 상위 후보: **메이크메이트원 · THE NEW SIX · 휘브 · 에이식스 · 아크(ARrC) · 버가부 ·
  하츠웨이브 · 아트비트 · 하이파이유니콘 · INI · TNX · ASC2NT · 여동생(YDS)**
- 등록 시 [[신규 그룹 추가 전 동명이인 오분류 점검]] 절차를 그대로 탄다.

### P5 · 오디션 프로그램 — `shows.json`
사용자 결정: **성격이 둘이라 개별로 정보를 넣는다.**

```jsonc
"프로듀스 101 시즌2": {
  "type": "predebut",              // 데뷔 직전 연습생 오디션
  "aired": ["2017-04-07","2017-06-16"],
  "channels": ["Mnet","M2"],
  "result": "워너원",               // groups.json 키 (projectRing과 연결)
  "contestants": ["이대휘(에이비식스)","박우진(에이비식스)"]   // 현재 유니버스 아티스트만
}
"킹덤": {
  "type": "reunion",               // 이미 데뷔한 아이돌의 경연/재결성
  "aired": ["2021-04-01","2021-06-03"],
  "groups": ["에이티즈","더보이즈","스트레이키즈"]
}
```

**태깅에서의 효과 두 가지**
1. 제목에 프로그램명이 있으면 **멤버 후보를 그 프로그램 명단으로 제한** — 전 로스터 대조보다 오탐이
   크게 준다. (지금 보류 더미의 `뉴 416 · 한 275 · 이니 168`이 정확히 이 문제다.)
2. **방영 기간 밖 영상**은 그 프로그램 매칭에서 제외.

**노출 규칙(사용자 결정)**: predebut 영상도 **일반 그룹 영상과 똑같이 취급**한다 —
결과 그룹 카드에 뜨고, 일부 멤버만 나오면 그 멤버만 태깅해서 **미출연 멤버 카드에는 안 뜬다.**
즉 별도 노출 규칙을 만들지 않는다(`group_ko` = 결과 그룹, `members` = 실제 출연자).
→ P0 데뷔 게이트의 −3년 여유가 이 영상들을 살려주는 것과 정확히 맞물린다.

---

## 5. 주간 리듬

```
월  재생 리포트 자동 생성 (일치율 + 불일치 TOP + 규칙별 기여도)
    ↓
    후보 검토 15분 — 흔한단어 N개 / 신규 그룹 N개 / 오디션 매핑 N개
    ↓
    승인분 반영 + 골든셋에 케이스 추가
```
월 1회: 기준선 재채점(무작위 300건)으로 실제 개선폭 확인.

---

## 6. 지금 결정이 필요한 것

1. **기준선 채점 300건을 언제 할지.** 이게 있어야 이후 모든 수치가 의미를 갖는다.
2. **P5 `shows.json`을 누가 채울지.** 프로그램별 참가자 명단은 조사가 필요하다(나무위키 기준,
   [[나무위키 링크로 그룹 데이터 조사]] 절차). 우선순위 프로그램 5~6개부터.
3. **P3 임계값.** "근거 비율 몇 % 미만이면 해시태그 전용"인지는 실측 분포를 보고 정한다.
