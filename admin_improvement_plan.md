# 관리자 기능 총체적 개선 계획 (2026-08-18)

전제: 관리자는 1인(운영자 본인). 목표는 "기능 추가"가 아니라 **매일 루틴의 마찰 제거**와 **검수 작업의 왕복 제거**. 가오픈 이후 유저 피드백이 들어오기 시작하면 검수·태깅 작업량이 늘어나므로, 그 전에 관리 동선을 정리해두는 것이 목적.

---

## 진단 요약

| 증상 (관리자 체감) | 구조적 원인 |
|---|---|
| PC 큰 화면인데 어드민이 좁고 답답함 | 어드민 전체가 252px 설정 패널(#sp-yt-sec) 안에 버튼 25+개 세로 나열 |
| 검수 센터에 손이 안 감 | 들어가야만 내용이 보이는 구조 — 할 일 개수가 바깥에 안 보임 |
| 동명이인 클릭해도 카드 안 열림 (PC) | `_dqGotoArtist`가 데스크톱 분기에서 `showT`(툴팁)만 호출, 멤버 패널 미오픈 |
| 검수하면서 카드 확인 불가 | 검수/영상 관리가 중앙 오버레이(z81~82)라 카드(z61~63)를 항상 덮음 |
| 편집 모달 뒤로 우주 호버/클릭 통과 | `vid-tag-overlay`가 window pointerdown 보호 목록에 누락. 근본적으로는 오버레이마다 stopPropagation을 수동으로 다는 구조 (동일 계열 버그 3회 재발 이력) |
| 버튼이 뭘 하는지 기억 안 남 | 버튼 라벨 외 설명 부재 (title 툴팁 일부만, 모바일에선 그마저 안 보임) |

---

## P1 — 세션 1: 이벤트 보호 구조 전환 (버그 수정, 최우선·소규모)

**목표: "모달 뒤 우주 호버/클릭" 계열 버그 원천 차단. 이후 어떤 오버레이를 새로 만들어도 재발 안 함.**

1. 모든 오버레이 루트 요소에 `data-modal` 속성 부여: `#vid-tag-overlay`, `#hnn-overlay`, `#vm-overlay`, `#gp-overlay`, `#feedback-overlay`, `#generic-confirm-overlay`, `#generic-prompt-overlay`, `#profile-overlay`, `#welcome-*-overlay`, `#date-edit-overlay`, `#col-picker`, `#feed-list-overlay`, `#venue-overlay`, `#trophy-overlay`, `#unfav-confirm-overlay`, `#logout-confirm-overlay`, `#link-confirm-overlay`.
2. window 레벨 핸들러 3곳에 공통 가드 추가 (기존 개별 stopPropagation은 그대로 둬도 무해):
   - `window pointerdown` (closePanels 호출부): `if(e.target.closest('[data-modal]'))return;`
   - `window click` (우주 클릭/카드 닫기): 동일 가드
   - `window mousemove` (멤버 툴팁 raycast, `_tipMouse` 갱신부): `if(document.querySelector('[data-modal].open,[data-modal][style*="flex"]'))return;` — 실제 오픈 판별 방식은 각 오버레이의 open 클래스/디스플레이 방식에 맞춰 헬퍼 `_anyModalOpen()` 하나로 통일 권장.
3. 검증: 편집 모달 열고 모달 바깥 배경에서 마우스 이동/클릭 → 행성 호버 하이라이트·카드 닫힘·패널 닫힘이 일절 없어야 함. hnn/vm/gp에서도 동일 확인.

**리스크: 낮음.** 가드는 "모달 열려 있을 때만" 발동하므로 평상시 동작 불변.

---

## P1 — 세션 2: 동명이인 → 카드 이동 수정 (버그 수정, 소규모)

`_dqGotoArtist` 데스크톱 분기를 모바일과 동등하게: `showT` 호출 후 멤버 패널까지 열기 (모바일의 `openMobSheet` 대응 동작 — 데스크톱에서 멤버 패널을 여는 기존 함수 경로 확인 후 호출). 검수 센터를 닫지 않는 동작은 세션 3(도킹 전환) 이후에 의미가 생기므로, 이 세션에서는 "닫히더라도 카드는 확실히 열리게"까지만.

---

## P1 — 세션 3~4: 어드민 콘솔 분리 — 좌측 도킹 워크스페이스 (구조 개편, 중규모)

**핵심 아이디어: 어드민을 설정 패널에서 완전히 분리해, 데스크톱에서 좌측에 도킹되는 넓은 패널(폭 380~460px)로 만든다. z-index를 카드(61~63)보다 낮게(≈58) 두면, 콘솔을 열어둔 채 우측에 실제 그룹/멤버 카드가 그대로 열린다 — "카드에 버금가는 확인 화면"을 새로 만드는 게 아니라 진짜 카드를 재사용.**

- 설정 패널의 `#sp-yt-sec` 전체 제거 → 설정 패널엔 "관리자 콘솔 열기" 버튼 하나만 (어드민 계정에만 노출, 기존 노출 조건 재사용).
- 콘솔 구조: 상단 탭 5개
  1. **홈(대시보드)** — 세션 5 참조
  2. **동기화** — 기존: API 키 입력, 전체 동기화, 조회수 갱신 3종, 백필, URL 직접 추가
  3. **검수** — 기존 검수 센터(태깅 품질/원곡 커버) + 동명이인 목록 통합
  4. **영상** — 기존 영상 관리 패널(vm) 내용 이관
  5. **정합성** — 매일 루틴 2~4번 + "필요할 때만" 버튼들
- **모든 버튼 아래 1줄 설명 캡션 상시 노출** (title 툴팁이 아니라 DOM 텍스트 — 폭이 380px+로 넓어지므로 공간 확보됨). 설명 문구는 기존 코드 주석에 이미 다 있음(예: "매칭 로직 고친 날은 이 버튼만" 류) — 주석을 캡션으로 승격만 하면 됨.
- 위험/일회용 버튼 시각 구분: "일회용", "전체 재처리(오래 걸림)" 계열은 주황 테두리 + 실행 전 확인 다이얼로그(기존 `_confirmDialog` 재사용).
- 모바일: 콘솔은 데스크톱 전용으로 시작. 모바일 어드민은 기존 설정 패널 방식 유지(모바일에서 하는 어드민 작업은 인라인 플래그 순환·간단 편집 위주이므로 콘솔 필요성 낮음). 유지비를 줄이기 위해 콘솔 DOM은 하나로 만들고 모바일에선 진입 버튼만 숨김.
- 기존 오버레이(hnn/vm/gp)는 콘솔 탭으로 내용을 이관한 뒤 제거. 이관 중엔 둘 다 살려두고 콘솔 쪽이 안정되면 오버레이 진입 버튼만 제거하는 2단계 마이그레이션 권장.

**검증 시나리오:** 콘솔 열기 → 검수 탭 → 동명이인 칩 클릭 → 콘솔 유지된 채 우측에 멤버 카드 열림 → 카드 확인 후 콘솔에서 다음 항목 클릭. 이 흐름이 왕복 없이 되면 성공.

---

## P2 — 세션 5: 콘솔 홈 = "오늘 할 일" 대시보드 (검수 센터 손 안 가는 문제의 근본 해법)

콘솔 첫 화면에 카운트 카드 나열 (각각 클릭 시 해당 탭으로 이동):
- 마지막 전체 동기화: N시간 전 · 추가된 영상 M개 (동기화 이력을 localStorage 또는 DB 한 행에 기록)
- 미태깅 영상: N개 (기존 미태깅 우선 정렬 쿼리의 count 재사용)
- 동명이인 의심: N건 / 원곡 커버 후보: N건 (기존 스캔 로직의 count만 가볍게)
- 미확인 피드백: N건
- **"매일 루틴 실행" 원클릭 버튼**: 1 전체 동기화 → 2 자동 태깅 → 3 콜라보 재검증 → 4 동명이인 스캔을 순차 실행 + 단계별 진행률 표시 (각 함수는 이미 독립적으로 존재하므로 체이닝만). 실패 시 해당 단계에서 멈추고 이유 표시.

숫자가 콘솔 열자마자 보이면 "검수 센터를 눌러야 할 이유"가 생기는 구조로 바뀜. 이게 P2 중 체감 효과 최대.

---

## P2 — 세션 6: 편집(연필) 모달 개선

- 데스크톱: 폭 320px → 560px급, 좌측에 영상 썸네일+제목 미리보기 고정(지금은 편집하는 영상이 모달에 가려 어떤 영상인지 확인하려면 닫아야 함).
- 위치: 화면 정중앙 대신 우측 치우침 배치(영상 그리드가 왼쪽에 보이도록) — 세션 1의 이벤트 가드가 선행돼야 안전.
- 키보드: Enter 저장, Esc 닫기.
- **연속 편집 모드(제안)**: 저장 시 그리드의 다음 영상으로 자동 이동하는 토글. 대량 검수 시 "닫기→다음 연필 클릭" 왕복 제거. 기존 `patchItem` 부분 갱신 로직과 궁합 좋음.

---

## P3 — 백로그 (여력 있을 때)

- **검수 큐 모드**: 미태깅/의심 영상을 한 장씩 크게 띄우고 키보드로 정상(1)/무관(2)/숨김(3)/편집(E) 즉시 분류. 대시보드 카운트를 태우는 소모 루프.
- **마지막 일괄 작업 실행 취소**: 일괄 무관/숨김/태깅 직전 상태를 1회분 저장해 undo 제공(오조작 보험).
- **동기화 리포트 로그**: 실행할 때마다 (시각, 채널 수, 추가/변경 영상 수, 실패 채널) 한 행 기록 → 대시보드에서 최근 5회 표시. YouTube API 쿼터 이상 징후 조기 발견용.

---

## 실행 순서 요약

| 순서 | 내용 | 규모 | 성격 |
|---|---|---|---|
| 1 | 이벤트 가드 구조 전환 | 소 | 버그 수정 (편집 모달 뒤 통과) |
| 2 | 동명이인 카드 이동 수정 | 소 | 버그 수정 |
| 3~4 | 어드민 콘솔 좌측 도킹 분리 + 버튼 설명 캡션 | 중 | 구조 개편 |
| 5 | 대시보드 + 매일 루틴 원클릭 | 중 | 워크플로우 |
| 6 | 편집 모달 개선 + 연속 편집 | 소~중 | 워크플로우 |
| 백로그 | 검수 큐 / undo / 동기화 리포트 | — | 제안 |

가오픈 10일 일정과의 관계: 1·2번(버그 수정)만 가오픈 전 권장. 3번 이후는 유저에게 안 보이는 관리자 영역이므로 가오픈 후 진행해도 무방하며, 오히려 가오픈 후 실제 검수량을 보고 대시보드 항목을 정하는 게 낫다.

---

## 2026-08-27 진행도 검토 (재구성 착수 전 — 사용자 요청 "검토만 해둬")

계획 대비 현재 상태를 소스에서 직접 확인한 결과:

| 항목 | 상태 | 근거 |
|---|---|---|
| P1-1 이벤트 가드(data-modal) | 🔶 부분 | 오버레이 `data-modal` **30곳** 적용됨. 단 계획이 권장한 `_anyModalOpen()` 헬퍼는 미도입 — window `pointerdown`/`click`/`mousemove` 3곳이 실제로 data-modal 가드를 타는지 착수 시 재검증 필요 |
| P1-2 동명이인→카드 이동 | ❓ 미확인 | `_dqGotoArtist` 심볼이 소스에서 안 잡힘(리네임/미구현 가능) — 착수 시 실제 동명이인 클릭 동작으로 확인 |
| **P1-3~4 좌측 도킹 콘솔 (핵심 재구성)** | ❌ **미완** | 설정 패널(`#sp-yt-sec`)에 `sp-*` 버튼 **64개**가 여전히 세로 나열. 콘솔 분리 안 됨 |
| P2-5 대시보드 + 매일루틴 원클릭 | ✅ 됨 | 관리자 홈(`adm-home-overlay`) + `_admRunRoutine` 구현. `tests/routine-parity.test.js`로 1→4 단계 대응 고정(통과 확인) |
| P2-6 편집모달 개선 | ❓ 부분 | `vid-tag-overlay` 존재. 폭 확대/썸네일 미리보기/연속편집 토글 여부는 착수 시 확인 |
| P3 undo/스냅샷 | ✅ 됨 | `_snapshotBeforeBulk` + `sp-yt-undo-bulk-btn` (거의 모든 스윕에 적용). 태깅 버튼 감사에서 22개 중 18개가 스냅샷+되돌리기 보유 확인 |

### 검토 의견
- **남은 핵심 미완은 P1-3~4 "설정 패널 → 좌측 도킹 콘솔"뿐이다.** 나머지 워크플로우(원클릭·대시보드·undo)는 이미 대부분 구현됨 — 계획이 순조롭게 진행돼 왔다.
- **가치**: 콘솔을 카드(z61~63)보다 낮은 z-index(≈58)로 띄워 "카드와 나란히" 보는 게 핵심 — 검수 왕복 제거. 체감 효과 최대.
- **리스크**: 어드민 전용이라 유저 영향 0. 기능 이관 중 누락만 주의 → 문서의 2단계 마이그레이션(기존 hnn/vm/gp 오버레이 살려두고 콘솔 안정 후 진입 버튼만 제거) 권장.
- **규모**: 중~대. `#sp-yt-sec` 64버튼 + 오버레이 3개(검수 hnn / 영상관리 vm / 그룹우선순위 gp) 이관 + 탭 5개 구성.

### ⚠️ 위 표의 정정 — 2026-08-28 소스 재실측

착수하면서 다시 재보니 위 8/27 표에 오류가 있었다. **위 표보다 이 절이 우선**:

| 항목 | 8/27 기록 | 2026-08-28 실측 |
|---|---|---|
| P1-1 이벤트 가드 | 🔶 부분 | ✅ **완료**. `index.html:9966`(pointerdown)·`10148`(click)이 `closest('[data-modal]')`, `9993`(mousemove)이 `elementsFromPoint` 목록에 `[data-modal]` 포함. `_anyModalOpen()` 헬퍼는 **도입 불필요** — 호버는 히트테스트 기반이 오히려 정확하다 |
| P1-2 동명이인→카드 | ❓ 미확인 | ✅ **완료**. `admin.js:1685 _dqGotoArtist`, 데스크톱 분기에 rAF 보완 오픈까지. 8/27에 못 찾은 건 index.html만 봤거나 null byte로 grep이 binary 스킵한 탓 |
| `#sp-yt-sec` 버튼 수 | 64개 | **33개** (66은 input·hint·prog 포함 `sp-*` id 전체 수) |
| 캡션·그룹핑 = "B안" | "반나절에 즉효, 먼저 권장" | **이미 다 돼 있음** — `sp-btn-hint` 32/33, `sp-subsec-lbl` 8개 섹션. B안은 소진, 남은 건 A뿐 |
| P2-6 편집 모달 | ❓ 부분 | ❌ **거의 미착수**. `#vid-tag-panel` 폭 `min(320px,…)` 그대로, z82, 썸네일 미리보기·연속편집 없음 |
| 설정 패널 폭 | 252px (진단 전제) | 320px로 이미 넓어짐 — "좁아서 답답함"은 상당 부분 해소됨 |

**진단 수정**: 남은 통증의 원인은 폭이 아니라 **z-index**다. 카드 `#gc`61/`side-panel`62~63 vs 어드민 81~82라 어드민을 열면 무조건 카드가 가려진다. 결정적 증거 — 대시보드 카드 클릭 핸들러가 `_admHomeClose()`를 먼저 부른 뒤 vm을 연다(`admin.js:6025~6031`). 오버레이끼리 공존을 못 해 서로를 닫는 구조. 즉 **가치의 대부분은 "z를 카드 밑으로 + 좌측 도킹"에서 나오고, "탭 5개 통합"은 그 위에 얹는 정리 작업**이다.

---

## 2026-08-28 실행 — 순서를 뒤집어 0+1단계만 (완료)

원안의 "내용 이관 후 오버레이 제거"가 아니라 **"먼저 도킹만, 통합은 나중"**으로 순서를 바꿨다. 기능 이관이 0이면 누락 사고가 원천적으로 없다.

- **0단계 (완료)** — `data-modal` 가드 재검증. 결론: **코드 수정 불필요**. 도킹 루트를 `pointer-events:none`으로 투과시키면 "패널 위 = 모달 안(closest·elementsFromPoint 둘 다 패널을 통해 루트를 찾음) / 우주 위 = 모달 밖"이 자동 성립하고, `e.target===e.currentTarget` 백드롭 닫기 핸들러는 이벤트가 루트에 안 닿아 저절로 무력화된다(도킹이 우주 클릭에 안 닫혀야 하므로 의도한 결과). `closePanels()`가 어드민 오버레이를 안 건드리는 것도 확인.
- **1단계 (완료)** — `adm-home`/`hnn`/`vm`/`gp` 4개를 데스크톱에서 **z58 좌측 도킹**(폭 `min(520px,44vw)`, 전체 높이). DOM·핸들러·탭 구조 무변경, 전부 CSS.
  - ⚠️ **좌측 슬롯 충돌** — 탐험 패널·저널 사이드바가 둘 다 `left:0`·300px·**z66**을 쓰고 탐험 패널은 세션마다 자동으로 열린다. 그냥 두면 도킹 패널 왼쪽 300px가 가려진다(첫 스크린샷에서 실제로 잘려 나옴). `body:has(…도킹.open)`으로 접고 `.open`은 유지 → 도킹을 닫으면 원래 transition으로 복귀. `#zoom-ctrl`(z50)도 도킹 폭만큼 이동.
  - 도킹은 홈과 좌측 슬롯 하나를 공유하므로 `_admHomeClose()`는 그대로 두고, 대신 vm/hnn/gp 헤더에 **🏠 홈 복귀 버튼**(`.adm-back-home`)을 넣어 왕복을 없앴다. 모바일 숨김.
  - 검증: `tests/admin-dock.test.js`(신설) 12항목 통과 — 기하·가드 3종·이벤트 주입·**도킹과 진짜 카드 동시 표시**·모바일 회귀.

### 남은 것 (다음 세션)
- **2단계** — 4개 도킹 패널 → 콘솔 셸 하나(탭 5개) 통합. **도킹을 실제로 며칠 써본 뒤 필요성을 재판단할 것.** 안 합쳐도 충분할 가능성이 있고, 그렇다면 원안 P1-3~4는 여기서 종료해도 된다.
- **3단계** — 편집 모달(`#vid-tag-panel`) 320→560px + 썸네일 미리보기 + 우측 치우침 배치 + 연속 편집 토글(원안 P2-6). 도킹과 궁합이 좋아 이 순서가 자연스럽다.

---

### (참고·소진됨) 8/27에 적어둔 착수 전 결정 사항
1. **범위 선택** — (A) 콘솔 도킹 전면 재구성(중대작업, 여러 세션) vs **(B) 저비용부터**: "버튼 아래 설명 캡션 상시 노출(문구는 이미 `sp-btn-hint`에 다 있음) + 섹션 그룹핑 정리"만 먼저 하면 반나절에 즉효. → **B를 먼저, A는 이후 권장.**
2. **선행 검증** — A 착수 전, P1-1 이벤트 가드가 실제 완결인지 재현 테스트(편집 모달 열고 배경에서 우주 호버/클릭 통과 없는지). data-modal 30곳은 붙었으나 window 가드 3곳 연결이 관건.
3. **모바일** — 콘솔은 데스크톱 전용, 모바일은 기존 설정 패널 유지(계획대로. 콘솔 DOM은 하나, 모바일은 진입 버튼만 숨김).
