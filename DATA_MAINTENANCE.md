# DATA_MAINTENANCE.md — 데이터 정기 업데이트 방안

> K-POP UNIVERSE의 기본정보·디스코그래피를 어떻게 최신으로 유지할지 정리(2026-09-03).
> 핵심 원칙: **원본(groups.json·artists.json)만 사람이/툴이 고치고, 파생물(slim·disco/·tracks_index)은
> `build_slim_data.mjs`로 재생성**. 데이터 소스는 나무위키(기본정보, 동명이인 안전)와 멜론(디스코).

---

## 1. 데이터 종류별 성격
| 종류 | 변동 빈도 | 소스 | 채우는 법 |
|---|---|---|---|
| 멤버 생일·국적·나무위키 링크 | 낮음(신인 데뷔 시) | 나무위키 인포박스 | 감사 → 서브에이전트 namu 조회 |
| 그룹 팬덤·SNS 링크 | 낮음 | 나무위키 | 위와 동일 |
| **디스코그래피(신보)** | **높음(수시)** | 멜론 | melon 파이프라인 |
| 해체(disbanded) 표기 | 낮음 | 나무위키 | 수시 보강 |
| 영상 태깅 | 상시 | — | 별도 캠페인([[project_mistag_rootout_campaign]]) |

## 2. 도구
- `tools/data_completeness_audit.mjs` — **기본정보 완성도 감사(읽기 전용)**. 빈 필드(생일·국적·팬덤·SNS)와
  오래된 디스코를 뽑아 `~/Downloads/data_audit/gap_*.json` 3종 생성. 정기 실행의 출발점.
- `tools/melon_solo_audit.mjs` → `result.json`(그룹 melon aid 매핑) — 디스코 파이프라인 1단계.
- `tools/group_disco_audit.mjs` — 디스코 내부정합/나무위키 대조(읽기 전용).
- `tools/group_disco_fill.mjs [--apply] [--groups A,B]` — 나무위키에 있는데 json에 없는 정규/미니를
  멜론에서 커버·트랙까지 채움. 번호충돌 시 그 그룹 통째 스킵(안전).
- `tools/build_slim_data.mjs` — 원본 수정 후 **반드시** 실행(slim·disco/·tracks_index 재생성).

## 3. 정기 루틴(권장 주기)

### A. 기본정보 — 월 1회
```
node tools/data_completeness_audit.mjs          # gap_member_basic / gap_group_basic 생성
# → 각 gap을 서브에이전트에 주고 namu WebFetch로 값 회수(빈 필드만, 동명이인 안전)
# → 원본 수정 → node tools/build_slim_data.mjs → 커밋
```
2026-09-03 1차 완료: 생일 12·국적 12·팬덤 14 채움. 남은 미확보는 **나무위키 링크 자체가 잘못된 것**
(영파씨 COLD/OTB/XXL은 노래문서로 연결, 이니는 404) → 링크부터 고쳐야 함.

### B. 디스코그래피 — 컴백 시즌 후 또는 월 1회
```
node tools/melon_solo_audit.mjs                 # result.json (전 그룹 melon aid)
node tools/group_disco_fill.mjs                 # 드라이런 — 추가 제안 검토
node tools/group_disco_fill.mjs --apply         # 반영
node tools/build_slim_data.mjs                  # 파생물 재생성
```
⚠️ 멜론은 한국 지역 기반이라 해외 CI(GitHub Actions)에서 막힐 수 있음 → **로컬/회사에서 실행**.

### C. 해체 표기 보강 — 수시
감사의 "신보 점검 후보" 상위는 대부분 **해체·비활동인데 `disbanded`가 안 찍힌 그룹**(H.O.T.·SS501·
미쓰에이·에프엑스 등)이다. 이 플래그가 없으면 (a)감사 노이즈 (b)활동 로직 오판이 생긴다. 나무위키
"해체" 여부로 일괄 보강하면 감사 정확도가 오른다.

## 4. 자동화 판단
- **완전 자동(CI cron)**: 멜론·나무위키 스크래핑이 지역/봇차단에 취약해 신뢰도 낮음 → 비권장.
- **권장 = 반자동**: 감사(자동, 안전) + 채우기(서브에이전트 조력, 사람이 커밋). 감사 툴이 갭을 JSON으로
  뱉으므로, 채우기는 "gap 파일 → namu 조회 → 빈 필드만 반영"이 반복 가능한 레시피가 된다.
- 원한다면 감사만 월 1회 자동 실행(cron)해서 갭 리포트를 남기고, 채우기는 그때그때 지시.

## 5. 발견된 데이터 이슈 (2026-09-04 밤 작업 중, 사람 확인 필요)
구조 변경이라 자동 수정하지 않고 남긴다 — 멤버 삭제·추가는 영상 태그·별 참조에 영향.

### ~~⚠️ 영파씨(YOUNG POSSE) 로스터 오류 [우선]~~ → **json 정정 완료(2026-09-04) · DB 태그 정리는 남음**
- 원문: 도은·정선혜·한지은(정상) + **COLD·OTB·XXL(a1155~a1157)** 이 멤버로 등록돼 있었고, **실제 멤버
  위연정·지아나가 누락**. 나무위키 확인 결과 COLD·OTB·XXL은 곡/앨범 제목(COLD=스페셜앨범, XXL=EP 2집).
- 처리: a1155~1157 제거 + 위연정(a1732, 2004.09.01)·지아나(a1733, 2006.02.02) 등록 → 5인조 완성.
  id는 재사용 안 함. 참조처는 artists.json·slim 두 곳뿐이었음(connections/SEO엔 없음).
- **남은 것 — 어드민 세션에서 실행**: DB에 옛 태그 90건(COLD 35·XXL 54·OTB 1, 전부 자동태깅).
  ① 영상관리 > 청소 > **"🧽 로스터에 없는 이름 태그 정리"** 에 `COLD, OTB, XXL` 입력
  ② **"자동 태깅"** 재실행 → 위연정·지아나 태그가 붙음(제목에 이름 있는 영상만 25건+, 지금 0건)
  ③ 그 90건 중 **20건은 group_ko까지 오배정**(HONNE `Warm On A Cold Night`, 빅오션 `콜드 문`,
     세븐어스 `Cold And Shiny`, 이기찬 `감기(Cold)` 등) → members를 비우면 무관 영상으로 남으니
     **"② 오태깅 그룹 재배정"** 또는 보류 처리 필요.

### 잘못된 나무위키 링크 → **전수 점검·22건 회수 완료(2026-09-04)**
- ~~영파씨 COLD/OTB/XXL~~ → 항목 자체가 삭제됨(위 참고).
- ⚠️ 위연정·지아나는 `(YOUNG POSSE)` 접미사 문서가 **없다** — 단독 문서 `/w/위연정`·`/w/지아나`.
  다른 멤버(도은·정선혜·한지은)는 접미사가 붙는다. 링크 만들 때 규칙으로 찍지 말고 실제 응답을 볼 것.
- 1,724명 전수 점검(`tools/namu_link_sweep.mjs`) → 정상 1,591 / 회수 대상 41. 그중 **22건 반영**.
  대부분이 같은 패턴이다 — 나무위키가 `이름(그룹)` 문서를 **`이름` 단독으로 옮겼다**(문별·지코·준호·
  후이·우석·피오·유권·원필·도운·페이·광수·주이·혜빈·가현·한동·휘연·이진·Kei·강우진).
  반대 방향도 있다: `티파니(가수)`→`티파니(소녀시대)`, `선(cignature)`→`선(시그니처)`, `예아(cignature)`→`예아`.
- **남은 19건**(자동으로는 안 건드림, 사람이 확인할 것)
  - 후보 전부 404(5): 토니안(H.O.T.) · 채솔(시그니처) · 뉴선(솔로) · 송경일(히스토리) · 나도균(히스토리)
  - 단독 문서는 있는데 그룹명 언급이 없음(8): 강타(H.O.T.) · 은(샤크라) · 민현(뉴이스트) · 이영(솔로) ·
    지아(미쓰에이) · 기범(유키스) · 다솜(씨스타) · 아영(솔로) — 동명이인일 수 있어 자동 채택 금지
  - 단독 문서가 동음이의 문서(6): 이니(샤크라) · 주연(솔로) · 나나(솔로) · 베카(솔로) · 민(미쓰에이) · 소진(솔로)
- 이하린/류/휘찬/리오/크리스(솔로, 전부 2008 해체 그룹 **배틀** 출신) → 나무위키 문서 자체가 없음. 정상.

#### ⚠️ 이 점검을 다시 돌릴 때 반드시 알아야 할 것
1. **캐시가 판정을 얼린다.** `~/Downloads/melon_solo_audit/namu_sweep/`에 옛 응답이 남아 있으면 그때의
   실패가 오늘의 "없는 문서"로 그대로 재보고된다. 실제로 이번 실행의 "회수 92건"은 **전부 현재 URL과
   회수 URL이 같았다** — 8/24에 실패해 캐시된 것들이 오늘은 멀쩡한 것뿐이었다. 캐시를 비우고 볼 것.
2. **스로틀링.** 1,700건을 연속으로 때리면 뒤로 갈수록 멀쩡한 문서가 404로 나온다(툴 주석에 이미 있음).
   재확인은 요청 간 1.5~2초를 두고 돌릴 것. `--recheck`는 딜레이가 없어서 이번에 8초/건까지 느려졌다.
3. **`curl -L` 없이 상태코드만 보면 안 된다.** 나무위키는 개명·이관 문서를 302로 넘긴다(`/w/재한`).
   L 없이 재면 정상 링크가 통째로 "깨짐"으로 나온다(이번에 한 번 그렇게 오판했다).
4. **한글 URL은 반드시 인코딩.** 인코딩 없이 curl에 넘기면 방탄소년단·트와이스까지 404가 되고,
   공백이 든 `KISS OF LIFE` 같은 건 아예 요청이 안 나간다.

## 6. 아직 기능에 안 붙은 데이터 — **지우지 말 것**

앱 코드가 참조하지 않아서 "미참조 자산"으로 보이지만, **조사 비용이 큰 원본**이라 지우면 다시 만드는 게
훨씬 비싸다(2026-09-04, 실제로 "죽은 파일이니 삭제" 후보로 올라왔다가 내용을 확인하고 되돌린 목록).
여기 적어두는 이유는 파일 존재 자체가 아니라 **"이게 살아있는 건가"를 매번 다시 판단하는 비용**이
진짜 문제이기 때문이다. 붙이거나 버리기로 결정하면 이 절에서 지운다.

| 파일 | 내용 | 왜 비싼가 | 붙일 곳(예정) |
|---|---|---|---|
| `mc_history.json` | 6대 음악방송 역대 MC 계보 **255건** (`{show, memberKo, groupKo, start, end}`) | 방송별 나무위키 전수조사 + 실제 멤버명 매칭. 동명이인은 그룹 데뷔·해체연도로 자동 판별하고, 그래도 안 풀리는 건 사람이 확정(소희 2007 원더걸스/2024 라이즈, 김규빈, 나나(WOOAH), 쇼챔 4대 도영·재현, 뮤뱅 14대 지성=배우라 제외) | ①MC 진행 클립 vs 무대 클립 구분(태깅) ②멤버 카드 MC 경력 표기 |
| `shows.json` | 오디션·서바이벌 프로그램 36개 · 확정 참가자 274명 · 검수후보 214건 | 파일 안 `_meta`에 생성법·주의사항이 전부 적혀 있음. 한 번 매처에 붙였다가 **실측 후 되돌린 이력**까지 `_meta.notWiredYet`에 남아 있다(참가자 6명을 지우는 순손해였음) | `candidates` 214건 검수가 선행돼야 함. `LEARNING_LOOP.md` P5 |

⚠️ 이 둘은 **드라마/영화 Show 탭과 무관**하다(`shows.json`은 오디션 프로그램 데이터다). 이름 때문에
혼동해서 "Show 탭이 안 쓰니 죽은 파일"로 오해하기 쉽다.

한편 `_a_audit_full.json`은 성격이 다르다 — A등급 111개 목록을 로컬에 떠둔 **일회성 스냅샷**이고
(당시 `group_priority` 테이블이 admin 전용이라 대신 썼다) 지금은 지워도 재생성이 싸다.

### disbanded 플래그 누락 (감사 노이즈 + 활동로직 영향)
- 해체·비활동인데 표기 없는 그룹 다수: H.O.T.·SS501·미쓰에이·에프엑스·브라운아이드걸스·크레용팝·
  슈퍼노바(2010 이후 무활동) 등. `data_completeness_audit.mjs`의 "신보 점검 후보" 상위가 대부분 이것.
- 나무위키 해체일로 일괄 보강하면 감사 정확도↑. (일부는 휴지기 vs 해체 판단 필요 → 사람 확인)
