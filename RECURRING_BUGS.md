# 재발 버그 추적

한 번 고쳤는데 같은 계열 증상으로 다시 터진 버그만 여기 기록. 처음 발견된 버그는 CHANGELOG.md에만 남기고,
같은 원인/증상이 두 번째로 재발했을 때부터 이 파일에 옮겨서 이력을 쌓는다.



## 모바일: 하단 탭바와 카드 사이에 틈이 생겨 배경 우주가 비쳐 보임

* 최초 발견 2026-08-14: 탭바가 다시 나타날 때 카드가 그 속도를 못 따라가 둘 사이가 벌어졌음. 원인은
  **트랜지션 속도/곡선 불일치**(탭바 .18s vs 시트 .32s) — 탭바 트랜지션을 시트와 같은
  `.32s cubic-bezier(0.32,0.72,0,1)`로 맞춰서 해결(kpop_universe.css:87 주석에 남아있음).
* 재발 2026-08-25: 이번엔 애니메이션이 아니라 **정지 상태에서도 항상 8px** 떠 있었음. 원인은
  `updateSheetBottom()`이 `--sheet-bottom`을 `탭바 높이 + 8px`로 계산한 것(상수 오차, git 최초
  업로드부터 존재). `#mob-sr`(검색 결과)이 `calc(var(--sheet-bottom) - 8px)`로 그 8을 되빼며
  혼자 붙어 있던 게 방증이었음 — 두 곳 다 정리.
* 패턴 노트: 이 계열은 **"움직이는 동안 벌어지는가(트랜지션)" 와 "멈춰 있어도 벌어지는가(상수)"가
  전혀 다른 원인**이다. 제보를 받으면 먼저 카드를 연 채로 가만히 두고 실측부터 할 것 —
  `tabbar.getBoundingClientRect().top - sheet.getBoundingClientRect().bottom`이 0이 아니면 상수 문제,
  0인데 스크롤 중에만 벌어지면 트랜지션 문제. 회귀 테스트는 tests/interaction.test.js [버그E].
* 관련 코드: `updateSheetBottom`/`_applySheetMetrics`(index.html), `#mob-sheet`·`#mob-sr`·`#tabbar`
  (kpop_universe.css)
* 상태: 해결(2026-08-25), 실기기 확인 대기



## 모바일: 카드 스크롤에 따른 탭바 숨김/복귀가 제대로 안 먹음

* 최초 발견 2026-08-24: iOS 고무줄(바운스) 구간에서 scrollTop이 음수/최대초과로 튀어 방향 계산이
  반전 → 탭바가 숨김 고착. scrollTop 유효범위 클램프 + "최상단<40px면 무조건 표시" 안전망 + 카드
  열 때 `_lastScroll=0` 리셋으로 조치(CHANGELOG 2026-08-24 참고).
* 재발 2026-08-25(사용자 제보 — "카드 여기저기 열었다 닫았다 하면 제대로 구현이 안 된다"): 방향
  계산이 아니라 **시트 높이를 인라인 스타일로 박던 구조** 자체가 원인이었음. `_extendSheets()`가
  시트 3개에 `min/max-height`를 "호출 시점의 bs-full 여부" 기준으로 써넣어서, 그때 숨겨져 있던
  `mob-card-stack`이 `60vh+탭바` 값에 갇히고 나중에 bs-full로 열려도 인라인이 CSS를 이겼음(실측
  572여야 할 inner가 442). 게다가 시트 높이가 바뀌면 clientHeight가 커지며 브라우저가 scrollTop을
  강제로 당기는데(클램프) 그 감소분이 "위로 스크롤"로 읽혀 탭바가 숨자마자 도로 나왔고, 밑에 깔린
  시트까지 같은 이벤트를 듣고 탭바를 두고 싸웠음.
* 조치(2026-08-25): 인라인 스타일 전폐 → CSS 변수 2개(`--sheet-bottom`/`--sheet-grow`)만 갈아끼움
  (지금 열려있든 나중에 열리든 CSS가 계산하므로 stale이 구조적으로 안 생김) + 방향 판정을 누적
  델타(내림 28px/올림 14px)로 + 클램프된 양만큼 델타에서 정확히 상쇄 + `_activeSheetScroller()`로
  맨 위 카드만 탭바를 조종.
* 패턴 노트: **"스크롤 이벤트로 레이아웃을 바꾸는" 로직은 자기가 만든 리플로우를 다시 입력으로
  읽는다.** 높이/위치를 바꿨으면 그로 인한 scrollTop 변화는 반드시 입력에서 빼거나 기준선을 다시
  잡을 것. 그리고 여러 컨테이너가 같은 전역 UI(탭바)를 조종하면 어느 하나만 권한을 갖게 할 것.
  시간창(setTimeout/타임스탬프)으로 이벤트를 통째로 무시하는 건 진짜 사용자 입력까지 삼키므로
  최후수단 — 실제로 360ms 창으로 짰다가 "빨리 올리면 탭바가 안 돌아옴"이 재현돼 폐기함.
  회귀 테스트는 tests/interaction.test.js [버그C]·[버그F].
* 관련 코드: `_wireSheetTabbarHide`/`_extendSheets`/`_retractSheets`/`_activeSheetScroller`
  /`_rebaseSheetScroll`(index.html), `#mob-sheet` 주석(kpop_universe.css)
* 상태: 해결(2026-08-25), 실기기 확인 대기



## 연결 카드: 실제 콜라보 영상 없는 그룹명 노출 ("유령 칩")

* 최초 발견: 2026-08-04(코드 주석에 남아있음, index.html:4668) — 같은 영상이 여러 조회 경로에서 부분
  정보로 따로 발견되어 with가 유실되던 케이스. 당시 `_dedupSongs`에서 url 기준 병합으로 수정.
* 재발 2026-08-20: 투어스 도훈 연결카드에 "워너원" 그룹 칩이 뜨는데 클릭하면 "해당 조합의 영상이
  없어요". 원인은 이번엔 dedup이 아니라 **태깅 자체의 이름/그룹 불일치**였음 — 영상 `sqNkzxKBx04`의
  `with_members`가 "황민현(워너원)"으로 저장돼 있는데, 로스터엔 이 사람이 활동명 "민현" + 현 소속
  "뉴이스트"로만 존재해서 `_findArtistByConnName`이 못 찾음. 그룹 칩 자체는(문자열만 있으면) 뜨지만,
  클릭 시 `roster.some(hasMember)`가 진짜 워너원 로스터 10명 중 아무도 못 찾아 전부 걸러짐.
* 조치(2026-08-20): `_apiVidToSong`의 with_members 처리부(index.html:4712)에서 로스터 매칭 실패하는
  이름은 애초에 withList에 안 넣도록 방어 코드 추가 — 실명/활동명 불일치, 해체 프로젝트그룹명(워너원
  등) vs 현 소속 표기 차이 등 어떤 조합으로든 재발해도 유령 칩 자체가 안 생김. DB의 "황민현(워너원)"
  원본 값 자체는 아직 안 고침(고치면 "민현(뉴이스트)"으로 정상 연결 가능 — 별도 SQL 필요).
* 패턴 노트: 이 클래스 버그는 "칩(이름 후보)이 붙는 경로"와 "그 칩 클릭 시 영상 필터링 경로"가
  서로 다른 매칭 로직을 쓸 때마다 재발 가능 — 태깅 파이프라인이 문자열을 만들 때 로스터 존재 여부를
  검증 안 하면 언제든 다시 터짐. 새 유령 칩 발견 시 먼저 해당 그룹 칩의 with_members/with_groups
  원본 문자열이 `_findArtistByConnName`으로 실제 resolve되는지부터 확인할 것.
* 관련 코드: `_buildConnContent`/`_getCollabMembers`/`_getCollabSongs`/`_apiVidToSong`/
  `_findArtistByConnName`(index.html)
* 상태: 코드 방어 완료, DB 데이터 정정은 보류



## 그룹 이름 라벨이 행성 위로 "붕 떠" 보임

* 여러 번 재발: 2026-08-20(1.2→0.5→0.2), 2026-08-22(radius+0.2→radius*1.05) — 매번 "상수/비율만
  조정"으로 접근했으나 안 잡혔음. 2026-08-22 사용자 재제보로 진짜 원인 규명.
* 진짜 원인: 그룹 라벨(CSS2DObject)이 `bubble` mesh의 **자식**인데 `bubble.scale=radius`(공유 단위구체
  `_sharedBubbleGeo`(반지름 1)를 실제 반지름으로 확대, index.html:1763·1932)라, 라벨의 **로컬 위치가 이미
  radius배로 월드 변환**됨. 그런데 오프셋을 `radius*1.05`(로컬)로 줘서 월드에선 `radius²×1.05` → 반지름
  큰 행성일수록 표면 위 오프셋이 radius의 수 배까지 커짐(radius 4.5 → ~3.7배). "상수를 줄여도" 지배항이
  radius²라 소용없었던 이유.
* 조치(2026-08-22): ①오프셋을 단위구체 기준 배수로 — `radius*1.05`→`1.05`(index.html:2726). ②라벨 텍스트
  앵커를 위로 — 인라인 `top:5px`→`bottom:5px`(index.html:1964). 카드 열림/닫힘 두 상태 헤드리스 렌더로 검증.
* 패턴 노트: **"자식으로 붙인 오브젝트에 부모 scale이 곱해지는" 함정.** 라벨/보조 오브젝트 위치가 이상하게
  크게/작게 나오면, 먼저 그 부모 mesh의 `.scale`부터 확인할 것. 위치를 "world 절대값"으로 주고 싶으면
  자식으로 붙이지 말고 scene 직속에 두거나, 부모 scale의 역수를 곱해 보정.
* 관련 코드: 라벨 생성/부모 부착(index.html:1930·1987), 매 프레임 재배치(index.html:2726),
  라벨 CSS 인라인(index.html:1962~1964)


## 행성 선택 시 배경 별이 "세포처럼 까맣게" 보임 (검은 점)

* 최초 발견 2026-08-23: 배경 잔별이 흰 사각형(■)으로 찍히고, 행성 앞에 온 별이 뒤 행성의 가산 글로우를
  가려 "검은 점"으로 보인다고 판단. 배경 별 재질에 `map`(soft 원형 텍스처) + `depthWrite:false`를 줘서
  조치(index.html:1209~ 주석). 사각형 아티팩트는 실제로 이걸로 해결됐음.
* 재발 2026-08-25(사용자 제보 "다른 행성 선택 시 배경 별이 세포처럼 까맣게"): 위 조치 이후에도 증상 지속.
  당시 이미 **모든 가시 재질이 `depthWrite:false`** 라 옛 depth-occlusion 이론으론 설명이 안 된다는 점까지는
  파악했으나 원인 미확정 상태로 넘어감.
* 진짜 원인(2026-08-26 확정): **배경 별이 아니라 멤버 별이었고, depth가 아니라 블렌딩 문제.**
  멤버 별 재질(`makePointsMat`, index.html:2328)이 `AdditiveBlending`인데, 별을 흐리게 하는 코드
  (`animate` 안 `ARTISTS.forEach`, index.html:2901~)는 **알파가 아니라 정점 색을 검정 쪽으로 곱해서** dim한다
  (`attr.array[i]=(…)*fa`). 가산 블렌딩의 알파는 `dstAlpha=ONE`으로 누적돼 점 중심이 항상 알파 255가 되므로,
  **색만 0에 수렴하고 알파는 꽉 찬 "불투명 검정 원"**이 되어 투명 캔버스 뒤의 CSS 배경(navy)을 가려버린다.
  행성을 선택하면 다른 그룹 별의 `_focusAlpha`가 떨어져 이게 한꺼번에 드러난 것.
* 조치(2026-08-26): 색은 기존 가산 그대로(`SrcAlpha`,`One`) 두고 **알파만 쓰지 않게**
  (`blendSrcAlpha:Zero`, `blendDstAlpha:One`) `CustomBlending`으로 분리(index.html:2328~). 발광 레이어가
  배경을 가리지 않으므로 어두운 별은 검게 덮는 대신 그냥 안 보이게 된다.
* 패턴 노트: **"투명 캔버스 위의 가산 블렌딩 레이어는 색과 알파가 따로 논다."** dim/페이드를 색에만 걸면
  알파가 남아 배경을 가리는 검은 자국이 된다. 가산 발광 레이어에서 무언가를 흐리게 할 땐 반드시
  **알파도 같이 낮추거나, 아예 알파를 안 쓰게(`blendSrcAlpha:Zero`) 만들 것.**
  진단 팁: 검은 점의 **캔버스 RGBA를 `gl.readPixels`로 직접 읽어라.** `[5,4,6,255]`처럼 알파가 꽉 찬 검정이면
  블렌딩 문제이고, `[0,0,0,0]`이면 아무것도 안 그려진 것(=지오메트리/실루엣 경계)이라 원인이 전혀 다르다.
  ⚠️ 진단 함정 두 가지: ①`performance.now()`를 얼려서 장면을 정지시키면 **렌더 스로틀
  (`_curRenderInterval`, index.html:2721)이 영원히 안 열려 렌더 자체가 멈춘다** — 토글 A/B가 전부 옛 프레임을
  찍게 되므로 `window._curRenderInterval=()=>-1`로 같이 무력화할 것. ②우주 배치가 매 로드 `Math.random()`이라
  **세션마다 하늘이 다르다** — 증상이 안 나오면 재현 실패가 아니라 배치 문제일 수 있으니 재로드 반복할 것.
* 관련 코드: 별 재질(index.html:2328~), 별 dim(index.html:2901~), 배경 잔별(index.html:1209~, 이 건과 무관)
