# 성능 · 발열 관리 (PERFORMANCE.md)

3D 우주(Three.js) + 유튜브 영상 재생을 한 화면에서 돌리는 앱이라 발열/배터리에 민감하다. 대응 로직이 코드 여러 곳에 흩어져 있어 여기에 한 곳으로 모은다. **발열 관련 코드를 고치거나 새 부하(애니메이션·blur·고화질 등)를 추가하기 전에 이 문서를 먼저 볼 것.** 발열은 **실기기(아이폰)에서만** 재현된다 — 헤드리스/데스크톱으론 확인 불가.

---

## ✅ 발열 대응 현황 (이미 구현됨)

### 3D 렌더 (평상시)
- **드로우콜 다이어트** (2026-08-24, P0-2): 링 그리드 45→1, 전체 2033→370. CHANGELOG 명시 — "발열 지표는 fps가 아니라 **드로우콜**".
- **렌더 fps 상한** (`index.html` `_curRenderInterval` ~3015): 활동 30fps / 유휴(2.5초 무조작) 20fps. 무거운 계산(연결선·행성 268 유니폼·별 포인트)을 매 rAF가 아니라 렌더 타이밍에만 묶음(~3056).
- **pixelRatio 상한** (`index.html` ~1359): `min(devicePixelRatio, 2)`.
- **라벨 LOD** (`index.html` ~3281): 원거리 라벨 상한 데스크톱 18 / 모바일 10.

### 절전 모드 (약한 기기 / 저fps 자동)
- **판정** (`_psDeviceLooksWeak` ~7784): deviceMemory≤4 또는 코어≤4 → 약한 기기. + FPS 실측(로드 4.5초 후 4초 샘플, `avg<45 || min<24` → 자동 강등, ~7102).
- **모드**: `_powerSaveMode` = auto / on / off (localStorage). 설정 패널 토글.
- **강등 효과** (`_applyPowerSave` ~7793): pixelRatio 2→**1.25**(픽셀 약 61%↓) · fps 활동20/유휴10 · 라벨 상한 데스크톱10/모바일6.

### 영상 재생 중 (라이트박스)
- **3D 완전 정지** (`index.html` animate): 백그라운드 탭 · 라이트박스 open · 모바일 시트 open 이면 `return`.
- **캔버스를 합성에서 제외 + rAF 저주파 전환** (2026-09-22): 아래 "영상 재생 중 발열" 절 참조.
  ⚠️ 렌더 정지만으로는 부족하다는 게 이때 밝혀졌다 — `renderer.render` 0회는 GPU 부담 0의 근거가 아니다.

### 모션
- **prefers-reduced-motion** (2026-08-26, P0-3): 장식 애니 제거, 비행 0ms.

---

## 🔥 영상 재생 중 발열 — 2026-09-22 조사 (해결 3건)

재제보: "가만히 영상 보고 있는데 폰이 뜨거워서 볼 수가 없다". 2026-08-28에 "3D는 라이트박스 중
멈추므로 원인 아님"이라고 결론냈는데, **"렌더"만 맞고 "합성"이 빠진 진단**이었다.

### ⚠️ 이 문서의 이전 진단을 정정한다
"3D 완전 정지 → 영상 중 3D 부담 없음(확인됨)"은 **`renderer.render`가 안 불린다**는 뜻일 뿐이다.
그리는 걸 멈춰도 **캔버스 레이어는 화면에 그대로 남아 컴포지터가 매 프레임 같이 얹는다.** 영상
재생 중엔 화면이 초당 60번 갱신되므로, 내용이 하나도 안 바뀌는 전체화면 캔버스(폰 @3x면 300만
픽셀)를 초당 60번 합성하게 된다. **`renderer.render` 호출수 0은 "GPU를 안 쓴다"의 근거가 아니다.**

### 실측 (헤드리스 CDP, 390×844 모바일 에뮬, 라이트박스 open 6초)

| 지표 | 수정 전 | 수정 후 |
|---|---|---|
| `animate()` 웨이크업 | 97.3/s | **8.2/s** |
| RecalcStyle 횟수 | 60/s | **10.5/s** |
| TaskDuration (CPU) | 2.9% | **0.9%** |
| 캔버스 visibility | visible | **hidden** |
| 돌고 있던 CSS 애니메이션 | 2개 | **0개** |
| `renderer.render` | 0 | 0 (원래 정상) |

### 고친 것 3건

1. **캔버스를 합성에서 제외** (`index.html` `_setUniverseCanvasVisible`) — 라이트박스가 뒤를 완전히
   가릴 때 `visibility:hidden`. WebGL 컨텍스트는 살아있어 닫을 때 재초기화가 없다. 라이트박스
   브라우즈·쇼츠 모드 둘 다 **픽셀 단위로 동일**함을 스크린샷 비교로 확인했다.
   - ⚠️ 데스크톱 "카드 보며 재생"(`#side-panel.open`)은 우주가 실제로 보이므로 **제외**한다.
   - ⚠️ 다른 전체화면 오버레이(`_FULLSCREEN_OVERLAY_IDS`)에 같은 처리를 복사하지 말 것 — 그쪽은
     대부분 반투명+`backdrop-filter`라 **뒤 우주가 비쳐 보이는 게 디자인**이다. 숨기면 티가 난다.
2. **렌더 스킵 중엔 rAF 대신 저주파 타이머** (`index.html` `_renderSkipped`) — 안 그리는 프레임에서도
   초당 100회 가까이 JS를 깨우던 걸 8회로. rAF를 아예 끊지 않는 이유는 재시작을 한 곳이라도
   빠뜨리면 우주가 영구 정지하기 때문 — 이 방식은 조건이 풀리면 120ms 안에 스스로 복귀한다.
3. **스피너 무한 회전 정지** (`css` `#yt-lb-loading.show .yt-lb-spinner`) — 이전 진단의 "영향 미미"는
   **틀렸다**. `#yt-lb-loading`은 `opacity:0`으로 가려질 뿐 DOM에 남아, 보이지도 않는 스피너가 영상
   내내 돌며 **초당 60회 RecalcStyle**을 유발하고 있었다. 이게 브라우저를 계속 "애니메이션 중"
   상태로 붙들던 주범. 모바일 탭바 배지 펄스도 같은 이유로 라이트박스 중 정지시켰다.

### 남은 항목
- **`vq=hd720` 화질 강제** — `index.html:5665` `const _vq=isMob()?'hd720':'hd1080'`. 1080p 강제는
  이미 완화됐지만 여전히 기기가 대역폭·성능에 맞춰 자동 선택하지 못한다. 모바일은 `vq`를 아예
  빼서(auto) 유튜브에 맡기는 게 맞는지 실기기 체감으로 판단할 것.
- 위 수치는 전부 **헤드리스 계측**이다. 체감 발열이 실제로 줄었는지는 **실기기(아이폰)** 확인이 필요.

---

## 원칙
- **`catch(e){}`로 완전히 조용히 삼키지 말 것** — 최소 `console.warn`은 남긴다 (2026-08-28: Trend 선반이 그렇게 며칠 죽어 있었다).
- 새 `backdrop-filter: blur` / 무한 CSS 애니메이션 / 고화질 강제를 추가하기 전 발열 영향 검토.
- 발열 검증은 **실기기(아이폰)** — 헤드리스·데스크톱은 재현 안 됨.
