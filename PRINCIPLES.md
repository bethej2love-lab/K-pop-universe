# K-POP UNIVERSE 원칙



비전

케이팝 씬의 모든 연결을 탐험하는 공간. 우린 사실 다 연결되어있어!



## 포함 기준 (절대 조건)

* 음악방송 무대 출연 경험
* 한국어 가사 있어야 함
* 케이팝 씬과 실질적으로 연결되어 있는가?



## 철학

* 그룹이 행성이어도, 별은 각자 빛난다
* 발견 > 평가. 순위·평점은 **지양하되 금지는 아니다** — 아래 "순위를 다루는 기준" 참고. 탐험과 연결이 핵심
* 상생 — 아이돌, 팬, 서비스가 서로를 빛나게 하는 구조
* 좋아하는 것에 책임지는 것. 소비가 아니라 존중



## 운영

* 수동 큐레이션 우선, 자동화는 보조
* 어그로/혐오 콘텐츠나 오염 영상은 노출 안 함
* 데이터 정확성 우선
* **앨범 종류 표기: EP는 "미니"로 통일**(2026-08-23 사용자 지침) — 데이터(groups.json / artists.json)의 `type` 필드에서 "EP"→"미니", "EP N"→"미니 N집"으로 적을 것. 앱은 이미 EP를 미니 버킷으로 처리하고 화면 표기도 미니로 치환하지만, 원본 데이터도 미니로 통일해 "EP" 문구가 남지 않게 함. 멜론이 미니앨범을 [EP]로 등록하므로 스크래핑 시 반드시 변환.
* 유저 데이터 보호 / 최소 수집 원칙 (구글 로그인 정도만)
* **광고·판매 없음, 후원(기부)으로 운영**(2026-09-05 사용자 결정) — 상업적 수익화(광고·유료판매·B2B)는 안 하되, 고마움이 생기는 곳에 후원은 받는다. 후원은 그 자체가 원칙 위반이 아니라 지속 운영의 근거다.



\*하지 않을 것

\- 아이돌/그룹 간 우열 비교

\- 팬덤 경쟁 조장

\- 광고 또는 판매(상업적 수익화) — 단 후원/기부는 허용(위 운영 원칙 참고)

\- 검증되지 않은 사생활 정보



\* 만든이가 생각하는 아이돌의 본질은 '열심'

Idols can do everything

\- 노래, 춤, 연기, 예능, 소통, 운동, ... and everything

이 서비스는 그걸 좀 더 쉬운 방식으로 널리 보여주고자 함



\## For AI \& Developers



이 프로젝트는 케이팝 아이돌 연결 관계를 3D 우주 지도로 시각화한 웹 서비스.

단일 HTML 파일(index.html) + JSON 데이터(groups.json, artists.json, connections.json) + Supabase 백엔드 구조.



핵심 기능:

\- 그룹(행성)/멤버(별) 3D 탐험

\- 연결 카드 (두 멤버 사이 교집합 콘텐츠)

\- 탐험 탭 (오늘의 발견/기념일/연결)

\- 유저 즐겨찾기/컬렉션/좋아요



코드 작업 시 이 원칙들을 항상 참고할 것.

### 순위를 다루는 기준 (2026-09-02 확정)

원래 "순위와 평점 없음"이라고만 적혀 있었는데, 실제 제품은 이미 조회수 기반 선반(연도별 TOP·주간
직캠 TOP·레전드 100)이 Discover의 골격이고 카드 영상에도 인기순 정렬이 있다. 문서와 코드가
갈라진 채로 두면 다음에 무엇을 만들지 판단할 근거가 없어져서, **지양하되 금지는 아님**으로 고친다.
경계는 "누가 더 낫냐"를 말하는가다.

**써도 되는 것 — "무엇을 볼까"의 입구**
- 조회수·업로드일처럼 **외부에 이미 존재하는 사실**을 그대로 보여주는 정렬·목록
  (연도별 TOP, 주간 직캠 TOP, 카드 영상 인기순 등). 우리가 매기는 점수가 아니다.
- 목적이 **발견**일 것 — 안 보던 영상·안 보던 그룹으로 데려가는 도구로만 쓴다.

**쓰지 않는 것 — 우열을 만드는 장치**
- 우리가 계산한 종합 점수·별점·등급으로 **아티스트끼리 줄 세우기**
- 인기투표·팬덤 대결·좋아요 수 공개 경쟁처럼 **팬을 겨루게 만드는 것**
- "이 그룹이 저 그룹보다 낫다"를 읽히게 하는 표현(1위/최고/승자 같은 단정)

애매하면 이렇게 자문한다: **이 화면이 "뭐 볼까?"에 답하는가, "누가 최고야?"에 답하는가.**
앞이면 만들고, 뒤면 만들지 않는다.

---

경쟁 요소(대결·투표·우열 비교)는 추가하지 말 것.

⚠️ **검증(헤드리스/스모크)은 반드시 모바일 뷰포트(≤768px)로도 할 것 — 데스크톱만 보면 안 됨.** `isMob()`으로 단락되는 코드는 데스크톱 뷰포트에선 실행조차 안 돼서 크래시가 안 잡힘. 2026-08-22 모바일 전용 치명적 크래시 사고: 최상위 즉시실행 코드가 `isMob() && myProfile?.x`처럼 나중에 선언된 `let`(myProfile, 선언은 48줄 뒤)을 참조 → **TDZ(선언 전 접근) 에러**로 모바일 신규유저 전원이 로딩화면 영구정지했는데, 데스크톱 스모크·육안검증을 전부 통과했음(isMob 단락 때문). 규칙 둘: ①로드-크래시 확인은 **모바일 뷰포트 + 빈 localStorage(=신규유저)**로도 돌릴 것 ②**최상위 즉시실행 코드에서 파일 뒤쪽에 선언되는 let/const 변수를 참조하지 말 것**(필요하면 localStorage 등에서 직접 읽기).

## 🔍 만들기 전에 "이미 어떻게 하고 있나"부터 (2026-09-03, 중복 필드 신설 후 명문화)

새 필드·새 메커니즘·새 규약을 만들기 전에 **가장 가까운 기존 사례가 데이터에 어떻게 표현돼 있는지 먼저 열어볼 것.** 이 프로젝트는 1년 넘게 쌓였고 대부분의 문제엔 이미 답이 있다.

**실제 사고(2026-09-03)**: "그룹 탈퇴 후 솔로 활동 영상이 옛 그룹에 붙어 있다"를 풀려고 `soloSince`라는 새 필드를 설계·구현·테스트까지 했다. 사용자가 "겸임일 순 없는 거야?"라고 묻고 나서야 다시 봤더니 **이미 있었다**:

```json
승한: group: {ko:"솔로"},
      groups: [{ko:"솔로"}, {ko:"라이즈", active:false, left:"2024.10.13"}]
```

`group_ko='승한'` 영상이 **295건** 정상 동작 중이었고, 빛새온·김민서도 같은 모양이었다. 더 뼈아픈 건 **그 9명 명단을 이미 출력해서 보고도**(대화에 "승한이 이미 여기 있어"라고 쓰기까지 했다) `groups[]`를 한 겹 더 열어보지 않은 것.

**원인은 검색 방향이다.** *문제*("솔로 영상이 갈 곳이 없다")를 검색했고, *기존 해법*("이 프로젝트는 솔로를 어떻게 표현하나")을 검색하지 않았다.

- 새 필드가 떠오르면 먼저 묻는다: **"이걸 이미 하고 있는 사람/행이 데이터에 있나?"** 있으면 그 레코드를 통째로 출력해 본다.
- 비슷한 개념을 다루는 기존 필드가 있으면 **파생 가능한지** 따져본다. `soloSince`는 `groups[]`의 `left`에서 100% 파생 가능해 순수 중복이었다.
- 기존 구조가 이미 구분하고 있는 걸 새로 구분하려 하지 말 것 — `groups[]` 항목의 `left` 유무가 **겸임(없음)과 탈퇴(있음)를 이미 구분**하고 있었다(현재 소속 162개 vs 탈퇴 표시 28개).
- ⚠️ 이건 "역할 분담"이나 "가상 페르소나"로 안 풀린다. 병렬 액터가 늘면 서로 모르는 중복이 오히려 늘어난다(멀티세션 규칙 참고). 값싼 해법은 **만들기 전 한 번 열어보는 습관** 하나다.

## 🔀 같은 동작에 진입점이 둘 이상이면 **둘 다** 고쳤는지 확인 (2026-09-03, 하루에 세 번 물린 뒤 명문화)

이 코드베이스는 같은 기능이 **경로별로 따로 구현**돼 있는 자리가 많다. 한쪽만 고치면 테스트는 초록인데 사용자는 그대로 겪는다.

**실제로 겪은 짝들(2026-09-03 하루)**
| 기능 | 진입점 A | 진입점 B |
|---|---|---|
| 검색·설정 패널 열기 | 탭바 `tab-search`/`tab-settings` | 데스크톱 헤더 `dh-search-btn`/`dh-settings-btn` |
| 유닛 멤버 확장 | 자체 채널 `_unitMembersFromTitle` | 외부 채널 `_m2ParseTitle` 내부 |
| 해체일 계산 | `index.html`의 `_groupEndDate` | `admin.js`의 `_disbandCutoffDate`(감싸기) |
| 재배정 판정 도구 | `_ytSweepMistagReclassify` | `_ytSweepHeldMistagReclassify` |
| 이름 가드 | 한글 경로 | 영문 경로(`#HYERI`) |
| 촬영 포맷 표기 | `_FANCAM_SHOW_PATTERNS`(인식) | `_FANCAM_FILLER_RE`(제거) |

**가장 아팠던 사례**: "카드 열린 상태에서 검색창이 뒤로 뜬다"를 고치고 브라우저 테스트까지 8/8 초록으로 만들었는데, **사용자에겐 그대로였다.** 테스트가 `tab-search`를 클릭했고 데스크톱에서 실제로 누르는 건 `dh-search-btn`이었다 — 완전히 별개 리스너다.

- 무언가를 고치기 전에 **`grep`으로 그 동작의 진입점을 전부 세어볼 것.** 버튼이면 id를, 함수면 호출부를.
- 가능하면 **한 곳으로 합칠 것**(`_MISTAG`·`_groupEndDate`처럼). 합칠 수 없으면 양쪽 주석에 서로를 가리키는 표시를 남긴다.
- **테스트는 사용자가 실제로 쓰는 진입점을 쳐야 한다.** 어느 쪽을 치고 있는지 모르겠으면 전부 친다.
- ⚠️ 이건 아래 "검사 level" 항목과 세트다 — 초록불이 곧 "고쳐졌다"가 아니다. **수정을 되돌려 빨간불을 확인**하기 전까진 그 테스트가 무엇을 지키는지 모른다.

**⚠️ 뷰포트도 진입점이다.** `isMob()` 분기가 있는 자리는 모바일과 데스크톱이 **다른 코드 경로**를 탄다 — 테스트 뷰포트가 한쪽이면 반대쪽 버그를 영영 못 본다.
- 실제 사례(2026-09-03): 탐험 패널 "맨 위로" 버튼이 데스크톱에서만 패널 뒤로 깔렸다. 탐험 패널은 **데스크톱에서만** `_bringToFront`(131+)를 부르고 모바일은 정적 z-index 39라, 정적 66인 버튼이 모바일에선 위·데스크톱에선 뒤였다. 회귀 테스트(`interaction.test.js` [버그G])는 390px 모바일로 돌아 계속 초록이었다.
- PRINCIPLES가 "검증은 모바일로도"라고 적어둔 건 **모바일을 빠뜨리지 말라**는 뜻이지 모바일만 보라는 뜻이 아니다. `isMob()` 분기를 건드렸으면 **양쪽 뷰포트로** 확인할 것(데스크톱 전용 회귀는 `tests/desktop-search-front.test.js`에 모아둔다).

## ✅ 검사는 "고칠 게 명확하면" error로 (2026-09-03, 경고 방치 발견 후 명문화)

`tests/validate-data.js`의 `add(level, ...)`에서 level을 고르는 기준:

- **error** — 고칠 게 명확히 있는 것. 유령 참조(그 연결선은 그냥 안 그려짐), 형식 오류(경계를 못 그어 조용히 누락됨). 배포를 막아야 한다.
- **warn** — 정상 데이터에 대한 주의 환기. 동명이인 존재, 그룹명↔멤버명 충돌 가능성. 사람이 판단할 일이지 막을 일이 아니다.

**왜 명문화하나**: `connections.json` 유령 참조를 검사기가 **정확히 잡고 있었는데 warn(exit 0)이라 CI가 초록으로 통과**했고, 그래서 몇 주 방치됐다(`Yoona` 6건 + 개명 잔재 `제이미(박지민)` 1건). 같은 패턴이 2026-09-03 하루에만 네 번 나왔다 — 9/2부터 빨간 CI 방치 · 자동 숨김 2,346건 · 검수 큐 3,051건 · 이 경고 13건.

> **이 프로젝트의 반복 실패 모드는 "감지가 안 되는 것"이 아니라 "감지는 되는데 아무도 안 보는 것"이다.**

새 검사를 추가할 때마다 이 기준으로 level을 고르고, **기존 warn 중 "고칠 게 명확한 것"이 보이면 승격할 것.** 단 승격 전에 그 데이터를 먼저 고쳐야 한다(안 그러면 CI가 빨간 채로 남아 또 무시된다).

## 🚦 배포 전 필수 게이트 (3종 세트 — 통과 전 `git push` 금지, 2026-08-23)

`git push origin main` = 즉시 kpop-universe.kr 배포(되돌리기 비쌈). push 전에 아래 셋을 **매번** 통과시킬 것 — 하나라도 실패면 배포 보류. 크래시가 3주 반복된 구조적 원인이 "검증을 매번 사람이 기억에 의존해 골라 돌린 것"이라, **"통과 = 커밋 조건"으로 승격**한다.

1. **모바일 스모크 통과** — `node tests/smoke.test.js` (390px 뷰포트 + 빈 localStorage = 신규 모바일 유저 경로 포함). 로딩 무한정지·TDZ류 크래시를 데스크톱 검증이 놓치는 걸 여기서 잡음(바로 위 ⚠️ 참고). 배포 전 폰 시크릿/비공개 탭으로 `kpop-universe.kr` 한 번 여는 것도 같은 목적(신규 유저 = 캐시·서비스워커 없음).
2. **데이터 무결성 통과** — `node tests/validate-data.js` 에서 **오류(error) 0**. 경고(warn)는 검토 후 판단하되, 오류는 배포 차단.
3. **신규 시각효과 실기기 1회** — 별/글로우/셰이더 등 시각 요소를 새로 만졌으면 **아이폰 실기기에서 1회 육안 확인**. 헤드리스·데스크톱만으론 iOS 렌더 버그(대형 스프라이트 분할 등)를 못 잡음.

시각효과를 안 건드린 배포는 3번 생략 가능. **1·2는 예외 없음.**

**2026-09-02: 1·2번은 이제 CI에서도 자동으로 돈다**(`.github/workflows/data-and-tests.yml`) — push/PR마다 `validate-data` + 브라우저 없이 도는 회귀 28개. 사람이 기억해서 고르는 단계를 없앤 게 목적이므로, **로컬에서 안 돌렸어도 CI가 빨간불이면 배포된 것으로 치지 말 것**. 새 테스트를 추가하면 자동으로 포함된다(목록 하드코딩이 아니라 제외 방식).

### ⚠️ 회사망에서 Node 스크립트가 "fetch failed"로 죽을 때

회사망(사내 프록시가 TLS를 가로채는 환경)에서는 **Node의 fetch가 인증서 검증에 막혀** 외부 요청이 전부 실패한다. 브라우저는 멀쩡히 되는데 스크립트만 죽어서 "데이터가 없다"로 오해하기 쉽다. 앞에 환경변수를 붙이면 우회된다:

```
NODE_TLS_REJECT_UNAUTHORIZED=0 node tests/rls.test.js
NODE_TLS_REJECT_UNAUTHORIZED=0 node build_group_pages.js
```

실제로 겪은 사례: `rls.test.js`가 23개 테이블 전부 "네트워크 오류"로 실패(우회하니 23/23 통과), `build_group_pages.js`가 관계 페이지를 조용히 건너뛰고 "기존 190개를 sitemap에만 유지" 경고만 남김. **로컬 검증 결과가 이상하면 이걸 먼저 의심할 것** — 코드 문제가 아니라 망 문제인 경우가 많다. (curl은 `-k`로 같은 우회.)

## 👥 새 그룹/멤버 추가 절차 (2026-08-25, 이름 충돌 전수 감사 후 명문화)

오태깅 사고는 거의 전부 **이름이 겹쳐서** 났다(동명이인, 흔한 단어, 그룹명==멤버명, 유닛 토큰). 그동안은 사용자 제보를 받고 나서야 하나씩 대응했는데, 새 그룹을 넣는 시점에 미리 거르는 게 훨씬 싸다. 그룹/멤버를 추가했으면 **커밋 전에** 아래를 돌 것.

1. `node tests/validate-data.js` — "새 그룹 이름 충돌 위험" 경고가 뜨는지 본다(배포 게이트 2번에 이미 포함돼 있으니 따로 돌릴 필요는 없음). 내부적으로 `tools/name_collision_audit.mjs --new`를 호출한다.
2. 경고가 떴으면 유형별로 판단:
   - **흔한단어(한글)** → `_ATM_COMMON_KO_WORDS`(admin.js)에 추가. 인퍼런스에서 해시태그만 인정하게 된다.
   - **동명이인 3명 이상** → 로스터에 **빠진 동명이인이 없는지** 먼저 확인. 런타임 dedup은 "둘 다 등록돼 있어야" 작동해서, 한 명이 미등록이면 조용히 등록된 쪽으로 쏠린다(체리블렛 메이 사고).
   - **그룹명==멤버명** → 자동(`_atmNameIsGroup`). 단 충돌 상대가 `strictSync` 그룹이면 게이트가 오히려 손해라 예외 처리돼 있음.
   - **그룹명이 흔한 단어** → groups.json에 `strictSync:true`. 자체 채널 동기화로만 영상이 들어온다.
   - **유닛명 충돌** → `_UNIT_HASHTAG_ONLY_TOKENS`(shared.js).
   - **활동명이 이미 있는 유명 멤버와 똑같음** → `name.ko`를 **본명으로** 넣고 활동명은 `matchAliases`에. 활동명을 그대로 `name.ko`에 넣으면 동명이인 dedup이 걸려 **"그룹명 없이 그 이름만 있는 제목"의 역추론이 양쪽 다 버려진다** — 즉 새 사람을 추가하는 것만으로 기존 유명 멤버의 태깅이 죽는다. `matchAliases`는 그룹이 확정된 뒤 멤버 추출에만 단어경계로 쓰이므로 옛 소속 시절 영상은 그대로 잡힌다. 예: 다이아 "제니"→`이소율`+`matchAliases:["제니"]`(블랙핑크 제니 보호), 엘즈업 "나나"→`권나연`, 퍼플키스 "도시"→`장은성`.
   - **탈퇴 멤버** → 로스터에서 지우지 말고 `active:false` + `left:"YYYY.MM.DD"`로 남길 것(겸임이면 `groups[]`의 해당 항목에). 지우면 탈퇴 게이트(`_atmLeftBefore`)가 작동할 근거 자체가 없어진다. ⚠️ 다만 **그룹 해체까지 있었던 멤버는 탈퇴가 아니다** — 해체 그룹 멤버 전원이 active인 게 관례라 혼자만 `active:false`로 두면 그 사람만 "전 멤버"로 빠진다(파이브돌스 승희 사례).
3. 이미 동기화된 영상까지 오염됐는지는 `NODE_TLS_REJECT_UNAUTHORIZED=0 node tools/name_pollution_probe.mjs` 로 실측(읽기 전용). 정정은 어드민의 "콜라보 재검증"·"자체 멤버 재검증"·"동명이인 그룹 오배정 전체 스캔" 버튼으로.

⚠️ 판정 로직은 `tools/name_collision_audit.mjs` **한 곳에만** 둔다. 복붙해서 두 벌이 되면 admin.js의 실제 게이트와 어긋나는 순간 검사가 거짓 안심을 준다.

## ⭐ 별 입자(Points) 불변조건 (2026-08-23, 같은 아티팩트 3회 재발 후 명문화)

별/입자 계열 Points 재질은 아래를 **반드시** 지킬 것 — 색점(iOS)→clamp 롤백→흰 사각형/검은 점, 같은 뿌리의 사고가 세 번 반복됐다.
- **soft 텍스처(map) 필수** — `map` 없는 PointsMaterial은 THREE가 **흰 사각형**으로 찍는다(좌상단 흰 ■의 원인). 멤버 별=glowTex, 배경 잔별=_bgStarTex.
- **`depthWrite:false` 필수** — 기본값 true면 투명 점이 depth를 써서 **뒤 행성의 additive 글로우를 가려 "검은 점"**으로 보인다.
- **`gl_PointSize`에 clamp(멤버 별 46, halo 150)** — iOS Safari가 큰 point sprite를 쪼개 깜빡이는 버그 예방. 함부로 올리지 말 것([[feedback_realdevice_visual_check]]).
- 블렌딩: 멤버 별은 `AdditiveBlending`(빛을 더함), 배경 잔별은 NormalBlending 유지 가능(홈 밝기 불변 목적) — **단 map·depthWrite:false는 블렌딩과 무관하게 항상.**
- 새 별/입자 시스템 추가 시 이 4개를 체크리스트로 확인하고, 배포 전 게이트 3번(실기기)으로 확정할 것.

## 🔒 어드민 마크업은 반드시 `<template>` 안에 (2026-08-28, 읽기 모드 노출 제보 후 명문화)

**`display:none`은 숨기는 게 아니다.** 크롬 읽기 모드(Alt+Shift+R)·스크린리더·검색엔진은 렌더링 결과가 아니라 **원본 DOM을 순회**하므로 `display:none`을 무시하고 다 읽어간다. 실제로 로그아웃한 시크릿 창에서 단축키 하나로 어드민 버튼 라벨 33개 + 설명 캡션 32개(약 2,400자, 밴 정책·검수 기준 포함)가 그대로 읽혔다.

- 어드민 전용 마크업은 `<template class="adm-tpl" data-adm-mount="…">`로 감싼다. `<template>` 내용은 **문서 트리 밖의 inert fragment**라 DOM 순회로 도달할 수 없다. `_mountAdminMarkup()`이 관리자일 때만 복제해 넣는다(`_updateAdminUI` 안에서 호출, 멱등).
- **마운트 전에는 요소가 아예 없다.** 어드민 id를 index.html 본문 스크립트에서 참조하면 반드시 null 가드를 붙일 것. 테스트의 "준비 조건"에도 어드민 요소를 넣지 말 것(없는 걸 기다리다 엉뚱한 원인으로 오진한다).
- **검증할 때 `innerText`를 쓰지 말 것** — CSS를 존중해서 "안 새는 것처럼" 보인다(실제로 이 함정에 한 번 빠졌다). `<script>/<style>`을 뺀 **TreeWalker 텍스트**로 볼 것. 회귀 고정: `tests/admin-markup-exposure.test.js`.
- 새 어드민 패널/모달을 추가할 때마다 이 테스트의 블록 목록에 추가할 것.

(참고: 소스 보기(Ctrl+U)로는 여전히 보인다 — 그건 별개 층이고, 지우려면 마크업을 admin.js로 옮겨야 한다. 기능적 위험은 없다: admin.js는 관리자에게만 전송되고 DB는 RLS로 막혀 있다.)

## 📱 "사파리는 되는데 웹앱은 안 됨"의 정체 (2026-08-28, 탭바 사라짐 제보 후 명문화)

사파리(브라우저)와 홈화면 웹앱(standalone)은 **UI/UX가 같아야 한다.** 실제 차이는 사실상 하나뿐이다 — **웹앱은 브라우저 크롬이 없어 세로를 100px 가까이 더 쓴다.** 코드에 `display-mode:standalone` 분기는 상단 여백 하나밖에 없다. 그런데도 "웹앱에서만 이상하다"가 반복되는 건, 그 세로 차이가 **아래 두 가지 부수효과를 켰다 껐다 하기 때문**이다:

- **사파리엔 스크롤이 항상 있다.** 화면이 짧아 카드가 거의 항상 스크롤 가능 → `scroll` 이벤트가 늘 온다. 웹앱은 화면이 길어 짧은 카드가 **아예 안 스크롤되고**, 그러면 scroll 이벤트가 **한 번도 안 온다**.
- **사파리엔 `resize`가 항상 있다.** 주소창이 접혔다 펴지며 `window.resize`가 수시로 발화 → 재측정 코드가 계속 돈다. 웹앱은 크롬이 없어 resize가 거의 안 난다.

즉 **상태 복구를 이벤트(scroll/resize)의 부수효과에 맡긴 코드는 사파리에선 우연히 자가치유되고 웹앱에선 그대로 고착된다.** 실제 사례: `tab-hidden` 제거가 **닫기 경로에만** 있고 여는 경로엔 없었는데, 사파리는 "최상단 40px면 표시" 안전망이 scroll 이벤트로 곧바로 돌아 증상이 안 보였다. (같은 뿌리의 다른 사례: `showT`는 `closeConnCard()`를 부르는데 `showGC`는 안 불러서, 탭바 복구가 **멤버 카드에서만 우연히** 되고 그룹 카드에선 안 됐다.)

**규칙**
- UI 상태는 **진입 시점에 명시적으로 못박을 것**. "스크롤/리사이즈가 오면 정리되겠지"에 기대지 말 것.
- 어떤 상태를 켜는 코드를 쓸 때 **끄는 코드가 여는 경로·닫는 경로 양쪽에 다 있는지** 확인할 것.
- A 경로에서만 되고 B 경로에선 안 되면, B에 없는 게 아니라 **A가 다른 함수의 부수효과로 우연히 되고 있는 것**을 의심할 것.
- 검증은 **키 큰 뷰포트(예: 390x1100)** 로도 할 것 — `display-mode` 에뮬레이션으론 재현되지 않는다. 회귀 고정: `tests/tabbar-open.test.js`.

UI는 텍스트보다 **아이콘 우선** — 직관적이고 깔끔하게(2026-08-22 사용자 명시). 버튼/액션은 웬만하면 아이콘(SVG)으로, 라벨이 필요하면 `aria-label`/`title`로만 보조. 예: 차트 더보기 셰브런, 사진 뷰어의 저장/삭제/좌우이동 아이콘, 어드민 검수센터 케밥 등. 특히 **닫기(×) 등 보조 액션은 얇은 SVG 아이콘 + 낮은 대비로 은은하게** — 주요 콘텐츠보다 안 튀게(텍스트 ✕/× 글자는 굵고 밝게 보여서 지양).

어드민 계정 관련 (태깅 수동 우선, tags\_manual 필드 보호 등 개발 시 주의사항)

CSS 분리됐다는 것, 파일 구조 간단히



⚠️ 2026-08-18 구조 변경: index.html이 유일한 정식 앱 파일(source of truth)이 됨. **코드 작업은 항상 index.html에 할 것 — kpop\_universe.html은 절대 직접 수정하지 말 것.** 원래는 index.html이 kpop\_universe.html의 수동 복사본이라 매번 동기화해야 했는데(2026-08-12에 고친 버그수정이 index.html엔 안 들어가 있던 걸 2026-08-18에 발견), 반복적으로 깜빡할 위험이 있어 구조 자체를 바꿈: kpop\_universe.html은 이제 `location.replace('./')`로 index.html로 즉시 리다이렉트만 하는 얇은 파일(옛 PWA 바로가기/북마크 하위호환용). manifest.json의 start\_url도 `./`로 변경했고, sw.js 오프라인 캐시 폴백 대상도 `./`로 바꿨음 — 이 셋(kpop\_universe.html 내용/manifest start\_url/sw.js 폴백)은 서로 맞물려있으니 혹시 다시 손댈 일 있으면 셋 다 같이 확인할 것.

핵심 파일

index.html — 메인 HTML (전체 UI + JS 로직). **모든 코드 작업은 여기.**

kpop\_universe.html — index.html로 즉시 리다이렉트하는 얇은 파일. 절대 직접 수정하지 말 것(위 경고 참고)

kpop\_universe.css — 스타일시트 (HTML에서 분리됨)

admin.js — 어드민 패널 JS. index.html이 `admin.js?v=버전`으로 캐시버스팅해서 로드함(`_ADMIN_JS_VER` 상수). **이 버전 값은 2026-08-25부터 손으로 안 올린다** — push하면 GitHub Action(`bump-cache-version`)이 파일 해시로 `?v=`·`_ADMIN_JS_VER`·`sw.js`의 `CACHE_VERSION`을 한꺼번에 갱신한다. 로컬 확인은 `node tools/bump_cache_version.mjs --check`.

groups.json — 그룹 데이터 (246그룹)

artists.json — 멤버 데이터 (1500명+)

connections.json — 연결 데이터

manifest.json — PWA 설정 (홈 화면 추가 지원)

sw.js — 서비스 워커 (PWA 오프라인 캐싱)

og-image.png — SNS 공유 시 미리보기 이미지

sitemap.xml — 검색엔진 크롤링용 사이트맵

robots.txt — 검색엔진 크롤러 허용/차단 설정

PRINCIPLES.md — 이 프로젝트 원칙 문서

CHANGELOG.md — 세션/기기 간 작업 인수인계 로그. 새 세션 시작 시 최근 항목 먼저 읽고, 끝날 때 한 줄 추가할 것

.gitattributes — Git 파일 처리 설정. 건드릴 일 없음



\## 작업 세션 규칙 (여러 Claude Code 세션 동시 운영 시)



⚠️ 2026-08-22 방식 변경: 이제 git 저장소로 운영함(그 전엔 zip을 풀어 GitHub 웹에서 드래그 업로드하는 방식이었음). 정식 작업 클론은 `/Users/a1234/Documents/GitHub/kpopuniverse`(원격 `github.com/bethej2love-lab/kpopuniverse`, main 브랜치), GitHub Desktop으로 받아둠. **모든 작업·배포는 이 클론에서** 함 — 터미널에서 `git commit` + `git push origin main`으로 배포하면 GitHub Pages(kpop-universe.kr)가 자동 재배포됨. **Claude Code가 작업 완료 후 커밋·푸시까지 해도 됨**(사용자 승인 하에 정착, 2026-08-22). 수동 드래그 업로드도 여전히 가능하지만 기본은 git push. ⚠️ 예전 zip 폴더(예: `Downloads/kpopuniverse-main`)는 구버전이라 절대 거기서 작업/업로드하지 말 것 — 반드시 위 클론에서 하고, 시작 전과 push 직전에 `git fetch origin`/`git pull`로 최신화(로컬 캐시만 믿지 말 것, CHANGELOG 사용규칙 참고).



\- 세션 시작 시 역할 선언하고 시작: \[데이터] \[기능] \[버그픽스] 중 하나.

&nbsp; 예: "\[데이터 세션] 시작. PRINCIPLES.md 읽고 시작해줘. 오늘 할 일: ~~~"

\- 세션은 자기 역할 범위 파일만 수정

&nbsp; - 데이터: groups.json / artists.json / connections.json

&nbsp; - 기능: index.html / kpop\_universe.css (새 기능, UI 변경)

&nbsp; - 버그픽스: 재현된 문제 최소 범위만, 파일 무관

\- 여러 세션(창)을 동시에 운영할 땐 같은 파일을 동시에 건드리지 말 것 — 이제 git이라 같은 파일 동시수정은 push 시 merge conflict로 드러나긴 하지만(예전처럼 조용히 묻히진 않음), 구버전 클론/zip에서 작업하면 앞선 커밋을 모른 채 덮어쓸 위험은 여전함. 겹치는 파일을 만지는 세션은 동시에 두 개 이상 열지 않기. push 전 항상 `git fetch origin` 먼저.

\- 세션 종료 시 CHANGELOG.md에 한 줄 기록. 배포는 이 클론에서 `git commit` + `git push origin main` — Claude Code가 작업 완료 후 커밋·푸시까지 진행해도 됨(2026-08-22 방식 정착). 단 커밋·push 직전에 반드시 `git fetch origin` 먼저(다른 기기/세션이 그 사이 올린 걸 놓치지 않게).

\- "경쟁 구도"(같은 작업을 여러 세션에 동시에 던지고 결과 비교)는 지금 단계에선 안 씀 — 코드베이스가 하나뿐이라 충돌 위험만 키움. 디자인 시안 비교처럼 결과물이 여러 개여도 되는 작업에만 쓸 것.



\## 자문(claude.ai) 활용 방식



이 프로젝트는 클로드 코드(실행)와 claude.ai 자문 세션(방향 검증·감사)을 역할 분리해서 씀 — 2026-08-20, 자문과의 대화로 정착.



\- 역할 분리: **만들기 전** 방향 검증과 **만든 후** 이상할 때의 감사(監査)는 자문, **실행**은 클로드 코드. 구현하는 자는 자기 코드의 구조적 문제를 못 보는 경우가 있음(예: 태깅 로직의 `disbanded` 필터 — 사고 이력에 매몰된 세션은 "그 필터 자체가 문제"라는 걸 못 잡았음). 클로드 코드와 설계 이견이 생기거나 클로드 코드가 확신 없이 제안한 게 있으면, 그 대화를 그대로 자문에게 들고 가서 검증받을 것(producedBy 구조 설계 때 실제로 효과 있었음).

\- 리듬 3층:

&nbsp; - **수시** — 떠오르는 질문은 그때그때.

&nbsp; - **주간** — zip 하나 + "이번 주 질문 하나". "전반적으로 봐줘" 같은 범용 리뷰 요청은 금지(범용 리뷰는 범용 답만 나옴) — "검수 큐 UX가 맞는지 봐줘"처럼 렌즈 하나를 지정하면, 그 렌즈로 보다가 렌즈 밖 문제(버그·구조 냄새)도 걸리는 대로 같이 보고받는 방식. 지금까지 발견된 굵직한 버그들이 전부 이 패턴에서 나옴.

&nbsp; - **월간/마일스톤** — 가오픈 후 첫 데이터, 커뮤니티 스파이크 통과 등 변곡점에 GA 수치(누적 참여 시간 포함)를 들고 가서 시나리오·다음 분기 우선순위를 재계산하는 세션. 고정 지표: **누적 참여 시간 vs 제작 투입 시간(66,000분) 손익분기**.

\- 자문에게는 항상 비판적 답변을 요구할 것 — 자문의 가치는 동의가 아니라 이견에서 나옴("하지 마", "순서가 틀렸어"가 나와야 정상 작동 중인 것).

