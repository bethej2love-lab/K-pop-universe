# CHANGELOG

여러 기기(회사/집 컴퓨터, 노트북)와 여러 Claude Code 세션이 공동으로 작업하는 프로젝트라, 세션 간 인수인계용으로 쓰는 로그.

## 사용 규칙
- 날짜별 역순(최신이 위), 같은 날 여러 세션 작업 가능
- `[완료]` `[진행중]` `[이슈]` `[보류]` 태그로 상태 표시
- 파일 수정 항목은 `[파일명]` 태그 추가 — 다른 세션이 충돌 여부 판단용
- 한 줄로 짧게, "뭘 했는지" 위주(자세한 이유/배경은 코드 주석에 있음)
- 새 세션 시작할 때: PRINCIPLES.md + 이 파일 최근 항목 먼저 읽고 시작할 것
- 작업 끝날 때마다 그 세션이 직접 이 파일에 한 줄씩 추가할 것

---

## 2026-08-19

- [완료][신규 build_group_pages.js] SEO용 그룹별 정적 페이지 생성 스크립트 신설 — 사용자가 Fable과 나눈 성장 시나리오 논의에서 나온 결론(핵심 병목: 해시 라우팅 `#g=...`는 검색엔진이 아예 못 읽어서 그룹 249개+멤버 데이터가 있어도 검색엔진엔 "페이지 1개"로만 보임) 반영. groups.json/artists.json을 읽어 그룹당 정적 HTML 2장(`g/{한글그룹명}/index.html`, `en/g/{영문슬러그}/index.html`) 생성 — 실행 결과 252개 그룹 × 2개 언어 = 504페이지, 총 4.2MB. 각 페이지: 검색형 title/description, 그룹별 OG 태그(첫 곡 유튜브 썸네일 재사용, 별도 이미지 생성 불필요), JSON-LD(MusicGroup 스키마, 멤버 목록 포함), hreflang(ko/en/x-default) 상호 연결, 실제 멤버 목록(각각 앱 딥링크로 연결)·데뷔일·소속사·팬덤명·디스코그래피(최근 30개) 표시, "우주에서 보기" 버튼(자동 리디렉트 없음 — 검색엔진의 "콘텐츠 없는 리디렉트" 저품질 판정 방지)으로 index.html 해시 딥링크 연결. 소속사 영문명은 index.html의 기존 `_AGENCY_EN` 맵을 정규식으로 추출해 재사용(중복 유지보수 방지), 앨범 종류(미니/정규 N집 등)는 서수 영역(EP/Album) 변환 함수 신설. sitemap.xml도 이 505개 URL(루트+그룹 504개) 기준으로 자동 재생성. index.html(진짜 앱)은 전혀 안 건드림.
  - **실행 방법**: groups.json/artists.json이 바뀔 때마다 `node build_group_pages.js`를 로컬에서 재실행 → 결과물(`g/`, `en/g/`, `sitemap.xml`)까지 같이 GitHub 웹 드래그 업로드에 포함시켜야 함. 504개 파일이 한 번에 새로 생기는 첫 업로드라, 기존 방식(개별 파일 몇 개 드래그)보다 무거울 수 있음 — 웹 드래그가 버벅이면 폴더 통째로 zip해서 GitHub 웹 업로드가 zip 자동 해제를 지원하는지 확인하거나, 이번만 별도 방법 고려 필요.
  - **사용자가 직접 해야 할 것(계정 필요, Claude 대행 불가)**: 구글 서치 콘솔 + 네이버 서치어드바이저 등록·소유확인. 소유확인용 코드(메타태그/HTML 파일)를 받으면 그건 반영 가능.
  - **2차 예정(보류)**: 멤버 개별 정적 페이지(1600명+).
- [완료][kpop_universe.css] 컬렉션 썸네일 정사각형으로 통일 — 사용자 제안: 영상이 가로(16:9)든 쇼츠(9:16)든 섞여있어서 지금처럼 가로로 긴 고정 박스(object-fit:cover)면 세로 영상이 심하게 잘리는데, 정사각형이면 어느 쪽이든 크롭 손실이 덜하고 디스코그래피 앨범 커버(.gc-discog-cover, 68×68 정사각형)랑도 톤이 맞음. `.col-row-thumb`(컬렉션 관리 리스트) 44×28→28×28(높이 기준 유지, 세로 리스트라 높이가 더 중요), `.gc-collection-chip-thumb-wrap`(카드 컬렉션 칩) 74×46→74×74(너비 기준 유지, 가로 스크롤 그리드라 너비가 더 중요) — 두 자리 각각 어느 축을 기준으로 정사각형을 맞출지 다르게 선택. Playwright로 렌더링+치수 확인 완료.
- [완료][index.html][kpop_universe.css] 연결 카드(멤버 카드 "연결" 버튼) 로딩 느림 — 로딩 표시 추가 + 근본 원인 수정. 사용자 제보: "연결 카드 뜨는데 로딩이 너무 길어, 뜨는 중 표시라도 보여야지 않을까, 근본적으로 빨라지면 더 좋고".
  - **근본 원인**: `_buildConnContent` 내부의 이름별 콜라보 영상 수 캐시(`_collabCountCache`)가 주석엔 "한 번만 계산해서 재사용"이라고 적혀있었지만 실제로는 **함수 호출할 때마다 새로 만드는 지역변수**라 전혀 재사용이 안 되고 있었음 — 칩 하나 펼치기만 해도(재렌더링) 이름 전부(_getCollabSongs, 건당 6~20ms)를 처음부터 다시 계산. 특히 백그라운드 API 콜라보 데이터(_enrichConnCardFromApi, 실제 DB 태깅분)가 도착해 이름 수가 확 늘어난 뒤 재렌더링될 때 활동 많은 멤버는 `_getCollabSongs`가 300회 넘게 호출되며 4~7초까지 걸림을 Playwright로 실측(계측 wrapper로 함수별 시간 분해해서 특정).
  - **수정**: 캐시 3종(이름별 콜라보 수·전체 콜라보 이름 목록·그룹 동시출연 쌍)을 앵커+API 데이터 보유 여부를 키로 하는 모듈 전역 캐시로 전환 — 앵커가 바뀌거나 API 데이터가 새로 도착할 때만 리셋, 그 사이(칩 선택/그룹 펼치기/Only 토글 등 순수 재렌더링)엔 재사용. 실측: 같은 앵커 재오픈 4700~7000ms → 700~1200ms, 칩 클릭 재렌더링도 동일하게 개선. 첫 오픈(콜드 캐시)은 여전히 느릴 수 있음(활동 많은 인기 멤버는 처음 한 번은 수 초 걸릴 수 있음) — 두 번째부터 체감 안 될 정도로 빨라짐.
  - **로딩 표시**: `#tt-conn-btn` 클릭 시 즉시 로딩 스피너로 전환(`.conn-btn-loading`, 작은 회전 링) 후 더블 `requestAnimationFrame`으로 한 프레임 쉬어 화면에 반영될 시간을 준 다음 무거운 `openConnCard` 호출 — 자바스크립트가 메인 스레드를 막는 동안에도 최소한 "로딩 중"이라는 걸 사용자가 인지할 수 있게 함.
  - Playwright로 검증: 앵커 전환 시 데이터 안 섞이는 것(캐시 오염 없음), 로딩 클래스 add/remove 정상 동작, 콘솔 에러 없음까지 확인 완료.
- [진행중][groups.json] 위 스포티파이 마일스톤 조사에서 discography 자체가 불완전한 그룹이 많다는 게 드러나서(방탄소년단 94곡·스트레이키즈 17곡 등 히트곡이 discography에 아예 없음) 멜론 기반 보강 착수. `_a_audit_full.json`의 A등급 111개 중 kworb 히트곡과 대조해 빠진 게 확인된 30여개 그룹만 우선 진행(전체 139개 재스크래핑은 별도 세션으로 보류, 사용자 확정).
  - 6개 병렬 fork(BTS 단독 + 나머지 5분할)로 멜론 앨범(제목·발매일·커버이미지·전체 트랙리스트) 조사 → **세션 전체에 걸린 WebSearch 횟수 제한(환경변수 CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION, 200회, fork 전체가 공유)에 걸려 다수 그룹 미완료**로 중단(트와이스·아이들·에스파·세븐틴·투모로우바이투게더·아일릿·아이즈원·마마무·악뮤 등). 재시도 fork 3개도 즉시 동일하게 막힘 확인 — 이 세션에선 더 이상 WebSearch 불가, 다음 세션(한도 리셋)에서 이어가야 함. TXT는 예외적으로 WebSearch 없이 위키피디아 WebFetch만으로 부분 데이터(커버·전체 트랙리스트 없음, 타이틀곡만) 확보.
  - 확보된 데이터 43개 앨범을 22개 그룹에 병합 완료(제목 기준 중복 자동 스킵, 기존 앨범 20건은 이미 있어서 스킵됨). 방탄소년단 6개(LOVE YOURSELF 結·MOTS:PERSONA·MOTS:7·BE·Butter·Permission to Dance·My Universe·Proof·Take Two 중 6개, 3개는 이미 있었음), 투모로우바이투게더 7개, 레드벨벳 5개, 엔시티127 5개, 르세라핌 3개, 엔시티드림 7개(트랙리스트 대부분 타이틀곡만, 후속 보강 필요), 엔하이픈·에이티즈·스트레이키즈·스테이씨·이엑스아이디·아이콘 계열 각 0~2개.
  - **중요 발견**: "누락"으로 잡혔던 것 중 일부는 실제 누락이 아니라 **표기 차이**였음 — 예: 스트레이키즈 "God's Menu"는 멜론 원제 "神메뉴"(한자 스타일)로 이미 있었는데 kworb의 영문 제목과 문자열이 안 맞아 새로 조사 대상으로 잘못 잡힘. 그래서 초기 "213곡 누락" 추정치는 실제보다 다소 부풀려져 있었을 가능성 있음 — 트랙 제목 뱃지 기능(`_appendGcDiscogTrackBadges`)이 정확 매칭이라 이런 표기차이 케이스는 뱃지가 안 뜰 수 있음(범위 밖, 별도 이슈).
  - 미해결로 남은 것: 엔하이픈 "CRIMINAL LOVE"/"Fatal Trouble", 에이티즈 "Answer"/"HALAZIA" — 앨범 여러 개 찾아봤지만 못 찾음(추가 조사 필요). 워너원·이엑스아이디는 커버/전체트랙 없이 최소 정보만. 스트레이키즈는 앨범 2개(GO生·IN生)만 상세, 나머지(Clé 시리즈 이후 다수)는 날짜만 확인되고 미검증.
  - **다음 세션에서 이어갈 것**: 트와이스·아이들·에스파·아일릿(마지막 fork 결과 대기 중이었으나 세션 종료로 미반영 — 병합 안 됨), 세븐틴, 아이즈원·마마무·악뮤, 위 미해결 항목들.
- [완료][spotify_streaming_milestones DB] A등급 그룹(group_priority 111개, `_a_audit_full.json` 로컬 파일로 목록 확보 — DB는 admin 세션에서만 읽혀서 대신 씀) 스포티파이 스트리밍 마일스톤 전수 조사. kworb.net을 111개 그룹 8배치로 나눠 병렬 fork로 조사(각 배치는 WebSearch로 kworb 아티스트 페이지 찾고 WebFetch로 곡별 누적 스트리밍 수 수집). 배치2 포크가 스스로 판단해 seed 파일에 직접 SQL을 써넣는 예상 밖 행동을 함(포크가 대화 전체 맥락을 물려받아 파일 관례를 알고 있었음) — 문제는 없었지만 병렬 포크가 같은 파일에 동시 쓰기하면 충돌 위험이 있어 이후 배치는 결과만 받아 직접 취합. 디스코그래피 트랙 제목과 엄격히 대조하는 1차 방식은 groups.json 디스코그래피 데이터 자체가 그룹마다 불완전해서(별도 진행 중인 멜론 스크래핑 프로젝트, 아직 다 안 채워짐) 버터·갓츠메뉴 같은 명백한 히트곡까지 걸러내는 문제가 있어 폐기 — 대신 (1) 같은 그룹 내 "of {그룹영문명}" 패턴(다른 가수 곡에 멤버가 게스트로 참여한 경우, 예: RM 참여 Old Town Road)과 (2) "Solo" 표기(멤버 개인곡)만 배제하는 가벼운 휴리스틱으로 교체 + 언어/버전별 중복 표기("- KR Ver." 등) 정규화 후 최고 스트리밍 수 기준 병합. 아오아 "Perfect Strangers"(12.9억, 지민 관련 곡이 잘못 잡힌 것으로 포크 자체 판단)는 하드코딩 배제. 기존 DB에 이미 있는 (그룹,곡) 조합은 스킵. 최종 341개 신규 항목을 `spotify_milestones_a_tier_sweep_2026-08-19.sql`로 저장 + `spotify_milestones_seed.sql`(누적 기록)에도 이어붙임 — **DB 미실행, 사용자가 admin 세션에서 직접 실행 필요**. 세븐틴(227곡 중 19개만)·트와이스(430곡+ 중 20개만) kworb 페이지가 너무 커서 fetch가 상위 일부만 가져옴 — 두 그룹은 더 깊은 후속 조회 필요. 러블리즈·미쓰에이·하이키·에이프릴·크래비티·뉴이스트·아이딧·엑스원·하이라이트·빅스·아이덴티티·앰퍼샌드원·애프터스쿨·에프티아일랜드는 kworb에 곡 목록 페이지 자체가 안 잡혀 수동 확인 필요.
- [완료][index.html] 공개 컬렉션도 커스텀 표지 사진이 다른 유저 카드에 반영되게 함 — 사용자 제보: "공개 컬렉션에 사진 올렸을 때 카드에 수정반영 안 되는 거 당연히 돼야 하는 거 아님?". 확인해보니 `public_collections` 테이블에 애초에 커스텀 표지(coverImg)를 담을 컬럼 자체가 없었음(핀 고정 영상용 items 순서 트릭만 있었음, 임의 이미지 URL은 실어 보낼 방법이 없었음). `cover_img` 컬럼 추가 + 3곳 연결: (1) `_syncPublicCollection` upsert에 `cover_img:col.coverImg||null` 추가, (2) 표지 사진 업로드 핸들러가 `col.isPublic`이면 즉시 `_syncPublicCollection` 재호출하도록 추가(전엔 다음 번 rename/공개토글 같은 다른 액션이 있어야만 어쩌다 동기화됐음), (3) `_warmPubCollections`의 select에 `cover_img` 추가 + `_loadCollectionRow`의 pub 매핑에 `coverImg:r.cover_img` 추가. Playwright로 업로드→upsert payload에 cover_img 포함 확인 + 남의 공개 컬렉션 카드 렌더링에서 커스텀 표지가 기존 영상 썸네일 대신 정확히 뜨는 것까지 양방향 검증. **DB 마이그레이션 미실행** — `ALTER TABLE public_collections ADD COLUMN IF NOT EXISTS cover_img text;` 사용자가 admin 세션에서 직접 실행 필요.
- [완료][index.html] 컬렉션 커스텀 표지 사진이 그룹/멤버 카드 칩에는 반영 안 되던 버그 수정 — 사용자 제보: "사진 추가/변경은 되는데 카드에서는 반영이 안 됨". 원인: `_loadCollectionRow`(카드에 컬렉션 칩 보여주는 함수, 13816-13817줄)가 카드용으로 컬렉션 객체를 얕은 복사하면서 `coverU`(핀 고정 영상)는 넣고 정작 `coverImg`(업로드한 커스텀 사진)는 필드 자체를 빠뜨림 — 그래서 항상 undefined가 되어 기존 영상 썸네일로 폴백됐음. 컬렉션 관리 리스트(`renderCollectionSection`)는 원본 객체를 그대로 써서 문제없이 보였던 것과 대비. `.map()`에 `coverImg:c.coverImg` 추가로 수정. Playwright로 실제 `_loadCollectionRow` 호출 검증(커스텀 커버 URL이 칩 이미지에 정확히 반영됨 확인). 참고: 다른 유저가 보는 "공개 컬렉션"(`public_collections` 테이블)엔 애초에 coverImg를 담을 컬럼이 없어서(기존 주석에 이미 명시돼있던 제약) 그쪽은 이번 수정 범위 밖 — 필요하면 별도로 논의.
- [완료][admin.js][index.html] 오태깅 재검증 스윕 2종에 "태그 완전 소실" 리스트업 추가 + 멤버 탈퇴일 자동 태깅 컷오프 신설 + 온리원오프 Love 오태깅 정리(일회용). `_ADMIN_JS_VER` 2026-08-18e→2026-08-19a.
  - 사용자 논의: "재검증에서 오태깅 판별나면 아예 무관 처리까지 가능해야 하지 않나" → 검토 결과 반대 결론. `members`/`with_members`가 다 빠졌다고 자동으로 content_flag='무관' 처리하면, 티저/연습영상처럼 개별 멤버명이 제목에 없을 뿐인 멀쩡한 자체채널 콘텐츠가 조용히 숨겨지는 회귀 위험이 큼(기존 '무관' 자동화는 `_ytSweepJunkKeywordVideos`/`_ytSweepBannedVideos`처럼 영상 제목 자체의 무관 신호를 보는 것과 별개 기준). 대신 `_ytSweepMembersMistag`/`_ytSweepAmbiguousCollabMistag`가 태그를 전부 지우게 되는 행만 콘솔에 목록으로 남기도록 수정(자동 무관 처리는 안 함, 관리자가 직접 확인).
  - 온리원오프 "Love" 멤버가 2021.08.02 탈퇴했는데도 최근 영상에 계속 자동 태깅되던 문제 발견(사용자 제보) — 그룹 자체 해체 컷오프(`_disbandCutoffDate`)는 있었지만 개별 멤버 탈퇴 컷오프는 없었음. `_memberLeftCutoffDate(a)` 신설(artists.json `left` 필드 기반) + `_atmResolveMembers`에 `publishedAt` 파라미터 추가해 탈퇴일 이후 발행 영상은 그 멤버를 매칭 후보에서 제외. 세 호출부(`_ytSweepMembersMistag`/`_ytAutoTagMembers`/`_ytRetagAllIncludingTagged`) 모두 select에 `published_at` 추가 + roster에 `left` 필드 포함해서 전달하도록 수정 — 이제 앞으로 신규 동기화도 안 걸리고, 재검증 버튼 누르면 기존 오염(온리원오프 자체채널 463건, 읽기전용 샘플 확인)도 자동 정리됨.
  - "Love"는 흔한 영단어라(아이유 "Love Poem"류 등 무관하게) 다른 그룹 채널 영상 제목에도 평문 매칭으로 대량 오염(읽기전용 샘플 1000건 중 394건이 무관 그룹에 잘못 콜라보 태깅됨 — 위너/몬스타엑스/빅뱅 등, "BORN TO LOVE YOU"/"Love Me A Little" 등 제목에 love가 들어간 것). 정식 해법(흔한단어 화이트리스트)보다 엄격하게 "온리원오프 자체 채널이거나 제목에 온리원오프/ONLYONEOF 있는 경우만 남기고 나머지 다 제거"하는 전용 원버튼(`_ytFixOnlyoneofLoveMistag`, `sp-onlyoneof-love-btn`, "필요할 때만" 섹션) 신설 — members/with_members/cover_of_members(원곡 태깅) 셋 다 정리, tags_manual=true는 미건드림.
  - 실제 DB 쓰기는 전부 admin 세션에서 사용자가 버튼을 직접 눌러야 실행됨(Claude는 조사·코드 작성까지만).
- [진행중] 어제(2026-08-18) Fable과 작성한 `admin_improvement_plan.md`(Downloads 루트, 프로젝트 폴더 밖) 검토 — P1 세션1(이벤트 가드)·세션2(동명이인 카드 이동)는 8/18에 이미 반영 완료 확인. P1 세션3~4(어드민 콘솔 좌측 도킹 분리)·P2 세션5(대시보드)·세션6(편집모달 개선)·P3 백로그는 아직 미반영 — 성격이 다른 큰 구조개편이라 별도 세션([기능 세션])으로 진행 예정.
- [완료][index.html] 탐험 패널 "Discovery" 무한스크롤 신설 — 기존엔 패널 열 때(3분 TTL 캐시) 14개 카드만 뽑히고 끝이었음. 사용자 요청: "끝이 없을 정도로 채워져있으면 좋겠다". 랜덤 그룹/멤버/연결/영상 인라인 4종을 `_buildFeedRandomGroup/Member/Connection/Video`로 독립 함수 분리(재사용 목적) 후, 매번 랜덤이라 반복 호출해도 새 결과가 나오는 7개 함수(위 4개 + 연도별TOP·커버스포트라이트·콜라보스포트라이트) 풀에서 스크롤 끝 도달 시마다 셔플해서 이어붙임. 시간대(1시간) 순환형 3개(`_buildFeedFormatPick`/`_buildFeedGenCamPick`/`_buildFeedDebutYearPick`)는 offset 파라미터 추가해서 반복 호출마다 순환이 계속 앞으로 진행되게 함(안 그러면 같은 시간 안엔 매번 똑같은 카드만 또 뜸). 하트 기준 카드(HiddenGem/TrendingHearts)와 기념일/오늘의과거는 세션 내내 결과 고정이라 반복 풀에서 제외. IntersectionObserver+MutationObserver 조합으로 구현 — 처음엔 IntersectionObserver 콜백 안에서 센티널을 한 번만 맨 끝으로 옮겼다가, 카드들이 Supabase 응답 순서대로 비동기로 들쭉날쭉 도착하면서 늦게 붙는 카드가 센티널보다 뒤에 붙어버려 두 번째 스크롤부터 무한스크롤이 멈추는 버그가 있었음 — MutationObserver로 매 append마다 센티널을 자동 재보정하도록 수정해 해결. Playwright로 4회 연속 스크롤 검증(24→26→34→36→43개, 중복 카드 0건, 에러 없음).
- [완료][index.html] 탐험 패널 "랜덤 연결" 카드 — 2인 전용→2~4인 확장 + 썸네일 정확도 개선 (`_buildFeedDiscovery` 3번 섹션, 12064줄 부근). 사용자 요청: "연결이 이 서비스 핵심 키워드니까 3/4인 연결도 제안하고, 썸네일도 그 인원만 나온 영상이어야". 기존엔 connections.json의 사전 정의 2인 엣지를 골라 `_getCollabSongs`로 매칭 영상을 "찾는" 방식이라 exactOnly 없이 매칭돼 그룹 전체 무대 영상 썸네일이 뽑히는 경우가 있었음. 대신 전 아티스트의 곡 `with` 태그 길이(1~3, 본인포함 2~4인)로 직접 클리크 풀을 만드는 방식으로 교체 — with 길이 자체가 "정확히 이 인원만 나옴"의 증거이자 정확한 썸네일 소스가 됨. 연결 카드(`openConnCard`)가 이미 `_connSelected` 배열로 다중 선택을 지원해서(라이트박스 "연결 보기"에서 이미 쓰던 것) 새 UI 없이 재사용. Playwright로 20회 샘플링해 2/3/4인 분포(10/8/2) 확인 + 3인 카드 실클릭으로 정확한 콜라보 영상("YUNA X KARINA X SULLYOON")만 뜨는 것까지 검증 완료.
- [완료][index.html] 데스크톱 멤버 이름 라벨 인원수 상한(10명) 제거 + 호버 툴팁 정리 + 별 간 최소거리 확대 — 사용자 요청: "이미 라벨 떠있으면 호버 필요 없음, 10명 제한도 없애도 될 듯, 대신 라벨 안 겹치게 최소거리는 확보". (1) `_gcLabelSet`이 `roster.length<=10` 조건 없이 항상 전체 로스터로 설정되도록 변경(2451줄 부근) — 그룹 카드가 열려있으면 인원수 무관 전원 라벨 표시. (2) 호버 툴팁(`mousemove`, 8201줄 부근)에서 "멤버 별 이름" 브랜치 제거, "그룹 행성 이름 프리뷰"(카드 열림과 무관하게 먼 거리에서 라벨 대신 미리 보여주는 것) 브랜치는 별개 기능이라 유지. (3) 멤버 배치 최소거리 `_minSep`을 `radius*0.34`→`radius*0.55`로, 재시도 횟수 60→150으로 확대(1753줄 부근). Playwright로 그룹 크기별 실측: 5명(뉴진스·코르티스)·11명(아이오아이)·13명(세븐틴·우주소녀)은 목표 최소거리 달성 확인, 20명(아이덴티티)은 목표의 70%, 24명(트리플에스, 전체 246그룹 중 최다인원)은 30%에 그쳐 라벨 겹침 가능성 있음 — 원인은 재시도가 멤버별 각도(방향)는 고정한 채 반경(깊이)만 다시 굴리는 구조라 인원이 몰릴수록 못 풂. "행성 반지름을 키우면 되지 않나" 검토했으나, `_minSep`(radius*0.55)·배치반경(radius*0.82)·카드 열 때 카메라 접근거리(`flyToBubble`: radius*5.5)가 전부 radius에 정비례라 반지름을 키워도 화면 투영 간격이 안 바뀜(수학적으로 무효, 실제 효과 있으려면 카메라 접근 배율만 별도로 줄여야 함) — 사용자 판단으로 이 두 그룹은 당장은 그대로 두기로 함(추후 민원 있으면 재검토).
- [완료][index.html] 재생 모드 둘째줄에 조회수 표기 — 그룹/멤버 카드 그리드에서 영상 열 때(가장 흔한 재생 경로)는 `buildBaseQuery`의 select에 `view_count`가 빠져있어 라이트박스 둘째줄(`_fmtVidMeta`)에 날짜만 뜨고 조회수는 항상 안 뜨던 문제. 탐험 피드 스포트라이트 카드는 이미 정상 표기 중이었음. selectCols에 `view_count` 추가 + 플레이리스트 매핑 3곳(단일 클릭/전체 목록/폴백)에 `vc:v.view_count` 추가. Playwright로 실제 `openLightbox()` 호출 검증(뉴진스 Attention 4K 영상, view_count 48,769,907 → "2022.08.05 · 48.8M+" 정상 렌더). 조회수 미수집 영상은 기존처럼 날짜만 표시(폴백 정상). 스크랩함·컬렉션 재생 등 다른 진입 경로는 아직 미적용(범위 밖).
- [완료][index.html] 조회수 표기를 소수점 없는 버림(예: 48.8M+→48M+)으로 통일 — 사용자 요청: "정확도보다 대략치가 중요, 정확한 숫자는 경쟁/서열 구도를 심화시킬 수 있음"(PRINCIPLES.md 철학과 일치). `_fmtVidMeta`(라이트박스 둘째줄)·`_fmtN`(주간 트렌드 카드)·`fmtN`(연도별 TOP 카드) 3곳 모두 `toFixed(1)` 방식 제거하고 K와 동일하게 `Math.floor` 방식으로 통일.
- [완료][index.html] 그룹/멤버 카드 공유 링크·버튼 개선 (사용자 제보: 코르티스 공유 URL이 `#g=%EC%BD%94...`처럼 지저분함 + 맥북에서 공유 버튼 눌러도 OS 공유 시트에 "링크 복사" 옵션이 없음). (1) `_cardHash`/`_connShareHash`가 그룹·멤버 한글명을 `encodeURIComponent`로 통째로 퍼센트인코딩하던 것을, 해시 구분자(` &=#%+`)만 최소 이스케이프하는 `_hashEsc`로 교체 — 공유 버튼이 만드는 링크 문자열 자체는 이제 `#g=코르티스`로 깔끔함(기존 인코딩된 링크도 `URLSearchParams`가 그대로 디코딩해 하위호환 유지). 단, 브라우저 주소창(`location.hash`)은 WHATWG URL 표준상 프래그먼트의 비ASCII를 항상 퍼센트인코딩해 정규화하므로 — Playwright로 실측 확인 — 이 부분은 앱이 손 댈 수 없는 영역이고, 실제 개선 효과는 "공유" 버튼으로 복사/공유되는 텍스트에 한정됨. (2) `_shareCurrentView`가 `navigator.share` 존재 여부만으로 무조건 OS 네이티브 시트를 띄우던 것을, `matchMedia('(pointer: coarse)')` 기반 `_isTouchPrimary()`로 터치 기기에서만 시트를 쓰고 나머지(맥북 등 마우스/트랙패드 기기, `isMob()`의 창너비 판정과 무관하게 정확히 구분됨)는 무조건 즉시 클립보드 복사+토스트로 통일. Playwright로 두 변경 모두 실측 검증 완료.

## 2026-08-18 (cont. 6)

- [완료][index.html][admin.js] 무한 스크롤 점 세개 미표시 + 로딩 멈춤 버그 근본 수정 — 원인: `_probeShortsBatch`(쇼츠 재확인 프로브)가 `appendLock`을 최대 64초 보유, 그 동안 `tryLoadMore`가 즉시 return해 점 세개도 그리지 못함. (1) `_probeShortsBatch`에 배치 전체 데드라인 5초 적용(`Promise.race`) → lock 보유 최대 5초로 단축, 발동 시 `probe_batch_deadline` GA 이벤트 기록. (2) `tryLoadMore`에서 lock 중에도 점 세개 선제 표시 → "끝난 것처럼 보이는" 시각 문제 완전 해결. (3) lock 해제 후 기존 polling/releaseLock이 다음 페이지 이어받음.
- [완료][index.html][admin.js] 이벤트 가드 구조 전환 — 23개 오버레이 전체에 `data-modal` 속성 추가; `window pointerdown`에 `[data-modal]` 가드(모달 내 클릭에서 `closePanels` 차단); `window click`에 동일 가드(우주 클릭 판정 차단); `mousemove` `_overPanel` 셀렉터의 하드코딩 오버레이 목록을 `[data-modal]` 단일 선택자로 통합. 편집 모달 뒤 우주 호버/클릭 통과 버그 원천 차단.
- [완료][admin.js] 동명이인 칩 클릭 시 카드 미오픈 수정 — 칩 click에 `e.stopPropagation()` 추가(버블링 1차 차단); `_dqGotoArtist` 데스크톱 분기에 `requestAnimationFrame` 보완 오픈 추가(이벤트 큐 소진 후 패널 미열림 시 `openSidePanel` 재호출). 근본 원인: 칩 클릭 이벤트가 `window click`까지 버블링돼 방금 열린 카드를 즉시 닫았음 — 위 이벤트 가드와 함께 이중 수정.
- [완료][groups.json] 디스코그래피 대규모 업데이트 — Batch 1(15개 그룹): 데이식스·러블리즈·스테이씨·이달의소녀·위너·인피니트·아스트로·아이오아이·여자친구·오마이걸·카라·씨스타·애프터스쿨·이엑스아이디·엠블랙. Batch 2(23개 그룹): 미쓰에이·베리베리·비원에이포·비투비·빅뱅·빅스·블락비·씨엔블루·아이콘·악뮤·에스에프나인·에이비식스·에이프릴·에프티아일랜드·우주소녀·케플러·크래비티·트레저·펜타곤·프로미스나인·하이라이트·제로베이스원·라이즈. 추가 수정: 에이오에이(2→7개)·티아라(4→6개). tracks 포맷 통일(`{no, title, isTitle}` dict). HTML 엔티티 디코딩 적용.
- [완료][groups.json] 엑소 앨범 커버 이미지 8개 누락 보완 — Mama·Miracles in December·LOVE ME RIGHT Repackage·LOTTO Repackage·Universe Winter Special·COUNTDOWN·LOVE SHOT Repackage·REVERXE. 멜론 albumId 검색 후 CDN URL 패턴(`id.zfill(8)` → 3/2/3 분할)으로 생성, HTTP 200 확인 후 반영.
- [완료][index.html] OST 포함/미포함 필터 — 디스코그래피 케밥 버튼 메뉴에 "OST 포함" 토글 추가(OST가 있는 그룹에만 노출). `currentList()` 수정, `updateSortMenuUI()` 수정, sort item 셀렉터 `[data-sort]` 한정으로 OST 버튼 오작동 방지.

## 2026-08-18 (cont. 5)

- [완료][index.html][admin.js] Fable(claude-fable-5)과의 협업 세션 — 무한 스크롤 근본 원인 분석(appendLock 누수) 및 admin 개선 계획(`admin_improvement_plan.md`) 작성. 해당 패치는 cont. 6에서 반영.

## 2026-08-18 (cont. 4)

- [완료][index.html][kpop_universe.css] 컬렉션 커스텀 표지 사진 업로드 — 컬렉션 관리 행에 카메라 버튼(🎥) 추가, 클릭 시 이미지 파일 선택 → `artist-pics` 버킷 `col-covers/{colId}.{ext}` 경로에 upsert 업로드 → publicUrl을 `col.coverImg`에 저장 → `_saveCollections()`. 칩 썸네일·관리 행 썸네일 둘 다 `col.coverImg` 우선, 없으면 기존 영상 썸네일 폴백.

## 2026-08-18 (cont. 3)

- [완료][index.html] 그룹 이름 레이블을 행성 위→아래로 이동 — CSS2DObject anchor를 `(0, radius+0.3, 0)` → `(0, -(radius+0.3), 0)`으로 변경, div top:5px(anchor 아래)로 통일. 행성 위에 있던 별 멤버 이름과 겹치던 문제 해결.

## 2026-08-18 (cont. 2)

- [완료][admin.js][index.html] 해체 그룹 자동 매칭 차단 — `_m2ParseTitle`의 `groupsSorted` 필터에 `&&!v.disbanded` 추가. 이전엔 해체 그룹도 다른 채널 영상 제목에서 자동으로 with_groups에 태깅됐음(disbanded는 공식 채널 수집 컷오프만 했고, 타 채널 제목 매칭은 막지 않았음). 이제 disbanded 그룹은 아예 매칭 대상에서 제외 → 앞으로 신규 영상에는 no-op, 기존 오태깅 영상은 "콜라보 오태깅 재검증" sweep 버튼으로 정리 가능. `_ADMIN_JS_VER` 2026-08-18d→2026-08-18e.

## 2026-08-18 (cont.)

- [완료][groups.json][artists.json][connections.json] 그룹 추가: 저스트비(JUSTB, 2021, 블루닷, 남성 6인, 온리비) — 멤버 5명 신규(임지민·이건우·배인·시우·김상우), 전도염(원더나인)은 groups 배열로 겸직 반영; 배드빌런(BADVILLAIN, 2024, 빅플래닛메이드, 여성 7인, 빌리온) — 멤버 7명 신규; connections 36쌍 추가
- [완료][groups.json][index.html] 유앤비(UNB) 행성→유닛 격하 — groups.json에서 제거, index.html _PROJECT_UNITS에 9명 유닛으로 추가(강유찬/에이스·고호정/핫샷·준/유키스·이의진·오광석·박대원·이형근·지한솔·김기중 모두 gko=유앤비)

## 2026-08-18

- [완료][artists.json] 젝스키스 이재진 인스타그램 링크 삭제.
- [완료][artists.json] 시크릿넘버 민지 인스타그램 링크 삭제(뉴진스 동명이인 민지와 구분해서 처리, 그쪽은 원래 인스타 링크 없었음).
- [완료][index.html] 탐험 피드 "모아보기"류 3종(포맷 모아보기/세대×성별 직캠 모음/N년도 데뷔 그룹 모아보기)에 멤버 단위 2차 라운드로빈 추가 — 사용자 제보("영상이 많아지면서 개인별 제한 없으면 중복 심하지 않을까"). 확인해보니 그룹 단위 캡(`_diversifyFeed`, 그룹당 5개 또는 3개)은 이미 있었지만 그룹 캡 *안에서* 멤버까지는 안 나누고 있어서, 실측(합성 데이터 200회 시행)해보니 영상 20개인 멤버와 1개인 멤버가 같은 그룹에 있을 때 그 1개짜리 멤버는 20%의 확률로만 노출되고 있었음(80%는 아예 안 뜸) — 사용자가 지적한 우려가 실제로 존재하는 문제였음. `_diversifyFeed`에 선택적 `subKeyFn` 인자 추가해 그룹→멤버 2단계 라운드로빈으로 확장(안 주면 기존과 동일하게 동작, 하위 호환), 3개 호출부에 `v=>v.members&&v.members[0]`로 멤버 서브키 적용 + 각 쿼리의 `.select()`에 `members` 컬럼 추가. 같은 합성 데이터로 재검증 — 노출률 20%→100%로 개선, 200회 시행 분포도 멤버A 400/멤버B 200(항상 1개는 보장)/무개인(포메이션) 400으로 고르게 분산 확인, 콘솔 에러 0건.
- [완료][index.html] 멤버 카드 소속사/생년월일 순서를 생년월일 먼저로, 그룹 카드 소속/데뷔 순서를 데뷔 먼저로 변경(사용자 요청 — "언제 시작됐는지"가 개인=생년월일/그룹=데뷔일로 먼저 오는 게 자연스럽다는 논리로 그룹 카드도 같이 결정). 멤버 카드 쪽(#tco 칩)은 `showT()` 초기 렌더와 `_refreshOpenCardText()`(언어 전환 시 재렌더) 두 군데에 순서 로직이 중복돼 있어서 둘 다 수정 — 겸사겸사 `showT()` 쪽에 빠져있던 소속사 영어 번역(`_dispAgency`) 누락도 같이 고침(영어 모드로 처음 들어오면 소속사가 언어 전환 전까지 한글로 남아있던 사소한 기존 버그). 그룹 카드 쪽(#gc-grid)은 정적 HTML 행 순서라 그냥 마크업만 스왑. 헤드리스로 멤버 카드(초기렌더+언어전환 둘 다 "생년월일→소속사→국적" 확인)와 그룹 카드("데뷔→소속→팬덤→리더→멤버" 확인) 둘 다 검증, 콘솔 에러 0건.
- [완료][artists.json] 인스타그램 링크 3건 반영 — 아이오아이 주결경(zhou_jieqiong1216로 수정), 퀸즈아이 키리(삭제), god 데니안(dannyahn_official 추가).
- [완료][index.html] 디스코그래피 트랙 뱃지 아이콘→개수 자체를 통일로 재수정 — 앞서 "아이콘 통일"을 모양만 같은 아이콘 2개(멜론용/스포티파이용)로 처리했는데, 사용자가 재확인해보니 의도는 "트로피 아이콘 딱 하나만 뜨고 클릭하면 멜론·스포티파이 정보가 같이 표기되는 것"이었음(모양 통일이 아니라 개수 통일). `_appendGcDiscogTrackBadges`를 소스별 반복 호출 구조에서, 트랙당 뱃지 하나만 만들고 있는 정보를 `parts.join(' · ')`로 합쳐서 하나의 info 텍스트로 보여주는 구조로 재작성. 헤드리스로 멜론+스포티파이 둘 다 있는 BTS "DNA"로 재검증 — badgeCount 1개, 클릭 시 "멜론 2018 · 16위 · 스포티파이 5억+" 한 줄로 같이 표기 확인, 콘솔 에러 0건.
- [완료][index.html][kpop_universe.css] 디스코그래피 트랙에 멜론 연간 TOP100/스포티파이 마일스톤 뱃지 추가 — 사용자 요청. 아이콘만 있고 클릭해도 정보가 없으면 이상하다는 재피드백으로, 클릭하면 트로피 패널 전체를 새로 열지 않고 그 자리에서 최소 정보("멜론 2018 · 16위"/"스포티파이 5억+")가 작게 펼쳐지는 인라인 토글로 구현. 앨범 넘길 때마다 매번 조회하면 깜빡이므로 디스코그래피 스트립을 그릴 때(그룹당 1회) 멜론/스포티파이 두 테이블을 병렬로 미리 받아 곡 제목(정규화) 기준 맵으로 캐싱(`_loadGcDiscogTrackTrophies`) — 응답이 늦게 와서 앨범 상세가 이미 열려있으면 지금 보던 앨범을 다시 그려 뱃지 반영. 배지 클릭은 `stopPropagation`으로 트랙 행 자체의 클릭(영상 필터 시트 열기, 어제 만든 기능)과 안 겹치게 분리. 헤드리스로 BTS "DNA"(LOVE YOURSELF 承 'Her') 실측 검증 — 뱃지 2개 정상 노출, 클릭 시 "멜론 2018 · 16위" 정확히 표시, 트랙 행 클릭(영상 필터)과 안 겹침, 재클릭 시 정상 접힘, 콘솔 에러 0건.
- [완료][index.html][artists.json] 업텐션/엑스원 이진혁 인스타그램 링크 추가.
- [완료][index.html][kpop_universe.css] 트로피 아이콘 3개 → 1개+탭 방식으로 통합(사용자 제안: "하나의 트로피 아이콘 눌러서 안에서 탭으로 나누는 게 제일 깔끔하지 않아?"). `gc-melon-top100`/`gc-spotify-milestone`/`tt-melon-top100`/`tt-spotify-milestone` 버튼 4개 삭제, `gc-trophy`/`tt-trophy` 하나로 통일. `_loadTrophy`/`_loadMelonTop100`/`_loadSpotifyMilestones` 3개 함수를 `_loadTrophies()` 하나로 합쳐 3개 테이블 병렬 조회 후 데이터 있는 종류만 모음 — 하나도 없으면 버튼 자체를 숨기고, 1종류만 있으면 탭 없이 바로 그 목록을, 2~3종류면 세그먼트 탭(기존 디스코그래피 종류필터와 같은 `.seg-control` 컴포넌트 재사용)으로 전환. 헤드리스로 아이유(3종류 다 있음 — 탭 3개 노출+기본 선택 "방송 1위" 94건+"스포티파이" 탭 전환 시 3건 정상 갱신) 검증, 옛 버튼 4개 DOM에서 완전히 사라진 것도 확인, 콘솔 에러 0건.
- [완료][index.html] 트로피 3종(음악방송 1위/멜론 연간 TOP100/스포티파이 마일스톤) 멤버 카드 노출 사각지대 발견+수정 — 사용자 질문("멜론 top100에서 개별 멤버 기록도 반영되어야 할 것 같은데")으로 발견. 실측 확인 결과 melon_top100_matched.json 799건 중 247건이 member_ko 있는 그룹멤버 개인기록, 78건이 group_ko="솔로"(아이유·비 등 완전 솔로 아티스트) — 이 트로피 버튼들은 지금까지 그룹 카드에만 달려있었는데, "솔로"라는 그룹 카드 자체가 없어서 78건은 어디에도 노출될 방법이 없었음(그룹 멤버의 개인기록 247건은 그래도 소속 그룹 카드에서는 보였음). `_loadTrophy`/`_loadMelonTop100`/`_loadSpotifyMilestones`에 선택적 `mko` 인자 추가해 멤버 카드(#tt)에도 트로피 버튼 3개 신설, `.eq('group_ko',a.group.ko).eq('member_ko',a.name.ko)`로 개인 기록만 조회(솔로 아티스트는 a.group.ko가 이미 "솔로"라 그대로 씀 — 콘서트/컬렉션이 쓰는 `_ytGroupKoFor` 변환과는 다른 값이라 혼동 주의 주석 남김). 헤드리스로 실제 DB 조회 검증 — 아이유 카드에서 멜론 56건/스포티파이 3건 정상 노출(이전엔 0건), 태연 카드에서 개인 솔로곡 17건 노출, 그룹 카드 쪽 회귀 없음, 콘솔 에러 0건. **음악방송 1위(music_show_wins)는 기존 솔로 레코드 94건(전부 아이유 확인됨)이 member_ko 컬럼 자체가 비어있어서 이 방식으로는 안 잡힘 — 별도 백필 SQL 필요(다음 항목 참고).**
- [완료][spotify_milestones_seed.sql] 스포티파이 누적 스트리밍 마일스톤 리서치+DB매칭 완료, SQL 시드 파일 생성 — 멜론 TOP100 799곡 범위 내에서 114곡 마일스톤 확정(1억+ 86곡/5억+ 21곡/10억+ 6곡/20억+ 1곡[BTS Dynamite 실측 23.3억], 30억·50억 도달곡 없음). kworb.net(3rd-party 스포티파이 집계 사이트)에서 아티스트당 페이지 1회 조회로 82개 아티스트 커버(4개 그룹은 스포티파이 아티스트ID 자체를 못 찾아 제외), 한글↔로마자 표기 차이(예: "하루하루"→"Haru Haru")는 자체 로마자 변환기로 재매칭. groups.json/artists.json 대조 독립 검증 통과(불일치 0건, 마일스톤 티어 값 오류 0건, 중복 0건). 654개 고유곡 중 매칭은 17%(114곡)뿐인데, 나머지 대부분은 `below_1eok_threshold`(159건, 1억 미달=정상 결과)이거나 2007~2012년 구곡이 스포티파이 카탈로그에 없거나 그룹 태그와 달리 실제론 멤버 솔로곡이라 그룹 단위 조회로 못 잡힌 경우(363건) — 상위 히트곡 위주로는 이미 커버됐다고 판단. **참고**: fork의 서술 요약에서 "BTS 32곡"이라고 보고했는데 실제 matched.json/debug.json 둘 다 17곡으로 일치 — 서술 요약 쪽 오기로 보이며, 실제 저장 파일(SQL 반영 대상)은 서로 일치하고 독립검증도 통과해 데이터 자체는 신뢰. **SQL 미실행 — 사용자가 Supabase SQL 에디터에서 spotify_milestones_seed.sql 실행 필요**(테이블 스키마는 이미 생성됨).
- [완료][index.html][kpop_universe.css] 디스코그래피 트랙 클릭 → 영상 목록 방식 재설계(사용자 재피드백: "돌아가기가 너무 어려움") — 처음엔 기존 영상 검색창에 곡 제목을 채워 그 자리에서 그리드를 필터링하는 방식으로 만들었는데, 원래 보던 앨범 화면으로 되돌아갈 방법이 없었음(검색창을 수동으로 비워야 함 + 앨범 상세 팝업은 이미 닫혀있어 위치를 잃음). 그리드를 안 건드리고 앨범 상세 팝업(#gc-discog-detail, z-index:102) 위에 새 시트(#gc-track-videos-overlay, z-index:110)를 얹는 방식으로 전면 재설계 — 앨범 상세는 뒤에서 그대로 살아있어서 ×로 닫기만 하면 원래 화면으로 즉시 복귀(별도 상태 복원 로직 불필요, 그냥 안 건드렸으니까). 새 시트는 그룹 카드 영상 그리드와 동일한 노출조건(무관/숨김 제외, 자체+게스트 태깅 포함, title_norm ilike 정규화 매칭)으로 독립 조회 후 썸네일 리스트로 렌더, 항목 클릭 시 그 목록을 플레이리스트로 넘겨서 라이트박스 재생(다음 영상 넘기기까지 지원). 헤드리스로 실제 그룹(코르티스) 앨범 열어서 트랙 클릭 → 새 시트 오픈+앨범 상세는 뒤에서 유지 확인 → 닫기 → 앨범 상세 그대로 복귀(제목 불변) 확인 → 재오픈 후 영상 클릭 → 라이트박스 정상 오픈까지 전체 플로우 검증, 콘솔 에러 0건.
- [진행중][index.html] 스포티파이 스트리밍 마일스톤 트로피 신설 — 멜론 연간 TOP100과 같은 모달(#trophy-overlay) 재사용, `_openTrophyList(rows,kind)`를 3종(music_show/melon_top100/spotify_milestone) 대응하도록 title/컬럼 매핑을 객체 기반으로 정리. 기간별(일간/주간) 순위는 스포티파이도 공식 과거 아카이브가 없어 소급 불가+상대적 기록이라 트로피 철학에 안 맞다고 판단해 배제, 대신 누적 스트리밍 티어(1억/5억/10억/20억/30억/50억)를 스냅샷으로 기록하는 방식 채택(사용자 논의 후 확정). `spotify_streaming_milestones` 테이블(그룹카드 SQL만 전달, admin.js 쓰기 UI 없음 — 기존 트로피들과 동일 패턴) + `#gc-spotify-milestone` 버튼 추가. 헤드리스로 3종 전부 렌더+제목/포맷("10억+" 등) 검증, 콘솔 에러 0건. **SQL 미실행, 실제 799곡 조사도 아직 시작 전.**
- [완료][melon_top100_seed.sql] 멜론 연간 TOP100(2008~2025, 18개년) 리서치+DB매칭 완료, SQL 시드 파일 생성 — 799건, 86개 그룹/솔로. 멜론 공식 연간차트 페이지 curl 파싱(18개년 전량 100위까지) 후 groups.json/artists.json과 대조. 동명이인 오매칭 감사 별도 수행 — "노을"(레인보우 vs 비아이돌 밴드), "주호"(에스에프나인 vs 비아이돌 솔로) 등 블랙리스트 처리, 그룹 데뷔연도보다 이른 차트연도 매칭 23건 시간모순으로 자동/수동 배제, 동명이인 후보 겹침 15건은 데뷔연도+곡 인지도로 수동 판정(`melon_top100_rejected.json`에 사유 기록). group_ko/member_ko를 groups.json/artists.json에 대조하는 독립 검증 통과(0건 불일치), year+rank 중복 0건. 2008~2013년은 매칭률이 낮은데(30~44%) 그 시기 국내 음원 유통이 멜론 외 다른 플랫폼(도시락/벅스 등)에 분산돼있었을 가능성(데이터 자체 한계로 판단, 매칭 로직 문제 아님). **SQL 미실행 — 사용자가 Supabase SQL 에디터에서 melon_top100_seed.sql 실행 필요**(테이블 스키마는 이미 생성됨).
- [완료][index.html][kpop_universe.css] 마이페이지 FAVORITES/ANNIVERSARIES 통합 리뉴얼 — 사용자가 지적한 3가지(스크롤 너무 김/시각적으로 밋밋함/편집 플로우 불편) 중 구조+시각 문제를 한 번에 해결. 기존 anniv-list가 사실 favGroups/favMembers를 날짜순으로 재배열한 것뿐이라 완전히 중복이었던 걸 확인하고, ANNIVERSARIES 섹션을 통째로 제거해 FAVORITES 카드 안에 흡수: 아바타(원형 사진 또는 색상원) + 이름/태그 + 기념일 배지(멤버 생일 D-day + 팬 기념일 D+day)를 한 줄 카드로. 멤버/연결 아바타는 이미 있는 인스타/위버스 사진 캐시(`_instaPicCache`) 그대로 재사용, 그룹 아바타는 groups.json에 아직 없는 `img` 필드를 읽어서(값 있으면 사진, 없으면 그룹 고유 색상 원형+같은 색 은은한 글로우) — `img` 필드는 인스타 링크처럼 그룹별로 하나씩 채워나갈 예정, 코드는 이미 준비됨. `renderFavList()`/`_buildAnniversaryList()` 두 함수를 `renderFavList()` 하나로 합치고(멤버 사진 캐시 프리로드 await, 재진입 시 낡은 렌더 버리는 세대 카운터 추가), 기념일 편집(`_openDateEditor`)은 각 카드 우측 ⋮ 버튼으로 유지. 헤드리스로 그룹(이미지 있음/없음 둘 다)·멤버(생일+팬기념일 배지 동시 표시)·연결 세 종류 전부 렌더 검증 + 편집 버튼 클릭이 카드 자체 네비게이션과 안 겹치는지 확인, 콘솔 에러 0건. 컬렉션 커스텀 표지 업로드(3번째 요청)는 Storage 인프라가 필요해 별도 작업으로 보류.
- [완료][index.html] 마이페이지(journal-sidebar) 열린 채로 "편집" 누르면 편집 모달(#profile-overlay)이 패널 뒤로 가려지던 버그 수정 — 사용자 제보. 원인: journal-sidebar는 `_bringToFront()`로 동적 z-index를 쓰는데(연결/필터 패널 등을 거치며 세션 내내 계속 누적 상승), profile-overlay는 정적 z-index:69 고정이라 세션이 길어지면(다른 패널 몇 번만 열어도) sidebar가 69를 넘어서 역전됨. `openProfilePanel()`에도 `_bringToFront()` 호출 추가해서 열 때마다 항상 최상단이 되게 수정 — 기존 "겹치는 UI 레이어는 항상 최근 실행 화면이 최상단" 원칙(_bringToFront 유틸) 그대로 재사용. 헤드리스로 실제 버그 재현(sidebar z-index를 70까지 인위적으로 쌓아올린 뒤 편집 열기) 후 수정 검증 — overlay가 항상 sidebar보다 위(72>70) 확인, 콘솔 에러 0건.
- [진행중][index.html][kpop_universe.css] 멜론 연간 TOP100 트로피 신설 — 기존 "음악방송 1위" 트로피(music_show_wins, SQL 에디터로만 관리)와 같은 철학(정적 성취 기록, 개인화 없음)으로 확장. `melon_yearly_top100` 테이블 신설(그룹 카드에 SQL만 전달, admin.js 쓰기 UI 없음 — music_show_wins과 동일 패턴), 그룹카드 이름줄에 두번째 트로피 아이콘(`#gc-melon-top100`) 추가. `_openTrophyList(rows,kind)`를 두 종류(music_show/melon_top100) 다 처리하도록 확장해서 모달 하나 재사용 — kind별로 제목/열 내용만 다르게(날짜+방송사+곡 vs 연도+순위+곡). 헤드리스로 가짜 데이터 렌더 검증(신규 케이스 정상, 기존 음악방송 1위 케이스 회귀 없음, 콘솔 에러 0건). **테이블은 아직 실제 SQL 미실행 — 사용자가 Supabase SQL 에디터에서 CREATE TABLE 실행 필요.** 실제 차트 데이터(연도별 TOP100 명단 리서치+DB 매칭)는 별도 진행 중, 아직 0건.
- [완료][index.html] 멤버 별 점 크기가 고밀도(HiDPI) 화면에서 유독 작아 보이던 버그 수정 — 사용자 제보("특히 웹으로 볼 때 멤버 별 점이 작게 느껴진다"). 실측으로 원인 특정: `makePointsMat`의 `size:sizeW/Math.min(devicePixelRatio,2)`가 devicePixelRatio로 "나눠서" 만들어져 있었는데, three.js(r128) 포인트 sizeAttenuation의 `scale` uniform이 드로잉버퍼(디바이스 픽셀)가 아니라 CSS 픽셀 높이 기준이라(직접 uniform 값 읽어서 확인 — DPR을 1→2→3으로 바꿔도 scale값 불변) devicePixelRatio를 전혀 보정해주지 않음. 결과적으로 CSS 픽셀 기준 체감 크기가 DPR²에 비례해 작아지는 구조였음(DPR=2 화면에서 이론상 75% 축소 — 맥/최신 노트북 스케일링 화면/대부분 폰이 DPR≥2). "나누기"를 "곱하기"로 뒤집어 DPR을 상쇄하도록 수정 — 곱하기로 바꾸면 대수적으로 DPR이 완전히 소거되어 화면 밀도와 무관하게 항상 같은 CSS 픽셀 크기가 됨. 헤드리스 크롬을 `--force-device-scale-factor=1`/`=2`로 각각 새로 띄워(런타임 오버라이드가 아니라 페이지 로드 시점부터 다른 DPR로) 수정 전/후 스크린샷 직접 비교 — 수정 전엔 DPR2에서 행성 안 멤버 별이 거의 안 보일 정도로 흐릿했는데, 수정 후엔 DPR1과 동등하게 뚜렷이 보임을 육안 확인. 콘솔 에러 0건.
- [완료][admin.js][index.html] 일회용 버튼 3개 제거(사용자 요청, "연도별 TOP 최초 백필"은 남김) — ①"아이돌 주도 채널 재태깅(일회용)"(`sp-idol-resync-btn`/`_ytResyncIdolChannels`), ②"HTML 엔티티 깨진 제목/설명 정리(일회용)"(`sp-yt-fix-entities`/`_ytFixHtmlEntities`), ③"cover_of ↔ with 겹침 정리(일회용)"(`wonkok-overlap-fix-btn`+하드코딩 `WONKOK_OVERLAP_FIXES` 4건). 버튼 HTML+함수+리스너+관련 상수/상태변수(`_extIdolResyncing`)까지 전부 삭제. 헤드리스로 admin.js 로드+영상관리패널 오픈까지 회귀 검증(콘솔 에러 0건, 남긴 백필 버튼은 그대로 존재, 지운 3개는 DOM에서 사라짐 확인). `_ADMIN_JS_VER` 2026-08-18c→2026-08-18d.
- [완료][admin.js][index.html] 영상 관리 패널 이중 검색(2차 재검색어) 저장 후 초기화되던 버그 수정 — 영상 편집 모달 저장·닫기(`_closeVidTagModal`)가 목록 새로고침을 위해 `_vmLoad()`를 다시 부르는데, 이 함수가 "1차 검색어 새로 바뀜=2차 재검색어 무의미"라는 원래 의도로 항상 `_vmSearch2`를 초기화하고 있어서, 1차 검색어가 안 바뀐 단순 새로고침 상황까지 2차 검색어가 매번 날아갔음(사용자 제보). `_vmLoad(searchTerm, preserveSearch2)`에 두번째 인자 추가해 편집 저장 후 새로고침(`_vmLoad(undefined,true)`)만 2차 검색어를 보존하도록 분기, 실제 검색창 타이핑으로 부르는 기존 호출들은 그대로 초기화 유지. 헤드리스로 sb 목(mock) 객체 씌워서 3가지 시나리오(1차 검색/편집후 새로고침/새 1차 검색) 전부 검증 완료. `_ADMIN_JS_VER` 2026-08-18b→2026-08-18c.
- [완료][index.html] 영상 제목 마퀴(슬라이드) 속도 소폭 하향 — `_MARQUEE_PX_PER_SEC` 140→128(사용자 요청, "아주 조금만 느리게").
- [완료][artists.json] 에이핑크 김남주 인스타그램 링크 수정 — kimnamjoo_official → sarangdungy(사용자 제보).
- [완료][artists.json] 에이핑크 손나은 탈퇴 반영 — active:true→false, left:"2022.04.08" 추가(웹검색으로 확인, IST엔터테인먼트가 2022-04-08 발표·5인 체제 전환). 같은 그룹 홍유경(2013 탈퇴) 기존 표기 패턴 그대로 따름 — co 필드는 안 건드림(현재 값이 전원 공통 표기라 탈퇴 시점 소속사와 무관).
- [진행중][index.html][kpop_universe.css] "내 우주 카드" 공유 이미지 기능 1차 구현 — 공유 기능 강화 논의 끝에, 새 카드 템플릿을 따로 디자인하는 대신 이미 있는 3D 씬(글로우/듄 컬러링/별밭)을 실제로 캡처해서 쓰는 방식 채택("아름다움"을 새로 만들 필요 없이 앱 본체 미학 재사용). 마이페이지 FAVORITES 섹션에 "✦ 내 우주 카드 만들기" 버튼 신설 — favMode(내 우주 모드)가 켜져서 궤도가 안정된(phase==='active') 상태에서만 동작, 그 라이브 상태를 라이브 카메라는 안 건드리고 임시 카메라로 한 번 더 렌더해서 캡처. `_renderMyUniverseSnapshotDataURL()`(1080×1350 오프스크린 WebGL 캡처, renderer.setSize→render→toDataURL→즉시 원복이 전부 동기라 화면 깜빡임 없음 확인) → `_buildMyUniverseCardDataURL()`(2D 캔버스에 딥네이비+내 별색 틴트 배경 합성 + 발광 타이포로 "{닉네임}의 우주"/"K·POP UNIVERSE" 워터마크) 2단계 파이프라인. 미리보기 모달(#uc-card-overlay)에서 저장(다운로드)/공유(Web Share Level 2, `navigator.canShare({files})`로 이미지 파일 직접 공유 — 미지원 브라우저는 다운로드+토스트로 폴백) 가능. 헤드리스로 실제 이미지를 저장해서 육안 검증하다가 발견 — 화각 안에 즐겨찾기 안 한 다른 그룹 행성이 우연히 걸려서 "남의 우주"처럼 보이는 문제 발견, 캡처 순간만 궤도 밖 행성(bubbleMeshes 중 orbit에 없는 것)을 숨겼다가 즉시 복원하는 방식으로 수정. i18n(T.ko/T.en) 정식 반영. **아직 실제 즐겨찾기 데이터로는 미검증** — 테스트는 헤드리스에서 즐겨찾기 상태를 흉내낸 것이라 실제 서비스에서 그룹 여러 개 + 멤버 동반 별들까지 있을 때 구도가 어떻게 나오는지 사용자가 실사용으로 확인 필요. 다음 단계(같은 렌더러에 파라미터만 바꿔 기념일 카드로 확장)는 이 확인 이후 진행.
- [완료][admin.js][kpop_universe.css][index.html] 영상 관리 패널 정리 — ①버튼 CSS 통일: `#vm-normal-btn`("선택-정상")·`#vm-coverclear-btn`("선택-원곡제외")에 CSS 규칙 자체가 없어서(vm-toolbar 안 다른 버튼들은 다 있는데 이 둘만 브라우저 기본 버튼 스타일로 표시되고 있었음) `#vm-indiv-btn`("선택-개별")과 동일한 블루 스타일 적용해 통일. ②검색 결과 줄에 그룹만 보이던 걸 멤버/콜라보/원곡 태깅 정보까지 한 줄로 요약해 보여주도록 `_vmTagsLine()` 신설(members→"멤버:", with_members+with_groups→"함께:", cover_of_members+cover_of_groups→"원곡:", 값 있는 항목만 " · "로 구분) — `.vm-item` 렌더에 `.vm-tags` 줄 추가, 이 정보를 안 담고 있던 4개 조회 쿼리(전체 탭 3자+ ilike/1~2자 avsCache/검수 탭/무관·숨김 탭)에 전부 필드 추가해서 어느 경로로 들어와도 빠짐없이 보이게 함. 썸네일 화질은 그대로 유지(변경 요청 없었음, mqdefault 그대로). 헤드리스로 가짜 데이터 주입 후 렌더 검증 — 태그 줄 정상 조합("멤버: 지수, 로제 · 함께: 카리나(에스파), 에스파 · 원곡: 아이유(솔로)"), 태그 없는 영상은 줄 자체가 안 생김(조건부 렌더 확인), 버튼 5개 computed style 전부 일치, 콘솔 에러 0건. `_ADMIN_JS_VER` 2026-08-18a→2026-08-18b.
- [완료][index.html][sw.js] CDN 스크립트(three.js/OrbitControls/CSS2DRenderer/supabase-js) 로드 실패 시 로딩 화면 영구 멈춤 수정 — fable이 별도 헤드리스 감사(가오픈 전 점검)로 발견+패치한 걸 검토 후 반영. three.js가 최상위에서 바로 `new THREE.Scene()`을 실행하는 구조라 CDN 차단(회사망/광고차단/CDN 순단)이면 로딩 화면("Here is Our K-Pop Universe..!")에서 에러 안내도 재시도 버튼도 없이 영원히 멈췄음(JSON fetch 쪽엔 이미 재시도가 있었지만 스크립트 로드는 그보다 앞단이라 해당 안 됨). 2중 방어 추가 — ①원본 CDN(cdnjs/jsdelivr) 실패 시 제3 CDN(unpkg)에서 한 번 더 로드 시도(`document.write` 동기 로드로 순서 보장), ②그래도 실패하면 메인 스크립트 최상단 KPU_LIB_GUARD가 기존 "우주를 불러오지 못했어요" 화면과 같은 UI(에러 문구+다시 시도 버튼, KO/EN 자동)로 전환하고 실행 중단 + GA `cdn_load_error` 이벤트 기록. fable이 준 패치 파일(index2.html)이 이 세션에서 그 사이 커밋된 다른 수정 2건(애니버서리 순서 재배열, admin.js 캐시버스팅)이 반영 안 된 시점 스냅샷이라 통째로 덮어쓰지 않고 새로 추가된 가드 블록만 diff로 골라내 현재 index.html에 이식 — 헤드리스로 diff 검증(그 두 수정 외 divergence 없음 확인). sw.js CACHE_VERSION v3→v4(index.html 변경이라 재방문 유저 캐시 무효화 필요). 미검증 1건(fable 보고) — "원본 CDN만 죽고 unpkg는 살아있는" 중간 시나리오는 배포 후 개발자도구에서 cdnjs 요청만 차단해 별도 확인 필요.
- [완료][index.html] admin.js 캐시버스팅 누락 버그 수정 — "HTML 엔티티 정리" 타임아웃을 admin.js에서 고쳤는데(위 항목) GitHub 재업로드까지 했다는데도 사용자가 계속 같은 타임아웃을 겪는다고 제보. 원인 확인 — `_loadAdminScript()`가 `s.src='admin.js'`로 쿼리 파라미터 없이 로드하고 있어서, 브라우저 디스크캐시나 GitHub Pages 앞단 CDN(Fastly)이 재배포 이후에도 옛 admin.js를 계속 서빙할 수 있는 구조였음(코드 자체 재검증 결과 admin.js 최신 파일엔 이미 고친 순수 페이지네이션 방식이 정상 들어있음 — 배포 코드 문제가 아니라 캐시 문제로 결론). `_ADMIN_JS_VER` 문자열 신설해 `admin.js?v=버전`으로 로드하도록 변경 — sw.js의 CACHE_VERSION과 같은 방식으로, admin.js 내용을 바꿀 때마다 이 버전 문자열도 같이 올려야 함(PRINCIPLES.md에 추가 필요).
- [완료][mc_history.json] 6대 음악방송(엠카운트다운/뮤직뱅크/인기가요/음악중심/쇼챔피언/더쇼) MC(엠씨) 역대 계보 데이터 신설 — `{show, memberKo, groupKo, start, end}` 스키마의 독립 JSON 파일, groups/artists/connections.json과 같은 급의 정적 데이터. 각 방송 나무위키 페이지를 fork로 전수 조사한 원본(방송별 기수/이름/재임기간)을 실제 DB 멤버명(artists.json)과 매칭 — 이름만으로 유일하게 매칭되는 케이스, 괄호 힌트(그룹명/영문명)로 동명이인 중 좁혀지는 케이스, 그래도 남는 동명이인은 그룹 데뷔연도/해체연도로 후보를 걸러내는 자동 로직까지 적용해 255건 확정(비아이돌·매칭불가 이름은 전부 제외). 자동 로직으로도 안 풀리는 예외 몇 건은 수동 처리 — 소희(2007 원더걸스·2024 라이즈로 동명이인이 실제 두 명), 김규빈(제로베이스원 vs 앤더블 — 앤더블 데뷔일이 해당 MC 임기 시작보다 늦어 시간상 불가능하다고 판단해 제로베이스원으로 확정), 나나(WOOAH — 매칭 로직이 멤버 영문명만 보고 그룹 영문명 힌트를 못 읽던 버그를 수동 확인으로 우회), 쇼챔피언 4대 도영·재현(NCT 정식 데뷔 전 MC 발탁 — 사용자가 실제로 있었던 일이라 확인해줌, 통상적인 데뷔연도 필터로는 걸러졌을 케이스), 뮤직뱅크 14대 지성(배우 지성이라 비아이돌로 제외, 사용자 확인). 아직 안 한 것 — 이 데이터를 이용한 영상 자동 태깅 개선(MC 진행 클립 vs 무대 클립 구분)과 멤버카드 MC 경력 노출(표기 방식 미정)은 둘 다 향후 과제로 남겨둠, 이번엔 데이터 파일만 신설.
- [완료][admin.js] "HTML 엔티티 깨진 제목/설명 정리(일회용)" 버튼이 "조회 실패: canceling statement due to statement timeout"로 계속 실패하던 버그 수정 — 서버에 `title.ilike.%&#%` 등 OR로 묶은 게 원인이었음. `%&#%`가 2글자 패턴이라 pg_trgm 인덱스가 트라이그램을 못 뽑아 인덱스를 못 타고(3글자 미만은 인덱스 가속 자체가 안 됨), description은 애초에 트라이그램 인덱스가 없어서, OR 조건 중 하나라도 인덱스를 못 타면 전체 테이블(실측 353,910행)을 순차스캔하게 됨 — 바로 위 "전체 영상 검색(admin)" 기능이 예전에 겪었던 "원곡" 스캔 타임아웃과 똑같은 원인. 그때 고친 방식 그대로 재사용 — 서버 필터 없이 순수 id 페이지네이션(1000개씩)으로 전량 가져와 클라이언트에서 엔티티 여부 판별. 실측으로 1페이지(1000행) 0.33초 확인 — 전체 훑는 데 몇 분 걸리겠지만 타임아웃 없이 끝까지 돎.
- [완료][index.html] 탐험 패널 "Today's Anniversary" 줄 순서 변경 — 그룹 데뷔 N주년이 멤버 생일보다 앞(왼쪽)에 오도록 `_buildFeedAnniversaries` 안 debut/bday 두 forEach 블록 순서를 맞바꿈(_appendAnnivStripItem이 매번 list 끝에 이어붙이는 방식이라 호출 순서=화면 순서). 사용자 확인 — 이 기능(Today's Anniversary)엔 그룹 데뷔주년/멤버생일 두 종류만 있고 다른 기념일 로직 없음(마이페이지 즐겨찾기 "기념일 아카이브"는 별개 기능, 거긴 연결 기념일까지 3종류).
- [완료][index.html] "주간 직캠 TOP 20" → "주간 개인 직캠 TOP 20"(EN: Weekly Solo Fancam TOP 20)로 개편 — 그룹 전체 무대/포메이션 영상 말고 개인 직캠만 노출되게 필터 추가. `_buildFeedWeeklyTopCams` 쿼리에 `members` 필드 추가해서 `(v.members||[]).length===1`(정확히 한 명만 태깅) 조건을 기존 `_FANCAM_TITLE_RE` 제목 필터에 AND로 결합. 실제 DB 샘플 조회로 검증 — K-Choreo/MIX직캠(그룹 전체 포메이션)은 members가 빈 배열, 진짜 개인 직캠은 그 한 명만 담겨있는 패턴 확인 후 적용. 최근 7일 실데이터로도 필터 적용 후 53개 후보(그룹당 3개 캡 적용 후) 확보 — 카드 유지에 필요한 최소 3개는 여유 있게 통과. **이 세션부터 index.html이 유일한 정식 파일**이라 별도 동기화 단계 없이 여기서 바로 끝(아래 구조변경 항목 참고).
- [완료][index.html][kpop_universe.html][manifest.json][sw.js][PRINCIPLES.md] **구조 변경: index.html이 유일한 정식 앱 파일이 됨.** 원래 index.html은 kpop_universe.html의 수동 복사본이라 매번 동기화해야 했고 실제로 몇 번 깜빡해서 옛 버전이 노출된 사고가 있었음(오늘 세션에도 2건 발견+수정) — 사용자가 "수동 복사 대신 kpop_universe.html이 index.html로 리다이렉트하게 하자"고 제안했는데, 확인해보니 sitemap.xml·og:url이 전부 루트(index.html)를 정식 URL로 선언해놔서 그 방향대로 하면 대부분 트래픽(검색·공유 유입)이 매번 이중 리다이렉트를 겪고 SEO에도 불리했음. manifest.json의 start_url이 `./kpop_universe.html`로 별도 지정돼있던 게 두 파일이 각자 존재해야 했던 진짜 이유였음을 발견 — 이걸 `./`로 바꿔서 kpop_universe.html을 더 이상 필요 없게 만들고, **반대 방향**으로 kpop_universe.html을 `location.replace('./')`하는 얇은 리다이렉트 파일로 전환(옛 PWA 바로가기/북마크 하위호환용). sw.js도 같이 손봐야 했음 — 오프라인 캐시 폴백 대상이 하드코딩으로 `./kpop_universe.html`이었던 걸 `./`로 변경, PRECACHE_URLS에 `./`·`./index.html` 추가, CACHE_VERSION v2→v3. 헤드리스 크롬으로 실제 리다이렉트 체인(kpop_universe.html 접속 → `/`로 이동 → 앱 정상 로드, GROUPS 249개·콘솔 에러 0건) 검증 완료. **앞으로 코드 작업은 항상 index.html에 할 것 — kpop_universe.html은 절대 직접 수정 금지**(PRINCIPLES.md에 명시).
- [완료][kpop_universe.html] 모바일에서 행성(구체)이 가끔 일시적으로 세로로 찌부/늘어나 보이는 버그 수정 — three.js camera/renderer resize 핸들러가 디바운스 없이 매 resize 이벤트마다 즉시 window.innerWidth/innerHeight로 camera.aspect를 갱신하고 있었음. 모바일(특히 iOS Safari)은 주소창 접힘/펼침·화면회전 도중 resize가 짧은 간격으로 여러 번 발생하는데 그 사이 너비/높이가 순간적으로 안 맞는 값을 보고할 수 있어(2026-08-18, 사용자 제보), 이미 검색창 리사이즈에 쓰던 것과 같은 디바운스(150ms) 패턴 적용. 헤드리스 크롬으로 CDP Emulation.setDeviceMetricsOverride를 30ms 간격 3연타(390×700→390×760→390×800)로 흔들어 실제 주소창 접힘과 유사한 지터를 재현+검증 — 디바운스 중엔 camera.aspect가 중간값 없이 이전 값 그대로 유지되다가 마지막 안정된 크기(390×800)로만 한 번에 갱신되는 것 확인.
- [완료][admin.js][kpop_universe.html] 영상 제목/설명에 "&#39;" 등 HTML 엔티티 그대로 노출되던 버그 수정 — YouTube API가 title/description을 종종 HTML 이스케이프해서 내려주는데(어퍼스트로피→&#39;, &→&amp; 등) 동기화 코드가 그대로 저장, 화면은 안전하게 textContent로만 찍다 보니 엔티티 텍스트 자체가 그대로 보였음. `_decodeHtmlEntities()`(textarea innerHTML→value 트릭, 모든 named/numeric 엔티티 커버) 신설해 동기화 3개 캡처 지점(채널 훑기 `_ytFetchNewVideos`/과거 백필 search API/URL 수동추가)에 전부 적용. 기존에 이미 오염된 채로 저장된 행 정리용으로 "HTML 엔티티 깨진 제목/설명 정리(일회용)" 버튼 신설(tags_manual 행 보호, title_norm도 같이 재계산). 헤드리스 브라우저로 디코딩 로직 자체는 검증 완료(정상 "&" 포함 제목은 안 건드림 확인) — 실제 DB 백필 실행은 관리자가 버튼 눌러야 함.
- [완료][kpop_universe.html] 구글 로그인 "한 번에 안 되고 다시 시도해야 됨" 제보 — 원인 두 가지 발견해 수정. ①buildUniverse()의 최초 데이터 로드(groups/artists/connections.json fetch)에 타임아웃/재시도가 전혀 없어서, 구글 인증 화면 갔다 리디렉트로 돌아오는 순간(모바일에서 특히)처럼 네트워크가 잠깐 불안정한 때와 겹치면 그대로 throw → "우주를 불러오지 못했어요" 화면 — 카드그리드 로딩에 이미 쓰던 조용한 자동재시도(`_withTimeout` factory+retries, 8초×3회)를 여기도 적용. ②createClient()의 detectSessionInUrl이 URL의 `?code=`를 세션으로 교환하는 게 비동기(Supabase 토큰엔드포인트 왕복)라, 리디렉트 직후 곧바로 `getSession()`을 부르면 아직 교환 전이라 예전 익명 세션이 잡히는 레이스 — "구글 로그인 콜백은 감지됐지만 세션이 익명 상태로 남음" 경고가 뜨던 게 바로 이 증상. 짧게(350ms×최대6회) 재확인하는 루프 추가. 헤드리스 크롬으로 정상 로딩 경로 회귀 없음 확인(GROUPS 249개 정상 로드, 콘솔 에러 0건) — 단 실제 구글 OAuth 리디렉트 레이스 자체는 이 환경에서 재현/검증 불가.
- [완료][kpop_universe.html] 디스코그래피 영어모드 "N집" 서수 처리 — 위 재정정 직후 사용자가 "Full 2집"처럼 "집"이 번역 안 된 채 남는 걸 발견, 영어 모드에서만 서수+타입 순서로 재배치("정규 2집"→"2nd Full", "미니 5집"→"5th Mini"). `_ordinal(n)` 헬퍼 신설(11/12/13 예외 포함 표준 서수 접미사 로직), 정규식으로 "N집" 패턴 파싱 후 재조합. 한국어 모드는 그대로 유지("Mini 5집"처럼 Mini만 원래 영단어라 안 어색함).
- [완료][kpop_universe.html] 디스코그래피 앨범 타입 표기 재정정 — 오늘 먼저 "미니"→"EP" 통일 요청받아 처리했는데(위 항목 참고) 사용자가 번복, "EP"→"Mini"가 맞다고 정정 + "정규"도 언어별 전환 신규 요청. 최종 규칙: All/Mini/Single은 Single·All처럼 언어 무관 항상 영문(탭 세그먼트 라벨용 `_discogBucketLabel()` 신설로 기존 정적 객체 `_DISCOG_BUCKET_LABEL` 대체), "정규"만 한국어 모드에선 원문 유지·영어 모드에서만 "Full". `_discogTypeLabel()`도 같은 규칙으로 수정 — 원본 데이터에 "미니 N집" 말고 단독 "EP" 문자열도 있어서(`_discogBucket`의 `/^(미니|EP)/` 버킷 판정과 동일 패턴 적용) 같이 안 잡았으면 "EP" 문구가 남을 뻔한 걸 검증 중 발견해 수정.
- [진행중][kpop_universe.html] 그룹/멤버 카드 무한스크롤 "점세개 안 뜸" 재발 — 8/14에 두 차례(releaseLock 뷰포트 재확인 + 4개 컨테이너 scroll 리스너 보완) 고쳤는데도 사용자가 계속 재발 제보. 로컬에 헤드리스 크롬(CDP, playwright 없이 Node 24 내장 WebSocket/fetch로 직접 구현)으로 실제 재현 시도 — 두 기존 수정 모두 코드상 정상 존재 확인, 스크롤 시뮬레이션으로도 재현 실패(여러 페이지 정상 연속 로드됨). 기존 두 수정의 근본 원인 설명(iOS Safari fixed-position 컨테이너에서 IntersectionObserver 재발화 누락)이 특정 브라우저 엔진 이슈라 Windows Chrome 환경에서 애초에 재현이 안 될 가능성 높음. 어느 이벤트(intersect/scroll)에도 의존하지 않는 3중 안전망으로 setInterval(700ms) 폴링 추가 — moreWrap 위치를 주기적으로 직접 재확인해서 tryLoadMore 호출(hasMore/appendLock으로 이미 막혀있어 평소엔 no-op). 추가하면서 카드 닫힌 뒤에도 이 폴링이 계속 도는 잠재 문제 발견(closeCards가 컨트롤러 상태/리스너를 안 지움 — 기존 scroll 리스너도 같은 결함 있었으나 실제 스크롤이 일어나야만 발화해서 안 드러났던 것) — offsetParent 체크로 카드 닫히면 폴링이 조용히 멈추도록 보완, 헤드리스로 재검증(열림 상태 스크롤 시 정상 페이지네이션, 닫은 뒤 3.5초간 요청 0건) 완료. 사용자가 "여전히 발생"이라 아직 [진행중]으로 남김 — 실제 발생 기기/브라우저 확인 필요.
- [진행중][kpop_universe.html] 위 무한스크롤 건 후속 — 사용자 확인 결과 데스크톱 사파리·모바일 사파리 둘 다에서 발생(안드로이드는 미확인). Windows Chrome으론 재현이 안 되고 실제 발생 환경(사파리)을 테스트할 방법이 없어서, 추측 대신 실측하기로 함 — `tryLoadMore(ko,source)`에 트리거 출처(observer/scroll/poll/releaseLock) 태그 추가해 `_gaEvent('infscroll_trigger',{source})`로 GA에 남김. 이제 실사용 데이터가 쌓이면 IntersectionObserver가 실전에서 얼마나 자주 놓치는지, poll(3중 안전망)이 실제로 얼마나 자주 구제하는지 확인 가능. (구현 중 스스로 발견: scroll 리스너 등록을 인라인 화살표 함수로 바꾸면서 _cleanupScroll의 removeEventListener 대상이 어긋나는 실수를 커밋 전에 잡아 별도 참조로 수정.)
- [완료][kpop_universe.html] 탐험 패널 "Today's Anniversary" 줄 가로 스크롤 안 되던 버그 수정 — #feed-anniv에 .hscroll-drag 클래스+_enableDragScroll 누락(다른 스트립엔 다 있었는데 여기만 빠짐, #feed-overlay가 메인 &lt;script&gt;보다 뒤에 있는 정적 HTML이라 스크립트 최상단에서 바로 바인딩하면 안 돼서 _buildFeedAnniversaries() 안에서 지연 바인딩). 근본 원인은 더 넓은 문제였음 — 모바일 카드 바텀시트 스와이프-닫기(_wireSheetSwipe)와 탐험 패널 스와이프-닫기(_wireFeedSwipeDismiss) 둘 다 .hscroll-drag 영역 제외 로직이 없어서, 콘텐츠가 맨 위일 때 가로 스크롤 시도가 세로 방향 손떨림만으로도 시트 닫기/펼치기로 오인식되던 상태였음(#tt의 멤버 스와이프 제외 로직엔 있었는데 이 둘엔 없었음). 두 함수 모두에 같은 제외 체크 추가 — 이제 .hscroll-drag 붙은 줄(카테고리 칩·태그 줄·디스코그라피 스트립·기념일 스트립 등)은 모바일 바텀시트/탐험 패널 안 어디에 있든 좌우 스크롤이 세로 제스처에 안 뺏김.
- [완료][kpop_universe.html] 디스코그래피 앨범 타입 표기 "미니"→"EP" 통일 — groups.json 원본 type 값("미니"/"미니 1집"~"미니 8집")은 안 건드리고(멜론 스크래핑 진행 중이라 동시수정 위험) `_discogTypeLabel()` 신설해 표시 단계에서만 치환(그룹카드 디스코그래피 스트립 배지+상세 팝업 2곳 적용). "미니 5집"→"EP 5집"처럼 뒤에 "집" 붙는 건 그대로 유지("정규 N집"과 같은 패턴이라 안 건드림).
- [완료][kpop_universe.html][kpop_universe.css][admin.js] 어드민 CSS 통일 + 신규 버튼 2종 — ①어드민 팝업 패널(vm/hnn/gp/fbv) 스타일 감사 결과 vm-panel이 가장 오래돼서 배경색·radius·shadow·헤더 폰트굵기·탭 스타일(개별 테두리 필박스)이 나머지 3개(hnn/gp/fbv, 공유 배경+무테두리 세그먼트 방식)랑 어긋나 있던 것 확인 후 값 전체를 gp/fbv 기준으로 맞춤(overflow:hidden 누락도 같이 보완). fbv-close도 지난 세션에 만들 때 살짝 다르게 넣었던 걸 gp-close 기준으로 재수정. HTML/JS(탭 전환 바인딩)는 안 건드리고 CSS 값만 교체. ②영상관리패널에 "선택-정상" 버튼 신설 — 기존 vm-apply-btn은 탭마다 동작이 고정(전체탭은 무관처리만 가능)이라 이미 플래그 섞인 항목들을 한번에 정상으로 되돌릴 방법이 없었음, vm-indiv-btn과 동일 패턴으로 추가. ③그룹/멤버 카드 영상 그리드에 어드민 전용 "전체선택" 버튼 신설 — state.allVids(현재 탭/검색 기준으로 로드된 목록, 필터 바뀔 때마다 리셋되는 값이라 항상 "지금 노출된 것"과 일치) 전체를 한 번에 _selIds에 담음. vm-toolbar/.gc-ch-hd에 flex-wrap:wrap 추가해 버튼 늘어나도 자동 줄바꿈되게 함.
- [완료][index.html] index.html이 kpop_universe.html과 2군데 어긋나 있던 것 발견+동기화 — 8/12 리액션 키 구분자 버그수정(스페이스→null byte)이 index.html엔 반영 안 됨. index.html은 항상 kpop_universe.html과 byte-for-byte 동일해야 함(PRINCIPLES.md에 경고 추가). 이번 세션 작업 끝나고 다시 전체 동기화 완료.
- [완료][kpop_universe.html][index.html] 영어모드(EN) 미번역 텍스트 전수 수정 — 사용자 제보(설정 "N세대" 버튼, 소속사명)로 시작해 전체 감사 후 우선순위대로 전부 수정: ①탐험 피드 카드 제목/부제(포맷모아보기·세대별직캠·주간TOP20·연도별TOP100·데뷔연도모아보기) ②앱 로드 실패 화면 ③피드백 폼 전체(data-i18n 신규 부여) ④컬렉션 관리(새로만들기/이름수정/삭제/공개전환/한줄평/영상개수, `_colVidCountText` 헬퍼 신설) ⑤다중선택 버튼("선택"/"취소"/"N개 선택됨", `_selCountText` 헬퍼 신설) ⑥공유링크 복사 토스트 ⑦기타(가족 라벨·기타앨범포함·즐겨찾기 전체보기·다른아티스트의 커버·콘서트 예매정보/다녀왔어요/한줄후기·음방1위 트로피·N명이 좋아해요·X계정 연결 alert). `applyLang()`에 `data-i18n-title` 속성 신규 지원 추가(title 툴팁용, 기존 data-i18n/data-i18n-ph와 동일 패턴). 설정 "N세대" 버튼은 아이콘이 같은 버튼에 섞여있어 `_updateGenButtonsLang()` 별도 함수로 처리, 이 과정에서 `_openGenInfo` let 선언이 파일 뒷부분에 있어 초기 `applyLang()` 호출 시 TDZ ReferenceError 나던 것 발견해 앞으로 옮겨 수정. 관리자 전용 UI(어드민 검수센터·연결카드 태깅 등)는 의도적으로 미번역 유지.
- [완료][kpop_universe.html][index.html] 소속사(agency) 영문 표기 정확도 개선 — 기존엔 "…엔터테인먼트"→"Ent." 단순 접미사 치환뿐이라 순수 한글 브랜드명(스윙·큐브·젤리피쉬 등)은 여전히 한글로 남았음. groups.json의 147개 소속사명 중 한글 포함 110개 전체를 나무위키/웹검색으로 실제 공식 영문 표기 확인해 `_AGENCY_EN` 매핑 테이블로 추가(`_dispAgency`가 테이블 우선 조회, 없으면 기존 접미사 치환으로 폴백). 판타지오(Fantagio, "Fantasio" 아님)·플레디스(Pledis)·웨이크원(WAKEONE)처럼 음차 추측과 실제 표기가 다른 사례 다수 확인. `포켓돌스튜디오 / 엠이오` 1건은 영문 자료 확인 못해 추정치로 남김(코드에 UNVERIFIED 표시).
- [완료][kpop_universe.html] 신규 유저 프로필(별) 첫 생성 시 기본 컬렉션 "Bookmarks" 자동 생성 — 별도 북마크 버튼이 없어 컬렉션이 그 역할을 대신하는데, 유저가 직접 만들어야만 시작할 수 있던 문제. saveProfile()에서 `_isNewProfile&&!collections.length` 조건일 때만 생성(기존 유저 소급 미적용, 익명 상태에서 이미 만든 컬렉션 있으면 건너뜀). 이름은 언어 설정 무관 고정 "Bookmarks"(사용자 결정)
- [완료][kpop_universe.html][kpop_universe.css][admin.js] 피드백 조회 패널 신설 — 기존엔 사용자가 보낸 피드백이 `feedback` 테이블에 저장만 되고 관리자가 볼 UI가 없었음. 설정 패널 어드민 섹션에 "피드백 보기" 버튼 추가, 그룹 우선순위 패널(gp-)과 같은 톤으로 카테고리별 탭(전체/버그/제안/기타)+목록+삭제 기능만 구현(읽음 상태 추적은 없음, 처리 끝나면 삭제로 정리하는 방식). `feedback` 테이블에 `id`·`created_at` 컬럼 있다고 가정하고 짰으니 실제 스키마와 다르면 확인 필요.

## 2026-08-15

- [완료][kpop_universe.html][kpop_universe.css] 몰입형 쇼츠 모드 UI 항상 표시 — 닫기·리액션·툴바 아이콘 기본 숨김(lb-ui-hidden) 제거, 탭-캡(탭→UI토글) 기능 제거
- [완료][kpop_universe.html] 탐험 버튼으로 새 카드 열릴 때 스크롤 위치 초기화 버그 수정 — display:none 중 scrollTop=0이 Safari에서 무시되던 문제, display:block 직후 재설정으로 보완(openMobSheet 첫 오픈 + 스택 푸시 2곳)
- [완료][kpop_universe.html][kpop_universe.css] 라이트박스 쇼츠 모드 UI 정리 — ① 닫기 버튼 아이콘 항상 chevron(↓)으로 통일(✕ 제거, 모드 무관하게 동일); ② 줄 세개 버튼(lb-shorts-list-btn) 완전 제거; ③ rotate 버튼 양방향 토글로 통합 — 세로(쇼츠)→가로(browse), 가로→세로(shorts) 자유 전환; _lbSwitchToShorts() 신설
- [완료][groups.json][artists.json] 그룹 추가: 아이칠린(ICHILLIN', 2021, KM엔터, 걸그룹 7인), 엔싸인(n.SSign, 2023, nCH엔터, 보이그룹 7인, 청춘스타 출신)
- [완료][kpop_universe.html] 동기화 무관 키워드 추가 — 스프링피버·유뷰녀 킬러 (_JUNK_TITLE_KEYWORDS_GLOBAL)
- [완료][kpop_universe.html] 빈 탭 숨김 존재 체크 쿼리 버그 수정 — cover는 category='cover' 대신 title_norm ilike(cover·커버·원곡) + content_formats 체크로 수정, variety에 제목 키워드(라디오스타·아는형님 등) 추가, show에 content_formats.cs.{show} 추가 → buildBaseQuery 실제 필터와 일치

## 2026-08-14

- [완료][kpop_universe.html] live·cover 탭 content_formats 크로스탭 지원 — content_formats에 'live'/'cover' 수동 태깅 시 해당 탭에 복수 노출; _pickFeaturedVidFromDB·buildBaseQuery·patchItem 3곳 동기화
- [완료][kpop_universe.html] 빈 탭 자동 숨김 — 카드 오픈 시 all 탭 제외 각 탭에 limit(1) 병렬 존재 확인, 영상 없으면 탭 제거·현재 필터가 해당 탭이면 all로 리셋; fan 탭은 기존 별도 처리 유지
- [완료][kpop_universe.html] 무한스크롤 점 세개 재발화 누락 보완 — scroll 리스너를 mob-card-stack-inner·mob-sheet-inner·side-panel·member-panel 4곳에 추가(IntersectionObserver가 iOS Safari 등에서 fixed 컨테이너 안에서 재발화 놓치는 경우 대비); 이전 releaseLock 뷰포트 체크와 병행
- [완료][kpop_universe.html] 모바일 카드 내 영상 스크롤 감도 상향 — _wireSlowScroll factor 0.85→0.92, coast decay 0.87→0.90 (한 번 터치에 더 긴 거리 스크롤+관성 여운 증가)
- [완료][kpop_universe.html] 멤버 카드 콘서트 분리 — 그룹 멤버는 멤버명 기반 솔로 이벤트만(_loadMemberConcertRow 신설), 솔로 아티스트는 기존대로; DB에 groups=['멤버명'] 추가 즉시 자동 표시
- [완료][kpop_universe.html] Cover 탭 제외 키워드 추가 — 무빙 커버·moving cover·original contents (대표영상쿼리·목록쿼리·patchItem 3곳 동기화)
- [완료][kpop_universe.html] 예능 탭 제목 키워드 분류 추가 — 라디오스타·아는형님·아는 형님·놀라운토요일·전참시 (buildBaseQuery 2곳+patchItem 3곳 동기화, patchItem에서 content_formats 체크도 누락분 추가)
- [완료][kpop_universe.css][kpop_universe.html] Collections 헤더에 인라인 컬렉션 수 표시 — "Collections · n개" 형식, cnt 스팬 별도 스타일(letter-spacing·uppercase 해제, 약간 연하게)
- [완료][kpop_universe.css] 콘서트 표시줄 폰트 11.5px→10.5px 축소, padding-top 10px→7px 축소 / gc-collection-row margin-top 9→7px·padding-top 10→8px 조정(아래 선 간격 감소)
- [완료][kpop_universe.css] 틱톡↔나무위키 아이콘 간격 보정 재수정 — 이전 -3px는 방향이 반대(gap 증가), -11px로 교체해 시각적으로 다른 아이콘 쌍과 균등하게 맞춤
- [완료][kpop_universe.css][kpop_universe.html] 대표(featured) 쇼츠 표시 크기 확대 — 42% 폭 제한 제거, 2열 전체 폭으로 9:16 비율 최대 크기 표시 / 버튼 위치 오버라이드 제거(기본값으로 복귀)
- [완료][kpop_universe.html] Shorts 탭 그리드에 간헐적 와이드 쇼츠 추가 — _groupGcVids에 widenSomeShorts 파라미터, 10% 확률로 단독 2열 폭 카드 삽입(normal 영상과 동일한 방식)

- [완료][kpop_universe.html] 무한스크롤 점 세개 안 나오는 버그 수정 — appendLock 해제 시점에 moreWrap이 이미 뷰포트 안이면 IntersectionObserver가 재발화 안 하던 문제, releaseLock에서 직접 viewport 체크 후 tryLoadMore 호출로 보완
- [완료][kpop_universe.html] Live 탭에 제목 기반 예외 추가 — '리무진서비스'/'리무진 서비스' 포함 영상 category 무관하게 live 탭으로 포함 (buildBaseQuery·_pickFeaturedVidFromDB·patchItem 3곳 동기화)
- [완료][kpop_universe.html][admin.js] variety/show 탭 분류를 category 단일값 → content_formats 배열 기반으로 전환 — 동기화 시 tier 자동 태깅, 수동 저장 시 기존 코너명 태그 보존하며 장르 태그만 교체, 쿼리는 category OR content_formats OR 병행(기존 레거시 행 누락 방지)
- [완료][kpop_universe.html] 어드민 영상 포맷 편집 select에 예능·드라마/영화 옵션 추가
- [완료][kpop_universe.html] Cover 탭 제외 키워드 추가 — discover/recovery/undercover 및 한글판(디스커버·리커버·언더커버) 3곳(대표영상쿼리·목록쿼리·클라이언트 필터) 동기화
- [완료][kpop_universe.css] 멤버 이름 레이블(10명 이하 그룹 웹) 폰트 9.5px→8.5px 축소, 투명도 0.55→0.82로 높이고 text-shadow를 group-label과 동일한 currentColor 이중 글로우 방식으로 변경
- [완료][kpop_universe.html] Cover 탭 제외 키워드 추가 — original ver / cover story / 커버 촬영 / uncover / choom original / the original (목록쿼리·대표영상쿼리·patchItem 클라이언트 필터 3곳 동기화)
- [완료][admin.js] 영상 태그 저장 후 그리드 갱신 딜레이 500ms → 100ms (단일/일괄 저장 모두)
- [완료][kpop_universe.html] 어드민 계정 GPU 렌더링 15fps로 제한 — 일반 유저는 30fps 유지, 로그인 전/일반 계정은 그대로
- [완료][kpop_universe.html][kpop_universe.css] 공개 컬렉션 칩에 삭제 버튼(×) 추가 — mine인 칩에만 hover 시 표시, 클릭 시 confirm 후 `public_collections` + 로컬에서 동시 삭제
- [완료][kpop_universe.html] 어드민 로그인 시 본인이 만든 공개 컬렉션 안 보이는 버그 수정 — `_loadCollectionRow`의 `pub` 필터를 `owner_id !== uid` → `!mineIds.has(r.id)`로 변경; 자기 uid 공개 컬렉션이 `user_data`에 없으면 mine/pub 양쪽에서 제외되던 문제

- [완료][kpop_universe.css][kpop_universe.html] 멤버 카드 소속 그룹 폰트 조정 — 유닛은 메인 그룹(12px)보다 1pt 작게(11px), 영어 텍스트는 추가 1pt 감소(그룹 11px·유닛 10px), `tg-en` 클래스로 한글 미포함 텍스트 자동 감지
- [완료][kpop_universe.html] WHO'S YOUR BIAS? 검색창 디자인 수정 — 위치 아래로(top 58→76px), 보라빛 제거·더 투명한 물방울 느낌(배경 0.06, blur saturate 140%, border 0.5px, 그림자 축소, border-radius 28px)
- [완료][kpop_universe.html] 탭바 위 떠있던 `#search-wrap` 자동오픈(신규 유저 모바일) 제거 — 중복 검색창이었음
- [완료][kpop_universe.html] "WHO'S YOUR BIAS?" 검색창 개편 — 비로그인 모바일에만 표시, 버튼이 아닌 바로 열린 입력창으로 변경, 탐험·별표 탭 클릭 시 / 검색결과 선택 시 자동 숨김
- [완료][admin.js] admin.js 전체 코드 검토 (파일 수정 없음) — 매칭 예외 목록 하드코딩 증식·`_m2ParseTitle` 복잡도 누적·sweep 함수 중복·two-step tags_manual 저장 취약점 등 개선 포인트 정리해 보고
- [완료] CHANGELOG.md 신설 — 오늘부터 세션마다 기록 습관화
- [완료][admin.js] 어드민 영상관리 검색: 결과 제한 200→1000개로 상향, 1~2글자 검색 대소문자 구분되던 버그 수정(_titleNorm 정규화 누락)
- [완료][kpop_universe.html] 재생 모드 업로드일 표시 — 탐험 피드 스포트라이트 카드 5종에 published_at 누락되어 있던 것 수정
- [완료][groups.json] disbanded 그룹 41개 전수 해체연도/날짜 리서치+반영 (39개 날짜 채움, 이달의소녀·카드는 "해체 아님" 확인되어 플래그 해제)
- [완료][kpop_universe.html] 영상 동기화 시 해체 그룹의 해체일 이후 영상은 수집 안 하도록 컷오프 로직 추가(`_disbandCutoffDate`)
- [완료][groups.json][artists.json] 그룹 추가: 배틀(2006, 배틀신화 출신), 스피카(2012~2017)
- [완료][kpop_universe.html] 해체 그룹·탈퇴 멤버의 소속사(agency) 표기 흐리게 처리(`.tag-chip-past`)
- [완료][groups.json][kpop_universe.html] JX(JYJ) 그룹 행성 제거 → 동방신기 소속 유닛으로 전환 — 전원 active:false인데 disbanded 아니라 행성에 별 없던 버그 처리
- [완료][groups.json] 서바이벌쇼 24개 전수조사 — projectShow 태그 11개 그룹 신규 추가(I-LAND→엔하이픈 등)
- [완료][kpop_universe.html] 온보딩 "Who's Your Bias?" 검색바 — 반투명·페이드인·텍스트 글로우, 최초 1회 타자 입력 모션 추가
- [완료][kpop_universe.html] 멤버 카드 그룹칩 줄(#tg-row) 웹 휠/드래그 스크롤 안 되던 버그 수정 — `_enableDragScroll` 미연결이 원인
- [완료][kpop_universe.html] _PROJECT_UNITS 68개 서브유닛 일괄 추가 (EXO-CBX·SC, 인피니트H·F, BTOB-BLUE·4U, VIXX LR, JJ Project, Jus2, NU'EST W, 이달의소녀 3종, MISAMO, 3RACHA 등 총 68개)
- [완료][artists.json] soloDiscography 추가: 빅스 전원·앰버·레이·디오(엑소)·아이즈원 4명·이찬혁 등
- [완료][artists.json] 나무위키 링크 45명 신규 추가 (라이즈·아이브·빅스·엔시티 드림·에프엑스·엑소 등)
- [완료][artists.json] 인스타그램 링크 추가 — 트리플에스 7명·사쿠라(르세라핌)·리센느 4명 등; 베이비몬스터/보이넥스트도어/엔하이픈/트레저 등 소속사 정책상 없음 확인
- [완료][artists.json] 제로베이스원 누락 멤버 4명 추가 (장하오·리키·김규빈·한유진) — artists.json 총 1,585명

## 2026-08-09~13

- [완료][artists.json] unitDiscography 필드 신설 — groups.json 246개 그룹 전수 서브유닛 조사·80개+ 유닛 멤버별 개별 적용 (NCT U는 앨범별 참여 멤버 분리)
- [완료][artists.json] soloDiscography 대량 추가 (슈퍼주니어 전원·씨스타·애프터스쿨·샤이니 키 등 총 80+명)
- [완료][kpop_universe.html] _PROJECT_UNITS 3개 추가 (아이린&슬기, SM THE BALLAD, NCT U)

