# CHANGELOG

여러 기기(회사/집 컴퓨터, 노트북)와 여러 Claude Code 세션이 공동으로 작업하는 프로젝트라, 세션 간 인수인계용으로 쓰는 로그.

## 사용 규칙
- 날짜별 역순(최신이 위), 같은 날 여러 세션 작업 가능
- `[완료]` `[진행중]` `[이슈]` `[보류]` 태그로 상태 표시
- 파일 수정 항목은 `[파일명]` 태그 추가 — 다른 세션이 충돌 여부 판단용
- 한 줄로 짧게, "뭘 했는지" 위주(자세한 이유/배경은 코드 주석에 있음)
- 새 세션 시작할 때: PRINCIPLES.md + 이 파일 최근 항목 먼저 읽고 시작할 것
- 작업 끝날 때마다 그 세션이 직접 이 파일에 한 줄씩 추가할 것
- **⚠️ git 상태 확인은 `git status`/`git log`만으로 하지 말 것 — 반드시 `git fetch origin`부터 먼저 실행.** 이 둘은 로컬에 캐시된 마지막 fetch 시점 기준으로만 판단해서, 그 사이 다른 기기/세션이 이미 push해둔 걸 놓치고 "최신"이라고 잘못 보고하는 사고가 실제로 있었음(2026-08-20 항목 참고). "다른 데서 작업한 거 반영됐는지 확인해줘" 같은 요청은 물론이고, **커밋하기 전·push하기 직전에도 항상 다시 fetch할 것.**

## DECISIONS (결정 대기 — 주간 자문 세션에서 처리)
- **"다녀왔어요"의 시간 단위 — 공연당 1회는 부정확(2026-08-28, 사용자 제보)**. 지금은 `concertLog`가 `{id, reaction, note, at}`이라 **공연 하나당 한 번만** 체크된다. 그런데 적재된 670건 중 **443건(66%)이 다일 공연**이고, 하루에 회차를 나눠 2회 하는 경우도 있다 → "8/2 막콘 다녀옴"과 "3일 다 감"이 구분이 안 된다. **선택지**: ⓐ**날짜 단위**(`{id, dates:[…]}`) — 대부분의 니즈를 덮고 UI도 날짜 칩 토글이면 끝, 회차는 못 나눔 ⓑ**날짜+회차**(`{id, sessions:[{date, slot}]}`) — 정확하지만 KOPIS가 회차 정보를 안 줘서 **사용자가 직접 입력**해야 하고 입력 비용이 확 오른다 ⓒ보류. **권장 ⓐ** — 회차까지 기록하고 싶은 사람은 한줄평에 적으면 되고, 그 니즈가 실제로 관측되면 그때 ⓑ로 올리면 된다. ⚠️`concertLog`는 `_sbPushColumn('concert_log', …)`로 서버에도 올라가므로 **형식을 바꾸면 기존 기록 마이그레이션이 필요**하다(옛 `{id}`를 `{id, dates:[공연 시작일]}`로 승격하는 백필).
- **나무위키로 공연 정보 보완 — 적중률 17%, 계속할지(2026-08-28)**. KOPIS는 국내 등록 공연만 다뤄서 해외 투어·소규모 팬미팅이 통째로 빠진다. 보완책으로 `tools/namu_events.mjs`를 만들어 A등급 대상 182개(그룹 23 + 솔로 디스코그라피가 있는 멤버 159)를 겨냥했는데, 12개 표본에서 **2개만 성공(뉴진스 2건·소녀시대 14건)**. 원인은 나무위키에 **문서 구조가 최소 세 가지**로 갈려 있다는 것: 표 형식(뉴진스) / 공연별 하위문단+정보상자(걸스데이) / 별도 `{이름}/콘서트` 문서(위너), 게다가 문서 자체가 없는 멤버가 다수. 파서 버그 4개를 고치고도 17%다 — KOPIS와 달리 **안정된 스키마가 없다**. 소녀시대 14건 중 5건은 중국 투어 행이 셀 병합이라 공연장명(`우타이산 체육관` 등)이 제목 칸에 들어갔다. **선택지**: ⓐ중단 ⓑ182개 전부 돌리고(≈17% 수확) 사용자가 검수 ⓒ**중요 그룹만 손입력**. **권장 ⓒ** — 2026-08-28에 어드민 "공연 추가" 폼을 만들어서 손입력 비용이 크게 낮아졌다(공연장 드롭다운·중복 경고·연속 입력). 공연 0건인 A등급 그룹부터 채우면 된다.
- **KOPIS 인증키 수명 관리(2026-08-28)**. 약관: 유효기간 **발급일로부터 1년**, **3개월 이상 미사용 시 승인 자동 취소**. 공연 수집이 수동 배치라 방치하면 키가 죽는다 → ①분기 1회 이상 수집을 돌리거나 ②만료 전 kopis@gokams.or.kr로 연장 신청. 정기 수집(cron/Action)을 붙일지 결정 필요.
작업 중 "이건 지금 판단 못 하겠다" 싶은 게 생기면 한 줄씩 여기 추가. 주간 자문(claude.ai) 세션 때 이 목록 + zip을 같이 던져서 처리하고, 처리된 항목은 지우면서 그날 날짜 항목으로 내려보낼 것. [[project_kpop_universe_git_setup]] 참고.
- **유저별 데이터 격리 미검증 — 소개글 게시 전 선행 확인(2026-08-26)**. 이번에 만든 `tests/rls.test.js`는 **로그인하지 않은 anon 키 기준**이라 "익명이 못 본다"까지만 확인한다. 검사하지 못한 것: **로그인한 유저 A가 유저 B의 데이터를 볼 수 있는가**(`user_data`의 즐겨찾기·컬렉션·프로필, `video_scraps`, `collection_likes` 등). RLS 정책이 `auth.uid() = user_id` 형태로 걸려 있으면 안전하지만, `atm_exception_rules`에서 실제로 발견된 `public write`(PERMISSIVE, cmd=ALL)처럼 **초기에 열어두고 안 조인 정책이 또 있을 수 있다**. 테스트가 이걸 자동으로 못 잡는 이유: 검사하려면 실서비스에 테스트용 auth 유저를 만들어야 해서 뺐음. **확인 방법**(둘 중 하나): ①Supabase 대시보드에서 `select tablename, policyname, permissive, cmd, qual, with_check from pg_policies where schemaname='public' order by tablename` 결과를 붙여주면 정책을 눈으로 훑어 판단 ②테스트 계정 하나를 만들어 그 세션 토큰으로 남의 행이 보이는지 실측. **언제**: 지금은 유저가 사실상 없어서 급하지 않지만, **커뮤니티 소개글 게시(병행 트랙) 전에는 반드시** — 유입이 시작된 뒤에 발견하면 이미 노출된 뒤다.
  - **[2026-08-26 준비완료 — 정책만 붙이면/토큰만 주면 즉시 판정]**
  - **경로② 자동화**: `tests/rls.test.js`에 **로그인 유저 간 격리 검사**를 추가함(읽기 전용이라 안전). 테스트 계정 2개의 access_token만 주면 바로 돈다: `KPU_TOKEN_A=… KPU_TOKEN_B=… node tests/rls.test.js` (토큰 없으면 자동 건너뜀). 검사 내용: 로그인 A로 **B 소유의 `user_data`·`collection_likes` 행이 읽히는지**(0행이어야 통과). ⚠️ 계정 B로 먼저 즐겨찾기 등을 만들어 둬야 판정이 유의미. access_token 얻는 법(브라우저 콘솔): `(await sb.auth.getSession()).data.session.access_token`.
  - **경로① 눈검사 루브릭**(pg_policies 결과 붙이면 이 기준으로 판정): 소유자 컬럼 — `user_data`=`user_id`, `public_collections`=`owner_id`, `collection_likes`·`video_scraps`·`video_reactions`=`user_id`. **정답 정책**: (a) `user_data`·`collection_likes`(비공개 per-user)는 SELECT/UPDATE/DELETE의 `qual`이 `(auth.uid() = user_id)`, INSERT의 `with_check`가 `(auth.uid() = user_id)`. (b) `public_collections`는 SELECT는 공개(공유 목적)여도 되지만 INSERT `with_check`/UPDATE·DELETE `qual`은 `(auth.uid() = owner_id)`. (c) `video_scraps`·`video_reactions`는 읽기는 공개(집계라 의도됨), 쓰기만 `user_id` 결속. **🔴 위험신호**: per-user 테이블에 `permissive=PERMISSIVE, cmd=ALL, roles={public}, qual=true` 류(=atm write 사고와 동형) / SELECT `qual`이 `auth.uid()`를 안 쓰고 `true`나 `auth.role()='authenticated'`만(=로그인한 아무나 전부 열람) / INSERT·UPDATE에 `with_check` 없음(=남 소유 행 쓰기 가능).
  - **곁다리 소견(해결)**: 위 테스트 실행 중 `atm_exception_rules` 익명 읽기 1행이 잡혔는데, 확인해보니 **정상**이었다 — `_loadDynamicAtmRules()`(index.html:~12530)가 로그인 무관 전원에게 매처 예외규칙을 로드해서 **공개 읽기가 필요**(내용은 태깅 설정 토큰, 유저 데이터 아님). 쓰기는 관리자만(RESTRICTIVE 3종) 확인됨. 테스트 기대값을 `read:'public'`으로 바로잡음(전엔 0행이라 우연히 통과하던 것).
- **라이브 solo 전수 스캔 — category 포함 재수출 후 Fable 감사(2026-08-23)**. 멤버 카드 "라이브 탭 only"(category=live + members가 본인 1명) 영상의 오태깅·커버리지(0건 멤버·그룹내 편중) 전수 점검이 필요. 근데 지금 CSV엔 category 컬럼이 없어 라이브 탭 필터 재현 불가 + 감사 스냅샷이라 Jin수정·소급재분류 미반영(낡음). **새 내보내기 스펙**(⚠️ channel_id 컬럼은 테이블에 없음 — 코드 0회 참조 확인, 채널단위는 group_ko로 대체): `id, group_ko, title, category, members, with_members, content_flag, tags_manual, published_at` — 라이브만이면 `where category='live'`로 필터. artists.json은 클로드코드가 레포에서 직접 읽으므로 zip 불필요, **CSV만** 주면 됨. 새 대화에서 "라이브 only 스캔 건"으로 이어서: ①멤버별 라이브 solo 커버리지 표(1,690명, 0건 목록·그룹내 편중=미태깅 신호, 그룹 라이브 총수와 대조해 "미태깅 의심"만 분리) ②오태깅 스캔(solo인데 제목에 타멤버·타그룹, 짧은영문/성뗀변형 잔재) ③channel_id로 채널 단위 계통 오염 특정. 산출물: 유형요약 + 멤버별 CSV(0건·의심 id).
- **connections.json "Yoona" 유령 참조 — 이름만으로 식별하는 구조의 한계 노출(2026-08-21)**. 소녀시대 윤아를 가리키는 6건이 "윤아"가 아니라 영문 표기 "Yoona"로 잘못 저장돼있는데, 단순히 "윤아"로 고치면 최근 데뷔한 아일릿에도 동명이인 "윤아"(Yunah)가 있어서 그룹 구분 없이는 어느 쪽인지 알 수 없어짐. connections.json 스키마에 그룹 구분자를 추가할지, 아니면 이번 건만 예외적으로 처리할지 설계 판단 필요.
- **"마크" 동명이인(엔시티 겸임 vs GOT7 마크 투안) — 매칭 테스트 작성 중 신규 발견(2026-08-21)**. `_m2ParseTitle`의 동명이인 충돌 로직(nameToGroups)이 "겸임 멤버 본인의 다중 소속"과 "서로 다른 두 사람이 같은 이름"을 person(아티스트 객체) 단위로 구분하는데, 그룹명 없이 "마크"만 있는 제목에서 이 둘이 동시에 걸리면(엔시티 마크의 겸임 2개 그룹 + GOT7 마크 1개 그룹) 겸임 인정 로직과 동명이인 충돌 로직이 상호작용하는 지점이라 실제로 어떻게 처리되는지 코드만으론 100% 확신 못함(테스트 결과는 null — 안전하게 버려지는 쪽으로 보이나 의도한 결과인지 설계 재검토 필요). 아직 실제 오염 사례로 확인된 건 아니라 급하지 않음.
- **멤버 카드 "실명(활동명 아닌)" 표기 — 전체 기능화 검토 필요(2026-08-21)**. 우즈(전 조승연) `name.ko`에 실명이 괄호로 같이 박혀있던 걸 정리하면서 사용자가 "멤버 카드에 활동명 옆에 실명을 작고 어둡게 표기하면 어떨지" 제안 — 확인해보니 지금 데이터 구조엔 "실명" 필드 자체가 아예 없음(디오=도경수처럼 유명한 사례도 안 보여줌, 1693명 전원 동일). 우즈만 유독 실명이 보이던 건 등록명에 실수로 같이 박혀있었던 부작용이지 의도된 기능이 아니었음. 한 명만 예외 처리하면 일관성이 깨지니, 하려면 ①실명 필드 신설(예: `realName`) ②어떤 멤버까지 채울지(전수 조사 규모 큼, 유명한 사람만 우선일지) ③멤버 카드 UI에 작고 어두운 보조 텍스트로 어디에 배치할지 — 셋 다 설계 필요한 별도 세션 규모 작업. 사용자가 퇴근 중이라 다음에 이어서 판단하기로 함.
- **백업전략 2층 — 대체불가 데이터 주간 덤프 루틴화(2026-08-22, 자문 백업전략 #2)**. `tags_manual` 검수분·`content_flag` 큐레이션·`name_match_whitelist`·유저 테이블을 매주 CSV/SQL로 내보내 별도 보관 — 오염이 몇 주 뒤 발견돼도 "몇 주 전 태그만" 부분 복원 가능(Supabase 일일백업은 7일 보존+전체 덮어쓰기라 이 서비스의 오염형 사고엔 거의 무력). ⚠️ 이 repo는 공개(GitHub Pages)라 유저 데이터(닉네임/이메일) 덤프는 반드시 비공개 repo나 로컬로. 일괄작업 사고 자체는 이번 undo가 커버하므로, 이 덤프는 그 외 큐레이션/유저 데이터 보존용. 루틴 주체·주기 확정 필요.
- **백업전략 3층 — 복구 리허설 분기 1회(2026-08-22, 자문 백업전략 #3)**. 덤프에서 태그 컬럼 복원을 실제로 한 번 해보는 리허설(백업은 복원해보기 전엔 백업이 아니라 희망사항). 첫 회는 절차 문서화. + 사용자 확인사항: Supabase 대시보드에서 일일백업 활성 여부 + "복원=DB 전체 덮어쓰기(그 사이 유저 즐겨찾기·컬렉션도 같이 날아감)"라는 점 숙지.
- ~~**약한 근거(weak) 매칭을 계속 `hidden`으로 감출 것인가(2026-08-25)**~~ → **A+B로 결정·반영 완료(같은 날)**. 아래 2026-08-25 항목 참고. 원문: 지금 정책은 "외부채널 + 그룹명 없이 멤버 이름만으로 역추론"이면 `needs_review:true` + `content_flag:'hidden'`으로 저장 → 사람이 검수해야 노출. 그런데 ①실측상 큐 3,051건이 대체로 정확하고(위험 이름 7%, 표본은 전부 맞음) ②그 대가로 **709건이 유저 화면에서 사라져 있으며** ③"사람이 검수한다"는 전제가 1년 가까이 안 지켜졌음(사용자: "안 누르게 되더라"). 2026-08-20 도입 당시엔 흔한단어·짧은영문·동명이인 dedup 게이트가 없어서 필요했지만 지금은 그것들이 앞단에서 위험을 다 걸러낸다. 선택지: **A)** hidden만 걷어내고 needs_review 플래그는 유지(유저에겐 다 보이되 어드민이 사후 감사) **B)** 해시태그로 멤버가 명시된 경우(전체의 22%)는 애초에 weak에서 제외 **C)** 현행 유지 + 큐를 주기적으로 비우기. A+B 조합이 유력하나 오염 재발 위험을 어디까지 감수할지는 판단 필요.
- ~~**정적 자산 캐시버스터 자동화(2026-08-25)**~~ → **①안(GitHub Action)으로 결정·구현 완료**(같은 날, Fable 자문 동의). 아래 2026-08-25 항목 참고. 원문: `kpop_universe.css?v=N`·`shared.js?v=…`·`sw.js`의 `CACHE_VERSION`을 전부 손으로 올리는데, 2026-08-24 Charts 가로선반 커밋에서 실제로 빠뜨려 폰이 "새 마크업 + 옛 CSS"를 그려 화면이 통째로 깨졌음(사용자 제보로 발견). 사람 규율로는 또 빠짐. 선택지: ①이미 도는 SEO 리빌드 GitHub Action에 "css/js 파일 해시 → `?v=` 치환 + CACHE_VERSION 갱신" 스텝 추가(자동, 커밋 diff가 매번 생김) ②pre-commit 훅(로컬 전용이라 여러 기기 세팅 필요) ③배포 전 체크리스트 문서화(비용 0, 실효성 낮음). 어느 쪽으로 갈지 판단 필요.
- **SEO: 탐험 모음/큐레이션을 검색에 노출(2026-08-22 사용자 아이디어)**. "탐험 패널 모음 제목을 구글/네이버에 치면 우리 사이트가 나오면 좋겠다." 현실: ①임시 모음(역대 TOP·수록곡 등)은 **고정 URL 없고 JS로 그려지고 3분마다 회전**이라 그대로는 검색 노출 불가(특히 네이버는 JS 콘텐츠 거의 못 봄). ②단 **사이트 발견 자체는 이미 됨** — 정적 그룹/멤버 페이지 1,949개(`g/*`, 각 title/description/canonical 완비) + 사이트맵 3,911 URL + 네이버 인증파일(`naverb9f...html`)로 서치어드바이저 등록됨. 하려면: **내용 고정된 대표 모음만 정적 SEO 페이지로 생성**(`build_group_pages.js` 방식 재활용, 예 `/collections/역대-여돌-직캠-top100/` 고정URL+HTML+사이트맵) → 검색 유입→앱 진입. 유저 공개 컬렉션 정적화는 저작권/모더레이션 고려 필요. **급하지 않음** — 그룹/멤버 페이지가 검색에서 자리잡는 걸 지켜본 뒤 모음 페이지를 더하는 순서 추천. 주간 자문 때 "SEO로 뭘 노출할지" 우선순위 정할 것.

- ~~**`active:false`인데 탈퇴일(`left`)이 없는 멤버 104명 — 그룹 미태깅 영상이 그 멤버 카드에 하나도 안 뜸(2026-08-27 발견)**~~ → **같은 날 처리 완료** — 폴백 4단계 재설계로 차단 110명→11명, 99명 복구. 아래 2026-08-27 항목 참고. ⚠️ 원 진단("탈퇴일 미기입이 원인")은 **틀렸었다** — 실제 원인은 `active` 필드가 "이 그룹 멤버인가"와 "개인이 아직 활동하는가" 두 뜻으로 섞여 쓰인 것이었고, 쥬얼리·걸스데이·스텔라에서 같은 시기 멤버가 은퇴 여부에 따라 갈리고 있었다.
---

## 2026-08-31

- [완료·SQL대기][admin.js][tag_edit_log_migration.sql][tests/tag-edit-log.test.js][tools/m2_harness.js] **수동 편집 이력 기록(`tag_edit_log`) — "학습하는 태깅 파이프라인"의 토대**(사용자 요청: "내 수동 태깅 편집 결과를 보고 학습해서 로직을 계속 고도화하는 시스템"). 문제: 지금까지 수동 편집은 그 행에 `tags_manual=true` 플래그만 남기고 **무엇을 무엇으로 고쳤는지는 어디에도 안 남았다** — 자동 태깅이 뭘 틀렸는지에 대한 가장 값진 신호(사람의 정답 라벨)가 편집할 때마다 그대로 버려지고 있었음. `tag_edit_log`(video_id, title, before jsonb, after jsonb, changed text[], source, editor, created_at) 신설 + `_tagEditLog(entries)`(테이블 없으면 조용히 no-op, 편집 자체는 절대 안 막음)·`_tagLogDiff`/`_tagLogSame`. **배선 7곳**: 편집 모달 단일저장(`modal_single`)·일괄저장(`modal_bulk`, 쓰기 직전 before 별도 조회)·영상관리 단일 플래그(`vm_flag`)·일괄 플래그(`vm_bulk_flag`, `_vmRows`에서 before)·카드 보류/숨김/무관 3버튼(`card_flag`). diff는 **배열 순서 무관 비교** — members는 저장 순서가 매번 달라서 그냥 비교하면 안 고치고 저장만 눌러도 로그가 쌓여 학습 재료가 노이즈로 덮임. `after`에 없는 필드는 판정 대상 제외(일괄 편집이 손댄 필드만 넘기므로). 제목은 호출부가 못 넘기면(카드 그리드는 DOM에 제목이 없음) 헬퍼가 한 번에 채움. 회귀 `tests/tag-edit-log.test.js` 20/20, matching 97/97·cover-resolve 45/45·hold-flag·vm-panel 통과. **⚠️ `tag_edit_log_migration.sql` 실행해야 실기록**(RLS 어드민 이메일 기반). ※다음: 쌓인 이력으로 ①골든셋 회귀(현재 매처 vs 사람 판단 일치율) ②규칙 후보 자동 제안(반복 제거되는 이름 → name_match_whitelist 승격).
- [완료][index.html] **탐험 패널에서 영상 열었다 닫으면 패널까지 꺼지던 버그**(사용자 제보 — "아래로 스와이프해서 영상 끌 때도 밑에 탐험 패널은 열려 있으면 좋겠다"). 원인: 자동으로 열린 탐험 패널을 "패널 아닌 걸 건드리면 닫는" **capture 단계** `pointerdown` 핸들러의 제외 목록에 `#yt-lightbox`가 없었다. ⚠️라이트박스에 이미 걸린 `pointerdown` stopPropagation은 **bubble**이라 capture를 못 막고, 터치 기기에선 `touchstart` 앞에 `pointerdown`이 먼저 오므로 **아래로 당겨 닫든 상단 버튼을 누르든 영상을 건드리는 순간** 밑의 패널이 닫혔다(`_dismissOnboardHint`→`_slideDownCloseFeed`). 라이트박스는 **탐험 패널에서 열린 것**이라 그걸 만지는 건 "패널 아닌 걸 건드린" 게 아니므로 제외 목록에 추가. 헤드리스로 **수정 전 재현(`feedOpen:false`) → 수정 후 유지(`feedOpen:true`)** 대조 확인. ※이 제외 목록이 문제가 된 건 세 번째다(2026-08-30 `#gc-totop` "top버튼 눌러도 닫힘"에 이어) — 탐험 패널 위에 새 오버레이를 띄우는 기능을 만들 때마다 여기 추가할 것.
- [완료·실기기확인필요][index.html][kpop_universe.css] **탐험 패널 탭을 X(트위터)식 언더라인으로**(사용자 요청 — "배경 거의 안 띄고, 두 탭이 딱 반씩, 활성 탭에 밑줄"). 기존엔 알약 버튼(배경+테두리, `flex:0 0 auto`)이라 존재감이 컸다. `#feed-tabs`를 gap·좌우패딩 0으로 두고 `.feed-tab`을 `flex:1 1 0`으로 바꿔 **폭을 정확히 반씩**(실측 195+195=390), 배경·테두리 제거, 활성은 흰 글자 + 밑줄. ⚠️**밑줄은 버튼이 아니라 안쪽 `<span>`에 건다** — 버튼에 걸면 반쪽 폭 전체(195px)에 그어져서 X처럼 안 보인다. 글자 폭(73px)에 맞아야 해서 라벨을 span으로 감쌌다(라벨은 i18n 대상이 아니라 안전). 하단에 hairline 구분선 추가, 밑줄은 3px 라운드+스케일 트랜지션. ※`.seg-control`(알약)과는 다른 카테고리 — 이건 화면 전환용 최상위 탭이고 seg는 목록 안 필터다([[세그먼트 컨트롤 컴포넌트]]). 헤드리스(390px) 검증: 반반·밑줄 글자폭·탭 전환 정상. ⚠️**발견(기존 문제)**: `#ob-top-search`(z41)가 `#feed-overlay`(z39)보다 위라, 검색 힌트가 떠 있는 상태에선 탭 밑줄을 가린다. 정상 흐름에선 `closePanels()`가 검색을 닫아서 같이 뜨지 않지만, 실기기에서 한 번 확인 필요.
- [완료·실기기확인필요][index.html][kpop_universe.css] **모바일 줌 컨트롤 노출**(사용자 요청 — "우주만 보일 때는 줌레벨 조절 바가 보이는 게 좋겠다"). 기존 `#zoom-ctrl` 슬라이더는 이미 있었는데 `@media(max-width:768px){display:none}`으로 **의도적으로 숨겨져 있었다**(핀치 줌이 있으니). 새로 만들지 않고 그걸 조건부로 노출. ⚠️**그냥 보이게만 하면 안 된다** — 드래그 핸들러가 전부 `mouse*`라 터치로는 안 움직인다. `touchstart/move/end`를 따로 얹었고(포인터 이벤트로 통합하면 마우스 경로 회귀 위험), **`stopPropagation` 필수** — 안 하면 같은 터치가 아래 캔버스(OrbitControls)로 흘러가 우주가 같이 돌아간다(`touch-action:none`도 같은 이유). 노출 조건은 **뭐라도 열려 있으면 숨김**: `body.feed-open`(탐험)·`#mob-sheet.bs-open`/`#mob-card-stack.bs-open`(카드)·`#yt-lightbox.open`·`#tabbar.search-mode`·설정/프로필/연결 패널. 하나라도 빠뜨리면 다른 화면 위에 떠버린다. 위치는 하단 탭바 위 **오른쪽 끝**(엄지 닿는 쪽, 데스크톱은 왼쪽 그대로), 터치 타깃 확보로 썸 10→18px·트랙 96→110px. 헤드리스(모바일 폭) 검증: 우주만 보일 때 `flex`·탭바 안 가림, 탐험/카드/라이트박스 열면 각각 `none`, 닫으면 복귀.
- [완료][admin.js][tests/desc-evidence.test.js] **영문 약칭이 약한 그룹은 "게스트 근거"로 인정 안 함**(사용자 승인). `_GROUP_WEAK_EN_AS_GUEST` 신설 — **방탄소년단(BTS)**: BTS가 behind the scenes 약자로도 쓰여 영문 토큰만으로 콜라보를 판정하면 오탐이 쏟아진다(실측 `with_groups` 329건 중 **174건(53%)이 제목에 한글명도 `#BTS`도 없음**, 표본은 전부 `bts of`·`@BTS` 멘션·플레이리스트 나열). **위너(WINNER)**: 영어 단어 winner와 철자가 같다(실측 59건 중 **55건(93%)**, 전부 `the Winner Is?`·`Remember the winner of that night?` 시상식 클립). ⚠️**면제 기준은 "위치(primary냐)"가 아니라 "다른 그룹이 같이 잡혔는가"다** — 위치로 걸었더니 `HITGS ver. Countdown! BTS #힛지스`에서 방탄이 primary·힛지스가 게스트로 **뒤집혀 잡혀서 규칙이 무력화**됐다(실측으로 발견하고 수정). 방탄만 잡힌 영상(`BTS - Dynamite @ MAMA`)과 자체 채널(selfGko)은 그대로 두므로 `group_ko` 단독 매칭분(방탄 2,329건·위너 860건)은 손대지 않는다. 적용 후 재확인: `라이즈×방탄`·`유니스×방탄`·`아이브×위너` 쌍이 사라지고 남은 건 진짜 콜라보(`신화 민우 & BTS 정국 Celeb Bros`, `2019 MAMA Reaction Cam`). 테스트 39/39, matching 97/97. ※`#BTS_SWIM #방탄소년단 @BTS`처럼 **해시태그로 편승**하는 경우는 여전히 통과 — 별개 문제로 남음.
- [완료][SEO][build_group_pages.js][tests/static-links.test.js] **관계 페이지 `/cover/` 신설 + 콜라보는 보류**(SEO 4단계). 이 서비스만 가진 데이터로 만드는 페이지 — "방탄소년단 커버"류는 경쟁이 사실상 0이고 우리에겐 태깅 36만 건이 있다. 그룹당 한 페이지에 **양방향**을 담았다(이 그룹이 커버한 곡 / 다른 아티스트가 커버한 이 그룹 곡 — 방탄 388건·스트레이키즈 152건). **95개 그룹 × 2언어 = 190페이지.** ⚠️**DB 태그를 그대로 믿지 않는다** — 검색엔진에 색인되는 페이지라 `tools/m2_harness`로 배포 매처를 불러 **지금도 그 관계가 성립하는 것만** 싣는다(판정 로직을 새로 짜지 않음). 이름이 서로 부분문자열인 쌍은 제외(`아이들 × 제국의아이들` 54건·`시크릿 × 시크릿넘버` 32건이 상위에 떴었다 — 후자는 이번에 새로 발견). ⚠️**콜라보(`/collab/`) 페이지는 생성기는 완성했지만 기본 비활성**(`REL_COLLAB=1`로 켬) — `with_groups` 태깅이 아직 색인시킬 품질이 아니다. 실측 잔여 오탐: `방탄소년단 × 위너` 6건("the Winner Is?"의 **영어 단어 winner**), `방탄소년단 × 유니스` 5건(`@BTS` 멘션), `라이즈 × 방탄소년단`(`Fame MV bts`). 반면 `cover_of`는 표본 검수 결과 깨끗해서(커버 댄스·`원곡:X` 등 명시적 근거) 커버만 냈다. **관계 페이지가 오염 거울 역할을 한다** — 다른 화면에서는 안 보이던 태깅 문제가 여기서 드러난다.
- [완료][admin.js][tests/desc-evidence.test.js] **`bts of ○○` = behind the scenes 오탐 차단**. 위 관계 페이지 작업에서 발견 — `라이즈 × 방탄소년단` 22건의 제목이 전부 `bts of RIIZING trailer` 류였다. 실측: 제목에 `bts of`가 든 62건 중 **55건이 방탄소년단 태그**였고 전부 오탐. 관용구라 정밀하게 걷어낼 수 있어서 `_atmStripCommonNounCtx`에서 `bts`(뒤에 `of`가 올 때만) 제거 — BTS 전체를 해시태그 전용으로 묶으면 정상 태깅이 대량으로 죽는다. ⚠️**미해결**: `of` 없는 bare `bts`(`Fame MV bts 1`·`Bootleg BTS`)는 아직 안 잡힌다. 실측 판단 근거 — `with_groups`에 방탄소년단이 든 **329건 중 174건(53%)이 제목에 `방탄소년단`도 `#BTS`도 없다.** "콜라보 근거로는 영문 BTS만으론 부족하고 한글명/해시태그가 있어야 한다"는 규칙이 후보인데, 174건이 걸린 판단이라 사용자 결정 대기.
- [보류·실행시점][P0] **"📅 데뷔 이전 영상 정리" 버튼은 지금 누르지 않는다**(사용자 결정 2026-08-31). 이 스윕이 보류로 보내는 508건의 상당수가 **아직 등록 안 된 그룹**의 영상(제국의 아이들 97·M4·9초 등)이라, 지금 보류로 밀어놨다가 그룹을 등록하면 또 꺼내서 재판정해야 한다. **보류 탭에서 신규 그룹을 실제로 등록하는 작업과 같이 돌리는 게 맞다**(LEARNING_LOOP P4와 함께). 게이트 자체(`_m2DebutBlocks`)는 이미 켜져 있어 **앞으로 들어오는 영상은 지금부터 막힌다** — 급한 건 없음.
- [완료·실행대기][P0][admin.js][index.html][tests/debut-gate.test.js] **데뷔 이전 게이트 + "📅 데뷔 이전 영상 정리" 버튼**(LEARNING_LOOP P0). "영상이 그룹 데뷔보다 한참 전에 올라왔으면 그 그룹일 수 없다" — 탈퇴 이후(`_atmLeftBefore`)·해체 컷오프는 있었는데 반대쪽 끝이 비어 있었다. `_m2DebutBlocks`(여유 **3년**)를 `_m2ParseTitle`의 **두 출구 모두**(literal 매칭 후보 / 역추론 결과)에 걸었다 — 역추론이 원래 가장 약한 경로라 거기가 더 중요하다. 임계값은 추측이 아니라 실측으로 골랐다(2016년 이전 12,536건 대상: −0년 783 / −1년 682 / −2년 581 / **−3년 508** / −5년 374 / −10년 126). 사용자가 처음 제안한 −10년은 126건밖에 못 잡고, −3년이면 데뷔 직전 오디션 예외(대부분 2년 이내, 최장 3년 SM루키즈→NCT)를 다 덮는다. ⚠️**이 게이트의 진짜 값어치는 연도 필터가 아니라 부분문자열 오배정 탐지**다 — −3년 미처리분 357건 중 최대가 `아이들`(2018 데뷔)에 붙은 **제국의 아이들(ZE:A) 2012년 영상 97건**이고 이건 `B.I`⊂`B.I.G`와 같은 병(이즈나←M4 30건, 온리원오프←9초 19건, 엔하이픈←박재범 10건도 같은 계열). 기존 오염은 새 버튼이 **'보류'로** 보낸다 — '무관'이 아닌 이유는 대개 "우주 밖"이 아니라 **아직 등록 안 된 그룹**의 영상이라 나중에 등록되면 재판정 대상이어야 하기 때문. 이미 플래그된 것·`tags_manual`·owner 채널(idol/fans tier)은 제외. 테스트 14/14(정상 영상이 안 걸리는지도 함께 검증), matching 97/97. ⚠️`tests/matching.test.js`는 harness를 안 쓰고 자체 슬라이스 목록을 갖고 있어 **거기에도 추가**해야 한다(안 하면 97건 전부 `is not defined`로 실패 — 실제로 겪음).
- [수정][tests/shorts-promote.test.js] **정리 커밋의 여파 — 지워진 SQL 파일을 읽던 테스트 복구**. 어제 실행 완료된 마이그레이션 12개를 지우면서 `is_short_migration.sql`을 읽던 `shorts-promote` 테스트가 ENOENT로 죽었다(정리 시 이 테스트를 안 돌려서 못 잡음). 스키마의 근거는 이제 파일이 아니라 라이브 DB이므로 **SQL 파일 대조 3건을 제거**했다 — 지워진 파일을 계속 읽으면 죽기만 하고 실제 어긋남은 못 잡는다. ※`feed-trend`·`admin-dock` 실패는 이 작업과 무관한 기존 실패(변경 전 트리에서 stash로 동일 재현 확인).
- [완료·스윕대기][admin.js][TAGGING.md][tests/desc-evidence.test.js][tools/m2_harness.js] **"그룹 전체 무대인데 멤버 한 명만 체크" 원인 두 가지 수정**(사용자 발견 — 탐험 패널 Discover의 CHART TOP에서 위너=송민호, 아이콘=B.I, 하츠투하츠=예온). 매처를 실DB 표본에 직접 돌려 근거 위치를 제목/설명으로 갈라보니 원인이 **둘**이었다. ①**설명란의 "출연이 아닌 언급"을 출연 근거로 읽음** — 위너 표본 40건 중 **30건이 제목엔 근거가 없고 설명란에서만** 매칭됐고, 걸린 문자열이 전부 앨범 트랙리스트(`・FIANCE (MINO)`)·멀티앵글 목록(`・EVERYDAY_YOON / JINU / HOONY / MINO`)·관련영상 링크(`MINO SOLO M/V @ http://…`)였다. **한 명만 붙는 이유**는 `MINO`만 등록 영문명이고 `YOON`·`JINU`·`HOONY`는 별명이라 로스터에 없어서 — 넷이 다 적힌 목록인데 하나만 걸린다. `_ATM_DESC_NOISE_LINE`+`_atmStripDescNoise`로 **줄 단위 포맷 마커** 기준으로 제거(이름 목록이 아니라 마커라 새 사례에 일반화되고, 출연자 해시태그 줄은 마커가 없어 그대로 살아남는다). ②**`B.I.G`(비아이지)가 아이콘 `B.I`로** — `_atmTokenize`가 `B.I.G`를 `b/i/g`로 쪼개 `['b','i']`가 앞 두 토큰과 맞아떨어졌고, 한글 경계 규칙 `(?<![가-힣])B\.I(?![가-힣])`도 다음 글자가 '.'이라 통과시켰다. 비아이지는 GROUPS 미등록이라 막을 그룹명도 없어 **group_ko까지 아이콘으로 오배정**(제목에 `비아이지` 34건·`B.I.G` 38건). 이니셜형 이름은 토큰 대조 대신 **원문에서 양옆에 점/영숫자가 없을 때만** 인정하도록 변경 — `B.I.G`는 탈락하고 `B.I X BOBBY`·`#B.I`는 통과(토큰 방식으론 이 둘을 못 가른다). 실측 재측정: 위너 설명만 30→18, 하츠투하츠 15→8, 아이콘 둘다아님 24→27. 테스트 24/24, 회귀 15종 통과. ⚠️**앞으로 붙는 태그만 고쳐진다 — 기존 오염은 "③ 자체 멤버 태깅 재검증(전체)"를 돌려야 정리된다.** ※비아이지는 사용자가 수동으로 보류 처리 후, 보류에 쌓인 그룹들과 함께 차후 일괄 등록 예정. ※harness 함정 2건 추가로 겪음(아래 검증 항목).
- [완료][정리][admin.js][index.html][TAGGING.md][SQL] **레포 정리 3건**(사용자 요청). ①**실행 완료된 SQL 12개 삭제** — `admin_bulk_snapshots`·`evan_rename`·`fav_order`·`is_short`·`melon_top100_seed`·`photo_collection_public`·`source_channel`·`spotify_milestones`(2)·`tag_edit_log`·`tag_review_queue`·`trace_week_rpc`. ⚠️짐작으로 지우지 않고 **anon 키로 테이블/컬럼 존재를 하나씩 실측**한 뒤 삭제(예: `user_data?select=fav_order`가 200이면 적용된 것). **`db_indexes_migration.sql`만 남겼다** — 인덱스는 REST로 존재 확인이 안 되고 메모리상 미실행이라 판단 불가. (git 히스토리에는 그대로 남아 복구 가능. `events.sql`은 애초에 추적 안 된 로컬 파일.) ②**어드민 버튼 4개 제거** — 죽은 버튼(리스너 없는 것)은 76개 중 **0개**였고, 실제로 뺄 것은 "역할이 끝난 것"이었다: `원곡 재분류 1차`·`2차`(오염 1,450건을 만든 세대차 휴리스틱, v2가 대체) · `커버곡 원곡 제목 자동 추출`(v2가 `cover_of_song`까지 채움) · `연도별 TOP 최초 백필(일회용)`(2026-08-12 실행 완료). 함수·리스너·마크업·힌트 전부 제거. `고아태그 정정`·`잠금-빈값 채우기`·`원곡 후보 검토 패널`은 **재실행 가능한 상시 도구라 유지**. ③**`FANCAM_PATTERNS.md` → `TAGGING.md`로 확장**(`git mv`로 이력 보존) — 직캠 카탈로그는 6장으로 그대로 살리고, 태그 컬럼 사전·파이프라인·불가침 원칙·방어 게이트 지도·한 사람=한 태그·상태 플래그·어드민 버튼 순서·사고 이력·미해결·검증 방법을 더해 282줄. 태깅을 손볼 때 먼저 읽고 고친 뒤 갱신하는 문서.
- [완료·실행대기][admin.js][tests/dual-group-tags.test.js] **동명이인 교차 태그는 제목에 그룹명이 있을 때만 + 대표 그룹은 프로젝트 그룹보다 정규 그룹**(아래 겸임 수정의 후속, 사용자 요청). ①**동명이인 교차 태그 게이트**: 자동 정리에서 "다른 사람이라 정상일 수 있음"으로 살려둔 124건을 실측하니 **114건(92%)이 제목에 그 그룹명이 아예 없었다** — 엑스원 김우석 영상에 판타지보이즈 김우석 52건, **밴드 LUCY** 영상에 위키미키/우아 루시 15건(이건 group_ko도 틀림), 유주(체리블렛) 9, 레나(프리스틴) 8. 근거가 이름뿐이면 매칭이 아니라 우연이므로 **제목에 그 그룹명이 literal로 있을 때만 인정**(`_ATM_HASHTAG_ONLY_NAMES`와 같은 원칙). ⚠️범위는 **이 영상 members에 같은 이름이 있을 때**(=홈 로스터와 충돌)로 좁혔다 — 그게 실측으로 오탐이 확인된 구간이고 넓히면 근거를 못 잰 정상 태그까지 날아간다. ②**대표 그룹 선택에 `projectRing` 반영**: 이대휘·박우진·최유정의 artists.json 주 소속이 각각 워너원·워너원·아이오아이(전부 `projectRing:true` 프로젝트 그룹)라 그대로 두면 현 소속(에이비식스·위키미키) 대신 옛 소속이 남았다. 순서를 ①제목 literal ②`repGroup`(사람이 못박은 값) ③**정규 그룹(비-projectRing)** ④주 소속으로 바꿈 — groups.json에 이미 있는 표식을 쓰므로 사람이 채울 게 없다. 실데이터 시뮬 **140 → 254행(4.2%)**. 테스트 25/25, 회귀 13종 통과.
- [완료·실행대기][admin.js][index.html][tests/dual-group-tags.test.js][tools/m2_harness.js] **겸임(이중소속) 멤버 더블 태깅 수정 — `_normalizeMemberTags` + "👥 겸임 멤버 중복 태그 정리" 버튼**(사용자 제보 — "사실상 동일인물인데 이중 소속인 친구들 더블 태깅이 너무 많이 됐다"). 실측(with_members 있는 6,000행 표본): 같은 사람이 두 그룹으로 중복 **99행**, members에 이미 있는 사람이 with_members에도 **164행**(동일인물 57 + 솔로 자기키 49 + 동명이인 124는 정상). **원인은 2026-08-26 "부소속 태그가 조용히 버려지던 버그" 수정의 부작용** — 매처가 전 소속(`_artistGroups`)을 보게 됐는데 **"한 사람당 태그 하나"** 규칙이 같이 안 들어가, 이진혁이 `이진혁(업텐션)`+`이진혁(엑스원)`으로 두 번 붙었다. 규칙 두 개(①같은 사람이 여러 소속 → 대표 하나 ②members에 이미 있는 사람 제거)를 `_normalizeMemberTags` 한 곳에 두고 태그 생성 지점 **4곳** 전부 통과시킴(동기화·직캠 재검증·그룹 재배정·수동 추가). ⚠️**동명이인은 절대 합치지 않는다** — 세븐틴 민규와 동키즈 민규는 다른 사람이라 `_amtSamePerson`이 **artists.json 한 항목이 두 그룹을 다 갖고 있을 때만** 동일인물로 본다(테스트로 못박음). ⚠️`승한(승한)` 표기는 **버그가 아니다** — 솔로는 group_ko 키가 본인 이름(`_ytGroupKoFor`)이라 정상 표기이고 읽는 쪽도 그렇게 파싱하므로, 라벨을 바꾸면 마이그레이션이 필요해진다. 그 49건의 진짜 문제는 members와의 중복이라 규칙 ②가 잡는다. 대표 그룹은 ①제목에 literal로 언급된 그룹 ②`repGroup` ③주 소속 순(제목을 먼저 보는 이유: 마시로가 케플러 영상에선 케플러, 메이딘 영상에선 메이딘이어야 맞음). 실데이터 시뮬 **140/6,000행(2.3%)** 변경. ⚠️**데이터 이슈 발견**: `repGroup`을 쓰는 아티스트가 0명이라 대표는 전부 주 소속을 따라가는데, **이대휘 주 소속이 워너원(해체), 한유진이 앤더블(유닛)**로 박혀 있어 현 소속보다 옛 소속이 남는다 — `repGroup`으로 못박아야 함. ⚠️**harness 함정**: 파라미터 구조분해(`function f({a,b})`)를 쓰면 `tools/m2_harness.js`가 선언 뒤 첫 `{`를 본문 시작으로 봐서 잘리고 회귀 테스트가 통째로 깨진다(실제로 겪음) — 슬라이스 대상 함수엔 쓰지 말 것. 테스트 16/16, 회귀 11종 전부 통과.
- [완료][admin.js][index.html][kpop_universe.css] **원곡 청소 확인창 — 미리보기 숫자를 confirm 밖으로 + 앱 자체 다이얼로그로 교체**(사용자 제보: "버튼 누르면 취소됨 뜨고 미리보기만 나오는데 어떻게 확인하냐"). 문제 둘: ①미리보기 숫자가 `confirm()` 문구 안에만 있어서 취소하면 "취소됨" 한 줄로 덮이고, 숫자를 보려면 F12를 열어야 했다 ②브라우저가 "추가 대화상자 생성 방지"를 걸면 `confirm()`이 **대화상자 없이 즉시 false**를 반환해 미리보기조차 못 본다(취소를 누른 것과 화면상 구분이 안 됨). 조치: confirm과 **독립적으로** `_ytSetProg`에 요약을 먼저 띄우고(취소해도 그대로 유지 + "다시 눌러 정리 실행을 선택하세요" 안내), 확인은 앱 자체 `_confirmDialog`를 우선 사용(없으면 native confirm 폴백). 공용 컴포넌트라 범위 먼저 확인 — 기존 호출부 4곳은 전부 한 줄짜리 짧은 메시지라, `white-space:pre-wrap`(줄바꿈 보존)과 `wide` **옵트인** 모드(460px + 긴 요약 스크롤)만 얹어 기존 호출은 안 건드림. `_confirmDialog`는 index.html 본문(classic script) 전역이고 admin.js도 classic으로 주입돼 호출 가능함을 확인(`_flagPatch` 등과 동일 경로).
- [완료·실행대기][admin.js][index.html][tests/cover-cleanup.test.js][tools/m2_harness.js] **"🧹 원곡 오탐 청소" 버튼 신설 + 옛 재분류 1차/2차 은퇴**. 아래 감사에서 나온 오염을 걷어내는 스윕(`_ytSweepCoverCleanup`). 판정: cover_of가 붙은 행 중 ①커버 문맥(`_coverContext`)이 없고 ②**현재 매처(`_coverResolve`)에게 cover_of를 지운 상태로 다시 물었을 때도 커버 근거를 못 찾는** 행만 건드린다. ⚠️**"커버 문맥 없으면 오탐"이라는 중간 결론은 실측으로 폐기했다** — 음악방송 커버 무대는 제목에 '커버'라는 말이 없는 게 정상이라(리센느 '다시 만난 세계'→소녀시대, 아이브 가을 'Pretty Girl (카라)'→카라, 다크비 'LE SSERAFIM - The Hydra'→르세라핌) 문맥 게이트를 걸었으면 정상 커버 5,087건이 날아갈 뻔했다. 정리는 두 갈래 — 콜라보 동반신호가 있으면 `cover_of`→`with_`로 **되돌리고**(옛 휴리스틱이 게스트를 원곡자로 강등시킨 것이라 그냥 지우면 콜라보 정보가 영영 사라짐), 없으면 해제. **되돌리기 게이트는 `_coverHasCollabSignal`을 못 쓴다** — 시뮬에서 "Good Boy Gone Bad - TOMORROW **X** TOGETHER"의 그룹명 속 X가 콜라보로 읽혀 지디·태양을 콜라보로 되돌리려 했다. x/×/vs를 뺀 좁은 `_coverRestoreSignal`을 따로 만들고 회귀 테스트로 못박음(cover-cleanup 17/17). **실데이터 시뮬(차트 사전 1,254행까지 실제와 동일하게 로드): 정리 1,439건(되돌림 354 / 해제 1,085) · 유지 5,400건(정상 커버 5,087 + 매처가 커버라 판정 313) · 수동보호 192.** 표본 콘솔 → confirm → 스냅샷. 함께 **옛 "원곡 재분류 1차/2차" 버튼을 disabled로 은퇴**(이 오염을 만든 장본인이라 그대로 두면 청소하고 또 눌러 재오염된다 — 패널 힌트에 은퇴 사유 명시). node --check + 회귀 8종 전부 통과.
- [감사·조치대기] **원곡 태깅 v2 실DB 전수 감사 — 커버 문맥 없는 자동 cover_of 1,931건**(사용자 제보: "그냥 제목에 충분히 들어갈 만한 단어들에도 다 원곡 태깅이 걸려서 또 오류가 엄청 생겼다"). cover_of_* 붙은 **7,031건** 전수 조회(anon 키, keyset 페이징 — OFFSET 4000+는 statement timeout) 결과 제목에 커버 문맥(커버/cover/원곡/챌린지/covered by/Original Song by)이 **없는 게 2,255건(32%)**, 수동확정 711건 빼면 **자동 오탐 1,931건**. 유형: 멘션·해시태그 891(`#오하영 선배님`·`@BTS` → 그 사람이 원곡자로) · 곡명 없이 원곡자만 495(옛 "커버키워드+6년선배" 세대차 휴리스틱 잔재) · 음악방송 자기무대 77 · 직캠 37 · 시각표기 34(`2024.04.06 2PM / 7PM` → **2PM**) · 기타 388. v2 자체 버그도 확인: `Good Boy Gone Bad`(TXT 곡) → 지디/태양, `Kissing You`·`Too Bad`·`Artist`·`Nobody`·`Special`·`Weekend`·`DNA`·`Killer` 등 흔한 어구가 곡명으로 매칭. **근본 원인은 `_COVER_COMMON_KEYS`가 손으로 적은 ~180단어 Set**이라 새 곡·새 어구를 영원히 못 따라가는 것([[태깅 파이프라인 구조적 비대칭]]과 같은 병). 게다가 `strength==='quote'`는 커버 문맥만 있으면 흔한단어도 통과. ⚠️**감사 1차 수치는 폐기**(문맥 판정을 손으로 짠 정규식으로 해서 `\bcover\b`가 "Covered"를 못 잡고 "Original song by"도 빠져 정상 커버를 오탐으로 셌다 — 1,931건은 과다계상). **배포 코드의 `_coverContext`로 재감사한 확정 수치**: 문맥 없음+자동 1,722건 중 **1,450건은 현재 매처도 커버로 안 보는 옛 로직 잔재**(→ 위 청소 버튼이 처리), **272건은 매처가 지금도 커버라 판정**. 그리고 그 272건은 대부분 원곡 로직 문제가 아니었다 — **88건은 group_ko가 제목에 아예 없는 오배정**(화사 직캠이 `group_ko=몬스타엑스`로 박혀 있어 `_coverIsSelf`가 자기 곡인 걸 못 알아보고 커버로 판정 / 윤지성→스테이씨, 싸이→투애니원, 나인투식스→온리원오프)으로 **"② 오태깅 그룹 재배정"의 몫**이고, 184건 중에도 정상 커버가 다수다. 즉 **보류 더미(오디션 참가자 영상이 원곡 그룹으로 배정)와 같은 병**이다. 남은 진짜 커버 로직 버그는 소수 — 코너명을 곡명으로(쇼챔 "수고했어, 오늘도"→악뮤), 부분문자열(`group_ko='아이들'`이 "제국의 아이들"에 걸림), 동명곡(코코몽 "더주세요(Yummy-Yummy)"→에이핑크). **미착수**: 흔한단어를 손목록(`_COVER_COMMON_KEYS`)이 아니라 실DB 통계로 자동 산출.
- [완료·SQL제공][데이터] **브브걸 은지 옛/새 키 병합**(사용자 요청). 그룹 키 개명(`브브걸(브레이브걸스)`→`브브걸`) 잔재로 `cover_of_members`에 `은지(브브걸(브레이브걸스))`와 `은지(브브걸)`가 **같은 행에 둘 다** 들어 있었다. 실측 19건, **19건 모두 새 키를 이미 갖고 있고** 옛 키만 있는 행은 0건, `tags_manual`도 0건이라 옛 키를 지우기만 하면 된다(`members`·`with_members`엔 잔재 없음). SQL은 청소 스윕이 태그를 `with_`로 옮겨도 안 어긋나게 두 컬럼을 함께 처리.
- [감사] **보류(content_flag='보류') 3,363건 실체 파악**(사용자 요청 — "보류 그룹 명단을 좀 파악해둘 필요가 있을 것 같다"). 2023(1,110)·2024(1,446)가 76%. **서바이벌/오디션 1,293건**: I-LAND2 411 · 유니버스 티켓 252 · 힙팝프린세스 217 · 빌드업 116 · 메이크메이트원 115 · 스타 레벨 테스트 85 · 로드투킹덤 73 · BOYS PLANET 43 · 소년판타지 23. **나머지 2,070건은 프로그램 시그널 없는 미등록 신인**(휘브·에이식스·아크·버가부·하츠웨이브·아트비트·하이파이유니콘·INI·TNX·더뉴식스·ASC2NT·여동생). ⚠️**보류 3,363건 중 2,542건에 members 태그가 붙어 있다** = 자동 태깅이 미등록 아이돌 영상에 등록 멤버를 갖다 붙인 결과. 최다 오염: 뉴 416 · 한 275 · 이니(INI라는 **그룹명**을 멤버명으로) 168 · Love 163 · 마이 88 · 코코 43 · 바로 41 · 조이 40. group_ko도 "그 곡의 원곡 그룹"으로 오배정(오디션 참가자가 더보이즈 곡 커버 → group_ko=더보이즈 431건). 즉 보류 더미는 매처 실패의 농축 코퍼스라 **학습용 오답셋**으로 그대로 쓸 수 있다. ※다음 후보: `pending_roster`(프로그램명 + 미등록 그룹/멤버 사전) 신설 → 그 시그널이 있으면 group_ko 자동배정 금지·자동 보류, 나중에 그룹 추가 시 로스터 그대로 승격.

---

## 2026-08-30

- [완료][index.html] **어드민 설정 패널: 탐험(✦) 눌러도 안 닫힘**(사용자 요청). 탐험 버튼 핸들러(모바일 tab-explore·데스크톱 dh-explore-btn)가 `closePanels()`로 설정을 닫던 걸, `_isAdmin()`이고 설정이 열려 있으면 closePanels 후 다시 열어 유지(설정은 어드민 작업 콘솔이라). 카드는 오른쪽에 열려 안 겹침. showGC는 설정 안 건드림 확인. 헤드리스: 어드민 유지·비어드민 기존대로 닫힘.
- [완료][index.html] **검색창 자연 닫힘 로직 개선**(사용자 제보 — 자연스럽게 안 꺼지는 경우). 원인: 바깥클릭 닫기는 `window` pointerdown(버블) 핸들러가 하는데, 탐험 패널(`#feed-overlay` 등)이 자체 pointerdown을 stopPropagation해서 그 안을 누르면 핸들러가 아예 안 불려 검색이 남았음(데스크톱은 탐험 패널이 늘 열려 있어 자주 발생). 해결: **캡처 단계** document pointerdown 리스너 신설 — stopPropagation 전에 항상 먼저 발동, 검색 UI(입력·결과: #search-wrap/#ob-top-search/#ob-sr/#mob-sr/#tab-sb-input/.sr-item)만 빼고 어디를 누르든 열린 검색을 닫는다(`_closeSearchUIs` 공용 헬퍼). `closePanels`에도 ob 검색 닫기 추가(버튼/네비로 닫을 때 누락 방지). 헤드리스: 탐험 패널 안 클릭→검색 닫힘, 검색창 자체 클릭→유지 확인.
- [완료][index.html] **영상 케밥 정렬 라벨 변경**(사용자 요청) — 카드 영상 ⋮ 정렬: '업로드순'→**'최신순'**, '오래된순'→**'날짜순'**(chSortNew/chSortOld KO 라벨만, 정렬 동작·EN은 그대로).
- [완료][groups.json][index.html] **그룹 인스타 대량 보강 + 인스타 2개 지원**(사용자 요청). ①무인스타 그룹 82개 웹 전수조사(백그라운드 에이전트) → **활성 40 + 해체-계정잔존 9 = 49개 공식 인스타 추가**(raw 타겟 삽입으로 포맷 보존, 49줄 추가만·삭제0). 개명 3건(클라씨→CLASSy `m25_classy`·걸셋←VCHA·앨리스←ELRIS) 보류, 계정 없는 해체 29개 제외. ②인스타 2개 케이스 지원 — `_LINK_DEFS`에 `instagram2` 키 신설(메인 오른쪽에 보조 아이콘). 브브걸: 새 BBGIRLS `weare_bbgirls`를 메인(프사 클릭 타겟 `_gcPicCfg.instaUrl`)으로, 기존 브레이브걸스 `bravegirls.official`은 삭제 않고 instagram2로 병기. 헤드리스로 프사 타겟·링크 줄 2아이콘 확인. ※클라씨 핸들·섹션B 해체계정은 비활성 가능성 있어 실기기 확인 권장. 감사: 사진 있는데 인스타 없음 0건, 인스타 있는데 사진 없음 3건(플레이브·브브걸=사진만 미업로드 / 라임라잇=MADEIN 개명으로 인스타 사망).
- [완료][index.html] **피드 쇼츠 썸네일 화질 개선** — newfav/Trend 등 피드 카드의 쇼츠(9:16)가 16:9 mqdefault(320×180)를 cover로 잘라 늘려 흐렸음. `_setFeedCardThumb`에서 쇼츠는 세로 원본 `oardefault.jpg`(실측 1080×1920) 우선 로드 + 120×90 플레이스홀더/에러 시 원래 썸네일 폴백(그룹/멤버 카드 renderItems와 동일 로직). 가로는 mqdefault 유지. 해상도 ~6배.
- [완료][kpop_universe.css][index.html] **어드민 데스크톱 레이아웃: 설정=맨 왼쪽 → 작업패널=그 오른쪽 옆 나란히**. 설정 패널 카드 가림 스톱갭을 여러 번 조정: 왼쪽 도킹→어드민 작업패널(vm/adm-home/hnn, 이미 왼쪽 도킹)과 겹침→최종은 설정 leftmost + `body.is-admin:has(#settings-panel.open)`로 작업패널을 설정 폭(332px)만큼 밀어 나란히. `_updateAdminUI`에 `body.is-admin` 토글. 카드는 오른쪽이라 설정과도 안 겹침(원래 문제도 해소). 헤드리스로 겹침0 확인.
- [완료·태깅관리3][admin.js][tag_review_queue_migration.sql] **검수 대기열 — console.log 무덤 → DB 테이블 + 어드민 홈 카운트 + 목록**(사용자 요청 3). 문제: 스윕이 판단 못 한 "애매한" 행(멤버 태그 전부 빠짐 등)이 브라우저 콘솔에만 남아 검수센터에 들어가도 처리할 게 없었음("들어가도 할 일이 없다"). `tag_review_queue`(video_id,reason,source,detail,resolved_at, unique(video_id,reason) dedup) 신설. `_tagReviewEnqueue(Batch)`(테이블 없으면 조용히 no-op)·`_tagReviewCount`·`_tagReviewResolve`·`_openTagReviewQueue`(목록 오버레이 — 제목·이유·[편집→_openVidTagModal]·[✓해결]). **고신뢰 애매만** 적재(내 보완: 전부 넣으면 큐도 방치됨) — 현재 소스는 members 재검증(전체·이름한정)의 "태그 전부 빠짐"(members_wiped). 어드민 홈에 "검수 대기 N" 카드→목록. 루틴: 홈 카운트→목록→편집→해결. 헤드리스: 함수 로드·테이블 없을 때 무예외(count 0·enqueue no-op·오버레이 마이그 안내) 확인. node --check+matching 97/97. **⚠️ `tag_review_queue_migration.sql` 실행해야 실동작**(RLS는 어드민 이메일 기반). ※다음: 5(정리) → 4(좌측 콘솔 P1).
- [완료·설정스톱갭][index.html][kpop_universe.css] **어드민 설정 패널이 카드 안 가리게(왼쪽 도킹)**(사용자 제보 — 데스크톱서 설정 누르면 카드 위로 덮임). 원인: `#settings-panel`(right:0·320px·z68)과 카드 `#side-panel`(right:0·320~640px·z63)이 완전히 같은 자리. 어드민은 33버튼+캡션이라 카드 전체가 가려짐. `_updateAdminUI`에 `body.is-admin` 토글 추가 → CSS `body.is-admin #settings-panel{left:0;right:auto}`로 어드민 설정만 왼쪽 도킹(카드는 오른쪽에 그대로). 일반 유저 패널은 항목 적어 오른쪽 유지. 헤드리스(1280): 어드민 설정 0~320·카드 960~1280 겹침0 확인. P1(좌측 콘솔) 전 빠른 스톱갭.
- [완료·태깅관리1][admin.js][kpop_universe.css] **편집 모달에서 오태깅 규칙 등록(⚑) + 이름 한정 재검증**(사용자 요청 — 흔한단어/동명이인 지속 관리 프로세스). 문제: 흔한단어 보호(`_ATM_DYNAMIC_HASHTAG_NAMES`←name_match_whitelist)는 유일하게 UI 추가 경로가 없어(스캔 화면 제거 후 SQL 전용) 오태깅이 계속 쌓였음. 편집(연필) 모달 각 멤버 옆에 ⚑ 추가 → `_atmMemberRuleFlow`: ①`_atmRegisterHashtagName`(로스터 검증 후 name_match_whitelist insert+동적셋, 유령이름 거부) ②곧바로 `_atmScopedMemberReverify`(그 이름이 든 자동태깅 행만 `_atmResolveMembers` 재판정·스냅샷·200배치 — 전체 스윕과 동일 안전장치)로 기존 오염 즉시 정리. "발견→규칙→청소"가 한 자리에서. 입력검증으로 죽은 규칙('JIN'류) 방지. 헤드리스(admin 마크업 마운트): 함수 로드·에스파 4멤버 ⚑4개 렌더·가짜이름 거부·실제이름 검증통과(RLS는 anon이라 막힘=정상) 확인. node --check+matching 97/97. `_ADMIN_JS_VER` 갱신. ※그룹 배정까지 틀린 건 "② 오태깅 그룹 재배정" 별도(재검증 confirm에 안내). 다음: 설정 패널 어드민 카드가림 스톱갭.
- [완료·Phase2][index.html][photo_collection_public_migration.sql] **공개 사진 컬렉션이 남의 카드에도 사진까지 노출**(사용자 요청). `_syncPublicCollection`이 사진 컬렉션이면 `photos` 배열도 upsert(영상 컬렉션 payload엔 안 넣어 마이그레이션 전에도 영상 공개 안 깨짐). `_warmPubCollections`는 photos 컬럼을 **있으면 받고 없으면 폴백**(없는 컬럼 select는 쿼리 전체 에러라 try→base 폴백). pub 매핑에 `kind`(photos 배열 실려오면 photo)·photos 추가 → 남의 공개 사진 컬렉션이 카드 칩(📷)으로 뜨고 클릭 시 `_openPublicColPhotoGrid`(읽기전용: 업로드/삭제/대상수정 없음, by 닉네임·가이드라인·**신고 버튼**). 신고=`_reportPublicCollection`→`collection_reports` insert(부적절 콘텐츠 takedown 경로). **⚠️ `photo_collection_public_migration.sql`을 Supabase에서 실행해야 공개 사진 공유+신고가 실동작**(photos 컬럼 + collection_reports 테이블+RLS). 헤드리스: 함수 파싱·warm 폴백 무예외·뷰어(신고/읽기전용/3열) 확인. **사진 컬렉션 기능 전체 완성**(대상지정·카드생성·로컬노출·공개공유·신고).
- [완료][index.html][kpop_universe.css] **카드 단위 ⋮ 메뉴 + 카드에서 사진 컬렉션 만들기(자동 대상 지정)**(사용자 요청 #2). 그룹/멤버 카드 이름 줄의 공유 아이콘 옆에 카드 전체용 ⋮(`#gc-card-menu`/`#tt-card-menu`, `.card-menu-btn`) 신설 — 디스코 정렬 ⋮·영상 ⋮와 별개, 앞으로 카드 단위 기능 담을 곳. `_openCardMenu(btn,ctx)` 드롭다운 + `_createPhotoColFromCard(ctx)`: 이름 입력→사진 컬렉션 생성→**이 카드 인물로 대상 자동 지정**(그룹 카드=그룹, 멤버 카드=그룹+멤버, 솔로는 그룹처럼)→사진 그리드 열림. 컬렉션 칩에 사진이면 📷 배지. 헤드리스: ⋮ 표시·메뉴·자동지정(에스파/카리나 match)·배치 스크린샷 확인.
- [완료·Phase1][index.html] **사진 컬렉션 대상(그룹/멤버) 지정 → 카드 노출**(사용자 요청 — "사진은 누구 건지 태그가 없어 노출 기준이 없다"). 사진 컬렉션에 `targetGroupKos`/`targetMemberKos` 추가. `_collectionGroupKos`/`_collectionMemberKos`가 kind==='photo'면 이 대상을 반환 → 노출(_loadCollectionRow)·공개동기화가 자동으로 따라옴. `_collectionMatchesCard(c,ko,memberKo)` 신설(영상=태그, 사진=지정대상)로 멤버 카드 노출까지. 사진 그리드 창에 "대상: X ›" 라인(눌러서 지정/변경), `_pickColTarget` 선택기(GROUPS/ARTISTS 이름 검색, 그룹=그룹전체·멤버=멤버+그룹), `_colTargetLabel`. 공개 게이트: 사진 컬렉션은 대상 없이 공개 불가(`_toggleCollectionPublic`에서 막고 선택기 띄움 — 사용자 결정 "공개면 대상 필수, 비공개면 선택"). 컬렉션 칩도 사진이면 첫 사진 썸네일·"N장"·클릭 시 사진 그리드. 헤드리스: 헬퍼 정확·에스파 카드에 칩 노출·게이트 작동 확인, 그리드/선택기 스크린샷 확인. **⚠️Phase2 남음**: 공개 사진 컬렉션이 **남의 카드에도 사진까지** 보이려면 public_collections에 photos 컬럼 추가(마이그레이션)+동기화+뷰어 필요(현재 공개테이블은 영상 items만). 지금은 대상 지정·게이트·**내 카드 노출**까지 로컬 완성.
- [완료][kpop_universe.css] **웹(데스크톱) favnew 잘림 + 패널 너비 통일**(사용자 제보). ①favnew가 세로 그리드로 바뀌었는데 예전 가로 스트립용 `#feed-favnew>.feed-card{width:158px}`(2796) 고정폭이 남아 있어, 데스크톱 탐험 패널(300px)에서 오른쪽 열 카드가 46px씩 잘렸다(실측 139장 중 68장 넘침). 그 선택자에서 `#feed-favnew` 제거 → 그리드 `1fr`(Trend와 동일)로 유연화, 넘침 0 확인. ②탐험 패널 `#feed-overlay` 데스크톱 너비 300→320px(그룹/멤버 카드 #gc·#tt, 연결 #conn-panel 전부 320과 통일). ③프로필 패널 `#profile-panel` 312→320px. 헤드리스 데스크톱 스크린샷으로 favnew 2열 안 잘림 확인.
- [완료][admin.js][tests] **온리원오프 'Love'·'나인'/'Nine' 역추론 오태깅 차단**(사용자 제보 — "love를 제일 엄격하게"). 실측: `members=['Love']` **1059건 전부 group_ko=온리원오프**, 표본 300 중 286(95%)이 제목에 OnlyOneOf도 없는 오태깅. `members=['나인']`도 185건(NINE to SIX 그룹을 멤버 Nine으로 오인). 원인: 온리원오프 로스터에 실제 'Love'(name.ko='Love')·'나인'(Nine) 멤버가 있는데 흔한단어 보호목록에 없어, 매처가 제목 영단어 love/nine을 역추론. 커버곡 경로(_COVER_COMMON_KEYS)는 이미 가드됐으나 **멤버명 역추론 경로가 뚫려 있었음**. 조치: `_m2ParseTitle`의 memberHit/memberHitTokens에 `_ATM_INFER_EXCLUDE_NAMES`(Love/나인/Nine) 추가 — 역추론(이름→그룹 추정)에서 **완전 제외**(해시태그 #Love조차 오탐 흔해서 "그룹명 동반 시에만" = 멤버추출 경로에서만 인정, 사용자 결정). 그룹 자체 매칭·자체 채널 태깅은 별개 경로라 온리원오프 정상 영상 유지. matching.test.js +3(평문 LOVE·NINE to SIX 차단 / OnlyOneOf 확정 시 유지), 97/97. ⚠️**남은 작업: 기존 1244건 오염은 admin "오태깅 그룹 재배정"·"members 재검증" 스윕을 돌려야 청소됨**(매처는 고쳤으나 저장된 행은 재검증 필요).
- [완료][index.html] **"다른 아티스트의 커버" 섹션도 비율 패킹 적용**(사용자 제보 — "영상 노출되는 모든 곳에 그 배치"). 카드 커버 탭의 메인 그리드는 이미 `_groupGcVids`(패킹)였지만, 그 아래 붙는 `_loadCoverOfSection`(다른 아티스트의 커버)만 `.tv-mix-grid`에 `songs.forEach(appendChild(makeSmallThumb))`로 그냥 쏟아부어 가로/쇼츠 섞여 여백이 남던 유일한 구멍이었음. 연결카드가 쓰는 `_renderMixedVidGrid(_sortMixedByAspect(songs),wrap,_sorted,ctx)`로 교체(같은 비율끼리 한 행·경계 전체폭·라이트박스 순서 일치). 전수조사(Explore) 결과 나머지 영상 그리드(카드 각 탭·연결카드·Trend·favnew·Discovery·컬렉션 미리보기)는 전부 이미 패킹 사용 확인 — 이게 마지막 구멍. 합성 검증: nn/nn/W/ss/s, 여백0.
- [완료][index.html] **탭 정체성 재정리: For You="나" / Discover="전체·탐색"**(사용자 결정). favnew를 "최근 7일 즐겨찾기 영상 싹 다 최신순 세로 그리드"로 키우니 그건 Trend의 즐겨찾기 필터판이라 둘을 같은 탭에 겹쳐 두면 중복 → **Trend를 Discover로 이동**. 최종: For You = 즐겨찾기 신작(그리드) → 이어보기 / Discover = 기념일 → 차트 → Trend → 디스커버리. favnew 변경: 가로 스트립→`feed-grid`+행패커(`_packRows`, wideProb 0.22), 7일 게이트(`.gte(published_at,since)`) 추가, 쇼츠 포함(그리드라 9:16 그대로), 그룹당 캡 제거(즐겨찾기라 "싹 다"), `_FEED_FAVNEW_MAX` 30→200·`limit` 150→400. `_buildFeedTrend()` 호출을 `_buildFeedRec`→`_buildFeedDisc`로 옮김. 온보딩 카드는 그리드서 전체폭(`gridColumn:1/-1`). 헤드리스(즐겨찾기 14그룹 주입): favnew 199장·98행·여백0, Discover 순서·Trend 199장 확인.
- [완료][index.html] **탐험 패널 시작 탭 조건부**(사용자 결정 — 신규는 For You가 얇으니). `_openFeedOverlay`에서 `favGroups.size||favMembers.size||시청기록(_kpu_recent_vids)` 중 하나라도 있으면 `rec`(For You), 다 없는 완전 신규면 `disc`(Discover)로 연다. 탭은 둘 다 유지·온보딩 카드도 유지(숨기지 않음 — 이어보기 손실 방지). 즐겨찾기/시청기록 생기면 자연히 다음부터 For You 기본. 헤드리스 3케이스 검증(신규→disc, 즐찾→rec, 기록만→rec). ⚠️테스트 노트: 헤드리스 CDP는 앱 `let`/`const`(favGroups 등)가 렉시컬이라 Runtime.evaluate로 못 건드림→localStorage 세팅 후 reload로 주입할 것. WebGL 플래그(`--enable-unsafe-swiftshader --use-gl=angle --use-angle=swiftshader`) 빠지면 로드 중 3D 초기화 예외로 스크립트가 중단돼 이후 전 let이 TDZ가 됨(디버깅 함정).
- [완료][index.html] **탐험 패널 탭 재구성 + Trend 대폭 확대 + Discovery 여백 해결**(사용자 요청). ①For You 순서 = 즐겨찾기 신작 → 이어보기 → Trend(기념일 섹션은 Discover로 이동). ②Discover 순서 = 오늘의 기념일 → 차트 → 디스커버리(`_buildFeedAnniversaries()`를 `_openFeedOverlay`에서 `_buildFeedDisc`로 이동, Discover 첫 섹션). ③Trend "거의 무한 스크롤" — `_FEED_TREND_MAX` 24→200, 쿼리 `.limit(300→800)`, 그룹당 캡 2→4(도배 방지). 7일치 대부분을 행패커로 깜(실측 199장, 썸네일 lazy라 성능 OK). fav-new는 별도 상한 `_FEED_FAVNEW_MAX=30`으로 분리(같이 안 늘게). ④Discovery 여백 — `_spreadFeedCollectionCards`를 **비율 인지 행 패킹**으로 교체: tail 구간을 같은 비율(가로/쇼츠)끼리 2열 짝지어 행 높이 맞춰 여백 제거, 전체폭(.feed-wide) 단독 행, 짝은 collection 연속 방지 우선. Trend의 `_packRows`와 동원리를 async·무한스크롤 DOM 노드에 적용(선행 리스트 불가라 도착 정착 후 tail만 재배치). 헤드리스 스크린샷으로 4개 전부 확인(Discovery 2열행 전부 비율 일치).
- [완료][index.html] **Trend 배치 = 행 패커(_packRows) — 그림대로 "가로행/쇼츠행 번갈아 + 가끔 전체폭 가로 + 여백0"**(사용자 요청 최종 해결). ⚠️교훈 정정: 앞서 "큰카드+섞인비율+여백0 동시 불가"라 한 건 **틀림** — 그건 메이슨리(열 스택)에 전체폭 끼울 때만이고, **행 단위 패커**면 가능. `_packRows`: 순서대로 꺼내 뒤에서 같은 비율 짝을 lookahead로 당겨 2열 행을 만들고(→행 높이 같아 여백0), 가로는 minGap 간격·wideProb 확률 또는 짝없으면 전체폭. 결과: 가로가로|쇼츠쇼츠|전체폭가로|… 번갈아. `.feed-grid`(span2 wide) 사용, 라이트박스 재생목록도 행 펼친 순서(flat)로 맞춤. 실데이터 스크린샷으로 그림과 동일 확인. 그룹/멤버 카드 `_groupGcVids`가 이미 같은 계열(lookahead+wideProb)이라 확장 용이. (앞서 시도: 메이슨리=열에 뒤섞임, 비율그룹핑=통째정렬로 블록, varied메이슨리=경계여백 — 전부 행패커가 정답.)
- [폐기][index.html] **Trend 순수 메이슨리(중간단계)** — varied 여백 문제로 순수 메이슨리 갔다가, 행패커가 정답이라 대체. 핵심 교훈: **큰(전체폭) 카드 + 섞인 비율 + 여백0은 동시 불가** — 전체폭 카드가 "행 경계"를 만들고 그 경계에서 두 열 높이가 안 맞아 여백 발생. `_makeVariedMasonry(strip,false)`로 순수 메이슨리(더 짧은 열에 append) → 여백0. varied 옵션(전체폭 featured)은 코드에 남기되 기본 off. 큰카드+여백0을 둘 다 원하면 유일한 길은 균일박스(레터박스) — 사용자 결정 대기.
- [보류·개념][index.html][kpop_universe.css] **Trend "varied 메이슨리" 실험(개념만 남김)** — 큰 것·작은 것 섞인 잡지형 시도했으나 앞서 넣은 단순 메이슨리(가로 항상 작음)·비율그룹핑(가로끼리 쇼츠끼리) 대신, `_makeVariedMasonry`: 여백없는 2열 메이슨리 세그먼트 + 가로영상을 최소 3장 간격으로 전체폭 featured(큰 16:9)로 끼움. 순서는 셔플 그대로(비율로 안 묶음)라 큰가로→작은가로+쇼츠 혼합→큰가로… 로 다양하게. 전체폭 쇼츠는 세로 과도(390×690)라 안 함(쇼츠는 9:16이라 이미 존재감). 실데이터 스크린샷으로 확인(여백0, 큰가로 featured+혼합). ※다음: 좋으면 그룹/멤버 카드 그리드에도 같은 헬퍼 확장(featured 간격만 튜닝).

- [완료·실기기확인필요][index.html][kpop_universe.css] **[근본해법] 2열 그리드 가로영상 아래 여백 → 메이슨리로 해결(Trend 먼저)**(사용자 제보 — 오래 못 고치던 문제). 원인: `.feed-grid`가 `grid-auto-flow:dense`로 "한 행=같은 높이"라, 16:9(가로) 옆에 9:16(쇼츠)가 오면 가로영상 아래 큰 여백이 지속 발생(실데이터 스크린샷으로 재현: Trend 4330px 곳곳). 근본 해법 = **2열 메이슨리**(`_makeMasonry`): 두 열을 독립 세로 스택으로 두고 카드를 "현재 더 짧은 열"에 붙임 → 행 개념이 없어 비율 뭐든 여백 0. 기존 정렬·짝짓기·dense·재그룹핑 불필요. Trend 적용 후 스크린샷: 여백 사라지고 섹션 4330→3683px. 헬퍼는 재사용형(무한스크롤도 add만). ※다음: 같은 헬퍼를 그룹/멤버 카드 그리드(.gc-ch-grid)에도 확장(그 fragile한 sort/pair/dense 대체). Discovery는 전체폭 스포트라이트 카드가 있어 별도(비율통일 B) 검토.

- [완료·실기기확인필요][index.html] **어드민 편집(연필) 버튼 — 탐험 패널·컬렉션·스크랩에서 연 영상에도 표시**(사용자 제보). 라이트박스 편집버튼은 `_isAdmin()&&s._apiId`일 때만 떴는데, 그룹/멤버 카드만 곡객체에 `_apiId`를 붙이고 탐험/컬렉션/스크랩 경로는 안 붙여 안 떴음. DB의 id=유튜브 영상 id라 곡 URL에서 유도하는 `_lbApiId(s)` 추가(watch/shorts/youtu.be/embed) → 표시조건·클릭 모두 이걸 사용. group_ko는 있으면 넘기고 없으면 ''(`_openVidTagModal`이 id로 실제 group_ko/members를 DB에서 다시 불러오므로 무방). 헤드리스로 id추출·어드민표시·비어드민숨김 검증. 한 곳 수정으로 전 경로 커버.

- [완료·실사용확인필요][index.html] **즐겨찾기 신작 — 멤버 즐겨찾기 시 그 멤버가 들어간 영상만**(사용자 제보). 전엔 멤버를 즐겨찾기하면 그 그룹 전 영상을 가져와 타 멤버만 나온 직캠도 떴음. `_feedFavGroupKos`→`_feedFavTargets`(그룹 즐찾=전영상 passAll / 멤버 즐찾=그 그룹 영상 중 members에 그 멤버 있는 것만 memberFilter). 특정 멤버 태깅 없는 그룹 전체 영상은 통과, 타 멤버만 태깅된 건 제외. 솔로 즐찾은 그룹처럼 전영상. 헤드리스(카리나/에스파)로 6케이스 검증. 문구 "New from Favorites"→"New from Your Favorites".

- [완료][index.html] **나침반 탐험 패널 "맨 위로(↑)" 버튼이 스크롤 대신 패널을 닫던 버그 수정**(사용자 제보). 원인: "자동 열린 탐험 패널을 밖을 건드리면 닫는" window pointerdown 핸들러가 2겹 — ①capture 단계(10321) 제외목록에 맨위로 버튼(#gc-totop)이 없어 밖으로 오인·닫음(capture라 버튼 bubble stopPropagation보다 먼저 발동) ②bubble 단계(10316)도 [data-modal] 아니라 닫음. 해결: capture 제외목록에 `#gc-totop` 추가 + 버튼에 pointerdown stopPropagation(bubble 차단). 헤드리스(데스크톱)로 버튼=유지·바깥=닫힘 검증.

- [완료][admin.js][index.html] **원곡 v2 — 동기화 자동적용을 credit·artist로 게이트 + 실DB 감사**. 실DB 커버 키워드 6천 표본 감사 결과: cover_of 추가 후보 885건 중 **크레딧(원곡:X)·아티스트표기는 오탐 0**(아일릿→보아·킥플립→스키즈 등 전부 정확), 반면 따옴표/대시/챌린지태그/평문은 **동음이의 곡명 오탐**(‘Winter Wonderland’→샤이니, ‘Smoke’·‘Wait’·‘SUMMER_FESTA’ 등)이 섞임. → 동기화 자동적용은 명시적 인간표기(credit/artist)만, 나머지는 "🎵 원곡 태깅 v2" 스윕(표본확인+스냅샷)이 담당하도록 게이트. `_COVER_EXCLUDE`에 `cover highlight(s)` 추가(패션필름). with_ 정리(원곡자를 콜라보에서 제거)는 감사 표본에서 깨끗(빌리 SHINee 커버 등). cover-resolve 45/45.

- [완료·실사용확인필요][admin.js] **원곡 태깅 v2 — 동기화 시점 적용**(남은작업①). `_extBuildRows`(외부채널=음악방송·커버 채널 동기화)에서 rows.push 직전 `_coverResolve` 호출(sync, try/catch) → 커버 확정 시 원곡자를 `with_*`→`cover_of_*`로 옮기고 with_ 정리·`cover_of_song` 저장. 기존 "커버키워드+6년선배" 휴리스틱(곡명 근거 없이 세대차만으로 커버 처리) 제거, v2로 대체. 게스트는 일단 with_로 넣고 v2가 재분류. 차트 A등급은 async라 동기화선 생략(디스코 사전만, 나머진 스윕 backfill). group_ko 재배정은 동기화에선 안 함(owner 고정). patch 계약 검증(원곡: 슈주/소시 → cover_of로 이동+with_비움 확인).

- [완료·실DB확인필요][admin.js][index.html][tests/cover-resolve.test.js][tools/m2_harness.js] **원곡(커버) 태깅 v2 엔진 이식**(사용자 요청 — 기존 1차/2차 로직 부실). Fable 설계(base=fancam 포함이라 admin.js 단일 추가 헝크로 클린 이식)를 라이브에 반영·재검증. `_coverResolve`(admin.js): 곡명→원곡자 사전(GROUPS 디스코 1.4만 트랙+멤버 솔로 602명+유닛+멜론799/스포티파이455 A등급) 구축, 제목에서 크레딧`(원곡:X)`>챌린지태그`#곡명_Challenge`>따옴표>대시>평문 순으로 곡명 추출→원곡자 확정. 자기곡은 커버 아님, 커버 확정 시 with_*에서 원곡자 제거·동반신호(with/X/님과/선배/feat) 없으면 with_* 비움(챌린지 포함), 동명곡은 등급·발매일·pri 점수로 동점이면 미확정. 어드민 버튼 "🎵 원곡 태깅 v2"(sp-cover-v2-btn·`_ytSweepCoverV2`, 표본콘솔→확인→스냅샷). 회귀 cover-resolve 45/45 + matching 94/94. ※다음: 동기화 시점 적용(_extBuildRows)·구 3버튼 제거·실DB 감사.
## 2026-08-29

- [완료][kpop_universe.css] **즐겨찾기 신작 선반 카드 "점처럼" 붕괴 수정**(사용자 제보, 웹). Step3에서 Trend를 그리드로 빼며 `#feed-trend`를 스트립 폭 규칙에서 제외했는데 `#feed-favnew`는 애초에 폭 규칙에 없어 flex 스트립에서 카드가 0폭으로 붕괴됨. Charts와 동일하게 `#feed-favnew>.feed-card` 158px + cap/title/sub 규칙 추가. 헤드리스(데스크톱 1280) 검증 158px·16:9. ※선반이 비어보이는 건 즐겨찾기 그룹의 최근 업로드가 적을 때는 데이터상 정상(온보딩은 즐겨찾기 0일 때만).
- [완료·실DB확인필요][admin.js][index.html][tests/matching.test.js][tools/m2_harness.js][tools/fancam_pattern_probe.js][FANCAM_PATTERNS.md] **음악방송 직캠 구조 파서(`_fancamParseTitle`) + "②-1 음악방송 직캠 재검증" 스윕**(사용자 요청 — 뮤뱅·음중·엠카·인가·쇼챔·더쇼·잇츠라이브·킬링보이스 직캠은 오태깅 0이어야). Fable 설계(zip 기준)를 **라이브 클론에 이식**(admin.js는 base 일치라 `git apply` 클린, 나머지 수동 병합)하고 **라이브에서 재검증**. 직캠 제목은 `[태그] 그룹 멤버 '곡명' (영문괄호) | 보일러플레이트` 구조라, 파서가 구간을 나눠 `_m2ParseTitle`이 (a)곡명 구간 매칭 전 제거 (b)primary=출연자 구간 순서 (c)멤버자리 동일그룹 버림 (d)그룹토큰 바로뒤 로스터 정확대조(단일음절 허용·점수제) (e)strictSync/해시태그도 출연자 선두면 인정(멤버 등록명과 같은 토큰 제외). 부수: "마마무+" 정규화→"마마무" 오확장 버그(리터럴만 인정)로 수정. **라이브 시뮬(`tools/fancam_pattern_probe.js`, 전 로스터×실제 곡명×17포맷=154,670제목): 문제 2,207건(1.4%)이나 그룹 오배정/콜라보 오염 = 0** — 잔여는 전부 의도된 동작(멤버누락 1,740=해체·탈퇴 그룹의 2026 미래날짜 게이트, 멤버추가 250=곡명 괄호 속 멤버 솔로버전, with 162=유앤비 등 프로젝트 유닛 확장). 회귀 `matching.test.js` 77→**94/94**(직캠 17 추가). 구조 안 잡히는 제목은 기존 경로 그대로. ⚠️**실DB 정정은 어드민 "②-1 음악방송 직캠 재검증" 버튼**(직캠 키워드 ilike만 조회·구조 인식 행만·새 그룹이 출연자 구간 literal일 때만 재배정·수동/owner 제외·표본 콘솔·확인·스냅샷). 문서 `FANCAM_PATTERNS.md`. (Fable 설계 zip은 폐기, 라이브 재검증본만 반영)

- [완료][index.html][kpop_universe.css] **[탐험 재구성 Step 4] 빈/오류 상태 + 다시 시도** — 선반이 하나도 안 뜨면(대개 Supabase 실패, 예전엔 제목만 남고 빈 화면) 각 탭에 "지금은 불러오지 못했어요 · [다시 시도]" 표시. 빌드 1.8초 뒤 `_feedCheckEmpty`가 `.feed-card`/`.feed-onboard-card` 유무로 판정(For You는 온보딩이 있어 거의 안 뜸, 실제로는 Discover 통째 실패 시). 다시 시도=해당 탭 재빌드. 헤드리스로 표시/숨김/버튼핸들러/오발동없음 검증. (탐험 패널 2탭 재구성 Step 1~4 완료 — 다음은 GA 2주 뒤 하단 탭바 개편)
- [완료·실기기확인필요][index.html][kpop_universe.css] **[탐험 재구성 Step 3] Trend 세로 2단 그리드화 + 쇼츠 9:16** — Trend를 가로 스트립(158px 16:9, 쇼츠 제외)에서 **세로 2단 그리드**로. 이제 쇼츠도 포함해 `.is-short`(9:16)로, 일반은 16:9로 렌더 → "쇼츠가 가로 틀에 갇혀 작고 가로 스크롤 어색"하던 제보 해결. `#feed-trend` class→`feed-grid`, 스트립 폭 규칙에서 제외, `_buildFeedTrend` 쇼츠 포함+`{short:_isShortV(v)}`. 헤드리스 검증(grid 2단·16:9·9:16). ※1열 원하면 `grid-template-columns` 한 줄이면 전환 가능.
- [완료·실사용확인필요][index.html][kpop_universe.css] **[탐험 재구성 Step 2] 즐겨찾기 신작 선반** — For You 탭에 "💖 New from Favorites" 추가(기념일 다음). 내 즐겨찾기 그룹 + 즐겨찾기 멤버의 소속 그룹(`_feedFavGroupKos`, 솔로는 본인명)의 최신 업로드를 최신순으로(`.in('group_ko',...)`, Trend 쿼리 패턴 재사용). 즐겨찾기 0이면 온보딩 카드로 채워 절대 안 비게. 헤드리스 검증(온보딩/그룹+멤버해석/크래시없음). ⚠️실제 영상은 라이브 Supabase라 실사용 확인 필요.
- [완료·실기기확인필요][index.html][kpop_universe.css] **[탐험 패널 재구성 Step 1] 2탭 껍데기(For You / Discover) + 지연 로드** — 열자마자 선반 34개를 동시에 치던 걸(덜컹거림·중복 원인) 2탭으로 분리. For You(이어보기·Trend·기념일)는 열 때 빌드, **Discover(차트 11+디스커버리)는 그 탭 처음 누를 때만 지연 빌드**(`_buildFeedDiscovery`→`_buildFeedRec`+`_buildFeedDisc`로 분리, TTL 캐시 각각). 헤드리스 검증: 기본=For You·Discover 미빌드·전환 시 빌드·GA `feed_tab`·복귀. 다음 Step: ②즐겨찾기 신작 선반(그룹+멤버) ③Trend 세로그리드 ④빈/오류 상태.
- [완료·실기기확인필요][index.html] **[Phase 1] 나침반 열 때 보던 카드 복원** — 카드 보다가 나침반(탐험 피드)을 누르면 `closeMobSheet()`로 카드가 파괴돼 사라지고 URL 해시(#g=…)만 남아 화면과 주소가 어긋났음(사용자 제보). 나침반 열 때 그 카드를 `_feedReturnCard`에 스냅샷, 피드를 "돌아가기"(나침반 토글/닫기버튼/스와이프)로 닫으면 `_reopenCard`로 복원. 홈/별/검색/프로필로 "이동"하며 닫을 땐 복원 안 함, 피드 중 다른 카드 열면(openMobSheet) 기억 해제. 헤드리스 3시나리오(복원/이동시비복원/피드내이동시해제) 검증. ※별버튼 인라인 closeMobSheet 중복 통합은 별개 리팩터로 보류. ⚠️실기기 동선 확인 권장.
- [완료·실사용확인필요][admin.js] **[Phase 1] is_short 삽입 시점 판별** — 동기화 업서트 공용 헬퍼(`_ytUpsertVideos`)에서 upsert 직전, is_short 아직 false인 신규 행을 `_probeShortsBatch`(oardefault.jpg 실측)로 프로브해 세로면 승격. 지금까진 스윕이 나중에만 해서 Trend(최근 7일)에 세로 영상이 가로 틀에 갇혔음(사용자 제보). 5초 배치 데드라인+try/catch로 동기화 절대 안 깨짐, 못 잡은 잔여분은 기존 스윕 backstop. 프로브 훅 로직 헤드리스 검증(세로 승격/가로 유지/이미쇼츠 스킵). ⚠️네트워크 프로브 자체는 샌드박스서 확인 불가 — 다음 실제 동기화 때 세로 영상 is_short 붙는지 확인 필요.
- [완료][index.html] **[탐험/탭바 개편 Phase 1] GA 계측 추가** — 탭 클릭 5종(home/search/explore=별/feed=나침반/profile)·video_play(mode: shorts/browse, advance)·explore_open. 기존 계측(view_group/member·favorite·search·infscroll 등 13개)은 있었으나 **탭 클릭·영상 재생 로그가 전무**했음 → "별/탐험 탭을 실제로 쓰나, 쇼츠 재생 비중" 같은 배치 결정 근거를 2주 수집 시작. 헤드리스로 8개 이벤트 발사 검증. UI 변화 없음.
- [완료][admin.js] **넥스지 멤버 "하루" 일반명사 오매칭 방어**(사용자 제보 — 수동 편집해도 매일 동기화마다 "○○의 하루"류 재오염). 원인: 흔한단어 게이트(`_ATM_COMMON_KO_WORDS`)는 **이름만의 역추론 경로**에서만 #해시태그를 요구하고, **그룹 확정 문맥의 멤버 추출**(`_atmMatchesMember`/`_atmResolveMembers`) + `_m2ParseTitle`의 평문 `hit`은 게이트 없이 "하루"를 단독 토큰으로 매칭 → 넥스지 공식채널 "○○의 하루" 브이로그가 멤버 하루로 붙음. 해결: 공유 헬퍼 `_atmStripCommonNounCtx`로 **일반명사 문맥("X의 하루"·"하루종일"·"하루하루")의 "하루"만** 매칭 전 제거. 소유자(리쿠의)·"하루의…"·"#하루"·"하루 직캠"은 보존. 두 매처에 적용, 10케이스 로직 검증. ⚠️기존 오염분 중 수동편집 안 한 행은 어드민 "멤버 오태깅 재검증" 스윕으로 정리(같은 헬퍼 공유).
- [완료][index.html] **동기화 제외 키워드 7개 추가**(`_JUNK_TITLE_KEYWORDS_GLOBAL`): 각집부부·실화탐사대·화성인x파일·[아무튼 출근·[이리와·어쩌다어른·화성인이지만. 신규 동기화는 자동 무관 처리. ⚠️**이미 올라온 기존 영상은 어드민 "제외 키워드 영상 무관 정리" 스윕(sp-yt-sweep-junk) 1회 실행 필요**(키워드 목록 버전이 바뀌어 스킵 안 됨).
- [완료·실기기확인필요][index.html][kpop_universe.css] **탐험 패널 "모음" 오버레이(`#feed-list-overlay`) 스와이프-닫기 고장 수정 + 닫기버튼 확대**(사용자 제보). ①스와이프: `_wireFeedListSwipe`가 `dragActive=grid.scrollTop<=0`을 터치 시작 시점에만 봐서, 목록을 조금이라도 스크롤한 뒤 **상단 헤더/손잡이를 잡고 내려도** 안 닫혔음 → 그리드 밖(헤더·손잡이) 터치는 스크롤 위치 무관하게 항상 닫기 드래그로 인정(`_fromHeader`). 그리드 안 터치는 종전대로 맨 위에서만. ②닫기버튼 `#feed-list-close`: 14px SVG·padding0(히트영역 거의 0) → 36×36 히트영역·아이콘 18px. 헤드리스로 close 호출 판정 4케이스 검증(헤더+스크롤/그리드맨위=닫힘, 스크롤된그리드/작은당김=유지).
- [완료·실기기확인필요][index.html][kpop_universe.css] **컬렉션 피커(`#col-picker`, "컬렉션에 추가" 바텀시트) 스와이프-닫기 추가 + 닫기버튼 확대**(사용자 제보 "스와이프 닫기 안 되고 닫기버튼 너무 작음"). ①닫기버튼 `#col-picker-close`: 맨 `×`(히트영역 ~24px) → 36×36 flex 히트영역. ②바텀시트에 아래로 스와이프→닫기 추가(`_wireColPickerSwipe`, feed-list 오버레이와 동일 패턴 — 목록 맨 위에서 아래로 dy>80). 손잡이 줄(::before) 힌트 + 재오픈 시 transform 잔상 초기화. 헤드리스 검증 완료. ※feed-list(컬렉션 목록) 오버레이는 이미 스와이프 있어 대상 아님.
- [완료][kpop_universe.css] **쇼츠 재생모드에서 상단 멤버/그룹 텍스트를 눌러도 하프카드가 안 열리던 문제 수정**(사용자 제보). 원인은 영상 크기가 아니라 `#yt-lb-member-info`에 걸린 `pointer-events:none`(쇼츠 한정) — 클릭 핸들러(`_openLbHalf`)와 하프시트(`.lb-half`)는 두 모드 공통이라 CSS만 문제였음. 브라우즈 모드처럼 `.lb-anchor-part`(멤버/그룹 텍스트 버튼)만 `pointer-events:auto`로 복구 — 컨테이너 빈 영역은 none 유지라 쇼츠 세로 스와이프(다음/이전 영상)는 그대로. 헤드리스로 클릭→하프카드 열림·영상 유지 검증 완료.
- [완료·실기기확인필요][index.html][tests] **가로 재생모드에서 "다음 영상" 목록 스크롤이 재생모드를 꺼뜨리던 버그 수정**(사용자 제보). 원인: 브라우즈 모드 "당겨서 닫기"가 목록 맨 위(scrollTop≤0)에서 아래로 당기면 켜지는데, 가로는 세로공간이 좁아 목록 훑는 손짓이 그 임계값을 쉽게 넘김. 수정: 목록 위 제스처는 가로(`innerHeight<500`)에서 당겨-닫기로 승격 안 함 — 세로는 인스타식 당김 유지, 가로 닫기는 드래그캡·X가 담당(`index.html` touchstart `_lbListCramped`). `tests/lb-swipe-ownership.test.js`에 가로 회귀 케이스(7번) 추가, 전항목 통과.

## 2026-08-28

- [완료·실기기확인필요][index.html][kpop_universe.css][PERFORMANCE.md] **영상 재생 중 발열 완화 — 유튜브 화질 모바일 720 + 라이트박스 blur 모바일 제거**(사용자 제보 "영상 볼 때 발열 심함", 주말 집 세션). 3D는 라이트박스 중 이미 정지(index.html:3035)라 원인 아님 — 실제 주범 ①iframe `vq=hd1080` 강제(모바일이 1080p를 계속 디코딩) → 모바일만 `hd720`(데스크톱 1080 유지, `openLightbox`) ②`#yt-lb-autonext`(재생 중 상시)·`#yt-lb-menu-panel` `backdrop-filter:blur` → 모바일 `none`(배경 반투명 rgba는 유지, 시각차 거의 없음). 발열 대응 로직 전수 매핑 + 원칙(`catch(e){}` 침묵 금지 등)을 **`PERFORMANCE.md` 신설**로 정리 — 앞으로 발열 관련 작업은 이 문서 먼저. ⚠️유튜브가 vq 힌트를 무시할 때도 있어 실기기 발열 체감 확인 필요.

### 🔚 세션 마감 메모 (금요일 · 다음 접속은 월요일 예정, 주말엔 집에서 작업)
- **레포 상태**: 회사 클론 `main`이 원격과 동일하고 작업 트리도 깨끗함. 오늘 커밋 6개(+캐시버스터 자동 커밋). **집에서 시작하기 전에 최신본부터 받을 것** — 오늘 `index.html`·`admin.js`·`kpop_universe.css`가 모두 바뀌어서 옛 사본 위에 작업하면 충돌이 크다. 월요일 회사 복귀 때도 `git fetch origin` 먼저(집 작업분이 올라와 있을 수 있음).
- **DB에 이미 반영된 것**(사용자가 콘솔에서 실행 완료): 솔로 공연 68건 추가(`console_import.js`), 공연장 이름 564건 표준화(`venue_fix.js`). **다시 돌릴 필요 없음** — 둘 다 재실행해도 안전하지만 이미 반영됨.
- **바로 이어서 할 수 있는 것**(우선순위 순):
  1. **그룹 프로필 사진 채우기** — 기능은 오늘 완성됐고 데이터가 0건이다. 그룹 카드 왼쪽 카메라 버튼(짧게=파일, 길게=URL). 인스타 있는 그룹 186/268은 인스타 프로필을, 없는 그룹은 이미지만(클릭 불가). 채우는 즉시 즐겨찾기 목록 아바타에도 반영된다.
  2. **공연 정보 손입력** — 어드민 "공연 추가" 폼(설정 → 그룹 데이터 관리)이 오늘 생겼다. 공연 0건인 A등급 그룹부터. 나무위키 자동 수집은 적중률 17%라 보류 상태(DECISIONS 참고).
  3. 미착수로 남은 것: 어드민 콘솔 2단계(탭 통합), 편집 모달 개선(`#vid-tag-panel` 320px/z82), 가족·제작 정보를 멤버 카드 칩 줄로, 숨김 20건 수동 정리.
- **결정 대기 2건**은 위 DECISIONS 블록에 등재해둠 — ①"다녀왔어요" 시간 단위(다일 공연 66%라 공연당 1회 체크는 부정확) ②나무위키 공연 보완 계속할지. 둘 다 주간 자문 때 처리하면 된다.
- **오늘 배운 것 2가지**(같은 실수 반복 방지): ①`catch(e){}`로 **완전히 조용히** 삼키면 기능이 통째로 사라져도 아무도 모른다 — Trend 선반이 그렇게 몇 일 죽어 있었다. 삼키더라도 `console.warn`은 남길 것. ②테스트의 **전제**가 틀리면 초록불이 거짓말을 한다 — "헤드리스엔 Supabase가 없다"는 전제로 "선반이 채워지는가"를 안 봤고, `--ignore-certificate-errors` 하나면 실조회가 됐다.

- [완료·데이터입력대기][index.html][kpop_universe.css] **그룹 프로필 사진**(사용자 요청 — "인스타 프로필 넣게, 인스타 없는 옛날 그룹은 이미지만 넣고 클릭은 안 되게, 인스타 없는 개별 멤버와 같은 방식"). ①**저장**: 새 테이블 없이 `artist_pics`에 **`ko_name='' + group_ko=그룹명`**으로 넣는다 — 멤버 행은 ko_name이 반드시 있어서 키가 안 겹치고, 프리워밍(`_warmInstaPicCache`)·캐시·스토리지 업로드·즐겨찾기 아바타 경로가 전부 그대로 재사용돼 마이그레이션이 없다(`_groupPicKey`). ②**코드**: 멤버 쪽을 복사하지 않고 `_loadProfilePic(cfg)` / `_mountPicCamBtn(cfg)`로 일반화하고 `_ttPicCfg`/`_gcPicCfg`만 다르게 넘긴다(복사하면 한쪽만 고쳐지는 사고가 반드시 난다). 기존 `_loadInstaPic(a)` 호출부는 얇은 래퍼로 그대로. 클릭 타겟 우선순위도 멤버와 동일: **그룹 인스타 > source_url > 없으면 href 자체를 안 붙임**(사진은 뜨고 클릭만 안 됨). ③⚠️**레이아웃**: 그룹 카드 헤더 글자 영역이 185px인데 SNS 아이콘 6개가 139px를 먹어서, 사진(62px)을 넣으니 **268개 그룹 중 90개(34%)의 영문명이 잘렸다**("TOMORROW X TOGETHER"→"TO..."). 사진을 줄여도 아이콘이 공간 대부분을 쓰므로 해결이 안 돼서 **사진이 있을 때만 아이콘 줄을 영문명 아래로 내린다**(`#gc.has-gc-pic #gc-en-row{flex-direction:column}`). 카드 폭은 못 늘린다 — 데스크톱은 `#side-panel`, 모바일은 시트가 폭을 정하고 `el.style.maxWidth`는 `!important`에 밀린다. ④**곁다리 수정**: 사진 없는 대상으로 넘어갈 때 `img`를 안 비워서 이전 사진이 남고, 그 탓에 어드민 업로드 버튼이 큰 점선 원 대신 작은 코너 아이콘으로 뜨던 문제(멤버 카드에도 있던 것) — 사진 없음 분기에서 `src`를 비우고 `cam-overlay`를 뗀다. ⑤**남은 것**: 사진 데이터는 아직 0건 — 어드민이 그룹 카드에서 카메라 버튼(짧게=파일, 길게=URL)으로 채우면 된다. 즐겨찾기 목록 그룹 아바타도 채우는 즉시 같이 뜬다.
- [완료][index.html][tests] **탐험 패널 Trend 선반이 안 뜨던 버그**(사용자 제보). 선반 5종(기념일 → Trend → 최근 본 → Charts → Discovery) 배치·CSS·쿼리 다 정상인데 화면에만 없었다. 원인: **`vidUrl`은 전역이 아니라 선반 함수마다 자기 안에서 다시 정의하는 지역 함수**(형제 선반 20곳이 전부 같은 한 줄을 갖고 있다)인데 `_buildFeedTrend`에서만 빠뜨렸다 → ReferenceError → **`catch(e){}`가 그대로 삼켜** 선반만 조용히 사라짐. 실측: 최근 7일 영상 300건 → 쇼츠 제외 281 → 그룹당 2개 캡 119건으로 데이터는 충분했다. **고친 것**: ①지역 `vidUrl` 추가 ②`catch`에서 `console.warn`으로 흔적을 남긴다(완전히 조용한 삼킴이 이 버그를 배포까지 통과시켰다). ⚠️**기존 테스트가 왜 못 잡았나**: `tests/feed-trend.test.js`가 "헤드리스엔 Supabase가 없다"는 **틀린 전제**로 배치·크기·스크롤만 보고 "선반이 실제로 채워지는가"를 안 봤다 — `--ignore-certificate-errors`를 주면 회사망 TLS 가로채기를 넘어 실조회가 된다. 검사 6번(실렌더)과 console.warn 수집을 추가했고, **수정을 되돌려 실패하는 것까지 확인**했다.
- [완료][index.html][admin.js][kpop_universe.css][tools] **공연 직접 추가 폼 + 공연장 이름 홀 단위 복구**(사용자 요청). ①**폼**: 설정 패널 "공연 추가" → `#ev-overlay`. 공연명·종류·날짜·공연장·지역·출연(그룹/멤버 검색 칩)·포스터·예매링크. **공연장은 DB에 이미 쓰인 이름 중 고르는 게 기본**이고 없을 때만 직접 입력이 열린다(사용자 요청 — 같은 장소가 여러 표기로 갈리면 장소별 모아보기가 쪼개진다). 직접 입력 시 비슷한 기존 이름을 "혹시 이거?"로 경고만 하고 막지는 않는다. 저장 후 **출연·공연장은 남기고 공연명·날짜만 비워** 투어 날짜를 연달아 넣게 했고, 방금 넣은 건 "되돌리기"로 지운다. 같은 (제목,날짜) 중복은 **입력 중에** 경고. ②**공연장 복구**: KOPIS **목록** API의 `prfplcnm`은 시설 단위라 '올림픽공원' 한 이름에 153건이 뭉쳐 있었는데, **상세** API의 `fcltynm`은 홀까지 준다 → 체조경기장73·올림픽홀46·핸드볼경기장32로 분리. 784건 상세 재조회(실패 0) → **564건 교정**, 표준 공연장 **165곳**(`tools/venues.json`). 후원사명·로마자별칭·`(구. ○○)`는 떼고 지리적 이름만 남긴다. ⚠️**괄호가 둘 연달아 나오는 형식**(`KBS스포츠월드(아레나) (KBS아레나)`)에서 파서가 깨져 `아레나) (KBS아레나`가 나왔었다 — 최상위 괄호만 뽑는 방식으로 재작성. ⚠️**어제 "venue는 DB를 안 고친다"고 적어둔 이유(중복 되살아남)를 이번 변경이 정면으로 건드린다** → 중복 판정 키에서 **venue를 뺐고**(`console_import.js`·`kopis_sql.mjs`, 같은 팀이 같은 날 두 공연장에 설 수 없으므로 안전), 수집기 자체가 상세 API로 표준 이름을 쓰도록 `normalizeVenues()` 신설(규칙은 `kopis_venue.mjs` 한 곳에만). ③**DB 반영 완료**(2026-08-28 사용자가 콘솔에서 `console_import.js`·`venue_fix.js` 둘 다 실행). 테스트 `tests/event-form.test.js` 신설(type 목록 2곳 일치·정규화 9건·마크업↔핸들러 id 대조), `admin-markup-exposure`에 `ev-overlay` 등재.
- [완료·실기기확인필요][index.html][kpop_universe.css] **공연 목록 — 포스터 표시 + 예정/지난 분리**(사용자 요청 2건). ①**포스터**: 이미 DB에 있었다 — KOPIS 수집 때 `poster_url`을 채워둬서 **654/670건**에 들어있고 UI만 안 읽고 있었다(추가 수집 불필요). 항목 왼쪽에 52×74로 표시, 없으면 칸 자체를 안 만들어 레이아웃이 비지 않게. 죽은 이미지는 onerror로 제거. ⚠️KOPIS 포스터 URL이 `http://www.kopis.or.kr/…`라 https 사이트에서 혼합 콘텐츠로 막힐 수 있어 **표시 직전에 https + www 제거**로 교정(실측: `https://kopis.or.kr/upload/pfmPoster/…` 200 image/jpeg, www는 301). DB 값은 그대로 — venue·city와 같은 이유. ②**목록 구조**: 토글 한 번에 역대 공연이 전부 쭉 나열돼 불편했다(데이식스 23건·엔플라잉 35건). **예정은 전부, 지난 공연은 3건만** 보이고 나머지는 "지난 공연 N개 더보기"로 접는다. 펼칠 땐 버튼 자리에 그대로 치환해 스크롤 위치가 안 튄다. **미착수(논의중)**: 다녀왔어요가 공연당 1회 체크라 **다일 공연(443건, 66%)에 부정확**하고 1일 2회 공연도 구분 못 함 — 데이터 구조 변경이라 별도 결정 필요.
- [완료][index.html] **공연 지역·공연장 표기 다듬기 — 표시만, 데이터는 그대로**(사용자 요청). ①지역: `서울특별시→서울`, `인천광역시→인천`, `경기도→경기`. 접미사만 떼면 "경상북"·"충청남"처럼 어색해져 도(道) 이름과 `전남광주통합특별시`(→광주)는 예외표로 통용 약칭 사용. ②공연장: `인스파이어 엔터테인먼트 리조트→인스파이어 아레나`(19건). ⚠️**둘 다 DB를 안 고치고 표시만 바꾼 이유**: 수집 중복 판정 키가 `(title,date_start,venue)`라 venue를 DB에서 바꿔두면 **다음 분기 수집 때 KOPIS가 주는 원래 이름을 "없는 행"으로 보고 다시 넣어 중복이 되살아난다.** 지역명도 같은 이유 + 나중에 다른 소스와 대조하거나 지역별 집계할 때 공식 표기가 안전. 적용 지점 3곳(공연 목록 칩·공연장 오버레이 제목·다녀왔어요 목록)이고, 공연장 오버레이의 **조회는 원래 값으로** 해서 같은 장소가 안 갈라진다. 별칭표(`_VENUE_ALIAS`)는 실제로 헷갈리는 것만 최소로 넣을 것 — "(구. ○○)" 병기처럼 이미 알아볼 만한 건 안 건드린다.
- [완료][tools][index.html] **공연 정보 670건 DB 적재 완료 — KOPIS 파이프라인 1차 종료**(2018~2026, 168개 그룹). 수집 26,705건 → 매칭 716건 → 오탐 46건 제외 → **670건**. 상위: 엔플라잉35·원위28·데이식스23·악뮤23·엑스디너리히어로즈22·god14·슈퍼주니어11·더보이즈11·방탄소년단11. **적재 경로**: SQL 에디터 붙여넣기가 e03에서 반복 실패()했는데, 파일은 문법상 정상이었고(문자열 밖 미지 토큰 0·따옴표 균형·세미콜론/제어문자 없음) 같은 크기의 e01·e02는 성공했으므로 에디터/붙여넣기 문제로 판단 → **그 경로를 버리고** 을 레포에 커밋해 raw URL로 받고 관리자 브라우저 콘솔에서 기존 Supabase 클라이언트로 배치 insert(). 중복은 로 클라이언트에서 걸러 SQL과 동작 동일, 재실행 안전. ⚠️스니펫은 **평문 .js 파일로** 제공해야 한다 — 마크다운 코드블록에서 복사하면 작은따옴표가 스마트쿼트로 바뀌어 이 난다(코드부에 작은따옴표를 아예 안 쓰는 것으로 한 겹 더 방어). **이번 건에서 내가 틀린 것 4가지**(전부 "확인 안 하고 짐작"): ①uid=197609(user) gid=197121 groups=197121가 uuid인데 text로 가정 ②이 NOT NULL인데 기본값 없음을 몰랐고 ③에 CHECK 제약이 있는데 "쇼케이스"라는 없던 값을 새로 만듦(바로 앞에서 "기존 어휘를 맞춰야 한다"고 써놓고) ④2년치를 한 번에 요청해 페이지 상한 5,000건에 걸려 오래된 쪽이 조용히 잘린 것. **남은 것**: 인증키 유효기간 1년·3개월 미사용 시 자동 취소(DECISIONS 등재).
- [진행중][tools] **KOPIS 실데이터 검증 — 데이터소스 유효 확인 + 매칭 정밀도 47%→100%**. 명령이 줄바꿈돼 `--find` 대신 전체 수집이 돌아버렸는데 오히려 그게 답을 줬다: 2025~2026 무필터 **5,000건** 중 aespa(고척스카이돔)·NCT 127·스트레이키즈·엔하이픈·레드벨벳·STAYC·보이넥스트도어·프로미스나인·TWS·트리플에스·QWER·PLAVE·빅뱅·악뮤·2PM·CNBLUE 등이 그대로 들어있었다 → **KOPIS는 K팝 공연을 잘 갖고 있고 데이터소스로 유효하다**(앞서 대중음악 표본 17건에 아이돌이 없던 건 표본이 1페이지뿐이라서였다). ⚠️단 **오탐이 절반**이었다(75건 중 ~40건): "이상한 나라의 앨리스"→앨리스, "발레 시그니처"→시그니처, "퀸 엘리자베스 콩쿠르 위너 콘서트"→위너, "신카이 마코토 하이라이트 필름"→하이라이트, "우리 신화 이야기"→신화, "다 내 아이들"→아이들, "천체관측 vol.15"→**피프틴앤드**(en이 `15&`라 숫자 "15"가 걸림). **한 일**: ①실제 응답 제목으로 회귀 픽스처 `tools/kopis_fixture.json`(57건, 양성26·음성31) 신설 — 합성 표본이 아니라 눈으로 라벨링한 실데이터 ②숫자만인 토큰 차단 ③흔한단어형 그룹명(앨리스·시그니처·아르테미스·하이라이트·위너·네이처·신화·아이들·레인보우·시크릿·트레저·슈가 등)은 **K팝 공연 신호**(WORLD TOUR·FANCON·단독 콘서트 등)가 같이 있을 때만 인정 ④멤버 이름 매칭도 같은 신호 요구. ⚠️**중간에 틀린 가정 2건 정정**: (a) "영문 표기로 걸리면 예외"로 뒀는데 WINNER·NATURE·TREASURE·Sugar가 전부 en으로 새어나갔다 — 영문 그룹명도 일상 영단어인 경우가 흔하다, 예외 제거 (b) 그룹 쪽에만 게이트를 걸었더니 "이상한 나라의 앨리스"가 **멤버 '앨리스'로 새어나갔다** — 멤버 매칭에도 동일 게이트. **결과: 재현율 100%(26/26) · 정밀도 100%(오탐 0)**. **남은 것**: 대중음악 장르코드 미확정(실측에서 AAAA=연극/CCCA=클래식/CCCC=국악/EEEA=복합/GGGA=뮤지컬만 나옴 — 후보 19개로 확대해 재실측 필요). 장르 필터를 걸면 오탐 원천이 더 줄어든다.
- [완료][index.html][kpop_universe.css] **KOPIS 출처 표기 추가**(오픈API 이용약관 준수). 약관이 "API를 활용해 서비스를 개발할 경우 아래 사항을 반드시 함께 명시"하도록 요구 → 공연 목록(`.concert-list`) 안에 "공연 정보 제공 · 공연예술통합전산망(KOPIS)" 한 줄 추가(별도 페이지가 아니라 **데이터 옆**에 있어야 표기 의미가 있고, 목록이 열릴 때만 보여 평상시 화면은 안 건드린다). ⚠️약관의 "집계기간·집계대상·발권 분량 기준" 세 줄은 **예매/공연 통계 API(prfsts*) 전용** 문구다 — 지금 쓰는 건 DB검색(공연목록·공연상세)뿐이라 해당 없음. 나중에 통계 API를 쓰게 되면 그때 세 줄을 여기에 추가해야 한다.
- [진행중·키대기][tools] **공연 정보 수집 파이프라인 — KOPIS 오픈API 경로 확정 + `tools/kopis_events.mjs` 신설**(사용자 요청: "아티스트별 공연 정보를 거의 안 채워뒀다, NOL·멜론티켓 위주로 수집 방법 고민"). ⚠️**NOL 티켓은 제외 결정**: `tickets.interpark.com/robots.txt` 실측 결과 `User-agent: * → Disallow: /`(Googlebot·Yeti·DAUM·Twitterbot·facebookexternalhit 5종만 허용)로 **전 경로 크롤링이 명시적으로 금지**돼 있다. `nol.interpark.com`은 일부 경로만 막혀 있으나 실제 공연 상품 데이터는 `tickets.` 도메인이라 대상이 안 된다. 멜론티켓은 robots.txt가 없지만(404) 목록이 JS 렌더라 Playwright 의존 + 개편에 취약. → **KOPIS(공연예술통합전산망) 공식 오픈API로 결정** — 무료·구조화·안정적이고 기존 `kpop_events(id,title,groups[],date_start,date_end,venue,city,official_url)` 스키마와 거의 1:1 매핑된다. ⚠️**키 발급 경로 주의**: KOPIS 본 사이트 회원가입은 "My통계"용이라 **공연시설·기획제작사만** 가능(사업자등록증·재직증명서 요구) — 사용자가 여기서 막혔다. 실제 경로는 **공공데이터포털 https://www.data.go.kr/data/15097805/openapi.do (예술경영지원센터_공연예술통합전산망_DB검색_공연목록, XML, 활용신청 → 자동승인)**로, 개인 회원가입만으로 즉시 발급된다. **스크립트**: 읽기 전용(DB 직접 쓰기 없음 — kpop_events는 admin RLS라 SQL만 출력하고 저장은 사용자가 admin 세션에서). 공연명→그룹 매칭은 `_m2ParseTitle`을 그대로 안 쓰고(유튜브 제목 문법에 조율돼 있음) **거기서 검증된 두 방어만 이식**: ①흔한 단어형 토큰 단독 매칭 금지 ②동명이인(같은 이름 2인 이상)은 그룹 표시 없으면 버림. 그룹 en·altNames도 매칭(보넥도/제베원/비스트 등). `--selftest`로 키 없이 매칭기 점검 가능 — **합성 표본 10건 전부 통과**(별칭·영문명 양성 6건 + 흔한단어 오탐 방지 음성 4건). ⚠️합성 표본이라 **실제 재현율은 키 받은 뒤 실데이터로 다시 재야 함**. 예매 링크(official_url)는 KOPIS가 제공 안 해 비워 두기로(사용자 결정 — 정보부터 채우자).
- [완료·실기기확인필요][index.html][kpop_universe.css][tests] **탐험 패널에 "📈 Trend" 선반 신설 — 최근 7일 업로드 영상을 랜덤 순서로**(사용자 요청: "최신 정보·영상이 중요한데 Discovery의 Trend 카드 하나로는 부족"). 위치는 Today's Anniversary 바로 밑, 가로 스크롤, 카드 크기는 Charts와 동일(선택자를 묶어서 나중에 크기를 바꿔도 둘이 안 어긋나게). **기존 Discovery Trend 카드와 목적이 다름**: 그건 `category='live'` **조회수 상위**를 한 장으로 접어 두는 카드(눌러야 목록)이고, 이 선반은 **업로드일 기준** 개별 영상을 바로 펼친다. **기준**: 최근 7일(`_FEED_TREND_DAYS`) · 그룹당 최대 2개(쏠림 방지, `_capByGroup` 재사용) · 최대 24개 · 노출 순서 랜덤(순위표처럼 보이지 않게 — Discovery Trend에서 확립된 규칙) · 숨김/무관/보류/기타 및 밴 인물 필터는 다른 피드 카드와 동일 · **쇼츠 제외**(이 선반 썸네일 틀이 16:9라 세로 영상은 좌우가 크게 남음) · 3개 미만이면 선반 자체를 숨김. 서브는 "그룹 · N일 전"(조회수 안 씀, 같은 이유). 카드를 누르면 **선반 전체가 재생목록**이 돼 그대로 다음 영상으로 넘어감(훑어보기 목적). ⚠️**작업 중 발견한 죽은 코드**: 최상단에 있던 `#feed-anniv` 휠 핸들러는 ①메인 `<script>`(1197~17758)가 `#feed-body`(17765)보다 먼저 끝나 그 시점엔 요소가 없고(`?.`라 조용히 무시) ②`_enableDragScroll`이 이미 같은 휠 처리를 품고 있어 **완전한 중복·무효 코드**였다. 실제로 동작하던 경로는 `_buildFeedAnniversaries`의 지연 바인딩. 제거하고 Trend도 같은 지연 바인딩 패턴을 따르게 함. **검증**: `tests/feed-trend.test.js` 신설 6항목(배치 순서·빈 상태 숨김·Charts와 카드 폭/글자 크기 동일·드래그 바인딩 실제 적용·7일 기준·예외 0건). ⚠️헤드리스엔 Supabase가 없어 실제 쿼리 경로는 못 탐 — 실기기 확인 필요.
- [완료·실기기확인필요][index.html] **연결 카드 칩 클릭을 전체 재렌더 → "함께한 영상" 섹션 부분 갱신으로**(사용자 불만 "칩 누르며 영상 확인하는 왕복이 번거롭다"의 구조적 원인 제거). 예전엔 칩을 누를 때마다 `container.innerHTML=''` + 전체 `_buildConnContent` 재구성 + 0.18s 페이드가 돌아서 가족·제작·칩 전체가 매번 다시 그려졌다. 칩 선택이 실제로 바꾸는 건 **영상 목록뿐**이므로 그 섹션(`_renderConnVidSec`)만 다시 그린다. `Only` 토글도 같은 경로. 섹션 노드가 떨어져 나갔으면 예전처럼 전체 재구성으로 폴백. ⚠️**같이 고친 잠복 버그**: 칩 클릭 핸들러가 생성 시점 클로저 값 `isSel`로 추가/제거를 판정했는데, 부분 갱신으로 칩 DOM이 살아남게 되면서 **같은 칩을 두 번째 누르면 판정이 뒤집히는** 상태가 됐다 — 클릭 시점에 `_connSelected.includes(name)`를 다시 보도록 수정(헤드리스로 2연타 검증: selected_n 1→0, 칩 sel 클래스 정상 해제). 그룹 칩은 칩 목록 자체를 바꾸므로 전체 재구성 유지(의도적 범위 제한). `conn_chip_select`에 `partial` 파라미터 추가해 부분 갱신 적중률도 같이 계측. **검증**: 헤드리스에서 섹션 노드 재사용(secReused) + 2연타 토글 확인, smoke·conn-lines·interaction·lb-topbar·conn-btn-loading·lb-swipe·tabbar-open 회귀 없음.
- [완료][index.html] **연결 카드 계측 신설 — 지표를 "사용률"이 아니라 "도달 시간·탭 수"로**(Fable 자문 정정 반영). 사용자의 실제 불만은 "쓰냐 안 쓰냐"가 아니라 **로딩이 길고 칩↔영상 왕복이 번거로워 즉각성이 떨어진다**는 것이었는데, 나(클로드 코드)는 `#conn-sheet{height:58vh}`(우주 58% 가림)를 근거로 형식 논쟁을 하고 있었다 — **그건 내 가설이지 사용자 불만이 아니었다.** 지표가 불만과 같은 축이어야 고칠 것이 정해진다. **신설 이벤트 4종**: `open_conn_card`(first_render_ms·collab_n) / `conn_api_enrich`(ms·rerender_ms·added) / `conn_chip_select`(rerender_ms·tap_n·selected_n) / `conn_video_play`(ms_from_open·taps·selected_n, isAdvance 자동재생은 제외). 기존 `_gaEvent`(perf 계열과 같은 방식) 재사용, 전부 try/catch. **코드로 확인한 병목 구조**(1단계에서 이 숫자를 근거로 고침): ①첫 렌더가 `_getCollabSongs` 전체 조회 + 이름별 `_collabCount` 정렬 호출 ②칩 클릭 때마다 `container.innerHTML=''` + 전체 `_buildConnContent` 재구성 + 0.18s 페이드 ③API 보강 도착 시 또 전체 재렌더. ⚠️**정정 2건**: (a) 앞서 "정적 정보(가족·제작) 때문에 카드가 무겁다"고 한 내 진단은 **틀렸다** — 코드 주석의 실측 원인은 `_getCollabSongs` 반복 호출이고 이미 캐시로 해결됐다(가족 18명·제작 3그룹 순회는 비용 없음). (b) "칩을 골라야 영상이 보인다"도 틀렸다 — `_getCollabSongs(anchor,[],…)`가 무필터 전체를 반환해 **열자마자 전체 콜라보 영상이 이미 뜬다**. ⚠️**Fable 주장 중 미채택**: "곧 fan·influence 연결 타입이 추가된다"는 아직 사실이 아님 — `connections.json` 실측 **4,924건 전부 `type:"member"`**, 나머지는 데이터 0건이라 그걸 전제로 구조를 뒤집는 건 이르다. "굵기 비례"도 WebGL `linewidth` 미지원으로 여전히 불가. **다음**: ①칩 클릭을 영상 섹션 부분 갱신으로 교체(왕복 끊김의 구조적 원인) ②첫 오픈 병목은 계측 실측 확정 후 수리 ③가족·제작을 멤버 카드 칩 줄로(독립) ④연결 모드는 계보 데이터가 실물로 생길 때 재론(보류).
- [완료·실기기확인필요][index.html][tests] **연결 카드를 열면 우주가 반응하게 — 연결선 0개 문제 해결**(사용자 문제 제기: "이 연결 카드라는 형식 자체가 연결을 보여주는 최선일까"). 코드 확인 결과 근거가 있었다: `openConnCard`가 `_connSelected=[]`로 시작하는데 `_updateConnLines3d`가 **선택된 것만** 그려서 **카드를 여는 순간 연결선이 0개**였고, `_applyConnFocus`도 선택이 없으면 앵커 그룹만 setFocus하고 끝이라 **우주가 사실상 반응하지 않았다**. 철학 문서가 "발견" 축의 대표로 꼽는 연결선이 주인공이 아니라 부산물이 돼 있던 것. **한 일**: ①선택이 없으면 **콜라보 상대 전부를 밝게, 나머지 페이드**(`_applyConnFocus`) — 상대가 수백 명이어도 화면이 안 지저분하고 드로우콜도 안 늘어 이쪽이 주력. ②연결선은 콜라보 편수 **상위 30개까지만** 은은하게(0.26), 칩을 고르면 그것만 밝게(0.72)·나머지는 더 죽임(0.10). ③선을 개별 `THREE.Line`에서 **층별 `LineSegments` 하나**로 합쳐 상대가 몇 명이든 **드로우콜 2개 이하**(발열 지표는 fps가 아니라 드로우콜 — PRINCIPLES). ④`_clearConnLines3d`에 `dispose` 추가(예전엔 scene.remove만 해서 열고 닫을 때마다 GPU 버퍼가 쌓였음). ⑤API 보강이 뒤늦게 도착하면 `_updateConnLines3d`+`_applyConnFocus` 재호출(카드만 갱신되고 우주는 안 따라가던 것). ⑥`closeConnCard`의 포커스 복원을 `_connSelected.length` 조건 없이 무조건으로(선택 없이 열었다 닫으면 상대가 밝은 채로 남던 문제). **Fable 자문 반영/정정**: 헤어볼 우려(①상한)와 포커스 전환(②), API 지연(⑤)은 타당해서 채택. 단 **"굵기를 콜라보 수에 비례"는 채택 불가** — WebGL에서 `LineBasicMaterial.linewidth`는 대부분의 드라이버가 무시하고 1px로 고정한다(쓰려면 Line2/LineMaterial 별도 도입 필요). **"그룹 단위 집계선"도 보류** — 카드 칩은 멤버 단위인데 선만 그룹 단위면 카드↔우주 대응이 깨진다. **검증**: `tests/conn-lines.test.js` 신설 8항목 — 열림 시 선 존재 / 드로우콜 상한(200명 주입해 강제로 태움) / 강조 대비 / 헤어볼 상한 30 / 선택 0에서의 포커스 / 닫기 시 복원 / dispose. 수정 전 4건 실패 → 후 전부 통과. **미착수**: 연결 버튼 위치(이름 줄 vs 소속 줄)는 이 변경을 실기기에서 체감해본 뒤 결정하기로.
- [완료·실기기확인필요][kpop_universe.css][tests] **재생모드 상단바 멤버 칩이 두 줄로 넘쳐 영상 위를 덮던 것 + 줄 간격**(사용자 제보, 세이마이네임 7명 스크린샷). 원인: `#yt-lb-member-info`에 걸린 `max-width:56vw`. 이건 원래 **1줄(그룹명)이 ⌄/⋮ 버튼과 안 부딪히게** 하려던 제약인데 그 아래 멤버 칩 줄까지 같이 묶여 있었다. 390px 실측 — 컨테이너 폭이 **195px뿐이라 좌우로 98px씩 놀고 있었고**(사용자 지적: "히토미 왼쪽, 소하 오른쪽에 자리가 충분하다"), 칩 7개는 **334px면 한 줄**에 들어간다. **한 일**: ①폭 제약을 컨테이너→**그룹명 줄(`#yt-lb-anchor-chip`)에만** 이동, 컨테이너는 전체 폭(좌우 12px 패딩). ②칩 줄은 넘쳐도 **줄바꿈이 아니라 가로 스크롤**(`flex-wrap:nowrap`+`overflow-x:auto`+`overscroll-behavior-x:contain`) — 줄바꿈을 허용하면 컨테이너가 `position:fixed`라 세로로 자라며 그대로 영상을 덮는다. `width:max-content`+`max-width:100%`라 칩이 적을 땐 내용만큼만 차지해 가운데 정렬되고, 좌우 빈 영역은 이 줄의 박스 밖이라 탭이 그대로 영상으로 통과한다. ③1↔2줄 간격 `7px→4px`(사용자: "예전엔 너무 좁았는데 이젠 너무 크다"). **결과 실측**: 7명 2줄→**1줄**, 상단바 바닥 110→**78px**(영상 상단과 정확히 접함, 침범 0). **검증**: `tests/lb-topbar.test.js`에 7명(제보 사례)·14명(과밀) 케이스 추가 — 줄 수·overflow-x·상단바↔영상 겹침·그룹명↔버튼 충돌까지 확인. 수정 전 2건 실패(7명 2줄 / 14명 4줄) → 후 전부 통과. **디자인 판단**: 멤버를 둘째 줄 텍스트 칩으로 두는 방식 자체는 유지 — 칩이 해당 멤버 카드로 가는 탭 진입점이라 이름 텍스트가 필수고(아이콘 대체 불가) 하프시트 동선이 이미 그 위에 있음. 바꾼 건 폭 제약과 간격뿐.
- [완료·실기기확인필요][kpop_universe.css][tests] **"연결" 버튼 로딩 스피너가 글자 위에 겹쳐 보이던 것**(사용자 제보). CSS는 원래부터 `.conn-btn-loading{color:transparent}`로 글자를 숨기려 했는데, 기본 규칙 `.tt-conn-btn`에 `transition: … color .18s …`가 걸려 있어 글자가 0.18초에 걸쳐 **서서히** 사라진다. 그런데 클릭 핸들러가 로딩 클래스를 붙인 직후(rAF 2번 뒤) `openConnCard`를 호출해 **메인 스레드를 통째로 막는다** → 트랜지션이 시작만 하고 진행을 못 해서, 글자가 거의 그대로 남은 채 그 위에 스피너만 얹힌 상태로 몇 초간 얼어붙었다. 로딩 표시는 부드럽게 바뀔 이유가 없으므로 `transition:none`으로 즉시 전환(한 줄). **검증**: `tests/conn-btn-loading.test.js` 신설 — 수정 전 1건 실패 → 후 4항목 통과. ⚠️테스트 방법 함정 2개를 주석으로 못박음: ①클래스 추가 **직후** getComputedStyle을 읽으면 트랜지션 유무와 무관하게 목표값이 나와 아무것도 구분 못 한다(처음에 그렇게 짰다가 수정 전 코드도 통과함) → 두 프레임 뒤에 잴 것. ②헤드리스는 화면이 없어 트랜지션이 t=0에 멈춰 있어 "되돌아오는" 애니메이션은 시간으로 못 잰다 → 클래스 없는 새 요소의 기준값과 비교할 것. **미착수(별건)**: 라이트박스의 "연결 보기"(`#yt-lb-conn-btn`)는 로딩 표시가 아예 없이 `openConnCard`를 동기 호출해 잠깐 멈춘 것처럼 보임 — 같은 스피너를 붙일지 미정.
- [완료·복구필요][admin.js][tests] **동기화가 겸임 멤버를 동명이인으로 오판해 즉시 숨김하던 버그**(사용자 제보: "이퀄러브 히토미·해찬 등 숨김할 영상이 아닌데 바로 숨김처리됨"). 경로: `_m2ParseTitle`의 애매성 판정(2026-08-26 옵션 A) → `confidence:ambiguous` → `_extBuildRows`(admin.js:4502)가 동기화 시점에 `content_flag:hidden`+`members:[]` 저장 → 유저 화면에서 즉시 사라짐. **근본원인**: 판정이 `토큰에 걸린 사람들의 **그룹 합집합** 크기>=2`였는데 **겸임 멤버는 혼자서도 2를 넘긴다** — 엔시티 해찬(127+드림), 세이마이네임 히토미(세이마이네임+아이즈원). 바로 위 주석은 "서로 다른 그룹 2곳 이상의 **사람**과 겹치는 토큰"이라고 의도를 적어놨는데 코드가 사람이 아니라 그룹을 세고 있었다. **영향 범위 실측**: 겸임 95명 중 **동명이인이 아닌 81명**이 오판 대상 — 안유진·장원영·사쿠라·은하·김세정·박우진·유연정 등 영상이 많은 대형 그룹 멤버 다수. **수정**: 사람 수 게이트를 먼저 건다(`people.size<2`면 겸임이어도 애매 아님), 그 뒤에 기존 "그룹 2곳 이상" 조건 유지 → 진짜 동명이인(#JIHOON→박지훈)은 그대로 검수행. **검증**: `tests/matching.test.js`에 그룹표시 없는 제목(역추론 경로) 케이스 3개 추가 — 수정 전 2건 실패 → 수정 후 77/77. ⚠️기존 "장원영" 회귀 테스트는 제목에 그룹명이 있어 역추론 경로를 안 타 이 버그를 못 잡았다(그룹표시 없는 제목으로 검증할 것). ⚠️**남은 일**: 이미 숨겨진 기존 행 복구 — 기존 `숨김 목록 재판정` 버튼은 `ng===group_ko`면 "판정 동일"로 건너뛰어서 이 케이스를 **못 살린다**. 별도 복구 스윕 필요(미착수). 곁다리: "이퀄러브"·"에이엔"은 등록 그룹이 아님(268개 중 없음), "준서"는 ko까지 겹치는 동명이인 3명이라 애초에 null로 버려져 이 경로와 무관 — 별건으로 확인 필요.
- [완료][index.html][PRINCIPLES.md][tests] **어드민 마크업을 `<template>`으로 격리 — 로그아웃 상태에서 읽기 모드로 어드민 텍스트가 읽히던 것**(사용자 제보: 크롬 시크릿 창에서 Alt+Shift+R 누르니 "출연 멤버 지정 / 타 그룹 멤버 / 영상 포맷 (관리자)" 등 영상 편집 모달 내용이 그대로 읽힘). 원인: 어드민 UI 8블록(약 434줄, 한글 약 2,400자)이 index.html에 그대로 있고 **CSS `display:none`으로만** 가려져 있었다. 읽기 모드·스크린리더·검색엔진은 렌더링 결과가 아니라 **원본 DOM을 순회**하므로 `display:none`을 무시한다. ⚠️**내 1차 진단이 틀렸던 이유**: `innerText`로 검증했는데 그건 CSS를 존중해서 "안 샌다"는 오답을 줬다 — 검증 지표를 잘못 고른 것(PRINCIPLES에 명시). **한 일**: `sp-yt-sec`/`vm-overlay`/`adm-home-overlay`/`hnn-overlay`/`gp-overlay`/`vid-tag-overlay`/`fbv-overlay`/`vm-coverset-overlay` 8블록을 `<template class="adm-tpl" data-adm-mount>`로 감싸고(내용이 문서 트리 밖 inert fragment가 되어 DOM 순회로 도달 불가), `_mountAdminMarkup()`이 관리자일 때만 복제 주입(`_updateAdminUI` 안, 멱등). 착수 전 전수 확인: 블록 안 id **191개 중 index.html 본문이 참조하는 건 5개뿐이고 전부 이미 null 가드**가 있어 익명 상태에서 요소가 없어도 안 깨짐. **범위 밖(그대로 유효)**: 기능적 위험은 원래 없었음 — admin.js는 관리자에게만 전송되고(익명이 받는 JS는 shared.js·sw.js뿐) DB는 RLS로 막힘, 시크릿/API 키 0건(Supabase는 공개용 `sb_publishable_`, YouTube 키는 어드민 localStorage에만). 소스 보기(Ctrl+U)로는 여전히 보이며, 그것까지 없애려면 마크업을 admin.js로 이관해야 함(어드민 콘솔 2단계와 묶는 게 효율적). **검증**: `tests/admin-markup-exposure.test.js` 신설 — `<script>/<style>` 제외 TreeWalker 텍스트 기준(innerText 함정 회피), **수정 전 4항목 실패 → 수정 후 8항목 전부 통과**. `admin-dock.test.js`도 준비 조건에서 어드민 요소를 빼고 주입 호출 추가.
- [완료][tests] **`shorts-promote.test.js`가 2026-08-27부터 빨간 상태였던 것 수정**(위 작업 중 전체 스위트 돌리다 발견). 8/27 쇼츠 판별 통일(`bab3c8f9`)에서 곡 객체용 `_songIsShort` 헬퍼가 새로 생겼는데, "직접 비교 잔재" 검사가 `_isShortV`만 이름으로 면제하고 있어 새 헬퍼의 **정의 줄 자체**를 위반으로 잡았다. 헬퍼가 또 늘어나도 안 깨지게 이름 대신 "헬퍼 정의 줄" 패턴으로 면제하도록 일반화. (참고: `rls.test.js`는 회사망 TLS 가로채기(`SELF_SIGNED_CERT_IN_CHAIN`)로 이 망에서 실행 불가 — 코드 무관, 집에서 확인 필요.)
- [완료·실기기확인필요][index.html][kpop_universe.css][tests] **재생(브라우즈) 모드에서 재생목록 훑다가 재생 모드가 꺼지던 것 — 제스처 소유권 도입**(사용자 제보 "재생목록 아래로 스크롤하며 둘러보는데 아예 스와이프 다운되면서 재생 모드가 꺼지는 경우 종종 발생"). 근본원인: "당겨서 닫기" 판정이 `_lbScrollEl.scrollTop<=0 && dy>0`를 **touchmove마다** 다시 보는데 `dy`가 증분이 아니라 **터치 시작점부터의 누적 이동량**이라, 목록을 위로 되감다 맨 위(scrollTop 0)에 닿는 순간 조건이 참이 되고 그때 이미 100~200px 쌓인 dy로 곧장 pull 모드에 진입 → 손 떼면 임계값(90px)을 넘겨 닫힘. 즉 **목록을 훑던 손짓이 도중에 닫기 제스처로 승격**됐다. **한 일**: `_lbStartedAtTop`/`_lbDidScroll` 신설 — 한 터치 안에서 목록이 실제로 아래로 스크롤됐으면 그 제스처는 끝까지 스크롤이고, 닫기는 "처음부터 맨 위에서 시작한 아래 당김"일 때만 인정. ⚠️iOS 고무줄로 `scrollTop`이 음수가 되는 건 스크롤로 세지 않도록 `>2` 기준(안 그러면 정작 맨 위에서 당겨 닫기가 막힘). `#yt-lb-uplist`에 빠져 있던 `overscroll-behavior:contain`도 추가(바운스가 바깥으로 새던 것 — 다른 스크롤러들은 이미 갖고 있었음). **검증**: `tests/lb-swipe-ownership.test.js` 신설 — 합성 터치에 "그 시점의 네이티브 scrollTop"을 같이 주입해 실기기와 동일한 입력을 재현. **수정 전 2건 실패(되감기 손짓으로 닫힘 / overscroll 없음) → 수정 후 6항목 전부 통과**. 맨 위에서 당기는 정상 닫기·바운스 구간 닫기·평범한 아래 훑기도 같이 회귀 고정. lb-topbar·tabbar-open·interaction·smoke 회귀 없음.
- [완료·실기기확인필요][index.html][PRINCIPLES.md][tests] **카드를 "열 때" 하단 탭바 복구 — 웹앱에서만 탭바가 사라지던 것**(사용자 제보: "웹앱에서 별 탐색 버튼 누르면 카드는 열리는데 탭바가 사라짐, 사파리는 멀쩡"). 근본원인: `tab-hidden`을 떼는 코드가 **닫기 경로 4곳**(closeMobSheet/스택 pop/연결카드 닫기/라이트박스 닫기)에만 있고 **여는 경로엔 하나도 없었다**. `openMobSheet`는 `_lastScroll`·`_tbAccum`만 리셋하는데(주석엔 "숨김 고착을 막는다"고 적혀 있었지만) 추적값을 되돌려도 클래스는 안 떨어진다. 탐험(별) 버튼은 `closeMobSheet` 로직을 인라인 복사하면서 복구 두 줄만 빠뜨렸고, `openConnCard`는 복구는 물론 스크롤 추적값 리셋도 없었다. ⚠️**"멤버 카드는 되는데 그룹 카드는 안 되던" 이유**: `showT`는 내부에서 `closeConnCard()`를 부르고 그 안에 탭바 복구가 들어있어 **우연히** 살아났고, `showGC`엔 그 호출이 없다 — 처음 쓴 테스트가 멤버 경로만 봐서 수정 전에도 통과해버렸다(테스트에 주석으로 못박음). ⚠️**사파리에서 안 드러난 이유**(= 웹앱 vs 사파리 차이가 반복되는 구조): 유일한 복구 안전망이 스크롤 핸들러의 "최상단 40px면 무조건 표시"인데 **scroll 이벤트가 와야만 돈다**. 사파리는 주소창 때문에 세로가 짧아 카드가 거의 항상 스크롤 가능 → 안전망이 즉시 발동해 버그를 덮었고, standalone 웹앱은 세로를 100px 가까이 더 써서 짧은 카드가 아예 안 스크롤 → 이벤트가 한 번도 안 나 고착. **한 일**: `_showTabbar()` 헬퍼 신설 + 여는 경로 3곳(`openMobSheet`/탐험 버튼/`openConnCard`)에서 호출, `openConnCard`에 스크롤 추적값 리셋 추가, `_ensureTabbarIfUnscrollable()` + 카드 본문 ResizeObserver로 "탭바 숨은 뒤 내용이 짧아져 스크롤 불가"까지 커버(무한루프는 "이미 보이면 즉시 반환" 가드로 차단), `updateSheetBottom`에서도 회전/뷰포트 변화 시 확인. **검증**: `tests/tabbar-open.test.js` 신설 — 웹앱 조건(390x1100 + display-mode:standalone) 재현, **수정 전 3건 실패(그룹 카드/연결 카드/스크롤 불가) → 수정 후 전부 통과** 확인. 스크롤 다운=숨김/업=표시(사파리와 동일 동작)도 회귀 고정. interaction·smoke·lb-topbar·pwa-safearea 회귀 없음. PRINCIPLES.md에 "사파리 vs 웹앱 차이의 정체" 절 신설(상태는 이벤트 부수효과가 아니라 진입 시점에 못박을 것).
- [완료·실기기확인필요][kpop_universe.css][index.html][admin.js][tests] **어드민 4개 패널 좌측 도킹 전환 (admin_improvement_plan 0+1단계 — 원안 P1-3~4의 핵심 가치만 선취)**. 착수 전 소스 실측으로 8/27 진행도 검토를 정정함: **P1-1 이벤트 가드는 이미 완료**(`index.html:9966` pointerdown / `10148` click이 `closest('[data-modal]')`, `9993` mousemove가 `elementsFromPoint`에 `[data-modal]` 포함 — `_anyModalOpen()` 헬퍼는 불필요), **P1-2 동명이인→카드 이동도 이미 완료**(`admin.js:1685 _dqGotoArtist`, 데스크톱 rAF 보완 오픈까지 있음. 8/27에 "심볼 안 잡힘"이었던 건 index.html만 봤거나 null byte 때문에 grep이 binary로 스킵한 탓), **버튼 수는 64개가 아니라 33개**(66은 input·hint 포함 `sp-*` id 전체), **캡션 32/33 + 섹션 8개로 "B안(캡션·그룹핑)"은 이미 소진**. → 남은 통증의 진짜 원인은 폭이 아니라 **z-index**(카드 61~63 vs 어드민 81~82)라고 판단, 원안의 "탭 5개 통합"은 뒤로 미루고 **도킹만 먼저** 했다. **한 일**: `adm-home`/`hnn`/`vm`/`gp` 4개 오버레이를 데스크톱에서 z58 좌측 도킹(폭 `min(520px,44vw)`, 전체 높이)으로. DOM·핸들러·탭 구조는 **한 줄도 안 옮김** — 기능 이관이 0이라 누락 사고 위험 없음. 루트는 `inset:0` 유지하되 백드롭 제거 + `pointer-events:none`, 패널만 `auto` → 기존 `data-modal` 가드 3곳이 **코드 수정 없이** 그대로 성립(패널 위=모달 안, 우주 위=모달 밖, 백드롭 닫기 핸들러는 이벤트가 루트에 안 닿아 자동 무력화). ⚠️**첫 스크린샷에서 걸린 함정**: 좌측 슬롯은 탐험 패널·저널 사이드바(둘 다 `left:0`·300px·**z66**)가 이미 쓰고 있고 탐험 패널은 세션마다 자동으로 열려서, 도킹 패널 왼쪽 300px가 통째로 가려졌다 → `body:has(...도킹.open)`으로 둘을 접고(`.open`은 유지해 닫으면 자동 복귀) `#zoom-ctrl`(z50)도 도킹 폭만큼 이동. 도킹은 홈과 좌측 슬롯 하나를 공유하므로 vm/hnn/gp 헤더에 **🏠 홈 복귀 버튼** 신설(`.adm-back-home`, 모바일 숨김). 모바일은 계획대로 기존 중앙 모달 그대로. **검증**: `tests/admin-dock.test.js` 신설 — 도킹 기하 4종 + 가드 [A]패널위/[B]우주위/[C]루트 미히트 + 실제 pointerdown 주입으로 "패널 안 클릭은 `closePanels` 안 돎 / 배경 클릭은 돎" + **도킹과 진짜 카드 동시 표시**(dock 12~532 / side-panel 1120~1440, 겹침 0) + 모바일 회귀까지 12항목 전부 통과. 기존 smoke·interaction·vm-panel·vm-panel-ui·lb-topbar 회귀 없음. **남은 것**: 2단계(4개 패널 → 콘솔 셸 탭 5개 통합)는 도킹을 실제로 써본 뒤 필요성 재판단, 3단계 편집 모달(`#vid-tag-panel` 아직 320px·z82·썸네일 미리보기 없음 — 8/27 "부분"이 아니라 거의 미착수였음)은 그 다음.

---

## 2026-08-27
- [예정·내일착수][index.html][kpop_universe.css] **어드민 설정 패널 → 좌측 도킹 콘솔 재구성 (admin_improvement_plan P1-3~4)**. 검토 완료(→ `admin_improvement_plan.md` "2026-08-27 진행도 검토" 표 참조). **이미 된 것**: 섹션 그룹핑 9개 + 주요 액션 버튼 캡션 완비(캡션 없는 건 입력창·초기화·피드백·힌트토글 등 자명 요소뿐 → 보강 불필요 확인함) · 매일루틴 원클릭 · undo/스냅샷. **남은 핵심(=이 작업)**: 콘솔 도킹 전면 — `#sp-yt-sec` 64버튼 + 오버레이 3개(검수 hnn / 영상관리 vm / 그룹우선순위 gp)를 **z≈58 좌측 도킹 콘솔(탭 5개: 홈·동기화·검수·영상·정합성)**로 이관, 카드(z61~63)와 나란히 띄워 검수 왕복 제거. **착수 순서**: ①선행 — 이벤트 가드 재검증(`data-modal` 30곳 적용됨, window `pointerdown`/`click`/`mousemove` 3곳이 실제로 가드 타는지 "편집모달 열고 배경서 우주 호버/클릭 통과" 재현으로 확인) → ②콘솔 컨테이너+탭 뼈대+"관리자 콘솔 열기" 진입버튼 신설(설정패널 살려둔 채 2단계 마이그레이션) → ③동기화·정합성 버튼 이관 → ④hnn/vm/gp 내용 이관 → ⑤콘솔 안정 후 기존 오버레이 진입버튼만 제거. **범위**: 데스크톱 전용(모바일은 기존 설정패널 유지, 콘솔 DOM 하나·진입버튼만 숨김). 어드민 전용이라 유저 영향 0.
- [완료·실기기확인필요][index.html] **쇼츠(세로) 판별 기준 전면 통일 — 2열 가로/쇼츠 섞임 + LIKES 썸네일 갇힘 근본 해결**(사용자 제보 "매번 제대로 고쳐진 적 없음" + LIKES 쇼츠가 세로 박스에 가로 썸네일로 갇힘). 근본원인: 세로 여부 판별이 40여 곳에 흩어져 `isShorts`(URL /shorts/)와 `_isShortV`(is_short 플래그)로 갈렸고, 특히 `_groupGcVids`가 API 객체(v.u 없음)에 `isShorts`를 써서 **전부 '가로'로 정렬** → 렌더(_isShortV)와 불일치로 한 행에 섞임. 세로 직캠은 watch?v= URL이 많아 URL 기준으론 항상 놓침. 해결: `_songIsShort(s)` 헬퍼 하나(is_short/category 플래그 우선, 없으면 URL 폴백)로 정렬·그룹·렌더·라이트박스·wide 판별 전 경로 통일(11곳 + _sortMixedByAspect/_groupMixedVids/buildVideoSection/_openFeedListOverlay). LIKES·모아보기(_openFeedListOverlay)에 빠져있던 세로 썸네일(oardefault.jpg) 로드+120x90폴백 로직 추가(카드 그리드 renderItems와 동일 규칙). ⚠️replace_all이 헬퍼 정의의 URL 폴백까지 건드려 무한재귀가 났다 즉시 복원 — 같은 실수 주의. 캐시버전 해시 자동.
- [완료][groups.json] **그룹 동기화 별칭 추가 — 보이넥스트도어←"보넥도", 제로베이스원←"제베원"**(사용자 요청). `mentions()`(admin.js:1248)가 `[그룹키, en, altNames]`를 제목에서 정규식 매칭하므로 altNames에 약칭을 넣으면 동기화 태깅 + 검색 둘 다 반영. 두 약칭 모두 다른 그룹/멤버명과 겹침 0(부분문자열 오매칭 위험 낮음). ⚠️기존 수집분은 재동기화 스윕 때 반영, 신규는 자동.
- [완료·실기기확인필요][index.html][kpop_universe.css] **모음 콘텐츠(_openFeedListOverlay)를 모바일 바텀시트로**(사용자 요청 — 가운데 팝업이 탐험 패널·카드 시트와 방식이 달라 흐름이 끊김). 모바일에서 아래→위 슬라이드업 + 상단 손잡이 + 아래로 스와이프 닫기(`_wireFeedListSwipe` 신설 — 탐험 `_wireFeedSwipeDismiss` 패턴 재사용하되 peek 없음, 배경≠패널이라 패널에만 transform). 데스크톱은 센터 모달 그대로. 배경탭/X 닫기 유지.
- [완료·실기기확인필요][index.html] **PWA(홈화면 앱) 페이지 줌인 고착 해제**(사용자 제보 — "웹앱에서 재생 모드가 통째로 확대돼 잘려 보이고, 사파리는 정상, 핀치로 축소도 안 됨"). 기존 줌 리셋은 페이지 로드 시 1회뿐이라, 새로고침이 없는 PWA에선 한 번 확대되면 영구 고착됐다(iOS는 user-scalable=no를 무시하고 핀치줌 허용). gesturestart/change/end 차단(핀치줌 원천 차단) + `visualViewport.scale>1` 감지 시 자동 리셋 추가. 유튜브 iframe은 별개 문서라 무영향.
- [완료][index.html] **디스커버리 탭 콘텐츠 로딩 논블로킹**(사용자 제보 — "오늘 작업 이후 디스커버리가 느려짐"). 순위변동 배지용 `_fetchPrevRanks`가 1000행 감사(9181f14a)로 페이지네이션되며 무거워졌는데, 이걸 카드 렌더 전에 순차 await(직캠·월간무대 2회)해서 콘텐츠 등장이 밀렸다. 순위 조회는 리스트를 '열 때'만 쓰이므로 백그라운드로 빼고 카드는 즉시 렌더(클릭 전 대개 채워지고, 아직이면 `_withRankDelta`가 배지만 숨김 폴백).
- [완료·실기기확인필요][index.html] **영상 2열 목록 — 쇼츠끼리·가로끼리 한 행에 배치**(사용자 요청 — "가로 영상과 쇼츠가 한 행에 섞여 높이 ~3배 차이로 들쭉날쭉"). 목록을 가로(norm)→쇼츠(short) 안정정렬(`_sortMixedByAspect`)해 각 행이 같은 비율로만 채워지게 하고 비율 경계에서만 넓힌다(`_groupMixedVids` 재작성 — 정렬 전제라 기존 swap은 자연 무효). 세로는 원본 9:16 유지. 세 경로 전부 적용: `buildVideoSection`(all 정렬) · `_openFeedListOverlay`(songs 정렬) · `_groupGcVids`(입력 정렬). 스와이프 playlist도 표시 순서와 정합(all/orderedSongs/state.allVids 모두 렌더 순서 공유). ⚠️무한스크롤(_groupGcVids)은 페이지 단위 정렬이라 스크롤 시 "가로들→쇼츠들"이 반복될 수 있음(각 행은 동일 비율). 캐시버전 해시 자동.
- [완료][index.html][admin.js][kpop_universe.css] **영상 관리 패널 일괄바 버튼 중복 정리**(사용자 제보 — "정상, 무관 버튼 같은 게 중복으로 생겼다"). **내가 만든 중복이다** — 전날 4상태 대칭 이동을 붙이면서 **기존 버튼을 안 보고 얹었다**.
  - **정리 전 실태**: 같은 동작 버튼이 세 계통으로 섞여 있었다.
    ⓐ 뜻이 고정된 3개 — `vm-normal-btn`(정상) · `vm-hold-btn`(보류) · `vm-nomem-btn`(무관, ss 탭만)
    ⓑ **탭마다 뜻이 바뀌는** `vm-apply-btn` — all→무관 / ss→숨김 / 나머지→정상
    ⓒ 내가 얹은 `vm-move-*` 4개 — →정상/→무관/→보류/→숨김 (ⓐⓑ와 전부 겹침)
  - **정리 후**: 뜻이 **탭과 무관하게 고정된 4개**(정상·무관·보류·숨김)만 남기고 ⓑⓒ를 없앴다. `vm-apply-btn`까지 지운 이유는, 뜻이 바뀌는 버튼이 하나라도 있으면 고정 4개와 **반드시** 겹치기 때문. 노출은 `_vmSyncFlagBtns()` 한 곳에서 정하고 **그 탭 자신의 상태 버튼만** 뺀다(제자리 이동은 무의미).
    | 탭 | 뜨는 버튼 |
    |---|---|
    | 전체 · 검수 계열 | 정상 · 무관 · 보류 · 숨김 |
    | 무관 목록 | 정상 · 보류 · 숨김 |
    | 보류 목록 | 정상 · 무관 · 숨김 |
    | 숨김 목록 | 정상 · 무관 · 보류 |
  - **색 언어도 원래대로 복구** — 이 패널엔 이미 규칙이 있었는데(파랑=일반 / 초록=되돌리기·승인 / 주황=내려놓기 / 빨강=거부) 내가 얹은 outline 스타일(`.vm-move-btn`)이 그걸 깼다. **정상=초록**(되돌리기), **무관·보류·숨김=주황**(카드에서 빼는 같은 방향, 구분은 라벨이 한다)으로 재배치하고 `.vm-move-btn`은 삭제.
  - ⚠️ **삭제하면서 잃을 뻔한 것**: 없앤 `vm-nomem-btn` 리스너에만 `_snapshotBeforeBulk`(되돌리기 스냅샷)가 있었다 — 보류/정상 버튼엔 원래 없었다. 셋 다 수백 행을 한 번에 바꾸는 같은 성격이라 기준이 갈릴 이유가 없어서, **`_vmBulkSetFlag`에 스냅샷을 넣어 4개 전부에 적용**했다(이 프로젝트는 undo가 곧 백업 — 2026-08-22 백업전략 #1). 대신 그 버튼에만 있던 confirm 창은 뺐다 — 선택 후 누르는 동작이고 되돌리기가 되므로.
  - **노출 판정과 목록 잔류 판정이 같은 표(`_vmTabFlag()`)를 보게** 통일 — 갈라지면 "버튼은 있는데 눌러도 목록에서 안 빠지는" 상태가 된다.
  - **검증**: `tests/vm-panel-ui.test.js` 갱신(49개) — 4개 버튼 존재·한 목록에서 배선·`vm-move-*`/`vm-apply-btn`이 **되살아나면 실패**·죽은 헬퍼 잔재 4종 감시·스냅샷 존재·두 판정이 같은 표를 보는지. 탭별 노출을 실제로 실행해 7개 탭 결과 확인. 전체 25개 스위트 통과.
- [완료][index.html][kpop_universe.css] **그룹 카드 데뷔일 옆에 "~ 해체일" 표기**(사용자 요청 — 클릭 불가 · 데뷔일보다 어둡게 · 폰트 크기는 동일).
  - 새 컴포넌트를 안 만들었다 — 데뷔 칩 뒤에 붙는 **클릭 불가 span(`.gc-show-suffix`)이 이미 있었고**(`[프로듀스 48]`·`(비스트, B2ST)`), 폰트도 이미 `.tag-chip`과 통일돼 있어서 색만 낮춘 변형(`.gc-disband-suffix`)을 얹었다.
  - 색은 **새로 만들지 않고 `.tag-chip-past`의 값**(`rgba(170,205,255,0.42)`)을 그대로 썼다 — 이 프로젝트에서 "지난 것"을 뜻하는 색이 이미 그거라, 새 값을 만들면 같은 의미에 톤이 두 개가 된다. glow도 같이 뺐다(빛나면 현재형으로 읽힘).
  - 위치는 데뷔 칩 **바로 뒤**, `[프로그램명]`·`(옛 이름)`보다 **앞** — `2018.10.29 ~ 2021.04.29 [프로듀스 48]`처럼 활동 기간으로 읽히게. 데뷔 연도 칩은 "같은 해 데뷔 그룹" 필터인데 해체일엔 대응 필터가 없어 클릭 대상에서 제외.
  - `disbanded`가 `true`(해체는 맞지만 날짜 미상 — 슈가·티에프엔·JBJ 3팀)면 붙일 게 없어 생략. 표기 대상은 47팀.
  - **검증**: `tests/disband-suffix.test.js` 신설(61개 — 붙는 위치 순서, `font-size` 재지정 금지(데뷔일과 어긋남), 알파가 0.6 미만, `.tag-chip-past`와 **같은 값**인지, `true` 가드, 데이터 형식). 가드를 일부러 제거해 실제로 잡히는지 확인. 헤드리스 실렌더로 아이즈원(`~ 2021.04.29`)·쥬얼리(`~ 2015`, 연도만)·하이라이트(미해체, 표기 없음)·슈가(날짜 미상, 표기 없음) 4케이스 확인.
- [완료][index.html][admin.js] **PostgREST 1,000행 제한 전수 감사** — 위 프로필 사진 사고와 같은 함정이 다른 곳에도 있는지 `sb.from(X).select(...)` **31곳 전부** 확인(사용자 요청).
  - **결과**: 이미 고친 5곳(워밍) 외에 **7곳 추가 수정**. 나머지 19곳은 단건/limit/count전용/단일개체필터로 이미 안전.
  - **추가 수정한 7곳**(전부 "전량을 받아야 하는데 상한이 없던" 것):
    | 위치 | 테이블 | 잘렸을 때 증상 |
    |---|---|---|
    | index.html:8252 · admin.js:3928 | `ext_channels` | 채널이 조용히 무시돼 그만큼 동기화에서 빠짐 |
    | index.html:12793 | `name_match_whitelist` | 매처 예외가 일부만 로드 → 조용한 오태깅 |
    | index.html:12812 | `atm_exception_rules` | 위와 동일 |
    | index.html:14317 | `rank_snapshots` | 순위 변동이 "없음"으로 잘못 표시 |
    | admin.js:3139 | `group_priority` | 우선순위 일부 누락 |
    | admin.js:3255 | `feedback` | 유저 피드백이 목록에서 사라짐 |
  - ⚠️ **`_sbFetchAll`(admin.js)은 `id` 키셋 방식**이라 `id` 컬럼이 없는 테이블(`group_priority`)엔 못 쓰고 호출부의 `.order()`도 덮어쓴다. 정렬을 보존해야 하는 곳은 range 방식인 `_sbSelectAll`을 쓸 것.
  - **안전으로 판정한 근거**(테스트의 ALLOW 목록에 사유와 함께 등록 — 나중에 "왜 얘만 안 고쳤지"가 안 되게): `video_reaction_counts`/`video_reactions`(영상 1개·유저 1명 기준, 상한 한 자릿수) · `melon_yearly_top100`(전체 799행, 그룹당 수십) · `spotify_streaming_milestones`(455행, 그룹당 수십) · `kpop_events`(전체 11행, 그룹/장소/id목록 1건 기준) · `feedback` count 조회(`_admHead()`={count:'exact',head:true}라 본문을 안 받음).
  - ⚠️ `rank_snapshots`·`feedback`·`group_priority`는 **admin 전용 RLS라 anon 키로는 0행**이라 크기 실측이 불가능하다 — "비어있다"고 판단하면 안 되고, 그래서 구조로만 판정했다.
  - **검증**: `tests/select-pagination.test.js` 신설(48개). 규칙 6종(헬퍼/range/단건/limit<1000/count전용/ALLOW등록) 중 하나를 못 만족하면 실패하고, **전량 조회에 `.order()`가 없으면**(페이지 경계에서 행이 새거나 겹침) 그것도 실패시킨다. 일부러 한 곳을 되돌려 실제로 잡히는지 확인함. 전체 24개 스위트 통과.
- [완료][index.html] **"사진 등록했는데 애니버서리·프로필 패널이 업데이트 안 된다" — 독립적인 버그 2개**(사용자 제보, 새로고침·재접속으로도 안 고쳐진다고 함).
  - **원인 ①(진짜 원인): PostgREST 1,000행 제한에 프리워밍이 조용히 잘리고 있었다.** `_warmInstaPicCache`가 `artist_pics`가 **32행이던 시절(2026-08-11)**에 만든 `.select()` 한 줄 그대로였는데, 사진이 쌓여 1,000행을 넘긴 뒤로는 넘친 만큼이 **에러도 경고도 없이** 버려지고 있었다. 실측 **1,111행 중 111행 누락**이고, 하필 잘리는 쪽이 **최근 등록분**이라 증상이 정확히 "새로 등록한 사진만 안 뜬다"로 나타났다 — 그날 등록분 40개가 통째로 여기 있었다(체리블렛 7명·틴탑·올아워즈 등).
    - `_sbSelectAll(build)` 헬퍼 신설 + **워밍 4곳 전부** 적용(`artist_pics`·`video_scrap_counts`·`collection_like_counts`+`collection_likes`·`public_collections`). 나머지 3개는 아직 작지만(58/0/5행) 유저 활동으로 늘어나는 테이블이라 같은 시한폭탄이었다. admin.js의 `_sbFetchAll`과 같은 개념이지만 그건 어드민 전용 스크립트라 일반 유저 경로에선 못 쓴다.
    - ⚠️ 호출부에 **order 필수** — 정렬이 없으면 페이지 경계에서 행이 새거나 겹친다. `public_collections`는 `updated_at` 동률이 날 수 있어 `id`를 2차 정렬로 붙였다.
  - **원인 ②: 애니버서리 스트립이 인스타 있는 멤버만 사진을 조회했다.** `if(a.links?.instagram){ _fetchInstaPicUrl(...) } else _fallbackThumb();` — 2026-08-14에 "인스타 없는 멤버도 위버스 등 출처로 사진 등록 가능"으로 **확대했는데 이 조건만 안 고쳐졌다**. 인스타 없는 **541명**은 사진을 등록해도 여기서 영영 안 떴고, 마침 그날 생일 4명 중 **민찬·브라이언·윤 3명이 정확히 이 경우**였다(DB엔 사진이 멀쩡히 있었음).
    - 인스타 유무와 무관하게 사진을 먼저 찾고 없을 때만 영상 썸네일로 폴백. 겸임 멤버를 위해 `_fetchArtistPicAnyGroup(a)` 신설 — `artist_pics` 키가 (이름, 그룹) 복합키라 주 소속만 찾으면 부소속에서 등록한 사진을 놓친다.
  - **곁다리: 프로필 패널 FAVORITES의 레이스도 같이 수정.** `_instaPicCache`는 인증 직후 **비동기로** 채워지는데 즐겨찾기 목록은 캐시를 **동기로** 읽어서, 워밍 완료 전에 패널을 열면 사진 자리가 빈 채로 굳고 워밍이 끝나도 다시 그리는 곳이 없었다. `_picRetryKey`로 워밍 완료 후 한 번만 아바타를 교체한다.
  - **검증**: `tests/pic-warm.test.js` 신설(36개 — `_sbSelectAll`을 가짜 빌더로 실제 호출해 0/500/1000/1111/2500행 페이지 수 검증, 중간 실패 시 부분결과+error 반환, **워밍 4곳에 헬퍼를 안 거치는 select가 하나라도 생기면 실패**, order 누락 감시). 헤드리스 실렌더로 캐시 1000→**1111행** 확인, 오늘 생일 5개 항목 전부 사진 표시 확인. 전체 23개 스위트 통과.
- [완료][index.html][admin.js][kpop_universe.css] **`flag_source`/`flagged_at` 배선 — "내가 숨긴 것"과 "매처가 숨긴 것" 구분**(사용자가 SQL 실행 완료).
  - **왜**: 숨김 2,822건 중 사람이 숨긴 건 **22건(0.8%)**뿐인데 둘이 같은 칸을 써서 구분 자체가 불가능했다. 그래서 숨김 탭이 검수 도구가 못 됐다.
  - **`_flagPatch(flag,source,extra)` 하나로 통일** — `content_flag`를 쓰는 자리가 **17곳**(스윕 5 · 패널 수동 10 · 동기화/백필 insert 2)이라 그대로 두면 또 복붙 드리프트가 난다. `tests/flag-source.test.js`가 "raw로 `content_flag`를 update하는 곳이 하나라도 생기면" 실패시킨다.
  - ⚠️ **자동 `hidden`은 `needs_review=true`를 강제로 같이 세운다** — 이번 오염 2,800건이 전부 `needs_review=false`라 **유저 화면에서 사라진 채 어떤 검수 목록에도 안 뜨는 조용한 블랙아웃**이었다. 자동 판정은 틀릴 수 있다는 전제 위에서만 화면에서 감출 자격이 있다. 수동은 사람이 이미 판단했으므로 안 붙인다.
  - 플래그 해제(`null`) 시 출처도 같이 지운다 — 안 그러면 "정상인데 auto가 숨긴 흔적이 남은" 유령 상태가 생긴다.
  - **스냅샷(`_BULK_SNAP_COLS`)에도 두 컬럼 추가** — `content_flag`만 되돌리고 출처를 안 되돌리면 복원 후 상태가 어긋난다. 컬럼 부재 환경 폴백(`_snapHasFlagSrc`)은 `reviewed_at`·`is_short`과 동일 패턴.
  - **무관/보류/숨김 탭에 출처 표시** — 배지 왼쪽 점(`.vm-flag-auto::before`)으로 자동분 구분 + 툴팁. 텍스트를 붙이면 배지 폭이 제각각이 되므로 무형으로만.
  - 검증: `tests/flag-source.test.js` 21개(헬퍼 실호출 + 배선 누락 감시 + 스냅샷 + 폴백). 전체 22개 스위트 통과.
- [완료][index.html] **무조건 제외 키워드 +`진짜괜찮은사람` +`지금거신전화는`**(띄어쓰기 양쪽 형태 같이 — `_titleNorm`이 공백을 안 지워서 `유부녀킬러`/`유뷰녀 킬러`처럼 둘 다 필요). 실측 39건, 공식 채널 업로드 0건이라 면제 규칙과 안 부딪힘. 그중 `지금거신전화는` 클립 다수가 **"9회 하이라이트"의 '하이라이트' 때문에 하이라이트 그룹에 붙어 있었다** — 같은 날 넣은 토큰 게이트가 신규를 막고 이 키워드가 기존을 정리한다.
- [완료][admin.js][index.html][kpop_universe.css][groups.json] **영상 관리 패널 정교화 + 숨김 목록 오염의 정체 규명**(사용자 요청 6건 + Fable 자문 검토).
  - **숨김 탭 오염의 정체**: hidden 2,822건(tags_manual=false) 중 **사람이 숨긴 건 22건(0.8%)뿐**이고 나머지는 전부 자동분. 원인은 **짧은 이름 게이트가 들어오기 전(~8/25)에 쌓인 오매칭**이었다 — 더보이즈 **뉴(NEW)**→"THE NEW SIX"·"New York"·"Brand New" / 스트레이키즈 **한(HAN)** / 세븐틴 **준(JUN)** / 스테이씨 **윤(YOON)**→강승윤·윤두준·윤보미. `needs_review`가 전부 0이라 **검수 큐에도 안 올라간 채 조용히 숨겨져 있었다**.
  - ⚠️ **Fable 자문 2건 정정**: ①"인물 차단 리스트가 없어 두더지잡기 구조" → **이미 있다**(`_BANNED_VIDEO_NAMES_*` + 동기화 시점 skip + `_ytBannedListVersion` 자동 재스캔). 진짜 격차는 "제목 문자열 기반이라 제목에 이름이 없으면 못 잡음"과 "코드에 있어 재배포 필요"라서, DB 테이블화 결론만 맞고 이유가 달랐다. ②"오숨김 = 재스캔 사고 잔재" → 그룹은 맞췄지만 원인이 다르고(1음절 이름), 그래서 **처방이 정반대**다 — 무관이 아니라 **재배정**.
  - ⚠️ **내 최초 제안(짧은 이름 자동 게이트 신설)도 틀린 전제였다**. `admin.js:696/4006/4017`에 `a.name.ko` 1음절 게이트가 **이미 있고 작동 중**이다. 숨김 표본 120건×4그룹을 현재 매처로 재판정하니 8/4/4/0건만 남았고 그마저 대부분 정당한 매칭이었다. "영문 3자 이하도 막자"는 추가 제안도 실측해보니 불필요 — 109명을 훑어 나온 오매칭 25건 중 진짜는 3건뿐이고 전부 **동명이인**(캔디샵 수이↔키키 수이 등)이라 별도 체계 소관이다.
  - **[신규] "숨김 목록 재판정" 스윕**(`sp-hidden-rejudge-btn`). 기존 `_ytSweepMistagReclassify`가 `content_flag` 있는 행을 통째로 제외해서 **숨김 처리된 오태깅은 어떤 스윕으로도 안 닿는 사각지대**였다. 전수 드라이런 결과: 재배정 **953건** / 보류 이동 **990건** / 판정 동일 417건 / 약한추론 제외 274건 / 콜라보·커버 제외 188건.
    - 무매칭분을 **'무관'이 아니라 '보류'로** 보낸다 — "매처가 못 잡는 것"과 "우주 밖"은 다르다. 실제로 무매칭분에 "THE NEW SIX - FUEGO"처럼 실존 그룹이 섞여 있어 무관으로 밀면 영영 안 보인다. 보류는 카드에선 빠지되 검수 목록에 남는다(사용자의 보류 정의와 일치).
    - 2026-08-20 대량 오숨김 사고를 의식해 `_ytSweepMistagReclassify`의 안전장치를 **그대로** 가져왔다: 새 그룹명이 제목에 literal로 있어야만 재배정 / 콜라보·커버 제외 / `tags_manual=true` 제외 / 스냅샷 후 적용 / confirm 취소 시 미리보기만(표본 각 40건 콘솔 출력).
  - **[데이터] 티엔엑스 `altNames` 신규** `["THE NEW SIX","더뉴식스"]` — 제목 표기 실측이 THE NEW SIX 135 · TNX 109 · 더뉴식스 60 · 티엔엑스 57인데 `en:"TNX"`만 있어 literal 판정이 절반 넘게 실패했다. 채우니 재배정 강한 근거가 **794→953건**.
  - **[UI] 4상태 대칭 이동**(`vm-move-*-btn` 4종 + 공통 `_vmBulkSetFlag`). 예전엔 탭마다 탈출구가 "정상" 하나뿐이라 숨김↔무관↔보류를 옆으로 못 옮겼다(막다른 골목). 현재 탭 자신의 상태 버튼과 기존 `vm-apply-btn`과 겹치는 것은 `_vmSyncMoveBtns()`가 숨긴다.
  - **[UI] 탭별 개수 배지** — "마지막 조회 시점" 기준(실시간 아님). `_vmCacheSync()` 안에서 갱신해 목록과 항상 붙어 다니게 했고, 캐시 히트 경로에선 배지만 갱신한다(거기서 `_vmCacheSync`를 부르면 ts가 갱신돼 재조회가 영영 안 됨).
  - **[UI] 데스크톱 패널 높이 고정**(`@media(min-width:769px){#vm-panel{height:86vh}}`) — width는 원래 560px 고정이었고 `max-height`라 **높이만** 출렁였다. 모바일은 짧은 목록에 빈 공간이 손해라 기존 `max-height` 유지.
  - **[UI] 목록 스크롤바 상시 표시** — `overflow-y:auto`만으론 맥 오버레이 스크롤바가 사라져 위치 단서가 없다. `overflow-y:scroll` + `::-webkit-scrollbar` 명시로 클래식 스크롤바 고정(폭 10px). 콘텐츠 몰입용인 `#mob-sheet-inner`(2px/알파 0.07)보다 굵고 진하게 — 여긴 몇 백 건을 훑는 관리 목록이라 위치 파악이 기능이다.
  - **검증**: `tests/vm-panel-ui.test.js` 신설(38개 — 안전장치 5종 존재, 무매칭이 무관으로 새지 않는지, `_norm` 구현이 두 스윕에서 갈라지지 않았는지, 캐시 히트에서 ts 미끄러짐 방지). 전체 21개 스위트 통과.
  - **알려주는 오탐(소급은 사용자가)**: 재배정 953건 중 `→위너` 40건은 38건이 강승윤 직캠(정당)이고 **2건이 영단어 "winner" 오탐**("Who will be the winner of BEST NEW ARTIST?" 등). 10건 미만이라 로직은 그대로 두고 기록만 남김.
  - **남은 것(사용자 SQL 필요)**: 플래그 출처 컬럼(`flag_source` auto/manual + `flagged_at`) · 보류 90일+ 노화 표시 · 밴 인물 리스트 DB 이전(`name_match_whitelist` 패턴). 자동 숨김은 반드시 `needs_review=true`도 같이 세워야 한다는 게 이번 사고의 교훈.
- [완료][index.html][admin.js][groups.json] **탈퇴일 모르는 비활동 멤버 카드가 통째로 비던 것 — 컷오프 폴백 재설계(99명 복구)**. 위 DECISIONS의 "active:false인데 left 없는 멤버 104명" 항목 처리.
  - **진단이 틀렸었다**: "탈퇴일 미기입이 원인"이 아니라 **`active` 필드가 두 뜻으로 섞여 쓰이는 것**이 원인. 쥬얼리(2015 해체) 하주연·김은정은 `active:true`(=개인 활동 계속), 정유진·서인영은 `active:false`(=은퇴)인데 **쥬얼리에 있던 기간은 똑같다**. 그런데 `_memberVideoCutoff`는 이걸 "이 그룹 멤버인가"로 읽어서 은퇴한 6명 카드에서만 쥬얼리 영상이 통째로 사라졌다. 걸스데이(혜리=배우 활동중 vs 황지선), 스텔라(4:4)도 같은 모양.
  - **폴백 4단계**: ①`left` 있음 → 그 날짜 ②그룹 해체일 있음 → 해체일 ③해체 확실·날짜 미상 → 컷오프 없음 ④현역 그룹 이탈·날짜 미상 → **기존대로 차단**. ④를 남긴 이유는 이 컷오프를 처음 만든 원인(수진 버그 — 탈퇴 이후 최신 영상이 새어 들어감)이 그대로 재발하기 때문. ④ 잔류는 11명(엑소 타오 등).
  - **실측**: 차단 110명 → **11명**. 되살아난 99명 / 미태깅 그룹영상 1,909건(에이프릴 415·티에프엔 310·나인뮤지스 195·티에이엔 146…).
  - **해체일 7팀 신규 기입**(나무위키 인포박스 "해체일" 또는 분류 근거): 쥬얼리 `2015`(정확한 날짜 없음, 분류+멤버 변천사 근거) · 스텔라 `2018.02.25` · 유니티 `2018.10.12` · 유앤비 `2019` · 헬로비너스 `2019.05.08` · 소나무 `2021.09.08` · 네이처 `2024.04.27`. **걸스데이는 제외** — 나무위키에 "해체를 선언하지 않은 채"라고 명시돼 있음(전 멤버 3명은 `left`를 채우는 게 맞는 처리).
  - ⚠️ **`disbanded`를 켜면 딸려오는 것들**(써보고 확인): 3D 별 `survivalDim` 0.4배로 어두워짐 · `bubble._isSurvivalShow` 취급 · 멤버 카드 그룹 태그 `tg-past` · **admin.js 동기화 시 해체일 이후 영상 수집 중단**(2026-08-14 사용자 정책). 전부 "해체 그룹"의 의도된 표현이라 그대로 뒀지만, 해체 후 올라온 레전드 무대 재업로드는 앞으로 안 들어온다.
  - **같은 계산 사본 3벌을 하나로**: 해체 판정이 `_gDisbanded`(연결카드 대표그룹 정렬)·`_isGrpDisbanded`(애니버서리 dim/정렬) 지역 const 두 벌이었고 **둘 다 groups.json의 명시 `disbanded` 필드를 안 읽었다** — 아이즈원·루셈블·프리스틴 등 **18팀이 해체일이 박혀 있는데도 "활동중"으로 판정**되고 있었음. 전역 `_groupIsOver`로 통일(+18팀/-0팀). 해체일 계산도 admin.js `_disbandCutoffDate`와 두 벌이 될 뻔한 걸 `_groupEndDate` 하나로 위임.
    - ⚠️ 로스터 검사를 `_artistGroups`로 넓히지 말 것 — 넓히면 **이달의소녀·유앤비가 거꾸로 "현역"으로 뒤집힌다**(겸임 멤버가 새 그룹에서 현역인데 옛 그룹 항목에 per-group `active:false`가 없어서). 명시 필드를 OR로 얹기만 하면 잃는 게 없다.
  - **검증**: `tests/member-cutoff.test.js` 신설(81개 — `_groupEndDate` 윤년/연도만/YYYY.MM 실행 검증, ①~④ 분기 순서, 사본 재복제 감시, TDZ 선언 순서, 데이터 회귀). 헤드리스 실렌더: 정유진(쥬얼리)·앨리스(헬로비너스)·이슬(스텔라)·노아(티에프엔) 0건→노출, 수진(①)·타오(④) 기존 유지.
  - **남은 것**: 걸스데이 전 멤버 3명(황지선·장혜리·지해) `left` 미기입 — 채우면 ①로 정확해짐. `active` 필드의 이중 의미 자체는 별 밝기·해체 판정까지 번지므로 손대지 않음.
- [완료][groups.json][admin.js][index.html][artists.json] **하이라이트=비스트 반영 — strictSync 해제 + 토큰 게이트, 장현승 등록(표기는 '비스트'), 용준형 밴**(사용자 요청 "그 하이라이트가 예전에 비스트였단 말이야? 이거 어떻게 반영하면 좋을까").
  - **사실관계(나무위키 하이라이트/비스트 교차 확인)**: 비스트 데뷔 2009.10.16(6인) → **2016.04.19 장현승 탈퇴**(5인) → 2016.10 큐브 퇴사 → **2017.03 하이라이트로 개명** → **2019.03.14 용준형 탈퇴**(현재 4인). 즉 **장현승은 하이라이트와 무관**(개명 전 탈퇴)하고 **용준형은 하이라이트로 2년 활동**했다. 나무위키 하이라이트 문서 [전 멤버]에 용준형만 올라 있는 것과 일치.
  - **① strictSync 해제 + 토큰 게이트(7팀→6팀)**: 하이라이트만 통째로 풀면 "N회 하이라이트"·"Highlight Medley"·"BEAST MODE" 같은 일반어에 걸려 **631건 회수에 오탈취 344건**(실제 매처 A/B 시뮬). 그래서 `_GROUP_TOKEN_HASHTAG_ONLY`에 **'하이라이트'·'Highlight'·'BEAST' 세 토큰만** 내리고 strictSync는 해제 → **45건 회수 / 의심 10건(실질 오탐 ~3건)**. 게이트 밖에 남긴 '비스트'·'B2ST'는 일반어 충돌이 없어 `hit()`으로 안전하게 잡히며, **이 둘로 개명 전 비스트 시절 영상을 회수하는 게 strictSync를 푼 이유 자체**다. `altNames`에 `B2ST` 추가(나무위키 표기 3종: 비스트/B2ST/BEAST). 에이스(A.C.E)와 정확히 같은 처리.
    - `_GROUP_AMBIGUOUS_IF_COMATCHED`도 시도했으나 **효과 없음** — 그건 멤버명 역추론 경로에만 걸리고 그룹명 해시태그 매칭에는 안 걸려서 결과가 게이트 단독과 동일했다.
  - **② 장현승 등록 + `dispKo`/`dispEn` 표기 전용 오버라이드 신설**(사용자 제안 "장현승 멤버 카드에도 '하이라이트' 아닌 '비스트'가 표기되는 거고. 표기만..?"). `group.ko`는 **'하이라이트' 그대로** 두고(영상 조회 키 `group_ko`이자 연결 태그 `"장현승(하이라이트)"`의 키 — 여기를 '비스트'로 바꾸면 DB에 없는 키를 찾게 돼 카드가 통째로 빈다, `soloDisplay`가 우즈에서 겪은 사고와 동형) **표기만** 갈아끼운다. `_gLabel(g)` 신설 + 그룹명을 그리는 자리 전부를 이 함수 하나로 모음: 멤버 카드 그룹 태그 2곳(모바일/데스크톱), `_dispGroupName`(생일 필터·인덱스 리스트·애니버서리 스트립), 연결 카드 그룹 칩. **클릭 타겟은 여전히 `g.ko`** — '비스트'를 눌러도 하이라이트 그룹 카드가 열린다.
    - 연결 카드 그룹 칩은 **버킷 안 멤버가 전원 같은 `dispKo`일 때만** 라벨을 바꾼다(장현승+양요섭이 섞이면 어느 쪽으로 써도 반쪽이 틀리므로 실제 그룹명 유지). 버킷 키는 `showGC`·`_groupsCoAppear`가 쓰는 값이라 절대 안 건드림.
    - 한쪽 언어만 채워도 오버라이드가 항상 이기게 함(`dispKo`만 있는데 영문에서 `g.en`='Highlight'로 새면 이 필드의 존재 이유가 그 화면에서만 조용히 무너진다).
  - **③ `left`가 실제로 뭘 자르는지 정정**: 처음엔 "`left:2016.04.19`를 넣으면 장현승 영상 10건이 전부 컷오프에 걸려 카드가 빈다"고 봤는데 **틀렸다**. `published_at.lte`가 붙는 건 `ownGroupWideClause`(=`members.eq.{}`인 그룹 전체 미태깅 영상) **하나뿐**이고, 본인이 직접 태깅된 절(`members.cs`/`with_members.cs`/`group_ko.eq.본인이름`)엔 날짜 제한이 없다. 그래서 결과가 정확히 의도대로 갈린다 — 하이라이트의 2016년 이후 미태깅 그룹영상은 차단, **이름이 붙은 그의 탈퇴 후 솔로 영상은 그대로 노출**. 그 10건이 지금까지 안 잡힌 건 컷오프 때문이 아니라 artists.json에 이름이 없어 매처가 매칭할 대상이 없었기 때문.
  - **④ 용준형 밴(artists.json 미등록 유지)**: 2019.03.14 탈퇴 사유가 승리·힘찬·종훈과 같은 계열이라 `_BANNED_VIDEO_NAMES_GLOBAL`에 추가. **밴 인물은 artists.json에 등록하지 않는 게 기존 규칙**임을 확인(승리·힘찬·종훈·이종현 전부 미등록. 등록돼 있는 태일(블락비)·크리스(배틀)는 동명이인이라 무관).
    - ⚠️ **로마자는 성(yong)이 붙은 형태만** 넣었다. `junhyung`/`jun hyung` 단독으로 걸면 **고스트나인 손준형(#SONJUNHYUNG, 현역)이 통째로 오폭** — 실측 55건 중 **48건이 손준형**이었다. 등록 토큰: `용준형`·`yong jun hyung`·`yong jun hyeong`·`yongjunhyung`.
    - 밴 목록은 정의가 index.html 한 곳뿐이고 `_ytBannedListVersion()`이 목록 변경을 자동 감지하므로, **관리자 "밴 인물 언급 영상 정리" 버튼**을 한 번 누르면 기존 행이 재스캔된다(`tags_manual=true` 행은 제외 — 헌법 준수).
  - **검증**: 새 `tests/disp-group.test.js` 43개 통과(`_gLabel`을 index.html에서 떼어내 실제 호출 + "raw 표기가 한 곳도 안 남았는지" 스캔 + 밴 정규식으로 손준형 제목 5종 오폭 여부 실검사). 헤드리스 390px 실렌더: 장현승 카드가 **`비스트 2009.10~2016.04`**(`tg-past` 흐림), 이름에 괄호 없음, 태그 클릭 시 하이라이트 그룹 카드 열림, 하이라이트 그룹 카드 멤버 목록에 `장현승(비활동)`, `_dispGroupName`도 '비스트'(양요섭은 '하이라이트' 그대로). 정적 페이지 리빌드(3,998개) 후 seo-url 통과.
- [이슈] **음방 채널 직캠의 `group_ko` 계통 오배정 — 별건(2026-08-27 발견)**. 용준형 밴 조사 중 부수적으로 발견: 제목에 "고스트나인"이 있는 657건 중 **42건이 다른 그룹으로 배정**돼 있고(예: `[안방1열 직캠4K] 고스트나인 손준형 'X-Ray'` → `group_ko=세븐틴`, `[쇼챔직캠] 고스트나인 이우진` → `러블리즈`), 더 크게는 **`group_ko=세븐틴` 6,796건 중 1,900건이 제목에 '세븐틴'/'seventeen'이 아예 없다**. 음방 채널(SBS 인기가요·쇼챔·엠카운트다운) 영상이 특정 그룹으로 흘러드는 계통 오염으로 보임. 전부 `tags_manual:false`라 재배정 가능. **먼저 확인할 것**: 기존 "오태깅 그룹 재배정"(`sp-mistagfix-btn`)·"동명이인 그룹 오배정 전체 스캔+재배정" 버튼이 이걸 이미 커버하는지 — 커버한다면 버튼 한 번으로 끝나고, 아니면 원인(외부채널 기본 group_ko?)을 따로 파야 함.
- [완료][index.html] **DISCOGRAPHY 헤더만 덩그러니 뜨던 멤버 카드 — 부가(OST·피처링)만 있으면 기본 노출**(사용자 제보 "텍스트는 있는데 콘텐츠는 부가 앨범이라 디폴트로 안 나와서 덩그러니 텍스트만 보이는 경우가 꽤 있다").
  - **실측(2026-08-27)**: 디스코그래피가 있는 멤버 **609명 중 99명(16%)** 이 이 상태였고 **전부 OST/피처링**이었다. 그룹 카드는 **0건** — 사용자가 "특히 멤버 카드"라고 한 것과 정확히 일치한다(멤버 개인 디스코그래피는 대부분 OST 참여라 그렇다).
  - **원인**: 섹션을 숨기는 조건이 `albums.length===0`뿐이라, 앨범은 있는데 전부 `isMain:false`면 헤더는 남고 스트립만 비었다. 기본값이 `extrasIncluded=false`(2026-08-21 결정)라 부가는 안 그려지기 때문.
  - **수정**: 메인(싱글/EP/정규)이 **하나도 없을 때만** `extrasIncluded`/`showingAll` 초기값을 켠다. 새로 만든 게 없다 — 부가 커버를 흐리게 그리는 `.gc-discog-extra{opacity:.55}`도, ⋮ 메뉴의 "OST·피처링" 토글도 이미 있어서 초기값만 바꿨다. 초기 ✓ 표시는 기존 `updateSortMenuUI()`가 그 값을 읽어 자동으로 맞는다.
  - **섹션을 통째로 숨기지 않은 이유**: 데이터는 실제로 있고 "OST에 이만큼 참여했다"가 그 멤버에 대한 정보다. 흐린 표기로 "부가"라는 성격은 그대로 드러나고, 예전처럼 ⋮에서 끌 수도 있다.
  - **검증(헤드리스 390px)**: 윈터(부가만 5장) → 5장 전부 표시·전부 흐림·OST 토글 ✓. 회귀 확인용 유타(메인3+부가3) → **메인 3장만** 표시·흐린 항목 0·토글 꺼짐(기존 동작 그대로).
- [완료][kpop_universe.css] **투데이 애니버서리 스트립 살짝 축소 + 프로필(저널) 첫 줄 여백 축소**(사용자 요청).
  - 스트립: 썸네일 76→**68px**, 아이템 폭 78→**72px**(공용 기본값 84→78), 이름 11→**10.5px**, 서브 9.5→**9px**, 아이콘 27→24, 아이템 내부 gap 7→6. 8/13에 60→76으로 키웠던 걸 "이번엔 살짝 크다"는 피드백으로 중간값에 맞춘 것.
  - **공용 클래스라 범위부터 확인했다** — `.feed-strip-item/-thumb-wrap/-name/-sub`는 같은 `.feed-strip` 컨테이너를 쓰는 세 곳 중 **애니버서리에서만** 쓰인다(최근 본 행은 16:9 사각 카드 별도 마크업, Charts는 `.feed-card`). 그래서 기본값을 줄여도 다른 스트립엔 영향이 없다.
  - 프로필(저널) 첫 줄(별·닉네임·편집)과 최애 표기 줄 사이: `padding-bottom 14 + margin-bottom 14 + 최애줄 margin-top 2 = 30px` → **17px**. 최애 줄이 비어 있으면 `#my-page-bias`가 `display:none`이라 그 아래 FAVORITES 구분선과의 간격도 같이 줄어든다(28→15px).
  - 헤드리스(390px, DPR3)로 실제 렌더 확인 — 아이템 폭 72 / 썸네일 68×68 / 이름 10.5px / 서브 9px 반영, 5장이 한 화면에 여유 있게 들어옴.
- [완료][index.html][kpop_universe.css][tests/lb-topbar.test.js] **재생모드 상단 중앙 바 — "함께한 멤버"가 안 눌리고 두 줄이 겹쳐 있던 문제**(사용자 제보 "멤버 텍스트는 클릭 안 되는 거임? 그룹은 되는데").
  - **먼저 정정**: 앵커 줄의 멤버(카리나)도 **이미 눌리고 있었다.** 안 눌리는 건 그 아래 "함께한 멤버" 칩이었고, 사용자가 본 화면을 재현해보니 **그룹만 태깅된 영상**이라 위줄=그룹(클릭 O)·아래줄=멤버 칩(클릭 X)이었다 — "위줄 그룹, 아래줄 멤버"라는 제보와 정확히 일치.
  - **실측(2026-08-27, 390px)**: 바 높이 36px 안에 두 줄(11+22)을 욱여넣어 **두 줄이 3px 겹쳐** 있었고(앵커 버튼 아래끝 31 > 칩 위끝 28), 앵커 버튼 세로 탭 영역은 **17px**(애플 HIG 최소 44의 39%)였다. 칩은 `<span>`에 핸들러도 없고 부모 `pointer-events:none`이라 완전한 장식.
  - **수정 3가지**: ①"함께한 멤버" 칩을 버튼으로 바꾸고 누르면 그 멤버 카드가 하프시트로 열리게(이름을 실제 아티스트로 해석 못한 칩은 예전처럼 텍스트) ②바 높이를 내용에 맡기고(min-height 36) 두 줄 사이 간격 확보 → **-3px 겹침 → +5px** ③앵커 버튼 세로 탭 영역 **17 → 36px**. 밑줄은 버튼이 아니라 안쪽 span이 그리게 옮겼다 — 버튼에 padding을 주면 border-bottom이 글자에서 떨어져 밑줄이 붕 뜬다.
  - **첫 줄이 닫기(⌄)·메뉴(⋮) 버튼과 같은 36px 밴드에 세로 중앙**이라는 기존 정렬은 유지했다(`justify-content:center` → `flex-start` + 앵커 줄에 36px). 안 그러면 칩이 생길 때마다 첫 줄이 위로 밀려 좌우 버튼과 어긋난다.
  - **범위**: 쇼츠(스와이프) 모드는 손대지 않았다 — 거긴 함께한 멤버 칩이 CSS로 숨겨져 있고 상단바 전체가 `pointer-events:none`(탭이 영상으로 통과)이라 별개 설계다.
  - **회귀 테스트 신설** — 이 자리는 2026-08-26에 이미 한 번 죽었던 곳이라(부모 pointer-events가 버튼까지 죽임) 정적 검사로 끝내지 않고 **실제로 눌러서 하프시트가 열리는지**까지 확인한다(앵커 멤버/그룹/함께한멤버 3종 + 두 줄 겹침 + 탭 영역 + 그룹만 태깅된 영상 케이스).
- [완료][artists.json] **나우즈 현빈 인스타그램 링크 삭제**(사용자 요청).
- [완료][kpop_universe.css][tests/pwa-safearea.test.js] **PWA(홈 화면 추가)에서 카드 '이전' 버튼이 노치 밑에 깔려 못 누르던 버그**(사용자 제보 + Fable 정리 8번).
  - **원인**: 시트가 꽉 찬 상태(`bs-full`)엔 상단 세이프에어리어만큼 `padding-top`이 붙는데, 뒤로가기 버튼은 `position:absolute; top:0`이었다. **absolute의 `top:0`은 padding box가 아니라 border edge 기준**이라, 손잡이(일반 흐름)는 노치 아래로 밀려나는데 버튼만 노치 밑에 그대로 남는다. 브라우저에선 `env(safe-area-inset-top)`이 0이라 둘이 나란해 보여서 **PWA에서만** 드러났다.
  - **같은 문제 하나 더**: '영상으로 돌아가기'(`.sheet-tovid-btn`)도 같은 `top:0`이라 같이 깨져 있었다.
  - **수정**: 값의 출처를 `--safe-top` 변수 하나로 묶었다(`:root{--safe-top:env(safe-area-inset-top,0px)}`). 시트의 `padding-top`과 버튼들의 `top`이 **같은 변수**를 보므로 한쪽만 고쳐서 또 어긋나는 일이 구조적으로 안 생긴다. `bs-full` 규칙 전부(#mob-sheet·#mob-card-stack·#conn-sheet, inner 높이 포함)를 이 변수로 통일했다.
  - **Fable 진단 중 하나는 사실이 아니었다** — "터치 영역이 너무 작음"을 의심했는데 이미 44×44(애플 HIG 최소)로 잡혀 있었다. **순수하게 위치만** 문제였고, 테스트가 터치 타깃도 같이 못 박는다.
  - **⚠️ 이 버그가 그동안 테스트로 안 잡힌 이유**: `env()`는 헤드리스/데스크톱에서 항상 0이라 노치 상황을 재현할 방법이 없었다. 변수로 감싼 덕에 이제 테스트가 `--safe-top`만 덮어써서 노치 기기를 그대로 흉내낼 수 있다.
  - **검증**: `--safe-top:47px`(아이폰 노치 대표값)로 재현 → 버튼 top 48(노치 아래), 손잡이와 같은 줄(차이 10px), 터치 타깃 44×44. **부정 검증도 했다** — 버튼 규칙만 `top:0`으로 되돌리니 top이 1로 떨어지고 손잡이(58)와 따로 놀아 테스트가 빨간불(= 제보된 증상 그대로 재현).
- [완료][index.html][kpop_universe.css] **흔적 지표 점 색 분리 — "이번 주 N명이…"와 "N명이 좋아해요"가 구분이 안 되던 문제**(사용자 지적).
  - 카드 상태줄은 세 지표가 같은 자리에서 로테이션된다: presence(코랄 텍스트+코랄 점) · favcount(핑크 텍스트+핑크 점) · traces(본문톤 #ccd8ee 텍스트 + **favcount와 같은 핑크 점**). traces가 `.favcount-dot`을 그대로 재사용하고 있어서, 텍스트 색은 달라도 **점이 똑같아 무엇이 바뀐 건지 구분이 안 됐다.**
  - `.trace-dot` 신설(민트 `rgb(110,225,195)`). presence·favcount가 둘 다 따뜻한 쪽이라 흔적만 **차가운 쪽**으로 뺐다 — 텍스트도 이미 차가운 본문톤이라 점과 글자가 한 덩어리로 읽힌다. `prefers-reduced-motion` 정지 목록에도 같이 넣었다(다른 두 점과 동일 처리).
  - 실기기 대신 헤드리스(390px, DPR3)에서 카드 안에 세 줄을 실제로 붙여 렌더해 확인 — 첫 캡처가 펄스 애니메이션 중간이라 점이 흐릿하게 나와서, 애니메이션을 멈추고 다시 찍어 실제 색으로 판단했다.
- [완료][index.html][tests/live-tab.test.js] **커버(COVER) 탭에서도 '비하인드'/'behind' 제외**(사용자 요청, 라이브 탭과 같은 이유). `_COVER_EXCLUDE`에 두 키워드 추가 — 커버 '무대'가 아니라 촬영/연습 현장 뒷이야기라, 실측 표본이 전부 "커버 촬영 현장 비하인드"·"STUDIO CHOOM ORIGINAL behind"류였다.
  - **실측**: 커버 조건 매칭 9,504건 → 기존 제외 적용 5,352건 → 비하인드/behind 추가로 **5,179건**(173건 추가 제외).
  - **곁가지: 커버 탭 노출 판정에도 제외 목록이 빠져 있었다** — 라이브에서 고친 것과 **같은 자리**다(`_chk`의 커버 분기가 or 조건만 쓰고 `_applyCoverExcludeQuery`를 안 거쳤다). "탭은 보이는데 열면 빈 목록"이 될 수 있어 같이 통일했다. 테스트에 "커버 OR 조건은 함수 안 1곳뿐"을 추가해 재발을 막는다.
- [완료][admin.js][index.html][tests/tag-modal-gko.test.js] **태그 편집 모달이 영상의 실제 `group_ko`를 안 읽어서, 수동 태그가 저장돼도 어디에도 안 보이던 버그**(사용자 제보 "조승연 영상 수동 편집으로 엑스원-우즈 선택하고 저장했는데 반영이 안 됨 — 솔로로 전환한 멤버 문제 같다"). **제보한 방향이 맞았고, 정확한 조건은 "영상의 실제 소속 그룹과 지금 보던 카드의 그룹이 다를 때"였다.**
  - **원인 ①**: 모달을 열 때 DB에서 `members`·`with_members`·`content_flag` 등은 읽어오는데 **`group_ko`가 select에 없었다.** 그래서 "소속 그룹" 칸과 멤버 체크박스 목록이 **지금 보던 카드의 그룹(`ko`)** 기준으로 그려졌다.
  - **원인 ②**: 저장 시 `groupKoInput !== ko`로 비교해서, 칸에 카드 그룹이 그대로 있으면 **group_ko를 아예 안 썼다.** → 화면엔 '엑스원'이라 떠 있는데 행은 '이즈나' 그대로.
  - **왜 안 보이나**: `members[]`는 "그 행의 `group_ko` 그룹 안에서 누가 나오는가"라는 뜻이고, 멤버 카드 조회 조건이 `and(group_ko.eq.그룹, members.cs.{이름})`이다. group_ko가 안 맞으면 저장된 태그가 **어느 카드에서도 조회되지 않는다.** 실제로 우즈는 `group_ko='이즈나'` + `members:["우즈"]` + `tags_manual:true`인 행이 7건 있었고(이즈나 로스터에 우즈가 없으니 카드 기준 모달에서만 나올 수 있는 값), 우즈 카드는 그걸 하나도 못 봤다.
  - **실측 피해**: members에 이름은 있는데 그 행 group_ko가 그 사람 소속이 아닌 **고아 태그 1,155개**(수동 저장 51개). 상위 — 아이유 353 · 이영지 151 · 김재중 128 · 보아 87 · 은지 77 · 김준수 66 · 민영 62 · 유정 61 · 싸이 31 · 승한 30 · 광수 27 … 우즈 7. **상위가 전부 솔로/솔로전환 아티스트**인 건 카드 키(`_ytGroupKoFor`)와 태깅 키(`group.ko`)가 갈리는 데다 외부 채널에서 엉뚱한 group_ko로 들어오는 일이 잦아서다 — 사용자의 직감이 이 지점을 정확히 짚었다.
  - **수정(사용자 선택: 실제 group_ko에 고정)**: ①select에 `group_ko` 추가 ②응답이 오면 **영상의 실제 소속**으로 "소속 그룹" 칸과 멤버 체크박스를 다시 그리고, 카드 그룹과 다르면 주황색으로 안내("이 영상의 실제 소속은 X예요 — Y 영상이 맞다면 이 칸을 Y로 바꾸고 저장하세요") ③저장 시 비교 기준을 `ko` → `_vidTagTarget.realGko`로. 이제 칸을 엑스원으로 바꾸면 **실제로 group_ko가 써진다.** 응답 대기 중 관리자가 칸을 직접 만졌으면 그 선택을 존중한다(기존 레이스 방어와 같은 원칙).
  - **기존 1,155개 정리(사용자 선택: 목록만)**: 영상 관리 패널에 **"고아 멤버태그" 탭** 신설. 정정 방법이 영상마다 달라서(group_ko 재배정이 맞는지, with_members로 옮기는 게 맞는지) 자동 스윕 대신 사람이 보고 고른다. 수동 저장분을 위로 정렬하고, 목록에서 **어느 이름이 고아인지 ⚠️로 표시**한다. 판정에 `_artistGroups`를 써서 겸임 그룹도 소속으로 인정(안 그러면 겸임 멤버가 전부 오탐), 미등록 이름은 제외(별개 문제).
  - **별개로 같이 발견(미해결)**: `active:false`인데 `left`(탈퇴일)가 없는 멤버 **104명**은 `_memberVideoCutoff`가 `{active:false,left:null}`을 돌려줘 `ownGroupWideClause`가 통째로 비고, 그 결과 **"그룹 전체 미태깅 영상"이 그 멤버 카드에 하나도 안 뜬다**(우즈도 이 케이스 — 엑스원 미태깅 영상이 안 보임). 예: 타오(엑소)·이현주(에이프릴)·쥬얼리 전원 등. 탈퇴일을 채우거나, left가 없으면 "그룹 해체일까지"로 폴백하는 방안 검토 필요.
- [완료][index.html][admin.js][kpop_universe.css][tests/hold-flag.test.js] **카드 편집 모달·일괄바에 '보류' 추가**(사용자 요청). 영상 관리 패널엔 이미 '보류' 탭과 '선택-보류'가 있었는데 카드 쪽(연필 버튼 편집 창, admin-bulk-bar)에만 없었다.
  - ~~보류는 무관/숨김과 동작이 다르다(카드에 남는다)~~ → **오판이었음(2026-08-27 정정, 사용자 지적 "보류 처리해도 카드에서 제외되어야 하는데")**. 카드 그리드 쿼리(`buildBaseQuery`)는 **서버에서 무관·보류·hidden을 함께 빼고 있었다** — 클라이언트 안전망(`_filterBannedVideos`)만 보고 "보류는 안 걸러진다"고 판단한 게 틀렸다. 보류도 무관/숨김과 **똑같이 카드에서 제외**되고, 차이는 의미뿐이다(무관="이 그룹/멤버와 관계없음" 판정 / 보류="판단을 미뤄둠", 나중에 관리 패널 '보류' 탭에서 다시 봄).
  - **정정에 딸려 고친 것**: 보류를 거르는 자리가 세 곳인데 서버 쿼리에만 있고 나머지 둘엔 빠져 있었다 — ②클라이언트 안전망 `_filterBannedVideos` ③편집 직후 재확인 `patchItem`. 평소엔 서버가 걸러줘서 안 드러나지만, **방금 보류로 바꾼 행을 재조회 없이 화면에서 걷어내는 경로**(일괄 처리·모달 저장 직후)에선 이 둘이 유일한 기준이라 그대로 남아 있었다. 셋을 같은 목록으로 맞췄고, 일괄 보류도 무관/숨김처럼 목록에서 바로 제거한다.
  - 테스트도 뒤집었다 — 처음엔 "보류는 DOM에서 지우지 않는다"를 못 박아뒀는데, 이제 **세 곳(서버 쿼리·안전망·patchItem)이 같은 목록인지**를 검사한다.
  - 편집 모달은 무관/숨김과 같은 3택 토글(하나 고르면 나머지 해제). 색은 무관(주황)·숨김(빨강)과 확실히 갈리는 **청록** — 셋이 나란히 있어 색이 비슷하면 급하게 누를 때 헷갈리고, 의미도 "제외"가 아니라 "판단 대기"라 경고색을 안 쓰는 게 맞다.
  - **곁가지로 발견·수정한 것 — 일괄바가 좁은 화면에서 잘리고 있었다.** 버튼이 최대 7개인데 `white-space:nowrap`에 줄바꿈이 없어서, 실측(헤드리스) **390px 뷰포트에서 바 폭이 475px(최악 조합 679px)**로 양쪽이 잘려 '취소'와 '0개 선택됨'이 화면 밖에 있었다. 보류 추가 전부터의 문제였지만 버튼이 하나 늘며 확실해졌고, 이대로면 **새로 넣은 보류 버튼 자체가 안 눌린다**.
    - ⚠️ 여기서 `flex-wrap:wrap`만 켜면 **더 나빠진다**: `position:fixed`에서 `right`가 auto면 가용 폭이 `100% - left` = **50vw**로 잡혀서 390px에서 바가 195px로 쪼그라들고 5줄(153px)이 됐다(실측으로 확인하고 되돌림). left/right를 양쪽으로 잡고 `margin auto`+`width:fit-content`로 바꿔서 해결.
    - 최종 실측: 390px → 바 370px·2줄(85px), 양쪽 여백 10px로 전부 화면 안. 데스크톱(1440px)은 475px·1줄로 **기존과 동일**.
  - 회귀 테스트: `tests/hold-flag.test.js` 신설(보류 배선 + "무관은 지우고 보류는 안 지운다"는 차이를 명시적으로 고정) + `interaction.test.js`에 **[버그H]** 추가 — 정적 CSS 검사만으론 위 50vw 함정을 못 잡아서 **실제 렌더 폭을 재서** 바와 보류 버튼이 화면 안에 있는지 확인한다.
- [완료][index.html][admin.js][tests/junk-exempt.test.js] **무조건 제외 키워드에 '닥터킴의'·'최강야구' 추가 + 공식 채널 업로드분은 면제**(사용자 요청).
  - **면제 근거**: 이 목록은 원래 "남의 채널이 짜깁기해 올린 것"(띵곡팔이류)이나 그룹과 무관한 외부 예능/상담 콘텐츠를 걸러내려고 만든 장치다. 그룹/솔로가 **자기 채널에 직접 올린** 영상은 제목에 뭐가 들어 있든 그 아티스트의 콘텐츠가 맞으므로 걸러낼 이유가 없다.
  - 판단을 `_shouldJunkFlag(title, sourceTier)` **한 함수**로 모으고(source_tier가 official이면 무조건 통과), 이 규칙을 쓰는 **세 경로가 전부 그 함수를 통과**하게 했다 — ①공식 채널 동기화(`_ytSyncGroup`) ②외부 채널 동기화(`_extBuildRows`, 채널 tier로 판단하므로 동작 변화 없음) ③소급 스윕(`_ytSweepJunkKeywordVideos`). 스윕은 판단에 필요한 `source_tier`를 같이 조회하도록 고쳤다(안 읽으면 전부 undefined라 면제가 통째로 무력화된다).
  - **실측**: '최강야구' 77건(그중 16건이 아직 노출 중 — 스윕 돌리면 정리됨) · '닥터킴의' 3건(전부 이미 무관). 둘 다 현재 `source_tier='official'` 행은 0건이라 이번 추가로 공식 채널 영상이 사라지는 일은 없다.
  - **⚠️ 한계(중요)**: `source_tier`는 2026-08-26에 생긴 컬럼이라 **실측 372,703건 중 official 태그가 붙은 건 310건뿐**이고 나머지는 NULL이다. 즉 이 면제는 **앞으로 동기화되는 분부터** 확실히 걸리고, 그 전에 들어온 옛 행은 공식 채널이었는지 알 방법이 없어 예전과 동일하게 취급된다. 소급 적용하려면 그룹별 동기화 북마크(`kpu_yt_last_*`)를 지우고 재동기화해 `source_tier`를 채워야 한다 — 쿼터가 크게 드는 작업이라 별도 판단 필요.
  - **범위 밖(같이 확인함)**: 공식 채널이어도 여전히 안 들어오는 두 경로가 있다 — ①밴 인물 언급(`_isBannedVideoTitle` → hidden, 별개 정책이라 손대지 않음) ②`_ytClassify`가 'skip'을 반환하는 제목(공식 음원·티저)은 **저장 자체를 안 한다**. 후자도 공식 채널은 끌어오길 원하면 따로 요청할 것.
  - 회귀 테스트 신설 — `admin.js`가 `_isJunkVideoTitle`을 **직접 부르는 곳이 하나라도 남으면 실패**한다(그 경로만 면제가 새서 공식 채널 영상이 조용히 사라지는 게 이 변경의 유일한 위험이라 거기에 못을 박았다).
- [완료][index.html][tests/live-tab.test.js] **비하인드 제외를 탐험 패널 차트까지 확대 + 예능 탭에 '놀면뭐하니' 추가**(사용자 지적 — 같은 날 1차 수정이 카드 탭에만 적용된 걸 잡아줌).
  - **① 탐험 패널 라이브 차트 8곳**(이번주 직캠 TOP · 월간 무대 TOP 30 · 연도별 TOP · 레전드 100 · 개인직캠 풀 · 무대 풀 · 그룹별 라이브 · 이맘때 그해)은 탭 조건이 아니라 `category='live'`를 **직접** 조회해서 1차 수정이 안 닿았다. 제외 조각만 `_applyLiveExclude(q)`로 떼어내 8곳 전부에 적용 — 같은 `_LIVE_EXCLUDE` 배열을 보므로 앞으로 키워드를 추가하면 카드 탭과 차트에 동시에 반영된다. 실측: `category='live'` + 조회수 있음 106,290건 중 비하인드 802 · behind 1,292가 차트에서 빠진다.
  - **② 예능 탭 제목 키워드에 '놀면뭐하니'(+띄어쓴 '놀면 뭐하니') 추가**. 실측 240건이 전부 `category='other'`라 예능 탭에 하나도 안 뜨고 있었다. 정식 명칭인 '전지적 참견'/'전지적참견'(31건)도 같은 프로그램이라 같이 넣었다.
    - **'전참시'는 이미 목록에 있었고 실제로 동작 중**이었다(넥스지·아이즈원 등에서 조회로 확인). 요청에 같이 있었지만 손댈 게 없어 그대로 뒀다.
  - **③ 예능/드라마 탭 조건도 일원화**: 라이브와 똑같이 네 군데(대표영상 선정·그리드 목록·탭 노출 판정·편집 후 클라이언트 재확인)에 같은 or 문자열이 복붙돼 있었다 → `_VARIETY_TITLE_KEYWORDS` 배열 + `_applyGenreTabQuery(q,filter)` 하나로. 클라이언트 재확인의 비교 기준도 `toLowerCase()` → `_titleNorm()`으로 올렸다(서버 `title_norm`과 같은 정규화).
  - **효과**: 예능 탭 매칭 3,611 → **3,857건**(+246).
  - 테스트 확장 — `category='live'` 직접 조회가 **하나라도 맨몸으로 남아 있으면 실패**하게 소스를 훑고(이번 누락이 정확히 그 형태였다), 예능 키워드가 정의 1곳 밖에 하드코딩돼 있으면 실패한다.
- [완료][index.html][tests/live-tab.test.js] **라이브(무대) 탭에서 '비하인드'/'behind' 제목 제외 + 라이브 탭 조건 일원화**(사용자 요청).
  - 손대보니 라이브 탭 조건이 **네 군데에 복붙**돼 있었다(대표영상 선정 · 그리드 목록 · 탭 노출 판정 · 편집 후 클라이언트 재확인). 그중 **탭 노출 판정에는 기존 제외 조건(`엠카드림`·`[live]`)이 통째로 빠져** 있어서 "탭은 보이는데 열면 빈 목록"이 될 수 있는 상태였다.
  - 커버 탭의 `_COVER_EXCLUDE`/`_applyCoverExcludeQuery`와 같은 방식으로 **`_LIVE_EXCLUDE` 배열 + `_applyLiveTabQuery(q)` 하나**로 묶고 네 곳이 전부 그걸 쓰게 했다. 클라이언트 재확인(patchItem)의 비교 기준도 `toLowerCase()` → `_titleNorm()`으로 올려 서버의 `title_norm`(NFKC+소문자)과 같은 정규화를 쓴다(스타일드 문자 "ＢＥＨＩＮＤ"까지 같이 걸림).
  - **실측**: `category='live'` 115,762건 중 '비하인드' 919건 · 'behind' 1,373건. 표본을 눈으로 확인했더니 전부 "[BEHIND THE SHOW]"·"콘서트 비하인드"·"안무 연습 비하인드"·"Performance Film Behind"류라 오제외 위험이 낮았다. 그룹별로는 에스파 1,327→1,307 · 세븐틴 1,750→1,729 · 아이브 1,802→1,778 정도.
  - **범위 주의**: 카드 안의 라이브 **탭**만 바뀐다. 탐험 패널의 "이번주 직캠 TOP" 같은 섹션들은 별도로 `category=live`를 직접 조회하므로 그대로다 — 거기도 빼려면 따로 요청할 것.
- [완료][index.html][tests/interaction.test.js] **탐험 패널에도 "맨 위로" 버튼**(사용자 요청 "카드처럼"). 카드용으로 이미 있던 `_wireTotop(scrollEl)`을 그대로 재사용해 `#feed-body`에 붙였다 — 버튼(z-index 66)이 오버레이(39)보다 위라 별도 스타일이 필요 없었다.
  - `#feed-body`는 본문 스크립트보다 나중에 파싱돼 스크립트 최상단에선 DOM에 없다. 그래서 패널이 처음 열릴 때 한 번만 바인딩하는 기존 `el._bound` 블록에 넣었다(스와이프 닫기와 같은 자리·같은 이유).
  - 스크롤을 내려둔 채 닫으면 버튼이 화면에 그대로 남는 문제(연결 패널에서 이미 겪음)를 막으려 `_closeFeedOverlay`에서 이 패널이 대상일 때만 `_hideTotop()`.
  - **회귀 테스트 [버그G]를 interaction.test.js에 추가** — 이 패널의 lazy-bind는 이미 한 번 빠뜨린 전례가 있어(스와이프 닫기) 정적 검사 대신 **헤드리스로 실제 열고·스크롤하고·눌러보고·닫아본다**: 스크롤 전 숨김 → 400px에서 등장 → 클릭 시 scrollTop 0 → 스크롤한 채 닫으면 사라짐.
- [완료][admin.js][tests/vm-panel.test.js] **영상 관리 패널 2건 — 검수 큐에서 무관 처리분 제외 + 탭 결과 캐시**(사용자 요청).
  - **① 그룹배정 검수 탭에 무관 처리한 영상이 계속 남던 문제**: 큐 조회가 `needs_review=true`만 보고 `content_flag`를 안 봤다. 무관은 "이 그룹배정이 틀렸다"는 판정이라 검수를 마친 것과 같은데도 큐에 남아 있었다 — **실측 156건 중 11건**. `_vmReviewQueueFilter(q)` 한 함수로 정의를 묶고(`needs_review=true` AND `content_flag`가 무관이 아님), **목록 조회와 관리자 홈 카운트 카드가 둘 다 이 함수를 쓰게** 했다(한쪽만 고치면 "카드엔 156인데 열면 145"로 어긋난다 — 같은 날 루틴/버튼 드리프트와 같은 종류라 아예 함수로 묶음).
  - 화면에서도 즉시 빠지게: `_vmRefreshRows`의 `stillFits`(편집 모달에서 무관으로 저장한 경우)와 `_vmSetFlag`의 탭 불일치 판정(목록에서 바로 무관 버튼을 누른 경우) 양쪽에 같은 기준을 넣었다.
  - **② 탭 결과 캐시(3분)**: 탭을 옮겼다 돌아올 때마다 같은 쿼리를 통째로 다시 던지고 있었다. 확인해보니 **이미 캐시되던 건 2개뿐**이었다 —
    - ✅ 이미 됨: `전체` 탭의 **1~2글자** 검색(`_avsEnsureCache` — 전량을 한 번 긁어 페이지 새로고침 전까지 유지) / `동기화 채널` 탭(`_EXT_CHANNELS` 메모리 배열이라 네트워크 자체가 없음)
    - ❌ 매번 재조회였음: `전체`(3글자 이상 서버 ilike) · `새로 들어온` · `무관` · `보류` · `숨김` · `검수(ss)` · `그룹배정 검수` · `라이브잠금` — 뒤 넷은 `_sbFetchAll`로 수만 행을 긁어 매번 몇 초씩 걸렸다.
    - 이제 `_vmCache`(key = 탭 + 검색어, TTL 3분)로 전부 복원된다. 캐시에 담는 `rows`는 `_vmRows`와 **같은 배열 참조**라, 행 하나만 고치는 경로가 `_vmCacheSync()`를 부르면 캐시도 같이 최신이 된다.
  - **무효화 설계**: 쓰기가 나면 `_vmCacheDropOthers()`로 **다른 탭** 캐시를 버린다(무관 처리 하나로 '무관' 탭과 검수 큐가 동시에 바뀌므로, 남겨두면 사라진 행이 되살아난 것처럼 보인다). 수천 행을 바꾸는 일괄 작업은 `_snapshotBeforeBulk` **한 곳**에서 캐시를 통째로 비운다 — 모든 일괄 버튼이 반드시 스냅샷을 거치므로 새 버튼이 생겨도 자동 적용된다.
  - **⚠️ 캐시 복원 경로에서는 `ts`를 갱신하지 않는다** — 갱신하면 TTL이 "마지막 방문 기준 3분"으로 미끄러져, 탭을 계속 오가는 동안 영원히 재조회가 안 된다. 테스트가 이걸 따로 검사한다.
  - **회귀 테스트 `tests/vm-panel.test.js` 신설**(12종 전부 통과) — 큐 정의가 한 함수인지·두 곳이 다 쓰는지·`needs_review=true` 직접 필터가 그 함수 밖에 없는지, 그리고 **`_vmRows`를 바꾸는 모든 지점에 캐시 반영이 붙었는지**를 소스 전체에서 훑는다(새 일괄 버튼 추가 시 빠뜨리면 여기가 빨개진다).
- [완료][index.html] **카드 시트 드래그 데드존 10 → 24px**(사용자 제보 "카드 스크롤 터치가 너무 민감, 처음만 좀 더 둔감할 수 있나? 내 피드백이 맞는지 확신 안 됨 — 확인해달라").
  - **확인 결과: 체감이 맞았다.** 헤드리스(390x844)에서 터치를 합성해 콘텐츠 맨 위에서 1px씩 끌며 시트에 transform이 처음 걸리는 거리를 쟀다. 상수 20→21px, 30→31px, 48→49px으로 **실측 = 상수+1px**(3회 반복 전부 동일)이라 이 값이 곧 체감 문턱이다. 바꾸기 전(10)은 손가락이 한 번 흔들리면 카드가 따라오는 수준이었고, 같은 사용자가 "지금 좋다"고 한 연결 카드 선택 상태는 데드존 48px(=24×2.0)이라 **4.8배** 차이였다.
  - **"처음만 둔감"은 이 상수 하나로 이미 성립한다** — 데드존은 콘텐츠가 맨 위(`scrollTop<=0`)일 때만 작동하고, 한 번 스크롤을 내리면 시트 드래그가 아예 개입하지 않고 네이티브 스크롤에 전부 맡겨진다. 그래서 값만 올려도 "카드를 막 열었을 때"에만 체감되고, 별도 상태 추적이 필요 없다. 사용자의 "계속 쓰다보면 괜찮다"가 이걸로 설명된다.
  - **24를 고른 이유**: 이미 "영상 한두 개라 스크롤할 게 없을 때"용으로 쓰던 값(2026-08-24 같은 사용자가 승인)이라 검증된 수치이고, 통일하면 스크롤 가능/불가 두 경로가 같아진다. `_dragSlop`의 분기를 지우고 상수 하나로 정리했다. **연결 카드 선택 상태는 ×2.0이 곱해져 48px 그대로** — 실제로 바뀌는 건 "영상 많은 카드" 한 경우뿐이다.
  - **데드존만 올리고 손가락 추적(1:1)·닫힘 임계(80px)는 안 건드렸다** — 불편의 원인은 "발동"이지 "따라오는 속도"가 아니었다. 최종 실측 25px(=24+1) 확인, 테스트 11종 전부 통과.
  - 아직 가벼우면 30/48로 한 줄만 바꾸면 된다(실측이 선형이라 그대로 체감 문턱이 됨).
- [완료][artists.json][tests/matching.test.js][정적페이지] **체리블렛 현 멤버 7명 누락 — 전 멤버 3명만 등록돼 있었음**(사용자 제보 "전 멤버만 반영되고 현 멤버만 싹 빠짐"). artists.json에 미래·코코로·린린(2019.12.13 탈퇴)만 있고 **해체까지 남은 7명(해윤·유주·보라·지원·레미·채린·메이)이 통째로 없었다.**
  - **출처**: 나무위키 체리블렛 문서 + 멤버 개별 문서 7개(2026-08-27 조회, curl -k 우회). 생년월일·국적·인스타를 개별 문서에서 확인. 허지원은 인포박스에 `huhjiwon_official`과 `jiwxoxni` 둘 다 있는데 전자는 소속사 운영 "개인 공식"이라 제외([[feedback_instagram_personal_only]]).
  - **관례**: `name.ko`는 활동명(기존 전 멤버 3명과 동일, 영상 제목에 실제로 쓰이는 표기). 아이즈원과 같이 **해체까지 남은 멤버는 `active:true` + `left` 없음**, 중간 탈퇴자만 `active:false` + `left`. 현 멤버를 전 멤버 앞에 삽입(나무위키 표기 순서). diff는 189줄 순수 추가.
  - **효과 실측**: `group_ko=체리블렛` 영상 275건 중 **257건이 `members` 비어 있었고, 제목에 현 멤버 이름이 있는 건 116건**(지원 28·유주 23·메이 16·보라 16·레미 12·채린 12·해윤 9). 개인 직캠들이 멤버 카드에 하나도 안 뜨던 상태 → 다음 "2. 멤버+콜라보 자동 태깅"에서 붙는다.
  - **동명이인 검증(사용자 요청)**: 7명 중 5명이 동명이인이다 — 유주(여자친구·하입프린세스), 지원(프로미스나인·시그니처), 메이(리센느·세이마이네임), 보라(씨스타), 채린(아이칠린). **추가 전/후 artists.json으로 실제 매처를 각각 조립해 DB 실제 제목 4,352건(토큰 16종)에 돌려 비교**했더니 결과가 달라진 건 **2건뿐**이었다.
    - 그룹명이 제목에 있으면 전부 정확히 갈린다(체리블렛 유주 ↔ 여자친구 유주, 체리블렛 지원 ↔ 프로미스나인 지원 등 양방향 확인).
    - 그룹 표시가 없으면(`#지원 챌린지`·`#메이 챌린지`) 동명이인 dedup이 **아무 그룹도 안 잡는다**(오배정 0). 유일 이름(해윤·레미)만 그대로 잡힌다.
    - 감사 스캐너가 경고한 "미래(체리블렛)가 그룹명 미래소년에 통째로 들어있음 · 보호 없음"은 **오탐** — `hit()`의 토큰 경계 검사가 막는다(같은 날 미래소년 strictSync를 푼 뒤라 확인해둠).
  - **[알려진 한계·미해결] 그룹명이 하나도 없는 제목의 primaryGroup은 artists.json 등장 순서로 갈린다**(역추론 경로의 `result[0]`). 그래서 "이승협 직캠 'Superstar (Feat. 해윤)'"이 엔플라잉→체리블렛으로 뒤집혔다(4,352건 중 이 1건). 다만 `confidence:'weak'`라 검수 큐로 가고, 이미 저장된 행은 `_ytSweepMistagReclassify`가 "엔진 그룹이 제목에 literal로 있어야 재배정"이라 안 건드린다. **근본 개선은 feat 절 안의 인물을 primary에서 강등하는 것** — 회귀 테스트에 현재 동작을 "알려진 한계"로 명시해뒀다.
  - 회귀 테스트 10건 추가(74/74 통과) + 정적 SEO 페이지 리빌드(`node build_group_pages.js`, 멤버 1730명 × 2언어).
  - ~~곁가지 발견: 여자친구가 3명만 등록됨~~ → **오독이었음(2026-08-27 정정, 사용자 지적)**. 6명 전원 등록돼 있고, 은하·신비·엄지는 **비비지 겸임**이라 주 소속(`a.group.ko`)이 비비지이고 `groups[]`에 여자친구가 들어 있다. 로스터를 셀 땐 `a.group.ko`만 보면 안 되고 `_artistGroups(a)`(주 소속 + 겸임)를 써야 한다 — 겸임 멤버가 있는 그룹은 전부 같은 함정이 있다.
- [완료][admin.js][tests/routine-parity.test.js] **매일 루틴 실행기의 1번 단계가 설정 패널 버튼과 다른 일을 하고 있었음 — 외부 채널 동기화·조회수 갱신 누락**(사용자 질문 "루틴 버튼에서 조회수 저장도 하고 있나?"에서 발견).
  - **증상**: 패널 버튼 라벨은 `1. 전체 동기화 (공식 + 외부 채널)`이고 그 핸들러는 `_ytSyncAll → _ytSyncExtChannels → _ytRefreshViewCounts` 셋을 순서대로 부르는데, 루틴은 `_ytSyncAll` **하나만** 직접 불렀다. 그래서 루틴만 돌린 날은 **①외부 채널(음방·예능·아이돌주도) 유입과 ②조회수 갱신(이번주 직캠 TOP10이 의존)이 통째로 빠졌다.** 화면엔 "1. 전체 동기화 ✅"로 떠서 다 된 것처럼 보이는 게 특히 나빴다.
  - **왜 났나**: 루틴은 "각 단계는 원래 함수를 그대로 호출하므로 동작이 갈릴 일이 없다"는 전제로 짜였는데, 실제로 집어온 건 **버튼이 하는 일이 아니라 안쪽 함수 하나**였다. 눈으로는 안 보이는 드리프트다.
  - **수정**: withSync 단계를 버튼 핸들러와 **순서까지 동일하게** 3단계로 분리(`1. 전체 동기화 (공식 채널)` / `1-2. 외부 채널 동기화` / `1-3. 조회수 갱신`). 비용 확인 후 넣었다 — 외부 채널은 playlistItems.list(쿼터 1/콜, sinceId 체크포인트 증분), 조회수는 videos.list(쿼터 1/콜, 최근 14일분이라 ~70콜). 비싼 search.list는 과거 백필 전용이라 무관. "동기화 빼고 실행"(2~4번만)은 그대로.
  - **회귀 테스트 `tests/routine-parity.test.js` 신설** — 소스에서 버튼 핸들러의 호출 목록과 루틴의 withSync 단계 목록을 각각 뽑아 **순서까지 대조**하고, 2~4번도 각 패널 버튼과 같은 함수인지, 라벨이 실제 동작과 어긋나지 않는지 확인한다. 수정 전 코드에 돌려 실제로 빨간불이 나는 것까지 확인함(git stash로 역검증).
- [완료][index.html][tests/coverof.test.js] **"다른 아티스트의 커버"가 솔로 아티스트 카드에서 안 보이던 버그 — 같은 제보 2회차, 이번엔 진짜 원인**(사용자 재제보: "어제 공유했는데 여전히 아이유 카드 cover 탭 아래 안 나옴. 솔로 다 그럴 듯" — **맞았다**).
  - **8/26 수정이 왜 안 먹었나**: 그날 고친 건 `_loadCoverOfSection`의 **멤버 카드 분기**(`memberKo` 있는 경로)인데, 무소속 솔로 카드는 그 분기를 **안 탄다**. `showT()`에서 `_hasRealGroup`이 false면 `_ttChVidCtl.build(_ytGroupKoFor(a))`로 **memberKo 없이**(그룹 카드처럼) 열리기 때문 → 삼항이 `cover_of_groups`만 조회 → 커버 태깅은 사람 단위(`cover_of_members`)라 결과 0건. 주석에 "무소속 솔로(아이유·보아)는…"이라고 써놓고 정작 솔로가 안 지나가는 분기를 고친 셈.
  - **실측(2026-08-27)**: 아이유 `cover_of_members "아이유(솔로)"` 61건 / `"아이유(아이유)"` 1건 / `cover_of_groups "아이유"` 0건. 같은 이유로 보아 29건·비 19건·이영지 6건·승한 5건·싸이 4건이 전부 안 보이고 있었다.
  - **수정 ①**: 조회 키 계산을 `_coverOfQueryKeys(ko,memberKo)` **한 함수로 모았다** — 두 번 놓친 진짜 이유가 이 계산이 섹션 로드와 탭 노출 판정 두 곳에 흩어져 있던 것이라, 한쪽만 고쳐지는 상황 자체를 없앴다. memberKo가 없어도 카드 키가 무소속 솔로 본인이면 사람 키(`이름(본인)`+`이름(솔로)` 둘 다)로 조회한다.
  - **수정 ②(별건, 같이 발견)**: Cover 탭 노출 판정이 "본인 채널의 커버 제목 영상"만 봐서, 그게 0건이면 탭째로 숨겨져 `cover_of`가 있어도 도달 불가였다 → 탭 판정도 `cover_of`를 같이 본다.
  - **수정 ③(별건, 같이 발견)**: `showT()`가 무소속 솔로에게 `a.links.youtube`(등록된 본인 채널)를 요구해서, 채널 미등록이면 **카드 영상 섹션이 통째로 스킵**됐다. 비(Rain)가 그 경우로 영상 5건+커버 19건이 있는데 아무것도 안 떴다. 폴백(`buildVideoSection`)이 쓰는 artists.json `songs`가 해당 3명(비·빛새온·김민서) 모두 0건이라 폴백으로 얻는 게 애초에 없어서, 조건을 `sb`만으로 완화(잃는 것 없음, 전수 확인 후 변경).
  - **회귀 테스트 `tests/coverof.test.js` 신설** — 문자열 검사가 아니라 **헤드리스 브라우저에서 실제로 카드를 열고 Cover 탭을 눌러** 섹션이 보이는지까지 본다(8/26 수정도 문자열 검사였다면 "통과"했을 것이다). 검증: 아이유 56개·비 19개 썸네일 렌더 + 탭 클릭 후 실제 노출, 그리고 멤버 카드(카리나)·그룹 카드(에스파) 키가 안 바뀌었는지.
- [완료][groups.json][admin.js][tests/matching.test.js] **strictSync 목록 2차 정리 — 22팀 → 7팀**(DECISIONS 처리, 2026-08-27 발견분). `_m2ParseTitle`의 그룹 후보 생성이 strictSync 그룹을 제목 키워드 매칭에서 통째로 빼는 바람에, 여자친구·에이프릴처럼 **제 그룹명이 제목에 뻔히 있는 영상까지 멤버 이름 역추론에 밀려 남의 그룹으로 가 있었다.**
  - **방법**: `tests/matching.test.js`와 같은 방식으로 실제 매처를 조립해, 각 그룹 토큰이 든 실제 DB 제목(그룹당 최대 1,000건)에 **strictSync 켠 결과 vs 그 그룹만 뺀 결과**를 돌려 1:1 비교. 22팀 전수.
  - **⚠️ 전제 하나 정정**: 원 DECISIONS의 "외부채널 미태깅 손실"은 DB 쿼리로 안 보인다 — `_extBuildRows`가 매칭 실패 영상을 **저장 자체를 안 하고 버려서** `group_ko is null`이 전 그룹 0건이다. 그래서 손실을 "미태깅 행 수"가 아니라 "앞으로 안 버려질 제목 수"로 재봤다.
  - **결론: 판별선은 "해체/규모"가 아니라 "그룹명이 한국어 일반명사·프로그램명·곡명과 겹치는가"였다.** 원 가설("단지 해체된 대형그룹은 제외 대상이 아닐 수 있다")은 맞았지만 기준이 한 겹 더 아래에 있었다.
  - **유지 7팀**(괄호 = 해제 시 오탈취 규모, 표본 기준): 레인보우(826 — "더 시즌즈-**이영지의 레인보우**" 프로그램명) · 배틀(427 — "랩배틀/댄스 배틀/Team Battle") · 하이라이트(331 — "N회 하이라이트" 코너명, "Highlight Medley" 앨범) · 시크릿(261 — "SECRET BOX"·"IVE SECRET" 앨범명) · 슈가(182 — 방탄 슈가 + "Sugar Rush Ride"/"Sugar" 곡명) · 스피드(87) · god(77).
  - **해제 14팀**(괄호 = 지금 DB에 남의 그룹으로 저장돼 있다가 정정될 행 수, 표본 기준): 여자친구(98) · 로켓펀치(81) · 씨아이엑스(51) · 위클리(32) · 미래소년(29) · 에이프릴(27) · 비디씨(27) · 구구단(7) · 티에이엔(7) · 피에스타(6) · 씨엘씨(3) · 엑스원(3) · 남녀공학(2) · 프리스틴(0). 여기에 "앞으로 외부채널에서 안 버려질 제목"이 그룹당 100~800건씩 더 붙는다.
  - **오배정 실제 사례 — 전부 멤버 이름 역추론이 그룹명을 이긴 것**: "Rocket Punch(로켓펀치) - CHIQUITA" → **베이비몬스터**(치키타=멤버명) / "씨아이엑스 배진영 직캠" → **워너원**(배진영 전 소속) / "에이프릴 양예나 직캠" → **아이즈원**(예나 동명이인) / "미래소년 이준혁 직캠" → **세븐틴**(준) / "위클리 지한 페이스캠" → **스트레이키즈**(한).
  - **에이스(A.C.E)는 중간안**: 유일하게 "한글 토큰만 위험"으로 갈렸다. 통째로 풀면 진짜 A.C.E 무대(THE SHOW 다수)를 되찾는 대신 "**에이스** 형사"·"1 vs 1 **에이스** 랩 배틀"·"갓기 **에이스** 응원"까지 훔친다 → strictSync는 풀되 한글 '에이스'만 `_GROUP_TOKEN_HASHTAG_ONLY`로 내렸다. 영문 'A.C.E'는 마침표 덕에 정규화 후 ' A C E '라는 고유 시퀀스가 돼서 `hit()`이 안전하게 잡으므로 게이트 밖.
  - **회귀 테스트 16건 추가**(matching.test.js, 64/64 통과) — 푼 14팀은 제 이름으로 잡히는지, 남긴 7팀은 계속 안 잡히는지 양쪽을 다 고정. 케이스 제목은 전부 DB 실제 행. 기존 케이스 1건(`여자친구(strictSync) — 제외`)은 **의도적으로 뒤집었고** 그 자리에 이유를 주석으로 남겼다.
  - **⚠️ 배포 후 할 것**: 코드는 "앞으로 들어올 영상"만 고친다. **이미 남의 그룹으로 저장된 행은 어드민 "오태깅 그룹 재배정"(`sp-mistagfix-btn`)을 한 번 돌려야 회수된다** — 드라이런(건수 confirm) + 스냅샷 되돌리기가 붙어 있으니 표본을 보고 진행할 것.
  - 재현 스크립트(`strictsync-sim.mjs`)는 세션 스크래치패드에 있음 — 다음에 목록을 또 손볼 일이 생기면 `tools/`로 승격할 것.
- [완료][is_short_migration.sql][index.html][admin.js][tests/shorts-promote.test.js] **short를 "형식 플래그"로 직교화 — category 단일값에서 is_short 불리언으로 분리**(DECISIONS 처리, 2026-08-26 사용자 지적). 세로 직캠을 쇼츠로 승격하면 장르가 통째로 날아가 Live 탭에서 사라지던 구조를 끊었다. 이제 세로 직캠은 Live 탭과 Shorts 탭에 동시에 뜨고 9:16으로 그려진다.
  - **왜 content_formats에 'short' 태그를 넣지 않고 새 불리언인가**: ①태그 저장(`_vidTagSave`)이 content_formats의 장르 태그를 통째로 재계산·교체해서, 거기 섞이면 관리자가 태그 한 번 만질 때마다 short가 조용히 날아간다 ②`get_content_formats` RPC로 뽑는 "콘텐츠별 보기" 목록에 장르인 척 끼어든다 ③불리언은 부분 인덱스가 싸고 "형식"이라는 의미가 그대로 드러난다.
  - **읽기**: `_isShortV(v)` 헬퍼 한 곳으로 모음(`is_short===true || category==='short'` 레거시 폴백) — index.html의 직접 비교 48곳 전부 교체. category를 뽑는 컬럼 목록 33곳에 is_short를 같이 넣음. **이게 가장 조용히 깨지는 지점이라**(빠지면 폴백만 남아 "승격은 됐는데 가로로 그려짐") 테스트가 두 파일의 select 문자열을 통째로 훑어 막는다.
  - **쓰기**: ①승격 스윕(`_ytSweepPromoteShorts`)이 category 덮어쓰기 → `is_short:true` 플래그만, 조회도 `.neq('category','short')` → `.eq('is_short',false)` ②동기화 3경로가 `isShortThumb?'short':_ytClassify()` → category와 is_short를 따로 계산(`_ytIsShortTitle` 신규 분리, `_ytClassify`는 더 이상 'short'를 반환하지 않음) ③클라이언트 자동 보정(`_probeShortsBatch` 계열) 3곳도 플래그로.
  - **탭/렌더**: Shorts 탭 필터 3곳이 `.eq('is_short',true)` — 부분 인덱스 `idx_ytv_is_short_group_published`와 같은 형태라 **조건을 바꾸면 SQL 쪽 predicate도 같이 고칠 것**. 9:16 렌더·라이트박스 쇼츠 진입·`patchItem` 자격 재확인도 전부 플래그 기준.
  - **어드민 모달**: "영상 포맷" select에서 Shorts 옵션 제거 → 별도 체크박스(`vid-tag-isshort`). 일괄 편집은 content_flag와 같은 3상태(안 건드림/켬/끔)로 동작. `is_short`를 `_BULK_SNAP_COLS`에 추가해 되돌리기가 실효를 갖게 함(reviewed_at과 같은 컬럼-부재 폴백도 같이).
  - **⚠️ 배포 순서: `is_short_migration.sql`을 먼저 실행하고 그 다음 push.** 반대로 하면 컬럼 없는 상태로 select가 나가 PostgREST 400 → 카드 조회 전멸. 실제로 지금 스모크/인터랙션 테스트가 이 400으로 빨간불이며(원인은 curl로 확인: `column yt_channel_videos.is_short does not exist`, 변경 전 트리에선 둘 다 통과), **SQL 실행 후 다시 돌려서 초록 확인할 것.**
  - **③기존 84,286건 장르 재추론**: 별도 일회용 버튼을 만들지 않고 기존 "영상 카테고리 재분류(전체)"(`_ytSweepCategoryMistag`)에서 쇼츠 제외를 없애는 것으로 대신했다 — 그 버튼엔 이미 스냅샷·되돌리기가 붙어 있고, 제외했던 이유("제목만으로 재분류하면 쇼츠가 live로 역행")가 직교화로 사라졌다. **SQL 실행 후 이 버튼을 한 번 누르면 3단계가 끝난다.**
  - 남은 것: 동기화 시점 세로 판별은 여전히 제목(#shorts)뿐이라, 새로 동기화된 쇼츠는 다음 승격 스윕 전까지 가로로 뜬다(2026-08-26 항목의 미해결 ①과 동일 — 직교화와는 별개 과제).

---

## 2026-08-26
- [완료][admin.js][tests/matching.test.js] **동명이인 영문토큰 애매성 → 검수 큐로(옵션 A, 사용자 결정)**(제보: 투어스 지훈 카드에 워너원 박지훈 영상 100+). 원인: 역추론 동명이인 dedup이 `name.ko`로만 판정해서, "JIHOON"이 투어스 지훈·트레저 지훈·워너원 박지훈 **3명의 en과 겹쳐도** 유일 ko인 '박지훈'(워너원)이 살아남아 **strong으로 확정**됐다. (그 100+개는 게이트 도입 전 옛 데이터 — 현재 매처는 "박지훈/PARK JIHOON" 제목을 이미 투어스로 안 보냄, 실측 확인. 남은 리스크는 **반대 방향**: 그룹표시 없는 `#JIHOON` 단독이 워너원으로 확정되던 것.) **무매칭(=영상 유실)은 사용자가 거부** → 검수로 보내는 방식(옵션 A). 수정: `_m2ParseTitle`에 토큰 단위 감지(`memberHitTokens`) 신설 — 한 토큰이 서로 다른 사람 2명 이상과 매칭 + 제목에 그룹표시 없음(owner-less=selfGko 없음)이면 `confidence:'ambiguous'`. `_extBuildRows`가 이를 **content_flag:'hidden' + needs_review:true + 멤버 비움**으로 저장 → 유저에겐 안 보이고(오배정 0) 관리자 **"그룹배정 검수" 탭**에만 뜸(승인/거부/✎재지정). 그룹표시 있으면(#TWS·트레저·워너원) 지금처럼 즉시 확정. `confidence` 소비처는 `_extBuildRows`뿐이라 selfGko 있는 다른 호출부엔 영향 0(primaryGroup 값도 불변). 회귀 4종 추가(matching 48/48). ⚠️**남은 것**: 같은 ko끼리 완전 충돌해 둘 다 dropped되는 경우(#성민 단독 등)는 여전히 null(무매칭) — group_ko 비움 저장 plumbing이 필요해 후속.
- [완료][index.html][kpop_universe.css] **프로필/컬렉션 UX 3건(사용자 요청, "일단 심플하게")**. ①**새 컬렉션 버튼 통합(방식 A)**: `+새 컬렉션`/`+사진 컬렉션` 두 버튼 → `+새 컬렉션` 하나. 이름 입력창에 `[영상][사진]` 인라인 토글(`_promptDialog`에 `toggle` 옵션 신설 — 있을 때만 `{name,choice}` 반환=하위호환). 사진 선택 시에만 로그인 확인 후 photo 컬렉션 생성+그리드. ②**LIKES 버튼 진입**: 담아둔 영상 인라인 미리보기(6개) 제거 → "담아둔 영상 N개 보기" 버튼 하나로(likes가 많을 수 있어 패널이 길어지던 것). 전체 리스트는 기존 `_openFeedListOverlay`. ③**즐겨찾기 순서 변경 — 케밥(⋮) 메뉴**: 상시 표식(드래그 핸들·화살표) 없이 각 행 기존 ⋮ 버튼을 눌렀을 때만 메뉴(기념일 수정 + ↑위로/↓아래로/⤒맨위로). 순서는 `favOrder`(dk 배열)에 저장, `renderFavList`가 그 순서로 DOM 재배치(신규 항목은 기존 순서로 뒤에, 안정 정렬). `favOrder`는 다른 fav_*처럼 **계정 동기화**(push+pull+신규insert 배선 완료). ⚠️**SQL대기**: `user_data`에 `fav_order jsonb` 컬럼 필요(`fav_order_migration.sql`) — 실행 전엔 로컬로만 유지되고 서버 push가 콘솔 에러(무해). 실기기 확인 권장(케밥 팝업 위치·터치).
- [완료·실기기확인필요][index.html] **연결 카드 선택 상태 드래그 감도 둔감화**(사용자 제보 "연결 카드에서 영상 2~3개뿐인데 너무 빠르게 스크롤돼 정신없다"). 원인: 영상이 적으면 콘텐츠가 스크롤되지 않아(scrollTop≈0) 모든 세로 제스처가 `_wireSheetSwipe`의 "시트 드래그"로 처리되는데, 카드가 손가락을 1:1로 따라가고 60~80px만 움직여도 닫힘/펼침이라 작은 플릭에도 확 반응. 수정: `_wireSheetSwipe`에 감도 배율 `sensFn`(≥1) 인자 신설 — 데드존·손가락 추적량(1/배율)·닫힘/펼침 임계값(×배율)을 한 배율로 함께 조절(임계 도달 시 카드의 화면상 이동량은 배율 무관 일정, "카드가 더 무겁게"). 연결 카드에만 `()=>_connSelected.length>0?2.0:1`로 배선 → **멤버/그룹 고른 상태에서만 2.0배 둔감**, 초기 상태(아직 선택 없음)·다른 카드(멤버/그룹 시트)는 배율 1로 기존과 동일. ⚠️네이티브 콘텐츠 스크롤 자체는 안 건드림(과거 커스텀 관성은 네이티브가 더 부드럽다고 걷어낸 이력 존중). **2.0 값은 실기기 체감에 따라 조정**.
- [완료][index.html] **카드 무한스크롤이 끝 전에 멈추고 "N개"가 뜨던 간헐 버그 — 정렬 tie-break 누락**(사용자 제보 "끝까지 로딩 안 됐는데 멈추고 N개 뜸, 새로고침하면 이어짐"). 원인: 영상 무한스크롤이 `published_at` **하나로만** 정렬 + `.range(offset,+47)` offset 페이지네이션인데, `published_at`이 동기화 때 **날짜만**(`slice(0,10)`) 저장돼 같은 날 영상은 정렬값이 전부 동일 → **고유키 tie-break가 없어** PostgreSQL이 동일값 행 순서를 요청마다 다르게 준다. 그래서 페이지 경계(48↔96)가 어긋나 일부 영상 중복(→`seenIds`로 걸러짐)·일부 누락 → 실제 그리드는 총계보다 적은데 라벨은 count 쿼리의 진짜 총계 "N개"를 보여줌(끝난 것처럼). 새로고침하면 DB가 동일값 순서를 다시 다르게 줘 누락 패턴이 바뀜(간헐성·재개). **수정**: `ordered()`(카드 그리드 순차+청크 랜덤 둘 다 이걸 탐)의 모든 분기 끝에 `.order('id')` 2차 정렬 추가 → 페이지 경계 결정적. 겸사겸사 `_coverEligibleGroups`의 정렬 없던 `.range()` 카운트 집계에도 `.order('id')` 추가(중복집계·누락 방지). 타임아웃은 원인 아님(에러 시 스피너만 지우고 재시도, "N개"와 다른 경로)로 확인. 캐시버전 해시 자동.
- [②완료·①후속][admin.js][index.html][tests/shorts-promote.test.js] **쇼츠 자동 판별이 사실상 작동하지 않고 있다 — 약 2.8만 건 오분류 추정**(사용자 제보 "shorts인데 형식 지정 안 돼서 가로 직사각형에 축소 노출되는 게 왜 이렇게 많냐"). **근본 원인을 찾았고, 핵심 해법(②일괄 승격 스윕)을 구현했다.**
  - **원인: 동기화 시점의 세로 판별이 원리적으로 불가능한 썸네일을 보고 있다.** `_ytSyncChannel` 계열 4곳이 전부 `const hiTh=th.high||th.standard||th.maxres; const isShortThumb=!!(hiTh&&hiTh.height>hiTh.width)`로 판별하는데, 실측해보니 **쇼츠도 high=480x360, standard=640x480, maxres=1280x720으로 전부 가로다.** 즉 `isShortThumb`가 **쇼츠에서 참이 될 수 없다.** 원본 비율을 유지하는 건 `oardefault.jpg`(1080x1920)뿐. 2026-08-10에 "썸네일 용량을 줄이려고 maxres 우선을 high 우선으로 뒤집으면서 — 세로 판별에는 어차피 다 같은 비율이라 영향 없음"이라고 주석에 적어두고 뒤집었는데, **그 전제가 틀렸다**(maxres도 어차피 가로였으므로 그 변경 이전에도 깨져 있었을 가능성이 높다). 결과적으로 지금 short로 잡히는 84,286건은 사실상 제목의 `#shorts`/`SHORTS` 정규식으로만 걸린 것들이다.
  - **실측 규모**: 전체 372,557건 중 short가 84,286건. short가 아닌 것에서 402건을 뽑아 `oardefault.jpg` 실제 픽셀을 재보니 **40건(10.0%)이 세로**였다(가로 1건, oardefault 없음=가로 확정 361건). 전체로 환산하면 **약 2.8만 건**이 쇼츠인데 가로 카드로 찌그러져 노출되고 있다.
  - **왜 화면에서 자동 보정이 안 되나**: 클라이언트에 `_probeIsPortrait`(oardefault로 판별)가 있긴 한데 ①**렌더된 영상만** 검사하고 ②배치 데드라인 5초에 걸려 중간에 끊기며 ③보정 결과를 저장하는 `update({category:"short"})`가 **anon 세션이라 RLS에 막혀 저장이 안 된다**(관리자가 직접 그 화면을 봤을 때만 저장됨). 게다가 기존 "영상 카테고리 재분류(전체)" 스윕은 `category==="short"`를 **조회에서 제외하고 결과로도 만들지 않아서**, 가로→쇼츠 방향 승격은 어떤 경로로도 일어나지 않는다.
  - **수정 방향(다음 작업)**: ①동기화 시점 판별을 `oardefault.jpg` 실측(또는 API `contentDetails.duration` 병용)으로 교체 — 지금 코드는 절대 못 맞춘다. ②관리자 일괄 스윕 신설(가로→short **승격 전용**, 반대 방향은 위험해서 콘솔 목록만). 대상이 약 28만 건 프로브라 재개 가능한 청크 방식 필요(동시 12개 기준 약 90분). ③`tags_manual` 보호는 기존 카테고리 스윕과 동일 규칙 적용.
  - **[2026-08-26 구현] ②일괄 승격 스윕 완료** — 설정 패널에 `⬆️ 가로→쇼츠 일괄 승격`(`_ytSweepPromoteShorts`) 추가. short 아닌 행을 id 오름차순으로 훑으며 `_probeIsPortrait`(oardefault 실측)로 세로면 `category='short'` **승격만**. 안전장치: **중단/재개**(localStorage 커서 `_kpu_shortsPromoteCursor` — 이미 확인한 가로 행 재프로브 안 함), **강등 금지**(조회에서 short 제외 + update는 항상 'short'만), **tags_manual 보호**, **실행 전체가 하나의 batch로 스냅샷**(`_snapshotBeforeBulk`에 `forceBatchId` 인자 신설 → `↩︎ 되돌리기`로 통째 복원). 되돌리기 실효를 위해 `_BULK_SNAP_COLS`에 `category` 추가(같은 날 카테고리 재분류 버그픽스와 공유). 회귀 테스트 16종(`tests/shorts-promote.test.js`). **동시 12개·전량 ~90분, 중단해도 커서로 이어서.** 캐시버전은 해시 자동(bump-cache-version.yml).
  - **⚠️ 남은 것 ①(동기화 시점 교체)**: 아직 안 함. 동기화는 여전히 제목 정규식 기반이라(썸네일 판별은 항상 false=무해) **새로 동기화된 쇼츠는 다음 승격 스윕 전까지 가로로 보임**. 승격 스윕이 재개 가능·재실행 가능하므로 **동기화 후 이 버튼을 주기적으로 돌리면 실무상 커버**되지만, 근본적으로는 동기화 4곳(`admin.js` 193·4345·4478·5215)의 판별을 oardefault 실측/프로브로 교체하는 게 남은 과제. 매 영상 프로브가 동기화 핫패스에 지연을 주므로 "신규분만 바운드 동시성으로 프로브 후 upsert" 형태 설계 필요 — 별도 세션. 사용자 부재 중이라 핫패스는 손대지 않고 승격 스윕만 먼저 배포.
- [완료][admin.js][tests/collab-sweep.test.js] **콜라보 재판정 근거를 태깅 매처와 동등하게(별칭·altName·겸임) — 오삭제 202건 구제**(2026-08-26, 아래 [예정] 착수). 제거 판정의 근거 검사(`_collabGroupEvidenced`/`_collabMemberEvidenced`)가 name.ko/en만 봐서, 태깅이 altNames·matchAliases·겸임(`_artistGroups`)으로 붙인 태그가 재검증마다 삭제후보(tags_manual만이 방파제)이던 것 해소. ①그룹 근거에 altNames(브브걸↔브레이브걸스·슈퍼노바↔초신성) ②멤버 근거에 matchAliases(황민현↔민현·JIN↔진) ③멤버 조회를 `_artistGroups`로(겸임 민현(워너원)을 주소속 뉴이스트로만 찾아 영문/별칭 검사가 통째 스킵되던 것). **그룹 근거 전제는 유지 → 동명이인 오삭제 방어 그대로.** 실측: with_members 15,415·with_groups 7,806 태그 중 **202개(멤버 147·그룹 55)가 오삭제→구제**(앤팀·초신성·브레이브걸스 등, 표본 전부 정당). 런타임 6종 + 회귀 3건. ⚠️**남은 것**: tags_manual 보호 제거는 아직 — 전체 재판정을 돌려 "보호분이 이제 안 지워지는지" 측정한 뒤 별도 판단(_collabRejudge는 여전히 positive 검증은 `_m2ParseTitle`(제목only)이라, 근거넷이 커버 못하는 잔여가 있는지 확인 필요).
- [예정→일부완료][admin.js] **콜라보 재판정 매처를 태깅 매처와 동등하게 올리기 — 집에서 작업 예정**(2026-08-26 합의). ↑위 완료 항목이 근거 검사(evidence) 대칭화를 처리함. 원문: 오늘 넣은 "이름+그룹이 둘 다 보이면 안 지운다" 안전장치는 **방어일 뿐**이다. 제거 판정에 쓰는 `_m2ParseTitle`이 여전히 ①**설명란을 안 보고** ②영문 표기·별칭을 약하게 다루며 ③한 글자/흔한단어 이름을 평문에서 못 잡는다. 그래서 "설명란에만 근거가 있던 234건"은 재판정으로 복원되지 않는다. **수동 태깅 보호(tags_manual)를 없애도 되는 상태**는 이 작업이 끝나야 온다 — 현재는 수동 보호가 오삭제를 막는 유일한 방파제다(오늘 실측: 삭제 후보 태그 515건 중 281건(55%)이 제목에 눈으로 보이는데도 후보였고, 전부 tags_manual 덕에 살아있었다). 관련: 태깅은 제목+설명+별칭+영문+해시태그로 붙이는데 제거는 제목만 보는 **비대칭**이 핵심.
- [완료][admin.js][tests/collab-sweep.test.js] **"콜라보 오태깅 재검증이 멀쩡한 걸 지우려 한다" — 제보 조사 + 2건 수정**(사용자 제보). **먼저 결론: 데이터는 안 날아갔다.** 오늘 오전 직접 잰 값과 비교 — with_members 있는 영상 12,126 → 12,143(+17), 태그 총 개수 15,399 → 15,409(+10). 루틴이 지운 게 아니라 동기화로 조금 늘었다.
  - **버그 ①: 미리보기가 거짓 보고를 하고 있었다.** 미리보기(_ytSweepDetectPreview)와 실제 스윕(_ytSweepAmbiguousCollabMistag)이 같은 판정을 **각자 복붙**해뒀는데, 미리보기 쪽에만 promote/consolidate(태그를 되살리는 경로)가 빠져 있었다. 결정적으로 tags_manual=true 행을 **세기만 하고 목록에서 안 걸러내서**, 콘솔의 "제거될 with_members 상위30"이 사실상 **보호되는 태그로 도배**돼 있었다. 전체 17,185건 재현 실측: 미리보기가 "제거 예정"이라 부른 **381건 중 380건이 수동 편집 행**(=실제 스윕이 절대 안 건드림), 실제로 바뀌는 행은 **1건**(그것도 방금 지운 라일락 유령 태그). 진행바 문구도 "N개에서 태그 제거 예정"을 앞세우고 "수동보호 M개"를 괄호 뒤에 붙여놔 오해를 키웠다. → 판정을 **_collabRejudge 하나로 합치고**(두 곳이 다시 갈라질 수 없게), 수동 편집분을 집계·목록·문구에서 완전히 분리했다.
  - **버그 ②(진짜 구조적 결함): 태그는 강한 매처로 붙이고, 제거는 약한 매처로 판단한다.** 재판정에 쓰는 _m2ParseTitle은 **설명란을 아예 안 보고** 사실상 한국어 평문 위주라, 강한 매처(제목+설명+별칭+영문+해시태그)나 **사람 손**만 찾을 수 있는 태그는 재검증 때마다 삭제 후보가 된다. 실측: 삭제 후보 태그 515건 중 **281건(55%)이 제목에 눈으로 보였다** — 해시태그 59건("#TEMPEST #혁", "#EPEX #뮤"), 평문 183건("현아&던", "문빈,윤산하,강민"), 영문표기 39건("PSY - 챔피언", "#SOYEON"). 지금 이게 안 지워지는 유일한 이유는 그 행들이 전부 tags_manual=true라서다 — **수동 보호가 방파제 역할을 하고 있을 뿐, 보호를 풀면 그대로 날아간다.** → 제거 안전장치 추가: **이름과 그룹이 둘 다** 제목/설명에 보이면 제거 후보에서 뺀다. ⚠️이름만으로 인정하면 동명이인 오태깅을 못 걷어내므로 **그룹 근거를 반드시 전제**로 했다 — 드림캐쳐 지유 영상의 "지유(키키)"는 "키키"가 없으니 그대로 제거 대상으로 남는 걸 확인. 흔한단어·한 글자 이름은 기존 정책(_isHashtagOnlyName, 해시태그만 인정)을 그대로 재사용해 "여름"/"온" 같은 우연한 단어를 근거로 오인하지 않게 했다. 효과: 삭제 후보가 된 행 384 → **286**(98건이 후보에서 빠짐), 실제 변경은 여전히 1건이라 **오늘 당장 바뀌는 건 없고 미래의 오삭제만 막는다**.
  - 회귀 테스트 `tests/collab-sweep.test.js` 신설 — 판정 일원화(두 함수가 각자 _m2ParseTitle/_classifyGuestGroup을 다시 부르면 실패), 안전장치 배선, 그룹 근거 전제, 설명란 조회, 수동 보호 문구까지 19개 항목을 잠근다.
  - 남은 과제(미해결): 재판정 매처를 태깅 매처와 동등한 수준으로 올리는 것. 지금 안전장치는 "눈에 보이면 지우지 않는다"는 **방어**일 뿐, "설명란에만 근거가 있던 234건"처럼 텍스트에 안 보이는 태그는 여전히 재판정으로 복원되지 않는다. 수동 태깅 보호를 정말로 없애려면 이쪽을 먼저 해결해야 한다.
- [완료][artists.json][connections.json] **데이터 정정 3건**(사용자 요청). ①엔믹스 릴리·②도드리 나영주 인스타 삭제 — 둘 다 소속사가 운영하는 "개인 공식" 계정(lily_m_official, yeongjooo_dodree)이라 "멤버 본인 개인 계정만" 원칙에 어긋남. ③**도드리 "라일락" 멤버 삭제** — 사용자 질문("무슨 근거로 들어갔던 거지?")에 대한 답: **멤버가 아니라 곡이다.** 나무위키 "라일락(dodree)" 문서는 2026.06.17 선공개 싱글 **앨범 문서**이고, 도드리 로스터는 나영주·이송현 2명뿐. 옛 나무위키 표 파싱이 디스코그래피 행을 멤버로 오인해 들어온 것으로 보인다. connections.json 2건·정적 페이지(ko/en) 삭제 완료. ⚠️DB에 곡 제목 "라일락"으로 오매칭된 members 13건 + with_members 1건이 남아있어 SQL 필요.
- [완료][index.html] **아이유·보아 커버 탭이 비어 보이던 버그**(사용자 제보 "아이유 커버탭에 다른 아티스트의 커버 안 뜬다. 솔로라 그런가?" — **맞았다**). 무소속 솔로는 카드 키가 _ytGroupKoFor 규칙에 따라 **본인 이름**("아이유")인데, 태깅 쪽은 a.group.ko를 써서 cover_of_members에 "아이유(솔로)"로 저장돼 있었다. 커버 탭은 "아이유(아이유)"로만 조회 → **실측 60건 중 1건만 잡힘**(사실상 빈 탭). 보아는 "보아(보아)" 18건 + "보아(솔로)" 11건으로 **두 표기가 섞여 있어** 어차피 한쪽만으론 부족. 두 키를 모두 조회하도록 수정(_findArtistByConnName이 두 형식을 다 인정하는 것과 같은 방어). 그룹 소속 멤버는 두 키가 같아 쿼리 수 변화 없음(카리나 30건 그대로).
- [완료][admin.js] **음악방송 MC 진행분을 라이브(무대)에서 분리**(사용자 요청 "제목에 mc 있으면 all로"). "MC 컷 모음"·"NEW MC THE SHOW"·"MC석"류는 채널이 음방이라 제목의 방송명(_YT_LIVE_SHOW_RE)에 걸려 전부 라이브로 분류돼 무대 탭을 오염시키고 있었다. ⚠️**MC를 부분 문자열로 잡으면 대참사** — MCOUNTDOWN(엠카운트다운) 13,796건, MCND(엠씨엔디) 271건이 통째로 오분류된다. **단독 토큰**(앞뒤 글자 경계 검사) + 엠씨엔디 명시 제외로 막았고 실측 검증함: MCOUNTDOWN 13,796건 중 걸리는 건 113건(전부 "MC 컷 모음 … #MCOUNTDOWN"처럼 진짜 MC분), MCND는 **0건**. ⚠️또 하나 — 제목에 직캠/팬캠류가 같이 있으면 라이브로 남긴다("MC 정우 서프라이즈 직캠"=굿바이 무대, "MC석 직캠 4K") — 180건이 여기 해당하고, 이 예외가 없으면 진짜 무대 영상이 날아간다. 전수 시뮬레이션: MC 단독 토큰 1,110건 → 930건이 other 대상, 그중 **현재 live인 533건이 실제로 바뀜**(short 90건은 장르가 아니라 포맷이라 유지 — 규칙을 short 판정 뒤에 뒀다). 단위 테스트 12케이스 통과. 기존 행은 **"영상 카테고리 재분류(전체)"** 버튼으로 소급 적용(그 버튼은 이미 tags_manual 보호 + short 제외).
- [완료][artists.json][index.html][정적페이지] **"전 소속 그룹은 있지만 표기는 솔로처럼" — soloDisplay 플래그 + 대표 그룹 수동 지정(repGroup)**(사용자 요청: 우즈·에반). **핵심은 표기와 소속을 분리한 것**: `group.ko`를 '솔로'로 바꾸면 될 것 같지만 **안 된다** — 그 값이 영상 조회 키(`_ytGroupKoFor`: 실존 그룹이면 그룹명, 아니면 본인 이름)라 `group_ko='우즈'`/`'에반'` 채널을 찾게 되는데 DB엔 각각 **0건**(우즈 영상 4건은 전부 `group_ko='엑스원'`, 에반은 363건이 '엔하이픈') → 카드가 통째로 빈다. 그래서 `soloDisplay:true`는 **표기 전용**으로 두고 소속은 그대로 유지한다. 적용 지점: ①연결 카드 — 태그가 "우즈(엑스원)"으로 남아있어도 '솔로' 버킷으로 보내 **그룹 칩 없이 이름만**(사용자 요청 그대로), ②색인 목록·기념일 스트립 그룹 표기 — 전 소속 대신 "솔로"(아이유·보아와 동일한 결과). `group.ko==='솔로'`인 기존 9명도 같은 함수를 타서 표기가 정확히 일치한다. 함께 추가한 `repGroup`은 겸임/전소속이 여러 개일 때 자동 규칙(등장 수→미해체→주 소속)을 **사람이 못박는 탈출구** — 이진혁을 업텐션 대신 엑스원으로 고정하는 식. **에반 이름 정리**: `name.ko`가 "에반(희승)"처럼 괄호를 이름에 박아넣은 형태였는데, 우즈(subName "조승연")와 같은 방식으로 `name.ko="에반"` + `subName="희승"`으로 분리(실측: 카드에 9.5px·투명도 0.42로 작고 흐리게 렌더). ⚠️`name.ko`는 영상 태깅의 정식 키라 **DB 마이그레이션 필요** — `evan_rename_migration.sql`(members 363건 + with_members 2건 + artist_pics). 실행 전엔 에반 카드가 비어 보인다. connections.json 6건·og_thumbs.json 1건·정적 페이지 3,984개 재생성 완료, 옛 URL(`/g/엔하이픈/에반(희승)/`, `/en/g/enhypen/evan-heeseung/`)엔 canonical+meta refresh 리다이렉트만 남김(sitemap에선 이미 빠짐). 게이트: 스모크·인터랙션·모션·절전·공유링크(3,984건)·데이터 전부 통과, 페이지 에러 0.
- [완료][index.html] **연결 카드 — 겸임 멤버가 그룹 칩 두 개로 갈라지던 문제 + 그 밑에 있던 조용한 태그 누락 658건**(사용자 질문에서 출발). ①**제보 확인**: 해찬처럼 엔시티 127+드림 겸임인 멤버는 `with_members`에 **한 영상 안에 두 표기가 동시에** 들어있다(DB 실측 — `["해찬(엔시티 127)","해찬(엔시티 드림)"]`). 전수 스캔: 한 영상에서 같은 이름이 2개 이상 그룹으로 태깅된 건 **194건**(이진혁 28·김요한 26·해찬 3 …). ②**표시만 접는다**: 태그 자체는 두 그룹 카드 어느 쪽에서도 검색돼야 해서 그대로 두고(2026-08월 겸임 자동태깅 수정의 목적), 연결 카드에서만 같은 사람으로 해석되는 이름을 하나로 접었다(`_connRepNames`). 대표 그룹 순서: ①이 카드에서 그 표기로 실제 등장한 영상이 더 많은 쪽(엑스원 시절 콜라보만 있으면 이진혁은 엑스원으로 뜬다) → ②동률이면 해체 안 한 그룹(유연정 주 소속이 데이터상 아이오아이라 그대로 두면 어색) → ③그래도 동률이면 주 소속. 사람이 다르면 안 접힌다(태용(127)+런쥔(드림)은 칩 두 개 유지). 앵커와 같은 그룹이라 접미사 없이 뜨는 이름과의 중복도 같이 정리. ③**파고들다 발견한 더 큰 버그**: `_findArtistByConnName`이 `a.group.ko`(**주 소속만**)로 매칭해서 "해찬(엔시티 드림)" 같은 **부소속 표기는 아무 아티스트로도 해석되지 않았고**, `_apiVidToSong`의 로스터 게이트가 그걸 "로스터에 없는 이름"으로 보고 **태그를 통째로 버리고 있었다** — 즉 콜라보 칩이 아예 안 뜨는 조용한 누락. 전수 실측 **649개 영상 · 658건**(한유진(제로베이스원) 193 · 이한결(BAE173) 49 · 장하오(제로베이스원) 41 …). `_artistGroups(a)`(겸임 포함 실제 소속 목록)까지 인정하도록 넓혀 복구했다. **주 소속을 먼저 찾고 없을 때만** 부소속으로 넓혀서 동명이인 오매칭 위험은 안 키움. 로스터 게이트의 원래 목적("황민현(워너원)"처럼 **이름 표기**가 로스터와 다른 오태깅 차단)은 이름 비교라 그대로 유지된다. ④**실측 검증**(헤드리스): 장용훈(아이딧) 연결 카드 — 원본 이름 목록엔 "해찬(엔시티 127)"·"해찬(엔시티 드림)" 둘 다 있는데 **엔시티 계열 그룹 칩은 1개**, 그리고 리졸버 확장 덕에 예전엔 아예 없던 **우주소녀 칩이 새로 등장**(버려지던 태그가 복구된 증거). 페이지 에러 0, 게이트 6종 전부 통과. 겸임 동시출연 쌍 계산(`_coGroupPairsCache`)은 이름별 등장 횟수 집계와 **한 번의 순회로 합쳐** 카드 열 때 비용은 그대로.
- [완료][index.html] **탐험 피드 — 순위/조회수 부제는 `TOP` 카드에만**(사용자 재요청). "수록곡 무대 모음"·"커버곡 모음"에 여전히 `1위 · 74M+`가 붙던 문제. 원인: 순위 부제를 만드는 코드가 **9곳에 각각 복붙**돼 있었고, 그중 `_renderBsideCard`/`_renderCoverCard`가 누락됐음(둘 다 내부적으론 조회수 정렬로 뽑기 때문에 값 자체는 맞지만, "모음" 카드가 랭킹으로 읽히면 안 된다는 게 요청 취지). 9곳을 공용 헬퍼 `_feedRankSub(label,vc,fallback,forceRank)` 하나로 합치고 **제목에 TOP이 있을 때만** 부제를 만들게 함 — 새 카드를 추가해도 같은 실수가 반복되지 않는다. 예외 1건: "케이팝 레전드 100"은 제목에 TOP이 없지만 진짜 순위표라 `forceRank:true`로 명시적 opt-in(이름 매칭을 넓히는 대신 예외를 눈에 보이게 둠). 죽은 지역 `fmtN` 선언 9개 정리. **실측**(헤드리스, 카드 76장까지 스크롤): 부제에 순위가 붙은 카드 전부가 TOP 카드 + 레전드 100 하나, "모음/모아보기" 카드 28장 중 순위/조회수 붙은 것 0장.
- [완료][kpop_universe.css] **기념일 스트립 간격 조임**(사용자 요청). 그룹명줄↔생년월일줄 3px→**0px**(`.feed-strip-sub+.feed-strip-sub{margin-top:-7px}`로 아이템 gap 7px 상쇄) — 두 줄이 한 덩어리로 읽히게. 이름↔그룹명 3px은 그대로 둬서 "누구"와 "부가정보"의 경계는 유지. 멤버 간 좌우 간격 14→**10px**(실측 확인). 사진 크기는 요청대로 유지.
- [완료][index.html][kpop_universe.css] **그룹 라벨 4종 개선**(사용자 요청). ①**폰트 축소** 12→10.5px(자간도 0.1→0.08em) — 홈에서 이름이 크게 느껴진다는 피드백. ②**지연 등장** — 진입하자마자 뜨지 않고 우주가 먼저 보인 뒤 fade in(실측: 로딩 해제 2.6초 → 3.4초에 완전 등장). `_lblFarStartAt`을 `_revealUniverse()` +1.2초로 잡고 CSS transition은 0.35→0.6초로 완화. ③**로테이션** — 같은 그룹만 계속 떠 있지 않고 번갈아 떴다 사라진다. 그룹 이름 해시로 고정된 위상의 느린 파도(주기 22초)를 점수에 곱하고, 이미 떠 있던 라벨엔 가산점(×1.45)을 줘 경계에서 깜빡이는 걸 막는다. ⚠️처음엔 주기 16초·진폭 0.5~1.5로 잡았다가 **4초마다 전량이 교체돼 화면이 부산했음** → 주기 22초·진폭 0.72~1.28로 완화해 4초에 2~3개씩만 교대하도록 조정(실측 확인). ④**라벨 클릭 가능** — 예전엔 원거리 라벨에 `pointer-events`를 안 줘서 **클릭이 3D 캔버스로 흘러 근처의 다른 행성 카드가 열리곤 했다**(사용자 제보). 이제 텍스트가 명시적 타깃이라 조준이 정확해진다(실측: 라벨 좌표의 최상단 요소가 `group-label`, 클릭 시 정확히 그 그룹이 열림). ⚠️ 이전에 "중간 줌 오클릭 재발 우려"로 일부러 클릭을 막아뒀었는데, 그건 **별/행성 레이캐스트 쪽 문제**라 라벨과는 별개였다 — 오히려 라벨이 정확한 타깃을 제공한다.
- [완료][index.html][kpop_universe.css] **프로필 사진 원 크기 확대 + 연결 즐겨찾기 겹친 원**(사용자 요청). ①탐험 패널 기념일 스트립: 썸네일 60→**76px**, 아이템 폭 66→84px, 이름 9.5→11px, 부제 8.5→9.5px, 아이콘 22→27px. ②프로필 패널 즐겨찾기 아바타: 30→**36px**. ③**연결 즐겨찾기는 앵커 사진 하나만 보여줬는데**, "연결"은 사람과 사람 사이의 일이라 무엇과의 연결인지 안 보였다 → 앵커 + 연결된 멤버 사진을 살짝 겹쳐 표시(`_favAvatarStackEl`). 겹침은 지름의 약 1/3(-12px) — 더 겹치면 얼굴이 가리고 덜 겹치면 목록이 옆으로 길어진다. 최대 3장까지 보이고 그 이상은 `+N` 배지로 접는다(4장부터 얼굴이 너무 작아짐). 각 원엔 배경색 테두리를 둘러 경계를 살리고, 왼쪽이 위로 오게 쌓아 읽는 방향과 맞췄다. 실측: 2인 연결 = 원 2개 60px, 4인 연결 = 원 3개 + "+1" 108px. 사진이 없는 멤버는 기존처럼 그룹 색 원으로 폴백.
- [완료·P1-3][index.html][kpop_universe.css][tests/powersave.test.js] **절전 모드(자동/켬/끔) — P1 완료**(로드맵 P1-3). P0-3에서 만든 모션 줄이기 스위치와 같은 자리의 "강등 레버" 묶음. **기본값 자동** — 유저 토글만 두면 켜야 할 사람일수록 설정을 안 찾기 때문(발열 겪는 라이트 유저가 제일 안 켬). **판정**: 기기 힌트(`deviceMemory<=4` 또는 `hardwareConcurrency<=4`)로 로드 즉시 1차 판단 → 이미 수집 중이던 fps 실측(`avg<45||min<24`)으로 확정. ⚠️ fps를 쓰는 이유가 "발열 측정"이 아님을 코드 주석에 명시 — 렌더가 20~30fps로 묶여 있어 fps는 발열 지표로 약하고(2026-08-26 감사), 여기서는 **"목표 프레임조차 못 내는 기기"** 신호로만 쓴다. 발열 자체는 P0-2 드로우콜 다이어트로 줄였다. **레버 실측**(끔 기준 대비): 픽셀비 2→1.25로 **캔버스 픽셀 61% 감소**(1,316,640→513,785), 프레임 간격 50→100ms(유휴), 원거리 라벨 상한 10→6. 활성 프레임은 15fps까지 내려봤다가 자동회전이 눈에 띄게 끊겨 20fps로 완화. 설정 패널에 세그먼트 컨트롤 + 현재 상태 설명 한 줄("자동 — 지금 절전 중이에요") — 자동이 기본이라 지금 절전 중인지를 글로 알려줘야 상태를 안다. localStorage 유지, 한/영 i18n. ⚠️**구현 중 실제로 낸 사고(재발 방지 테스트 신설)**: 절전 상태 `let _powerSaveOn`을 설정 UI 근처(파일 뒤쪽)에 선언했는데 `animate()`가 로드 직후 즉시 돌면서 `_curRenderInterval()`이 그걸 읽어 **매 프레임 TDZ 예외 → 로딩화면 영구 정지**. ⚠️⚠️ `typeof` 가드로는 못 막는다 — typeof는 "미선언"만 안전하게 넘기고 **TDZ에서는 똑같이 던진다**(가드를 넣어놨기 때문에 오히려 안심하고 있었음). 상태 선언을 animate보다 앞으로 옮겨 해결. `tests/powersave.test.js` 신설 — 선언 순서(`_powerSaveOn` < `animate`)와 레버 연결을 잠근다. 스모크도 이 사고를 잡지만(로딩 해제 실패) 원인은 안 짚어줘서 별도로 둠. 게이트: 스모크·인터랙션·모션·데이터·절전배선 전부 통과, 페이지 에러 0.
- [완료·P1-2][index.html][kpop_universe.css] **라이트박스 하프시트 — 재생 중단 없이 멤버/그룹 카드 보기**(로드맵 P1-2, 사용자 요청 방식 그대로). 재생 화면 상단의 "카리나 · 에스파"가 예전엔 한 덩어리 텍스트라 누를 수 없었다. 이제 멤버/그룹 각각이 버튼이고, 누르면 그 카드가 화면 아래 **52%**로 올라와 다음 영상 목록만 가린다. **설계 원칙 ①**: iframe을 절대 건드리지 않는다(DOM에서 떼거나 부모를 바꾸면 유튜브 임베드가 리셋됨). 새 컨테이너로 카드를 옮기는 대신 **이미 카드가 들어있는 시트를 라이트박스 위로 올리고 높이만 절반으로** 만든다. **설계 원칙 ②**: 카드는 정상 경로(showT/showGC → openMobSheet)로 연다. 임시로 빌려오는 방식은 라이트박스 뒤에 깔린 카드가 비어버리는 상태 충돌이 생긴다. 정상 경로라 **카드 스택·URL·뒤로가기가 평소대로** 동작하고(실측: 스택 `gc:에스파 > tt:카리나`, hash `#g=에스파&m=카리나`), 나중에 "위로 끌어 풀카드 승격"도 기존 시트 제스처에 그대로 얹힌다. 연결 카드와 달리 **일시정지도 하지 않는다** — 라이트박스가 열린 상태 그대로라 3D 렌더 정지 보호막이 유지돼 발열 페널티가 0(로드맵에서 미니플레이어보다 하프시트를 앞세운 이유). 닫기: 손잡이 줄 오른쪽 끝 아래 셰브런(왼쪽은 카드 뒤로가기), 시트 스와이프 다운, ESC. **ESC 계층**을 `_closeTopOverlay` 맨 앞에 끼워 1회는 시트만·2회에 라이트박스가 닫히게 함 — 이게 없으면 데스크톱에서 카드만 내릴 방법이 없었다. **검증**(헤드리스 실측): 탭 전후로 iframe의 부모·src가 완전히 동일(=재생 무중단), 시트 높이 439px/844(52%), 복귀 버튼·ESC 모두 영상 복귀, 라이트박스를 닫으면 하프 상태가 정리되고 카드는 남음, 페이지 에러 0. 데스크톱은 카드가 우측에 영상 위로 뜨고 재생 유지 확인. 게이트 5종(스모크·인터랙션·모션·데이터·공유링크) 전부 통과.
- [완료·P1-1(2차)][groups.json][index.html][tools/bake_group_priority.mjs] **라벨 LOD에 어드민 등급 반영 — 이제 아는 그룹이 뜬다**. 1차에서 뜨던 건 인기 그룹이 아니라 **인원 많은 그룹**이었다(반지름이 멤버 수 기반이라 세븐틴·나인뮤지스·파이브돌스·고스트나인이 뜨고 뉴진스·에스파는 안 뜸). **해결 두 가지**: ①`group_priority`(A 114 / B 140 / C 14)를 `groups.json`의 `pri` 배수로 구움(A×4 · B×1.5 · C×0.6) — 그 테이블은 admin 전용 RLS라 앱이 직접 못 읽어서 굽는 방식(사용자 선택 B안). ②점수 공식에서 **반지름에 제곱근**을 씌워 크기 영향을 최대 1.94배로 압축 — 그래야 A등급 소인원 그룹이 C등급 대인원 그룹을 이긴다. 최종 점수 = √(반지름) × 등급배수 / 거리. **결과**: 세븐틴·스트레이키즈·에이티즈·아이들·스테이씨·트레저·엑소·비투비… 전부 A등급으로 바뀜(파이브돌스·나인뮤지스 사라짐). 겹침 0쌍, 드로우콜 불변, 데이터 오류 0. 갱신은 `node tools/bake_group_priority.mjs` — 스크립트 헤더에 export SQL과 절차를 적어둠. ⚠️**함정 기록**: `groups.json`을 `JSON.stringify(...,1)`로 저장했다가 들여쓰기가 2→1칸으로 바뀌어 **10만 줄 diff**가 났음(원복 후 2칸으로 재작성, 정상 diff 539줄). 데이터 JSON을 프로그램으로 다시 쓸 땐 기존 들여쓰기를 반드시 맞출 것. ⚠️ 표시되는 그룹은 우주 배치가 매 로드 랜덤이라 방문마다 달라진다 — 의도된 동작(발견 철학과도 맞음).
- [완료·P1-1(1차)][index.html] **원거리 그룹 라벨 LOD — 홈 화면에 이름이 뜨기 시작**(로드맵 P1-1). 예전엔 라벨이 거리 40 이내에서만 떴는데 홈은 행성까지 100~300이라 **268개가 전부 이름 없는 구슬**이었다. 지도 앱 방식 도입: 점수 = 화면상 크기(반지름/거리) × 등급 배수로 정렬해 점수 높은 순으로 놓고, 이미 놓인 라벨과 화면 사각형이 겹치면 건너뛴다(그리디 충돌 회피). 상한은 데스크톱 18 / 모바일 10 — 밀도가 아니라 "읽히는 것"이 목적. **검증**: 데스크톱 18개·모바일 10개 표시, **겹침 0쌍**, 줌인 시 52~57개로 자동 증가(근거리는 기존 동작 유지), 드로우콜 370/324로 불변, 페이지 에러 0건. 곁다리 최적화: 라벨 루프가 매 프레임 `bubbleMeshes.find`를 268번 돌던 것(=268² 비교)을 캐시로 제거. 라벨 폭도 요소에 캐시(언어 전환 시 무효화)해 레이아웃 스래싱 방지. ⚠️ **원거리 라벨엔 pointer-events를 주지 않았다** — 행성이 작을 때 조준 대상이 늘면 예전에 고친 "중간 줌 오클릭"(조준 게이트) 계열 문제가 되살아날 수 있어 표시 전용. ⚠️ **남은 문제 — 무엇을 보여줄지**: 점수의 크기 항이 행성 반지름인데 반지름은 **멤버 수**로 정해진다(1.6~4.5). 그래서 지금 뜨는 건 인기 그룹이 아니라 **인원 많은 그룹**이다(세븐틴·워너원·나인뮤지스·파이브돌스·고스트나인… 뉴진스·아이브·에스파는 인원이 적어 안 뜸). 대안 신호를 찾아봤으나 로컬 JSON엔 없음 — `discography`는 268개 전부 있지만 **경력 길이** 지표라(샤이니 62·에프티아일랜드 44 상위, 뉴진스 5·블랙핑크 7 하위) 인기와 무관. 따라서 어드민 큐레이션 등급(A/B/C)이 유일한 올바른 소스. 코드는 이미 `GROUPS[ko].pri` 배수를 읽도록 준비돼 있고(없으면 1), **`group_priority` 테이블을 `groups.json`에 굽는 작업만 남음**(사용자 선택: B안). ⚠️ 그 테이블은 admin 전용 RLS라 익명으로는 0행이라 Claude가 직접 못 읽는다 — 사용자가 admin 세션에서 export해줘야 함.
- [완료·보안][tests/rls.test.js] **RLS 회귀 테스트 신설 + 익명 쓰기 구멍 1건 발견**(사용자 요청 — "관리자 기능이 공개 HTML에 노출됐다"는 외부 지적 검증). **검증 결과: 취약점 아님.** 관리자 패널 마크업이 `index.html`에 있고 `admin.js`도 정적 파일이라 누구나 받을 수 있는 건 맞지만, ①비밀값이 하나도 없고(`service_role`·하드코딩 유튜브 키·시크릿 전부 0건, 유튜브 키는 관리자가 입력창에 직접 타이핑) ②`_isAdmin()`은 UI만 숨기고 진짜 방어선은 서버 RLS인데 그게 서 있음. 실증: 실제 행에 UPDATE·DELETE → 0행 영향(행 그대로), INSERT → `42501 new row violates row-level security policy`. `user_data`(유저 데이터)도 익명 0행. 공개되는 유저 정보는 본인이 정한 닉네임과 UUID뿐이고 이메일 패턴 0건. **발견된 구멍 1건**: `atm_exception_rules`가 **익명 INSERT를 허용**하고 있었음. 자동 태깅 예외 규칙 테이블이라 익명이 규칙을 주입하면 태깅 파이프라인이 오염될 수 있음(읽기·수정·삭제는 막혀 있어 INSERT 전용 노출). 쓰기 지점 8곳이 전부 admin.js에만 있으므로 어드민 전용으로 조여도 기능 영향 없음 → RESTRICTIVE 정책 SQL 전달(사용자가 admin 세션에서 실행). **판별 방법**: 빈 INSERT를 던져 에러 코드를 본다 — `42501`=RLS 차단 / `23502`=NOT NULL 위반(RLS는 통과했다는 뜻) / `55000`=뷰. ⚠️이 판별의 근거는 **PostgreSQL이 RLS WITH CHECK을 NOT NULL보다 먼저 평가**한다는 것이고, `yt_channel_videos`(PK가 NOT NULL인데 42501이 남)로 실측 확인함. 어느 쪽이든 트랜잭션이 중단돼 **데이터는 한 줄도 안 쓰인다**. 테스트는 코드에서 `.from('...')`를 긁어 대상 테이블을 자동 추출하므로 **새 테이블을 쓰기 시작하면 자동으로 검사 대상**이 되고, 기대값(EXPECT)에 없으면 실패시켜 "RLS 켰나?"를 의식적으로 확인하게 만든다. 19개 테이블 검사 통과. ⚠️ 한계: 로그인 안 한 anon 키 기준이라 **유저별 격리(내 데이터만 보이는지)는 검사하지 않음** — 별도 확인 필요. 회사망에서는 `NODE_TLS_REJECT_UNAUTHORIZED=0` 필요.
- [완료·P0-3][index.html][kpop_universe.css][tests/motion.test.js] **모션 줄이기(prefers-reduced-motion) 대응**(로드맵 P0-3). 감사에서 이 미디어 쿼리 대응이 HTML·CSS 통틀어 **0곳**인 게 확인됐는데, 이 앱은 상시 자동회전하는 3D 우주라 전정기관이 민감한 사용자에겐 접근성 이전에 사실상 못 쓰는 화면이었다. **원칙: 기능은 하나도 빼지 않고 움직임의 "연출"만 없앤다** — 카메라 비행은 즉시 도착, 리플/펄스는 생략, 자동회전은 정지, 도착 상태는 동일. 적용: `_reduceMotion` 플래그 신설(OS 설정을 도중에 바꿔도 반영되는 change 리스너 포함) → `controls.autoRotate` · `flyState.dur`(0으로 두면 0/0=NaN이라 1ms) · 링 `rotateZ` · `pulseGroup/pulseMember/pulseGroupRipple` · 생일 별똥별. CSS는 **끝없이 반복되는 장식성 애니메이션만** 끔(뱃지 펄스·프레즌스 점·추모 반짝임·제목 자동 마퀴·스와이프 힌트). ⚠️**일부러 남긴 것 둘**: ①로딩 스피너·시머 — 끄면 "멈춘 화면"으로 보여 사용성이 오히려 나빠짐 ②`transition` 전반 — 이 앱은 시트/카드 정리를 `transitionend` 타이밍에 의존하는 곳이 있어서 일괄 `none`을 주면 카드가 중간 상태로 고착될 수 있음(과거 시트 높이 고착 버그가 반복됐던 지점). **검증**(헤드리스, 켬/끔 양쪽): 켬 → 3초간 카메라 이동거리 **0.0000**, 비행 dur=1, 장식 애니메이션 전부 none, **그룹 카드 정상 오픈**. 끔 → 이동거리 1.99, dur=1000, 애니메이션 정상. 양쪽 페이지 에러 0건. 이 플래그는 앞으로 절전 모드(P1-3, 자동/켬/끔)가 그대로 재사용한다 — 강등 레버를 한 곳에 모으는 게 목적. **`tests/motion.test.js` 신설** — 단 이건 동작 테스트가 아니라 **배선 테스트**다(가드가 리팩터링 중 조용히 빠지는 것만 막음). 접근성 기능은 화면에 티가 안 나서 한번 빠지면 몇 달을 모른 채 갈 수 있어서 잠가둠. 게이트: 스모크 · 데이터 오류 0 · 인터랙션 12건 · 배선 13건 전부 통과.
- [완료·P0-2][index.html] **드로우콜 다이어트 — 홈 화면 2,033 → 370 (82% 감소)**(로드맵 P0-2). 서바이벌 출신 39그룹의 장식 링을 각각 45겹의 별도 Mesh로 그리고 있어서, 홈에서 실제로 그려지는 메시 2,023개의 **87%(1,755개)**가 이 링이었다 — 눈에 보이는 건 링 39개인데 GPU엔 1,755번을 부탁하던 셈. 45겹을 그룹당 하나의 지오메트리로 병합하고, 겹마다 다르던 투명도는 정점 속성(`aRingAlpha`)으로 구웠다(three r128의 vertexColors는 vec3라 알파를 못 실어서, 별 재질이 flareSeed를 넘기는 것과 같은 onBeforeCompile 방식 사용). 정점 순서를 그대로 유지해 겹침 구간 블렌딩 순서까지 동일. **검증**: 링 하나만 남기고 회전각·카메라를 고정해 병합 전/후 픽셀 비교 → **최대 차이 0, Δ>2 픽셀 0개(비트 단위 동일)**, 드로우콜 45→1, 삼각형 12,870 동일. ⚠️**중간에 두 번 헛발질했고 둘 다 계측이 잡아냈다**: ①시드 고정 PRNG로 전체 화면을 비교했더니 9%가 달랐는데, 원인은 three.js가 객체 UUID를 `Math.random`으로 만들어서 **객체 수가 다르면 시드를 고정해도 배치가 갈리는 것**이었음 ②링만 격리했더니 여전히 54%가 달랐는데, 링이 매 프레임 `rotateZ`로 돌아가서 **두 버전의 프레임 수 차이만큼 회전각이 어긋난 것**이었음. 둘 다 배제하니 완전 일치. **되돌린 시도**: 원거리 행성을 `DoubleSide`→`FrontSide`로 컬링하는 최적화는 **화면의 4.06%가 달라지고 최대 밝기차 210**이라 폐기 — 이 행성은 투명해서 뒷면(먼 반구)이 앞면 너머로 실제로 비쳐 보이고 셰이더가 `gl_FrontFacing`으로 그걸 정상 처리하고 있다. 즉 뒷면은 장식이 아니라 그림의 일부. 덤으로 **"양면이라 삼각형이 2배"라는 내 앞선 추측도 틀렸음**을 확인 — `renderer.info.triangles`는 지오메트리 삼각형을 세지 래스터화된 면을 세지 않는다. 그래서 로드맵의 "삼각형 97만→50만" 수용 기준은 근거가 잘못됐고, P0-2의 실제 성과는 드로우콜 82% 감소다. **회귀**: `baseOps`를 읽는 소비처가 매 프레임 루프 말고도 둘 더 있었고(`_setNctRingScale` · 즐겨찾기 dim) 거기서 null 참조로 터졌는데 **인터랙션 테스트가 잡아냄** → 세 곳 모두 merged 분기 추가. **NCT 패밀리 링은 여전히 비활성**(필터 `ko.startsWith('NCT')` vs 한글 키) — 지금 고치면 링이 새로 생기는 시각 변경이라 성능 작업과 분리, 주석으로 명시. 게이트: 스모크 · 데이터 오류 0 · 인터랙션 12건 · 네비게이션 · 검은 점 0건 전부 통과.
- [완료][index.html][build_group_pages.js][tools/build_og_thumbs.mjs][tests/seo-url.test.js] **공유 링크 미리보기 수정 — 네이버 블로그에 이미지가 안 뜨던 문제**(사용자 제보). **원인 ①**: 공유 버튼이 앱 딥링크(`/#g=에스파&m=카리나`)를 내보냈는데 **URL의 # 뒤는 서버로 전송되지 않는다** — 네이버·카카오·트위터 크롤러가 전부 루트 페이지만 읽어서 무엇을 공유하든 미리보기가 똑같았다. 정적 SEO 페이지 URL(`/g/에스파/카리나/`, 퍼센트 인코딩)을 내보내도록 변경. 경로 규칙이 index.html과 build_group_pages.js 두 곳에 중복되므로 **`tests/seo-url.test.js` 신설** — index.html에서 헬퍼를 추출해 실제 생성된 디렉터리와 전수 대조(ko/en 3,984건 통과). **원인 ②(더 큼)**: 정적 페이지의 og:image가 `songs[0]`에서만 나오는데 그 필드가 그룹 14개·아티스트 56명에만 있어서 **그룹 페이지 96.3%·멤버 페이지 97.0%가 전부 같은 일반 og-image.png로 폴백**하고 있었다(영상은 전부 Supabase에 있고 JSON엔 없음). `tools/build_og_thumbs.mjs` 신설 — 그룹당 1회 조회(268회)로 그룹·멤버 대표 영상을 함께 뽑고, maxresdefault 존재를 HEAD로 확인해 `og_thumbs.json`에 캐시. 빌드는 이 캐시만 읽으므로 SEO 리빌드 Action이 DB에 의존하지 않는다(캐시 없으면 기존 동작으로 폴백). **결과**: 일반 이미지 폴백률 그룹 96.3%→**1.1%**, 멤버 97.0%→**5.5%**. 수집 커버리지 그룹 265/268·멤버 1,631/1,724, 그중 **96%가 maxresdefault(1280×720)**. `og:image:width/height` 메타도 추가. ⚠️ 회사망에서 로컬 실행 시 MITM 인증서 때문에 node fetch가 전부 실패한다 — `NODE_TLS_REJECT_UNAUTHORIZED=0`을 앞에 붙여 실행할 것(스크립트에는 박아넣지 않음). 다음 단계로 자체 브랜드 카드 이미지 생성(1200×630)을 검토하기로 함(사용자 선택: 1번 먼저, 나중에 2번 확장).
- [완료·P0-1][index.html][kpop_universe.css] **카드 네비게이션 스택 감사 + 뒤로가기 통일**(로드맵 P0-1). 사용자 제보 "이전 레이어가 잘 안 쌓이는 느낌"의 실체를 헤드리스 조작 실측으로 특정하고 5건 수정. **감사로 드러난 구조**: 스택이 하나가 아니라 셋(`_cardStack` 모바일전용 / `_memberNavStack` 연결카드경유 / 브라우저 history)이 서로 동기화 없이 돌고 있었음. **①이중 푸시(10곳)** — `showT`/`showGC`는 모바일에서 이미 `openMobSheet`를 부르는데 호출부가 한 번 더 불러서 같은 카드가 스택에 2번 들어감(탐험 피드·가족관계·프로듀서·발견 토스트 등). 한 겹만 열었는데 뒤로가기 버튼이 뜨고 눌러도 화면이 안 바뀌던 정체. 올바른 패턴 3곳(`_navHistoryActive`로 감싼 것)은 그대로 유지. **②히스토리 항목 2배 적립** — `_syncUrl`의 중복방지 검사가 `location.hash===hash`인데 `_hashEsc`는 한글을 raw로 두고 `location.hash`는 퍼센트 인코딩으로 돌아와서 매번 "다름" 판정 → 카드 하나 열 때 항목 2개(2→4→6). 뒤로가기를 두 번 눌러야 한 겹 벗겨지던 원인. `_normHash()`로 양쪽 디코드 비교. **③브라우저 뒤로가기가 팝이 아니라 푸시** — popstate가 무조건 `_openFromHash()`로 다시 열어서 2겹에서 뒤로 누르면 3겹이 됨(이미 밑에 있는 카드를 위에 또 얹음), 그다음 카드 뒤로가기가 오히려 "앞으로" 가는 것처럼 보였음. `_stackIndexForHash()` 신설 → 대상이 스택 안에 있으면 그 깊이까지 팝. **④URL·화면 불일치** — `showGC`가 `_openTArtist`를 안 지워서 `_cardHash()`가 그룹 카드 위에서도 멤버 해시를 뱉음. 모바일은 스택 최상단을 진실로 삼도록 변경 + 팝 시 replace 동기화(`_popMobCard` 래퍼 신설, 실제 로직은 `_popMobCardInner`). URL 동기화 지점을 스택 변경 직후(`openMobSheet` 푸시)로 옮김 — `_navHistoryActive` 경로에선 showX의 동기화가 푸시보다 먼저 일어나 m 파라미터가 안 붙던 문제도 같이 해소. **⑤뒤로가기 버튼 위치 통일** — 카드 안(제목 옆)에서 **시트 손잡이 줄 왼쪽 끝**으로 이동, 탭 영역 44×44(HIG 최소치), `.sheet-back-btn`. 카드(#tt/#gc)는 mob-sheet↔mob-card-stack을 오가지만 손잡이 줄은 컨테이너 고정이라 어떤 카드가 올라와도 위치가 같음. 기존 `tt-back`/`gc-back` 핸들러를 클릭 위임으로 재사용(연결 시트 닫기·세대 목록 복귀 같은 특수 케이스 보존), 모바일에선 카드 안 버튼은 숨김. **곁다리**: `pushMobNav` 인자가 두 함수 모두 본문에서 안 읽히는 죽은 인자였음 → 위치 인자라 삭제하면 뒤의 `noFly`가 밀리므로 `_unusedPushMobNav`로 개명 + 주석. `showT`의 4번째는 `keepNavStack`, `showGC`의 4번째는 `keepMemberPanel`로 의미가 달라 혼동 주의(주석 추가). **검증**: 3겹 스택에서 뒤로 1·2회가 정확히 한 겹씩 팝 + 앞으로 가기도 복원, 히스토리 항목 카드당 +1, 버튼 실좌표 탭 동작, 배포게이트 3종(모바일 스모크 통과 / 데이터 오류 0 / 인터랙션 테스트 12건 통과), 검은 점 회귀 0건. **미해결(별도 과제)**: 데스크톱은 카드 스택 개념 자체가 없어 그룹/멤버 카드에 뒤로가기가 없음 — 이번엔 브라우저 뒤로가기 정상화까지만(사용자 결정).
- [완료·문서][로드맵] **완성도 로드맵 수립 — 실측 진단 + 9개 앱 벤치마크 + P0~P3 우선순위**(사용자 요청, Fable 자문 대화 반영). 문서: https://claude.ai/code/artifact/db08ef53-031c-4cbe-aaac-186003e1b9ef **확정 순서(2026-08-26, 사용자 선택 "안정성·완성도")**: P0 뒤로가기 통일+네비스택 감사 / 드로우콜 다이어트 / 모션 줄이기 대응 → P1 줌 레벨별 그룹 라벨 / 라이트박스 하프시트 / 절전 모드 → P2 이어보기·본것 / 최애 새영상 뱃지 / 대기열→미니플레이어 → P3 우주 리포트 / 우주가 시간에 반응 / 키보드 접근성. **실측 근거**(헤드리스, renderer.info): 홈 드로우콜 2,033·삼각형 97만인데 보이는 메시 2,023개 중 **1,755개(87%)가 프로젝트 링 스트로크**(39그룹×45겹) → 그룹당 1개로 병합하면 1,755→39. 행성 줌인 시 드로우콜 15로 급락 = **가장 비싼 화면은 홈**. ⚠️**Fable 전제 2건 정정**: ①"fps 계측 미구현"은 틀림 — 이미 있음(index.html:6326~ perf_fps/perf_lcp GA 전송), 측정만 하고 반응을 안 할 뿐 ②**fps는 발열 지표로 약함** — 렌더가 이미 20~30fps로 묶여 있어 뜨거워져도 fps는 멀쩡해 보임, 발열은 프레임당 비용이라 드로우콜 다이어트가 절전 모드보다 먼저. **누락 실측**: 이어보기/시청기록·대기열/전체재생·알림/구독·prefers-reduced-motion·:focus-visible·aria-live 전부 0건. **이미 잘된 것**: 그룹별 og:image가 대표영상 썸네일로 분기됨, 검색은 초성(_csScore)·altNames·로마자 정규화까지 있음.
- [버그·미수정][index.html] **NCT 패밀리 링이 한 번도 렌더된 적 없음** — 필터가 `bubbleMeshes.filter(b=>b.ko.startsWith('NCT'))`인데 실제 그룹 키는 `엔시티 127`/`엔시티 드림`/`엔시티 위시`라 매치 0건(groups.json 실측). 그룹 키 한글화 시점에 죽은 것으로 추정. 위 P0 드로우콜 다이어트 때 같이 처리(고치거나 걷어내기).
- [완료][index.html] **"행성 선택 시 배경 별이 세포처럼 까맣게" 반복 재발 버그 — 근본 원인 규명 + 수정**(2026-08-25 [이슈] 항목 해소). **원인**: 멤버 별 재질(`makePointsMat`)이 `AdditiveBlending`인데, 별을 흐리게 하는 코드(`animate` 안 `ARTISTS.forEach`, 대략 index.html:2901~)는 **알파가 아니라 정점 색을 검정 쪽으로 곱해서** dim한다(`attr.array[i]=(…)*fa`). 가산 블렌딩의 알파는 `dstAlpha=ONE`으로 누적돼 점 중심이 항상 알파 255가 되므로, **색만 0에 수렴하고 알파는 꽉 찬 "불투명 검정 원"**이 되어 투명 캔버스 뒤의 CSS 배경(navy `#0d0c20`)을 가려버림 → 배경보다 어두운 검은 점. 행성을 선택하면 다른 그룹 별의 `_focusAlpha`가 떨어져 이게 한꺼번에 드러났음. **수정**: 색은 기존 가산 그대로(`SrcAlpha`,`One`) 두고 **알파만 쓰지 않게**(`blendSrcAlpha:Zero`,`blendDstAlpha:One`) `CustomBlending`으로 분리 — 발광 레이어가 배경을 가리지 않으므로 어두운 별은 검게 덮는 대신 그냥 안 보이게 된다. ⚠️**옛 depth-occlusion 이론(index.html:1210 주석)은 무관함이 실측으로 확인됨** — 가시 재질은 전부 `depthWrite:false`이고 행성 셰이더의 `depthTest`를 꺼도 검은 점 개수 불변. `uNoise`/`uGrain`/`uVirtual`/`uTime`/`uDisbanded`/`uFilterOp` 토글도 전부 무영향(그레인 셰이더 가설도 기각). **검증**(헤드리스 크로미움 + SwiftShader, 스크린샷 픽셀 실측): ①검은 점의 캔버스 픽셀 = `rgba(5,4,6,255)`(알파 255 불투명 검정) ②가산 재질 전수 개별 토글 → 멤버 별 Points 단독으로 검은 픽셀 118→0 ③검은 점 좌표에서 3.1px 거리에 별이 있고 그 정점색이 `rgb(4,3,5)` ④같은 카메라에서 선택만 해제하면 3세션 모두 0개(선택 시 43~128개) ⑤수정 후 4세션 A/B 전부 0개(되돌리면 11~133개), 밝은 픽셀·평균 밝기는 오히려 미세 상승(어두운 별이 배경을 덮는 대신 빛을 더하게 되어 정상화), 페이지 에러 없음, 홈 화면(선택 없음)은 수정 전후 픽셀 동일. **남은 관찰**: 헤일로 스프라이트(`SpriteMaterial` + 가산)도 같은 구조적 취약점을 갖고 있으나 이번 건의 원인은 아니었음(개별 토글 시 1~2px 변동) — 유사 증상 재발 시 여기부터 볼 것.

---

## 2026-08-25 (집 세션 — 오태깅 뿌리뽑기)
- [완료][index.html] **탐험 Trend 카드 둘째줄 수정** — TOP 모음도 아닌 발견용(셔플·비랭킹) 카드인데 "1위·조회수"가 떠서, 대표 영상 제목으로 변경(카드1·카드3과 동일 패턴). 다른 rank-sub 피드 카드는 전부 제목에 TOP이 있어 정당함 확인.
- [이슈·내일 실기기 확인 필요][index.html] **행성 선택 시 배경 별이 "검은 점"으로 보임(반복 재발)** — 사용자가 받은 카드 이미지(`~/Downloads/kpop-universe-anniversary.png`)를 클로드가 직접 열어 확인: 밝은 행성 글로우/헤일로 위에 작고 어두운 점 다수. 사용자 단서 "다른 행성 선택 시 배경 별이 세포처럼 까맣게". ⚠️**핵심**: 별·행성버블(fillMat 2086)·헤일로·라인 **모든 재질이 이미 `depthWrite:false`** 라 옛 주석(index.html:1210)의 depth-occlusion 이론으론 설명 안 됨 → blending/텍스처/렌더순서 상호작용일 가능성이 큼. **내일 브라우저 devtools에서 확정**: `starGroup.visible=false` 토글로 별이 원인인지 격리 → 맞으면 bg star `PointsMaterial`의 blending을 바꿔보며(Normal↔Additive) 관찰. **fix 후보**: ①효과 스프라이트(index.html 6569·6620·6651·6695·6755)가 `depthTest:false`만 있고 `depthWrite:false`가 없음 — 추가는 안전하나 이들은 pulse/ripple의 **일시적** 스프라이트라 "선택 중 지속되는" 검은점의 주원인은 아닐 수 있음 ②bg star가 밝은 글로우 위에서 어둡게 보이는 blending 문제 → additive로 바꾸면 사라질 가능성 ③선택 시 배경 별 opacity를 낮춰 회피. **시각효과라 반드시 실기기 확인 후 적용**([[feedback_realdevice_visual_check]]) — 블라인드 수정 금지.
- [이슈·데이터][groups.json] **디스코그래피 트랙 누락 — 11개 앨범 / 6그룹** — 정규·미니인데 타이틀 곡 1~2개만 저장돼, 앨범 커버 클릭 시 트랙이 하나만 보임. **엔시티 드림 6개**(Go Back To The Future 2곡·DREAMSCAPE 1·DREAM( )SCAPE 1·Candy 1·Beatbox 1·Hello Future 1) + 오마이걸·신화·god·NRG·쥬얼리 각 1. (ISTJ 10·Glitch Mode 11 등은 정상). **fix**: 멜론 재수집(`tools/group_disco_fill.mjs` 계열 — ⚠️멜론 차단 시 406에 정상크기 HTML 주므로 상태코드 확인 필수). 소수라 타깃 처리 가능, 다른 그룹으로 확산 여부도 같이 점검.
- [완료][admin.js][shared.js][groups.json][tests/matching.test.js] **오태깅 매처 폴루터 6종 근절 + 날짜게이트**(사용자 제보 연쇄). 그룹 영상이 동명/흔한단어 멤버·그룹에 대량으로 빨려가던 계열을 뿌리째. ①**유사랑→"사랑"**: `memberHit` 흔한단어 게이트가 풀네임만 봐서 성 뗀 변형("사랑"=love)이 통과 → 변형마다 게이트 적용(이즈나 group_ko 오염 실측 385건). ②**"마이"(이즈나 Mai=My)** `_ATM_COMMON_KO_WORDS` 추가. ③**M&N 유닛**: `&`가 'M N'으로 정규화돼 "A.M.N Showcase"에 걸려 브아걸 미료/나르샤로 → `_UNIT_HASHTAG_ONLY_TOKENS`. ④**스텔라(하츠투하츠 멤버 ↔ Stellar 그룹)**: `_atmNameIsGroup`의 strictSync 제외 제거 + strictSync 자체 해제 + 영문 'STELLAR' 해시태그게이트 + `_GROUP_TITLE_CONFLICT_EXCLUDE['스텔라']=하츠투하츠` + **이름재사용 날짜게이트**(`_GROUP_DISBAND_REUSE_CUTOFF{스텔라:2024-06}` — 그룹 Stellar 2018해체 ↔ 하츠 멤버 스텔라 2025데뷔를 **업로드 날짜로 구분**). ⑤**세이마이네임(Say My Name 가사)**·⑥**NATURE(네이처, NATURE REPUBLIC 브랜드)**: 영문 토큰 해시태그전용(`_GROUP_TOKEN_HASHTAG_ONLY`) 신설 — 한글 토큰은 평문 유지라 정당 영상 안 끊김. 회귀 테스트 다수(44/44). **strictSync 22개 전수 감사**: 스텔라만 "이름 안 흔한데 갇힌" 예외라 해제, 나머지 4개(슈가·스피드·배틀·남녀공학)는 제목이 전부 단어 의미라 복구할 게 없어 유지가 정답.
- [완료][scratchpad][DB read] **전수 진단 리포트(B)** — 고친 엔진을 DB 372,393행에 돌려 "저장 태그 ≠ 엔진" 오염을 전수 대조(읽기전용 스크립트, matching.test.js 하네스 재사용). MIS_CLEAN(재배정후보)·ORPHAN(무관후보) 분류 → 대부분이 오염이 아니라 **옛 버그로 엉뚱한 그룹에 저장된 직캠의 정당한 복구**로 판명(하이키·체리블렛 등). ⚠️함정: ORPHAN 대부분은 오너채널 정상 브이로그(출처 컬럼 없어 구분 불가) → 자동청소 불가 확인.
- [완료·스냅샷필요][admin.js][index.html] **"🧭 오태깅 그룹 재배정" 버튼** — 고친 엔진 재적용해 "저장 그룹은 제목에 근거 없고 제목엔 다른 그룹이 literal"인 행을 재배정. 드라이런(건수 confirm)+콜라보(원문 with/선배/feat)·수동태그 제외+스냅샷 되돌리기+표본 콘솔. 엔진 재사용이라 폴루터 고칠수록 자동으로 정확(재실행형). ⚠️**오너채널 맹점**: owner 문맥 없이 돌아 '혜리's club'(걸스데이) 같은 오너 영상을 오추론할 수 있음 → 출처 컬럼(#3)으로 해결 예정. 프리뷰 실측 ~2,993건.
- [완료][index.html] **관리자 태깅 버튼 상황별 4묶음 재배치** — 매일루틴 / 매칭로직 고친 날(①재태깅→②오태깅재배정) / 커버곡 정리 / 청소. 핸들러·ID 그대로, group_ko 바꾸는 버튼엔 '(그룹 변경)' 표시. **모순 없음 확인**: group_ko 변경 버튼(4·🧭)과 멤버/콜라보만 손대는 버튼(2·재태깅·3·자체멤버)이 분리돼 어떤 순서로 눌러도 수렴.
- [완료·SQL실행됨][admin_bulk_snapshots] **되돌리기(undo) 활성화** — 이미 있던 `admin_bulk_snapshots_migration.sql`을 실행. 이제 스윕/재배정 버튼이 실행 직전 자동 스냅샷 → "↩︎ 마지막 일괄 작업 되돌리기"로 복구.
- [완료·SQL실행됨·토대][admin.js][source_channel_migration.sql] **출처 채널 컬럼(#3) — 동기화가 출처 기록 시작**. 오태깅 상당수의 근본원인 = 영상이 어느 채널서 왔는지 미저장(혜리's club→오너 무시, 음악 서바이벌 오유입). `source_handle`·`source_tier` 컬럼 추가(SQL 실행됨) + `_ytUpsertVideos` 래퍼(컬럼 없으면 자동 폴백해 sync 안 깨짐) + 외부채널=tier·handle / 공식채널=official 기록. **앞으로 들어오는 영상부터** 출처 찍힘.
- [완료][admin.js][index.html] **영상 관리 패널 "🆕 새로 들어온" 탭 + 홈 카드 클릭 진입** — 신규분(created_at>기준선)을 최신순으로, 기존 편집·일괄 무관 UI 재사용. 그때그때 품질관리 동선(홈 숫자→클릭→검토→편집).
- [완료][admin.js] **관리자 홈 "마지막 루틴 실행" 기기간 공유** — localStorage만 써서 회사 PC 실행이 집 PC엔 "기록 없음"이던 것 → `atm_exception_rules`(type=admin_meta) 재사용해 공용 DB에도 기록(새 테이블 불필요). 로컬은 즉시표시 폴백.
- [완료][artists.json][index.html][kpop_universe.css][tests] **탈퇴/고인 멤버 표기 + 데이터 반영**. ①탈퇴 멤버 그룹칩 옆 **활동 종료 시점(~YYYY.MM)** 표기(그룹별 left 우선, 합류일 채우면 기간 전체로 확장). ②고인은 나이/사망일 빼고 **생년월일만**(사망은 이름 옆 별로), **사망 이탈은 탈퇴 표기 안 함**(종현·문빈·구하라·리세·은비 / 설리는 생전탈퇴라 유지), 생년월일 칩 살짝 어둡게. ③**레이디스 코드 리세·은비 사망일**(2014.09.07/09.03, 나무위키). ④**BAE173 전 멤버 정리**(한결=이한결·도현=남도현/toni rei·도하=나규민 신규) + 영서 인스타. ⑤**아이칠린 전 멤버 소희(엄소희) 추가** + 현 멤버 인스타 6명. ⑥고스트나인 이신 인스타.
- [완료][admin.js] **백필 cover_of NOT NULL 위반 수정** — `_extBuildRows`가 커버 정보를 조건부로만 넣어, 200개 배치에 커버 영상 섞이면 키 없는 일반행이 null로 저장되며 배치 전체 실패(M2·뮤직뱅크·쇼챔피언). 두 칸 항상 [] 이상으로.
- [완료(진단만)] **GA infscroll 오염(releaseLock/scroll이 utm_source로)** — 이미 2026-08-23에 파라미터 source→trigger로 고쳐져 배포됨 확인. 측정ID G-Z5NTV6X3YF 일치. 지금 보이는 건 8/23 이전 과거 데이터.
- [진행중] **#3 백필 — 기존 372k에 출처 소급**(다음 세션). 기존 행은 source=None이라 버튼/청소의 오너채널 보호가 아직 안 먹음. 채널별로 영상목록 재fetch 필요(대형 채널 쿼터 큼) → **전량 말고 오너채널·서바이벌 채널만 타깃 백필** 권장. anon 쓰기 불가라 관리자 버튼으로 만들어 사용자가 실행하는 구조. 그 뒤 재배정 버튼이 출처 인식하도록 업그레이드하면 오너오작동·서바이벌 오유입이 클래스째 해결.

## 2026-08-25
- [완료][artists.json][tests/matching.test.js][PRINCIPLES.md] **다이아 전 멤버 3명 반영 + "활동명이 유명인과 겹칠 때" 규칙 명문화**(사용자 요청). 나무위키 DIA(아이돌) 전 멤버 표 기준 4명 중 **3명 반영**: 승희(=조승희, 파이브돌스 겸임 · 2015.09.14~2016.04.12) · **이소율**(활동명 제니 · 2015.09.14~2019.07.06) · **안솜이**(활동명 솜이 · 2017.04.19~2022.01.09). ⚠️**활동명이 아니라 본명으로 등록하고 활동명은 `matchAliases`에 둔 이유**: `name.ko="제니"`로 넣으면 블랙핑크 제니와 같은 이름이 되어 `_m2ParseTitle`의 동명이인 dedup이 **"그룹명 없이 제니만 있는 제목"의 역추론을 양쪽 다 버린다** — 즉 이 사람을 추가하는 행위 자체가 블랙핑크 쪽 태깅을 죽인다. `matchAliases`는 **그룹이 확정된 뒤 멤버 추출에만 단어경계로** 쓰이고 `_m2NameVariants`(역추론이 보는 것)엔 안 들어가므로, 다이아 시절 영상("다이아 제니 직캠")은 그대로 잡으면서 블랙핑크는 보존된다. 기존 권나연/나나·장은성/도시와 같은 패턴. ⚠️**승희는 파이브돌스 쪽 `active`를 건드리지 않았음** — 그 그룹은 2015.03.10에 **해체**했고 해체 그룹은 멤버 전원 active가 관례라, 혼자만 `active:false`로 두면 그 사람만 "전 멤버"로 빠져 어긋난다(탈퇴와 해체는 다름). 다이아 쪽만 `active:false`+`left`. 🚫**은진(1997.08.31, 2015.09.14~2018.05.07)은 의도적으로 제외** — 나무위키에 "2026년 5월 26일에 은진이 나무위키 잊혀질 권리를 요청하여 완전히 방송계를 떠났다"고 기록돼 개인 문서가 실제로 삭제된 상태라, 생일·사진·SNS를 실은 프로필을 새로 만드는 건 본인 의사에 반한다고 판단(사용자에게 "이름만 있는 최소 항목으로 넣을지" 물어둔 상태 — **미결**). 검증: 매칭 테스트 3종 추가(별칭 매칭 / **블랙핑크 역추론 회귀 방지** / 탈퇴 게이트) 32/32, 스모크·인터랙션·validate-data 통과 + 브라우저 실측(현 6명 / 전 3명). PRINCIPLES.md "새 그룹/멤버 추가 절차"에 위 두 규칙(활동명 충돌 → 본명+matchAliases, 탈퇴 표기 → 지우지 말고 active:false+left, 단 해체는 예외) 추가. ⚠️솜이 활동 종료일은 자료마다 다름 — 그룹 문서 표 `2022.01.09`(공식 탈퇴 발표) vs 본인 문서 `2019.12.14`(실질 활동 중단), 공식 날짜 채택.
- [완료][index.html] **커버 탭 제외에 `커버스타`·`커버 스타` 추가**(사용자 요청). 2026-08-24에 넣은 건 영문 `coverstar`뿐이라 **한글 표기 제목은 그대로 커버 탭에 노출되고 있었음**. `_COVER_EXCLUDE` 상수 한 곳만 고치면 대표영상·목록·태그모달 세 경로에 다 먹는 구조는 유지됨(2026-08-25 통합분).
- [완료][index.html][kpop_universe.css] **사진 출처 URL 입력창이 선택사항임을 명시**(사용자 질문 "입력 안 하고 넘길 수도 있나?"). 인스타도 위버스도 없는 멤버는 **취소를 누르거나 빈 칸으로 저장하면 이미 넘어가고**(사진은 뜨되 클릭만 안 됨, 링크 우선순위는 `인스타 > source_url > 없음`이라 나무위키 폴백 같은 건 없음), 동작은 원래 맞았는데 **창 문구가 그걸 전혀 안 알려줘서 필수처럼 보였던 것**이 문제. `_promptDialog`에 선택적 `hint` 파라미터 + `.pp-hint` 줄 추가(제목보다 한 톤 낮춘 색·굵기, `white-space:pre-line`으로 줄바꿈 유지). hint 없이 부르는 기존 호출은 `display:none`이라 영향 없음. 헤드리스로 취소·빈값 모두 `null`로 오는 것과 모바일 390px 잘림 없음 확인.
- [완료·SQL실행됨][admin.js][index.html][kpop_universe.css] **검수(ss) 탭 — 처리한 영상이 다시 올라오던 버그 + "확인" 상태 신설**(사용자 제보 "무관·보류 처리되면 목록에서 빠져야 하지 않나?"). ①**버그**: 처리한 그 자리에선 목록에서 빠지는데(`_vmRows` 필터), **조회 쿼리가 `tags_manual=false`만 걸고 이미 플래그가 붙은 행을 제외하지 않아** 탭을 다시 열면 그대로 다시 올라왔음 → "검수 대상 N개"가 아무리 처리해도 안 줄어드는 것처럼 보임. `.is('content_flag',null)` 추가. ②**"확인" 상태 신설**(사용자 질문 "정상·무관·보류와 별개로 확인 처리 같은 걸 만들어야 하나?" → 맞음): 검수 대상 12,113건의 **대부분은 태그가 이미 맞는 정상 행**인데 `정상 = content_flag 없음`이라 **"봤고 맞음"과 "아직 안 봄"이 DB상 완전히 같은 상태**였음 — 무관도 보류도 숨김도 아니니 표시할 방법이 아예 없어 큐가 구조적으로 절대 안 줄어드는 상태였다. → **`reviewed_at` 컬럼 + 초록색 "선택-확인" 버튼**(ss 탭 전용, 스냅샷 + 200건 청크 + 목록에서 제거). ⚠️**`tags_manual=true`를 이 용도로 쓰지 않은 이유**: 그건 "관리자가 태그를 직접 고쳤으니 자동 태깅이 건드리지 말 것"이라는 전역 원칙의 표식이라([[feedback_never_overwrite_manual_tags]]), 눈으로 훑어 통과시킨 1만여 건에 찍으면 **앞으로의 매처 개선이 그 행들엔 영원히 반영되지 않음**. "확인"은 큐에서만 빼고 노출·자동 태깅은 그대로 둔다. ③`reviewed_at`을 `_BULK_SNAP_COLS`에 포함해 다른 일괄 작업과 똑같이 되돌리기 가능. ⚠️**컬럼 추가 SQL보다 코드가 먼저 배포되는 창을 대비해 폴백 2곳** — 검수 탭 조회와 **스냅샷 생성**(이건 모든 일괄 작업의 전제라 여기서 400이 나면 일괄 기능이 통째로 막힘) 양쪽에서 `reviewed_at` 에러면 컬럼을 빼고 1회 재시도. SQL 실행 완료(컬럼 + `(group_ko,id) WHERE tags_manual=false AND content_flag IS NULL AND reviewed_at IS NULL` 부분 인덱스). 검증: 스모크·인터랙션·매칭(29/29)·validate-data 통과.
- [완료][artists.json][tools/] **솔로 디스코 배치 16명(300장) + 같은 앨범 중복 20장 정리 + 수지 이두나! OST 재추가**. ①**16명 완료**: 솔라(4→27)·휘인(11→27)·태연(10→42)·온유(6→21)·태민(9→26)·정국(4→13)·제이홉(2→11)·문별(12→18)·티파니(2→20)·화사(9→16)·강승윤(2→16)·송민호(3→5)·지코(24, 신규 0)·백현(6→19)·지민(5→10)·주헌(5, 신규 0). `tools/melon_solo_fill.mjs` 신설 — 검색 결과를 그대로 믿지 않고 **후보 aid의 앨범 목록이 기존 `discography` 제목과 겹치는지**로 검증해 최고 점수만 채택(겹침 0이면 건너뜀 = 잘못 채우느니 안 채운다). ②⚠️**같은 앨범인데 표기가 달라 중복되던 것 20장 정리** — 기존 `[미니 6집] Letter To Myself` vs 멜론 `[미니] Letter To Myself - The 6th Mini Album`처럼 **키가 달라 중복 체크를 통과**해 카드에 같은 앨범이 두 번 떴음(태연 9·태민 8·온유 3, +연준 1은 이전부터 있던 완전동일 중복). 판정 규칙은 **같은 발매일 + 기존 제목이 멜론 제목에 통째로 포함**이면 동일 앨범으로 보고 `몇 집`이 붙은 기존 쪽을 남김. 같은 로직을 스크립트에도 넣어 재발 방지. 온유 `Knock On My Door`/`INSPIRATION`(같은 날 별개 앨범)과 정국 `Seven` 버전 3종은 규칙상 안 걸리는 것 확인. ③**수지 `이두나! OST` 재추가** — 어제 "20트랙 컴필이라 본인 앨범처럼 보인다"고 뺐는데, **수지가 주연한 드라마이고 본인이 3곡(보통의 날 #1, Undercover #9, 라이브밴드ver #11)을 부름**이 확인돼 특이 케이스로 포함(사용자 판단). `isMain:false`+`feat:true`라 기본 목록엔 안 뜨고 케밥에서만. ④⚠️**멜론 차단 함정**: 요청이 쌓이면 멜론이 **HTTP 406에 정상 크기의 HTML 안내페이지**를 주는데, 길이만 보고 성공으로 치면 파싱 0건 → "멜론 후보 없음 — 건너뜀"으로 **대상 전원이 조용히 스킵**된다(실제로 겪음). 상태코드까지 받아 200만 성공으로 보고, 차단이면 크게 알리고 `exit 3`. ⑤`tools/melon_aid_probe.mjs` 신설 — 검증 실패 시 후보 aid들의 **멜론 상세(소속그룹/유형)를 직접 읽어** 눈으로 고르는 용도. `이름=aid` 문법으로 수동 지정 가능(자동검증 우회라 위험, 확인 후에만).
- [진행중][artists.json] **솔로 디스코 남은 34명 / 141장**(집 세션에서 이어서). 실행: `node tools/melon_solo_fill.mjs --top 8` 반복(멜론 차단 나면 시간 두고 재실행). **우선 2명은 aid까지 확인해둠 — `node tools/melon_solo_fill.mjs "키=614026" "아이엠=837032"`** (자동검증이 겹침 0으로 걸러냈지만 둘 다 맞는 aid임을 확인: 키는 멜론이 `Gasoline - The 2nd Album`처럼 접미사를 붙여서, 아이엠은 기존 `discography`가 0장이라 비교 기준이 없어서 실패한 것. 아이엠 837032는 주헌 837027과 연번 = 몬스타엑스 일괄 등록). **목록**(현재→최대, 격차순): 아이엠 0→13 · 지코 24→37 · 키 6→18 · 주헌 5→15 · 조이 1→10 · 뷔 1→8 · 웬디 3→9 · 효연 3→9 · 태양 6→12 · 첸 10→15 · 카이 4→9 · 원호 0→5 · 수호 7→11 · 최강창민 10→14 · 라비 0→4 · 슈가 1→4 · 피오 0→3 · 보라 0→3 · 우지 0→2 · 지디 5→7 · 탑 0→2 · 대성 17→19 · 한경 0→2 · 기현 17→19 · 박경 15→17 · 윈터 5→6 · 슬기 8→9 · 예리 5→6 · 서현 1→2 · 희철 0→1 · 시원 0→1 · 구준회 4→5 · 유현 0→1 · 혁 0→1. ⚠️**지코·주헌의 격차는 허수** — 멜론 검증 겹침이 각각 24/5(전량 일치)인데 신규 0장이라, 남은 `soloDiscography` 항목은 멜론에 솔로 앨범으로 존재하지 않는 것들(구버전 필드의 유령/피처링). 채우려 하지 말고 확인 후 필드만 지우면 됨. ⚠️`soloDiscography`는 **신뢰 불가 판정된 죽은 필드**라 옮기지 말고 멜론에서 새로 긁는 게 방침(2026-08-23).
- [완료][artists.json][index.html] **수지 듀엣 8장 양방향 반영 + 제외 키워드 2건**(사용자 요청). ①**듀엣을 상대방 카드에도**: 유니버스 내 상대는 백현(Dream) · 택연(Classic, 드림하이 OST Part.1) · 아이유(드림하이 OST Part.1) · 디오(Dream High 쇼뮤지컬) — 양쪽 `discography`에 같은 레코드를 넣음. 유니버스 밖 상대(강승원·박원·Babylon·윤덕원)는 수지 카드에만. ②**표시 정책**: "디폴트엔 안 뜨고 케밥에서만"이라는 요구가 **기존 필터의 `isMain:false`와 정확히 일치**해서 **코드 수정 0줄**로 해결(mainAlbums에서 빠지고 `showingAll`일 때 합류). 실측 확인 — 기본 8장(싱글6+미니2) → "OST·피처링 포함" 23장 → "부가 앨범까지 포함"(마스터) 29장. **OST 13장은 원래부터 들어있었고** 기본 노출만 안 됐던 것(사용자가 빠진 줄 알았던 부분). ⚠️**컴필레이션 제외 판단**: `이두나! OST`(20트랙·참여 아티스트 12명)가 듀엣 검출에 딸려왔는데, 수지는 그중 한 곡만 불렀는데도 개인 디스코에 20트랙짜리로 박히면 본인 앨범처럼 보여서 제거. **듀엣(2~3인)은 넣고 컴필레이션은 빼는 것**이 기존 관례(개인은 `OST Part.N` 단위로만 들어가 있음). ③동기화 무조건 제외 키워드에 **`물건너온아빠들` + `물 건너온 아빠들`**(띄어쓰기 변형) 추가 — `_isJunkVideoTitle`이 대소문자는 무시하지만 공백은 그대로 비교하므로 표기 변형은 별도 등록 필요.
- [완료][artists.json] **수지 솔로 디스코 2→21장 복구 + `soloDiscography` 죽은 필드 정리**(사용자 제보 "미쓰에이 수지 single 다 빠졌다"). **진단**: 수지에게 필드가 둘 있었음 — `discography`(멜론 기반, 카드에 실제로 렌더되는 것) 2장 + `soloDiscography`(32장). 후자는 **`index.html` 어디서도 안 읽는 죽은 필드**라 화면엔 미니 2장만 떴던 것. ⚠️`soloDiscography`는 2026-08-23에 이미 **신뢰 불가 판정**된 Apple Music 스크레이핑 구버전(민호=존재하지 않는 가짜 싱글, BOBBY=동명이인 유럽 아동가수) — **그대로 옮기면 안 되고 멜론에서 새로 긁는 게 방침**이라 그대로 따름. 실제로 이번 32장에도 중복이 6건(START-UP Pt.14 2회, While You Were Sleeping Pt.13 3회 등) 있었음. **수집**: 멜론 aid 514741, `albumPaging` 55장 → `play_artist` aid로 솔로 판별 21장 → 상세에서 트랙·커버 보강. 결과 **싱글 6 · OST 13 · 미니 2**, 커버/타이틀곡/트랙 결손 0. ⚠️**멜론 함정 2건 추가 확인**: (a) **앨범유형은 상세 페이지에 없고 목록의 `vdo_name`(`[싱글]`/`[EP]`)에만 있음** — 상세만 긁으면 type을 못 채움. (b) 멜론이 OST를 `[싱글]`로 표기하는 경우가 많아(드림하이 OST Part.4 등) **제목의 OST 문자열로 재분류**해야 기존 관례와 맞음(`type:OST` + `feat:true` — 기존 895장이 이 패턴). EP→미니 치환은 PRINCIPLES 규칙대로. ⚠️`artists.json`은 `JSON.stringify(A,null,2)`와 **바이트 단위로 동일**함을 확인하고 라운드트립(그냥 쓰면 20만 줄 diff 나는 함정, 앞서 겪음) → 실제 diff 607줄. **듀엣은 제외**(Dream/수지·백현, 기다리지 말아요/수지·이승기 등 7장) — 상대방 카드에도 안 들어있는 게 기존 관례라 임의로 안 넣음, 사용자 판단 대기. 📌**전체 범위**: `soloDiscography` 보유자가 아직 **86명** 남았고, 그중 49명은 `discography`보다 많아 **총 450장이 카드에 안 뜨는 상태**(솔라 38·휘인 35·태연 25·온유 23·태민 19·정국 19…). 같은 방식으로 처리 가능하나 1인당 멜론 조회 수십 건이라 배치 작업 필요.
- [완료][admin.js][DB] **`created_at` 컬럼 추가 → "신규분만 검수" 가능해짐**. Fable이 제안한 일일 루틴의 핵심 규칙("하루 신규분만 보고 끝낸다")이 **지금까지 실행 불가능**했던 이유 — `yt_channel_videos`에 유입 시각 컬럼이 아예 없어서 무엇이 새로 들어온 건지 알 방법이 없었음(`published_at`은 2015년 영상이 오늘 큐에 들어올 수 있어 대용 불가). SQL 실행 완료. ⚠️**함정**: `add column ... default now()`라 **기존 371,448행이 전부 ALTER 시각 하나로 채워짐** → "최근 24시간" 같은 조건이 오늘은 테이블 전체를 잡아버림(실측 371,448건). 그 시각을 기준선 상수(`_ADM_CREATED_BASELINE`)로 박아두고 **그보다 큰 것만** 신규로 센다(경계값 실측 확인, 0건). ①관리자 홈에 **"새로 들어온 영상"** 카드 추가(5장). ②그룹배정 검수 탭 정렬을 **그룹명순 → 유입 최신순**으로 변경 — 신규분이 맨 위로 와야 "위에서부터 오늘 것만" 처리하고 끝낼 수 있음. 상태줄에 신규 유입 건수도 별도 표기. 검증: 데스크톱/모바일 카드 5장 렌더 + 값 확인, 스모크·인터랙션·매칭(29/29)·validate-data 통과.
- [완료][admin.js][index.html][kpop_universe.css] **관리자 홈 "오늘 할 일" 대시보드 + 매일 루틴 실행기 신설**(Fable 자문 + 사용자 요청). **왜**: 검수 큐가 3,051건 쌓이는 동안 아무도 안 봤고 사용자 본인이 "이걸 해야 하는지 까먹고 있었다"고 함 — 원인은 의지가 아니라 **할 일이 있다는 걸 알 방법이 없던 것**(버튼이 설정 패널에 20개 넘게 흩어져 있어 매번 뭘 눌러야 하는지 기억해내야 했음). ①**카드 4장**: 마지막 루틴 실행(N시간 전) / 그룹배정 검수 대기 / strictSync 오염 검수 / 새 피드백. 각 카드 클릭 → 해당 탭으로 바로 이동. **무거운 집계는 일부러 뺐음**(미태깅 155,070건=5.6초 — 숫자가 커도 행동으로 안 이어지는 지표). 피드백 "새 것"은 스키마 변경 없이 localStorage의 마지막 열람 시각 기준. ②**매일 루틴 실행**: 기존 1~4번 버튼(동기화→자동태깅→콜라보재검증→동명이인스캔)을 원래 함수 그대로 순차 호출 + 단계별 성공/실패와 **결과 요약**을 남김. ⚠️요약이 핵심 — 오늘 실제로 "재태깅 눌렀는데 group_ko가 안 바뀌어서 다 된 건지 모르겠다"는 일이 있었음. **"동기화 빼고 실행"** 별도 제공(동기화는 YouTube 쿼터 때문에 수십 분씩 걸리거나 끊김) + 중단 버튼. ③**데스크톱/모바일 양쪽 대응**(사용자가 둘 다 씀) — 카드는 auto-fit 그리드로 1~3열 자동 접힘, 모바일은 전체화면 시트 + 버튼 100% 폭. ⚠️**만들며 잡은 것 3건**: (a) supabase-js는 `.select()`를 **먼저** 불러야 필터가 걸린다 — `from().eq()` 순서로 짰다가 카드가 전부 "?"로 떴음. (b) `Promise.all`로 묶었더니 **제일 느린 쿼리에 발이 묶여** 카드 셋이 다 멈춰 보임(콜드 커넥션 첫 쿼리 **11.2초** 실측) → 각자 도착하는 대로 채우게 분리. (c) `sb` 준비 전에 열면 카드가 "…"로 영원히 굳던 것 → 대기 후 재시도 + 실패 시 사유 표기. 기존 버튼은 **하나도 안 없앴음**(이 화면은 진입점만 추가하는 것이라 회귀 위험 0). 검증: 데스크톱1280/모바일390 렌더·카드값·가로스크롤 없음·페이지에러 0 + 스크린샷, 스모크·인터랙션·매칭(29/29)·validate-data 통과.
- [완료][.github/workflows/][tools/bump_cache_version.mjs][PRINCIPLES.md] **캐시버스터 자동화 — DECISIONS 항목 해소**(Fable 자문 ①안 채택). `index.html`의 `kpop_universe.css?v=`·`shared.js?v=`·`_ADMIN_JS_VER`와 `sw.js`의 `CACHE_VERSION`을 **파일 내용 해시로 자동 갱신**하는 `bump-cache-version` Action 신설. 형식 `<YYYYMMDD>-<해시8>` — 날짜는 사람이 읽기 위한 것이고 무효화는 해시가 함(내용이 같으면 값도 그대로라 불필요한 재다운로드 없음). **근거**: 2026-08-24에 손으로 올리는 걸 빠뜨려 폰이 "새 마크업 + 옛 CSS"를 그려 화면이 깨졌고, 그 뒤 하루 작업하는 동안만 **12번**을 손으로 올렸음(`_ADMIN_JS_VER` a→k, `sw` v72→v86, `css?v` 15→19) — 사람 규율로는 반드시 또 빠진다는 게 증명됨. ⚠️**무한 루프 가드 2겹**: 트리거 경로에 `index.html`/`sw.js`가 있고 Action이 바로 그 둘을 고치므로, ①봇 커밋이면 아예 안 돌게 `if` 가드 ②그걸 뚫려도 해시가 최신이라 diff가 없어 자연 종료. ⚠️**멱등성 함정**: `CACHE_VERSION` 해시에 `index.html`을 넣는데 그 안에 방금 바꾼 `?v=` 값이 들어있어서 **매 실행마다 값이 달라지는 무한 갱신**이 처음에 실제로 발생 → 해시 입력에서 버전 문자열 자리를 placeholder로 지우고(`stripVersions`) 3회 연속 실행으로 멱등 확인. 패턴을 못 찾으면 조용히 넘어가지 않고 **exit 2로 실패**(자동화가 있는 줄 알고 방심하는 게 더 위험). 로컬 확인은 `node tools/bump_cache_version.mjs --check`. PRINCIPLES.md·index.html의 "손으로 올릴 것" 문구도 갱신.
- [완료][admin.js][index.html][kpop_universe.css] **검수(ss) 탭 조회 32배 + 선택-무관 버튼 + 툴바 버튼 스타일 통일**(사용자 제보). ①**조회 19.2초 → 0.6초**: `.in('group_ko', 23개).order('id')` 한 방으로 12,113행을 받고 있었는데, group_ko 인덱스는 있어도 **전역 id 정렬과 안 맞아** 페이지마다 매칭 3.3%(12,113/371,448)를 찾느라 테이블을 훑는 꼴이었음 → **그룹별로 쪼개 동시 6개**로 받으면 각 쿼리가 group_ko 인덱스를 그대로 타서 0.6초(실측 32배). **인덱스 추가 불필요** — 원곡 탭과 달리 여기선 쿼리 형태만 바꿔 해결됐다. ②**"선택-무관" 버튼 신설**: 검수 탭엔 `선택-숨김`만 있고 무관 처리 수단이 없었음(숨김=보류에 가깝고 무관=이 그룹 콘텐츠가 아님이라 성격이 다름). 스냅샷 + 200건 청크 + 처리한 행은 목록에서 제거. ③**툴바 버튼 스타일 통일**: `#vm-hold-btn`만 **CSS 규칙이 아예 없어** 브라우저 기본 버튼으로 튀어나오고 있었음(사용자 "선택-보류만 못생겼음"). 버튼마다 같은 선언을 복붙하던 걸 `#vm-toolbar>button` 공통 규칙 하나로 묶고 색만 변형으로 — 파랑=일반 / 주황=내려놓기(보류·무관) / 초록=승인·해제 / 빨강=거부 / 보라=원곡지정. `#vm-coverset-btn`의 인라인 스타일도 CSS로 이관. ⚠️**함정**: 변형 규칙을 `#vm-review-reject-btn`처럼 단독 id로 쓰면 **공통 규칙 `#vm-toolbar>button`(id+태그)이 특이도가 더 높아 색이 안 먹음** — 변형에도 `#vm-toolbar>` 접두사를 붙여야 함(헤드리스 실측으로 발견). 검증: 툴바 전 버튼 computed style 확인(브라우저 기본 잔존 0) + 스크린샷, 스모크·인터랙션·매칭(29/29)·validate-data 통과. `_ADMIN_JS_VER→2026-08-25k`, `css?v=19`, `sw v85→v86`.
- [완료][admin.js][index.html] **검수 센터 "원곡 커버" 탭 — 매번 전량 재스캔하던 것 개선**(사용자 요청). ①**결과 캐시**: 검수 센터를 닫을 때 `_wonkokScanned=false`로 리셋해서 **열 때마다 처음부터 다시 스캔**했음 → 리셋 제거, 세션 동안 결과 유지. 대신 숨겨져 있던 자동 트리거 버튼을 **"🔄 다시 스캔"**으로 노출. ②**서버 필터로 전환**: `_wonkokScan`이 **전체 371,448행을 통째로** 페이지네이션해 받아온 뒤 클라이언트에서 "원곡" 문자열을 찾고 있었음(`ILIKE '%원곡%'`가 2글자라 pg_trgm 인덱스를 못 타는 게 이유). PostgREST `imatch`(`~*`)로 서버에서 거르도록 변경 — 판정 축이 둘이라(제목 표시 / with태그 보유 구조신호) 각각 조회 후 id로 합침. 전송량 **371,448 → 30,435행(8%)**. ③**부분 인덱스로 완결**: 서버 필터만으론 안 빨라졌음(실측 37초 vs 전량 34초 — 두 조건 다 인덱스를 못 타 스캔량이 같아서). `title ~* ...`와 `with_groups<>{} or with_members<>{}` 각각에 부분 인덱스를 걸고 **실측 3.5초**(제목 11.9→1.5초, with태그 25.2→2.0초). 기존 전량 방식 대비 **10배**. 그래도 캐시(①)만으로 "열 때마다 대기"는 사라지고, 전송·파싱량은 8%로 줄어든다. ⚠️ 서버 정규식은 클라이언트 `_wonkokIndicatorRe`와 **같은 의미**를 유지해야 함(한쪽만 고치면 후보가 조용히 빠짐, 주석에 명시). ⚠️인터랙션 테스트가 1회 실패 후 3연속 통과 — 헤드리스 2개 동시 실행 타이밍으로 추정되나 재현 안 됨, 관찰 대상. `_ADMIN_JS_VER→2026-08-25j`, `sw v84→v85`.
- [완료][admin.js][tests/matching.test.js] **weak 매칭 정책 변경 — hidden 폐지 + 해시태그는 strong으로**(사용자 결정, 위 DECISIONS 해소). ①**`hidden` 폐지**: 약한 근거 매칭을 `content_flag:'hidden'`으로 감추던 걸 없애고 **`needs_review` 플래그만** 남김. 유저에겐 정상 노출되고 어드민은 사후 감사만 한다. 근거: "사람이 검수한다"는 전제가 실제로 안 지켜져(사용자 "안 누르게 되더라") 큐가 3,051건 쌓이는 동안 **709건이 유저 화면에서 사라져 있었음**. ②**해시태그 명시는 strong**: 역추론 결과를 일괄 `weak`로 매기던 걸, `#KEY`·`#키`처럼 **업로더가 해시태그로 특정해준 경우엔 `strong`**으로 바꿈(`_inferViaHashtag`) — 큐의 22%(661건)가 이 경우였고, 태그로 박아준 걸 "추측"으로 볼 이유가 없음. ③**왜 지금 바꾸나**: 이 정책은 2026-08-20 Love/루나/조이 오염 대응으로 도입됐는데, 그 사이 흔한단어(`_ATM_COMMON_KO_WORDS`)·짧은영문(`_atmNameNeedsCtx`)·동명이인 dedup 게이트가 앞단에 생겨 위험한 이름은 애초에 인퍼런스에 도달하지 못함 → 이중 방어가 됐고, 실측으로도 큐의 93%가 고유 이름이며 표본은 전부 정확했음. 검증: 매칭 테스트 29/29(confidence 판정 2종 신규 — 해시태그=strong / 평문=weak 유지), 스모크·인터랙션·validate-data 통과. `_ADMIN_JS_VER→2026-08-25i`, `sw v83→v84`. **후속**: 기존 큐 3,051건은 새로 만든 "선택-승인"으로 일괄 처리(709건 hidden도 같이 풀림).
- [완료][admin.js][index.html] **그룹배정 검수 큐를 실제로 쓸 수 있게 — 일괄 승인/거부 + 편집 후 상태 유지**(사용자 제보 "검수센터를 진짜 안 누르게 되더라"). 왜 안 썼는지 파보니 도구 문제였음: ①**한 건씩만 처리 가능** — review 탭만 체크박스가 없어서(`showCheckbox`에 미포함) 3천 건짜리 큐를 한 건씩 눌러야 했음 → 체크박스 + **선택-승인/선택-거부** 버튼 신설(`_vmReviewBulk`, 200건씩 청크, 실행 전 스냅샷, 재조회 없이 목록에서만 제거). review 탭에선 이 탭 질문과 무관한 기존 일괄 버튼들(무관/보류/정상/원곡제외)은 숨겨 오조작 방지. ②**편집 한 번에 처음부터 재조회** — `_closeVidTagModal`이 무조건 `_vmLoad()`로 탭 전체를 다시 긁어서, 3천 건 탭에선 연필 한 번 누를 때마다 몇 초씩 정지 → `_vmRefreshRows(ids)` 신설해 **편집한 행만** 다시 읽고 제자리 갱신(탭 조건에서 벗어난 행은 목록에서 제거, 실패 시에만 전체 재조회로 폴백). ③**조회 자체가 느림** — `_sbFetchAll`은 이미 keyset 페이징인데 `needs_review`에 인덱스가 없어 매 페이지가 371,448행을 훑음 → 부분 인덱스 SQL 별도 제공. **📊 큐 구성 실측(사용자 "훑어봤는데 틀린 게 거의 없다" 확인 겸)**: 판정식은 `needsReview = !owner && confidence==='weak'` = **외부/모음 채널 + 제목에 그룹명이 하나도 없이 멤버 이름만으로 역추론**. 3,051건 중 동명이인·흔한단어·한글자 이름이 섞인 건 **217건(7%)**뿐이고 93%는 단독·고유 이름. 게다가 **661건(22%)은 해시태그로 멤버가 명시**돼 있어(`#KEY #키`) 사실상 강한 근거인데 weak로 분류됨. 표본을 봐도 태민→샤이니, 태양→빅뱅, 카이/세훈→엑소처럼 전부 정확 — **위험한 이름은 이미 앞단 게이트(흔한단어·짧은영문·동명이인 dedup)가 다 걸러서, 여기까지 온 건 대체로 맞다**는 게 원인. 2026-08-20 도입 당시엔 그 게이트들이 없었음. ⚠️**부작용**: 이 정책이 **709건을 `hidden`으로 유저 화면에서 감추고 있음**. 정책 재검토 필요(아래 DECISIONS). `_ADMIN_JS_VER→2026-08-25h`, `sw v82→v83`. 검증: 스모크·인터랙션·매칭(27/27) 통과.
- [완료][admin.js][artists.json][tests/matching.test.js] **탈퇴 게이트 신설 — 탈퇴 후 영상이 옛 그룹에 붙던 문제**(사용자 제보 "라이즈 탈퇴한 승한 직캠이 라이즈 카드에 뜬다"). ①**진단**: 승한 관련 71건 중 16건은 `승한앤소울(XngHan&Xoul)` = 탈퇴 후 솔로 프로젝트라 명백한 오태깅이고, 나머지 대부분은 **2023년 라이즈 활동기 직캠이라 정당**했음. ⚠️**제목만으론 절대 구분 불가** — `[MPD직캠] 라이즈 승한 'Talk Saxy'`(2023, 정당)와 `승한 댄스 실력`(2026, 오태깅) 둘 다 제목엔 승한만 있음. **발행일이 유일한 단서**(= 사용자가 제안한 날짜 신호가 정확히 필요한 지점). ②**전제 데이터 복원**: 승한이 라이즈 로스터에서 **통째로 삭제**돼 있어(group.ko='솔로'만) "라이즈였다"는 정보 자체가 없었음 → 나무위키 교차확인(승한 문서 "2024년 10월 13일에 팀을 탈퇴", 라이즈 문서 "2024년 10월 승한이 탈퇴하면서 6인조로 개편")해서 `groups:[{솔로},{라이즈,active:false,left:"2024.10.13"}]`로 복원(이달의소녀→루셈블과 동일 스키마). ⚠️**복원만 하면 오히려 역효과** — 매처가 탈퇴 후 솔로 영상까지 라이즈로 잡기 시작함(실측 확인). **로스터 복원과 게이트는 반드시 세트**. ③**`_atmLeftBefore` 게이트 신설**: `_m2ParseTitle`에 4번째 인자 `publishedAt` 추가, 역추론·멤버추출 두 경로 모두에서 "이 영상 발행 시점에 이미 그 그룹을 떠난 사람"을 제외. `publishedAt` 없으면 게이트 미적용(하위호환 + 근거 없이 안 지움), 탈퇴일 미상이면 판단 보류. 호출부 5곳에 `v.published_at` 배선(재검증/미리보기 쿼리엔 `published_at` 컬럼도 추가). ⚠️**데뷔일 게이트(원안 B)는 채택 안 함** — 전체 그룹 실측 결과 "데뷔 이전 업로드" 13,106건인데 대부분 **정당한 프리데뷔 공식 콘텐츠**였음(이달의소녀탐구 487·베이비몬스터 BATTER UP MV·이즈나 데뷔직전 Diary·서바이벌 출신 그룹 다수). 자체채널/외부채널 구분이 DB에 없어(`channel_id` 미존재) 소급 판별도 불가 → 날짜는 **탈퇴처럼 근거가 명확한 경우에만** 쓴다. 검증: 매칭 테스트 27/27(탈퇴 게이트 3종 신규 — 탈퇴후 차단·활동기 유지·날짜없음 하위호환), 스모크·인터랙션·validate-data 통과. `_ADMIN_JS_VER→2026-08-25g`, `sw v81→v82`. **후속**: 기존 71건은 group_ko라 재태깅으로 안 고쳐짐 → 아래 SQL 필요.
- [완료][admin.js][groups.json][artists.json][tests/matching.test.js] **앤팀 &TEAM 토큰 오염 + "솔로" placeholder 누출 + 보넥도 인스타 정리**(사용자 제보). ①**앤팀**: `hit()`이 특수문자를 공백으로 바꾸는 탓에 `&TEAM`이 그냥 `TEAM`이 돼서 "Team 5JANGNAM"·"Team A vs B"·"이주은 Team 1" 같은 무관 제목이 전부 앤팀으로 매칭됐음 — **`15&`(피프틴앤드)와 완전히 같은 계열**이라 그때 만든 `_GROUP_TOKEN_LITERAL_ONLY`에 `&TEAM` 추가(`&`가 살아있어야만 인정). ⚠️단, 실제 유튜브 제목은 `&TEAM`보다 해시태그 `#andTEAM`을 압도적으로 많이 써서 리터럴 전용만 걸면 정상 영상이 대량으로 끊김 → `groups.json` 앤팀에 `altNames:["andTEAM"]`을 **세트로** 추가. 실측: 앤팀 태깅 3,438건 중 **3,110건(90%)은 앤팀 표기가 있는 진짜, 328건(10%)만 `TEAM`만으로 걸린 오염**. ②**"솔로" placeholder 누출**: 무소속 솔로(아이유·비·싸이·승한)는 artists.json `group.ko`가 `"솔로"`인데 이건 여러 명이 공유하는 가짜 값이라 group_ko로 쓰면 안 됨(`_isValidVidGroupKo`도 무효 처리). 그런데 `_m2ParseTitle` 역추론 경로가 이걸 그대로 `primaryGroup`으로 반환해 **`group_ko='솔로'` 영상이 633건** 쌓였고, 어느 카드에도 안 걸리는 미아 상태였음 → 나머지 코드의 관례(`_ytGroupKoFor`: 실존 그룹이면 그룹명, 아니면 본인 이름)와 동일하게 변환. 이제 "승한앤소울 승한 직캠"이 `승한`으로, "카리나의 아이유 커버"가 `아이유`로 잡힘. ③**보이넥스트도어 인스타 5명 삭제**(재현·성호·리우·태산·이한) — 전부 `.koz` 소속사(KOZ) 운영 계정이라 [[feedback_instagram_personal_only]] 원칙에 해당. 검증: 매칭 테스트에 회귀 7종 추가(24/24), 스모크·인터랙션·validate-data 통과. `_ADMIN_JS_VER→2026-08-25f`, `sw v80→v81`. **후속**: 앤팀 328건·솔로 633건은 어드민 재태깅으로 정리 필요(코드만 고쳐선 기존 행이 안 바뀜).
- [완료·SQL대기][admin.js][index.html][artists.json] **외부채널 개편 3·4단계 — 개인 채널을 DB로 이관 + 탭 경계 재정의**. ③`artists.json`의 `channels[]` 제거(슬기·효연·설아 3건) → `_ytSyncAll`·`_officialChannels`에서 개인 채널 경로 삭제, 동기화는 "외부채널 동기화"가 담당. ④채널 탭 라벨을 "그룹/멤버 공식·그외" → **"그룹 공식(파일)·직접 관리"**로 — 경계를 "공식/비공식"이 아니라 **"파일이 정본(읽기전용) / DB에서 직접 관리(편집가능)"**로 다시 그음(같은 성격의 개인 채널이 편집 가능/불가능으로 갈려 있던 게 원래 문제였음). ⚠️**중간에 잡은 오판**: 처음엔 "기존 1,072건이 사이트 어디에도 안 보인다"고 판단했는데 틀렸음 — 멤버 카드 쿼리에 `group_ko.eq.멤버본인이름` 절이 따로 있어서 **본인 카드엔 보이고 있었음**(안 보이는 건 그룹 카드 쪽, 멤버 태깅이 없는 건 맞음). 그래서 그 절을 **지우지 않고 그대로 뒀다** — 이관 SQL이 돌기 전까지 그 절 하나로만 보이는 상태라 지우면 그 사이 영상이 통째로 사라짐. 이관 후엔 매칭 행이 없어 자연히 no-op. ⚠️`artists.json` 편집 시 `JSON.stringify(A,null,1)`로 통째로 다시 쓰면 포맷이 달라져 **diff가 20만 줄**이 됨(실제로 한 번 발생 → 되돌리고 해당 라인만 sed로 삭제, 16줄 diff). **후속: 아래 이관 SQL 실행 필요**(ext_channels 3건 시드 + 기존 1,072건 `group_ko`·`members` 재배정). 검증: 스모크·인터랙션·매칭(17/17)·validate-data(오류 0) 통과. `_ADMIN_JS_VER→2026-08-25d`, `sw v78→v79`.
- [완료·SQL대기][admin.js][index.html][kpop_universe.css] **외부채널 개편 1·2단계 — 채널별 기본 카테고리 + 아이돌 개인채널 유튜브 아이콘 자동화**(사용자 요청). 발단: "멤버 공식"과 "아이돌개인"이 왜 나뉘어 있냐 → 조사해보니 UI만이 아니라 **동작이 다름**(공식=`artists.json` 파일·읽기전용·`group_ko`가 본인 이름 / idol=DB·편집가능·`group_ko`가 소속 그룹, members는 owner 고정). ①**카테고리 폴백을 채널별 설정으로**: 제목 분류(`_ytClassify`)가 'other'로 떨어진 영상의 행선지가 tier에서 자동으로 정해져(show=show, 나머지 전부 variety) 개인 채널 브이로그까지 예능 탭에 쌓였음 → `ext_channels.default_category` 신규(빈값=기존동작, `'none'`=폴백 안 함→전체탭만, 그 외=지정 카테고리). `_extBuildRows`에 인자 추가, 호출부 2곳 전달, 목록 행에서 즉시 변경 가능. ②**멤버 카드 유튜브 아이콘 자동화**: 개인 채널은 어드민이 DB로 관리하는데 SNS 아이콘은 `artists.json links.youtube`만 봐서 "등록했는데 아이콘이 안 뜬다"가 됨(같은 정보를 파일·DB 이중 관리). `_loadIdolChannels`/`_withIdolChannelLink` 신설 — `tier='idol'`만 읽어 멤버 카드 링크에 병합, **파일 값이 있으면 그쪽 우선**(수동 지정 보존), 공동운영은 owner 전원 카드에, 한 멤버 다채널은 `is_primary` 신규 컬럼으로 대표 선택. ⚠️**스모크가 잡은 사고**: 처음엔 새 컬럼을 명시해서 `select`했는데 마이그레이션 전이라 PostgREST가 400을 뱉었고, `_loadExtChannels`는 error면 return이라 **`_EXT_CHANNELS`가 통째로 비어 외부채널 동기화가 전면 중단**되는 상태였음 → 양쪽 다 `select('*')`로 바꿔 마이그레이션 순서에 안 묶이게 함(컬럼 없으면 undefined→기본값 폴백). 코드가 SQL보다 먼저 배포돼도 안전. **⚠️ 아래 SQL 실행 전까지는 신규 기능만 무동작(기존 동작은 그대로)**. 검증: 스모크·인터랙션·매칭(17/17)·validate-data 통과 + SQL 실행 전 안전성 전용 검증. `_ADMIN_JS_VER→2026-08-25c`, `css?v=18`, `sw v77→v78`. **다음 단계(미착수)**: ③효연·슬기·설아를 `ext_channels`로 이관 + 기존 1,072건 `group_ko`(본인이름→소속그룹)·`members`(빈배열→본인) 재배정 일회용 버튼 ④공식 탭을 그룹 전용으로 정리.
- [완료][shared.js][admin.js][index.html][tests/matching.test.js] **NCT U를 "로테이션 유닛"으로 재정의 + 흔한단어 6개 보호**(사용자 요청). ①**흔한단어 보호**: `_ATM_COMMON_KO_WORDS`에 가을(아이브)·노을(레인보우)·소원(여자친구)·하나(피프티피프티)·루비(프림로즈)·미소(드림노트) 추가 → 감사 🔴 7건→1건(남은 1건은 그룹명 "다이아"). ②**NCT U 오염 767건의 근본 원인 수정**: `_PROJECT_UNITS['NCT U']`의 members 21명은 다른 유닛(부석순·유아유)과 성격이 달라서 "고정 멤버"가 아니라 **"NCT U로 활동한 적 있는 사람 풀"**인데, 유닛명이 제목에 뜨면 무조건 전원 확장하고 있었음 → **`rotating:true` 플래그 신설**, 로테이션 유닛은 **제목에 이름이 따로 언급된 멤버만** 태깅. 유닛명만 있고 개별 이름이 없으면 멤버 0명(자체 채널이면 group_ko는 채널 기준으로 남아 영상 유실 없음, 사용자 합의). 확장 지점 2곳(`_unitMembersFromTitle` 자체채널 / `_m2ParseTitle` 외부채널) 모두 적용하고 후자는 **명시된 멤버의 그룹만** matchedGroupKos에 넣음. ⚠️**단위테스트로 발견한 함정**: 유닛 members에 한글 활동명(mko)만 있어서 처음엔 한글로만 판정했는데, NCT 계열은 영문 표기·해시태그(`#JISUNG`·`TAEYONG`)가 압도적이라 영문 제목 영상에선 아무도 못 잡았음 → `_unitMemberNamedInTitle` 헬퍼 신설해 `name.en`까지 보되, 짧은 영문명(TEN·KUN)은 흔한 단어와 겹치므로 5자 미만은 해시태그로만 인정(`_atmNameNeedsCtx`와 같은 원칙). ③**원곡자로 유닛 지정 가능**: 태그 모달의 원곡자 검색이 `Object.keys(GROUPS)`만 뒤져서 유닛은 애초에 못 골랐음("행성 승격 안 함" 원칙상 유닛은 groups.json에 없음) → `_PROJECT_UNITS`도 후보에 추가("NCT U (유닛 전체)"). ④**멤버 커버탭 하단에 유닛 커버 노출**: `_loadCoverOfSection`이 본인 `cover_of_members`뿐 아니라 **본인이 속한 유닛명으로도 `cover_of_groups`를 조회**(`_unitTagsFor` 재사용, id 기준 dedup) → 다른 아티스트의 NCT U 커버가 NCT 전 멤버 카드 "다른 아티스트의 커버" 섹션에 노출됨. 곡별 참여자 정밀 태깅은 별도 과제로 남김(사용자 합의). ⚠️`tests/matching.test.js`는 admin.js에서 **함수를 이름으로 잘라오는** 하네스라 새 최상위 함수(`_unitMemberNamedInTitle`)를 추출 목록에 추가해야 했음 — 안 하면 전 케이스가 "is not defined"로 실패(주석에 경고 남김). 검증: 매칭 테스트에 NCT U 회귀 케이스 5종 추가(17/17), 스모크·인터랙션·validate-data 통과. `_ADMIN_JS_VER→2026-08-25b`, `shared.js?v→2026-08-25a`, `sw v76→v77`. **후속**: 기존 오염분 767건은 어드민 "콜라보 재검증"으로 정리 필요(코드는 고쳤지만 이미 저장된 행은 재태깅해야 반영됨).
- [완료][tools/][tests/validate-data.js][admin.js][PRINCIPLES.md] **동명이인·흔한단어 오염 전수 감사 + 신규 그룹 자동 점검 체계 신설**(사용자 요청). ①**정적 스캐너 `tools/name_collision_audit.mjs` 신설** — admin.js의 실제 게이트 조건(단일음절/라틴4자/흔한영단어/`_atmNameIsGroup`/`_ATM_COMMON_KO_WORDS`/`strictSync`/유닛토큰)을 **소스에서 파싱해와** 재현하고 "이미 보호되는 것 vs 구멍"을 가른다(복붙하면 코드와 어긋나 거짓 안심을 주므로 파싱 방식 채택). 결과: 그룹 268/인원 1720 중 보호됨 60건, 🔴7·🟡71. ⚠️**설계 시행착오 2건 기록**: (a) 동명이인 96건을 전부 high로 냈다가, 런타임 `nameToGroups` dedup이 "로스터에 2명 이상이면 역추론 자체를 버린다"는 걸 확인하고 info로 강등 — dedup이 못 받쳐주는 조합(흔한단어 겸함/3명 이상=미등록 4번째 가능성)만 올림. (b) 그룹명 위험도에 `_atmNameNeedsCtx`(길이 기준)를 재사용했다가 BTS·EXO·IVE·ITZY까지 58건 오탐 — 그룹명 매칭은 단어경계 기반 별도 경로라 "짧다"가 아니라 "사전에 있는 흔한 단어냐"가 기준이어야 함(→1건으로 축소). ②**실측 도구 `tools/name_pollution_probe.mjs` 신설**(읽기 전용) — 이론적 위험이 실제 DB 오염으로 나타났는지 `이름(그룹)` 태그 수 + 표본 제목의 근거 유무로 잰다. 371,448건 대상 실측으로 **실제 오염 확인**: NCT 계열 767건(엔시티 유 영상에 비참여 멤버 일괄 태깅, 지성321·마크114·정우113·재현113·도영106), 트리플에스 지연45·김채원42(#GLOW 챌린지 유닛토큰 오염 잔존), 이유(에버글로우)47, 민규(동키즈)40(나인투식스 영상), 지유(드림캐쳐)23(유아유 지유), 현석(트레저)16(CIX 현석), 나나(애프터스쿨)23. ⚠️함정: 해시태그를 한글 이름으로만 비교해 `#JUNGWOO` 같은 영문 태그를 못 봐 정상 태그를 대량 오탐했었음 → `name.en`도 같이 보게 수정. ③**`_atmNameIsGroup` 과잉 게이트 수정** — 충돌 상대가 `strictSync` 그룹이면 애초에 제목 매칭 대상이 아니라 막을 모호함이 없는데 정당한 태깅만 잃고 있었음: **방탄소년단 슈가**(↔2002년 걸그룹 슈가/Sugar)와 **하츠투하츠 스텔라**(↔스텔라/Stellar) 둘이 해시태그 없이는 영영 매칭 안 되던 상태 → 예외 추가로 회복. ④**신규 그룹 자동 점검**: `tests/validate-data.js`가 `--new --json`으로 스캐너에 위임해 "새 그룹 이름 충돌 위험"을 경고로 올림(배포 게이트 2번에 이미 포함 → 새 그룹 추가 시 자동으로 걸림). PRINCIPLES.md에 "새 그룹/멤버 추가 절차" 섹션 신설(유형별 조치법 명시). **후속(사용자 판단 필요)**: 위 실측 오염분을 어드민 "콜라보 재검증"·"동명이인 그룹 오배정 전체 스캔"으로 정정, `_ATM_COMMON_KO_WORDS`에 가을·노을·소원·하나·루비·미소 추가 여부. 검증: 스모크/인터랙션/validate-data(오류 0) 통과. `_ADMIN_JS_VER 2026-08-24b→2026-08-25a`, `sw v75→v76`.
- [완료][index.html] **제외 키워드 2건 추가 + 그 과정에서 발견한 복붙 드리프트 정리**(사용자 요청). ①**cover 탭 제외에 `pop original` 추가.** 넣으려고 보니 같은 제외 목록이 **세 군데 복붙**돼 있었음 — 대표영상 쿼리(12976)·cover 탭 목록 쿼리(14943)·태그모달 재검증(15566). 그래서 **2026-08-24에 추가한 `_original`·`original per`·`coverstar`가 재검증 쪽에만 들어가고 정작 목록 쿼리엔 빠져 실제로는 안 걸러지고 있었음**(어제 작업이 사실상 무효였던 것). `_COVER_EXCLUDE` 상수 + `_applyCoverExcludeQuery(q)` 하나로 합쳐 세 경로가 같은 값을 쓰게 하고, 빠졌던 3개도 같이 회수. 이제 `be original` 문자열은 코드 전체에 상수 1곳에만 존재 — 새 키워드는 거기만 고치면 됨. 참고로 admin.js의 `_isBeOriginal`은 이미 `/(be|pop)[\s_-]+original/i`로 pop까지 보고 있었음(어드민 원곡 파서만 앞서 있었고 유저 화면이 뒤처져 있던 셈). ②**동기화 무조건 제외에 `relay dance battle` 추가**(`_JUNK_TITLE_KEYWORDS_GLOBAL`). ⚠️`_isJunkVideoTitle`이 `t.includes(k)`로 **대소문자 구분**하고 있었음 — 목록이 전부 한글이라 여태 안 드러났을 뿐이고, 영문 키워드는 실제 제목이 "Relay Dance Battle"이라 그대로 넣으면 아예 안 걸림 → 양쪽 `toLowerCase()` 비교로 수정(한글 항목은 영향 없음). 일반 "Relay Dance"는 통과하는 것도 확인(과잉차단 없음). 검증: 대소문자 3형태·기존 한글 항목 회귀·서버 쿼리에 20개 전 항목 실림 여부까지 헤드리스 단위검증 전부 통과, 스모크/인터랙션 통과. `sw v74→v75`. **후속: 어드민 "무조건 제외 키워드 영상 무관 정리" 버튼 1회 실행 필요**(키워드 버전 문자열이 바뀌어 기존 오염분 스윕 대상이 됨, tags_manual 행은 건드리지 않음).
- [완료][kpop_universe.css] **그룹 카드 "흔적" 상태줄이 검정이라 안 보이던 버그**(사용자 제보). 상태줄은 `_renderGcStatusLine`이 `type-presence`/`type-favcount`/`type-traces` 클래스를 붙이는데 **CSS엔 앞의 둘만 색이 있었음** — 어제 신설한 traces는 색 규칙이 없어 부모 색을 물려받았고, 멤버 카드는 `#tt{color:#ccd8ee}`라 멀쩡했지만 **그룹 카드 `#gc`는 color 지정이 없어 UA 기본 검정**을 물려받아 어두운 카드 위에서 안 보였음(실측: `#gc` 상태줄 물림색 `rgb(0,0,0)` vs `#tt` `rgb(204,216,238)`). `.gc-status-inner.type-traces{color:#ccd8ee}` 추가. presence(코랄)·favcount(핑크)와 달리 강조색을 안 준 건 의도 — 순위·경쟁 아닌 조용한 합산이라 본문 톤 유지하고 분홍 점만 시선을 끔. ⚠️**"#gc에 기본 color를 주는" 근본수정은 오히려 위험**: 모바일 카드 스택이 이전 카드를 cloneNode 후 id를 지워 배경에 남기는데(`_freezeSheetBottomBeforeOverwrite`), 그러면 `#gc` 아이디 셀렉터가 무효화돼 얼어붙은 카드만 도로 검정이 됨 → 클래스 셀렉터로 박아야 클론에서도 삶(주석에 남김). 그룹 카드 전체를 "색 지정 없이 검정 물려받는 텍스트" 기준으로 헤드리스 전수 스캔한 결과 나머지 5건은 전부 이모지 전용 버튼(`concert-reaction-btn`)이라 무해. 검증: 두 카드 모두 `rgb(204,216,238)` 실측 + 스크린샷. `css?v=16→17`, `sw v73→v74`.
- [완료][index.html][kpop_universe.css][tests/interaction.test.js] **모바일 카드 하단 3건 — 탭바 틈 / 시트 높이 고착 / 방향 판정**(사용자 제보 3건 중 2건, 회사 세션). ①**탭바-카드 사이 8px 틈으로 배경 우주가 비침(재발)**: `updateSheetBottom()`이 `--sheet-bottom`을 **탭바 높이 +8px**로 잡고 있었음(상수 오차, git 최초 업로드부터 존재). `#mob-sr`이 `calc(var(--sheet-bottom) - 8px)`로 그 8을 되빼고 있던 게 방증. `+8` 제거 + `#mob-sr`의 `-8px`도 제거 → 실측 GAP 8→0. ⚠️2026-08-14에 같은 증상이 "탭바/시트 트랜지션 속도 불일치"로 한 번 났던 재발 계열이라 RECURRING_BUGS.md에 이력 추가. ②**측정 자체가 깨지던 잠재버그**: 탭바 높이를 `getBoundingClientRect()`로 재고 있었는데 이건 transform 반영값이라 `.tab-hidden`(translateY 110%) 중에 resize가 오면(iOS 주소창 접힘/펼침) 높이가 0 근처로 망가짐 → `offsetHeight + computed bottom`(transform 무관)으로 교체. ③**"카드 여기저기 열었다 닫았다 하면 탭바 로직이 깨진다"의 정체**: `_extendSheets()`가 시트 3개에 인라인 `min/max-height`·`bottom`을 직접 박는데, 그 값이 **호출 시점의 bs-full 여부** 기준이라 그때 숨겨져 있던 `mob-card-stack`이 `60vh+탭바` 값에 갇히고, 나중에 bs-full로 열려도 인라인이 CSS를 이겨 화면을 못 채웠음(헤드리스 실측: 572여야 할 inner가 442로 고착). **인라인 스타일을 전부 없애고 CSS 변수 2개(`--sheet-bottom`/`--sheet-grow`)만 갈아끼우는 방식으로 재설계** — 지금 열려있든 나중에 열리든 CSS가 계산하므로 stale이 구조적으로 안 생김. ④**방향 판정 견고화**: 2px 히스테리시스 → 누적 델타(내릴 때 28px/올릴 때 14px, 방향 바뀌면 리셋). 그리고 시트 높이가 바뀌면 clientHeight가 커져 브라우저가 scrollTop을 강제로 당기는데(클램프) 그게 "위로 스크롤"로 읽혀 탭바가 숨자마자 도로 나오던 것 → **max 감소분만큼 델타에서 정확히 상쇄**(시간창으로 무시하면 그 사이 진짜 손가락까지 삼켜서 안 됨 — 실제로 360ms 창으로 먼저 짜봤다가 "빨리 올리면 탭바 안 돌아옴"이 재현돼 폐기). 밑에 깔린 시트도 클램프로 scroll을 쏘며 탭바를 두고 싸우던 문제는 `_activeSheetScroller()`(맨 위 카드만 조종)로 차단. 검증: 인터랙션 테스트에 **버그E(틈 0px)·버그F(탭바 숨김 중 연 카드 높이 정상)** 신규 추가, 인터랙션/스모크/validate-data 전부 통과, 이음새 확대 스크린샷 전후 비교.
- [완료][index.html][sw.js] **탐험 Charts 가로선반이 폰에서 깨져 보이던 원인 = 옛 CSS 캐시**(사용자 제보). 어제 커밋(`2fdadf8a`)이 `kpop_universe.css`를 바꾸면서 `index.html`의 `kpop_universe.css?v=15`도, `sw.js`의 `CACHE_VERSION`(kpu-v72)도 안 올렸음 → 폰이 **새 마크업 + 옛 스타일**을 조합해서 그림. 옛 CSS엔 `#feed-chart>.feed-card{width:158px;flex:0 0 auto}`가 없어 카드가 46~68px로 찌그러지고 제목이 세로 한 글자씩 흐름(옛 CSS로 재현해 실제로 그 화면 확인). CSS 코드 자체는 정상. **`?v=16` + `kpu-v73`으로 올림** + `<link>` 위에 "CSS 고치면 ?v= 같이 올릴 것" 경고 주석 추가. ⚠️수동 절차라 또 빠질 수 있음 — SEO 페이지 리빌드 액션처럼 `?v=`를 파일 해시로 자동 치환하는 걸 검토할 것(DECISIONS 후보).

---

## 2026-08-24
- [진행중][groups.json] **싱글 전량 추가(라이트JP) — 배치1(10그룹)**. 큐레이션 방침 확정(사용자): **한국 싱글 전부 포함**하되 ①선공개 싱글(싱글 후 ~45일내 미니/정규가 그 타이틀곡 수록)은 흡수중복이라 제외 ②일본판 제외(결정A). 배치1: 오마이걸(12→26)·에이핑크(14→32)·펜타곤(13→30)·제로베이스원(6→16)·드림캐쳐(13→26)·아일릿(4→14)·보이넥스트도어(7→15)·크래비티(9→20)·이엑스아이디(7→20)·원위(10→23). 총앨범 2,348→2,475. mergesingles.mjs: 기존유지+신규싱글 병합, 선공개제외(제로베이스원 5·펜타곤 2 등 정확히 잡힘), 리믹스/Sped Up/라이브/콘서트/예능(KCON·스테이지파이터)·OST 제외, dedup(에디션중복). ⚠️**한계**: 로마자 일본판(아일릿 "Toki Yo Tomare")은 제목·트랙 kana 둘다로 못 걸러 잔존 가능 — 그래서 **헤비JP 레거시(동방신기·슈주·소녀시대·트와이스·BTS·세븐틴·엔시티계열·에스파·있지·마마무·블랙핑크 등 ~30그룹)는 제외**하고 B전환(jp태그) 때 나무위키 한국음반 섹션대조로 처리 예정. 버전변형(Korean/English Ver.)도 일부 잔존(수동정리 가능). **다음: 라이트JP 배치2~11**(총 103그룹 중 10 완료, gap_remaining.json 참고 — 단 aid 오류/기완료 그룹 있어 검증 필요: 스피드·티아라·에이티즈 등 제외). 검증: validate-data 오류 0, nbsp 정규화.
- [완료][admin.js][index.html] **그룹명↔멤버명 동명 충돌 오태깅 수정(유니스)**. 사용자 제보: 그룹 유니스(UNIS) 영상이 다이아 멤버 유니스(Eunice) 카드에 뜸(편집엔 태그 없는데 카드엔 뜸 = with_members에 "유니스(다이아)"가 잘못 박힘). 원인: `_m2ParseTitle`의 `memberHit`(이름 역추론)이 제목의 평문 "유니스"를 다이아 멤버로 매칭해 콜라보 오태깅 → 카드 withQ가 끌어옴. 동명이인이 아니라 **그룹명==멤버명 충돌**이라 nameToGroups 동명이인 dedup(매칭 1명)에도 안 걸림. **수정**: `_atmNameIsGroup`(멤버명이 실존 그룹명과 같고 본인 그룹은 아님) 게이트 추가 — `_ATM_COMMON_KO_WORDS`·'메이'와 동일 원칙으로 역추론에선 해시태그 명시만 인정(그룹 자체 매칭 경로는 별개라 그룹 태깅 정상 유지). 일반 규칙이라 향후 그룹명=멤버명 충돌 전부 커버. 기존 오염분은 사용자 수동정리(재태깅 시 재발 없음). `_ADMIN_JS_VER 2026-08-24a→b`. 문법검사·스모크 통과.
- [완료][artists.json] 모모랜드 연우 인스타 추가(@chloelxxlxx, 사용자 제보).
- [완료][index.html][trace_week_rpc_migration.sql] **상태줄에 "흔적" 익명 합산 추가 — "이번 주 N명이 흔적 M개를 남겼어요"**. 그룹/멤버 카드 상태줄(presence "지금 N명 보는 중" + favcount "N명이 좋아해요"가 4초마다 번갈아 도는 pill)에 세 번째 항목 추가. 흔적=영상 하트(video_scraps)+이모지 리액션(video_reactions), 최근 7일. N=서로 다른 사람 수, M=총개수. 순위·개인귀속 없이 총량만(화력과시 불가=비경쟁 설계, Fable 5프레임 중 "익명 합산"). ⚠️흔적 테이블엔 group_ko 없고 video_url만 있어 클라 쿼리 불가 → **Supabase RPC 필요**(`get_group_trace_week`/`get_member_trace_week`, URL→yt_channel_videos.id 조인). 프론트는 `_showTraceCount`로 favcount와 동일 패턴 배선(RPC 없으면 error 잡혀 조용히 안 뜸=fail-safe). **SQL(trace_week_rpc_migration.sql)은 Supabase에서 실행 완료**(created_at 컬럼 보장+RPC 2개+grant, 2026-08-24). created_at을 새로 추가한 거라 과거 하트/리액션은 제외되고 지금부터 누적(과거가 이번 주로 뻥튀기되는 것 방지) → 초기엔 거의 안 뜨다가 쌓이면 표시됨. N>0·M>0일 때만 노출. 멤버 RPC는 members 컬럼 text[] 가정(jsonb면 주석대로 수정). 스모크 통과, 콘솔 에러 0.
- [완료][index.html] **발열 저감 Tier 1+2 (렌더 루프 최적화)**. 기존(30fps GPU 캡·hidden/영상/모바일카드 렌더 스킵·세그먼트다이어트·pixelRatio 2캡) 위에 추가. ⚠️제약: `controls.autoRotate` 상시 ON이라 화면이 늘 미세 회전 → 완전 정지 렌더중단 불가. **Tier 1**: 무거운 시각계산(연결선·행성 268개 유니폼·별 포인트)+실제 렌더를 렌더 fps로 묶음 — 예전엔 매 rAF(120Hz폰 초당 120회) 돌면서 그림은 30fps로만 그려 3/4 낭비(ProMotion서 CPU 루프 최대 4배↓). flyState/controls.update만 매 프레임 유지(드래그·관성감). NCT 링 회전은 실경과 기준 정규화(fps 달라도 속도 일정). **Tier 2**: 사용자가 2.5s 무입력(패시브 감상)이면 30→20fps, 입력 시 즉시 30 복귀(autoRotate가 느려 체감 없음). 시각변화 사실상 0. 효과는 기존 fps 계측(perf_fps GA)으로 실측 가능. 실기기 발열 확인 권장. 스모크·인터랙션 테스트 통과, 콘솔 에러 0. (Tier 3=pixelRatio↓/antialias off, Tier 4=idle시 autoRotate 정지+렌더중단은 보류.)
- [완료][groups.json] **부실 디스코 재구성 라운드2 완료 — 28그룹 + 앨리스, nbsp 전역정리**. batch A(11)+B(16)+앨리스 멜론 재구성으로 부실 established 그룹 대거 보강. batch B: 이달의소녀(5→24)·악뮤(5→21)·모모랜드(5→18)·에프티아일랜드(6→44)·2AM(6→21)·뉴이스트(6→18)·씨스타(6→13)·레인보우(6→13)·피프티피프티(5→20)·케플러(6→22)·프로미스나인(6→19)·동키즈(6→20)·저스트비(6→16)·아이즈원(5→9)·오메가엑스(5→14)·앨리스(4→9, album.htm 폴백). ⚠️**앤팀은 rebuild 제외**(batch3서 이미 한국반 First Howling으로 큐레이션됨 — rebuild하면 일본싱글 붙음). 일반필터: 일본반(kana·통상반/초회한정·Download Ver)·예능콜라보(스케치북·유스케·웹툰·킹덤)·OST·중복(에디션) 제외. 번호중복 정리(케플러 Kep1going On 리패키지→bare·피프티피프티 Sped Up ver 제거). **⚠️nbsp 버그 교훈**: 멜론 제목/트랙 공백이 raw U+00A0라 필터가 조용히 실패 반복 → node에서 U+00A0를 직접 타이핑하면 일반공백으로 들어가 no-op됨, **정규화는 반드시 코드포인트 매칭 확인 필요**. 전 데이터셋 discography 텍스트 nbsp 정규화(잔존 0 확인). **결과: 268그룹 100% 디스코 보유, 총 2,348장**. 일본판 처리는 사용자결정 A(제외), B(jp태그) 전환은 멜론캐시 영구보존이라 재스크랩 없이 가능(SS501·에프티아일랜드 등 회복대상). **남은 부실**: 씨스타류 "메인 완비·싱글만 빠짐"은 큐레이션 판단 필요, 감사 gap 노이즈(일본반) 다수는 정상.
- [진행중→해소][groups.json] batch A 상세(위 항목에 통합): 유키스(1→31)·업텐션(1→15)·빅톤(1→12)·SS501·애프터스쿨·투애니원·크레용팝·피에스타·크나큰·멜로디데이·티엔엑스, barefill 번호채움. 감사에서 나온 부실 established 그룹을 멜론 전면 재구성: 유키스(1→31)·업텐션(1→15)·빅톤(1→12)·SS501(3→12)·애프터스쿨(3→11)·투애니원(4→14)·크레용팝(4→11)·피에스타(4→8)·크나큰(4→10)·멜로디데이(2→10)·티엔엑스(4→12). barefill로 번호까지 채움(유키스 미니4~13+정규1~3, 업텐션 미니2~11 등). ⚠️**앨리스(aid 6966)는 이탈리아 가수 Alice 오선택**(Mi chiamo Alice 등) → 제외, 올바른 K-pop ALICE는 albumPaging 고장(1704756) 앞으로 album.htm 폴백 필요. dedup(투애니원 2nd mini 중복 등)·2NE1/SS501 정리. **일본판 처리 = 사용자 결정 A(제외)** — 단 **B(jp:true 태그 보존)로 언젠가 전환 예정**. ⚠️B 전환 시: 멜론 원본이 `~/Downloads/melon_solo_audit/cache`에 **영구 캐시**라 재스크랩 불필요, build 스크립트에서 jp 섹션 미제외+플래그만 붙이면 됨(+ index.html 표시 토글). SS501은 일본반(Lucky Days·Kokoro 7버전·Distance 등) 대량 제외했으니 B 전환 시 특히 회복 대상. **다음: batch B**(이달의소녀·아이즈원·에프티아일랜드·2AM·뉴이스트·씨스타·레인보우·악뮤·모모랜드·앤팀·피프티피프티·케플러·프로미스나인·동키즈·저스트비·오메가엑스 등 ~16 + 앨리스 aid수정). 검증: validate-data 오류 0.
- [완료][index.html][kpop_universe.css] **탐험 패널 Charts 가로 선반 전환 + 더보기 토글 제거**. Charts를 세로 2열 그리드+더보기(접기) 토글에서 가로 스트립(feed-strip)으로 전환 — 기념일·최근본 스트립과 일관되게. 더보기 토글은 "차트 카드가 Discovery를 아래로 밀어냄"을 막던 땜빵이었는데, 가로 선반이 한 줄 높이만 차지해 근본 원인이 사라져 토글·버튼·`fg-collapsed` CSS·`_wireFeedChartToggle`째 제거. 가로 스크롤은 `_enableDragScroll(chart)`로. 차트 카드는 고정폭(158px) 타일, 오른쪽 패딩으로 다음 카드 peek. 이제 기념일·최근본·Charts(가로) + Discovery(세로 무한)로 역할 분담 또렷. ⚠️레이아웃 감이라 실기기 확인 필요(카드폭 조정 여지). 스모크 통과(Charts 렌더 확인 포함), 콘솔 에러 0.
- [완료][artists.json] **인스타그램 누락채우기 18명 + 오등록 정정 12건(에이전트 검증)**. ①**그룹 내 일부만 인스타 등록된 1~3세대 그룹의 누락멤버 23명** 조사(나무위키 프로필+웹 교차검증) → 고신뢰 18명 등록(핫샷·모모랜드·다이아·미쓰에이·쥬얼리·펜타곤·구구단·스텔라 등). 없음 4명(휘영·문희준·광수·지현=개인 인스타 실제 없음), 애매 1명(스텔라 민희) 보류. ②**3~5세대 "소수만 인스타 등록" 그룹 60건 검증** → **12건 오등록 적발**(사용자 가설 적중, 오류율 20%): 교체 6(공유빈 화장품브랜드→s8_yubin·켄신 아버지테너→kenshin_i2o2·도희 사칭계정→nyangdohee·구한나→gusrudl·나영주 언니계정→yeongjooo_dodree·니코 ←니콜라스@앤팀 계정 오복사→sheisniko), 제거 6(정원=강아지계정·치키타=엄마계정·황은수=트위터팬계정·최태훈=미확인·박지후=소속사연습생계정·윤시윤=동명이인 배우 윤시윤). ⚠️함정: 나무위키 프로필 인스타도 오귀속 있음(니코·한해빈·민희 문서 등) → 웹 교차검증 필수. ⚠️사진은 Supabase라 레포서 "사진없음" 신호 못 읽음 → "소수만 등록" 그룹 전수검증으로 대체. 남은 인스타 채우기: 4~5세대 누락 210명(신인 다수 개인계정 없음이 정상 — 억지로 안 채움). 검증: validate-data 오류 0.
- [참고][도구] **그룹 디스코 부실 감사(json vs 멜론 앨범수) 완료** — 원더걸스(3장→13 재구성)가 드러낸 "부실 디스코" 전수점검. gap≥2 부실의심 155그룹이나 대부분 노이즈(멜론이 일본싱글·디지털싱글·OST까지 세거나 aid 오선택). **json≤6 강력의심 50그룹** 중 established 재구성대상: 유키스(json1)·업텐션(1)·빅톤(1)·SS501·애프터스쿨·앨리스·투애니원·크레용팝·피에스타·크나큰·이달의소녀·악뮤·모모랜드·에프티아일랜드·2AM·뉴이스트·씨스타·레인보우·멜로디데이 + 4세대 앤팀·피프티피프티·케플러·프로미스나인·동키즈·저스트비·아이즈원·오메가엑스·티엔엑스. 리포트 scratchpad(세션소실). **다음: 이 그룹들 멜론 재구성**(디스코전무 라운드2 규모). ⚠️멜론 albumPaging이 일부 aid서 빈응답(원더걸스 217764) → album.htm(최근~15) 폴백 필요.
- [완료][connections.json] **멤버 연결선 누락 280쌍 채움(감사→일괄)**. 별들 사이 연결선은 그룹 내 완전그래프가 의도인데, 둘 다 active인데 빠진 쌍 280개/15그룹 발견. ①**겸임(다중소속) 멤버의 세컨더리 그룹 누락**(윈윈@엔시티127·강예서/마시로@케플러·임나영@프리스틴·성민@티오원) — 연결선이 주소속 기준으로만 생성돼 2번째 그룹에서 안 이어져 있었음. ②**완전부재 그룹**(아이칠린·엔싸인·고스트나인·메이브·루셈블 등, 아이덴티티 135쌍 포함). ③블락비 태일(엔시티 태일은 영구제명→데이터 세션서 숨김). ⚠️선 그리기 코드는 `conn.group` 없으면 주소속끼리만 매칭 → 신규 항목은 전부 `group` 필드 명시(겸임/해체그룹 세컨더리 필수). 4646→4926항목. 검증: 재감사 잔여누락 0, validate-data 오류 0, 스모크 통과. 탈퇴자(그룹내 active=false) 1,138쌍은 정상적으로 선 없음(의도대로).
- [완료][groups.json] **bare 앨범 번호 채우기 118장 + 원더걸스 재구성 + nbsp 전역정리**. ①번호없이 `정규`/`미니`로만 있던 앨범에 나무위키 번호를 **발매일+제목 이중매칭**으로 채움(118장/36그룹): 엔하이픈·에이티즈·원어스(미니1~10)·아이들·앤팀·카드·씨아이엑스·신화·쥬얼리·god·젝스키스·NRG·SS501·구구단 등. 나무위키 레이아웃 2종 대응(형식1 `제목→라벨→날짜`, 형식2 `라벨→제목→(날짜)`). ⚠️가드: json 제목에 이미 `N집`이 박혀 있고 나무위키와 다르면 오정렬 위험으로 **자동적용 안 함**(젝스키스 "Road Fighter -2집"↔나무위키 정규3집 등 수동 플래그), 그룹내 번호중복 배정도 스킵(레드벨벳 Perfect Velvet 중복·원위 정규1 충돌 → 다음에 수동). bare 잔여 437→355(샤이니31=제목내장 정상, 나머지는 나무위키 파싱0/제목불일치). ②**원더걸스 3→13장 재구성**(사용자 지적 — 2008년에서 멈춰있었음). ⚠️이 aid(217764)는 albumPaging이 빈응답+album.htm은 최근15개만 노출이라 2007 데뷔싱글(Irony·Tell Me)은 멜론서 못 받음 → 15개+기존 정규1 병합(정규1 The Wonder Years·정규2 Wonder World·정규3 REBOOT·미니1 Wonder Party + Nobody·So Hot·Why So Lonely 등). ③⚠️**멜론 제목/트랙 공백이 raw U+00A0(nbsp 문자)** — `dec()`가 `&nbsp;`엔티티만 변환하고 raw 문자는 안 바꿔서 이번에 추가된 제목 다수가 nbsp 보유(필터 반복실패 원인) → discography title/titleTrack/tracks 전역 정규화 **431필드**. 검증: validate-data 오류 0, 스모크 통과.
- [완료][groups.json] **디스코 전무 그룹 46개 전량 신규 추가 → 268그룹 100% 디스코 보유**. 디스코그래피가 아예 비어있던 46그룹을 멜론(커버·트랙·발매일·타이틀곡) + 나무위키(번호) 파이프라인으로 신규 구성. 디스코 보유 그룹 **222→268(전 그룹)**. 3배치: ①레거시 걸그룹(걸스데이18·헬로비너스10·레이디스코드12·스텔라10·스피카8·네이처8) ②스피드·히스토리·소나무·트라이비·라잇썸·아이칠린·고스트나인·티에프엔 ③프로젝트/신인 32(아르테미스·메이브·엔싸인·유앤비·JBJ·유니티·원더나인·티에이엔·파우·루네이트·나인아이·엘즈업·B.D.U·배틀·슈가·메이딘 + 2025~26 신인 키라스·유스피어·베이온 등 16). ⚠️함정: ①동명이인 aid(스텔라·네이처·스피드·TFN·배틀·슈가·JBJ·유니티·유앤비·파우 등 후보 여럿 → 앨범목록으로 그룹 본체 확정, 슈가=BTS슈가/배틀=외국밴드 오선택 위험), ②멜론 제목 공백이 **non-breaking space( )**라 제목 문자열/정규식 필터가 조용히 실패(TFN·엔싸인 일본판/라이브 이걸로 놓칠 뻔 → 공백정규화 후 재필터, 발매일 기준 제외 병행), ③시리즈 앨범 부분일치로 자동 번호매칭 오염(걸스데이 EVERYDAY #4/#5→미니1 등) → 나무위키 발매일 매칭으로 수동 확정, ④나무위키가 "미니 N집" 안 쓰는 그룹(스피드·고스트나인·TFN·유니티·엘즈업 등 EPISODE/시리즈 네이밍)은 bare 유지(추측 금지), ⑤장식괄호(`<Dall>`·`〈Pre1〉`)·클린판(`(Clean)`)·컴필(`(Singles)`) 정리. 컬래버/타아티스트프로젝트/OST/서바이벌커버/일본판/라이브앨범 제외. 검증: validate-data 오류 0, 스모크 통과. 스크래퍼는 scratchpad(세션 소실).
- [완료][kpop_universe.css][index.html][tests/interaction.test.js] **모바일 카드 UI 3건 수정**(Fable 진단→클로드코드 수정). ①**상단 잘림(아이폰14 PWA)** — 카드 기본 100%(bs-full) 상태가 노치 여백을 무시해 상단 핸들이 먹히던 문제 → `#mob-sheet`/`#mob-card-stack`/`#conn-sheet`의 bs-full에 `padding-top:env(safe-area-inset-top)` 추가하고 -inner max-height에서 차감(box-sizing:border-box). ⚠️실기기(PWA) 확인 필요. ②**탭바 미복귀** — `_wireSheetTabbarHide` 방향 감지가 iOS 바운스에서 반전돼 탭바가 숨김 고착 → scrollTop 유효범위 클램프(바운스 무시)+최상단<40px 무조건 표시(안전망)+`openMobSheet` 진입 시 `_lastScroll=0` 리셋. ③**카드 50% 고착(빠른 연속 오픈)** — 열림 판정을 bs-open 클래스(rAF 지연) 대신 `_cardStack` 길이로 바꿔 오분기 차단 + 스택 슬라이드업에 재진입 가드(`_stackAnimSeq`/`_stackAnimTimer`)로 뒤늦은 콜백이 새 애니를 초기화하는 걸 방지(close/pop에도 무효화). tests/interaction.test.js에 버그C(탭바)·버그D(연속오픈) 회귀 케이스 추가 — 인터랙션+스모크 전부 통과.
- [완료][groups.json] **번호 충돌 그룹 재구성 — 감사 ERROR 실제 오류 4그룹 정정**. 감사 ERROR 18건(번호불일치 11·제목중복 7)을 나무위키+멜론 대조로 전수 triage: 대부분 파서 부분일치 오탐(갓세븐·다크비·에이스·오마이걸·빅뱅 등, 예 "Dear OHMYGIRL"→"OH MY GIRL")이거나 정상적 별개 발매(샤이니 일본 정규+동명 싱글, 아이브 선공개싱글+EP)로 확인. **실제 오류 4그룹만 수정**: ①**소녀시대** — "훗(Hoot)" 미니3집+bare미니 한/영 중복 → bare 제거, 미니3집 제목 '훗 (Hoot)'로 정리. ②**엔시티 드림** — "맛(Hot Sauce)"+"Hot Sauce"(영문) 같은 날 중복 → 영문판 제거. ③**2PM** — "Still 2:00PM"이 정규1집으로 잘못 등록(실제 미니 1집, 발매일도 2010.09.09→2010.10.11이 맞음, 기존 날짜는 'Nori For U' 싱글 날짜였음) + 진짜 정규 1집 "1:59PM"(2009.11.03, 타이틀 Heartbeat) 누락 → 멜론서 재수집해 추가. ④**티아라** — 날짜·번호·**트랙목록까지 뒤섞인 심각한 오염**(예 "And&End"인데 트랙은 REMEMBER 내용, "John Travolta Wannabe"인데 트랙은 Funky Town 내용, 멜론에 없는 유령 "NUMBER NINE") → 나무위키 번호(미니 1집 없음=멤버교체로 미니2집부터, 각주 확인)에 맞춰 정규1+미니2~13 = 13장 멜론 전면 재구성. 검증: validate-data 오류 0 / 모바일 스모크 통과 / 재감사 잔여 ERROR 3건은 전부 파서 부분일치 오탐(엔시티드림 "DREAMSCAPE"≠"DREAM( )SCAPE" 별개앨범, 티아라 "Again 1977"→"Again", 소녀시대 정규1→다시만난세계). 스크래퍼는 scratchpad(세션 소실). 그룹 앨범 1,737→1,737장(순증 없음, 2PM +1·티아라 -1).
- [완료][index.html][artists.json] **키워드 3건 + 인스타 1건**(사용자 요청). ①동기화 영상 제외 키워드에 "님과 함께2" 추가(_JUNK_TITLE_KEYWORDS_GLOBAL). ②카드 영상 cover 탭 제외 키워드에 "_original", "original per", "coverstar" 추가(_coverExclude). ③러블리즈 Kei 인스타 lvlz_kei → _flower_kei 교체(소속사성 계정 → 개인 계정). sw v71→v72.
- [남은 과제][2026-08-24 기준] **①그룹 디스코 감사 ERROR 22건** — 대부분 일본 발매반 동명 앨범/제목 부분일치 오탐으로 확인됨. 실제 미해결은 비투비 Hour Moment(나무위키에 근거 없음) 1건. **②나무위키 파싱 실패 21그룹 / 번호 충돌 47그룹** — 번호 체계를 그룹 단위로 수동 재구성해야 하는 백로그(씨엔블루·에이오에이·에이프릴처럼 한 그룹씩 대조하는 방식으로 처리 가능). **③나무위키 링크 보류 43명** — 단독명이 동음이의 문서라 안전한 대상 URL을 못 찾은 건. **④멜론에 없어 보류된 그룹 앨범 100장** — 상당수는 NCT 통합 앨범이 유닛 문서에 실린 것으로 유닛 디스코에 안 넣는 게 맞음. **⑤솔로 디스코 번호 미확정 236장**(`미니`/`정규` 로만 기록) — 나무위키에 해당 앨범이 없어 번호 근거를 못 찾은 것.
- [완료][artists.json][tools/namu_link_sweep.mjs] **나무위키 링크 전수 점검(1,714명) + 143건 정정**. ⚠️**최초 실행 결과는 통째로 틀렸음** — `curl` 에 `-L`(리다이렉트 추적)이 없어서 "키(샤이니) → KEY(SHINee)" 처럼 정본 문서로 리다이렉트되는 링크가 74바이트 응답으로 와 "없는 문서"로 판정됐음. 판정률이 뒤로 갈수록 2%→16% 로 늘어 스로틀링으로 오해했다가 응답 본문을 직접 열어보고 리다이렉트인 걸 확인. `-L` 을 넣고 표시된 326명을 재검증하니 **정상 140 / 실제 깨짐 143 / 동음이의 위험으로 보류 43**. 회수한 143건 반영(예: `재한(오메가엑스)` → `재한`). 나무위키를 쓰는 다른 스크립트(group_disco_audit·group_disco_fill·melon_solo_reflect)에도 `-L` 반영. 전수 확인은 문서 앞 60KB 만 받아 판정함(1,714명 통짜 수신은 1.7GB).
- [완료][groups.json] **레인보우 "Over The Rainbow" 종류 정정**(정규 1집 → 10주년 기념 싱글). 나무위키 파서가 바로 앞 항목(Rainbow Syndrome Part.2 정규 1집 파트 2)의 표기를 집어온 오매칭이었음.
- [이슈][tools/group_disco_fill.mjs] **일본반 필터가 빠진 채 한 번 적용된 사고**. `markSecondary` 로 리팩터링하면서 `parseNamuAlbums` 반환문의 `!n.jp && !n.secondary` 필터를 빼먹어 씨엔블루 Stay Gold(일본 정규)가 국내 정규 6집으로 들어갔음 — 되돌리고 필터 복구 + 주석 경고 추가. 같은 라운드에 적용된 27장은 발매일 위치로 한국 음반 구간 소속인지 전부 재확인함(레인보우 1건만 오류였고 위에서 정정).
- [완료][groups.json][tools/group_disco_fill.mjs] **Round 3 후속 — 파서 보강 + 3그룹 재구성 + 추가 27장**. ①나무위키 날짜가 `2010/06/09` 슬래시 형식인 문서(인피니트·뉴이스트 등)를 아예 못 읽고 있었음 — 구분자 확장으로 파싱 실패 31→21그룹. ②구간 판정(일본 음반 분리)을 "첫 역행에서 자르기"로 두면 에스파처럼 목록 앞머리 SMCU 묶음 때문에 한국 음반 전체가 잘려나감 → 1년 이상 크게 되감기는 지점만 자르도록 수정. ③⚠️**"날짜만 맞으면 채택" 폴백 때문에 씨엔블루 Stay Gold(일본 정규)가 국내 정규 6집으로 들어갔다가 되돌림** — 제목 일치 필수로 변경. 변형판(… Ver./Edition) 회피 + 정규/미니인데 2곡 이하면 싱글 오매칭으로 보고 제외하는 가드도 추가(오메가엑스 "Dream" 1곡짜리 미니 3집 사례). 결과 드림캐쳐 1→13장, 티아라 6→14장 등 27장 추가. 그룹 앨범 **1,706 → 1,737장**.
- [완료][groups.json] **에이프릴·비투비·에이오에이 디스코 전면 재구성**(감사 ERROR 실제 수정 대상 3건). ①에이프릴 — DECISIONS 항목 해결: Prelude(미니 3집)·the Ruby(미니 6집) 추가, Eternity 미니3→4집·Da Capo 미니6→7집 정정, Hello Summer 는 미니 4집이 아니라 **썸머 스페셜 앨범**, 데뷔작 Dreaming 발매일 2015.11.25→2015.08.24, 나무위키·멜론 어디에도 없는 유령 "Dreaming"(2021.01.11) 제거. ②비투비 — Hour Moment 는 미니 12집이 아니라 **스페셜 앨범**, Wind and Wish 미니13→12집, 누락돼 있던 BTODAY(미니 13집, 2025) 추가. ③에이오에이 — 제목·번호·발매일이 전반적으로 어긋나 있었음: Angel(2012)은 실제 "Angels Story" 싱글, Like a Cat 은 한국 미니 2집 **사뿐사뿐**, 단발머리 미니3→1집, Heart Attack 미니5→3집, Good Luck 미니6→4집, Angel's Knock 미니7→**정규 1집**, 누락된 BINGLE BANGLE(미니 5집)·NEW MOON(미니 6집) 추가. ⚠️수정 지시서를 제목 부분일치로 찾다가 "Angel" 이 "Angel's Knock" 에 먼저 걸려 두 항목이 뒤바뀌는 사고가 있었음(완전일치+발매일 한정자로 수정).
- [완료][groups.json][tools/group_disco_fill.mjs] **Round 3 — 그룹 디스코 공백 채우기(165장 추가 + 번호 425장 채움)**. 나무위키 한국 음반 구간의 정규/미니를 정본으로 json 에 없는 앨범을 찾고, 멜론 그룹 앨범 목록(대표아티스트 aid == 그룹 aid)에서 같은 앨범을 찾아 커버·트랙·타이틀곡까지 채움. 그룹 앨범 **1,541 → 1,706장**. 세븐틴 정규 1집 "FIRST 'LOVE&LETTER"(2016) 처럼 대표작이 통째로 없던 그룹이 많았음(틴탑 +10, 에이핑크 +9, 달샤벳 +9, 이펙스 +9 등). 번호 없이 `미니`/`정규` 로만 있던 앨범 425장에 나무위키 번호를 채움(방탄소년단·세븐틴·트와이스·소녀시대 등 표본 검증 완료). ⚠️**나무위키가 같은 앨범을 두 번 싣는 문서(시리즈 박스 + 음반 목록)** 때문에 한쪽이 "누락"으로 잡혀 중복 16장이 추가됐다가 제거함 — 번호는 기존 항목으로 이관하고, 스크립트엔 json 전체 재대조 가드 추가. 멜론에 없어 보류 21장(대부분 NCT 통합 앨범이 유닛 문서에 실린 것으로, 유닛 디스코에 안 들어가는 게 맞음). 감사 재실행 시 유령앨범의심이 72→110 으로 늘어나는데, 이는 번호를 채운 앨범들이 새로 검사 대상이 된 것이지 데이터가 나빠진 게 아님(대부분 "Golden Child 2nd Mini Album [奇跡 (기적)]" vs 나무위키 "기적" 같은 표기 차이).
- [완료][artists.json][groups.json] **앨범 커버·트랙 누락 전량 회수**(사용자 확인 요청에서 발견). ①멤버 디스코 커버 886장 누락 — 구형 앨범은 `/cm/album/`, 신형은 `/cm2/album/` 이고 뒤에 `?` 가 없는 경우가 있어 목록 정규식이 놓쳤음. 상세페이지 `og:image` 로 전량 회수. ②그룹 디스코 중 트랙이 비어 있던 기존 25장(라이브·컴필·리패키지 위주) 중 23장 회수, 남은 2장(엑소 Mama, H.O.T. Age of Peace OST)은 멜론 목록에 없음. **현재 멤버 3,722장·그룹 1,706장 거의 전량이 커버·트랙(곡번호/곡명/타이틀곡 플래그)·타이틀곡 보유**.
- [완료][groups.json] **발매일 표기 `yyyy.mm.dd` 로 통일**(769장). 하이픈/점이 섞여 있던 기존 데이터 문제 — 문자열 정렬 시 `-`(45) < `.`(46) 이라 같은 달 안에서 순서가 뒤집히고 있었음(정렬 재조정 155그룹). `index.html:7462` 가 `releaseDate.replace(/./g,"-")` 로 점을 하이픈으로 바꿔 쓰는 걸 확인하고 점 형식으로 맞춤. 멜론에 월까지만 있는 옛 앨범 2장(문희준)은 `2002.07` 그대로 둠.
- [완료][artists.json] **솔로 디스코 Round 2 반영(싱글/OST만 보유 235명, 800장)**. 전량 싱글·OST라 번호 근거가 필요 없어 검토 0건. discography 보유 멤버 **374 → 609명**. 나무위키 링크 오류 33건 추가 정정(오늘 누적 67건). ※ `(그룹명)` 형식 링크를 가진 멤버가 아직 955명 있는데, 이번엔 디스코 대상자만 검사한 것이라 나머지도 깨진 링크가 섞여 있을 수 있음 — 전수 점검은 별도.
- [완료][groups.json] **번호 충돌 그룹 3개 수동 재구성**(감사 ERROR 후속). ①씨엔블루 — "Can't Stop" 이 실제 미니 5집인데 정규 3집으로 등록돼 이후 번호가 전부 밀려 있었음(6장 정정). "TRIGGER" 는 나무위키 한국 음반 목록에 없어(일본반 추정) 번호 없이 `미니` 로. ②크래비티 — "LIBERTY: IN OUR COSMOS" 는 나무위키 기준 **정규 1집 PART 2** 인데 미니 4집으로 등록돼 NEW WAVE 와 충돌(4장 정정). ③여자친구 — "Sunny Summer" 는 번호 없는 **여름 미니** 인데 미니 7집으로 등록돼 Fever Season 과 충돌(3장 정정). ⚠️감사기 중복판정을 접두사→**전체 문자열 일치**로 수정 — "정규 1집 PART 2"·"정규 3집 Act 1/2" 같은 파트 앨범을 중복으로 오판했었음.
- [참고][groups.json] **그룹 디스코 커버리지 공백이 생각보다 큼**. 감사의 "누락의심 271건"은 대부분 오탐이 아니라 실제 공백 — 세븐틴 정규/미니 13장뿐(정규 1집 "FIRST 'LOVE & LETTER" 없음, 게다가 전부 번호 없는 `미니`/`정규` 타입), 에이핑크 5장뿐, 틴탑·달샤벳·티아라 각 8~10장 누락. 채우려면 앨범별 멜론 상세(커버·트랙) 재수집이 필요해 별도 라운드 규모(후보 228장 / 약 100그룹). 남은 감사 ERROR 22건 중 실제 수정 대상은 **에이오에이(번호·발매일 둘 다 어긋나 전면 재구성)·에이프릴(기존 DECISIONS 항목)·비투비 Hour Moment(근거 부족)** 3건뿐이고, 나머지(아이브 3·샤이니 2·빅뱅·오마이걸·베리베리·에이비식스·2PM 등)는 **일본 발매반 동명 앨범**이거나 제목 부분일치 오탐으로 확인됨.
- [완료][artists.json][tools/] **솔로 디스코 오디트 재수집 + 반영(회사 세션)**. 8/23 오디트의 페이지네이션 언더카운트가 실재로 확인됨 — 멜론 `albumPaging` 의 `startIndex` 는 페이지 번호가 아니라 **행 인덱스(1-base)** 라 1페이지만 읽히고 나머지가 통째로 누락됐던 것. 0건 나올 때까지 순회하도록 고쳐 재수집하니 **솔로 보유(디스코 없던 멤버) 172명 → 342명**(정규/미니 보유 51→107). Round 1(정규·미니 보유 107명) 반영 완료 — discography 보유 멤버 **267 → 374명, 앨범 1,436장 추가**. 사용자 결정으로 "번호 근거 없는 것도 번호 없이 반영"(236장은 `미니`/`정규` 로만). 스크립트: `tools/melon_solo_audit.mjs`(수집) / `melon_solo_reflect.mjs`(반영안+나무위키 대조) / `melon_solo_apply.mjs`(적용). 파이프라인 함정은 [[project_kpop_solo_disco_pipeline]] 메모.
- [완료][groups.json][tools/group_disco_audit.mjs] **그룹 디스코 감사 신설 + 번호 오표기 23장 수정**. 8/23 의 "멜론 전체 diff"(오탐 1,922건)와 달리 **타겟 검출**로 전환 — 내부 정합성(번호중복·제목중복·번호불연속·동일발매일) + 나무위키 대조(번호불일치·유령앨범·누락). 222그룹 1,541장 스캔. 수정: 갓세븐 9장(MAD 가 정규2집으로 등록돼 이후 번호가 전부 밀려 있었음), 2PM 3장(데뷔 3장이 실제로는 싱글), 티아라 3장, 비원에이포 3장, 빅뱅·에스에프나인·애프터스쿨 등. ⚠️**부분 재번호는 매칭 안 된 앨범의 옛 번호와 충돌**하므로(실제로 8건 발생 → 되돌림), 적용 전 그룹 내부 충돌 시뮬레이션 통과분만 반영함. 남은 ERROR 30건(제목중복 8·번호불일치 22)은 수동 재구성 필요 — 특히 **씨엔블루·에이오에이·비투비·여자친구·크래비티는 재번호 시 충돌**이라 통째 재정리 대상. 에이프릴은 기존 DECISIONS 항목대로 전면 오염 확인(Prelude·the Ruby 누락, Eternity·Da Capo 번호오표기, Dreaming 중복) — 감사기가 독립적으로 동일 결론.
- [완료][artists.json] **나무위키 링크 오류 34건 정정**. `이름(그룹명)` 형식이 실재하지 않는 문서를 가리키던 것(이창민(2AM)·렌(뉴이스트)·이효리(핑클) 등). ⚠️단순히 `이름` 단독으로 바꾸면 안 됨 — 렌·JR·Kei·소유·강타 등 12명은 단독 문서가 **동음이의 문서**였음. 그룹명 언급 + 동음이의 아님을 검증해 28건은 단독명, 6건은 `이름(가수)`/`이름(VIXX)` 로 회수. 회수 실패 6명(JR·유니스·Kei·민·지코·강타)은 링크 그대로 두고 그 문서로 확정됐던 앨범 번호만 제거.
- [완료][artists.json] **인스타 4명 교체/추가**(사용자 제보) — 효린·혜리·새봄은 소속사 `.official` 계정이었어서 개인 계정으로 교체, 홍석(펜타곤) 신규 추가.
- [완료·실기기확인대기][index.html] **해체/비활동 그룹 밝기 완화(원더걸스 등)**. ①비활동 멤버 별 밝기 0.22→0.34(2758) — 전원 비활동 그룹은 별자리가 너무 어두워 안 보이던 것. ②disbanded 행성 딤 완화(1986-87: col 0.55→0.72, alpha 0.8→0.88) — 어두운 색(원더걸스 마룬)에 딤 겹쳐 거의 안 보이던 것. 과거감 유지하며 "모여서 보이게". ⚠️전역 시각 상수라 배포 전 게이트 3번(실기기) 확인 필수.
- [보류][index.html] **#3 행성 비침(다른 행성이 뒤로 비쳐 보임)** — 원인은 fill의 depthWrite:false(1990)로 행성끼리 depth 오클루전이 없는 구조. 정석 해법=alpha-test+depthWrite(불투명 몸체만 depth 써서 뒤 행성 가리고 투명 중심은 별 보이게 유지) 또는 포커스 시 비포커스 행성 딤. 셰이더 depth 변경이라 회귀 위험 큼(과거 사고 이력 많은 영역) → 실기기 보며 전용 패스로. 블라인드 변경 금지.
- [완료][admin.js] **메이 인퍼런스 게이트(리센느 메이 오염)**. 메이 동명이인 3명(리센느 en=May·세이마이네임 en=Mei·체리블렛 en=May[로스터 미등록])+A2O MAY 그룹명 충돌. 한글 "메이"가 인퍼런스 게이트에 없어 체리블렛 메이 직캠·"5월/May" 월표기가 리센느 메이로 추론됨(en May는 이미 len<=4로 게이트). _ATM_COMMON_KO_WORDS에 메이 추가 → 인퍼런스는 해시태그/그룹문맥만, 자체채널·리센느문맥은 유지. 기존 오염(90건 중 ~55): A2O 등 사용자 수동정리, 체리블렛 메이는 로스터 등록 필요(데이터). _ADMIN_JS_VER→2026-08-24a.
- [완료][index.html] **디스코 "부가 앨범까지 포함" 마스터 토글(옵션1+B)**. OST·피처링과 기타 앨범을 따로 두 번 체크해야 하던 불편 해소 — 케밥 ⋮ 인클루전 그룹 맨 위에 마스터 토글 신설(둘 다 있을 때만 노출). 한 번 탭으로 extrasIncluded+showingAll 둘 다 on/off, 개별 토글은 유지(둘 다 켜지면 마스터 자동 ✓). 라벨을 세그먼트 '전체'(=종류)와 안 겹치게 '부가'(=범위)로 분리 — 세그먼트 전체=정식 종류 전체, 부가=OST·피처링·기타. .
## 2026-08-23
- [완료][artists.json][데이터] **나무위키 링크 66명 + 인스타 43명 채우기**. 인스타 등록·나무위키 누락 11명(프리스틴·스피드·제국의아이들·엔하이픈 에반=前희승·현아·이은상) + 링크전무 55명(TFN9·고스트나인9·A2O·메이브·파이브돌스·슈가·유앤비·JBJ·TAN 등). 각 문서 웹검증 후 본명/활동명 정식문서 연결. 개인문서 없어 제외6(파이브돌스 지현·배틀 4인·슈가 이하린).
- [완료][groups.json] **TXT 디스코 한/영 중복정리**. 투모로우바이투게더 앨범이 한국어번역명/영문명 이중등록된 것: 영문 껍데기 5개 제거(The Dream/Chaos/Name Chapter), 꿈의 장:MAGIC 타입 EP→정규(정규1집), The Star Chapter SANCTUARY/TOGETHER→별의 장:… 정본명+트랙/커버 채움. 16→11장.
- [완료][groups.json][artists.json][PRINCIPLES.md] **앨범종류 EP→미니 통일**(사용자 지침, groups 607+artists 6). 멜론이 미니앨범을 [EP]로 등록해 섞였던 것. 앱은 이미 EP를 미니버킷 처리하나 원본 통일. PRINCIPLES 운영규칙 명문화.
- [진행중][데이터] **멤버 솔로 디스코 멜론 전수 오디트(읽기전용) 완료 — 반영은 진행중**. 멜론 그룹→멤버aid→앨범상세 파이프라인 확립(핵심: 멤버 개인페이지엔 그룹앨범도 섞여 나와 groups.json 제목대조 실패 → **앨범 상세 대표아티스트 크레딧 aid==멤버aid**가 유일한 솔로판별 신호). 255/256 그룹 스캔(NRG만 실패). **솔로 보유 172명 발굴**(디스코 없던 멤버 중): 정규/미니 보유 51명(나무위키 번호대조 필요), 싱글만 121명. 리포트: `~/Downloads/솔로디스코_오디트_보유자172.txt`. 탈퇴멤버(제시카·소희 등 ~95명)는 멜론 그룹페이지에 없어 unresolved→개별검색 별도. 스캔중 멜론 IP스로틀링 겪음(빈페이지→재시도 로직으로 복구). ⚠️**중대 주의 — 오디트 앨범목록은 언더카운트**: 멜론 `artist/album.htm` 기본 페이지가 **최근 ~30개만** 노출해서(다작 아티스트는 최근 싱글이 옛 정규/미니를 밀어냄) 스캔이 옛 메인앨범을 놓침(예: 정은지 기본30 vs 전수79, 옛 미니 Dream·혜화·공간 등 누락). **보유자 172명 '누가 솔로 있나'는 유효하지만, 각자 앨범수/목록은 불완전.** 반영 시 반드시 멤버별 `albumPaging.htm?startIndex=1..&pageSize=25` **전수 페이지네이션 재수집 + 크레딧필터(대표아티스트 aid==멤버aid) + 나무위키 번호대조**. **다음**: 172명 이 방식으로 재수집→커버검증→artists.json `discography` 반영(무인 대량반영 금지, 검토 라운드 방식 — 라이브 사이트 직배포라 불완전데이터 주의). 파이프라인 상세는 [[project-kpop-solo-disco-pipeline]] 메모.
- [참고][데이터] 그룹 디스코 갭 리포트 `~/Downloads/그룹디스코_갭_리포트.txt` 생성했으나 1922건 대부분 오탐(멜론은 그룹 전 싱글/일본반 노출, groups.json은 큐레이션이라 갭≠누락). TXT처럼 눈에 띄는 건 타겟 검출 필요, broad diff는 신호 약함.
- [보류→완료][index.html] **디스코 앨범 포함 토글 UX 재검토**(사용자 제보 — "OST·피처링 포함"과 "기타 앨범 포함"을 따로 두 번 체크해야 해서 불편). 현재 ⋮ 메뉴에 독립 토글 3개(OST·피처링/솔로만/기타앨범). 취향 갈리는 결정이라 보류 — 옵션: A)OST·피처링+기타를 "전체 포함" 하나로 병합(간편, 세밀제어 상실) B)현행 유지 + 맨 위 "모두 포함" 마스터 토글 추가(추천 — 세밀함 유지 + 한방) C)세그먼트 종류칩에 흡수(구조 큼). ~~사용자 A/B/C 선택 후 구현.~~ → **해소(2026-08-24)**: 사용자가 **옵션 B** 선택, 케밥 ⋮ 인클루전 그룹 맨 위에 마스터 토글로 구현 완료(2026-08-24 "디스코 '부가 앨범까지 포함' 마스터 토글(옵션1+B)" 항목 참고).
- [완료][index.html] **디스코 정렬 케밥, 카드 빈 곳 탭하면 닫히게**(사용자 요청). closeDiscogSortMenu의 document 클릭 리스너가 카드(#tt/#gc)의 stopPropagation에 막혀 카드 안 탭엔 안 닿던 게 원인. 카드 클릭 핸들러에 _closeOpenSortMenus() 추가 — 케밥/메뉴항목은 자체 stopPropagation이라 카드까지 안 와서 그 외 빈 곳 탭만 메뉴를 닫음. 영상 그리드 정렬메뉴(.gc-ch-sort-menu)도 커버. sw v67→v68.
- [보류][index.html] **별 발광(글로우) + 연결선 포커스 강조 — Fable 검토 후 대기**. ③글로우: 멤버 별에 halo 레이어(bloomHaloTex) 이미 있으나 별 커진 만큼 상대적으로 옅어 밋밋. Fable의 "포커스일 때만 발광(홈 불변)" 방향 채택하되, 멤버 별이 regular/sparkle 전역 Points라 "그 행성만 스왑"은 불가 → 정점별 focus 플래그 + uFocus 유니폼 셰이더 수정 필요(Fable의 "제로코스트 텍스처 스왑"은 오판). ④연결선: 선마다 baseOp(0.15)+grpKo 있어(2203) 포커스 그룹 선만 opacity/굵기 상향 가능(전역 상향 금지—홈 거미줄화). 게이트: setFocus/focusGroupKo 재사용. 배경 별밭(위 항목) 실기기 확인 후 착수.
- [완료][index.html] **배경 별밭 흰 사각형·검은 점 아티팩트 수정**. Fable 감사 검토 중 원인 재규명: 멤버 별(makePointsMat)은 map+additive+depthWrite:false 정상인데, **배경 잔별(makeStarField)**이 map 없음(→THREE가 흰 사각형으로 렌더) + depthWrite 기본값 true(→행성 앞에 온 별이 뒤 행성 additive 글로우를 가려 검은 점). soft 원형 텍스처(_bgStarTex)+depthWrite:false 부여로 둘 다 해소. NormalBlending 유지(홈 밝기/산만함 불변), gl_PointSize 무관(iOS 안전). Fable는 멤버 별을 지목했으나 코드 검증상 배경 별밭이 진짜 원인이었음. 실기기 확인 필요(배포전 게이트 3번). `sw v66→v67`.
- [완료][admin.js] **2PM·2AM 시간표기 오매칭 게이트**. 라디오/방송 시간표기("⏰ RYO : 2PM-4PM")가 그룹 2PM으로 매칭돼 "원곡: 2PM" 오태깅까지 발생(Fable/사용자 제보). `_GROUP_TITLE_CONFLICT_EXCLUDE`에 2PM·2AM 추가 — 숫자+AM/PM이 다른 시각과 대시/물결로 이어진 시간범위(\d[AP]M[-~]\d[AP]M)일 때만 그룹 매칭 제외. 진짜 곡 "2PM - My House"(대시 뒤 두번째 시각 없음)는 안전. 재발 방지(적용하려면 재태깅). `_ADMIN_JS_VER→n`, `sw v65→v66`.
- [완료][admin.js] **한글 흔한단어 이름 인퍼런스 게이트 — 베이비→아워벌스데이 오추론 수정**. 사용자 제보: 그룹 베이비돈크라이 영상이 아워벌스데이 멤버 '베이비'로 끌려감. 원인: `memberHit`(제목→그룹 역추론)의 흔한단어 게이트 `_atmNameNeedsCtx`가 라틴 전용이라 한글 '베이비'(baby, 노래·가사에 흔함 + 베이비돈크라이/베이비몬스터/베이비복스의 부분문자열)를 평문 매칭. `_ATM_COMMON_KO_WORDS`(베이비·하루·하늘·바다·봄·여름·겨울·별·사랑·달·천사·하트) 신설 → 단일음절 이름처럼 인퍼런스에선 해시태그만 인정. 자체 채널 태깅(_atmMatchesMember, 그룹 확정 문맥)은 평문 유지라 영향 없음. 적용하려면 자동태깅/재태깅 재실행.
- [완료][admin.js][index.html] **영상관리 전체 탭 '선택-보류' 버튼 추가**(사용자 요청). 체크한 영상을 content_flag='보류'로 일괄 지정(유니버스 미등록 아이돌/검토 대기 큐로). vm-indiv-btn과 동일 패턴, _vmUpdateCount에 disabled 관리 추가. `_ADMIN_JS_VER→m`, `sw v64→v65`.
- [완료][index.html][sw.js] **GA 이벤트 파라미터 source→trigger 개명(계측 오염 방지)**. `infscroll_trigger` 이벤트의 `{source}` 파라미터가 GA 예약어(traffic source/utm_source)와 충돌해 무한스크롤 트리거값(observer/scroll/poll/releaseLock)이 유입경로 차원과 섞일 위험 → `{trigger:source}`로 키만 개명(변수·값 그대로). `sw v63→v64`.
- [완료][PRINCIPLES.md] **배포 전 필수 게이트(3종 세트) 명문화 + 모바일 크래시 해소 확인**. 유저가 폰 비공개탭으로 kpop-universe.kr 접속 → 별 우주 정상 로드 확인(3주째 열려있던 모바일 신규유저 크래시 리스크 해소). 재발 방지 구조화: PRINCIPLES에 "통과=커밋 조건" 게이트 신설 — ①모바일 스모크(smoke.test.js, 390px+빈 localStorage) ②validate-data 오류 0 ③신규 시각효과 실기기 1회. 1·2는 예외 없음, 3은 시각효과 만졌을 때만. 크래시가 매번 사람 기억에 의존해 검증 골라 돌린 데서 반복된 구조적 원인을 게이트로 못 박음.
- [완료][admin.js][index.html][sw.js] **자동태깅(미태깅) 버튼 병렬화 — 체감 속도 개선**. 268개 그룹을 순차로 돌며 매번 왕복 대기하던 것을 동시성 6 워커풀로 병렬 처리(`processGroup` 클로저+`_worker`). 결과·로직·egress 총량 100% 동일, 벽시계 시간만 대략 그룹당 왕복대기가 6분의 1로. 코어 함수라 본체(_atmResolveMembers·_m2ParseTitle 매칭, tags_manual 보호, description 지연조회)는 그대로 두고 루프 스캐폴딩만 교체. `_ADMIN_JS_VER→l`, `sw v61→v62`.
- [완료][admin.js][index.html][sw.js] **고아태그 정정 스윕 신설(`_ytSweepCanonicalizeMembers`, 버튼 🏷️ sp-canon-btn)**. live-only 오염 스캔에서 발견: members/with_members에 정식명(name.ko)이 아닌 별칭/활동명으로 태깅된 고아 태그(여정→전여여정 14, 홍의진→의진 26, 제롬·페이지·BLUE 등)가 남아 카드에 안 뜨고 사라지던 문제. 외부채널 태깅(`_m2ParseTitle`이 활동명 매칭)·병합 이전 데이터가 원인. **스윕**: matchAliases·groups[].name 오버라이드로 (그룹|고아형태)→정식명 매핑을 만들어 순수 정규화(사람 그대로, 이름형태만). 확실히 아는 것만 정정하고 모르는 고아는 손 안 대고 콘솔 리뷰(동명이인 방지, 별칭 추가하면 다음 실행에서 정정). 잠금행은 two-step(해제→정정→재잠금)+스냅샷. **참고**: live-only 오염 3종 스캔 결과 B(흔한단어 839)·C(원곡 601)는 대부분 오탐(하늘·하루·바다는 실명 멤버, 원곡 주석 영상은 출연자 올바로 태깅됨) — 진짜는 A 고아태그 + 김가람(금지인물) 삭제. `_ADMIN_JS_VER→k`, `sw v60→v61`.
- [완료][artists.json][sw.js] **활동명 불일치 별칭 4명 추가(라이브 solo 0건 해소)**. 라이브 solo 스캔의 "소스없음" 상당수가 실은 등록명≠직캠표기였음(실제 매처는 성떼기·영문토큰으로 더 잡지만, 완전 다른 활동명은 별칭 필요). 직캠 제목으로 검증 후: **장은성→도시/DOSIE(33건, 탈퇴아님·DOSIE 본인), 모리 코유키→유키/YUKI(53건, 퍼플키스·엘즈업 겸임), 전여여정→여정(14건, TO1 YEO JEONG), 권나연→나나(23건, 한글표기)**. matchAliases는 그룹확정 문맥에서만 매칭돼 안전(도시=city 오탐 없음). 표기는 유지(태깅 전용). 적용하려면 자동태깅/잠금-빈값 스윕 재실행 필요. .
- [완료][admin.js][index.html][sw.js] **잠금-빈값 멤버 채우기 스윕 신설(`_ytSweepFillLockedEmpty`, 버튼 🔓 sp-lockfill-btn)**. 라이브 solo 전수 스캔(CSV 108,446행)에서 "그룹 라이브 많은데 solo 0" 원인을 데이터로 규명: **`tags_manual=true`(잠금)인데 members=[]인 직캠 297건/42명**(아이칠린 이지27·지윤16, 러블리즈 JIN31, 엔싸인 카즈타27·한준22 등, 엘즈업 Kei8). 자동태깅이 잠금을 존중해 영영 스킵하던 것. **원인**: tags_manual=true는 오직 관리자 액션(태그모달·벌크)에서만 설정되는데, category 등 비-멤버 필드 벌크변경의 two-step(해제→수정→재잠금)에서 members가 []인 채 재잠김 = 벌크작업 부수효과(인제스트 `_extBuildRows`는 tags_manual 안 건드림 확인). 관리자가 일부러 비운 게 아니라 채우는 게 올바름. **스윕**: tags_manual=true & members빈 & 콜라보흔적 없는 행을 자동태깅과 100% 동일 매처(`_atmResolveMembers`, 단일음절·흔한단어·별칭 게이트)로 채움. 쓰기는 편집모달과 동일 트리거우회 two-step(해제→members세팅→재잠금)+스냅샷 롤백+드라이런 confirm. **가드**: 흔한영단어 단독매칭(온리원오프 Love 등)은 곡제목 오매칭 위험으로 자동채움 제외→콘솔 수동확인. `_ADMIN_JS_VER→2026-08-23j`, `sw v58→v59`.
- [참고][DECISION] **Fable "라이브 solo 미태깅 71명 핸드오프" 재분류 교정**. Fable가 A유형(이름매칭실패의심) 18명 등으로 분류했으나 데이터 검증 결과 **태깅으로 고칠 수 있는 건 3명뿐(이지·지윤·Kei=잠금-빈값)**, 나머지 68명은 **"소스없음"**(퍼플키스·티오원·씨스타 등 그룹 라이브 수백건이 다 그룹단위, 개별 직캠이 DB에 없음)→백필 필요. 단 등록명으로만 확인해서 활동명(예 장은성→은성) 직캠 누락분이 섞였을 수 있어 백필 전 활동명 확인 필요(다음 작업). 리스트 산출: `~/Downloads/live_solo_locked_empty.csv`(297건 위험도별), `live_solo_71_reconciled.csv`(71명 고침3/소스없음68).
- [완료][index.html][sw.js] **표면(창) 닫힘 전수 점검 + ESC를 최상단 표면 닫기로 확장**. 배경: "열린 창이 배경 탭/다른 창 열기/뒤로가기로 자연스럽게 닫혀야 하는데 안 닫히는 곳" 조사. **검증 결과(대부분 이미 정상)**: ①3D 배경 탭 닫힘 정상(`closePanels` 8865 + 빈우주 클릭 `closeCards` 8961). ②검색↔설정 상호배제 정상(8529/8540). ③유저 화면 스와이프-닫기 이미 커버 — 카드/연결/필터 바텀시트(`_wireSheetSwipe`), 라이트박스 일반영상=browse모드(드래그캡+당겨닫기 4058·4037), 쇼츠는 위아래=넘기기+X(표준). ④중앙모달(컬렉션상세·프로필·어드민)은 배경탭+X가 관용 — 스와이프 불필요. **유일한 실질 구멍 = 뒤로가기/ESC가 모달·오버레이에 안 닿음**(popstate 6356은 카드 전용, 기존 ESC 6717은 데스크톱+멤버카드만). **이번 조치**: `_closeTopOverlay()` 신설(우선순위 라이트박스>data-modal>컬렉션피커>연결카드>검색/설정패널>카드) + capture단계 ESC 핸들러 — 데스크톱에서 어떤 표면이든 ESC로 최상단 하나 닫힘(입력창 포커스 중 미관여, data-modal은 자체 -close/-cancel 버튼 재사용, 연결카드는 `_connAnchor` 감지). `sw v57→v58`.
- [보류][DECISION] **모바일 하드웨어 뒤로가기로 모달/오버레이 닫기 — 가오픈 안정기 이후**. 유저 핵심 니즈(모바일 back 제스처로 모달 닫기)지만 기존 카드-해시 히스토리(pushState #g=/#m=)와 얽혀 "back 한 번이 헛도는" phantom-back 엣지케이스가 있어 가오픈 직전 도입은 회귀 위험(Fable도 동일 시점 권고). **설계(준비됨)**: 오버레이 열릴 때 `history.pushState({_ovl:id})`, popstate에서 `_closeTopOverlay()`로 최상단 닫고 소비; 수동 X닫기 시 `history.back()`로 더미엔트리 정리하되 `_ovlPopping` 플래그로 재진입 가드; 연결카드는 `_connAnchor`/시트는 `bs-open`으로 감지('open' 클래스 아님 주의); 기존 popstate 카드 로직보다 **먼저** 오버레이 체크. MutationObserver로 open클래스 관찰하면 개별 open/close 사이트 안 건드리고 중앙집중 가능. 착수 시 헤드리스로 back 시퀀스(모달열기→back→닫힘, X닫기→back→앱안나감) 회귀 검증 필수.
- [완료][admin.js][artists.json][index.html][sw.js] **영상 태깅 대공사(Fable 감사 기반) — 오태깅 로직 + 원곡 라우팅 + 라이브 solo 스캔**. 하루 종일 세션 요약: ①**짧은영문/흔한단어 게이트**(memberHit `_atmNameNeedsCtx`) — 짧은 영문명·흔한단어는 맥락(해시태그/그룹명) 없이 역추론 금지. ②**이종 게이트**(`_isCrossGate`) — 소속사·세대(6년+) 둘 다 먼 그룹은 이름만으론 역추론 금지. ③**matchAliases** — 정식표기 영문 멤버(러블리즈 JIN/Kei, JAY B)의 한글 별칭을 태깅에서만 인식(표기는 유지). `_extBuildRows` 확정추출 + `_atmMatchesMember`(자체채널 자동태깅) 둘 다 wiring. ④**그룹명↔멤버명 충돌 차단**(스텔라→하츠투하츠 190건·슈가→방탄 = strictSync) + validate-data 충돌검사. ⑤**원곡 라우팅**: 커버 영상에서 원곡 그룹이 with로 오분류되던 것 → cover_of로. 태깅 시점(`_extBuildRows`, 커버키워드+6년선배 or 3그룹+나열) + 소급 어드민 버튼 2개(1차 커버키워드+6년, 2차 무키워드+타소속사+10년, 둘다 드라이런·스냅샷·tags_manual보호). 오태깅 미리보기 버튼도 추가. ⑥**라이브 solo 전수 스캔**(CSV 108k행): 활동멤버 121명 단독라이브 0건이나 대부분 "정말 솔로없음"(정상), 진짜 미태깅은 소수 — JAY B(영문명→별칭 완료), 이지·지윤(아이칠린, 제목에 이름 있는데 members=[] — 자동태깅 재실행으로 잡힐 것), 빅(단일음절·설계), 가은(동명이인). **열린 것**: 이지류 미태깅 자동태깅 재실행 후 재확인, 나머지 영문/외국명 멤버(제이창·차오루 등) 별칭 추가, 라이브 스캔 재수출로 감소 검증. `sw v46→v57`, `_ADMIN_JS_VER→2026-08-23i`.
- [완료][admin.js][index.html][sw.js] **영상 오태깅 일반화 차단 — 짧은 영문명/흔한 영단어 "맥락 필수"**(Fable 감사 #1, 러블리즈 Jin의 통계적 형제들). 오태깅 상위 대부분이 "짧은 영문 로마자(≤4자)나 흔한 영단어형 이름이 타 그룹 채널 곡 제목·가사 영단어에 걸려 그 멤버로 그룹을 역추론"하는 한 뿌리(리세→Rise/라이즈, 지디·태양→GD/SUN/TXT, 천둥→Thunder/CIX 등). 이름 목록 늘리던 방식 대신 `memberHit`을 **변형별 게이트**로 일반화: 라틴 변형이 ≤4자거나 흔한영단어(`_ATM_COMMON_EN_WORDS`)면 해시태그/그룹명 등 맥락 없이는 매칭 금지(`_atmNameNeedsCtx`). ⚠️ 이 게이트는 "이름→그룹 역추론"에만 작용, 제목에 그룹명 확정된 그룹의 멤버 추출은 평문 유지 → 진짜 콜라보·자체채널 손실 작음. 한글 변형은 대상 아님이라 name.en만 짧고 name.ko 멀쩡하면 회귀 없음. 헤드리스(admin.js 주입) 검증: "JIN 단독"·"Rise 단독"→null(차단), "에스파 카리나"·"트와이스 미나"→정상(회귀0), "러블리즈 #JIN"→매칭. `_ADMIN_JS_VER→2026-08-23b`, `sw v46→v47`. **미결(제안)**: Fable #2 이종게이트(소속사·세대 먼 with 근거등급↑), #3 admin "이종 고빈도 with 상위N" 스윕(Supabase SQL), with_members 유령태그(여정·제롬) 정리, 러블리즈 JIN→진·Kei→케이 데이터 정정.
- [완료][artists.json][connections.json][sw.js][데이터] **소나무 "우지"→"의진" 정정 + 유니티 홍의진 동일인물 병합**(사용자 제보). 소나무 멤버가 "우지"(세븐틴 우지와 혼동, 잘못 등록·namu링크도 우지 것)로 들어가 있던 걸 실제 활동명 "의진"으로 정정. 생일 1996.10.08이 유니티 홍의진과 동일=동일인물 → 유니티 홍의진 중복 엔트리 삭제하고 의진 엔트리에 통합(주소속=원소속 소나무, `groups[]`에 유니티 겸임+라벨 오버라이드 name:"홍의진" — 유니티 선례 진예=라붐 방식). connections: "홍의진" 참조 8건→"의진"(고아방지), 소나무 멤버 완전연결에서 빠져있던 의진 연결 6건 추가("우지" 18건은 전부 세븐틴이라 안 건드림). 잘못된 우지 namu 링크 제거. validate-data 신규오류 0, 스모크 통과. `sw v45→v46`.
- [완료][groups.json][artists.json][sw.js][데이터] **그룹 5개 추가**(사용자 요청, namu.wiki 기반 병렬 리서치). 슈가(Sugar, 2002 걸그룹·해체 5명), 고스트나인(GHOST9, 활동중 7+탈퇴2=9명), 티에프엔(TFN, 前 T1419·2024 해체 9명, altNames T1419), JBJ(프듀101S2 프로젝트·2018 해체, projectRing), 메이브(MAVE: 버추얼걸그룹·virtualIdol 4명). **소나무는 이미 존재해 스킵**. 멤버 31명 신규. **JBJ 겸임**(워너원 선례): 노태현(핫샷)·김동한(위아이)은 기존 엔트리 `groups[]`에 JBJ 추가(주소속 유지, JBJ 행성에도 표시), 나머지 4명(김상균·타카다켄타·김용국(중국)·권현빈) 신규. 좌표는 자동배치, 디스코/대표곡은 추후 멜론 툴링. validate-data 신규오류 0(휘찬/Yoona는 기존), 스모크 통과. `sw v44→v45`.
- [완료][index.html][sw.js] **모바일 선택해제 줌아웃도 덜 되게**(위 카드-오픈 당김에 맞춤, 사용자 요청). 빈 우주 클릭으로 포커스 해제 시 `_zoomOutABit` factor를 모바일만 `3.0→2.5`(현재거리 배수, 2975). 데스크톱 3.0 유지. `sw v42→v43`.
- [완료][index.html][sw.js] **모바일 카드-오픈 줌 살짝 더 가깝게**(사용자 요청). 그룹 카드(planet 클릭 fly-in) `flyToBubble` 모바일 거리 `radius×11→×9`(2971), 멤버 카드 `flyTo` 모바일 고정거리 `40→33`(2964). 데스크톱 값은 그대로(모바일만). 튜닝 여지 — 더 가까이 원하면 숫자 더 낮춤. `sw v41→v42`.
- [진행중/1단계][index.html][sw.js] **별 재작업 1단계: 코어 유광화(텍스처만)**. 롤백 후 재접근 — 이전 실패(clamp↑→iOS 색점, 홈 빛번짐)를 피하려고 **크기(gl_PointSize)를 안 건드리고 `makeGlowTex` 텍스처의 밝은 코어만 densify**: 꽉 찬 코어 6%→11% + 중간밝기대 상향(예: 20%지점 0.32→0.62). ⚠️바깥 꼬리(0.52~1.0)는 그대로 → 별 글로우 footprint=홈 인상 불변(홈 안전), 크기 불변(iOS 스프라이트 버그 무관). "코어 너무 투명" 피드백 해결 목적. 헤드리스(모바일412): 코르티스 근접 별이 흐린 점→진한 유광 별로, 홈 footprint 유지 확인. **미결**: ①실기기(아이폰) 확인 필수 ②2단계(근접 시 별 더 크게)는 clamp 아닌 방식으로 별도. [[feedback_realdevice_visual_check]] `sw v40→v41`.
- [롤백][index.html][sw.js] **멤버 별 거리 적응형 크기(e72365d) 되돌림 — 모바일 2대 문제로**. 실기기(아이폰) 확인 결과 ①**홈에서 빛번짐 심함**: 별 거리를 행성 반지름으로 정규화해 판단하니, 홈에서 카메라 근처의 큰 행성(트리플에스 등, z/r≈13)이 bloom 밴드(11~18)에 걸려 헤일로가 부풀어 산만해짐(원경이 "이전과 동일"로 고정 안 됨). ②**모바일 색점(치명)**: 코어 클램프를 46→220·헤일로 150→440으로 올린 게 **iOS 큰 point sprite 분할 버그를 깨움**(원래 clamp=46이 바로 이 버그 예방용이라고 코드 주석에 명시돼 있었음) → 별 주위에 알록달록한 색 사각형 조각이 웹엔 없고 모바일만, 나타났다 말았다. **교훈: 근접 별 크기를 clamp로 키우는 방식은 iOS 클램프 천장 때문에 원천적으로 못 씀.** 재작업 방향은 스프라이트가 아니라 **텍스처의 밝은 코어를 키우기**(gl_PointSize 불변→iOS 안전, + "코어 너무 투명" 피드백도 동시 해결) + focus(카드 열림) 게이트로 홈 불변 보장 + 매 단계 실기기 확인. `sw v39→v40`(되돌림 배포).
- [완료][admin.js][index.html][sw.js] **팬 채널 동기화 크래시 수정 + 멤버급 팬채널 + '팬' 필터칩**(사용자 제보: 성현·영재 팬채널 추가·전체동기화했는데 영상 0개). ①**크래시(핵심)**: `_extBuildRows`가 `match`(제목 파싱결과)를 널가드 없이 참조 — 팬 채널은 owner(owner_gko)가 있어 skip 안 되고, strict라 제목에 그룹명/해시태그 없으면 match=null → **그룹명 없는 팬캠 제목("성현 직캠") 하나만 걸려도 그 채널 동기화 통째로 크래시 → 영상 0개**. `match?...:[]` 널가드 추가(admin.js:3265). ②**멤버급 팬채널**: 팬 채널에 전용 멤버 실명 지정 허용(비우면 기존 그룹급) — 지정하면 그 채널 영상 전부 그 멤버로 자동 태깅 + category 'fan' 유지(성현·영재처럼 단일멤버 팬채널용). 백엔드(`_extBuildRows` owner.mko 경로)는 이미 지원해서 add핸들러/UI만 확장(`memberOwned` 분기, owner입력칸을 fans에도 노출). ③**'팬' 필터칩**: `_EXT_TIER_OPTIONS`에 `['fans','팬']` 추가 — "그외" 탭 유형칩·인라인 유형변경 select에 팬 등장(전엔 빠져 있어 팬 채널만 못 걸렀음). 기존 그룹급 팬채널을 멤버급으로 바꾸려면 삭제 후 재등록(삭제 버튼 기존부터 있음). 헤드리스에서 admin.js 주입 후 `_extBuildRows` 직접 호출 검증: 파싱null 제목이어도 크래시0·그룹급=멤버빈배열/멤버급=멤버태깅+fan, 문법검사·스모크 통과. `_ADMIN_JS_VER→2026-08-23a`, `sw v37→v38`.
- [완료][index.html][sw.js] **버그B: "이전에 열었던 카드가 다시 나오는" 모바일 버그 수정**(사용자 제보, Fable 진단 후 클로드코드가 코드추적·재현·수정). 원인: 카드를 스택으로 쌓으면(A 열고 위에 B) 이전 카드 A가 `_freezeSheetBottomBeforeOverwrite`로 **id 뗀 클론**이 되어 mob-sheet-inner 바닥에 얼려짐 — 그런데 `closeMobSheet` 계열 teardown은 **id 있는 라이브 카드만** 치우고 클론은 안 지워서, 닫은 뒤에도 클론이 바닥에 남고 다음 카드(C)를 열면 `[클론A, 라이브C]`로 예전 카드가 같이 떴음. 수정: `_placeInContainer`(첫 오픈 시 카드 배치)가 **새 카드 넣기 전 컨테이너의 다른 자식(클론)을 싹 제거** — 타이머 타이밍과 무관하게 매 오픈마다 정리. 헤드리스(모바일390) 재현: 열기A→스택B→닫기→열기C 시 바닥에 클론A 잔존 확인 → 수정 후 C만 남음. 회귀 확인: 스택→뒤로가기(pop) 정상(A 복원), 스모크 통과. `sw v36→v37`.
- [완료][index.html][sw.js] **버그A: 중간 줌에서 행성 대신 별이 눌리던 것 수정**(사용자 제보, Fable "조준 가능 게이트" 제안 반영). 원인: "멤버 별 탭 허용"은 행성채움(uFillOp>0.12, ≈거리42) 기준인데 "이름 라벨 표시"는 거리<28 기준이라 **28~42 구간에서 이름도 안 뜨는 뭉친 별이 탭돼** 그룹 대신 멤버 카드가 열렸음. 수정: 라벨 임계거리 상수(`STAR_LABEL_FADE_DIST=28`)를 신설해 **라벨 페이드·멤버 탭 게이트가 이 값 하나를 공유** — "이름이 보일 때만 그 별을 탭할 수 있다". 멤버 탭 판정 2곳(모바일 touchend 8925, window click 9054) 모두 `거리>=28이면 멤버 제외` 추가. 헤드리스(모바일390) 재현·검증: 트리플에스 D=35(이름안뜸)→탭시 그룹 열림(수정전=멤버 열림), D=22(정상)→멤버 유지(회귀없음), 클릭·실제터치 경로 둘 다 통과, 에러0.

## 2026-08-22

- [완료][index.html][tests/smoke.test.js][sw.js] **가오픈 전 갭 2건(자문 지적) 처리 — 성능 실측 계측 + 스모크 모바일 시나리오**. ①**성능 계측 신설**: fps(로드 4.5초 후 4초 rAF 샘플링 → 평균+초당최저=잰크지표)·LCP(PerformanceObserver, 이탈 시)를 **세션당 1회** GA 이벤트로 수집(`perf_fps`/`perf_lcp`, device·dpr 포함, `_gaEvent` 재사용, 전부 try/catch로 앱 안 깨지게). 세그먼트 다이어트 효과를 실기기 숫자로 확인용. 헤드리스: 두 이벤트 정상 전송·에러0(fps 값은 헤드리스 rAF스로틀로 1, 실기기선 실측됨). ②**스모크에 모바일 뷰포트(390px, localStorage 비움=신규유저) 시나리오 추가** — `isMob()` 단락 코드(모바일 전용 TDZ 크래시류)를 데스크톱 검증이 못 잡던 갭 메움(로딩해제+캔버스+콘솔에러0 재검). 부수 개선: 맥/리눅스 Chrome 후보 추가(맥에서도 실행 가능), 크로스플랫폼 종료(win=taskkill/기타=SIGKILL, 오펀 방지), 헤드리스 소프트웨어 WebGL 플래그(`--enable-unsafe-swiftshader` 등 — 없으면 WebGL 컨텍스트 실패로 전항목 오탐), 로딩-사라짐 판정을 고정4초대기→폴링(느린 SwiftShader 오탐 수정). **`node tests/smoke.test.js` 맥에서 전항목 통과(exit 0), 데스크톱+모바일 둘 다.** `sw v35→v36`.
- [완료][index.html][sw.js][PRINCIPLES.md] **🚨 모바일 전용 치명적 크래시 수정(회귀)**. 나침반 탭 뱃지 코드(index.html:10123)가 최상위 즉시실행에서 `myProfile?.nickname`을 참조하는데 `let myProfile` 선언은 10171(48줄 뒤)이라 **TDZ(선언 전 접근) 에러** → 모바일 신규/뱃지 미확인 유저 전원이 로딩화면 영구정지. 조건이 `isMob()`로 단락돼 **데스크톱은 멀쩡, 모바일만 사망** — 그래서 데스크톱 기반 스모크/육안검증을 다 통과함(자문 제보). 수정: `myProfile` 대신 `JSON.parse(localStorage.getItem('kpu_profile')||'null')?.nickname` 직접 읽음(PROFILE_KEY도 10166이라 TDZ→리터럴). 다른 myProfile 참조(2531·2717·8374 등)는 전부 함수 내부라 실행시점엔 초기화돼 안전(확인함). 헤드리스 재검증: **모바일390(빈 localStorage=신규)·데스크톱1280 둘 다 씬로드·에러0·뱃지 정상**. 교훈→PRINCIPLES에 "헤드리스 검증 시 모바일 뷰포트 필수" 규칙 추가. `sw v34→v35`.
- [완료][artists.json] **우즈 `subName:"조승연"` 추가**(카드/검색에서 활동명 '우즈' 옆에 실명 '조승연' 흐리게). name.ko='우즈'가 이미 깨끗해서 추가 필드만 — displayName 아님(우즈=활동명이 곧 메인이라 성민=제롬과 같은 subName 케이스). 렌더링은 기존 것 재사용, 코드/sw 변경 없음(데이터만). ⚠️ **에반은 별도(미처리)**: name.ko가 `'에반(희승)'`으로 실명이 괄호로 박힌 오염 상태라, 제대로 하려면 name.ko 정리('에반')+`subName:'희승'`가 필요 = **데이터세션 몫**(예전 우즈 name.ko 정리와 동일 작업, connections·영상태깅 영향 확인 필요해 기능세션이 name.ko 임의변경 금지).
- [완료][index.html][artists.json][sw.js] **displayName(리브랜딩 멤버 표기) 구현 — 디오→도경수**. 현재활동명이 스테이지명과 달라진 케이스를 카드/라벨/검색에 크게 보여줌. `name.ko`(디오)는 connections·영상태깅 정식 키라 **안 바꾸고**, 표시만 `displayName`으로 덮어씀(`_dispKo(a)=displayName||name.ko`). 적용: ①멤버 카드 타이틀(메인=도경수, 흐리게 디오 병기 — `_appendSubName` 확장, 기존 subName/성민=제롬과 공존) ②별(행성) 라벨(`_starLabelName` 우선순위: groups[].name > displayName > name.ko) ③검색(도경수로도 검색됨 + 결과 메인=도경수·흐리게 디오). 한국어 표시 기준(영문은 name.en=D.O. 유지). 데이터: 디오 엔트리에 `"displayName":"도경수"` 1건 추가(기능 데모용, CHANGELOG 디오 스펙대로 — 데이터세션이 추후 후보 확장). ⚠️ displayName은 **표시/검색 전용** — 🚨 영상 매칭 풀엔 절대 넣지 말 것(도경수 영상 opt-in 태깅은 별도 `matchAliases` 향후 과제). 헤드리스(ko/en): 카드 도경수+[디오]·별라벨 도경수·검색 도경수/디오 양방향·성민 회귀없음·에러0. **미적용(다음 단계): 연결카드 칩·그룹 멤버 목록은 아직 name.ko(디오).** `sw v33→v34`.
- [완료][index.html][sw.js] **멤버 카드 Live 탭 전체폭 카드 빈도 상향(0.15→0.24)**. Live 탭은 직캠(가로 16:9)이 대부분이라 거의 다 2열 짝이라 단조롭다는 재피드백. `_groupGcVids` wideProb 라이브값을 0.15→0.24로(두 호출부 14578·14924). 실측 전체폭 비율 7.5%→12.4%. 앞서 `.gc-ch-grid`에 `grid-auto-flow:dense`가 적용된 뒤라 넓힘으로 생기던 빈칸(0.28을 낮췄던 원래 이유) 우려도 줄어 더 올려도 안전. 튜닝 여지 있음. `sw v32→v33`.
- [완료][index.html][sw.js] **그룹 클릭 리플이 맥북(레티나)에서 "선 있는 도넛"으로 보이던 것 수정**. `pulseGroupRipple` 텍스처가 128px+4스톱(0.45에서 알파 0→0.70 급상승)이라, 레티나서 저해상도 확대 밴딩+안쪽 급경사가 또렷한 경계선으로 해상됨(윈도우/폰은 dpr 낮아 뭉개져 우연히 부드러웠음). → 512px + 가우시안 감쇠 링(경계가 수학적으로 없음)으로 교체, **기기 분기 없이 통합 텍스처**(윈도우/모바일은 그대로거나 미세하게 더 매끈, 회귀 없음). 스프라이트/확산 애니/dispose는 그대로. 텍스처는 클릭당 1회 생성이라 512px여도 성능 영향 0(렌더 비용은 스프라이트 화면 크기로 결정). 자문(claude.ai) 진단+제안 블록 반영. 헤드리스: 알파 인접급변 16→11 완만화·피크 179→139·함수 에러0. 튜닝여지: 피크 0.55/폭 0.14. `sw v31→v32`.
- [완료][index.html][sw.js] **동명이인 컬렉션 누수 버그 수정(중요)**. 성민(크래비티)에 만든 영상 컬렉션이 이름만 같은 다른 성민(슈주·원팩트 등) 멤버 카드에 전부 뜨던 문제(사용자 제보). 원인: `_loadCollectionRow`가 컬렉션-멤버 매칭을 이름(memberKo=.ko)만으로 함(`_collectionMemberKos(c).includes(memberKo)`). → 카드가 보는 그룹(ko)까지 써서 구분: 로컬은 "그룹(ko)+멤버가 같은 영상이 담긴 컬렉션"만(`c.videos.some(v=>v.groupKo===ko&&v.memberKos.includes(memberKo))`), 공개는 `member_kos`뿐 아니라 `group_kos`까지 함께 요구(근사치지만 동명이인+그룹 동시겹침은 드묾). 헤드리스 대조: 구버전=크래비티·슈주·원팩트 성민 전부 노출, 신버전=크래비티 성민만. `sw v30→v31`.
- [완료][index.html][sw.js] **사진 컬렉션 그리드 겹침 버그 근본수정 + 큰보기 탭 UI토글**. ① 겹침: 726a382에서 셀에 `aspect-ratio:1` + img `position:absolute`로 바꾼 게, 셀 인플로 콘텐츠가 0이라 그리드가 행 트랙을 0으로 재고 이후 셀이 커지며 다음 행 침범 → 데스크톱서 크게 겹침(헤드리스 재현: 셀414 vs 트랙261). → aspect-ratio를 인플로 img에(`width:100%;aspect-ratio:1;object-fit:cover`) 옮김(영상그리드 `.gc-ch-thumb`와 동일한 올바른 패턴). ② 데스크톱서 행이 컨테이너 높이에 눌려 정사각 깨지던 것(flex:1 그리드의 aspect-ratio↔정의높이 순환): 그리드에서 `flex:1;overflow` 떼고 **스크롤 래퍼(`flex:1;min-height:0;overflow-y:auto`)로 분리** → 셀 항상 정사각+넘치면 스크롤. 헤드리스(데스크톱/모바일×20장): 겹침0·정사각·스크롤OK. ③ `_openPhotoFull` 사진/배경 탭하면 주변 버튼 전부 토글(아이폰 앨범식), 닫기는 ✕/ESC. `sw v29→v30`.
- [완료][kpop_universe.css][index.html][sw.js] **탐험 패널(피드) 2열 그리드 빈 칸 버그 수정**. 크기섞기(a3ca66d) 도입 뒤, 전체폭 카드(`.feed-wide` span 2)가 홀수 위치 뒤에 오면 직전 행 오른쪽 칸이 텅 비던 문제(사용자 제보: "왼쪽만 썸네일, 오른쪽 텅 빔"). CSS 그리드 기본 배치가 구멍을 안 메우는 게 원인. → `.feed-grid`에 `grid-auto-flow:dense` 추가(빈 칸을 뒤쪽 단일 카드로 자동 채움, 구멍 있을 때만 작동). 동일 구조(2열+span2)인 그룹/멤버 카드 그리드 `.gc-ch-grid`에도 예방적으로 같이 적용(featured가 맨 위일 땐 채울 구멍이 없어 변화 없음). 헤드리스: 단일3+전체폭1+단일2 배치 시 중간행 빈칸 0 확인. CSS `?v=14→15`, `sw v28→v29`.
- [완료][index.html][sw.js] **"다른 아티스트의 커버" 섹션을 커버 탭 전용으로 이동**. 기존엔 무한스크롤 영상 목록 맨 밑에 항상 붙어 있어서, 위 영상이 계속 로딩되며 섹션이 밑으로 파묻혀 모아보기 어려웠음(사용자 제보). → `_loadCoverOfSection`은 카드 열 때 1회 로드하되(`wrap.dataset.hasContent`), 노출은 `state.viewMode==='category'&&state.filter==='cover'`일 때만. 커버 탭은 목록이 짧아 바로 밑에 보이고, 다른 탭에선 숨겨 첫 화면 깔끔. 탭 클릭 핸들러(renderFilterTabs)·⋮뷰토글 핸들러 2곳에 노출 동기화 추가. 멤버·그룹 카드 둘 다 커버 탭 있어 동일 적용. 헤드리스: 씬 로드·에러0(실제 커버목록은 Supabase 데이터라 라이브 검증 필요). `sw.js v27→v28`.
- [완료][index.html][kpop_universe.css][sw.js] **서브네임 검색 별칭 + 검색결과 조건부 병기 + 병기 유사가드**. ⓐ 검색이 별칭(subName·groups[].name 오버라이드)도 매칭 — "제롬"으로 검색해도 성민(원팩트)이 뜸(`_artistAliases(a)` 신설, doSearch ARTISTS 루프에 `mainMatch||matchedAlias`). ⓑ 별칭으로만 걸렸을 때(메인 이름엔 안 걸림) 검색결과에 그 별칭을 흐리게 병기 — "제롬" 검색→"성민 〔제롬〕", "성민" 검색→병기 없음(`hit.alias`, `.sr-alias`, 이름+별칭 `.sr-nl` flex 래핑). ⓒ `_appendSubName`에 유사가드 — 메인이름과 한쪽이 다른 쪽에 포함되면(박재범⊃재범) 흐린 병기 생략(데이터세션은 이미 박재범 subName 미설정). Playwright 헤드리스: "제롬"→[성민,alias=제롬,ONE PACT], "성민"→성민들·alias=null, 에러0. CSS `?v=13→14`, `sw.js v26→v27`.
- [완료][index.html][kpop_universe.css][sw.js] **동일인물 병합 렌더링 (기능세션 — 아래 데이터세션 스펙 ⓐⓑ 구현)**. ⓐ `subName`(옛 활동명/실명) → 멤버 카드 이름 옆에 작고 흐리게(`_appendSubName`, `.tn-subname` 9.5px·opacity 0.42, `#tn` 2곳=showT+언어리렌더). ⓑ 별(행성) 라벨 → `_starLabelName(m,groupKo)` 신설: 그 그룹의 `groups[]` 항목에 `name` 오버라이드 있으면 그걸로(성민이 **티오원 행성 라벨엔 "제롬"**, 원팩트엔 "성민"). 라벨은 멤버당 1개(주소속 위치)라 라벨이 어느 그룹 행성에 만들어지든 그 그룹 이름과 일치하게 됨. ⓒⓓ(메인=name.ko, 겸임 다중배치)는 기존 로직 그대로. Playwright 헤드리스: `_starLabelName(성민,'티오원')="제롬"`·`('원팩트')="성민"`, 성민 카드 `.tn-subname="제롬"`, 에러0. CSS `?v=12→13`, `sw.js v25→v26`. (한계: 겸임 멤버의 세컨더리 행성엔 라벨 자체가 없어(ⓓ 기존 구조) "제롬"이 티오원 행성에 늘 뜨는 건 아님 — 라벨이 그쪽에 만들어질 때만. 멤버당 다중 라벨은 별도 작업.)
- [완료][artists.json][데이터세션] 동일인물 병합 2건 + 서브네임 데이터 필드 신설. **✅ 기능세션 렌더링 완료(2026-08-22, 위 항목 참고).**
  - **병합**(순차 이동+개명, 같은 생일+같은 IG로 동일인물 확인): ①제롬(티오원, 2022 탈퇴)→**성민(원팩트)** ②여정(티오원)→**전여여정(뉴비트)**. 각각 옛 엔트리 삭제하고 현 엔트리에 `groups:[현그룹, {옛그룹, name:"옛활동명"}]`으로 통합. 로아(프리스틴)는 병합 아님(실명 표기용 subName만).
  - **신설 데이터 필드 2개 (기능세션이 읽어서 렌더링해야 함):**
    1. `subName`(string) — **멤버 카드**에서 이름 옆에 **작고 흐리게** 보조표기. 성민→"제롬"(옛 활동명), 전여여정→"여정", 로아→"김민경"(실명). 있을 때만 표기.
    2. `groups[].name`(string, 옵션) — 그 그룹 항목에만 있는 필드. **별 라벨(행성)** 렌더링 시, 멤버가 그 그룹 행성에 표시될 때 `name.ko` 대신 이 이름을 쓸 것. 예) 성민은 티오원 행성에선 "제롬"으로 표기. 없으면 기존 `name.ko` 사용.
  - **기능세션 할 일**: ⓐ 멤버 카드에 `subName` 있으면 이름 옆 작고 흐린 텍스트로. ⓑ 별 라벨 생성 시 `groups[]`에서 현재 행성 그룹의 항목에 `name` 있으면 그걸로 라벨. ⓒ 카드 클릭 시 메인은 현재 활동명(`name.ko`). ⓓ 겸임 렌더링(groups[] 다중 행성 배치)은 기존 로직 그대로면 됨(주결경·마크와 동일).
  - [완료][connections.json] 병합으로 삭제된 옛 이름 '여정' 8건 → '전여여정'으로 수정(고아참조 방지, compact 포맷 유지). '제롬'은 참조 0. 성민/로아는 병합해도 name.ko 안 바뀌어 참조 영향 없음(단 로아·성민은 동명이인 다수라 이름만으론 모호 — 기존 이슈, 이번 범위 밖).
  - **🚨 영상 동기화 오염 주의(2026-08-22 사용자 지적)**: `subName`·`groups[].name`은 **표시(카드/라벨)+인앱 검색 별칭 전용**. **영상 매칭 풀(`_apiVidToSong`/`_m2ParseTitle` 등 영상→멤버 태깅)엔 절대 넣지 말 것.** 흔한 실명(로아=김민경)으로 영상을 끌면 동명이인 영상 대량 오염됨. 특이해 안전한 이름(도경수)을 영상도 끌고 싶으면 표시용 subName과 **별개로 멤버별 수동 opt-in 매칭 alias(향후 예: `matchAliases`)로만** — 흔한 실명 자동추가 절대 금지. (인앱 검색 별칭 `_artistAliases`는 무해하니 OK, 영상 태깅만 제외)
  - **[미결/기능세션 설계 판단 필요] 리브랜딩 멤버 표기(디오=도경수 류)**: 현재활동명이 스테이지명과 다른 케이스(로아=김민경, 디오=도경수 등)를 카드 메인=현재활동명으로 보여주는 안. `name.ko`(디오)를 바꾸면 connections·영상태깅 다 깨지므로 **바꾸지 말고**, 카드 메인용 현재활동명은 별도 필드(예: `displayName`)로 두고 렌더가 그걸 크게·`name.ko`를 작게 표시하는 방식 추천. 스키마(필드명) 확정은 기능세션. 데이터세션이 IG 표시이름+네이버로 "현재활동명≠활동명" 후보 리스트를 뽑아 제공 예정(진행중).
    - **선정 기준(2026-08-22 사용자)**: "현재 케이팝 가수 위주 활동이 아닌가"(배우 등으로 전환 = 메인을 현재활동명으로). 절대 기준 아니고 애매하면 사용자가 개별 최종판단(큐레이션).
    - **디오 확정 스펙(예시)**: 카드 메인=`도경수`, 서브=`디오`. **영상 함께한멤버 태깅**: 기본 `엑소 디오`(디오 시절 영상이 다수), **단 영상 제목에 '도경수' 포함 시 '도경수'로 표기**. → 이때 '도경수'를 매칭/표기에 쓰는 건 **도경수가 특이명이라 안전**(위 🚨 오염규칙의 opt-in 케이스). 흔한 실명(김민경)은 이 규칙 적용 금지 = 멤버별 개별 판단 필수.
- [완료][index.html][kpop_universe.css][sw.js][PRINCIPLES.md] 닫기(×) 버튼 은은하게 (사용자 요청 — 디스코 앨범 상세 닫기가 너무 밝고, 탐험 리스트 닫기가 옆 공유 버튼보다 크고 두꺼움). 원인: 닫기가 `✕`/`×` **텍스트 글자**(18~20px·opacity 0.66)라 옆의 얇은 SVG 아이콘(공유 13px·0.54)보다 굵고 밝게 보였음. 수정: `#feed-list-close`·`#gc-discog-detail-close`를 텍스트 글자→**얇은 SVG ×**(stroke 2, 14~15px)+대비 낮춤(0.5·0.4). Playwright 헤드리스 검증(둘 다 svg·텍스트0·저대비, 공유 0.54와 비교해 안 튐, 에러0). + **PRINCIPLES에 "닫기 등 보조액션은 얇은 아이콘+낮은 대비로 은은하게" 명문화**. CSS `?v=11→12`, `sw.js v24→v25`.
- [완료][index.html][sw.js][PRINCIPLES.md] 사진 크게 보기를 **갤러리**로 + 버튼 **아이콘화** (사용자 요청). `_openPhotoFull`을 (사진리스트,인덱스,onDelete) 받는 갤러리로 재작성 — 좌우 화살표(작고 은은, 2장+일 때만)·**모바일 좌우 스와이프**(터치 40px+ 수평)·키보드 ←→/ESC로 넘김. 버튼 전부 텍스트→**아이콘(SVG)**: 저장(다운로드)·삭제(휴지통)·이전/다음(셰브런)·닫기(×), `aria-label`로 접근성. 오버레이는 열 때마다 새로 만들어 리스너 누적 방지, 삭제 시 리스트에서 빼고 다음 사진 표시. 그리드 클릭이 `col.photos` 배열+인덱스 넘기게 변경. Playwright 헤드리스: 버튼 5개 전부 아이콘(텍스트0)+svg·aria-label 확인, ArrowRight로 사진 넘김 스샷 확인, 에러0. + **PRINCIPLES.md에 "UI 아이콘 우선" 원칙 명문화**(사용자 지적대로 원래 없었음). `sw.js v23→v24`.
- [완료][index.html][sw.js] 사진 그리드 다듬기 3건 (사용자 요청, 사진 업로드 라이브 정상 확인 후). ①각 사진 우상단 × 삭제버튼 제거 → 그리드는 사진 위 표시 없이 깨끗하게. ②삭제는 사진을 크게 본 화면(`_openPhotoFull`)에 🗑 버튼으로 이동(`onDelete` 콜백, 본인 소유만 노출). ③위아래로 사진이 겹치던 것(iOS) — 그리드 이미지를 `position:absolute;inset:0`로 빼서 셀 높이를 `aspect-ratio`로만 결정(in-flow `height:100%` img가 iOS 그리드에서 행 높이를 오산해 겹치던 것 방지) + `gap 5→7px`, 위 여백 `2px`. Playwright 헤드리스(가짜 사진 주입): 셀 117² 정사각·세로 gap 7·× 없음·큰보기에 저장/삭제 버튼 확인. `sw.js v22→v23`.
- [완료][index.html][sw.js] 컬렉션 생성 이름 다이얼로그가 마이페이지(프로필) 패널 뒤로 가려지던 버그 (사용자 제보). 원인: `_bringToFront`가 `_topLayerZ`(시작 67)를 증가시키며 인라인 z-index를 부여하는데, `dh-profile-btn`으로 마이페이지 열면 사이드바가 `_bringToFront`를 받아 z-index가 정적 70(생성 다이얼로그)을 넘어섬 → 다이얼로그가 사이드바 뒤로 숨음. (`journal-toggle`로 열면 사이드바가 66에 머물러 안 걸려서 데스크톱 재현이 처음엔 안 됐음.) 수정: `_promptDialog`/`_confirmDialog`도 열 때 `_bringToFront` 호출해 항상 최상단. Playwright로 버그 재현(사이드바 z76까지 올림)+검증(다이얼로그 z77, 화면중앙 최상단=입력창). `sw.js v21→v22`.
- [완료][index.html][sw.js] **사진 업로드가 빈 화면 되던 버그픽스** (사용자 제보 — 사진 골라 파란체크 눌러도 빈 화면, 여러 개 선택도 안 되는 듯). 원인: `_openColPhotoGrid`의 파일입력 change 핸들러에서 `const files=fi.files;fi.value='';` — **`fi.value=''`가 라이브 FileList(`fi.files`)를 그 자리에서 비워서** 잡아둔 `files`가 0개가 됨 → 업로드 0장 → 빈 화면(여러 장 골라도 0개라 "다중선택 안 됨"으로도 보임). 기존 커버 업로드는 `files[0]`(File 객체)를 먼저 잡아 안 걸렸음. 수정: `const files=[...fi.files]`로 **초기화 전에 배열 복사**. + `_uploadPhotosToCol`에 에러 표시 추가(전부 실패 시 조용히 빈 화면 대신 원인 alert — 스토리지 권한 문제 진단용). 헤드리스 스모크(문법0). `sw.js v20→v21`.
- [완료][index.html][sw.js] 사진 컬렉션 Phase 1a 후속 2건 (사용자 요청). ①큰 사진 보기(`_openPhotoFull`)에 **다운로드 버튼** 추가 — 크로스오리진(Supabase Storage)이라 `fetch`→blob→`a[download]` 저장, 실패(CORS/모바일)하면 새 탭 열어 길게눌러 저장 폴백. ②가이드라인 문구를 **거의 안 보이게** 은은하게(사용자: "보이면 흥이 식음") — `font-size:10.5→8.5px`, `opacity 0.5→0.22`, ⚠️ 이모지 제거, 문구 축약("비하·성적대상화 금지 · 저작권자 요청 시 삭제"). 법적 #5 "명시"는 유지하되 시각적 존재감만 최소화. 헤드리스 스모크 통과(문법0). `sw.js v19→v20`.
- [진행중][index.html][kpop_universe.css][sw.js] **사진 컬렉션 기능 Phase 1a** (사용자 요청, Fable 법률대화 기반 — "부족한 데이터가 사진, 컬렉션으로 제한해 큐레이션"). 컬렉션을 영상/사진 `kind`로 분리 — 사진 전용 컬렉션에 유저가 직접 사진 업로드. **기존 영상 컬렉션 경로와 `kind==='photo'`로 완전 격리(회귀 위험 0)**. 구현: `_createCollection(name,'photo')`+`photos[]`; 업로드는 **이미 검증된 커버업로드 패턴 재사용**(Supabase Storage `artist-pics` 버킷, `collection-photos/{colId}/` 경로, 다중파일 `_uploadPhotosToCol`); **장당 100장 제한**(`_COL_PHOTO_MAX`, 법적 #6 "무한 아카이브 아님"); 사진 그리드 뷰어(`_openColPhotoGrid`, 사용자데이터 XSS방지로 전부 DOM생성)+큰사진보기(`_openPhotoFull`)+개별 삭제; 업로드 **가이드라인 문구 상시 노출**(#5 "실존인물 비하·성적대상화 금지 · 저작권자 요청 시 삭제"); '+ 사진 컬렉션' 생성 버튼(로그인 필요, `col-create-photo-btn`, i18n ko/en). localStorage+계정동기화는 기존 `_saveCollections`로 자동. Playwright 헤드리스 스모크: 씬 정상로드(=스크립트 문법0)+버튼+함수4개 정의+콘솔에러0 확인. CSS `?v=10→11`, `sw.js v18→v19`. **⚠️ 로그인 필요 UI라 create→upload→view 실동작은 헤드리스 미검증 → 라이브 테스트 필요.** 업로드 실패 시 `artist-pics` 버킷의 `collection-photos/` 경로 storage 정책 확인(커버업로드 `col-covers/`가 되므로 대개 문제없음). **남은 것**: Phase 1b(공개 공유 — `public_collections`에 photos 컬럼/싣기 + 타인 카드 노출 + 신고버튼 `feed-list-report-btn` 재사용=#6 OSP takedown), Phase 2(이용약관에 "업로드 사진" 명시+게시자 책임, 관리자 사진 삭제, **#4 후원 프레이밍은 수익화 시**). [[project_photo_collection_legal]] 참고.
- [완료][kpop_universe.css][index.html][sw.js] 자체 웹앱(홈화면 설치, standalone) 상단바가 상태바에 바짝 붙어 타이트하던 문제 (사용자 제보 — 사파리 브라우저는 정상, 웹앱만). `apple-mobile-web-app-status-bar-style: black-translucent`라 standalone에선 콘텐츠가 상태바 밑까지 꽉 참. `#mob-top-bar`가 이미 `env(safe-area-inset-top)`은 쓰고 있었지만 기본 여백 14px이 최소한이라 타이트했음. 해결: `@media (display-mode: standalone) and (max-width:768px)`로 **설치 웹앱일 때만** `#mob-top-bar` 상단 여백을 `14→24px(+env(safe-area-inset-top))`로 키움(사파리는 이 미디어쿼리에 안 걸려 그대로). 파일 끝에 둬서 기존 패딩 확실히 덮어씀. ⚠️ 헤드리스로 standalone 재현 불가 → 실기기(홈화면 추가한 아이폰)에서 보고 24px만 튜닝 필요. CSS `?v=9→10`, `sw.js v17→v18`.
- [완료][kpop_universe.css][index.html][sw.js] 그룹/멤버 카드 디스코그라피 세그먼트 컨트롤(All/Mini/Single) 크기 살짝 축소 (사용자 요청) — `.seg-control-sm .seg-btn` 패딩 `4px 11px`→`3px 8px`(높이 -2px "진짜 조금", 너비 버튼당 -6px "조금"), 폰트 9px 유지. CORTIS 카드로 렌더 확인(축소돼도 legible). CSS `?v=8→9`, `sw.js v16→v17`.
- [완료][index.html][kpop_universe.css][sw.js] 탐험 패널(차트·디스커버리) 균일 2열 → 크기 섞기 (사용자 요청, 그룹/멤버 카드 레이아웃 참고). 단일영상 하이라이트 카드 4종(커버 스포트라이트·콜라보 스포트라이트·트렌딩 하트·랜덤영상)을 landscape면 **전체폭 16:9**(`feed-wide`=`grid-column:span 2`)로, 대표가 쇼츠면 **9:16 세로+블러배경**(`is-short`, 그룹/멤버 카드 `.gc-ch-thumb-bg` 방식 재활용)으로 노출 — 쇼츠가 가로 네모에 눌려 잘리는 것 방지(사용자 우려 반영). `_appendFeedCard`에 opts{wide,short} 추가, `_setFeedCardThumb`가 쇼츠일 때 블러 배경 생성, `.feed-grid`에 `align-items:start`. Playwright 헤드리스 검증: 전체폭 카드(RIIZE·CORTIS 트렌딩)가 패널 전체폭으로·쇼츠 9:16(125×221)+블러배경 정상, 콘솔에러 0. CSS `?v=7→8`, `sw.js v15→v16`. (랜덤영상 카드는 썸네일이 mqdefault 16:9라 쇼츠 세로처리 제외, landscape 전체폭만. 나머지 컬렉션 카드는 2열 유지 — 오버하지 않게.)
- [완료][index.html][sw.js] 탐험 패널 조회수 표기 '조회' 잔여 3곳 제거 (사용자 재제보) — 8/20에 '조회' 7곳 정리 때 누락됐던 모음류 카드 서브(수록곡 무대 모음 `_renderBsideCard`:13276 + 13177·13340)가 여전히 `1위 · N 조회`였음 → 다른 카드와 동일하게 `1위 · N`으로 통일(영문 `views`는 유지). `sw.js CACHE_VERSION v14→v15`.
- [진행중][보류] 솔로 디스코그라피 스윕 — **방식 전환 예정**. 지금까지 멜론 **이름검색** 스윕은 동명이인 오매칭을 대량 유발함이 확인됨(준→JUNE래퍼, 소연(티아라)→(여자)아이들 전소연 디스코 통째, 시온(엔시티위시)→다른 Sion, 마틴/카르멘 등 외국인). 앞으로는 **멜론 그룹→멤버링크 방식**으로 전환 — 그룹 페이지에서 멤버 텍스트 클릭→개별 아티스트 페이지의 정확한 aid를 써서 동명이인을 원천 차단(사용자 제안). 스크립트 준비완료(`scratchpad/sweep4.mjs`). **멜론 IP 차단**(수시간~하루 지속) 풀리면 실행 예정이며, **디스코 없는 멤버 + 이미 반영분까지 전수 audit**로 돌려서 정확한 결과 ↔ 현재 `artists.json` 대조 → 기존 오매칭 자동 검출·수정할 것. 현재 전체 1550명 중 ~335명만 검사됨(1차 200 + 재스윕 135, 나머지 ~1050명 차단으로 미검사). 검증 보조: og공식영문명 완전일치 + 생년월일 + 그룹데뷔연도 3중 대조, 커버 로드검증. 리포트: `Downloads/솔로매칭_리포트.txt`·`재스윕_검토리포트.txt`.
- [완료][artists.json] 솔로 디스코그라피 확실분 반영(위 1차 스윕에서 3중검증 통과분만) — 아이브·(여자)아이들·에이티즈·NCT DREAM 등 nCand=1 다수 + 소연(아이들)·허윤진·윈터·소희 + 재스윕 확실 45명(동방신기 유노윤호·최강창민, 원더걸스 유빈·선예, 카라 4인, 티아라, 2NE1 공민지, 이달의소녀 츄, 김재중36 등) + 김재경(레인보우). 동명이인 의심분은 전부 보류. (데이터 세션 전담 — 기능세션과 파일 분리)
- [완료][index.html] 디스코그라피 케밥에 '솔로만 보기' 토글 추가 — 앨범에 `unit` 태그(EXO-SC 등 유닛앨범) 있을 때만 노출, 켜면 유닛 제외하고 솔로만 표시. 찬열(20장, EXO-SC 3장 unit태그)·세훈(We Young+EXO-SC) 반영과 함께. sw.js v13.
- [완료][index.html] 권리보호 3종 — 설정 '권리 침해 신고' mailto 링크, 남의 공개컬렉션 케밥(⋮) 신고 버튼, 라이트박스 보일듯말듯 'youtube' 출처 라벨. sw.js v12. (Fable 법률감사 #1·2·3, 나머지 #4·5·6은 메모리에 보류메모)
- [완료][admin.js][index.html] 일회용 버튼 3개 제거 — "트리플에스 유닛명 오매칭 정리"(`sp-yt-cleanup-unittoken-btn`/`_ytCleanupUnitTokenMistag`)·"흔한단어 멤버명 오매칭 정리"(`sp-yt-cleanup-hashtagonly-btn`/`_ytCleanupHashtagOnlyMistag`+`_HASHTAG_ONLY_CLEANUP_NAMES`)·"아이돌개인 채널 owner_gko 백필"(`sp-ec-backfill-ownergko-btn`/`_ecBackfillOwnerGko`). 전부 2026-08-21 소급정리용 1회성이라 실행 완료 후 역할 끝남. 버튼 HTML+힌트·함수·바인딩·상수 전부 삭제, `node --check`로 문법 검증, `_ADMIN_JS_VER`→2026-08-22a. ⚠️ 작업 중 발견: Downloads의 8/20 zip 폴더가 라이브보다 구버전이라 하마터면 그걸 고쳐 올려 8/21 작업을 덮어쓸 뻔 → 이후 GitHub 클론(Documents/GitHub/kpopuniverse)에서 직접 작업+push하는 것으로 정리(PRINCIPLES.md 갱신).
- [완료][admin.js][index.html][SQL] 일괄 작업 실행 취소(undo) 기능 신설 — 대량 스윕/재검증/재스캔 6개 버튼(밴 숨김·제외키워드 무관·콜라보 재검증·자체멤버 재검증·그룹배정 신뢰도 재스캔·커버곡 제목추출)이 대량 update 직전에 바뀔 행들의 현재값을 `admin_bulk_snapshots` 테이블에 1회분(batch)으로 자동 스냅샷(`_snapshotBeforeBulk`), 어드민에 "↩︎ 마지막 일괄 작업 되돌리기"(`_ytUndoLastBulk`) 버튼 추가해 최근 batch를 통째 복원(되돌린 batch는 삭제→연속 undo 가능). 3.5만 재스캔 사고 재발 방지책(자문 백업전략 #1, undo가 곧 백업). 스냅샷 실패해도(테이블 없음/권한) 원작업은 안 막음(비차단, 안내만). `node --check` 통과, `_ADMIN_JS_VER`→2026-08-22b. ⚠️ 활성화하려면 `admin_bulk_snapshots_migration.sql`을 Supabase에서 1회 실행 필요. 범위 밖(다음 후보): 자동태깅/재태깅(추가만 하고 삭제 안 해 비파괴적)·동명이인 재배정(행별 처리라 별도 설계)·수동 다중선택 조작(관리자가 직접 고른 것)은 이번 미적용.
- [완료][index.html] 그룹 이름 라벨이 행성 위로 붕 떠 보이던 문제 — 라벨 3D 오프셋을 상수(`bm.radius+0.2`)→반지름 비율(`bm.radius*1.05`)로 변경(index.html:2728). 상수 방식은 작은 행성(멤버≤4, R≈1.3)일수록 0.2가 상대적으로 커서 라벨이 높이 떴음(큰 행성 ≈4% vs 작은 행성 ≈15% 간격). 비율로 바꿔 모든 크기에서 균일하게 5%만 위로 — 작은 행성 라벨은 내려오고 큰 행성은 거의 유지. Playwright 헤드리스로 여러 줌 렌더해 확인. 어제(8/21) 1.2→0.5→0.2로 줄인 데 이은 후속 — 남아있던 "작은 행성 편차"가 근본 원인.
- [완료][index.html] 무한 스크롤 "점 세개 로딩 모션이 3초 넘게 늦게 뜨던" 문제 (Phase ①, Fable 자문 검증) — 원인은 트리거 부재가 아니라 8/18에 넣은 "점세개 선제표시" 브랜치가 도달 불가 코드였던 것. `_onScroll` 게이트(index.html:14999)가 `state.appendLock`이면 조기 return이라, 스크롤·700ms 폴링이 락 중엔 `tryLoadMore`의 점세개 브랜치에 도달을 못 했음(락 ~3초 무표시 → 해제 후에야 뒤늦게 표시). 게이트에서 `state.appendLock` 제거(중복로드 방지는 tryLoadMore 내부가 이미 담당). **+ Fable이 "확인 필요"라 짚은 지점 실제로 버그였음**: 점세개 브랜치(14128)가 `cont?.querySelector`를 쓰는데 `cont`는 `tryLoadMore` 스코프에 없음(전부 다른 함수의 지역변수) → 게이트만 풀면 `ReferenceError: cont is not defined`(지난번 솔루션이 오류 낸 정체). `document.getElementById(contId)?.`로 교체. Playwright 헤드리스 검증: IVE 카드 46→94개 무한스크롤 정상, 점세개 즉시 표시(로딩 전 46개인 채로 dots 렌더 확인), ReferenceError 0. Phase ②(쇼츠 프로브가 잡는 ~3초 락 자체를 settlePage 앵커 insertBefore로 조기 해제 → 대기 소멸)는 회귀위험 있어 다음 세션으로 보류.
- [완료][admin.js][index.html] "그룹배정 신뢰도 재스캔(기존 데이터)" 버튼 제거 — 이미 2026-08-20에 비활성화(early return)돼 눌러도 경고만 뜨던 버튼. 원래 일회용 소급 마이그레이션이었는데 정상 영상 35,168개를 대량 오숨김한 사고(SQL로 전량 원복)를 내고 막아둔 상태였음. 새로 들어오는 영상의 weak 배정은 이미 동기화 시점(admin.js `_m2ParseTitle` confidence + needs_review)에 자동으로 "그룹배정 검수" 큐로 가므로 이 버튼 없이도 진행 흐름엔 구멍 없음 — 버튼은 옛 데이터용 일회성이었을 뿐. 비활성 버튼을 남겨두는 것 자체가 지뢰(weak 기준 안 좁히고 재활성 시 사고 재발)라 버튼 HTML+힌트·바인딩·죽은 함수 전부 삭제(로직은 git 이력에 보존). `_ADMIN_JS_VER`→2026-08-22c, `node --check` 통과. (참고: undo 스냅샷 대상이 6→5개가 되나, 이 버튼 스냅샷 호출은 비활성 return 아래 죽은 코드라 실제론 처음부터 5개만 유효했음.)
- [완료][admin.js][index.html] 아이돌개인 채널 공동운영(소유자 2명+) 지원 + 동기화채널 목록에 소유 아이돌 이름 표기 (사용자 발견: 이펙스 백승·뮤 공동 채널). **스키마 변경 없이** 기존 `owner_mko` 텍스트 칸에 쉼표로 여러 명 저장(예: `백승,뮤`). ①등록 폼(`_ecAddChannel`/`_ecUpdateOwnerGkoOptions`)이 쉼표 파싱, `owner_gko`(그룹)는 공동소유자가 공유한다고 보고 하나로 확정 — 동명이인이거나 소유자들 그룹이 갈릴 때만 그룹 선택 드롭다운 노출. ②동기화(`_extBuildRows`)가 그 채널 영상 전부에 `members=[소유자 전원]`으로 붙임(`_extOwnerGko`는 첫 이름으로 그룹 방어). ③채널 목록(`_vmRenderChannels`)에 아이돌개인이면 `👤 이름`(공동운영은 `·`로 구분) 뱃지 표기. 단일 소유자 기존 동작은 100% 하위호환(검증). 한계: 서로 **다른 그룹** 멤버가 공동운영하는 채널은 group_ko가 하나로 안 정해져 미지원(등록 시 그룹선택 요구, 실제 거의 없음) — 정식 배열컬럼(`owner_mkos`)은 필요해지면 그때. `node --check` 통과, 쉼표분리 로직 노드 유닛테스트 통과, `_ADMIN_JS_VER`→2026-08-22d. (개인채널은 원래 official audio 포함 전 영상을 소유 멤버로 끌어옴 — 타입별 제외 없음이 디폴트로 확정, 함께한멤버 제목파싱 태깅은 기존 그대로.)
- [완료][index.html] 세대 기반 디스커버리·차트 카드에서 1세대 노출 빈도 살짝 낮춤 (사용자 요청 — 1세대는 데이터·팬덤이 상대적으로 적음). 5개 세대 카드(세대별 직캠모음/개인직캠TOP100/무대TOP100/수록곡/커버)가 공유하는 회전 목록 `_GEN_CAM_COMBOS`에 세대 가중치(`_GEN_CAM_WEIGHT`) 도입 — 기본 3, 1세대만 2. 결과 1세대 20%→14.3%, 나머지 세대 각 21.4%(노드로 분포 검증). 1세대 여돌·남돌은 그대로 다 남고(god·신화 등 안 사라짐) 순환 빈도만 줄어듦. 조정은 `_GEN_CAM_WEIGHT={1:2}`의 숫자만(3=예전 균등, 1=절반 수준). 랜덤 그룹/멤버 카드는 원래 전체 비례 픽이라 1세대가 이미 소수 → 손 안 댐. (차트 쪽 연도별TOP은 원래 2004년~만 뽑아 1세대 제외, 레전드100은 전체기간이라 세대무관.)
- [완료][index.html][kpop_universe.css][sw.js] 나침반 탐험 패널 UI 2건 (사용자 요청). ①"차트 더보기" 토글 버튼 축소 — 44px 원형→22px(셰브런 16→12px), 배경·색 더 은은하게(`.feed-more-toggle`). ②차트/디스커버리 카드 서브(첫 영상 제목 등)가 여러 줄로 늘어나 자리를 너무 먹던 것 → 한 줄(끝 페이드 마스크) 고정 + 그룹/멤버 카드 영상 제목과 동일한 마퀴(`addMarquee` 재사용)로 hover(데스크톱)/탭(모바일) 시 전체 슬라이드(`.feed-card-sub` nowrap+overflow, `_appendFeedCard`에서 순수텍스트 서브에만 배선). Playwright 헤드리스 검증: 버튼 22×22, 서브 17개 전부 한 줄(최대 16px·여러줄 0), 마퀴 17/17 배선, 콘솔 에러 0. CSS 캐시버스터 `?v=6→7`, `sw.js CACHE_VERSION kpu-v13→v14`.
- [완료][index.html] 그룹 이름 라벨이 행성 위로 "한참 붕 떠" 있던 문제 **진짜 원인 발견+해결** (오늘 앞선 radius*1.05·어제 radius+0.2·그제 1.2→0.5→0.2 시도 전부 원인 못 짚어 미해결이었음, 사용자 재제보로 규명). 원인: 그룹 라벨(CSS2DObject)이 `bubble` mesh의 자식인데 `bubble.scale=radius`(공유 단위구체 `_sharedBubbleGeo`를 반지름으로 확대, 1763·1932행)라 라벨 로컬 위치가 이미 radius배로 변환됨 — 근데 오프셋을 `radius*1.05`(로컬)로 줘서 월드에선 **radius²×1.05**가 돼 반지름 큰 행성일수록 planet 반지름의 수 배 위로 붕 떴음(radius 4.5면 표면 위 ~3.7배). 수정 2곳: ①오프셋을 "단위구체 기준 배수"로 — `radius*1.05`→`1.05`(index.html:2726, 1.0=표면·1.05=표면 위 5%, radius 곱 제거). ②라벨 텍스트 앵커 방향 — 인라인 `top:5px`(앵커 아래로, 작은 행성서 이름이 행성에 겹침)→`bottom:5px`(앵커 위로). Playwright 헤드리스로 **카드 열림/닫힘 두 상태 모두** 렌더 확인 — 그룹 이름이 행성 top 바로 위 "살짝 위"에 붙고(카드 열려도 멤버 라벨과 공존), 붕 뜨는 현상 소멸. 조정 시 1.05 숫자만(1.02≈거의 붙음, 1.10 더 위). [[RECURRING_BUGS]] 등재.

## 2026-08-21

- [완료][artists.json] 엑스원(X1) 우즈(전 조승연) `name.ko`를 "우즈(조승연)"→"우즈"로 정리(사용자 제보 — 콜라보 표기 시 "우즈(조승연)(엑스원)"처럼 그룹 접미사가 이중으로 붙어 지저분하게 보임). 원인은 에반(희승, 엔하이픈)과 같은 패턴 — 등록명 자체에 실명이 괄호로 같이 박혀있어서, 콜라보 태깅 시스템이 붙이는 "(그룹)" 접미사와 합쳐지면서 "이름(실명)(그룹)" 형태가 됨. 헤드리스로 태그 조합/역매칭까지 재현 확인 — 이제 "우즈(엑스원)"로 깔끔하게 생성되고 `_findArtistByConnName`도 정상 역매칭됨.
- [완료][artists.json] 엑스원(X1) 이은상 인스타그램 신규 추가, 이한결 인스타그램 계정 교체(사용자 제공 — 기존 `lee_gyul_gyul`→`lee_han_gyul`).
- [완료][index.html] 성능 예산 개선 2단계 — 세그먼트 다이어트(사용자 승인 후 진행, 전후 스크린샷 검증 완료). 그룹 구체 `SphereGeometry` 64×64→24×16, 장식 링(`RingGeometry`, NCT류 그룹) 96→32(스트로크 2곳). **트라이앵글 256만→68만(73% 감소)**, 저사양 모바일 예산 근처까지 도달. 원경/근경(코르티스 그룹 flyTo) 스크린샷 전후 비교로 시각 차이 없음 확인 — 프레넬 글로우 셰이더가 실루엣을 감싸는 아트스타일이라 세그먼트를 크게 낮춰도 각짐이 안 보인다는 사전 판단이 실측으로 들어맞음. 드로우콜 수는 변화 없음(세그먼트는 폴리곤 수만 줄임, 드로우콜 병합은 별도 과제로 보류).
- [완료][index.html] 성능 예산 감사(Fable) 검토 후 1단계(지오메트리 공유) 반영 — 실측(renderer.info+scene.traverse) 결과 Fable 진단 중 "그룹 구체 트라이앵글이 예산 초과 주범"(83%, 212만/256만)은 정확했고 "멤버 1,649개 개별 지오메트리라 InstancedMesh 필요"는 틀림(실제론 THREE.Points 5개로 이미 효율적으로 그려지고 있어 불필요), "장식 링(RingGeometry seg 96) 39개 그룹, 트라이앵글 20%"는 Fable이 놓친 추가 발견. 시각 위험이 전혀 없는 지오메트리 공유부터 사용자 판단으로 우선 진행(세그먼트 감소는 시각 검증 필요해 별도 보류) — 그룹 구체 263개가 각자 `SphereGeometry(radius,64,64)`를 새로 만들던 걸 단위 구체(r=1) 하나 공유 + `mesh.scale.setScalar(radius)`로 전환(반지름 r 구체를 균등 스케일한 것과 수학적으로 동일해 시각 차이 없음).
  - **작업 중 발견+수정**: 인트로 리빌 애니메이션(`_revealUniverse`)이 "scale=1이 완성 크기"라고 가정하고 있어서(지오메트리에 반지름이 굳어있던 시절 코드), 공유 전환 직후엔 모든 행성이 반지름 1(가장 작은 크기)로 굳어버리는 회귀가 실제로 발생 — 헤드리스로 `scale.x` 실측 확인 중 잡아냄. 리빌 애니메이션의 목표 scale에 `bm.radius`를 곱하도록 수정, 재검증 후 정상 확인(스크린샷 첨부 확인 완료).
  - 검증: 클릭→그룹카드 정상 오픈, 콘솔 에러 0건, 스모크테스트 통과, 고유 SphereGeometry 인스턴스 263→1 확인.
  - 다음 단계(세그먼트 다이어트, CSS2D 라벨 컬링, fps 계측)는 이번엔 미착수 — 세그먼트는 시각 위험이 있어 스크린샷 전후비교 먼저 필요.
- [완료][index.html] Fable "EN 모드를 영어권 유저 눈으로" 감사 검토 후 확정 버그 1건+구조 문제 2건 전부 실측 확인 후 수정 — 전부 반나절 이내 규모, 정적 SEO(en/g)가 이미 살아있어 지금 막아야 유입이 안 헛돈다는 Fable 판단에 동의.
  - **① 라벨 언어 버그(확정)**: EN 모드 접속 시 행성 라벨이 매번 한국어로 뜨다가 언어 토글 2회 왕복해야 영어로 바뀌던 버그, 실측 후 확정 — `applyLang()`의 `groupLabels` 갱신 루프가 데이터 로드 직후(라벨 생성 전)에 딱 한 번만 호출되고, 실제 라벨 DOM은 그보다 한참 뒤(`buildUniverse` 내부)에 만들어져서 그 갱신을 영영 못 받고 있었음. `_revealUniverse()` 직전에 `applyLang()` 재호출 추가 — 헤드리스로 EN 첫 로드 시 라벨 8개 샘플 전부 영문("CORTIS"/"aespa"/"IVE"/"NewJeans" 등) 확인.
  - **② 브라우저 언어 자동감지 부재**: `currentLang`이 무조건 `'ko'`로 시작해서 영어권 유저도 첫 화면부터 한국어로 시작하고 우상단 "KO" 버튼을 스스로 찾아야 했음. 저장된 설정이 없을 때만 `navigator.language`로 초기값 결정하도록 수정(en-US 에뮬레이션으로 `currentLang==='en'` 확인).
  - **③ 번역 커버리지 갭**: 프로필 열자마자 보이는 빈 상태 문구 3종(즐겨찾기/담아둔 영상/공연 기록)과 컬렉션 추가 토스트("~에 추가됨"/"다른 컬렉션")가 하드코딩 한국어였음 — `favEmpty`/`scrapEmpty`/`concertEmpty`/`colAddedSuffix`/`colChangeBtn` 5개 키 신설(정적 HTML 3곳 `data-i18n` 부착 + 동적 렌더 4곳 `T[currentLang]` 치환), 헤드리스로 EN 모드 렌더 확인.
  - **Fable 진단 중 1건 정정**: "내 우주 카드 만들기" 버튼이 미번역이라고 했는데, 확인해보니 이미 `data-i18n="ucCardBtn"` + EN 사전값("✦ Create My Universe Card")까지 정상 존재 — 실측 결과 이 버튼은 원래부터 정상 동작이었음.
  - **미착수(Fable 스스로도 후순위로 표기)**: aria-label 전체 한국어 고정(스크린리더 EN 유저), 검색결과 "WonyoungIVE"류 이름+그룹 태그 시각 구분, 라이트박스/피드 목록 EN 표기(샌드박스 한계로 이번에도 미확인) — 다음 세션 후보로 남김.
- [완료][index.html] 솔로 아티스트 연결 카드에서 본인 채널 콜라보 영상(예: 아이유×차은우)이 통째로 안 뜨던 버그 수정(사용자 제보 — "차은우 태깅된 영상이 연결 카드에 안 뜬다") — `_fetchApiCollabData`의 own 쿼리(`ownQ`)가 `members` 배열에 본인 이름이 들어있길 요구했는데, 실측해보니 솔로 아티스트 본인 채널 영상은 `members`가 항상 빈 배열이었음(아이유 539건 전수 확인 — "아이유"가 `members`에 들어간 행 0건). 그룹 채널은 "이 영상에 누가 나왔는지" 구분이 필요해 이 조건이 맞지만, 솔로 채널은 `group_ko=groupKo`인 것 자체가 이미 본인 영상이라는 뜻이라 조건 자체가 불필요+해로웠음. `GROUPS[anchor.group.ko]`가 있을 때만(진짜 그룹 멤버) 이 필터를 적용하도록 수정. 실측 결과 아이유 콜라보가 20곡→32곡, 32명→41명으로 늘어남(뷔·지디·뉴진스 5명·이기광·정용화·우즈·김세정·디오 등 새로 잡힘).
  - 멤버 카드 쪽 영상 그리드는 형제 함수 `_enrichMemberVideosFromApi`에 이미 `members={}` 전용 폴백 쿼리(`generalQ`)가 있어서 처음부터 정상 동작 중이었음(실측 확인 — 539+16건 전부 조회됨, 초기 48개 렌더 후 스크롤로 이어짐, 정상적인 페이지네이션). 사용자가 "다 노출 안 됨"이라 느낀 건 이 버그와 무관할 가능성 있어 추가 확인 필요.
  - **⚠️ 별도로 발견, 아직 미해결**: (여자)아이들·아이브·데이식스·트와이스처럼 `with_groups`(그룹 전체 태깅)를 거쳐 개별 멤버 전원이 로스터로 펼쳐진 콜라보는 `_groupWideNames`로 분류돼 연결 카드에서 멤버 칩이 아예 안 열림(그룹명만 표시, 개별 확장 불가) — 실제 DB의 `with_groups` 태그 자체가 개별 멤버 구분 없이 그룹 전체로 들어가있어서 생기는 현상으로 보이나, 의도된 동작인지 데이터 품질 문제인지 이번엔 판단 못함.
- [완료][index.html][kpop_universe.css] Fable "신규 유저 첫 3분 시뮬레이션" 자문 검토 후 2건 처리 — 나머지는 비판적으로 보류/재설계.
  - **① 온보딩 검색 "뉴" 버그(실측 재검증 후 수정)**: Fable은 "그룹 +0.5 감점 때문에 멤버만 5개 뜬다"고 진단했는데, 헤드리스로 실제 UI 경로(버튼탭→입력→렌더)까지 따라가 재검증했더니 원인이 달랐음 — 실제 5개 결과는 뉴(더보이즈)·뉴선(소나무)·NU'EST W 유닛멤버 3명(렌·백호·아론)이었고, 유닛 매칭이 "NU'EST"를 "뉴"의 로마자 표기로 오인해 같은 점수로 끼어들며 뉴진스·뉴이스트·뉴비트 그룹 3개를 전부 밀어낸 것. Fable이 제안한 수정 방향(온보딩 검색만 그룹 감점 제외)은 여전히 유효해서 그대로 적용 — 감점 없애니 "뉴"-접두 그룹들이 한글 정렬상 유닛멤버보다 앞으로 와서 뉴진스가 5위 안에 들어옴(헤드리스로 실측 확인). 일반 검색(40개 캡, 자리 넉넉함)은 감점 그대로 유지.
  - **② 나침반 탭 첫방문 뱃지 신설**: 탐험 패널의 존재를 신규 유저가 스스로 발견해야만 하는 문제(기념일/차트/디스커버리 전부 예고 없음, Fable 시뮬레이션으로 확인) 대응. 텍스트 없이 반짝이는 점 하나(`#tab-feed-badge`, `.tab-badge-dot`)만 표시, `localStorage`(`kpu_feed_badge_seen`)로 한 번이라도 열면 영구히 다시 안 뜨게 함. 헤드리스로 초기표시→클릭→소멸까지 실측 확인.
  - **검증된 사실이지만 보류**: 기본 줌 레벨에서 그룹 라벨 263개 전부 opacity 0(실측 확인, Fable 진단 정확) — 라벨 페이드 거리 자체를 만지면 263개 전체에 영향이라 오늘은 보류. Fable이 제안한 대체안(첫방문 텍스트 힌트)은 사용자가 "구리다"고 판단해 재검토 중 — 텍스트 없는 다른 방식으로 다시 고민 필요(예: ①②처럼 시각적 신호 위주).
  - **다음으로 미룸(계측/저위험, 급하지 않음)**: 로드타임 GA 계측 추가 — 버그 수정이 아니라 계측 신설이라 우선순위 낮게 판단, 필요시 별도 진행.
- [완료][index.html][kpop_universe.css] Charts "더보기" 버튼 아이콘 전용으로 교체 + "연말 무대 모음 TOP 100" 카드를 Discovery→Charts로 이동(사용자 요청 — "텍스트 적지 말고 아이콘만으로", "TOP 들어가면 다 charts 쪽으로"). 텍스트바(가로 100%, 텍스트+화살표)를 작은 원형 셰브런 버튼으로 축소, 접힘/펼침은 회전으로만 표현하고 텍스트는 title/aria-label에만 보조로 남김(기존 아이콘 전용 버튼들과 동일 패턴). 라벨에 "TOP"이 들어간 카드 중 유일하게 Discovery에 남아있던 `_buildFeedFestivalYearEnd`("연말 무대 모음 TOP 100")를 Charts로 이동 — KBS/MBC/SBS/썸머는 "모아보기"라 TOP이 없어 그대로 Discovery 유지. 스모크 테스트로 실브라우저 확인.
- [이슈→완료][index.html] **오늘 앞서 커밋한 "솔로 아티스트 연결 카드" 수정이 실제로는 회귀였음 — 재수정.** 사용자가 "여전히 다른 그룹 메인에서 아이유 태깅한 게 안 뜬다"고 재확인 요청해서 이번엔 코드 추론이 아니라 `yt_channel_videos`를 직접 조회해 실측했더니, 진짜 데이터는 "아이유(아이유)"가 0건, "아이유(솔로)"가 18건이었음 — 정반대였음. 오늘 오전 수정 때 admin.js의 `_isValidVidGroupKo` 주석("'솔로' 태그는 가짜 값이라 그대로 쓰면 안 됨")을 with_members 태그 형식에도 적용된다고 잘못 유추했는데, 그 주석은 **채널 동기화 키**(`_extOwnerGko`, group_ko 컬럼) 얘기였지 **콜라보 태그 접미사** 얘기가 아니었음 — 코드 주석만 보고 실측을 안 한 게 원인. 형제 함수 `_enrichMemberVideosFromApi`(멤버 카드용)는 애초에 `a.group.ko` 그대로 써서 처음부터 정상이었던 게 결정적 단서.
  - `_fetchApiCollabData`의 `withKey`를 `groupKo`(`_ytGroupKoFor`)→`anchor.group.ko`로 되돌림, `_findArtistByConnName`/`_getCollabMembers`는 실측 형식(`a.group.ko`)을 기본으로 하되 `_ytGroupKoFor` 형식도 방어적으로 같이 인정(그룹 소속 멤버는 둘이 항상 같은 값이라 기존 동작엔 영향 없음).
  - **실측 검증**: 헤드리스 브라우저로 `_fetchApiCollabData(아이유)`를 직접 호출해 확인 — 이제 루나(에프엑스)·지코(블락비)·(여자)아이들 5명·아이브 5명·데이식스 5명·트와이스 전원 등 20곡·29명이 정상적으로 잡힘.
  - **교훈**: 코드 주석이 그럴듯해 보여도 그게 "지금 내가 보는 이 코드"에 대한 얘기인지 확인 없이 유추하면 안 됨 — 이 프로젝트의 기존 원칙("Fable 보고서도 실측 재검증")을 내 스스로의 판단에도 똑같이 적용했어야 했음.
- [완료][shared.js][index.html][tests/matching.test.js] 파일 분리 0단계 완료(사용자 요청 — Fable 지적 4번 "파일 분리" 검토 후 위험 낮은 조각만 우선 진행, 나머지는 아래 백로그로 기록) — `_artistGroups`/`_ytGroupKoFor`/`_UNIT_HASHTAG_ONLY_TOKENS`/`_PROJECT_UNITS`를 새 `shared.js`로 이전. admin.js가 원래도 index.html과 같은 전역 스코프로 이 4개를 그냥 참조하던 구조였고(admin.js는 관리자 확인 후 늦게 동적 로드되니 순서 문제 없었음) 로직 변경은 전혀 없음 — `<script src="shared.js?v=2026-08-21a">`를 본문 스크립트보다 먼저 로드하도록 태그만 추가. admin.js는 무수정(전역 스코프라 그대로 동작).
  - **바로 증명된 가치**: `tests/matching.test.js`가 이 4개를 index.html에서 줄번호/중괄호 스캔으로 손으로 잘라내던 걸 이제 `require`+통째 실행으로 단순화 — 카피 드리프트 위험이 그만큼 줄었음.
  - **검증**: 매칭 테스트(`node tests/matching.test.js`) 12/12 유지, 스모크 테스트(`node tests/smoke.test.js`)로 실제 브라우저에서 `shared.js` 로드+`_artistGroups`/`_PROJECT_UNITS` 정의 확인+탐험 패널 정상 동작까지 확인.
  - **스모크 테스트 안정성 버그 발견+수정(부수 효과)**: 검증 중 스모크 테스트가 재실행마다 랜덤하게 "탐험 패널 안 열림"으로 실패하는 걸 발견 — 원인은 이 리팩터링이 아니라 헤드리스 브라우저 프로필 디렉터리를 고정 경로로 재사용해서 지난 실행의 localStorage(온보딩 힌트 등)가 남아있던 것(진단 스크립트로 원인 특정 — 새 프로필로는 항상 통과). `fs.mkdtempSync`로 실행마다 새 임시 프로필을 쓰고 종료 후 정리하도록 수정, 연속 3회 통과 확인.
  - **백로그(이번엔 미착수, 검토만)**: 1단계(나침반 탐험 패널/라이트박스 등 경계 뚜렷한 UI 서브시스템 분리) — 낮음~중간 위험, 다음에 여유 있을 때. 2단계(3D 유니버스/Three.js 씬 핵심부 분리) — 그룹카드·연결선·포커스 상태 등과 전부 뒤섞여있어 리스크 큼, 스크린샷 diff 같은 시각적 회귀 테스트가 먼저 생기기 전까진 보류 권장.
- [완료][tests/smoke.test.js] 배포 전 스모크 테스트 신설(우선순위 3, Fable 지적 "스테이징 없이 직배포·검증은 배포 후 육안"에 동의해서 진행) — 이 세션엔 Playwright 등 별도 브라우저 도구가 없어서 Node 24 내장 fetch/WebSocket으로 Chrome DevTools Protocol을 직접 구현, 헤드리스 msedge를 전용 프로필/포트로 띄워서 실제 페이지를 로컬 서버로 서빙+로드. 확인 항목: 로딩 오버레이 정상 해제(무한로딩 아님)·3D 캔버스 초기화·콘솔 에러 0건·나침반 탐험 패널 열기+Charts 카드 렌더+상호작용 중 새 에러 0건. **실행 결과 전부 통과.**
  - ⚠️ **msedge 프로세스 kill 안전장치**: 이 프로젝트에 "msedge를 프로세스 이름으로 일괄 kill하면 사용자의 실제 브라우저 창까지 꺼진다"는 기존 사고 기록([[feedback_dont_kill_edge]])이 있어서, spawn한 정확한 PID만 `taskkill /PID`로 종료하도록 작성 — PID 로그 출력 후 종료 확인까지 실측 검증(`tasklist`로 해당 PID 소멸 확인).
  - 실행: `node tests/smoke.test.js`. 이걸로 이번 세션에서 시작한 3종 검증 도구(매칭 테스트/데이터 검증/스모크 테스트)가 다 갖춰짐 — Fable "일류 IT 기업 개발자" 지적 우선순위 1~3 완료, 남은 4번(파일 분리)은 이번엔 착수 안 함.
- [완료][index.html][connections.json][tests/validate-data.js] 검증 스크립트가 찾은 3개 경고 후속 처리(사용자 확인) — "이종현"(연결 3건)·"태일"(연결 6건)·"김가람"(연결 5건)은 버그가 아니라 **의도적 제외 인물**이었음: 이종현(씨엔블루)·태일은 승리·크리스와 같은 사유(성범죄 등)로 퇴출된 인물, 김가람(르세라핌)은 공식 데뷔 전 탈퇴해 애초에 등록 대상이 아니었음. 3명 참조 connections.json 14건 삭제.
  - **씨엔블루 "종현"(이종현) 콘텐츠 차단 신규 추가**: 기존 `_BANNED_VIDEO_NAMES_GLOBAL`/`SCOPED`(승리/힘찬 등, "성범죄로 퇴출된 인물 관련 영상은 이 우주에 어떤 형태로도 노출되지 않아야 함")에 이종현이 빠져있던 걸 발견 — 샤이니에도 실존 활동중 멤버 "종현"이 있어 전역으로 걸면 안 되므로 `_BANNED_VIDEO_NAMES_SCOPED`에 `'씨엔블루':['종현']`로 그룹 한정 추가.
  - `validate-data.js`에 알려진 제외 인물 화이트리스트(`_KNOWN_EXCLUDED_PEOPLE`) 추가 — 앞으로 같은 이름이 다시 경고로 뜨지 않음. 남은 경고는 "Yoona"(소녀시대 윤아 연결 6건) 1건뿐 — connections.json이 이름만으로 식별하는 구조 자체의 한계(최근 데뷔한 아일릿에도 동명이인 "윤아" 있음)라 별도 설계 검토 필요, DECISIONS로 보류.
- [완료][artists.json] 솔로 디스코그래피 잔여 13명 완료 — 민호·종현·크리스탈·제니·지수·로제·리사·지디·태양·B.I·BOBBY·나연·채영(비아이/바비는 `name.ko`에 "B.I"/"BOBBY"로 등록돼있음). 기존 확립된 워크플로우(나무위키 원문 확인 → 멜론 대조 → 커버 이미지 실제 로딩 검증) 그대로 적용, 포크(백그라운드 서브에이전트)가 진행 중 폴더 소실 사고(아래 항목 참고)를 겪었지만 복구 후 전량 무사히 반영 완료.
  - **⚠️ 가장 중요한 발견 — 크리스탈(에프엑스) "솔로 음반 없음" 기록이 stale했음.** 지난 라운드(2026-08-20)에 "f(x) 그룹뿐, 솔로 음반 없어서 스킵"으로 기록해뒀던 건데, 이번에 재확인하니 **2025년 11월 27일 데뷔 16년 만에 첫 솔로 싱글 'Solitary' 발매** + 2026년 5월 26일 두 번째 싱글 'PWLT'까지 나온 상태였음. "예전에 없었다"는 과거 기록을 그대로 믿지 않고 매번 재검색한 덕분에 잡아낸 케이스.
  - **완료 인원별 앨범 수**: 민호 5(전부 신규 조사)·종현 5(기존 3 + 이야기 Op.1·Op.2 2개 발굴)·크리스탈 2(전부 신규, 위 참고)·제니 4(기존 1 + SOLO·You&Me·Less than a Lover 3개 추가, 프리싱글 3곡은 이미 Ruby 앨범에 포함돼있어 중복 안 넣음)·지수 3(기존 1 + ME·EYES CLOSED 2개 추가, EYES CLOSED의 "2X"/"BARE" 리믹스 재발매 2건은 같은 곡 변주라 제외)·로제 2(기존 1 + R 1개 추가, APT.·number one girl은 이미 rosie 앨범에 포함)·리사 2(기존 1 + LALISA 1개 추가, 4개 프리싱글은 이미 Alter Ego에 포함)·지디 5(기존 4 + 미니2집 '권지용' 1개 발굴, 무제(無題)가 타이틀곡)·태양 6(기존 그대로, 갭 없음 확인)·B.I 7(기존 6 + BUZZIN 싱글 1개 발굴)·BOBBY 5(기존 3 + S.i.R·Sir.Robert 2개 발굴, Sir.Robert는 S.i.R+ROBERT 전곡을 재수록한 정규3집이라 3개 다 별개 유지)·나연 2(기존 그대로, 갭 없음)·채영 1(기존 그대로, LIL FANTASY vol.1이 데뷔 유일작 맞음 확인).
  - **데이터 오염 정리**: 이번에 처음 만진 사람들 중 상당수가 옛 `soloDiscography` 필드(Apple Music 스크레이핑, 트랙 정보 없이 커버만 있던 구버전 — 지금은 index.html 어디서도 안 읽는 죽은 필드)를 갖고 있었는데, 이 필드 자체가 신뢰할 수 없는 걸로 확인돼 손댄 사람은 전부 제거(`discography`로 완전 대체): **민호는 존재하지도 않는 가짜 싱글("August 16th", 2020) 하나만 들어있었고, BOBBY는 아예 동명이인(유럽 계열 아동/청소년 가수로 추정, "Bittersweet Sixteen" 등 무관곡 10곡) 전체가 잘못 매칭돼있었고, 리사는 2009년(블랙핑크 데뷔 7년 전) "Lisa"라는 이름의 무관 EP가 섞여있었음.** 제니/지수/로제도 각각 OST·리믹스·콜라보 단독곡이 "솔로 앨범"인 것처럼 뒤섞여 있었음 — 전부 멜론 기준 실제 솔로 발매만 골라 `discography`에 재구성.
  - 커버 이미지는 브라우저 Image() 로딩 대신(도구 부재) `curl -k`로 HTTP 200 + `image/jpeg` content-type 확인하는 방식으로 대체 검증 — 완전히 동등하진 않지만 실측 대안으로 기록.
  - B.I/BOBBY는 원래 13명 리스트엔 없었으나(비아이·바비로 이미 포함돼있었음) 보너스로 갭체크까지 완료.

- [완료][tests/validate-data.js][connections.json] 데이터 무결성 검증 스크립트 신설(우선순위 2, 테스트 스위트에 이어서) — artists.json 중복 인물(이름+생일 동일)·group.ko/groups[].ko 유령 참조·필수 필드 누락·groups.json 중복 최상위 키(JSON.parse가 조용히 마지막 값만 남기는 함정)·producedBy 유령 참조·connections.json 유령 참조 6종 검사. 실행: `node tests/validate-data.js`.
  - **실제 실행해서 진짜 문제 발견**(검증 스크립트의 가치를 바로 증명한 셈): connections.json에서 "희승" 6건이 실제 등록명 "에반(희승)"이 아니라 잘못된 형태로 저장돼있어 엔하이픈 멤버 연결이 통째로 유령 참조였음 — `"에반(희승)"`으로 정정 완료.
  - **오탐 1건 발견+수정**: 빛새온/김민서(B.D.U) 같은 "솔로 primary + 실제 그룹 secondary" 인물은 `groups[]`에 자신의 primary인 "솔로"를 0번째로 포함하는 관례라(마크의 groups[0]=엔시티127과 같은 패턴), 이 필드도 group.ko와 동일하게 "솔로"를 예외 처리하도록 수정.
  - **남은 경고 3건 — 판단 필요해서 그대로 둠**(자동 수정 안 함): ①"Yoona"(소녀시대 윤아 그룹 연결 6건) — 단순히 "윤아"로 고치면 최근 데뷔한 아일릿에도 동명이인 "윤아"(Yunah)가 있어서 오히려 새로운 모호함이 생김. connections.json이 이름만으로 식별하는 구조 자체의 한계 — 그룹 구분자 추가가 근본 해법(DECISIONS 항목 후보). ②"김가람"(르세라핌 연결 5건) — 실존 인물(공식 데뷔 전 탈퇴한 전 르세라핌 멤버)인데 artists.json에 아예 없음, 포함 여부는 판단 필요. ③"이종현"(씨엔블루 정용화·강민혁·이정신과 연결 3건) — 씨엔블루 이종현으로 추정되나 artists.json에 없음(샤이니 "종현"은 동명이인, 다른 사람).
- [이슈] **`Downloads\kpopuniverse-main` 폴더 내용물 전체 소실 + 복구.** 작업 도중(포크 세션이 artists.json 편집 중) 이 폴더가 `tests/` 하위폴더 하나만 남기고 통째로 비워지는 사고 발생 — 원인 불명(포크는 rm/mv류 명령을 쓴 적 없다고 확인, 사용자도 "안 지웠다"고 했다가 뒤늦게 "내가 다시 복원했다"고 정정 — 정확히 무엇이 지웠는지는 끝내 특정 못함, [[project_kpop_universe_git_setup]] 멀티세션 동시편집 리스크와 연관 가능성). **다행히 사용자가 직접 복원했고 대조해보니 데이터 유실은 거의 없었음** — `index.html`/`admin.js`/`CHANGELOG.md`는 git 리포(`Documents\GitHub\kpopuniverse`)와 완전히 일치, `artists.json`만 그 리포보다 더 최신(포크가 진행 중이던 솔로 디스코그래피 편집분이 이미 저장돼 있었음)이라 오히려 복원본이 더 최신이었음. ⚠️ **재발 방지 교훈**: git이 없는 `Downloads\kpopuniverse-main`은 이런 사고에 무방비이니, 앞으로 큰 작업 전엔 더 자주 git 리포에 동기화해둘 것(현재도 매 변경마다 push하고는 있으나 간격을 더 좁히는 게 안전).
- [완료][tests/matching.test.js] 매칭 엔진(`_m2ParseTitle`) 회귀 테스트 스위트 신설(사용자 요청 — Fable 자문 "일류 IT 기업 개발자가 봤다면?" 사고실험에서 "테스트 제로"가 1순위 지적으로 나옴, 검증 후 동의) — 빌드스텝 없는 프로젝트라 admin.js/index.html 원본에서 함수 선언부를 이름으로 찾아 그대로 잘라내(중괄호 균형 스캐너, 하드코딩 줄번호 아님 — 리팩터링에도 안 깨짐) Node vm에서 그대로 실행, 실제 배포 코드 자체를 테스트(카피 드리프트 없음). 과거 실제 사고 12개를 회귀 케이스로: 트리플에스 AAA/15&·여자친구(strictSync)·단일음절 이름(키/온)·지유 동명이인·마크 겸임소속 등. **12/12 통과.** 실행: `node tests/matching.test.js`. 커버 범위는 `_m2ParseTitle` 하나뿐(가장 사고 많던 함수), DB 로드 동적 화이트리스트(`_ATM_DYNAMIC_*`)는 빈 Set이라 하드코딩된 보호 로직만 검증됨.
  - **테스트 작성 중 신규 발견**(DECISIONS 참고): "마크" 동명이인(엔시티 겸임 vs GOT7 마크 투안) 처리가 코드만으론 결과를 100% 확신 못하는 지점 — 급한 오염 사례는 아직 없음.
- [완료][index.html] 솔로 아티스트(GROUPS에 없는, group.ko="솔로" placeholder) 연결 카드에서 "다른 채널이 나를 콜라보로 태깅한" 역방향 연결이 거의 안 잡히던 버그 수정(사용자 제보 — "아이유 연결 카드 보면 분명 연결이 더 많은데 아주 일부만 나온다"). 실측: artists.json에서 "아이유"가 다른 아티스트의 `songs[].with`에 태그된 사례는 0건이었고, IU 본인의 `songs`도 빈 배열 — 그런데도 "일부"는 뜨고 있었으므로 그 일부는 `_fetchApiCollabData`(실시간 DB 조회)에서 온 것으로 판단, 그 함수를 정독해서 원인 특정.
  - **근본 원인**: 다른 채널이 이 사람을 콜라보로 태깅할 때 붙는 "(그룹)" 접미사는 admin.js 태깅 파이프라인이 명시적으로 `_ytGroupKoFor` 규칙(솔로는 본인 이름이 곧 그룹 키 — "'솔로 태그'는 여러 명이 공유하는 가짜 값이라 그대로 쓰면 안 됨"이라는 기존 주석으로 이미 확립돼있던 규칙)을 따르는데, `_fetchApiCollabData`의 역방향 조회 키(`withKey`)만 이 규칙을 안 따르고 `anchor.group.ko`(솔로면 literal "솔로")를 그대로 써서 실제 DB엔 "아이유(아이유)"로 저장된 걸 "아이유(솔로)"로 찾고 있었음 — 그룹 소속 멤버는 `_ytGroupKoFor(a)===a.group.ko`라 이 차이가 안 드러나고 솔로 아티스트에서만 조용히 실패하던 버그.
  - 수정 3곳: ① `_fetchApiCollabData`의 `withKey` 생성을 `anchor.group.ko`→`groupKo`(`_ytGroupKoFor` 결과)로, ② `_findArtistByConnName`의 "이름(그룹)" 파싱 후 매칭을 `a.group.ko===m[2]`→`_ytGroupKoFor(a)===m[2]`로(안 그러면 API가 이제 올바르게 찾아온 "아이유(아이유)" 문자열을 정작 클릭했을 때 실제 아티스트로 못 찾음), ③ `_getCollabMembers`의 정적 데이터(artists.json) 쪽 동일 패턴도 같이 정정(현재는 실제로 걸리는 데이터 없음, 향후 대비). 그룹 소속 멤버는 `_ytGroupKoFor(a)===a.group.ko`라 세 곳 다 기존 동작과 100% 동일 — 솔로 아티스트에서만 새로 정확해짐.
  - ⚠️ 실제 브라우저 확인 도구가 없어 코드 수정만 완료, IU 카드로 실제 확인 필요.
- [완료][index.html] 그룹 라벨(행성 이름)이 행성 내부로 들어가 멤버 이름과 겹치는 버그 수정(사용자 제보) — 라벨을 행성의 로컬 Y축(`radius+1.2`)에 고정해뒀던 게 원인. 카메라가 위/아래에서 내려다보는 각도가 되면 그 축이 "화면상 위쪽"과 거의 평행해져서 라벨이 행성 실루엣 한가운데(=행성 내부)로 투영됨. 고정 로컬축 대신 매 프레임 카메라의 실제 화면상 위쪽 방향(`camUp=Vector3(0,1,0).applyQuaternion(camera.quaternion)`)으로 오프셋을 재계산하도록 변경(`bubbleMeshes`에 `labelObj` 참조 추가) — bubble에 회전을 안 주는 걸 확인(grep)했으므로 월드 공간 camUp을 로컬 오프셋으로 그대로 사용. 이제 어느 각도에서 봐도 라벨이 항상 행성 바깥(화면상 위)에 남음. ⚠️ 실제 브라우저 확인 도구가 없어 코드상 수정만 완료, 실기기 확인 필요.
- [완료][admin.js][index.html] 우선순위 정리 3건 중 1~2번 처리(사용자 요청 — CHANGELOG 복기해서 우선순위 1~3위 선정 후 순서대로 진행) —
  - **① 아이돌개인 채널 owner_gko 소급 백필**: 어제 등록 폼에 동명이인 그룹선택 드롭다운을 추가했지만 신규 등록부터만 적용돼서, 그 전에 등록된 채널은 여전히 이름만으로 그룹을 추측하는 위험한 상태였음. 새 일회용 버튼 `_ecBackfillOwnerGko`(`sp-ec-backfill-ownergko-btn`) — 매치 0~1명(동명이인 없음)인 채널만 안전하게 자동 백필, 매치 2명 이상(진짜 동명이인)은 자동으로 못 정하니 이름만 보고하고 삭제 후 재등록하도록 안내. 실행은 admin 세션에서.
  - **② 그룹배정 검수센터(승인/거부 버튼) 코드 재검증**: 실브라우저 클릭 테스트 도구가 이번 세션에도 없어서 코드 정독으로 대신 확인 — `review` 탭 쿼리(`needs_review=true`)·탭 버튼(`data-tab="review"`)·`_vmReviewDecide`(승인→content_flag null, 거부→'무관', 둘 다 needs_review false) 전부 정상 연결 확인, 구조적 결함 못 찾음. **여전히 실제 클릭 1회는 확인 안 됨** — 다음에 확인 필요.
- [완료][index.html][kpop_universe.css] Charts 섹션 접기/펼치기 + 정렬 조정(사용자 제보 — 랭킹 카드 10종으로 늘어나면서 Discovery가 너무 아래로 밀림) — 기본 4장(2행)만 노출, "더보기 ⌄"/"접기 ⌃" 토글(`#feed-chart-more`, CSS `nth-child(n+5)` 방식이라 비동기 도착 카드 수와 무관하게 항상 4번째까지만 보임). 패널 재오픈마다 항상 접힌 상태로 리셋(사용자 요청 — "접은 게 기본"). Trend 카드는 개념상 고정 랭킹이 아니라 랜덤 발견 카드라 Charts가 아니라 Discovery로 재배치(`_buildFeedWeeklyTopCams`에 `discList` 파라미터 추가) — 그 결과 Charts 상단이 "주간 개인 직캠 TOP 20"/"월간 무대 TOP 30"(최근 기준)로 시작하고 그 뒤로 연도별/역대 TOP류(오래된 순)가 이어지는 자연스러운 최신순 배치가 됨.
- [완료][index.html][kpop_universe.css] 탐험 패널 밀도/층위 개편 — Fable UX 감사 3건(밀도·고정/랜덤층 분리·최근 본) 코드로 재검증 후 반영, 개인화 50%믹싱·탭 통합 2건은 비판적으로 보류(전자는 방금 합의한 라이트 버전과 충돌, 후자는 탭바 구조 변경이라 별도 세션 필요).
  - **밀도**: `#feed-discovery`가 실측 확인 결과 진짜 1열 풀블리드였음(`.feed-card-thumb-wrap{aspect-ratio:16/9}`, 폭 100%) — `.feed-grid{display:grid;grid-template-columns:1fr 1fr}`로 전환, 카드 자체(`_appendFeedCard`)는 그대로 재사용하고 컨테이너 CSS만 교체(다른 호출부 영향 없음).
  - **고정/랜덤층 분리**: `_buildFeedDiscovery()`의 22개 카드는 원래도 호출 순서가 고정이라 실제로는 랜덤이 아니었음(레전드100 등 무한스크롤 반복 풀에서 이미 제외돼있던 것도 확인) — 문제는 시각적 구분이 없었던 것. 랭킹류 10개 카드(주간/월간 TOP·연도별 TOP100·레전드100·역대 개인/여돌/남돌 직캠 TOP100·역대 여돌/남돌 무대 TOP100·세대별 TOP)를 새 `#feed-chart`("📊 Charts") 섹션으로 분리, 나머지는 "✨ Discovery"에 유지. "Chart" 단수보다 스포티파이 실제 표기와 일치하는 "Charts" 복수형 채택.
  - **최근 본**: `openLightbox` 진입점 1곳에서 `_recordRecentView`로 최근 20개를 localStorage에 기록(서버 저장 없음, 기기 로컬), 기념일 스트립 바로 아래 "🕐 Recently Viewed" 가로 행(`#feed-recent`) 신설 — 원형 아바타(`feed-strip-item`)와 다른 16:9 사각 썸네일 마크업(`feed-recent-item`) 새로 추가.
  - ⚠️ 실제 브라우저 렌더링/터치 확인은 이번 세션에서 못 함(테스트 도구 없음) — 실기기 확인 필요.

- [완료][index.html][kpop_universe.css][kpopuniverse-share] 주간/월간 TOP 순위 변동 배지 신설 — "매주 한 번 트리거는 어색하다, 랭킹 자체는 롤링 7일인데 비교 주기는 일간이어야 자연스럽다"는 사용자 지적 반영해 일간 스냅샷으로 설계. 새 카드 "월간 무대 TOP 30" 추가(최근 30일, 필터 없이 조회수 기준, "완전체 무대"라는 이름은 사용자가 어색하다고 해서 폐기하고 그냥 기간을 늘린 별도 차트로 단순화) — 기존 "주간 개인 직캠 TOP 20"과 함께 둘 다 순위 배지 대상. Trend 카드는 그대로 두되 노출 순서만 랜덤 셔플로 변경(셀렉 기준은 조회수 유지, 순위 배지는 미부착 — "차트"가 아니라 발견용이라는 사용자 판단).
  - DB: `rank_snapshots(chart_type, snapshot_date, video_id, rank)` 신규 테이블 — public read RLS, 쓰기는 서비스롤 전용. SQL 실행 완료, Vercel `SUPABASE_SERVICE_ROLE_KEY` 환경변수 설정 완료(사용자 확인).
  - `kpopuniverse-share`(Vercel) 신규: `api/cron/snapshot-ranks.js` — 매일 UTC 00:05(`vercel.json` crons) 두 차트의 오늘 순위를 계산해 upsert. `SUPABASE_SERVICE_ROLE_KEY`는 코드에 하드코딩하지 않고 Vercel 환경변수로만 설정 필요(사용자 조치 대기) — anon key(읽기 전용, 기존 `api/col/[id].js`에서처럼 공개해도 무방)와 달리 서비스롤 키는 절대 커밋 금지.
  - 프론트: `_fetchPrevRanks(chartType)`가 어제 스냅샷 조회, 스냅샷이 아직 없으면(신규 배포 직후) 배지 자체를 숨김(전부 "NEW"로 잘못 표시 방지). 배지는 `_openFeedListOverlay`의 공용 그리드 렌더러(`gc-ch-item`)에 `s.rankDelta` 있을 때만 그리는 방식으로 추가해 다른 호출부(컬렉션 등)엔 영향 없음.

- [완료][index.html][kpop_universe.css] 라이트박스 연속재생 신설 — Fable 자문 세션이 코드 정독으로 지적한 "영상 종료 postMessage(playerState=0) 미처리"를 코드로 재검증 후 확인(재생 시작=1만 처리, 종료 이벤트 자체가 없었음). shorts 모드는 스와이프 넘기기와 같은 슬라이드 애니메이션으로 즉시 다음 영상, browse 모드는 5초 카운트다운 배너(`#yt-lb-autonext`, 취소 가능) 후 자동 이동. 목록 끝이면 자연스럽게 멈추고, 종료 이벤트 중복 수신 대비 가드(`_lbEndedHandled`)도 넣음. **⚠️ 샌드박스가 유튜브 iframe을 막아 실제 재생/배너 겹침 여부는 미검증 — 실기기 확인 필요.**
  - Fable이 제안한 나머지(하트/리액션/북마크 통합, 주간 순위 변동, 피드 3층구조+밀도, 탭 통합)는 비판적으로 검토 후 보류 — 액션 통합은 제품 판단 필요, 주간 순위는 서버 스케줄러가 없어서(오늘 새로 생긴 Vercel 인프라로 가능은 해짐) "작음"이 아니라 "인프라 추가 필요"로 재평가, 나머지는 구조 개편급이라 별도 세션 필요.

- [완료][index.html][admin.js] Fable 태깅 정확도 감사(361,230행 전수) 기반 실제 버그 규명+수정 — 감사가 "성뗀변형_동명충돌"(881건)로 분류한 유형을 실측 검증했더니 진단이 틀렸음: 실제로는 `_PROJECT_UNITS`에 등록된 트리플에스 서브유닛 트리거명이 흔한 단어/약칭과 충돌한 것이었음.
  - **"AAA"**(Acid Angel from Asia 유닛 약칭) = 아시아 아티스트 어워즈(Asia Artist Awards) 약칭과 완전히 겹쳐, IVE·RIIZE·제로베이스원·TXT·다크비·NMIXX·키키·크래비티 등 8개+ 그룹의 "AAA 비하인드" 영상마다 무관하게 트리플에스 멤버 4명이 붙음.
  - **"Aria"/"아리아"** = 흔한 단어(곡명)라 다른 그룹들의 해당 곡 챌린지/커버 해시태그 크레딧(예: 스테이씨·아이칠린·싸이커스의 "#Bubble_Challenge... #아리아")에도 걸려 5명이 잘못 붙음.
  - 추가 재점검(사용자 지적 — "나무위키에 있는 유닛을 다 기계적으로 반영해놔서 위험 검토가 안 돼있었음")으로 3개 더 발견: **"EVOLution"**(ONEWE "O!NEW Evolution" 콘서트·2NE1 "NEW EVOLUTION" 투어·크레용팝 "Evolution" 앨범 등과 충돌), **"Glow"**(흔한 영단어), **"hatch!"**(흔한 영단어), **"NXT"**(짧은 약어, WWE NXT 등과 겹침).
  - 수정: "AAA"/"EVOLution"/"Glow"/"hatch!"/"NXT" 5개는 해시태그 전용 매칭으로 좁힘(`_UNIT_HASHTAG_ONLY_TOKENS`, 트리플에스 자체 채널의 정식 해시태그는 그대로 인식), "Aria"/"아리아"는 안전한 대체 트리거가 없어(챌린지 크레딧도 해시태그를 쓰므로 hashtag-only로도 구분 안 됨) 유닛 자체를 title 자동매칭에서 제외. `_unitMembersFromTitle`(자체 채널 경로)·`_m2ParseTitle`(외부채널 경로) 양쪽 다 반영 — 한쪽만 고치면 반쪽만 고쳐지는 함정이 있었음(코드에 이미 별개로 구현된 동일 로직 2곳).
  - **소급 정리용 일회용 버튼** 신설: "트리플에스 유닛명 오매칭 정리(일회용)"(`_ytCleanupUnitTokenMistag`) — 고친 로직으로 다시 매칭해서 더는 안 걸리는 with_members만 제거(진짜 콜라보는 보존). 실행은 admin 세션에서.
  - 검증: 문제였던 실제 제목들(IVE AAA MC 비하인드, 2NE1 NEW EVOLUTION 투어, ONEWE 해시태그) 전부 이제 매칭 안 되고, 정상적인 해시태그 기반 진짜 언급은 여전히 매칭되는 것 Node로 직접 확인.
  - **교훈**: Fable 리포트를 코드로 재검증하는 습관이 실제로 오진단을 잡아냄(881건 중 상당수가 "동명이인 문제"가 아니라 "유닛 트리거 흔한단어 충돌"이었음) — 이 프로젝트 "재검증 필수" 원칙 재확인. [[project_kpop_tagging_accuracy_audit]] 참고.
- [완료][admin.js] Fable 감사 5번 유형("원곡커버 오인") 원천 차단 — "(원곡: X)"/"[Dance Cover]"/"(X 커버)" 절 안의 이름이 실제 출연자가 아니라 커버 대상(원곡자)인데 콜라보로 잘못 붙는 문제. 이미 있던 관리자 검수 전용 헬퍼 `_wonkokStripClause`(기존 데이터 찾기용 `_wonkokScan`에서만 쓰임)를 `_m2ParseTitle` 진입점에 통합해서, 매칭 전에 그 절을 먼저 제거 — 새로 들어오는 영상부터 원천 차단됨. "마마무 - 아주 NICE(원곡: 세븐틴)"→"마마무 - 아주 NICE"로 정상 스트립, 무관 제목은 안 건드리는 것 확인. 미착수: "8TURN - 부석순 '파이팅해야지'"처럼 원곡/커버 키워드 자체가 없는 경우(잔여_개별검토 2,594건 상당수)는 디스코그래피 대조가 필요해 별도 작업.
- [완료][DB][admin.js] Fable 감사 2번 유형("흔한단어·성뗀변형·영문축약 오태깅", 1,722건) 중 확정된 10명 처리 — `name_match_whitelist`에 고우리(→"우리" 389건)·미래(체리블렛, 380건)·아이엠(몬스타엑스, 309건)·리세(레이디스 코드)·써머(헬로비너스)·이현(베이비돈크라이)·태양(빅뱅)·지디(빅뱅)·솔라(마마무)·김채원(르세라핌/트리플에스/에이프릴 3명 동시 존재, 4번 유형 "동일표기 동명이인"과 겹침) 추가 → 해시태그 전용으로 전환. 소급 정리용 일회용 버튼 "흔한단어 멤버명 오매칭 정리(일회용)"(`_ytCleanupHashtagOnlyMistag`) 신설 — 트리플에스 정리 버튼과 동일 패턴(with_members만 대상, group_ko는 안 건드림). 실행 완료(사용자 admin 세션, 트리플에스 버튼은 474개 중 345개 행에서 1,542건 제거 확인됨).

- [완료][index.html][admin.js] 어드민 "라이브잠금" 검수 탭 신설(사용자 요청) — "영상 카테고리 재분류(전체)"(`sp-catfix-btn`/`_ytSweepCategoryMistag`)는 이미 있었고 "OO 최애직캠"류 브랜드명 붙은 제목도 `_ytClassify`가 부분문자열 매칭("직캠")으로 이미 잡아주지만, `tags_manual=true`(수동 편집)인 행은 원칙상 이 자동 재분류가 절대 안 건드리고 조용히 건너뜀 — 그 목록이 안 보이는 게 진짜 빠진 기능이었음. 새 `_vmTab='catlock'` — `tags_manual=true`면서 카테고리가 live가 아닌데 `_ytClassify(title)==='live'`인 후보만 걸러 보여줌(자동 수정은 안 함, ✎로 개별 확인).
- [완료][index.html][kpop_universe.css] 컬렉션 공유 이미지 카드 신설(사용자 요청 — "네이버 블로그/인스타/X에 카드 공유할 때 이미지가 없어서 별로") — 기존 "내 우주 카드"/"데뷔 기념일 카드"(3D 씬 캡처+텍스트 합성)와 같은 톤이지만, 컬렉션은 3D 공간의 한 점에 안 묶여있어 이미지 소스가 다름: 커스텀 커버(`col.coverImg`) 있으면 그것 하나를 크게, 없으면 담긴 영상 썸네일 최대 4개를 2x2 그리드로(사용자와 확인한 방향). `#col-card-overlay` 신설, 컬렉션 상세("내 컬렉션"만, `ctx.mine&&ctx.colId`) 헤더에 공유 버튼 추가. 4:5 비율(인스타 피드용). **⚠️ CORS 미검증** — 외부 이미지(유튜브 썸네일/Supabase Storage)를 캔버스에 그려서 `toDataURL`하는 게 이 세 카드 중 처음이라, 소스가 CORS 허용 안 하면 그 낱장만 빈칸으로 빠짐(전체 실패는 아님) — 실제 브라우저에서 커버 있는 컬렉션/없는 컬렉션 둘 다 확인 필요.
  - **우선순위 재조정(사용자 요청)**: 그룹/멤버 카드는 "내 우주 카드" 비주얼 다듬기가 먼저라 보류, 대신 탐험 패널 "모음" 카드 공유를 바로 이어서 완료 — `_openFeedListOverlay`의 공유 버튼 노출 조건을 `ctx.mine===false`(남의 공개 컬렉션)만 빼고 전부 허용으로 확장, `_openColCardPreview`가 컬렉션 id 문자열뿐 아니라 `{name,videos}` 즉석 객체도 받게 일반화(모음 카드는 저장된 컬렉션이 아니라 그때그때 만들어지는 목록이라 coverImg 없이 항상 썸네일 그리드 경로를 탐). 그룹/멤버 카드는 다음 세션.
  - **후속 확장(사용자 요청 — "링크가 있어야지, 이미지는 링크를 위한 것")**: 공유에 링크가 없으면 의미 없다는 지적으로 큰 폭 확장 —
    - **컬렉션 딥링크(`#col=id`) 신설** — `_openFromHash`에 `col` 파라미터 분기 추가, `_openCollectionFromHash`가 내 컬렉션은 로컬에서, 남의 공개 컬렉션은 `public_collections` 단건 조회로 열어줌.
    - **공유 = 반드시 공개 상태여야 함** — `_openColCardPreview`가 내 컬렉션이면서 비공개면 "공개로 전환할까요?" 확인 후 진행(비공개 링크는 남에게 무의미하므로). 남의 공개 컬렉션도 공유 가능하게 확장(전엔 내 것만).
    - **탐험 모음 카드 → "컬렉션에 저장" 버튼으로 전환** — 임시 목록은 고정 링크가 없어 공유 버튼 대신 저장 버튼만 노출, 저장하면 그 자리에서 바로 내 컬렉션 상세로 전환(그 다음부터는 위 공유 플로우 그대로 탐).
    - **⚠️ 새 인프라: Vercel 서버리스 함수 추가 배포** — GitHub Pages(정적)는 요청마다 다른 og:image를 못 내려줘서 네이버 블로그 등에 링크만 붙였을 때 자동 미리보기가 안 뜨는 문제 발견, 사용자가 "자동 미리보기도 되면 좋겠다"고 요청. 새 저장소 `kpopuniverse-share`(Vercel에 별도 배포, GitHub Pages 메인 사이트와 무관) 신설 — `api/col/[id].js`가 `public_collections`를 REST로 직접 조회해 og:title/og:image/og:description 찍은 HTML을 즉시 응답하고 실제 클릭 시 `kpop-universe.kr/#col=id`로 리다이렉트. Gabia DNS에 `share.kpop-universe.kr` CNAME 레코드 추가(Vercel 타겟), SSL 발급 확인 완료, curl로 라이브 동작 검증 완료(`/`→메인사이트, `/col/x`→앱 해시로 정상 리다이렉트). 앱 쪽 `_colShareUrl(colId)`이 이제 이 서브도메인 주소를 만들어 공유 버튼에 씀(내부 네비게이션용 `_colDeepLink`/`#col=` 해시와는 별개 — 그건 앱이 직접 열 때만 씀).
    - GitHub 파인그레인드 토큰이 특정 저장소만 허용해서 새 저장소 push가 403 나는 일이 있었음 — classic PAT(`repo`+`workflow` 스코프)로 재발급해서 해결. 토큰은 이번 대화에 평문으로 남아있으니 신경 쓰이면 나중에 재발급 권장(급한 건 아님).
    - **실사용 중 재발견한 버그+수정**: 컬렉션 공유를 데스크톱(마우스)에서 실제로 눌러보니, Web Share API가 띄우는 Windows 네이티브 공유창에서 "복사"를 눌러도 클립보드에 실제로 안 들어가 있고 창을 닫으면 그대로 유실되는 사고 재현(사용자 제보). 그룹/멤버 카드 공유 버튼(`_shareCurrentView`/`_isTouchPrimary`)에서 이미 겪고 고쳤던 것과 똑같은 패턴인데 새로 만든 카드 3종(컬렉션/내우주/데뷔기념일)엔 그 구분이 없었음 — `_shareColCard`/`_shareUcCard`/`_shareDebutCard` 전부 `_isTouchPrimary()`로 감싸서, 터치기기만 OS 공유시트 쓰고 데스크톱은 무조건 앱이 직접 클립보드 복사+다운로드로 확정 처리하도록 통일.
    - **두 번째 실사용 버그+수정**: 링크는 잘 복사되는데 네이버 블로그 미리보기가 우리가 만든 카드가 아니라 원본 영상 썸네일로 뜸(사용자 제보) — 캔버스로 만든 카드 이미지가 브라우저 안에만 있고 실제 서버에 업로드된 적이 없어서 링크 미리보기 봇이 볼 방법이 없었던 게 원인. 컬렉션 미리보기를 열 때(내 컬렉션 한정) `_uploadColShareCard`가 카드를 Supabase Storage(`artist-pics/col-share-cards/{id}.png`, 기존 컬렉션 표지 업로드와 같은 버킷 재사용)에 업로드하고 `public_collections.share_card_img`에 URL 저장(신규 컬럼, SQL 별도) — Vercel 함수가 이제 이 필드를 og:image 최우선으로 씀. 공개 전환도 `_syncPublicCollection`을 awaitable로 바꿔서(기존 호출부는 그대로 무해) 행이 실제로 생긴 뒤에만 업로드가 진행되도록 순서 보장.
    - **스포티파이 스타일 개편(사용자 요청)** — 미리보기 텍스트를 `Collection · {이름} · N items · M saves` 한 줄로(대부분의 미리보기 카드가 제목+도메인만 보여주고 description은 아예 표시 안 하는 경우가 많아서, 핵심 정보를 제목에 다 담음). `collection_like_counts` 뷰에서 좋아요 수도 같이 조회. 도메인도 `share.kpop-universe.kr`→**`open.kpop-universe.kr`**로 변경(Vercel에 도메인 추가+Gabia CNAME 추가, 기존 share는 그대로 살아있음). `_colShareUrl`이 이제 open 주소를 씀.
- [완료][index.html] 탐험 패널 "모아보기"류 카드 개선 (사용자 제보 — 연말 무대 콘텐츠 연속 노출, "아스트로 수록곡 모음" 중복 노출) —
  - KBS/MBC/SBS/가요대전 썸머 카드 라벨 "모음"→"모아보기"(연말 통합 카드는 "TOP 100"이라 그대로 둠).
  - 이 4개 카드 정렬을 조회수→랜덤(published_at으로 모은 뒤 `_shuffle`)으로 변경 — "TOP 100" 아닌 모아보기류는 조회수 정렬 안 한다는 원칙 적용, "1위 · N 조회" 서브텍스트도 정렬 기준 바뀐 카드는 제거.
  - `_appendFeedCard` 7번째 인자로 카테고리(`'collection'`) 표시 가능하게 하고, 포맷/직캠/페스티벌x5/수록곡x2/커버x2/데뷔연도 총 11개 모아보기류 카드에 표시 → `_spreadFeedCollectionCards`가 새로 도착한 카드 구간만 훑어 collection/그 외 카드를 번갈아 재배치(이미 본 카드 위치는 안 건드림, `_feedSpreadBoundary`로 진행 지점 추적).
  - 포맷/직캠/수록곡/커버/데뷔연도/세대별TOP100 등 "시간대+offset 회전" 카드 9종에 세션 전역 `_feedRotUsed` Set 추가 — 무한스크롤 offset이 겹쳐 같은 후보(그룹/조합/연도)가 재선택되는 걸 방지, 패널 재오픈 시에만 리셋.
  - 미착수: 사용자가 언급한 "모아보기인데 10여개밖에 안 나오는" 개수 부족 이슈는 데이터 실측 필요해서 보류.
- [완료][index.html] 라이트박스 쇼츠 상하 스와이프 임계값 조정(사용자 요청) — `thresh=vel>0.45?28:72` → `?35:65`로 좁힘(빠른 플릭 최소거리는 늘리고, 느린 드래그 최대거리는 줄임).
- [완료][index.html] 멤버/그룹 카드 라이브 탭 단조로움 완화(사용자 제보 — 1행2열만 계속 나옴) — `_groupGcVids`에 `wideProb` 인자 추가(기본 0.05 유지), 라이브 탭(`state.filter==='live'`) 호출부만 0.15로 상향. 다른 탭(예능/뮤비 등)은 기존 5% 그대로.
- [진행중][index.html][admin.js][kpop_universe.css] 어드민 "동기화 채널" 관리 개선(사용자 요청 4건) —
  - 채널 검색창이 채널 탭에서 `display:none`으로 숨겨져 있던 버그 수정(렌더 함수 `_vmRenderChannels`는 이미 검색 지원 중이었음) — 검색창 노출 + placeholder 전환.
  - 채널 등록 시 handle 중복 체크 추가(`_ecAddChannel` 앞단에서 `_EXT_CHANNELS` 대조) + DB 유니크 인덱스 병행.
  - "그외" 탭에 유형별(음악/예능/잡지/아이돌개인/드라마영화) 필터 칩 신설(`_vmChTierFilter`/`_vmRenderChTierChips`) — 5종이 한 리스트에 섞여 보기 불편하다는 제보.
  - **아이돌개인 채널 동명이인 처리 버그 발견+수정** — `_extOwnerGko`가 `ARTISTS.find`로 이름만 보고 첫 매치를 쓰던 문제(등록 폼도 그룹 구분 없이 이름 텍스트만 받음). 등록 폼에 동명이인 있을 때만 뜨는 그룹 선택 드롭다운(`vm-ch-add-owner-gko`) 추가, `ext_channels.owner_gko`에 직접 저장해서 `_extOwnerGko`가 이름 재추론 없이 그 값을 그대로 신뢰하도록 변경. **기존에 이미 등록된 idol 채널은 소급 적용 안 됨**(재저장 전까진 예전처럼 이름만으로 추론) — 흔한 이름 쓰는 idol 채널 있으면 재확인 필요.
  - [완료] SQL 실행됨 — `ALTER TABLE ext_channels ADD COLUMN IF NOT EXISTS owner_gko text; CREATE UNIQUE INDEX IF NOT EXISTS ext_channels_handle_uq ON ext_channels(handle);`
  - **[완료] "fans" 채널 유형 신설** — 표시 쪽(`category='fan'`, "by Fans" 탭)은 2026-08-10에 이미 있었으나 등록 경로가 없던 걸 마저 연결. tier 셀렉트에 '팬' 추가, 전용 "대상 그룹" 입력(`vm-ch-add-target-gko`, 기존 `#vid-tag-group-list` 데이터리스트 재사용+`_isValidVidGroupKo`로 검증) 신설 — 아이돌개인처럼 owner 메커니즘을 그대로 쓰되 `owner_mko` 없이 `owner_gko`만 채워 그룹 전체를 대상으로 고정. `_extBuildRows`에서: members는 owner.mko 있으면 고정, 없으면(fans) 제목에서 그 그룹 멤버 언급을 찾아 채움(없으면 빈 배열=그룹 전체 영상) / category는 tier==='fans'면 원래 분류가 뭐였든 무조건 'fan'으로 강제(variety/show와 달리 'other'만 덮어쓰는 방식이 아님 — 팬캠도 팬 채널 콘텐츠면 다 "by Fans"로 가야 하므로). `_EXT_STRICT_TIERS`에도 추가(해시태그 없는 게스트 추론 방지).
  - 넷플릭스코리아류(예능/드라마 혼재 채널) 분류는 사용자가 "드라마/영화" 쪽으로 직접 등록하기로 함(코드 변경 없음).
  - 음악(music) tier의 느슨한(strict:false) 매칭은 "영상=단일 인물의 무대/직캠"이라는 전제 때문이라 스포티파이류(다양한 아티스트 큐레이션 채널)엔 안 맞음 — 등록 시 'music' 대신 'magazine'(strict) 쪽으로 넣도록 안내함(코드 변경 없음).
- [완료][artists.json] "파파존스 피자 알바" 워크돌 영상 재배정(사용자 제보) — 실제 출연자는 아이브 리즈인데 동명이인 아닌 다른 멤버 "이서" 밑에 잘못 들어가 있었음(with:지유(키키)는 그대로 유지). 제목도 과하게 축약된 커스텀 문구 "파파존스 피자 알바 (워크돌)" → [[feedback_video_title]] 기준(그룹명만 제거, 나머지 원제목 유지)대로 복원.
- [완료][.github/workflows/rebuild-seo-pages.yml] artists.json/groups.json/build_group_pages.js push 시 정적 SEO 페이지 자동 재생성 워크플로우 신설(사용자 요청 — "수정할 때마다 자동 반영 안 되냐"). GitHub Pages가 main 브랜치 루트에서 바로 서빙되는 구조라 워크플로우가 단순: checkout→setup-node→`node build_group_pages.js`→변경분 있으면 bot 커밋+push. **주의**: `.github/workflows/` 경로는 일반 PAT(workflow 스코프 없음)로 push 불가라 웹 UI로 직접 추가했음(2회 — 1차는 들여쓰기 깨져서 재수정). 리포 Settings→Actions→Workflow permissions도 "Read and write"로 변경 필요(완료).
- [완료][index.html][build_group_pages.js] 멤버별 정적 SEO 페이지 1,693명분 신설(Fable 자문 세션 방향 확인 — 그룹 249개보다 멤버 개별 진입점이 롱테일 검색에 전략적으로 더 중요) —
  - `build_group_pages.js`를 확장해 그룹 페이지와 같은 스크립트에서 멤버 페이지도 같이 생성(별도 스크립트로 안 만든 이유: sitemap.xml을 두 스크립트가 따로 쓰면 서로 덮어써서 충돌).
  - 그룹 소속 멤버는 `g/{그룹}/{멤버}/`, `en/g/{그룹}/{멤버}/`로 그룹 폴더 밑에 중첩(동명이인이 다른 그룹에 있을 수 있어 그룹으로 반드시 구분 — 드림캐쳐 지유/키키 지유 오배정 사고가 실제 사례). GROUPS에 없는 솔로 아티스트(9명 — group.ko="솔로" placeholder)는 `member/{이름}/`, `en/member/{이름}/` 최상위.
  - 페이지 내용: 프로필(생일/소속/소속사/활동상태), 대표 영상(songs, with 콜라보 태그 포함), 개인 디스코그래피(discography 필드), 인스타/나무위키 링크, "우주에서 보기" CTA. meta description은 그룹 페이지와 같은 톤("다른 아이돌들과 어떻게 연결되는지 탐험해보세요")으로 통일.
  - **겸임(이중소속) 멤버**는 주 소속(`group`) 밑에 페이지를 두고 나머지 소속 그룹은 "소속 그룹" 섹션에 링크로 표시(예: 마크 → 엔시티127 페이지에 있고, 엔시티드림도 링크로 노출).
  - **부수 발견+수정**: 솔로 아티스트(group.ko="솔로")의 딥링크(`#g=솔로&m=이름`)가 `_openFromHash`의 `if(!gko||!GROUPS[gko])return;` 가드에 막혀 원래부터 전부 깨져있었음(솔로는 GROUPS에 없는 placeholder라서) — 멤버 지정된 경우에 한해 예외 허용하도록 수정, 이번에 만든 솔로 멤버 페이지의 "우주에서 보기" CTA가 실제로 작동하려면 필수였음.
  - 실행 결과: 그룹 263개+멤버 1,693명 × 2개 언어 = 정적 페이지 3,912장, sitemap.xml 3,913개 URL. 로컬에서 직접 실행+검증 완료(리즈/보아/마크 페이지 스팟체크), 실제 배포/자동화 트리거는 이 커밋 push 이후 GitHub Actions가 처리.

## 2026-08-20

- [진행중][artists.json] 솔로 디스코그래피 이어서 작업 (그룹없는 솔로 아티스트 + 명단 잔여) — 워크플로우 확립: 나무위키 공식 앨범 목록(번호·타입)을 **사람이 읽어** 확보 → Melon 아티스트 앨범페이지+상세로 커버/트랙 **날짜 매칭** → 브라우저 Image()로 커버 로드검증 → `discography` 필드 적용. **자동 번호 추론 금지**(일본반/스페셜/리패키지가 [정규]로 섞여 오번호 발생 — 내 자동이 보아 Crazier를 정규9집으로 뽑았으나 실제 정규11집, 아이유 Real을 미니1집으로 뽑았으나 실제 미니3집, 초기앨범 누락으로 번호 밀림. 반드시 나무위키 대조).
  - 완료 8명: **아이유**(정규5+미니6+리메이크3=14)·**비**(정규6+미니2+스페셜1=9, 일본반 제외)·**보아**(정규3~11집+미니1~3=12)·**B.I**(정규3+미니2+싱글=6, iKON/일본반 제외)·**나연**(미니 IM NAYEON·NA=2)·**싸이**(정규1~9=9)·**세븐**(정규1·2·4+미니1~3=6, 정규3집 24/Seven Melon 없음)·**빛새온**(미니1~4=4). 커버 전부 Image() 로드 검증. **크리스탈=솔로 음반 없음(f(x) 그룹뿐)이라 스킵.**
  - **다음 라운드(잔여)**: 보아 정규1·2집(매칭 실패)·세븐 정규3집·승한(artistId 오류)·이영지(namu slug 404)·김민서(slug 404)·BOBBY(slug 404)·채영(솔로 음반 확인 필요). 그룹 멤버는 namu에 그룹 앨범 섞여 나오니 솔로 섹션만 골라야 함.

- [진행중][index.html][artists.json] 개인 멤버 솔로 디스코그래피 기능 신설 — 사용자가 26명 나열(백현·청하·태민·태연·수지·이진혁·이채연·백예린·제이미·최예나·전소미·카이·김재환·디오·키·온유·민호·종현·크리스탈·제니·지수·로제·리사·지디·태양·비아이·바비·나연·채영 등), 3~4명씩 라운드로 진행하기로 함. 26명 전원 이미 artists.json에 있음(바비="BOBBY", 디오/제이미/청하는 표기 차이만) — 새 멤버 추가는 불필요.
  - **스키마**: groups.json의 discography와 동일한 구조를 개인 아티스트 객체에 `discography` 필드로 그대로 추가(멤버별 있으면 노출, 없으면 그룹처럼 섹션 자체 숨김).
  - **UI**: `_renderGcDiscography(ko)`를 `_renderDiscography(prefix,albums)`로 일반화(`gc-discog-*`/`tt-discog-*` id 접두어만 다르게) — 정렬·종류 필터(세그먼트)·"기타 앨범 포함" 토글 전부 그대로 재사용. 상세 트랙리스트 팝업(`#gc-discog-detail`)도 공용이라 멤버 카드에서 앨범 클릭해도 그대로 열림. 멤버 카드 HTML에 `#tt-discog-row`를 `#tt-nav`와 `#tt-concert-row` 사이에 신설(그룹카드의 discog-row/concert-row 배치와 대칭). 트로피 뱃지(멜론 연간TOP100/스포티파이)는 그룹 단위 캐시라 멤버 쪽은 이번 범위에서 제외(조용히 스킵, 에러 없음). Playwright로 테스트 데이터 주입 검증(스트립+상세팝업 정상) 후, 실데이터로 재검증 완료.
  - **1라운드 데이터(4명, 31개 앨범)**: 백현(미니 5개)·청하(정규2+미니4+EP1=7개)·태민(정규4+미니5=9개, 미니6집 SOFT VIOLENCE는 2026.8.31 발매 예정이라 이번엔 제외)·태연(정규3+미니6+윈터스페셜1=10개). 나무위키 개인 음반 문서로 정확한 앨범 순번·목록 확보(WebSearch 요약만 믿으면 태민 정규 4장을 통째로 놓치는 등 누락이 있어서 반드시 원문 대조) 후 멜론에서 앨범별 검색→상세페이지(og:title로 아티스트 일치 확인, `.meta dl.list` 발매일, `tbody tr[data-group-items]` 트랙리스트+타이틀곡 배지)로 커버+트랙+발매일 수집. 커버 31개 전부 `<img>` 로딩 검증 완료. 태연 카드로 실제 렌더링까지 스크린샷 확인(세그먼트 필터 자동 생성, 상세팝업 트랙리스트 정상).
  - **2라운드(4명, 18개 앨범)**: 수지(미니2)·이진혁(솔로1집+미니4=5, 자체발매 믹스테잎 3개·싱글 Holiday는 멜론 정식 앨범 페이지가 없어 이번엔 제외)·이채연(미니4+싱글1=5)·백예린(EP3+정규3=6, "백예린/음반" 서브문서가 없어 본문 문서에서 목록 확보). 전부 나무위키 대조+멜론 확인, 커버 18개 전부 로딩 검증, 백예린 카드로 최종 렌더링 스크린샷 확인.
  - **3라운드(4명, 17개 앨범)**: 제이미(미니3)·최예나(미니5+싱글1=6)·전소미(정규1+EP2+데뷔싱글=4)·카이(미니4). 카이 "KAI"(미니1집) 검색에서 동명이인 함정 발견+수정 — 멜론에 뮤지컬 배우 "카이(Kai)"가 따로 있어서 자동매칭이 그쪽 뮤지컬 앨범("뮤지컬 팬텀 2018")을 잘못 집었던 것을 발견, EXO 카이는 멜론에서 대문자 "카이 (KAI)"로 구분 표기되는 걸 확인해 수동으로 재검색+교체(사용자가 "커버 없는 6개" 조사 때 알려준 "멜론이 틀리는 경우는 거의 없다"는 전제와 별개로, 검색 자동매칭 자체는 동명이인에 여전히 취약함 — 매 라운드 og:title로 아티스트 일치 확인하는 습관이 실제로 이 오류를 잡아냄).
  - **4라운드(4명, 23개 앨범)**: 김재환(미니7)·디오(정규1+미니3=4)·키(정규3+미니3=6)·온유(정규2+미니4=6). 디오는 검색 함정 하나 더 발견 — 멜론 아티스트 표기가 활동명 "디오" 대신 본명 "도경수(D.O.)"라 "디오"로는 검색 자체가 0건이었음, "D.O."로 바꿔서 해결(카이 동명이인 건과는 다른 종류의 함정 — 이쪽은 존재하는데 이름 표기가 다른 경우). 나머지 3명도 og:title 아티스트 확인 정상. 커버 23개 전부 검증, 키 카드로 최종 렌더링 확인.
  - **다음 라운드**: 민호·종현·크리스탈·제니 예정.
  - **⚠️ 미완료 — 다음 세션에서 이어갈 것**: 사용자가 처음 나열한 29명 중 16명(4라운드) 완료, 남은 13명 — 민호·종현·크리스탈·제니·지수·로제·리사·지디·태양·비아이·바비·나연·채영. 진행 방식은 위와 동일(나무위키 개인 음반 문서로 정확한 목록 확보 → 멜론 검색+상세페이지로 커버/트랙/발매일 → og:title로 아티스트 일치 확인 → `<img>` 로딩 검증 → artists.json에 `discography` 적용 → 스크린샷으로 최종 확인). 그룹 활동이 아닌 "진짜 솔로 활동"만 대상, 유닛/피처링/OST는 원칙적으로 제외(단 데뷔싱글 등 상징성 있는 건은 케이스별로 포함). 검색 시 동명이인(카이=뮤지컬배우와 겹침)·활동명이 아닌 본명으로 등록된 경우(디오=멜론에선 "도경수") 함정 주의.
- [완료][groups.json] 앨범 커버 빈 값 30개(16개 그룹) 실사 조사+수정 — 멜론(Melon) 검색/아티스트 디스코그래피 페이지+Bugs 보완, 전부 실제 `<img>` 로딩 검증(HTTP HEAD/fetch는 CORS·TLS 이슈로 이 환경에서 신뢰 불가, 반드시 브라우저 Image() 로딩으로 검증할 것 — 재발 방지 기록).
  - 24개는 순수 커버 누락이라 그대로 채움(크래비티·SF9 4곡·이엑스아이디 2곡·에프티아일랜드 3곡·에이오에이 4곡·저스트비·비투비 스릴러·악뮤 3곡·미쓰에이·티아라 2곡·씨엔블루·이달의소녀).
  - **6개는 커버 누락이 아니라 앨범 메타데이터 자체가 틀려있었음** — 데이터 생성 당시 앨범 번호가 밀리면서 "실제로 존재하는 앨범이 빠지고 그 자리를 곡명이 앨범명으로 둔갑한 가짜 항목이 채운" 패턴 반복 발견(나무위키 디스코그래피 문서 대조로 확인):
    - **에이비식스(AB6IX) "PARACHUTE"(가짜 미니8집)** → 실제로는 미니5집 [A to B](2022.05.18)의 타이틀곡명이었음. 앨범명·발매일·트랙리스트 전부 실제 데이터로 교체, 뒤로 밀려있던 TAKE A CHANCE(5→6집)·LOST(6→7집)·FOUND(7→8집) 번호도 나무위키 기준으로 같이 정정.
    - **SF9 "Bag"(가짜 미니12집)** — SF9 디스코그래피 어디에도 근거 없는 완전 가짜 항목이라 삭제. The Wave OF9(10→11집)·The Piece OF9(11→12집) 번호 정정(실제 12집은 THE PIECE OF9).
    - **에이프릴 "SIMPLE IS THE BEST"(가짜 미니8집)** — 에이프릴은 2022.01.28 해체, 나무위키 확인상 미니앨범은 7집(Da Capo)까지가 전부라 이 앨범 자체가 존재하지 않음. 삭제. **단, 에이프릴 디스코그래피는 이 외에도 중복/누락이 심해 이번엔 그 항목 하나만 삭제하고 전면 재구성은 안 함 — CHANGELOG DECISIONS 참고.**
    - **블락비 "Jackpot"** — "정규 2집"으로 잘못 분류돼있었음(실제로는 2014.04.17 싱글, 4집 미니앨범 [HER] 활동 전 선공개곡). type을 싱글로, 날짜도 정정.
    - **카라 "카라 (1st Mini)"(Rock U)** — 메타데이터는 맞았음(2008.07.25 미니1집), 순수 커버 누락이라 Bugs에서 보완.
    - **비투비 "Beep Beep"** — 메타데이터는 맞았음(2014.02.17 미니4집, 정식명 "뛰뛰빵빵"), 멜론 영문 검색으로 안 잡혀서 못 찾았던 것뿐 — 한글 정식명으로 재검색해 커버 확보.
- [완료][groups.json][artists.json][connections.json] 그룹 7팀 신규 추가(걸스데이·헬로비너스·소나무·네이처·트라이비·히스토리·스텔라), 253→260팀 — 전부 나무위키 링크만 받아서 조사. 네이처(2024 해체)·트라이비(소속사 파산으로 사실상 활동종료, 2025)는 아직 레거시 페이드 대상 아니라 `survivalShow:true` 적용, 나머지는 10년+ 경과라 기존 관례대로 미적용. connections.json은 7팀 전부 "원년 데뷔 후 순차 탈퇴" 패턴이라 완전연결(174쌍) 적용.
- [완료][artists.json][connections.json] 동일인물 중복 레코드 병합 8명 — Fable(다른 세션) 감사 리포트 검증 후 처리.
  - 제로베이스원↔앤더블 4명(장하오·리키·김규빈·한유진): 2026-07-31에 이미 병합했던 건인데 8/14~19 대량업로드 중 ZB1 단독 레코드가 재발한 것 발견, 삭제해서 재정리(primary는 사용자 결정으로 앤더블 유지).
  - 정채연(아이오아이+다이아)·하성운(핫샷+워너원, 핫샷 쪽 "2021 탈퇴" 기록 있는데 생일 결측이라 자동탐지 회피했던 케이스)·이현주(에이프릴+유니티)·양지원(스피카+유니티) 신규 병합, groups[] 배열로 통일.
  - **'솔로' primary는 유령참조 아님으로 정정**(`_ytGroupKoFor`의 의도된 sentinel) — Fable 리포트 중 이 지적만 틀렸음, 코드로 재검증해서 확인.
- [완료][groups.json][artists.json][connections.json] 유앤비(UNB)·B.D.U 신규 그룹 등록, 260→262팀 — groups.json엔 없는데 여러 멤버 `groups[]`가 참조하던 진짜 누락 케이스. 등록 과정에서 동일인물 2건 추가 발견(B.D.U 빛새온=기존 솔로 엔트리, 제이 창=기존 원팩트 멤버) 병합, UNB/B.D.U 둘 다 완전연결 신규 추가.
- [완료][connections.json] 스피카(SPICA) 그룹 5명 사이 연결선이 원래 하나도 없던 기존 공백 발견, 완전연결 10쌍 추가(양지원 병합 조사 중 발견).
- [완료][groups.json] 하이라이트에 `altNames:["비스트","BEAST"]`+데뷔일을 하이라이트 개명 시점(2017)→비스트 원년(2009.10.16, 나무위키 확인)으로 정정 — 브브걸(브레이브걸스) 때 이미 확립한 관례를 안 따르고 있던 걸 발견해서 통일.
- [완료][index.html] 영상 제외 키워드 목록에 `[#학연]` 추가, 탐험 패널 조회수 표기에서 "조회" 텍스트 제거(7곳, "1위 · 1.2만 조회"→"1위 · 1.2만").
- [완료][admin.js] 겸임 멤버(NCT 마크·해찬, 아이즈원 안유진 등) 외부채널 태깅 매칭 통일 — `_m2ParseTitle`의 그룹매칭후 멤버추출부/그룹미매칭시 역추론부 둘 다 `a.group.ko`(주 소속만)→`_artistGroups`(겸임 포함) 전환. 단순 스왑만 하면 겸임 멤버 1명이 자기 소속 그룹 2개에 걸리는 걸 "동명이인 충돌" 로직이 오인해서 외부채널에서 두 그룹 모두 지워버리는 자기파괴적 회귀가 생겨서, `nameToGroups`를 groupKo Set 대신 "매칭된 사람 수" 기준으로 재구성해 방지(격리 유닛테스트 4케이스로 검증).
- [완료][admin.js][groups.json] 해체그룹 39팀 title-matching 차단 전면 재검토 — 예전엔 `!v.disbanded` 필터로 39팀 전체를 외부/모음채널 제목 매칭에서 통째로 제외했었음. 39팀 전수 검토해서 실제 흔한단어 충돌위험 있는 17팀(배틀·남녀공학·스피드·엑스원·구구단·여자친구·에이프릴·미래소년·위클리·에이스·로켓펀치·피에스타·프리스틴·씨엘씨·비디씨·티에이엔·씨아이엑스)만 `strictSync:true`로 개별 보호 이전하고, 나머지 21팀(아이즈원·스피카·티오원 등 고유명사형)은 필터 제거해서 매칭 복구.
- [완료][DB] `name_match_whitelist`에 영문 흔한단어 이름 10개(릴리·메이·조이·루나·소울·루비·리오·레오·미아·Love) 추가 — Love(온리원오프)는 실측 표본 사실상 전량 오탐 확인(체리블렛·우즈 등 완전 무관 무대가 전부 온리원오프로 오귀속), 루나(에프엑스)는 해리포터 캐릭터/시계 모델명 오탐, 조이(레드벨벳)는 무관곡 영어제목("Rush of Joy" 등) 오탐 확인 후 추가.
- [완료][admin.js][index.html][DB] 그룹배정 신뢰도 2단계 도입 — 위 3건(Love/루나/조이) 오염이 전부 "제목에 그룹명 없이 멤버 이름 하나로 그룹 자체를 역추론"하는 단일 경로에서만 발생한다는 걸 확인하고 그 경로만 타겟팅.
  - `_m2ParseTitle`이 이제 `confidence:'strong'`(그룹명/해시태그 리터럴)/`'weak'`(멤버이름 단독 역추론) 표시.
  - `yt_channel_videos`에 `needs_review boolean` 컬럼 신설, `_extBuildRows`(owner 없는 외부/모음채널 동기화)에서 weak+무관 채널 매칭은 실제 group_ko는 채우되 `content_flag:'hidden'`(기존 은닉 메커니즘 재사용, 기존 select 호출부 23곳+ 무변경)+`needs_review:true`로 저장.
  - 영상관리 패널에 새 탭 "그룹배정 검수" 신설, `needs_review=true` 목록+항목별 승인/거부 버튼(`_vmReviewDecide`) — 일괄처리는 이번 범위 밖.
  - `_ytRescanWeakGroupAssignments`(어드민 버튼 "그룹배정 신뢰도 재스캔(기존 데이터)") — 기존 라이브 데이터 소급 스캔해서 검수 큐로 이동. 조사 중 기존 "온리원오프 Love 오태깅 정리" 버튼이 members 배열만 지우고 group_ko는 안 고쳐서 근본원인이 안 풀렸던 걸 발견, 새 버튼이 실제로 해결.
  - **⚠️ 실제 브라우저 테스트 미완료**(로컬 서버+헤드리스 브라우저 환경 아니었음) — 다음에 승인/거부 버튼·재스캔 버튼 실동작 확인 필요. [[project_kpop_tagging_accuracy_audit]] 참고.
- [보류] 지속관리 항목 2건(나머지 하드코딩 상수 4개 DB 이전, 표본 감사 화면 신설) — 제안까지만 하고 실행은 다음으로 미룸.
- [보류] Fable과의 "이수만/민희진이 이 프로젝트를 보면?" 사고실험 대화에서 나온 방향성 4개, 실행은 안 하고 방향만 기록:
  1. **연결(connections.json)에 타입+사연 부여** — 지금 4,644건 전부 `type:"member"` 하나뿐, `note` 필드 사용 0건(코드로 확인). 기존 4,644건 전부 재분류는 비현실적이라, **앞으로 새로 추가하는 연결부터 type/note를 다는 습관**으로 시작하는 게 현실적 — 커버곡 관계(`cover_of_members`를 연결로 승격)나 서바이벌 동기 등.
  2. **프로듀서/제작자 관계 노출 — 파일럿 3건 [완료]로 승격**: 비→엠블랙, 지코→보이넥스트도어, 재범(2PM 전멤버, "박재범"으로 개명)→롱샷. `groups.json`에 `producedBy:[{ko,group,artist,note}]` 필드 신설(group은 동명이인 방지용, artist:false면 로스터에 없는 인물도 기록만). **디자인 결정**: 처음엔 그룹 카드에 별도 줄(row)로 만들었다가, "프로듀서 있는 그룹이 소수뿐인데 상시 줄 하나는 과하다"는 사용자 피드백으로 데뷔 옆 서바이벌 프로그램명/구이름과 같은 톤의 소속(gc-co) 옆 대괄호 접미사로 변경(클릭 불가, 정보성 텍스트만). 실제 클릭 이동은 연결 카드의 새 "제작" 섹션(가족 섹션과 동일 패턴, 양방향 — 정방향/역방향 둘 다)이 담당. 데이터 출처가 groups.json 한 곳뿐이라 그룹 카드/연결 카드 항상 일치.
  3. **그룹별 시각 아이덴티티(셰이더 분화) — [진행중]으로 승격, 오늘 세션에서 실제 착수함**: `virtualIdol` 셰이더 분화(2026-08-03) 선례를 그대로 확장.
     - **1세대 그레인 효과**: 세대별 TOP100 카드에서 이미 쓰던 `_groupGeneration(debut,ko)` 헬퍼로 1세대만 판정. 신규 `uGrain` 유니폼 — 고주파(최종 20.0) simplex noise 샘플 추가해 필름 그레인 느낌(강도 0.35), 채도 50% 낮춤, 밝기 25% 낮춤, diffuse 곱셈 항 외에 조명과 무관한 가산항(0.06)도 추가해 그림자/가장자리 쪽에서도 고르게 보이게 함 — 총 5라운드 튜닝(사용자가 화면 보면서 매번 강도 조정 요청).
     - **⚠️ 사고 기록**: 1차 시도(배율 32.0)가 행성 자체가 안 보이는 렌더링 회귀를 일으킴(추정 원인: snoise 내부 permute 연산이 큰 입력값에서 정밀도 불안정 — 확정은 못 함, 브라우저 콘솔 미확인). 즉시 되돌리고 배율을 10.0→14.0→20.0으로 점진적으로만 올려 재발 없이 완료.
     - **⚠️ 발견 — 애니메이션 루프가 매프레임 일부 유니폼을 전역 덮어씀**: `uFillOp`/`uBaseOp`/`uFresnelMult`/`uSpecMult`/`uSpecPow`/`uAmbient`는 카메라 거리 기준 애니메이션 루프(bubbleMeshes.forEach)가 모든 그룹에 동일한 공식으로 매 프레임 재설정함 — 그래서 1세대만 다르게 주려던 초기 `uFresnelMult` 값(0.09)은 실제로는 거의 안 먹혔을 것으로 추정(재현 미확인). `uGrain`/`uDisbanded`처럼 이 루프가 안 건드리는 새 유니폼만 세대/상태별 차별화가 실제로 유효함 — **앞으로 이 루프가 덮어쓰는 유니폼으로 그룹별 차별화 시도할 땐 루프 안에서 분기해야 함**.
     - **가장자리→내부 불투명 범위 확대(전체 그룹 공통)**: fresnel 지수 1.8→1.2로 낮춰 "가까이 갈수록 속이 텅 빈 느낌"(사용자 피드백) 완화 — 이건 1세대 한정이 아니라 260여개 전 그룹에 영향.
     - **해체 그룹 잔광**: 신규 `uDisbanded` 유니폼(disbanded 필드 있으면 1.0) — 최종 색 45%·알파 20% 낮춰 "꺼져가는" 느낌. uGrain과 같은 이유로 애니메이션 루프 밖의 새 유니폼으로 구현.
     - **⚠️ 브라우저 실제 확인 상태 들쭉날쭉**: 그레인 최종본은 사용자가 직접 확인하며 튜닝 완료. fresnel 지수 변경과 해체 잔광은 코드 반영 직후라 사용자 확인 전(다음에 확인 필요).
  4. **마이크로카피 전면 퇴고** — "소속/데뷔/팬덤/멤버" 표 형식 라벨, 빈 상태 문구 등. 비용은 적지만 톤은 사용자가 정해야 함.
  5. **계보(사제/영향관계) 뷰** — 장기 과제로만 기록, 지금 3D 배치축(데뷔연도=거리) 하나로는 표현 안 되고 완전히 다른 시각화(가계도형) 필요해서 스케일이 다름.
- [이슈] **푸시 직전 git 충돌 발견 — 원인은 세션 시작 시 `git fetch` 없이 `git status`만으로 "최신"이라 오판한 것.** 이 세션 작업은 전부 `Downloads\kpopuniverse-main`(zip 폴더)에서 진행하고 마지막에 `Documents\GitHub\kpopuniverse`로 옮겨 커밋했는데, 그 사이(정확히는 이 세션 시작 전인 8/19 밤) 다른 세션이 이미 3커밋(레이디스코드·드림노트 정리·무한스크롤·셰이더 등, 위 "## 2026-08-19" 항목들)을 origin에 올려둔 상태였음. `git status`는 **로컬에 캐시된 마지막 fetch 시점 기준**으로만 "최신"을 판단해서 실제 서버 상태와 어긋나 있어도 못 잡아냄 — **다음부턴 세션 시작 시(특히 "다른 데서 작업한 거 반영됐는지 확인해줘" 같은 요청 받으면) `git status` 전에 반드시 `git fetch`부터 할 것.** 다행히 파일은 100% 겹쳤지만(6개 전부) 내용상 주제는 안 겹쳐서(저쪽: 레이디스코드/드림노트/무한스크롤/셰이더, 이쪽: 그룹9팀/동일인물병합/태깅파이프라인) `git reset --hard origin/main`으로 로컬을 최신으로 맞춘 뒤 이 세션의 JSON 작업은 스크립트 재실행으로, index.html/admin.js는 `git diff`로 뽑은 패치를 `git apply`로 재적용해서 무손실 병합 완료(충돌 0건, `git apply --check`로 사전 확인).
- [이슈][admin.js][index.html] **(2026-08-21 소급 기록 — 커밋 1c6f246엔 있었는데 이 파일엔 안 남아있었음) "그룹배정 신뢰도 재스캔" 버튼 실행 사고.** 위 신뢰도 2단계(a) 재스캔 버튼을 실제로 클릭했더니 "제목에 그룹명 없이 멤버 이름만 있는" **정상** 직캠·챌린지·예능 클립 35,168개를 검수 큐(content_flag='hidden'+needs_review)로 통째로 잘못 보냄 — "멤버명만 있는 제목"을 그룹 오배정 신호로 취급한 weak 판정 자체가 과잉일반화(설계 결함). Supabase SQL로 전량 원복(38,954→3,684, 밴 인물 진짜 숨김만 잔존). 재발 방지로 함수 즉시 조기 반환하도록 **비활성화**, weak 판정 기준을 훨씬 좁히기 전엔 재실행 금지. `_ADMIN_JS_VER` 2026-08-19f→2026-08-20a. [[project_kpop_tagging_accuracy_audit]] 참고.

## 2026-08-19

- [완료][index.html] 그룹/멤버 카드 영상 무한스크롤 회귀버그 수정(사용자 제보 — "영상 더 있는데 'N개'만 뜨고 스크롤 죽음") — 원인: 오늘 넣은 "추천순 기본 오픈도 청크 랜덤 샘플링"이 부작용. 청크는 24곳×2개=48개를 id로 중복제거해 합치는데, 지점이 겹치면 이번 페이지가 _YT_PAGE(48) 미만이 됨. 근데 hasMore를 "페이지 충만도(data.length>=48)"로 판정하던 else 분기가 청크 첫 로드를 잘못 잡아 "더 없음"으로 조기종료 → 총개수 "N개" 라벨만 뜨고 스크롤 죽음. 수정: 청크 정방향 구간 전용 분기(`else if(state.wrapEnd>0)`) 추가해 "남은 구간(offset<total)"으로 판정(빈 응답이면 무한루프 방지 종료). 부수효과로 잘못된 hasMore=false가 8/14 넣은 700ms 폴링 안전망까지 무력화했던 것도 같이 복구. 옛 appendLock/iOS 옵저버 버그와는 별개의 신규 회귀. Playwright로 세븐틴 카드 검증 — 46→94→142→190개 연속 로드, "N개" 조기표시 없음(디버그로 hasMore=true·offset 정상 전진도 확인).
- [완료][groups.json] 오마이걸 앨범 커버 12개 복구(사용자 제보) — **원인 파악**: A2O MAY 때(멜론 /cm/→/cm2/ URL 이관)와 다른 문제였음. A2O MAY는 이미 고쳐짐(3개 URL 다 200 확인). 오마이걸은 **cover 필드가 아예 빈 문자열('')** — 멜론 스크래핑이 커버를 못 받아온 것. 앨범에 멜론 ID도 안 박혀 있어 재검색 필요. Playwright로 멜론 앨범검색(q=그룹명+앨범명) → 아티스트·제목 매칭으로 커버 URL 재수집, 12개 전부 HEAD 200 검증. "Remember Me"만 멜론 앨범명이 "OH MY GIRL 6th Mini Album (REMEMBER ME)"라 정확매칭 실패→수동 보정. **미해결 전체 규모**: 빈 커버가 384개/52그룹(비투비16·SF9 15·여친13·펜타곤13·데이식스12 등) — 같은 방식으로 복구 가능(전체는 별도 진행).
- [완료][index.html] 디스코그래피 앨범 캡션 한국어 표기(사용자 요청, 이전 "미니"→"Mini" 통일 번복) — 커버 밑 줄은 "미니 5집"/"정규 2집"/"싱글" 원본 한국어(번호는 멜론 type에 있을 때만, 없으면 "미니"), 필터 탭(`_discogBucketLabel`)은 Mini/Single 영문 그대로 유지. `_discogTypeLabel` 한국어 분기만 수정(원본 그대로 두되 "EP" 단독→"미니"). 영어 모드는 기존 "5th Mini"/"2nd Full" 유지. 참고: 'n집'은 순서 추론이 아니라 멜론 type 문자열에서 옴 — 실측 번호있음 380/번호없음 1168개(들쭉날쭉), 앨범명 서수는 66개뿐.
- [완료][artists.json][connections.json] 드림노트 로스터 오염 정리(사용자 제보) — **팬덤명 '페이지(Page)'가 멤버로**, **앨범명 'BLUE'·'Dreamlike'가 멤버로** 잘못 등록돼 있던 것 제거(셋 다 생일 없어 사람 아님 정황). 누락돼 있던 현재 멤버 **유아이·은조 추가**(현재 6인조: 보니·유아이·라라·미소·수민·은조, 전 멤버 유하빈·안한별 active:false 유지). 연결 0→현재 6인 완전연결 15쌍 추가, 가짜 이름이 타 연결에 물려있던 15개도 제거. groups.json 팬덤 {페이지/Page}는 정상이라 유지. 유아이·은조 생일은 나무위키 rowspan 테이블이라 자동추출 실패→비움(확인 필요). Supabase 영상 태깅도 심하게 오염(members에 페이지 35·BLUE 16개, with_members에 '페이지(드림노트)' 53개, 반대로 유아이·은조는 미태깅) → **배포 후 어드민 "멤버+콜라보 재태깅(기존 태깅분 포함, 전체)" 버튼 실행**하면 무효 태그 제거+유아이·은조 추가로 정리됨(anon 키는 쓰기 불가라 코드에선 못 고침).
- [완료][groups.json][artists.json][connections.json] 레이디스 코드(LADIES' CODE) 신규 추가, 253→254팀(사용자 나무위키 링크). 2013.03.07 데뷔 5인 걸그룹, 폴라리스엔터테인먼트, 2020.02.01 해체(`disbanded`). 멤버 전원 동시 데뷔라 완전연결(10쌍). 2014년 교통사고로 사망한 리세·은비는 `active:false`로 구분(사용자 결정 — 앱에 사망 필드 없어 비활성으로 은은히 구분), 애슐리·소정·주니는 active:true. 애슐리 nat 미국(코리안-아메리칸). 멤버 링크는 정확도 위해 비움(잘못된 링크 방지).
- [완료][index.html] 행성(그룹 구) 셰이더 재질 개선 — 매끈한 반투명 "구슬"感 완화(fable 분석 참고, 사용자 요청). 프래그먼트 셰이더만 수정(지오메트리·줌별 페이드 로직 불변, 회귀 위험 낮음): (1) 3D 심플렉스 노이즈 추가 + 그룹별 시드(uSeed=이름 해시)로 표면 밝기 미세 변조·아주 느린 흐름("모든 별은 다르다" 시각화), (2) 터미네이터(명암경계) 웜톤 mix, (3) fresnel을 알파뿐 아니라 색에도 기여시켜 대기 림 강화(가장 눈에 띄는 개선), (4) 스펙큘러 기여도 0.20→0.11로 완화(구슬 하이라이트 줄임). 멤버 "별"은 이미 발광 스프라이트라 손 안 댐. Playwright로 세븐틴 행성 전후 스크린샷 비교 검증(셰이더 정상 컴파일·렌더). 참고: 행성이 반투명 설계라 표면 효과는 은은함 — 더 큰 도약은 UnrealBloom(EffectComposer+모바일 부담)이 다음 후보. 성능: 유일한 비용인 snoise를 `uNoise` 유니폼(모바일=0)으로 게이트해 모바일에선 노이즈 자체를 건너뜀(별 브리딩과 동일 패턴) — 웜톤·대기 림은 거의 공짜라 유지. 사용자 1순위가 "버벅임 절대 금지"라 이렇게 처리.
- [완료][index.html] 탐험 패널에 "커버곡 모음" 카드 신설 — N세대 여/남돌(세대×성별 회전) + 그룹별(회전) 2종(사용자 요청). 신호는 cover_of_groups/cover_of_members(원곡 아티스트, admin 검수 태깅)가 비어있지 않은 영상 = 케이팝 원곡 커버. b-side와 달리 이미 태깅돼 있어 **fuzzy 매칭 없이 순수 서버 쿼리**(`.or('cover_of_groups.neq.{},cover_of_members.neq.{}')` + group_ko in + content_flag 배제, 조회수순 cap). 비케이팝 원곡 커버는 신뢰 신호가 없어 제외(cover_of는 케이팝 아티스트만 참조, content_formats cover 0건; 제목 커버 키워드는 4318개 중 3980개가 미태깅/오탐 노이즈라 안 씀 — 사용자와 확인). 그룹 회전은 커버 없는 그룹에서 헛돌지 않게 cover_of 영상 group_ko를 1회 페이지네이션 집계해 "커버 ≥6 그룹"만 캐시(`_coverEligibleGroups`, 87개)해서 그 안에서만 회전. 임계 `_COVER_MIN`=6. 회전형이라 무한스크롤에도 offset 포함. 라이브 검증: cover_of 1856개/152그룹/적격 87개, 세대 4세대 남100·여100·5세대 남95·2세대 여37 카드생성, 1세대 여돌(5개)만 스킵.
- [완료][index.html] 탐험 패널에 "수록곡(타이틀곡 아닌 트랙) 무대 모음" 카드 신설 — N세대 여/남돌(세대×성별 회전) + 그룹별(회전) 2종(사용자 요청). discography `tracks[].isTitle`로 타이틀/수록곡 구분(수록곡=isTitle 아님, Inst./MR/remix 파생 트랙 제외). 무대 영상 매칭은 커버처럼 fuzzy라 정밀도 우선 규칙(사용자와 실측 확인): (1)곡명은 영상 제목 따옴표 안에서만 추출(공백제거 부분매칭 오탐 "Ash←BOOMPALA|SHow" 방지), (2)무대 마커 필요·비무대(behind/teaser/vlog/커버/원곡) 제외, (3)따옴표 곡명이 그 그룹 수록곡 일치 && 타이틀곡 불일치, (4)매칭은 groups.json 대조라 전부 클라이언트. **핵심 함정**: 한 그룹의 전체 영상이 수천 개(르세라핌 5331·세븐틴 6756·아이브 5502 등, 여러 팬캠 채널을 다 긁어와서)인데 PostgREST가 한 응답에 최대 ~1000행만 줌 → 그냥 조회수순으로 상위 1000개만 받으면 전부 조회수 높은 타이틀곡 콘텐츠라 조회수 낮은 수록곡 직캠이 1000등 밖으로 밀려 매칭 0. (수록곡 무대 자체가 많은 게 아니라 "전체 영상 풀이 커서 잘린다"는 문제.) → fetch에 직캠/팬캠류 서버 ilike 필터(`_BSIDE_CAM_OR`)로 풀을 좁혀 해결 — 직캠류만 세면 대부분 그룹이 1000 미만(르세라핌 974·TXT 947·있지 977)이라 그 그룹 직캠이 통째로 들어와 수록곡 직캠도 포함됨. 매칭 8개 미만 그룹/조합은 스킵하고 회전으로 다음 후보(아이브처럼 수록곡 직캠 적은 그룹 자동 제외). 회전형이라 무한스크롤에도 offset으로 포함(`_buildFeedGenStageTop`과 동일). 공용 헬퍼 `_bsideStagesFromVideos`/`_groupBsideIndex`로 분리해 두 카드가 공유. 라이브 데이터로 검증(르세라핌·TXT·세븐틴·있지 100, 에스파 74, 뉴진스 25, 세대별 16~29 — 전부 임계 통과).
- [완료][index.html] 탐험 패널에 연말/여름 음악축제 무대 모음 카드 5종 신설(사용자 요청) — 연말 무대 모음(3사 통합), KBS 가요대축제, MBC 가요대제전, SBS 가요대전, 가요대전 썸머. 무대 TOP 100(`_fetchStagePool`) 패턴을 제목 키워드 버전(`_fetchFestivalPool`)으로: content_flag(기타/무관/hidden)만 배제하고 제목 ilike로 필터, 조회수순 TOP 100, 그룹당 5개 상한. 라이브 데이터 실측으로 필터 규칙 확정 — (1) category 무제한: 축제 영상이 live/other/short에 흩어져 있고 'other'에 풀캠/페이스캠 같은 진짜 무대가 30% 있어(가요대전 표본 live136·other61) live만 걸면 무대가 대거 누락됨(사용자와 확인). (2) "가요대전"에 여름판(가요대전 썸머, 2507중 496) 섞임 → SBS 연말은 `.not(title,ilike,%썸머%)`로 제외, 여름 카드는 가요대전+썸머 둘 다 포함(단독 "썸머"는 비축제 노이즈). (3) 연말 통합 = 가요대축제 OR 가요대제전 OR 가요대전, 썸머는 클라이언트에서 제외. 고정 순위라 무한스크롤 랜덤 풀엔 미포함(다른 TOP 100 카드와 동일). `_buildFeedGenStageTop` 다음에 배치. 필터는 라이브 DB로 카운트 검증(OR통합4545·SBS연말2011·여름496·KBS571·MBC1476).
- [완료][index.html][admin.js] 어드민 영상 관리 패널에 "선택-원곡지정" 버튼 신설(사용자 요청) — 제목 검색으로 필터·다중선택한 여러 영상에 원곡 정보(cover_of)를 일괄 지정. 영상마다 출연 멤버가 달라 기존 벌크 편집 모달로는 멤버를 안 건드리고 원곡만 넣을 수 없었던 문제 해결(벌크 모달은 cover_of가 "멤버 덮어쓰기" 토글에 묶여 있었음). "선택-원곡제외"의 반대 동작으로, 전용 팝업(vm-coverset-overlay)에서 그룹/멤버 검색해 지정 → cover_of_members/cover_of_groups를 SET(교체). cover_of는 tags_manual 보호 트리거 대상이 아니라 plain update로 처리(원곡제외와 동일), tags_manual 안 건드려 멤버 자동 태깅 유지. 검색 UI는 vid-tag-cover-search 로직 복제(상태만 _vmCs*). 팝업/버튼은 CSS 안 늘리고 인라인 스타일+기존 클래스 재사용. `_ADMIN_JS_VER` e→f.
- [완료][index.html] 자동 탐험 패널(나침반 peek) 동작 개선(사용자 요청) — 자동으로 열리기 전에 다른 걸 누르면 안 열리고, 반쯤 뜬 직후 다른 걸 눌렀으면 열린 패널이 닫히게. 통합 규칙 하나로 처리: capture 단계 `pointerdown` 리스너가 탐험 패널/탐험 버튼(#feed-overlay·#tab-feed·#dh-feed-btn)이 아닌 걸 건드리면 `_dismissOnboardHint` 호출(예약 취소 or 닫기) — capture라 설정·검색바·탭바처럼 bubble에서 stopPropagation 하던 버튼도 잡힘(기존 bubble 리스너의 사각지대). 모바일 peek는 뚝 안 사라지고 카드 시트처럼 아래로 슬라이드아웃(스와이프 닫기 모션을 `_slideDownCloseFeed` 공용 헬퍼로 추출해 재사용). 2026-08-12 "peek를 카드 밑에 깔아두기" 설계를 이 케이스에서만 뒤집음. `_showOnboardHint` 진입 시 `_obTimer=null`로 발동 상태 정리.
- [완료][admin.js][index.html] 커버 끌어오기(원곡 스캔) 제외 키워드 추가 — `_isBeOriginal`이 "BE ORIGINAL"만 걸러내던 걸 "POP ORIGINAL"·"*_original"(언더스코어 시리즈 태그)까지 제외하도록 확장(사용자 요청). 세 사용처(스캔 필터/괄호절 파서 2개) 공용이라 한 곳만 고치면 전부 적용. `_ADMIN_JS_VER` d→e 갱신.
- [완료][index.html] 탐험 패널에 역대/세대별 무대 TOP 100 카드 3종 추가 신설(사용자 요청) — 역대 여돌 무대 TOP 100, 역대 남돌 무대 TOP 100, 세대별(1~5세대×남/여) 무대 TOP 100(시간대 순환). 위 개인 직캠 시리즈와 짝을 이루지만 멤버 1명 단독 필터가 없어 그룹 완전체 무대까지 포함(`_fetchStagePool`). "역대 전체 무대 TOP 100"은 이미 있는 "케이팝 레전드 100"과 쿼리가 완전히 같아져서 중복 카드를 새로 안 만들고 레전드 100이 그 역할을 겸함. 그룹당 상한(5개)을 둬서 인기 그룹이 100자리를 독점 못 하게 함 — Playwright로 실제 반환 목록 확인(블랙핑크 여러 곡 나와도 5개로 제한되고 나머지는 다른 그룹으로 채워짐).
- [완료][groups.json] 나우즈(NOWZ) 옛 팀명 "나우어데이즈" `altNames` 추가(사용자 제보) — 슈퍼노바/브브걸과 동일한 기존 관례(데뷔정보 옆에 옛 이름 괄호 표기) 그대로 적용.
- [완료][index.html] 탐험 패널에 역대 개인 직캠 TOP 100 계열 카드 4종 신설(사용자 요청) — 역대 개인 직캠 TOP 100, 역대 여돌 개인 직캠 TOP 100, 역대 남돌 개인 직캠 TOP 100, 세대별(1~5세대×남/여) 개인 직캠 TOP 100(시간대 순환).
  - "개인" 직캠 판정 기준은 기존 "주간 개인 직캠 TOP 20"(_buildFeedWeeklyTopCams)과 동일(제목에 직캠/팬캠/FANCAM/음방명 + 태깅된 멤버 정확히 1명)을 그대로 재사용하는 공용 헬퍼(`_fetchSoloFancamPool`) 신설, 4개 카드가 전부 이걸 공유.
  - **"개인" vs "멤버" 용어 통일 질문에 대한 결정**: 기존에 이미 정착돼 쓰이던 "개인"으로 통일(새 카드 4개 전부 "개인 직캠"). 사용자가 새 카드는 "멤버"로 언급했지만, 기존 카드와 용어가 갈리면 오히려 혼란스러울 것 같아 기존 표현에 맞춤.
  - 전체/여돌/남돌 3종은 결과가 고정(전체기간 조회수 기준)이라 레전드 100과 동일하게 무한스크롤 반복 풀에서 제외, 세대별 카드는 기존 세대별 직캠 "모음" 카드(_buildFeedGenCamPick)와 같은 시간대 순환 방식이라 반복 풀에 포함.
  - Playwright로 4개 카드 전부 정상 렌더링 확인(라벨/1위 조회수), 실제 반환 영상 목록도 직접 검증(전부 정확히 멤버 1명 태깅된 진짜 개인 직캠, 조회수 내림차순 정상).
- [완료][index.html] 탐험 "연결 보기" 카드 텍스트/구분자 수정(사용자 요청) — "N인 연결 보기" 인원수 표기 제거, 이름 사이 구분자 "↔"(화살표, "구리다"는 피드백) → "~"(물결)로 교체. Playwright로 2인/4인 케이스 다 확인.
- [완료][groups.json] A2O MAY 디스코그래피 앨범 커버 깨짐 수정 — 사용자 제보. 멜론이 앨범 커버 이미지 CDN 경로를 `cdnimg.melon.co.kr/cm/album/images/...` → `cdnimg.melon.co.kr/cm2/album/images/.../{ID}_{타임스탬프}_500.jpg`로 최근 바꾼 게 원인. 처음엔 이게 전체 데이터에 광범위한 문제일 거라 판단해(전체 1,548개 앨범 중 옛 `/cm/` 패턴이 289개) 전부 다시 조회하려 했으나, **실제로 HEAD 요청으로 289개를 다 찔러보니 진짜 깨진 건 A2O MAY의 3개뿐**이었음(나머지 284개는 옛 경로가 여전히 살아있어 멀쩡) — 옛 앨범은 마이그레이션 전부터 있던 파일이라 `/cm/` 경로에 그대로 남아있고, A2O MAY처럼 최근(2025~2026) 발매작만 `/cm2/`에만 올라가고 `/cm/` 쪽엔 아예 없어서 404였던 것으로 보임. 불필요한 284개 재조회를 사전에 걸러내고 진짜 깨진 3개만 멜론 앨범 페이지 재조회로 새 URL 확보해 수정. 앱에서 실제로 로드되는 값까지 재확인 완료.
- [완료][groups.json][artists.json][connections.json] 그룹 나인아이(NINE.i) 신규 추가, 252→253팀 — 나무위키 링크만 받아서 조사([[reference_namuwiki_scraping_workflow]] 그대로 재사용, curl -sk + 브라우저 UA 우회).
  - 퍼스트원 엔터테인먼트가 2022.03.30 10인조로 런칭, 2024~2025년 3명(위니·주형·서원) 순차 탈퇴해 현재 7인조. 2024년 소속사와 법정분쟁 이후 한국 소속사 없이 FC ENM이 실질 소속사 역할 — `co:"FC ENM"`으로 반영.
  - 멤버 10명(활동 7 + 탈퇴 3) 전부 추가, 전원 데뷔 당시(2022.03.30)부터 동시에 활동한 팀이라(세대 교체 아님) 10명 전체 완전연결(45쌍) 적용 — 쥬얼리처럼 활동시기가 안 겹치는 경우가 아니므로 선별 연결 불필요.
  - **survivalShow/projectRing 둘 다 미적용**: 2025.12 "당분간 그룹 활동보다 개인활동 집중" 발표는 해체가 아니라 잠정 활동중단이라 survivalShow(해체/과거형) 대상 아님. 멤버 다수(제원·반·민준·지호·이든)가 개별적으로 프로듀스X101/언더나인틴/보이즈플래닛 등에 출연했지만 그룹 자체가 서바이벌 산출물은 아니라 projectRing도 미적용 — 앤더블(AND2BLE) 때 확립한 판단 기준 그대로 재사용.
  - 위니(전 멤버)는 유일한 외국인(태국) 멤버, 나머지 전원 한국.
  - `artists.json`의 `x`/`y`/`sat`/`op`/`planet` 필드는 죽은 코드([[project_kpop_map]] 기존 확인사항)라 안 넣음 — 실제 배치는 `debut`+`co`만으로 런타임에 자동 계산됨.
  - 동명이인 발견: 기존 오마이걸 지호(1997.04.04)와 이름만 같고 생년월일 달라 별개 인물 확인, 병합 안 함.
  - Playwright로 그룹 카드(이름/데뷔일/소속사/멤버 7명/팬덤)·3D 행성 배치까지 실제 렌더링 확인 완료.
- [완료][index.html] 탐험 "레전드 100" → "케이팝 레전드 100"으로 이름 수정(사용자 요청).
- [완료][kpop_universe.css] 컬렉션 상세보기(`.cve-item`, 컬렉션 클릭 시 나오는 영상 목록) 썸네일 56×34(가로형) → 44×44(정사각형) — 사용자 제보: "쇼츠만 있는 컬렉션인데 목록이 한눈에 안 들어온다". 가로로 긴 상자에 쇼츠(9:16)를 넣으면 폭 기준으로 채워지며 위아래 대부분이 잘려나가 거의 안 보였던 게 원인 — 오늘 컬렉션 목록 썸네일(`.col-row-thumb`)에 적용했던 것과 동일 판단(정사각형이면 어느 쪽이든 크롭 손실이 덜함)을 여기도 적용. Playwright로 가짜 쇼츠 2개+가로 영상 1개 목록 렌더링해 레이아웃 확인(뱃지/텍스트 정렬 문제없음).
- [완료][index.html][kpop_universe.css] 데뷔 기념일 공유 카드 신설 — 사용자가 다른 세션(Fable)과 나눈 논의를 참고: 스포티파이 공유 이미지처럼 예쁜 이미지를 자동 생성해서 X/인스타 스토리에 바로 공유하고 싶다는 요청.
  - **설계 결정 두 가지**(사용자 피드백 반영): (1) 나침반(탐험 패널)엔 안 넣음 — 첫 화면이라 노출은 크지만 공유는 극소수만 쓸 액션이라 안 맞는다는 지적, 대신 그룹 카드 안에 오늘이 그 그룹 데뷔일일 때만("🎉 오늘 데뷔 N주년! 카드 공유하기") 뜨는 배너로 배치. 이미 있는 `_isDebutAnniversaryToday()`(우주에 노바 터뜨리는 조건과 동일)를 그대로 재사용. (2) 새 카드 디자인을 처음부터 그리는 대신, 며칠 전 만든 "내 우주 카드"(`_buildMyUniverseCardDataURL`)와 같은 철학 — 이미 있는 3D 씬(그 그룹 행성의 글로우/색상)을 실제로 캡처해서 쓰고, 그 위에 2D 캔버스로 큰 숫자(N주년)·그룹명·데뷔일·워터마크를 합성. html2canvas 같은 새 외부 라이브러리는 전혀 필요 없었음.
  - 캔버스 크기는 내 우주 카드(4:5)와 다르게 9:16(1080×1920)으로 — "이미지 자체는 세로가 긴 게 좋을 듯"(사용자), 인스타 스토리에 그대로 맞는 비율.
  - 공유는 기존 "내 우주 카드"에 이미 구현돼있던 Web Share API 패턴(`navigator.canShare({files})`) 그대로 재사용 — 지원되면 시스템 공유 시트(인스타/X 선택지)로 바로, 안 되면(대부분 데스크톱) 다운로드+토스트로 폴백.
  - Playwright로 검증: 평소엔 배너 숨김, 데뷔일 강제 시뮬레이션 시 배너 노출+올바른 N주년 숫자, 카드 생성 후 정확히 1080×1920 PNG 생성, 공유/다운로드 버튼 활성화, 에러 없음. 그룹 2개(에스파/방탄소년단, 색상 다름)로 실제 생성된 이미지를 직접 확인 — 그룹 컬러 그라데이션·별밭·큰 숫자·워터마크 다 정상.
- [완료][admin.js][index.html] 커버곡 원곡 제목 자동 추출 기능 신설(반복 재실행 가능) — 사용자 요청: "특정 커버곡 모아볼 수 있게 되면 좋겠고... 근데 원곡 태깅된 건 많지 않으니까 제목 중복되는 거 뽑고 디스코그라피에도 곡 정보 있을 거고 자동 안 됨?" + "일회성이 아니고 반복 가능한 로직을 아예 만들면 좋겠는 거임".
  - **조사 결과 공유**: `cover_of_members`/`cover_of_groups`(원곡 아티스트 태깅)는 실제로는 "진짜 커버곡" 전용이 아니었음 — 검수 센터에서 "출연/콜라보가 아니다"로 재분류된 것도 이 필드에 같이 옮겨짐(비하인드 영상, 축하 메시지, 챌린지 등). 실측: 전체 1,854건 중 제목에 커버/원곡/Original 키워드까지 있는 진짜 커버는 763건. 이 사실을 먼저 사용자에게 공유하고 방향을 다시 잡음.
  - **구현**(`_ytExtractCoverSongTitles`, admin.js): (1) cover_of_members/groups가 있고 제목에 커버 관련 키워드가 있는 영상만 대상 (2) 제목에서 정규식 패턴 4종으로 곡명 후보 추출("'곡명' (원곡:...)", "곡명 by 아티스트 (Original Song:...)" 등) (3) 원곡 아티스트(cover_of_members/groups)의 디스코그래피(groups.json)와 대조해 실제로 확인되는 것만 `cover_of_song`에 저장 — 후보를 못 뽑거나 디스코그래피에 없으면(솔로 아티스트는 디스코그래피 데이터 자체가 없어 확인 불가) 비워둠, 틀린 곡명 저장보다 안 채우는 게 안전. `cover_of_song`이 이미 있는 행은 쿼리에서부터 제외되므로 몇 번을 다시 눌러도 매번 새로 늘어난 커버 영상+지난번엔 실패했지만 이번엔 잡히는 것만 처리됨(전량 재스캔 아님) — 다른 스윕 버튼들과 같은 "반복 재실행 가능" 패턴. tags_manual=true는 절대 안 건드림.
  - **버그 2건 발견+수정**(구현 중 실제 DB 샘플 14건으로 검증하며 발견): (1) 이모지로 시작하는 제목이 많은데 "곡명 by 아티스트" 패턴이 선행 이모지까지 곡명에 같이 집어삼킴 → 선행 이모지 제거 후 매칭. (2) 따옴표 안쪽 매칭에서 아포스트로피를 배제하는 문자 클래스를 썼더니 "Life's Too Short"의 내부 아포스트로피를 진짜 닫는 따옴표로 착각해 "s Too Short"로 잘려나감 → 탐욕적 매칭으로 전환해 실제 닫는 따옴표를 정확히 찾도록 수정.
  - 검증: 실제 DB에서 뽑은 14개 샘플로 후보 추출·디스코그래피 대조 로직을 Node로 직접 실행 — 확인된 4건 전부 정답(Bite Me/404 (New Era)/OVERDRIVE/Life's Too Short), 오탐 0건. 나머지는 디스코그래피 데이터 부재(솔로 아티스트)나 제목 패턴 모호성으로 정상적으로 비워짐(의도된 보수적 동작).
  - "커버곡 원곡 제목 자동 추출" 버튼 신설(위 버튼 설명 캡션 토글 적용), `_ADMIN_JS_VER`를 2026-08-19d로 올림.
  - **다음 단계(미착수)**: 이 `cover_of_song` 데이터가 쌓이면 (a) 탐험 패널에 "특정 커버곡 모아보기" 카드/오버레이 추가 (b) 커버 스포트라이트 계열 카드 확장 — 둘 다 다음 세션 과제.
  - **사용자가 직접 실행해야 할 SQL**(RLS로 Claude가 직접 못 씀):
    ```sql
    ALTER TABLE yt_channel_videos ADD COLUMN IF NOT EXISTS cover_of_song text;
    ```
- [완료][index.html][admin.js][kpop_universe.css] 어드민 버튼마다 한 줄 설명 캡션 추가 — 사용자 요청: "어드민 계정 버튼마다 이해하기 쉽게 설명 좀 토글로든지 써줄 수 있어?", Downloads/admin_improvement_plan.md 참고(그 문서의 훨씬 큰 콘솔 재설계 제안 중 "버튼 아래 1줄 설명 캡션" 아이디어만 지금 규모에 맞게 우선 반영, 좌측 도킹 콘솔 전면 개편은 안 함).
  - `#sp-yt-sec` 상단에 "ⓘ 버튼 설명 보기" 토글(`sp-hint-toggle`, 기존 `.sw`/`.sp-row` 컴포넌트 재사용) 신설 — 켜면 각 버튼 바로 아래 `.sp-btn-hint` 캡션이 나타남, 기본은 숨김. 켠 상태는 localStorage(`kpu_admin_show_hints`)에 저장돼 다음에 열 때도 유지됨.
  - YouTube 영상 관리 섹션의 버튼 21개 전부에 캡션 작성(기존 코드 주석에서 실제 동작 근거를 확인해 요약) — 이미 자체적으로 상시 노출 설명이 있던 "멤버+콜라보 재태깅(전체)" 버튼만 중복 방지로 제외.
  - Playwright로 검증: 토글 끔 상태에서 캡션 전부 숨김, 켜면 21개 전부 노출, 다시 끄면 전부 숨김, 버튼-캡션 1:1 매칭 누락 없음 확인.
- [완료][admin.js][index.html] 전체 카테고리 조회수 순환 갱신 기능 신설 — 사용자 제안: "한 달에 한 번쯤은 전체 조회수 돌리면 안 되나.. 대신 매번 전체를 도는 건 아니고, 2~3달에 한 번 돌려도 큰 변화 없을 영상도 많을 테니까 로직을 잘 짜서".
  - **배경**: 기존 `_ytRefreshAllViewCounts`는 live 카테고리만 다루고, mv/쇼츠/예능 등 나머지(전체 35만7천여 건 중 live 제외 25만여 건, 2026-08-19 실측)는 한 번 동기화된 뒤 조회수가 계속 방치됨. 그렇다고 진짜 전체를 매번 다 돌면 API 약 7,150회(35만여 건÷50)로 유튜브 일일 쿼터(1만) 대부분을 그 한 번에 다 써서 정기 동기화(search.list, 콜당 100쿼터)와 부딪힘.
  - **구현**: "어떤 영상이 안 변할지" 미리 판별하는 복잡한 로직 대신, `view_count_synced_at`(마지막 갱신 시각) 컬럼을 기준으로 **가장 오래전에 갱신된 것부터** 고정 배치(기본 5,000개 = API 약 100회)만 매번 갱신하는 라운드로빈 방식 채택(`_ytRotateViewCountRefresh`, admin.js) — 이번에 갱신한 건 자동으로 맨 뒤로 밀리므로 버튼을 정기적으로(월 1회 정도) 누르기만 하면 몇 달에 걸쳐 전체가 공평하게 한 바퀴씩 순환됨. 삭제/비공개라 API 응답에 안 잡힌 영상은 기존 view_count를 null로 덮어쓰지 않고 synced_at만 갱신(안 그러면 죽은 영상이 계속 "가장 오래됨"으로 맨 앞에 다시 뽑히기만 하고 안 끝남). "전체 카테고리 조회수 순환 갱신" 버튼 신설(index.html, sp-yt-rotateviewcount-btn), `_ADMIN_JS_VER`를 2026-08-19b로 올림.
  - Node로 sb/fetch를 목킹한 드라이런 테스트로 배치 크기·API 응답 누락(touchOnly) 처리·다음 실행 시 겹치지 않고 다음 배치로 넘어가는 것까지 검증 완료.
  - **사용자가 직접 실행해야 할 SQL**(RLS로 Claude가 직접 못 씀):
    ```sql
    ALTER TABLE yt_channel_videos ADD COLUMN IF NOT EXISTS view_count_synced_at timestamptz;
    CREATE INDEX IF NOT EXISTS idx_ytcv_vc_synced_at ON yt_channel_videos (view_count_synced_at);
    ```
- [완료][index.html] 모바일 카드(그룹/멤버) 첫 터치가 너무 민감한 문제 수정 — 사용자 제보: "카드 내 가끔 첫 터치가 너무 민감. 막상 스크롤은 괜찮은데.. 레거시 다른 어플 쓸 때랑 느낌이 다르고 불편함".
  - **근본 원인**: `_wireSheetSwipe`(카드 패널의 펼치기/닫기 드래그 제스처)가 콘텐츠 맨 위(scrollTop=0)일 때 touchmove 이동거리에 최소 임계값이 전혀 없어서, 손가락이 1px만 움직여도 곧바로 카드 전체가 시각적으로 따라 움직이는 transform이 시작됐음 — 카드를 막 열고(peek 상태) 처음 스크롤하려는 순간(콘텐츠가 맨 위)에 유독 예민하게 느껴지고, 이미 스크롤이 진행된 뒤(scrollTop>0)엔 이 로직이 관여 안 해서 "스크롤 자체는 괜찮다"는 것과 정확히 일치. 네이티브 앱들이 보통 갖고 있는 최소 이동거리(터치 slop)가 빠져있던 게 원인.
  - **수정**: `_SHEET_DRAG_SLOP=10`(px) 도입 — 이 거리를 넘기 전엔 transform도 preventDefault도 안 걸고 가만히 둠, 넘는 순간의 손가락 위치를 새 기준점으로 다시 잡아 점프 없이 그 지점부터 자연스럽게 이어짐(기존에 있던 "scrollTop 도달 시 재기준점" 로직과 동일한 방식 재사용). 최종 펼치기/닫기 확정 임계값(60~150px)엔 영향 없음 — 그 전 단계의 "미세한 흔들림에도 반응하는" 부분만 없앰.
  - Playwright로 실제 TouchEvent를 합성해 검증: 3px/8px 이동 시 transform 그대로 빈 값(반응 없음), 임계값을 넘는 순간부터 점프 없이 0에서 시작해 이후 이동량만큼(translateY) 정확히 따라오는 것 확인.
- [완료][index.html] 카드(그룹/멤버) 영상 그리드 "추천순" 기본 정렬이 거의 안 바뀌는 문제 수정 — 사용자 제보: "카드 내 영상 디폴트 순서인 추천순이 너무 안 바뀌어.. 유튜브 홈화면처럼 좀 적당히는 바뀌면 좋겠다".
  - **근본 원인**: 카드를 처음 열 때(reset, ↻ 아님)는 항상 `published_at desc, offset 0`으로 그 채널의 **최신 48개만** 가져온 뒤 그 안에서만 위치를 섞었음 — 영상 자체는 매번 똑같고 배치만 바뀌니 사람 눈엔 "그대로"로 보임. 게다가 캐시 TTL이 15분이라 자주 재방문하면 정말 완전히 동일했음. ↻ 새로고침 버튼에만 있던 "청크 랜덤 샘플링"(전체 영상 풀 여기저기서 24곳×2개씩 골라오는 로직)은 기본 오픈엔 안 쓰이고 있었음.
  - **수정**: 기본 오픈도 '추천순'일 땐 같은 청크 랜덤 샘플링을 타도록 확장(`load()`, `isChunkedRandomize` 조건에 `state.sortMode==='recommend'` 추가) — 다만 청크 개수 계산에 전체 개수가 필요해서, '추천순' 기본 오픈(캐시 미스)에 한해 가벼운 head:true 총 개수 조회를 짧게 기다린 뒤 판단하게 함(그 외엔 기존처럼 백그라운드로만 채움, 중복 조회 방지). 업로드순/오래된순/미태깅우선 정렬은 그대로 결정적 순서 유지(건드리지 않음). 캐시 TTL도 15분→1분으로 단축(`_CH_VID_CACHE_TTL`) — 재방문 시 너무 오래 똑같은 결과가 캐시되는 걸 방지.
  - **버그 발견+수정(구현 중 Playwright로 재현)**: 위 총 개수 사전조회를 `buildBaseQuery(true).catch(...)`로 짰다가 "buildBaseQuery(...).catch is not a function"으로 카드 전체가 로드 실패하는 걸 발견 — supabase 쿼리 빌더는 thenable이지만 진짜 Promise가 아니라 `.catch()`가 없음. try/await로 감싸는 방식으로 수정.
  - Playwright로 대형 그룹(방탄소년단, 영상 수 많음) 카드를 캐시 초기화 후 두 번 열어 완전히 다른 영상 10개씩 나오는 것 확인, 소형 그룹(코르티스, 48개 미만)은 기존과 동일하게 에러 없이 정상 동작 확인.
- [완료][index.html] 탐험 모아보기에 "레전드 100" 카드 신설 — 사용자 요청: "탐험에 Legend 100 추가 조회수 기준으로". 기존 연도별 TOP 100(`_buildFeedYearlyTop`)과 완전히 같은 구조인데 연도 범위 필터만 제거해 라이브·직캠 전체 기간(역대) 조회수 상위 100개를 뽑음(`_buildFeedLegend100`). 아이콘은 연도별 카드(🏅)와 구분되게 👑 사용. 매번 같은 결과라 시간대 순환/랜덤 카드와 달리 무한스크롤 반복 풀(`_FEED_MORE_RANDOM_FNS`)엔 넣지 않음 — `_buildFeedHiddenGem`/`_buildFeedTrendingHearts`와 같은 제외 사유(고정 결과). `_buildFeedDiscovery()` 카드 순서상 연도별 TOP 100 바로 다음에 배치. Playwright로 직접 호출 검증(썸네일/라벨/1위 조회수 정상 렌더 확인).
- [완료][신규 build_group_pages.js] SEO용 그룹별 정적 페이지 생성 스크립트 신설 — 사용자가 Fable과 나눈 성장 시나리오 논의에서 나온 결론(핵심 병목: 해시 라우팅 `#g=...`는 검색엔진이 아예 못 읽어서 그룹 249개+멤버 데이터가 있어도 검색엔진엔 "페이지 1개"로만 보임) 반영. groups.json/artists.json을 읽어 그룹당 정적 HTML 2장(`g/{한글그룹명}/index.html`, `en/g/{영문슬러그}/index.html`) 생성 — 실행 결과 252개 그룹 × 2개 언어 = 504페이지, 총 4.2MB. 각 페이지: 검색형 title/description, 그룹별 OG 태그(첫 곡 유튜브 썸네일 재사용, 별도 이미지 생성 불필요), JSON-LD(MusicGroup 스키마, 멤버 목록 포함), hreflang(ko/en/x-default) 상호 연결, 실제 멤버 목록(각각 앱 딥링크로 연결)·데뷔일·소속사·팬덤명·디스코그래피(최근 30개) 표시, "우주에서 보기" 버튼(자동 리디렉트 없음 — 검색엔진의 "콘텐츠 없는 리디렉트" 저품질 판정 방지)으로 index.html 해시 딥링크 연결. 소속사 영문명은 index.html의 기존 `_AGENCY_EN` 맵을 정규식으로 추출해 재사용(중복 유지보수 방지), 앨범 종류(미니/정규 N집 등)는 서수 영역(EP/Album) 변환 함수 신설. sitemap.xml도 이 505개 URL(루트+그룹 504개) 기준으로 자동 재생성. index.html(진짜 앱)은 전혀 안 건드림.
  - **실행 방법**: groups.json/artists.json이 바뀔 때마다 `node build_group_pages.js`를 로컬에서 재실행 → 결과물(`g/`, `en/g/`, `sitemap.xml`)까지 같이 GitHub 웹 드래그 업로드에 포함시켜야 함. 504개 파일이 한 번에 새로 생기는 첫 업로드라, 기존 방식(개별 파일 몇 개 드래그)보다 무거울 수 있음 — 웹 드래그가 버벅이면 폴더 통째로 zip해서 GitHub 웹 업로드가 zip 자동 해제를 지원하는지 확인하거나, 이번만 별도 방법 고려 필요.
  - **사용자가 직접 해야 할 것(계정 필요, Claude 대행 불가)**: 구글 서치 콘솔 + 네이버 서치어드바이저 등록·소유확인. 소유확인용 코드(메타태그/HTML 파일)를 받으면 그건 반영 가능.
  - **2차 예정(보류)**: 멤버 개별 정적 페이지(1600명+).
- [완료][kpop_universe.css] 컬렉션 썸네일 정사각형으로 통일 — 사용자 제안: 영상이 가로(16:9)든 쇼츠(9:16)든 섞여있어서 지금처럼 가로로 긴 고정 박스(object-fit:cover)면 세로 영상이 심하게 잘리는데, 정사각형이면 어느 쪽이든 크롭 손실이 덜하고 디스코그래피 앨범 커버(.gc-discog-cover, 68×68 정사각형)랑도 톤이 맞음. `.col-row-thumb`(컬렉션 관리 리스트) 44×28→28×28(높이 기준 유지, 세로 리스트라 높이가 더 중요), `.gc-collection-chip-thumb-wrap`(카드 컬렉션 칩) 74×46→74×74(너비 기준 유지, 가로 스크롤 그리드라 너비가 더 중요) — 두 자리 각각 어느 축을 기준으로 정사각형을 맞출지 다르게 선택. Playwright로 렌더링+치수 확인 완료.
- [완료][index.html][kpop_universe.css] 연결 카드(멤버 카드 "연결" 버튼) 로딩 느림 — 로딩 표시 추가 + 근본 원인 수정. 사용자 제보: "연결 카드 뜨는데 로딩이 너무 길어, 뜨는 중 표시라도 보여야지 않을까, 근본적으로 빨라지면 더 좋고".
  - **근본 원인**: `_buildConnContent` 내부의 이름별 콜라보 영상 수 캐시(`_collabCountCache`)가 주석엔 "한 번만 계산해서 재사용"이라고 적혀있었지만 실제로는 **함수 호출할 때마다 새로 만드는 지역변수**라 전혀 재사용이 안 되고 있었음 — 칩 하나 펼치기만 해도(재렌더링) 이름 전부(_getCollabSongs, 건당 6~20ms)를 처음부터 다시 계산. 특히 백그라운드 API 콜라보 데이터(_enrichConnCardFromApi, 실제 DB 태깅분)가 도착해 이름 수가 확 늘어난 뒤 재렌더링될 때 활동 많은 멤버는 `_getCollabSongs`가 300회 넘게 호출되며 4~7초까지 걸림을 Playwright로 실측(계측 wrapper로 함수별 시간 분해해서 특정).
  - **수정**: 캐시 3종(이름별 콜라보 수·전체 콜라보 이름 목록·그룹 동시출연 쌍)을 앵커+API 데이터 보유 여부를 키로 하는 모듈 전역 캐시로 전환 — 앵커가 바뀌거나 API 데이터가 새로 도착할 때만 리셋, 그 사이(칩 선택/그룹 펼치기/Only 토글 등 순수 재렌더링)엔 재사용. 실측: 같은 앵커 재오픈 4700~7000ms → 700~1200ms, 칩 클릭 재렌더링도 동일하게 개선. 첫 오픈(콜드 캐시)은 여전히 느릴 수 있음(활동 많은 인기 멤버는 처음 한 번은 수 초 걸릴 수 있음) — 두 번째부터 체감 안 될 정도로 빨라짐.
  - **로딩 표시**: `#tt-conn-btn` 클릭 시 즉시 로딩 스피너로 전환(`.conn-btn-loading`, 작은 회전 링) 후 더블 `requestAnimationFrame`으로 한 프레임 쉬어 화면에 반영될 시간을 준 다음 무거운 `openConnCard` 호출 — 자바스크립트가 메인 스레드를 막는 동안에도 최소한 "로딩 중"이라는 걸 사용자가 인지할 수 있게 함.
  - Playwright로 검증: 앵커 전환 시 데이터 안 섞이는 것(캐시 오염 없음), 로딩 클래스 add/remove 정상 동작, 콘솔 에러 없음까지 확인 완료.
- [진행중][groups.json] 위 스포티파이 마일스톤 조사에서 discography 자체가 불완전한 그룹이 많다는 게 드러나서(방탄소년단 94곡·스트레이키즈 17곡 등 히트곡이 discography에 아예 없음) 멜론 기반 보강 착수. `_a_audit_full.json`의 A등급 111개 중 kworb 히트곡과 대조해 빠진 게 확인된 30여개 그룹만 우선 진행(전체 139개 재스크래핑은 별도 세션으로 보류, 사용자 확정).
  - 6개 병렬 fork(BTS 단독 + 나머지 5분할)로 멜론 앨범(제목·발매일·커버이미지·전체 트랙리스트) 조사 → **세션 전체에 걸린 WebSearch 횟수 제한(환경변수 CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION, 200회, fork 전체가 공유)에 걸려 다수 그룹 미완료**로 중단(트와이스·아이들·에스파·세븐틴·투모로우바이투게더·아일릿·아이즈원·마마무·악뮤 등). 재시도 fork 3개도 즉시 동일하게 막힘 확인 — 이 세션에선 더 이상 WebSearch 불가, 다음 세션(한도 리셋)에서 이어가야 함. TXT는 예외적으로 WebSearch 없이 위키피디아 WebFetch만으로 부분 데이터(커버·전체 트랙리스트 없음, 타이틀곡만) 확보.
  - 확보된 데이터 43개 앨범을 22개 그룹에 병합 완료(제목 기준 중복 자동 스킵, 기존 앨범 20건은 이미 있어서 스킵됨). 방탄소년단 6개(LOVE YOURSELF 結·MOTS:PERSONA·MOTS:7·BE·Butter·Permission to Dance·My Universe·Proof·Take Two 중 6개, 3개는 이미 있었음), 투모로우바이투게더 7개, 레드벨벳 5개, 엔시티127 5개, 르세라핌 3개, 엔시티드림 7개(트랙리스트 대부분 타이틀곡만, 후속 보강 필요), 엔하이픈·에이티즈·스트레이키즈·스테이씨·이엑스아이디·아이콘 계열 각 0~2개.
  - **중요 발견**: "누락"으로 잡혔던 것 중 일부는 실제 누락이 아니라 **표기 차이**였음 — 예: 스트레이키즈 "God's Menu"는 멜론 원제 "神메뉴"(한자 스타일)로 이미 있었는데 kworb의 영문 제목과 문자열이 안 맞아 새로 조사 대상으로 잘못 잡힘. 그래서 초기 "213곡 누락" 추정치는 실제보다 다소 부풀려져 있었을 가능성 있음 — 트랙 제목 뱃지 기능(`_appendGcDiscogTrackBadges`)이 정확 매칭이라 이런 표기차이 케이스는 뱃지가 안 뜰 수 있음(범위 밖, 별도 이슈).
  - 미해결로 남은 것: 엔하이픈 "CRIMINAL LOVE"/"Fatal Trouble", 에이티즈 "Answer"/"HALAZIA" — 앨범 여러 개 찾아봤지만 못 찾음(추가 조사 필요). 워너원·이엑스아이디는 커버/전체트랙 없이 최소 정보만. 스트레이키즈는 앨범 2개(GO生·IN生)만 상세, 나머지(Clé 시리즈 이후 다수)는 날짜만 확인되고 미검증.
  - **다음 세션에서 이어갈 것**: 트와이스·아이들·에스파·아일릿(마지막 fork 결과 대기 중이었으나 세션 종료로 미반영 — 병합 안 됨), 세븐틴, 아이즈원·마마무·악뮤, 위 미해결 항목들.
- [완료][spotify_streaming_milestones DB] A등급 그룹(group_priority 111개, `_a_audit_full.json` 로컬 파일로 목록 확보 — DB는 admin 세션에서만 읽혀서 대신 씀) 스포티파이 스트리밍 마일스톤 전수 조사. kworb.net을 111개 그룹 8배치로 나눠 병렬 fork로 조사(각 배치는 WebSearch로 kworb 아티스트 페이지 찾고 WebFetch로 곡별 누적 스트리밍 수 수집). 배치2 포크가 스스로 판단해 seed 파일에 직접 SQL을 써넣는 예상 밖 행동을 함(포크가 대화 전체 맥락을 물려받아 파일 관례를 알고 있었음) — 문제는 없었지만 병렬 포크가 같은 파일에 동시 쓰기하면 충돌 위험이 있어 이후 배치는 결과만 받아 직접 취합. 디스코그래피 트랙 제목과 엄격히 대조하는 1차 방식은 groups.json 디스코그래피 데이터 자체가 그룹마다 불완전해서(별도 진행 중인 멜론 스크래핑 프로젝트, 아직 다 안 채워짐) 버터·갓츠메뉴 같은 명백한 히트곡까지 걸러내는 문제가 있어 폐기 — 대신 (1) 같은 그룹 내 "of {그룹영문명}" 패턴(다른 가수 곡에 멤버가 게스트로 참여한 경우, 예: RM 참여 Old Town Road)과 (2) "Solo" 표기(멤버 개인곡)만 배제하는 가벼운 휴리스틱으로 교체 + 언어/버전별 중복 표기("- KR Ver." 등) 정규화 후 최고 스트리밍 수 기준 병합. 아오아 "Perfect Strangers"(12.9억, 지민 관련 곡이 잘못 잡힌 것으로 포크 자체 판단)는 하드코딩 배제. 기존 DB에 이미 있는 (그룹,곡) 조합은 스킵. 최종 341개 신규 항목을 `spotify_milestones_a_tier_sweep_2026-08-19.sql`로 저장 + `spotify_milestones_seed.sql`(누적 기록)에도 이어붙임 — **DB 미실행, 사용자가 admin 세션에서 직접 실행 필요**. 세븐틴(227곡 중 19개만)·트와이스(430곡+ 중 20개만) kworb 페이지가 너무 커서 fetch가 상위 일부만 가져옴 — 두 그룹은 더 깊은 후속 조회 필요. 러블리즈·미쓰에이·하이키·에이프릴·크래비티·뉴이스트·아이딧·엑스원·하이라이트·빅스·아이덴티티·앰퍼샌드원·애프터스쿨·에프티아일랜드는 kworb에 곡 목록 페이지 자체가 안 잡혀 수동 확인 필요.
- [완료][index.html] 공개 컬렉션도 커스텀 표지 사진이 다른 유저 카드에 반영되게 함 — 사용자 제보: "공개 컬렉션에 사진 올렸을 때 카드에 수정반영 안 되는 거 당연히 돼야 하는 거 아님?". 확인해보니 `public_collections` 테이블에 애초에 커스텀 표지(coverImg)를 담을 컬럼 자체가 없었음(핀 고정 영상용 items 순서 트릭만 있었음, 임의 이미지 URL은 실어 보낼 방법이 없었음). `cover_img` 컬럼 추가 + 3곳 연결: (1) `_syncPublicCollection` upsert에 `cover_img:col.coverImg||null` 추가, (2) 표지 사진 업로드 핸들러가 `col.isPublic`이면 즉시 `_syncPublicCollection` 재호출하도록 추가(전엔 다음 번 rename/공개토글 같은 다른 액션이 있어야만 어쩌다 동기화됐음), (3) `_warmPubCollections`의 select에 `cover_img` 추가 + `_loadCollectionRow`의 pub 매핑에 `coverImg:r.cover_img` 추가. Playwright로 업로드→upsert payload에 cover_img 포함 확인 + 남의 공개 컬렉션 카드 렌더링에서 커스텀 표지가 기존 영상 썸네일 대신 정확히 뜨는 것까지 양방향 검증. **DB 마이그레이션 미실행** — `ALTER TABLE public_collections ADD COLUMN IF NOT EXISTS cover_img text;` 사용자가 admin 세션에서 직접 실행 필요.
- [완료][index.html] 컬렉션 커스텀 표지 사진이 그룹/멤버 카드 칩에는 반영 안 되던 버그 수정 — 사용자 제보: "사진 추가/변경은 되는데 카드에서는 반영이 안 됨". 원인: `_loadCollectionRow`(카드에 컬렉션 칩 보여주는 함수, 13816-13817줄)가 카드용으로 컬렉션 객체를 얕은 복사하면서 `coverU`(핀 고정 영상)는 넣고 정작 `coverImg`(업로드한 커스텀 사진)는 필드 자체를 빠뜨림 — 그래서 항상 undefined가 되어 기존 영상 썸네일로 폴백됐음. 컬렉션 관리 리스트(`renderCollectionSection`)는 원본 객체를 그대로 써서 문제없이 보였던 것과 대비. `.map()`에 `coverImg:c.coverImg` 추가로 수정. Playwright로 실제 `_loadCollectionRow` 호출 검증(커스텀 커버 URL이 칩 이미지에 정확히 반영됨 확인). 참고: 다른 유저가 보는 "공개 컬렉션"(`public_collections` 테이블)엔 애초에 coverImg를 담을 컬럼이 없어서(기존 주석에 이미 명시돼있던 제약) 그쪽은 이번 수정 범위 밖 — 필요하면 별도로 논의.
- [완료][admin.js][index.html] 오태깅 재검증 스윕 2종에 "태그 완전 소실" 리스트업 추가 + 멤버 탈퇴일 자동 태깅 컷오프 신설 + 온리원오프 Love 오태깅 정리(일회용). `_ADMIN_JS_VER` 2026-08-18e→2026-08-19a.
  - 사용자 논의: "재검증에서 오태깅 판별나면 아예 무관 처리까지 가능해야 하지 않나" → 검토 결과 반대 결론. `members`/`with_members`가 다 빠졌다고 자동으로 content_flag='무관' 처리하면, 티저/연습영상처럼 개별 멤버명이 제목에 없을 뿐인 멀쩡한 자체채널 콘텐츠가 조용히 숨겨지는 회귀 위험이 큼(기존 '무관' 자동화는 `_ytSweepJunkKeywordVideos`/`_ytSweepBannedVideos`처럼 영상 제목 자체의 무관 신호를 보는 것과 별개 기준). 대신 `_ytSweepMembersMistag`/`_ytSweepAmbiguousCollabMistag`가 태그를 전부 지우게 되는 행만 콘솔에 목록으로 남기도록 수정(자동 무관 처리는 안 함, 관리자가 직접 확인).
  - 온리원오프 "Love" 멤버가 2021.08.02 탈퇴했는데도 최근 영상에 계속 자동 태깅되던 문제 발견(사용자 제보) — 그룹 자체 해체 컷오프(`_disbandCutoffDate`)는 있었지만 개별 멤버 탈퇴 컷오프는 없었음. `_memberLeftCutoffDate(a)` 신설(artists.json `left` 필드 기반) + `_atmResolveMembers`에 `publishedAt` 파라미터 추가해 탈퇴일 이후 발행 영상은 그 멤버를 매칭 후보에서 제외. 세 호출부(`_ytSweepMembersMistag`/`_ytAutoTagMembers`/`_ytRetagAllIncludingTagged`) 모두 select에 `published_at` 추가 + roster에 `left` 필드 포함해서 전달하도록 수정 — 이제 앞으로 신규 동기화도 안 걸리고, 재검증 버튼 누르면 기존 오염(온리원오프 자체채널 463건, 읽기전용 샘플 확인)도 자동 정리됨.
  - "Love"는 흔한 영단어라(아이유 "Love Poem"류 등 무관하게) 다른 그룹 채널 영상 제목에도 평문 매칭으로 대량 오염(읽기전용 샘플 1000건 중 394건이 무관 그룹에 잘못 콜라보 태깅됨 — 위너/몬스타엑스/빅뱅 등, "BORN TO LOVE YOU"/"Love Me A Little" 등 제목에 love가 들어간 것). 정식 해법(흔한단어 화이트리스트)보다 엄격하게 "온리원오프 자체 채널이거나 제목에 온리원오프/ONLYONEOF 있는 경우만 남기고 나머지 다 제거"하는 전용 원버튼(`_ytFixOnlyoneofLoveMistag`, `sp-onlyoneof-love-btn`, "필요할 때만" 섹션) 신설 — members/with_members/cover_of_members(원곡 태깅) 셋 다 정리, tags_manual=true는 미건드림.
  - 실제 DB 쓰기는 전부 admin 세션에서 사용자가 버튼을 직접 눌러야 실행됨(Claude는 조사·코드 작성까지만).
- [진행중] 어제(2026-08-18) Fable과 작성한 `admin_improvement_plan.md`(Downloads 루트, 프로젝트 폴더 밖) 검토 — P1 세션1(이벤트 가드)·세션2(동명이인 카드 이동)는 8/18에 이미 반영 완료 확인. P1 세션3~4(어드민 콘솔 좌측 도킹 분리)·P2 세션5(대시보드)·세션6(편집모달 개선)·P3 백로그는 아직 미반영 — 성격이 다른 큰 구조개편이라 별도 세션([기능 세션])으로 진행 예정.
- [완료][index.html] 탐험 패널 "Discovery" 무한스크롤 신설 — 기존엔 패널 열 때(3분 TTL 캐시) 14개 카드만 뽑히고 끝이었음. 사용자 요청: "끝이 없을 정도로 채워져있으면 좋겠다". 랜덤 그룹/멤버/연결/영상 인라인 4종을 `_buildFeedRandomGroup/Member/Connection/Video`로 독립 함수 분리(재사용 목적) 후, 매번 랜덤이라 반복 호출해도 새 결과가 나오는 7개 함수(위 4개 + 연도별TOP·커버스포트라이트·콜라보스포트라이트) 풀에서 스크롤 끝 도달 시마다 셔플해서 이어붙임. 시간대(1시간) 순환형 3개(`_buildFeedFormatPick`/`_buildFeedGenCamPick`/`_buildFeedDebutYearPick`)는 offset 파라미터 추가해서 반복 호출마다 순환이 계속 앞으로 진행되게 함(안 그러면 같은 시간 안엔 매번 똑같은 카드만 또 뜸). 하트 기준 카드(HiddenGem/TrendingHearts)와 기념일/오늘의과거는 세션 내내 결과 고정이라 반복 풀에서 제외. IntersectionObserver+MutationObserver 조합으로 구현 — 처음엔 IntersectionObserver 콜백 안에서 센티널을 한 번만 맨 끝으로 옮겼다가, 카드들이 Supabase 응답 순서대로 비동기로 들쭉날쭉 도착하면서 늦게 붙는 카드가 센티널보다 뒤에 붙어버려 두 번째 스크롤부터 무한스크롤이 멈추는 버그가 있었음 — MutationObserver로 매 append마다 센티널을 자동 재보정하도록 수정해 해결. Playwright로 4회 연속 스크롤 검증(24→26→34→36→43개, 중복 카드 0건, 에러 없음).
- [완료][index.html] 탐험 패널 "랜덤 연결" 카드 — 2인 전용→2~4인 확장 + 썸네일 정확도 개선 (`_buildFeedDiscovery` 3번 섹션, 12064줄 부근). 사용자 요청: "연결이 이 서비스 핵심 키워드니까 3/4인 연결도 제안하고, 썸네일도 그 인원만 나온 영상이어야". 기존엔 connections.json의 사전 정의 2인 엣지를 골라 `_getCollabSongs`로 매칭 영상을 "찾는" 방식이라 exactOnly 없이 매칭돼 그룹 전체 무대 영상 썸네일이 뽑히는 경우가 있었음. 대신 전 아티스트의 곡 `with` 태그 길이(1~3, 본인포함 2~4인)로 직접 클리크 풀을 만드는 방식으로 교체 — with 길이 자체가 "정확히 이 인원만 나옴"의 증거이자 정확한 썸네일 소스가 됨. 연결 카드(`openConnCard`)가 이미 `_connSelected` 배열로 다중 선택을 지원해서(라이트박스 "연결 보기"에서 이미 쓰던 것) 새 UI 없이 재사용. Playwright로 20회 샘플링해 2/3/4인 분포(10/8/2) 확인 + 3인 카드 실클릭으로 정확한 콜라보 영상("YUNA X KARINA X SULLYOON")만 뜨는 것까지 검증 완료.
- [완료][index.html] 데스크톱 멤버 이름 라벨 인원수 상한(10명) 제거 + 호버 툴팁 정리 + 별 간 최소거리 확대 — 사용자 요청: "이미 라벨 떠있으면 호버 필요 없음, 10명 제한도 없애도 될 듯, 대신 라벨 안 겹치게 최소거리는 확보". (1) `_gcLabelSet`이 `roster.length<=10` 조건 없이 항상 전체 로스터로 설정되도록 변경(2451줄 부근) — 그룹 카드가 열려있으면 인원수 무관 전원 라벨 표시. (2) 호버 툴팁(`mousemove`, 8201줄 부근)에서 "멤버 별 이름" 브랜치 제거, "그룹 행성 이름 프리뷰"(카드 열림과 무관하게 먼 거리에서 라벨 대신 미리 보여주는 것) 브랜치는 별개 기능이라 유지. (3) 멤버 배치 최소거리 `_minSep`을 `radius*0.34`→`radius*0.55`로, 재시도 횟수 60→150으로 확대(1753줄 부근). Playwright로 그룹 크기별 실측: 5명(뉴진스·코르티스)·11명(아이오아이)·13명(세븐틴·우주소녀)은 목표 최소거리 달성 확인, 20명(아이덴티티)은 목표의 70%, 24명(트리플에스, 전체 246그룹 중 최다인원)은 30%에 그쳐 라벨 겹침 가능성 있음 — 원인은 재시도가 멤버별 각도(방향)는 고정한 채 반경(깊이)만 다시 굴리는 구조라 인원이 몰릴수록 못 풂. "행성 반지름을 키우면 되지 않나" 검토했으나, `_minSep`(radius*0.55)·배치반경(radius*0.82)·카드 열 때 카메라 접근거리(`flyToBubble`: radius*5.5)가 전부 radius에 정비례라 반지름을 키워도 화면 투영 간격이 안 바뀜(수학적으로 무효, 실제 효과 있으려면 카메라 접근 배율만 별도로 줄여야 함) — 사용자 판단으로 이 두 그룹은 당장은 그대로 두기로 함(추후 민원 있으면 재검토).
- [완료][index.html] 재생 모드 둘째줄에 조회수 표기 — 그룹/멤버 카드 그리드에서 영상 열 때(가장 흔한 재생 경로)는 `buildBaseQuery`의 select에 `view_count`가 빠져있어 라이트박스 둘째줄(`_fmtVidMeta`)에 날짜만 뜨고 조회수는 항상 안 뜨던 문제. 탐험 피드 스포트라이트 카드는 이미 정상 표기 중이었음. selectCols에 `view_count` 추가 + 플레이리스트 매핑 3곳(단일 클릭/전체 목록/폴백)에 `vc:v.view_count` 추가. Playwright로 실제 `openLightbox()` 호출 검증(뉴진스 Attention 4K 영상, view_count 48,769,907 → "2022.08.05 · 48.8M+" 정상 렌더). 조회수 미수집 영상은 기존처럼 날짜만 표시(폴백 정상). 스크랩함·컬렉션 재생 등 다른 진입 경로는 아직 미적용(범위 밖).
- [완료][index.html] 조회수 표기를 소수점 없는 버림(예: 48.8M+→48M+)으로 통일 — 사용자 요청: "정확도보다 대략치가 중요, 정확한 숫자는 경쟁/서열 구도를 심화시킬 수 있음"(PRINCIPLES.md 철학과 일치). `_fmtVidMeta`(라이트박스 둘째줄)·`_fmtN`(주간 트렌드 카드)·`fmtN`(연도별 TOP 카드) 3곳 모두 `toFixed(1)` 방식 제거하고 K와 동일하게 `Math.floor` 방식으로 통일.
- [완료][index.html] 그룹/멤버 카드 공유 링크·버튼 개선 (사용자 제보: 코르티스 공유 URL이 `#g=%EC%BD%94...`처럼 지저분함 + 맥북에서 공유 버튼 눌러도 OS 공유 시트에 "링크 복사" 옵션이 없음). (1) `_cardHash`/`_connShareHash`가 그룹·멤버 한글명을 `encodeURIComponent`로 통째로 퍼센트인코딩하던 것을, 해시 구분자(` &=#%+`)만 최소 이스케이프하는 `_hashEsc`로 교체 — 공유 버튼이 만드는 링크 문자열 자체는 이제 `#g=코르티스`로 깔끔함(기존 인코딩된 링크도 `URLSearchParams`가 그대로 디코딩해 하위호환 유지). 단, 브라우저 주소창(`location.hash`)은 WHATWG URL 표준상 프래그먼트의 비ASCII를 항상 퍼센트인코딩해 정규화하므로 — Playwright로 실측 확인 — 이 부분은 앱이 손 댈 수 없는 영역이고, 실제 개선 효과는 "공유" 버튼으로 복사/공유되는 텍스트에 한정됨. (2) `_shareCurrentView`가 `navigator.share` 존재 여부만으로 무조건 OS 네이티브 시트를 띄우던 것을, `matchMedia('(pointer: coarse)')` 기반 `_isTouchPrimary()`로 터치 기기에서만 시트를 쓰고 나머지(맥북 등 마우스/트랙패드 기기, `isMob()`의 창너비 판정과 무관하게 정확히 구분됨)는 무조건 즉시 클립보드 복사+토스트로 통일. Playwright로 두 변경 모두 실측 검증 완료.

## 2026-08-18 (cont. 6)

- [완료][index.html][admin.js] 무한 스크롤 점 세개 미표시 + 로딩 멈춤 버그 근본 수정 — 원인: `_probeShortsBatch`(쇼츠 재확인 프로브)가 `appendLock`을 최대 64초 보유, 그 동안 `tryLoadMore`가 즉시 return해 점 세개도 그리지 못함. (1) `_probeShortsBatch`에 배치 전체 데드라인 5초 적용(`Promise.race`) → lock 보유 최대 5초로 단축, 발동 시 `probe_batch_deadline` GA 이벤트 기록. (2) `tryLoadMore`에서 lock 중에도 점 세개 선제 표시 → "끝난 것처럼 보이는" 시각 문제 완전 해결. (3) lock 해제 후 기존 polling/releaseLock이 다음 페이지 이어받음.
- [완료][index.html][admin.js] 이벤트 가드 구조 전환 — 23개 오버레이 전체에 `data-modal` 속성 추가; `window pointerdown`에 `[data-modal]` 가드(모달 내 클릭에서 `closePanels` 차단); `window click`에 동일 가드(우주 클릭 판정 차단); `mousemove` `_overPanel` 셀렉터의 하드코딩 오버레이 목록을 `[data-modal]` 단일 선택자로 통합. 편집 모달 뒤 우주 호버/클릭 통과 버그 원천 차단.
- [완료][admin.js] 동명이인 칩 클릭 시 카드 미오픈 수정 — 칩 click에 `e.stopPropagation()` 추가(버블링 1차 차단); `_dqGotoArtist` 데스크톱 분기에 `requestAnimationFrame` 보완 오픈 추가(이벤트 큐 소진 후 패널 미열림 시 `openSidePanel` 재호출). 근본 원인: 칩 클릭 이벤트가 `window click`까지 버블링돼 방금 열린 카드를 즉시 닫았음 — 위 이벤트 가드와 함께 이중 수정.
- [완료][groups.json] 디스코그래피 대규모 업데이트 — Batch 1(15개 그룹): 데이식스·러블리즈·스테이씨·이달의소녀·위너·인피니트·아스트로·아이오아이·여자친구·오마이걸·카라·씨스타·애프터스쿨·이엑스아이디·엠블랙. Batch 2(23개 그룹): 미쓰에이·베리베리·비원에이포·비투비·빅뱅·빅스·블락비·씨엔블루·아이콘·악뮤·에스에프나인·에이비식스·에이프릴·에프티아일랜드·우주소녀·케플러·크래비티·트레저·펜타곤·프로미스나인·하이라이트·제로베이스원·라이즈. 추가 수정: 에이오에이(2→7개)·티아라(4→6개). tracks 포맷 통일(`{no, title, isTitle}` dict). HTML 엔티티 디코딩 적용.
- [완료][groups.json] 엑소 앨범 커버 이미지 8개 누락 보완 — Mama·Miracles in December·LOVE ME RIGHT Repackage·LOTTO Repackage·Universe Winter Special·COUNTDOWN·LOVE SHOT Repackage·REVERXE. 멜론 albumId 검색 후 CDN URL 패턴(`id.zfill(8)` → 3/2/3 분할)으로 생성, HTTP 200 확인 후 반영.
- [완료][index.html] OST 포함/미포함 필터 — 디스코그래피 케밥 버튼 메뉴에 "OST 포함" 토글 추가(OST가 있는 그룹에만 노출). `currentList()` 수정, `updateSortMenuUI()` 수정, sort item 셀렉터 `[data-sort]` 한정으로 OST 버튼 오작동 방지.

## 2026-08-18 (cont. 5)

- [완료][index.html][admin.js] Fable(claude-fable-5)과의 협업 세션 — 무한 스크롤 근본 원인 분석(appendLock 누수) 및 admin 개선 계획(`admin_improvement_plan.md`) 작성. 해당 패치는 cont. 6에서 반영.

## 2026-08-18 (cont. 4)

- [완료][index.html][kpop_universe.css] 컬렉션 커스텀 표지 사진 업로드 — 컬렉션 관리 행에 카메라 버튼(🎥) 추가, 클릭 시 이미지 파일 선택 → `artist-pics` 버킷 `col-covers/{colId}.{ext}` 경로에 upsert 업로드 → publicUrl을 `col.coverImg`에 저장 → `_saveCollections()`. 칩 썸네일·관리 행 썸네일 둘 다 `col.coverImg` 우선, 없으면 기존 영상 썸네일 폴백.

## 2026-08-18 (cont. 3)

- [완료][index.html] 그룹 이름 레이블을 행성 위→아래로 이동 — CSS2DObject anchor를 `(0, radius+0.3, 0)` → `(0, -(radius+0.3), 0)`으로 변경, div top:5px(anchor 아래)로 통일. 행성 위에 있던 별 멤버 이름과 겹치던 문제 해결.

## 2026-08-18 (cont. 2)

- [완료][admin.js][index.html] 해체 그룹 자동 매칭 차단 — `_m2ParseTitle`의 `groupsSorted` 필터에 `&&!v.disbanded` 추가. 이전엔 해체 그룹도 다른 채널 영상 제목에서 자동으로 with_groups에 태깅됐음(disbanded는 공식 채널 수집 컷오프만 했고, 타 채널 제목 매칭은 막지 않았음). 이제 disbanded 그룹은 아예 매칭 대상에서 제외 → 앞으로 신규 영상에는 no-op, 기존 오태깅 영상은 "콜라보 오태깅 재검증" sweep 버튼으로 정리 가능. `_ADMIN_JS_VER` 2026-08-18d→2026-08-18e.

## 2026-08-18 (cont.)

- [완료][groups.json][artists.json][connections.json] 그룹 추가: 저스트비(JUSTB, 2021, 블루닷, 남성 6인, 온리비) — 멤버 5명 신규(임지민·이건우·배인·시우·김상우), 전도염(원더나인)은 groups 배열로 겸직 반영; 배드빌런(BADVILLAIN, 2024, 빅플래닛메이드, 여성 7인, 빌리온) — 멤버 7명 신규; connections 36쌍 추가
- [완료][groups.json][index.html] 유앤비(UNB) 행성→유닛 격하 — groups.json에서 제거, index.html _PROJECT_UNITS에 9명 유닛으로 추가(강유찬/에이스·고호정/핫샷·준/유키스·이의진·오광석·박대원·이형근·지한솔·김기중 모두 gko=유앤비)

## 2026-08-18

- [완료][artists.json] 젝스키스 이재진 인스타그램 링크 삭제.
- [완료][artists.json] 시크릿넘버 민지 인스타그램 링크 삭제(뉴진스 동명이인 민지와 구분해서 처리, 그쪽은 원래 인스타 링크 없었음).
- [완료][index.html] 탐험 피드 "모아보기"류 3종(포맷 모아보기/세대×성별 직캠 모음/N년도 데뷔 그룹 모아보기)에 멤버 단위 2차 라운드로빈 추가 — 사용자 제보("영상이 많아지면서 개인별 제한 없으면 중복 심하지 않을까"). 확인해보니 그룹 단위 캡(`_diversifyFeed`, 그룹당 5개 또는 3개)은 이미 있었지만 그룹 캡 *안에서* 멤버까지는 안 나누고 있어서, 실측(합성 데이터 200회 시행)해보니 영상 20개인 멤버와 1개인 멤버가 같은 그룹에 있을 때 그 1개짜리 멤버는 20%의 확률로만 노출되고 있었음(80%는 아예 안 뜸) — 사용자가 지적한 우려가 실제로 존재하는 문제였음. `_diversifyFeed`에 선택적 `subKeyFn` 인자 추가해 그룹→멤버 2단계 라운드로빈으로 확장(안 주면 기존과 동일하게 동작, 하위 호환), 3개 호출부에 `v=>v.members&&v.members[0]`로 멤버 서브키 적용 + 각 쿼리의 `.select()`에 `members` 컬럼 추가. 같은 합성 데이터로 재검증 — 노출률 20%→100%로 개선, 200회 시행 분포도 멤버A 400/멤버B 200(항상 1개는 보장)/무개인(포메이션) 400으로 고르게 분산 확인, 콘솔 에러 0건.
- [완료][index.html] 멤버 카드 소속사/생년월일 순서를 생년월일 먼저로, 그룹 카드 소속/데뷔 순서를 데뷔 먼저로 변경(사용자 요청 — "언제 시작됐는지"가 개인=생년월일/그룹=데뷔일로 먼저 오는 게 자연스럽다는 논리로 그룹 카드도 같이 결정). 멤버 카드 쪽(#tco 칩)은 `showT()` 초기 렌더와 `_refreshOpenCardText()`(언어 전환 시 재렌더) 두 군데에 순서 로직이 중복돼 있어서 둘 다 수정 — 겸사겸사 `showT()` 쪽에 빠져있던 소속사 영어 번역(`_dispAgency`) 누락도 같이 고침(영어 모드로 처음 들어오면 소속사가 언어 전환 전까지 한글로 남아있던 사소한 기존 버그). 그룹 카드 쪽(#gc-grid)은 정적 HTML 행 순서라 그냥 마크업만 스왑. 헤드리스로 멤버 카드(초기렌더+언어전환 둘 다 "생년월일→소속사→국적" 확인)와 그룹 카드("데뷔→소속→팬덤→리더→멤버" 확인) 둘 다 검증, 콘솔 에러 0건.
- [완료][artists.json] 인스타그램 링크 3건 반영 — 아이오아이 주결경(zhou_jieqiong1216로 수정), 퀸즈아이 키리(삭제), god 데니안(dannyahn_official 추가).
- [완료][index.html] 디스코그래피 트랙 뱃지 아이콘→개수 자체를 통일로 재수정 — 앞서 "아이콘 통일"을 모양만 같은 아이콘 2개(멜론용/스포티파이용)로 처리했는데, 사용자가 재확인해보니 의도는 "트로피 아이콘 딱 하나만 뜨고 클릭하면 멜론·스포티파이 정보가 같이 표기되는 것"이었음(모양 통일이 아니라 개수 통일). `_appendGcDiscogTrackBadges`를 소스별 반복 호출 구조에서, 트랙당 뱃지 하나만 만들고 있는 정보를 `parts.join(' · ')`로 합쳐서 하나의 info 텍스트로 보여주는 구조로 재작성. 헤드리스로 멜론+스포티파이 둘 다 있는 BTS "DNA"로 재검증 — badgeCount 1개, 클릭 시 "멜론 2018 · 16위 · 스포티파이 5억+" 한 줄로 같이 표기 확인, 콘솔 에러 0건.
- [완료][index.html][kpop_universe.css] 디스코그래피 트랙에 멜론 연간 TOP100/스포티파이 마일스톤 뱃지 추가 — 사용자 요청. 아이콘만 있고 클릭해도 정보가 없으면 이상하다는 재피드백으로, 클릭하면 트로피 패널 전체를 새로 열지 않고 그 자리에서 최소 정보("멜론 2018 · 16위"/"스포티파이 5억+")가 작게 펼쳐지는 인라인 토글로 구현. 앨범 넘길 때마다 매번 조회하면 깜빡이므로 디스코그래피 스트립을 그릴 때(그룹당 1회) 멜론/스포티파이 두 테이블을 병렬로 미리 받아 곡 제목(정규화) 기준 맵으로 캐싱(`_loadGcDiscogTrackTrophies`) — 응답이 늦게 와서 앨범 상세가 이미 열려있으면 지금 보던 앨범을 다시 그려 뱃지 반영. 배지 클릭은 `stopPropagation`으로 트랙 행 자체의 클릭(영상 필터 시트 열기, 어제 만든 기능)과 안 겹치게 분리. 헤드리스로 BTS "DNA"(LOVE YOURSELF 承 'Her') 실측 검증 — 뱃지 2개 정상 노출, 클릭 시 "멜론 2018 · 16위" 정확히 표시, 트랙 행 클릭(영상 필터)과 안 겹침, 재클릭 시 정상 접힘, 콘솔 에러 0건.
- [완료][index.html][artists.json] 업텐션/엑스원 이진혁 인스타그램 링크 추가.
- [완료][index.html][kpop_universe.css] 트로피 아이콘 3개 → 1개+탭 방식으로 통합(사용자 제안: "하나의 트로피 아이콘 눌러서 안에서 탭으로 나누는 게 제일 깔끔하지 않아?"). `gc-melon-top100`/`gc-spotify-milestone`/`tt-melon-top100`/`tt-spotify-milestone` 버튼 4개 삭제, `gc-trophy`/`tt-trophy` 하나로 통일. `_loadTrophy`/`_loadMelonTop100`/`_loadSpotifyMilestones` 3개 함수를 `_loadTrophies()` 하나로 합쳐 3개 테이블 병렬 조회 후 데이터 있는 종류만 모음 — 하나도 없으면 버튼 자체를 숨기고, 1종류만 있으면 탭 없이 바로 그 목록을, 2~3종류면 세그먼트 탭(기존 디스코그래피 종류필터와 같은 `.seg-control` 컴포넌트 재사용)으로 전환. 헤드리스로 아이유(3종류 다 있음 — 탭 3개 노출+기본 선택 "방송 1위" 94건+"스포티파이" 탭 전환 시 3건 정상 갱신) 검증, 옛 버튼 4개 DOM에서 완전히 사라진 것도 확인, 콘솔 에러 0건.
- [완료][index.html] 트로피 3종(음악방송 1위/멜론 연간 TOP100/스포티파이 마일스톤) 멤버 카드 노출 사각지대 발견+수정 — 사용자 질문("멜론 top100에서 개별 멤버 기록도 반영되어야 할 것 같은데")으로 발견. 실측 확인 결과 melon_top100_matched.json 799건 중 247건이 member_ko 있는 그룹멤버 개인기록, 78건이 group_ko="솔로"(아이유·비 등 완전 솔로 아티스트) — 이 트로피 버튼들은 지금까지 그룹 카드에만 달려있었는데, "솔로"라는 그룹 카드 자체가 없어서 78건은 어디에도 노출될 방법이 없었음(그룹 멤버의 개인기록 247건은 그래도 소속 그룹 카드에서는 보였음). `_loadTrophy`/`_loadMelonTop100`/`_loadSpotifyMilestones`에 선택적 `mko` 인자 추가해 멤버 카드(#tt)에도 트로피 버튼 3개 신설, `.eq('group_ko',a.group.ko).eq('member_ko',a.name.ko)`로 개인 기록만 조회(솔로 아티스트는 a.group.ko가 이미 "솔로"라 그대로 씀 — 콘서트/컬렉션이 쓰는 `_ytGroupKoFor` 변환과는 다른 값이라 혼동 주의 주석 남김). 헤드리스로 실제 DB 조회 검증 — 아이유 카드에서 멜론 56건/스포티파이 3건 정상 노출(이전엔 0건), 태연 카드에서 개인 솔로곡 17건 노출, 그룹 카드 쪽 회귀 없음, 콘솔 에러 0건. **음악방송 1위(music_show_wins)는 기존 솔로 레코드 94건(전부 아이유 확인됨)이 member_ko 컬럼 자체가 비어있어서 이 방식으로는 안 잡힘 — 별도 백필 SQL 필요(다음 항목 참고).**
- [완료][spotify_milestones_seed.sql] 스포티파이 누적 스트리밍 마일스톤 리서치+DB매칭 완료, SQL 시드 파일 생성 — 멜론 TOP100 799곡 범위 내에서 114곡 마일스톤 확정(1억+ 86곡/5억+ 21곡/10억+ 6곡/20억+ 1곡[BTS Dynamite 실측 23.3억], 30억·50억 도달곡 없음). kworb.net(3rd-party 스포티파이 집계 사이트)에서 아티스트당 페이지 1회 조회로 82개 아티스트 커버(4개 그룹은 스포티파이 아티스트ID 자체를 못 찾아 제외), 한글↔로마자 표기 차이(예: "하루하루"→"Haru Haru")는 자체 로마자 변환기로 재매칭. groups.json/artists.json 대조 독립 검증 통과(불일치 0건, 마일스톤 티어 값 오류 0건, 중복 0건). 654개 고유곡 중 매칭은 17%(114곡)뿐인데, 나머지 대부분은 `below_1eok_threshold`(159건, 1억 미달=정상 결과)이거나 2007~2012년 구곡이 스포티파이 카탈로그에 없거나 그룹 태그와 달리 실제론 멤버 솔로곡이라 그룹 단위 조회로 못 잡힌 경우(363건) — 상위 히트곡 위주로는 이미 커버됐다고 판단. **참고**: fork의 서술 요약에서 "BTS 32곡"이라고 보고했는데 실제 matched.json/debug.json 둘 다 17곡으로 일치 — 서술 요약 쪽 오기로 보이며, 실제 저장 파일(SQL 반영 대상)은 서로 일치하고 독립검증도 통과해 데이터 자체는 신뢰. **SQL 미실행 — 사용자가 Supabase SQL 에디터에서 spotify_milestones_seed.sql 실행 필요**(테이블 스키마는 이미 생성됨).
- [완료][index.html][kpop_universe.css] 디스코그래피 트랙 클릭 → 영상 목록 방식 재설계(사용자 재피드백: "돌아가기가 너무 어려움") — 처음엔 기존 영상 검색창에 곡 제목을 채워 그 자리에서 그리드를 필터링하는 방식으로 만들었는데, 원래 보던 앨범 화면으로 되돌아갈 방법이 없었음(검색창을 수동으로 비워야 함 + 앨범 상세 팝업은 이미 닫혀있어 위치를 잃음). 그리드를 안 건드리고 앨범 상세 팝업(#gc-discog-detail, z-index:102) 위에 새 시트(#gc-track-videos-overlay, z-index:110)를 얹는 방식으로 전면 재설계 — 앨범 상세는 뒤에서 그대로 살아있어서 ×로 닫기만 하면 원래 화면으로 즉시 복귀(별도 상태 복원 로직 불필요, 그냥 안 건드렸으니까). 새 시트는 그룹 카드 영상 그리드와 동일한 노출조건(무관/숨김 제외, 자체+게스트 태깅 포함, title_norm ilike 정규화 매칭)으로 독립 조회 후 썸네일 리스트로 렌더, 항목 클릭 시 그 목록을 플레이리스트로 넘겨서 라이트박스 재생(다음 영상 넘기기까지 지원). 헤드리스로 실제 그룹(코르티스) 앨범 열어서 트랙 클릭 → 새 시트 오픈+앨범 상세는 뒤에서 유지 확인 → 닫기 → 앨범 상세 그대로 복귀(제목 불변) 확인 → 재오픈 후 영상 클릭 → 라이트박스 정상 오픈까지 전체 플로우 검증, 콘솔 에러 0건.
- [완료][index.html] 스포티파이 스트리밍 마일스톤 트로피 신설 — 멜론 연간 TOP100과 같은 모달(#trophy-overlay) 재사용, `_openTrophyList(rows,kind)`를 3종(music_show/melon_top100/spotify_milestone) 대응하도록 title/컬럼 매핑을 객체 기반으로 정리. 기간별(일간/주간) 순위는 스포티파이도 공식 과거 아카이브가 없어 소급 불가+상대적 기록이라 트로피 철학에 안 맞다고 판단해 배제, 대신 누적 스트리밍 티어(1억/5억/10억/20억/30억/50억)를 스냅샷으로 기록하는 방식 채택(사용자 논의 후 확정). `spotify_streaming_milestones` 테이블(그룹카드 SQL만 전달, admin.js 쓰기 UI 없음 — 기존 트로피들과 동일 패턴) + `#gc-spotify-milestone` 버튼 추가. 헤드리스로 3종 전부 렌더+제목/포맷("10억+" 등) 검증, 콘솔 에러 0건. ~~SQL 미실행, 실제 799곡 조사도 아직 시작 전.~~ → **해소 확인(2026-08-25)**: `spotify_streaming_milestones` 실제 조회 결과 **455행** 적재됨(2026-08-19 `spotify_milestones_seed.sql`+A등급 스윕분). 조사·시드·실행 전부 끝난 상태.
- [완료][melon_top100_seed.sql] 멜론 연간 TOP100(2008~2025, 18개년) 리서치+DB매칭 완료, SQL 시드 파일 생성 — 799건, 86개 그룹/솔로. 멜론 공식 연간차트 페이지 curl 파싱(18개년 전량 100위까지) 후 groups.json/artists.json과 대조. 동명이인 오매칭 감사 별도 수행 — "노을"(레인보우 vs 비아이돌 밴드), "주호"(에스에프나인 vs 비아이돌 솔로) 등 블랙리스트 처리, 그룹 데뷔연도보다 이른 차트연도 매칭 23건 시간모순으로 자동/수동 배제, 동명이인 후보 겹침 15건은 데뷔연도+곡 인지도로 수동 판정(`melon_top100_rejected.json`에 사유 기록). group_ko/member_ko를 groups.json/artists.json에 대조하는 독립 검증 통과(0건 불일치), year+rank 중복 0건. 2008~2013년은 매칭률이 낮은데(30~44%) 그 시기 국내 음원 유통이 멜론 외 다른 플랫폼(도시락/벅스 등)에 분산돼있었을 가능성(데이터 자체 한계로 판단, 매칭 로직 문제 아님). **SQL 미실행 — 사용자가 Supabase SQL 에디터에서 melon_top100_seed.sql 실행 필요**(테이블 스키마는 이미 생성됨).
- [완료][index.html][kpop_universe.css] 마이페이지 FAVORITES/ANNIVERSARIES 통합 리뉴얼 — 사용자가 지적한 3가지(스크롤 너무 김/시각적으로 밋밋함/편집 플로우 불편) 중 구조+시각 문제를 한 번에 해결. 기존 anniv-list가 사실 favGroups/favMembers를 날짜순으로 재배열한 것뿐이라 완전히 중복이었던 걸 확인하고, ANNIVERSARIES 섹션을 통째로 제거해 FAVORITES 카드 안에 흡수: 아바타(원형 사진 또는 색상원) + 이름/태그 + 기념일 배지(멤버 생일 D-day + 팬 기념일 D+day)를 한 줄 카드로. 멤버/연결 아바타는 이미 있는 인스타/위버스 사진 캐시(`_instaPicCache`) 그대로 재사용, 그룹 아바타는 groups.json에 아직 없는 `img` 필드를 읽어서(값 있으면 사진, 없으면 그룹 고유 색상 원형+같은 색 은은한 글로우) — `img` 필드는 인스타 링크처럼 그룹별로 하나씩 채워나갈 예정, 코드는 이미 준비됨. `renderFavList()`/`_buildAnniversaryList()` 두 함수를 `renderFavList()` 하나로 합치고(멤버 사진 캐시 프리로드 await, 재진입 시 낡은 렌더 버리는 세대 카운터 추가), 기념일 편집(`_openDateEditor`)은 각 카드 우측 ⋮ 버튼으로 유지. 헤드리스로 그룹(이미지 있음/없음 둘 다)·멤버(생일+팬기념일 배지 동시 표시)·연결 세 종류 전부 렌더 검증 + 편집 버튼 클릭이 카드 자체 네비게이션과 안 겹치는지 확인, 콘솔 에러 0건. 컬렉션 커스텀 표지 업로드(3번째 요청)는 Storage 인프라가 필요해 별도 작업으로 보류.
- [완료][index.html] 마이페이지(journal-sidebar) 열린 채로 "편집" 누르면 편집 모달(#profile-overlay)이 패널 뒤로 가려지던 버그 수정 — 사용자 제보. 원인: journal-sidebar는 `_bringToFront()`로 동적 z-index를 쓰는데(연결/필터 패널 등을 거치며 세션 내내 계속 누적 상승), profile-overlay는 정적 z-index:69 고정이라 세션이 길어지면(다른 패널 몇 번만 열어도) sidebar가 69를 넘어서 역전됨. `openProfilePanel()`에도 `_bringToFront()` 호출 추가해서 열 때마다 항상 최상단이 되게 수정 — 기존 "겹치는 UI 레이어는 항상 최근 실행 화면이 최상단" 원칙(_bringToFront 유틸) 그대로 재사용. 헤드리스로 실제 버그 재현(sidebar z-index를 70까지 인위적으로 쌓아올린 뒤 편집 열기) 후 수정 검증 — overlay가 항상 sidebar보다 위(72>70) 확인, 콘솔 에러 0건.
- [완료][index.html][kpop_universe.css] 멜론 연간 TOP100 트로피 신설 — 기존 "음악방송 1위" 트로피(music_show_wins, SQL 에디터로만 관리)와 같은 철학(정적 성취 기록, 개인화 없음)으로 확장. `melon_yearly_top100` 테이블 신설(그룹 카드에 SQL만 전달, admin.js 쓰기 UI 없음 — music_show_wins과 동일 패턴), 그룹카드 이름줄에 두번째 트로피 아이콘(`#gc-melon-top100`) 추가. `_openTrophyList(rows,kind)`를 두 종류(music_show/melon_top100) 다 처리하도록 확장해서 모달 하나 재사용 — kind별로 제목/열 내용만 다르게(날짜+방송사+곡 vs 연도+순위+곡). 헤드리스로 가짜 데이터 렌더 검증(신규 케이스 정상, 기존 음악방송 1위 케이스 회귀 없음, 콘솔 에러 0건). ~~테이블은 아직 실제 SQL 미실행 — 사용자가 Supabase SQL 에디터에서 CREATE TABLE 실행 필요. 실제 차트 데이터는 별도 진행 중, 아직 0건.~~ → **해소 확인(2026-08-25)**: `melon_yearly_top100` 실제 조회 결과 **799행** 적재됨(2026-08-19 `melon_top100_seed.sql`). 테이블 생성·시드 실행·차트 데이터 전부 끝난 상태.
- [완료][index.html] 멤버 별 점 크기가 고밀도(HiDPI) 화면에서 유독 작아 보이던 버그 수정 — 사용자 제보("특히 웹으로 볼 때 멤버 별 점이 작게 느껴진다"). 실측으로 원인 특정: `makePointsMat`의 `size:sizeW/Math.min(devicePixelRatio,2)`가 devicePixelRatio로 "나눠서" 만들어져 있었는데, three.js(r128) 포인트 sizeAttenuation의 `scale` uniform이 드로잉버퍼(디바이스 픽셀)가 아니라 CSS 픽셀 높이 기준이라(직접 uniform 값 읽어서 확인 — DPR을 1→2→3으로 바꿔도 scale값 불변) devicePixelRatio를 전혀 보정해주지 않음. 결과적으로 CSS 픽셀 기준 체감 크기가 DPR²에 비례해 작아지는 구조였음(DPR=2 화면에서 이론상 75% 축소 — 맥/최신 노트북 스케일링 화면/대부분 폰이 DPR≥2). "나누기"를 "곱하기"로 뒤집어 DPR을 상쇄하도록 수정 — 곱하기로 바꾸면 대수적으로 DPR이 완전히 소거되어 화면 밀도와 무관하게 항상 같은 CSS 픽셀 크기가 됨. 헤드리스 크롬을 `--force-device-scale-factor=1`/`=2`로 각각 새로 띄워(런타임 오버라이드가 아니라 페이지 로드 시점부터 다른 DPR로) 수정 전/후 스크린샷 직접 비교 — 수정 전엔 DPR2에서 행성 안 멤버 별이 거의 안 보일 정도로 흐릿했는데, 수정 후엔 DPR1과 동등하게 뚜렷이 보임을 육안 확인. 콘솔 에러 0건.
- [완료][admin.js][index.html] 일회용 버튼 3개 제거(사용자 요청, "연도별 TOP 최초 백필"은 남김) — ①"아이돌 주도 채널 재태깅(일회용)"(`sp-idol-resync-btn`/`_ytResyncIdolChannels`), ②"HTML 엔티티 깨진 제목/설명 정리(일회용)"(`sp-yt-fix-entities`/`_ytFixHtmlEntities`), ③"cover_of ↔ with 겹침 정리(일회용)"(`wonkok-overlap-fix-btn`+하드코딩 `WONKOK_OVERLAP_FIXES` 4건). 버튼 HTML+함수+리스너+관련 상수/상태변수(`_extIdolResyncing`)까지 전부 삭제. 헤드리스로 admin.js 로드+영상관리패널 오픈까지 회귀 검증(콘솔 에러 0건, 남긴 백필 버튼은 그대로 존재, 지운 3개는 DOM에서 사라짐 확인). `_ADMIN_JS_VER` 2026-08-18c→2026-08-18d.
- [완료][admin.js][index.html] 영상 관리 패널 이중 검색(2차 재검색어) 저장 후 초기화되던 버그 수정 — 영상 편집 모달 저장·닫기(`_closeVidTagModal`)가 목록 새로고침을 위해 `_vmLoad()`를 다시 부르는데, 이 함수가 "1차 검색어 새로 바뀜=2차 재검색어 무의미"라는 원래 의도로 항상 `_vmSearch2`를 초기화하고 있어서, 1차 검색어가 안 바뀐 단순 새로고침 상황까지 2차 검색어가 매번 날아갔음(사용자 제보). `_vmLoad(searchTerm, preserveSearch2)`에 두번째 인자 추가해 편집 저장 후 새로고침(`_vmLoad(undefined,true)`)만 2차 검색어를 보존하도록 분기, 실제 검색창 타이핑으로 부르는 기존 호출들은 그대로 초기화 유지. 헤드리스로 sb 목(mock) 객체 씌워서 3가지 시나리오(1차 검색/편집후 새로고침/새 1차 검색) 전부 검증 완료. `_ADMIN_JS_VER` 2026-08-18b→2026-08-18c.
- [완료][index.html] 영상 제목 마퀴(슬라이드) 속도 소폭 하향 — `_MARQUEE_PX_PER_SEC` 140→128(사용자 요청, "아주 조금만 느리게").
- [완료][artists.json] 에이핑크 김남주 인스타그램 링크 수정 — kimnamjoo_official → sarangdungy(사용자 제보).
- [완료][artists.json] 에이핑크 손나은 탈퇴 반영 — active:true→false, left:"2022.04.08" 추가(웹검색으로 확인, IST엔터테인먼트가 2022-04-08 발표·5인 체제 전환). 같은 그룹 홍유경(2013 탈퇴) 기존 표기 패턴 그대로 따름 — co 필드는 안 건드림(현재 값이 전원 공통 표기라 탈퇴 시점 소속사와 무관).
- [진행중][index.html][kpop_universe.css] "내 우주 카드" 공유 이미지 기능 1차 구현 — 공유 기능 강화 논의 끝에, 새 카드 템플릿을 따로 디자인하는 대신 이미 있는 3D 씬(글로우/듄 컬러링/별밭)을 실제로 캡처해서 쓰는 방식 채택("아름다움"을 새로 만들 필요 없이 앱 본체 미학 재사용). 마이페이지 FAVORITES 섹션에 "✦ 내 우주 카드 만들기" 버튼 신설 — favMode(내 우주 모드)가 켜져서 궤도가 안정된(phase==='active') 상태에서만 동작, 그 라이브 상태를 라이브 카메라는 안 건드리고 임시 카메라로 한 번 더 렌더해서 캡처. `_renderMyUniverseSnapshotDataURL()`(1080×1350 오프스크린 WebGL 캡처, renderer.setSize→render→toDataURL→즉시 원복이 전부 동기라 화면 깜빡임 없음 확인) → `_buildMyUniverseCardDataURL()`(2D 캔버스에 딥네이비+내 별색 틴트 배경 합성 + 발광 타이포로 "{닉네임}의 우주"/"K·POP UNIVERSE" 워터마크) 2단계 파이프라인. 미리보기 모달(#uc-card-overlay)에서 저장(다운로드)/공유(Web Share Level 2, `navigator.canShare({files})`로 이미지 파일 직접 공유 — 미지원 브라우저는 다운로드+토스트로 폴백) 가능. 헤드리스로 실제 이미지를 저장해서 육안 검증하다가 발견 — 화각 안에 즐겨찾기 안 한 다른 그룹 행성이 우연히 걸려서 "남의 우주"처럼 보이는 문제 발견, 캡처 순간만 궤도 밖 행성(bubbleMeshes 중 orbit에 없는 것)을 숨겼다가 즉시 복원하는 방식으로 수정. i18n(T.ko/T.en) 정식 반영. **아직 실제 즐겨찾기 데이터로는 미검증** — 테스트는 헤드리스에서 즐겨찾기 상태를 흉내낸 것이라 실제 서비스에서 그룹 여러 개 + 멤버 동반 별들까지 있을 때 구도가 어떻게 나오는지 사용자가 실사용으로 확인 필요. 다음 단계(같은 렌더러에 파라미터만 바꿔 기념일 카드로 확장)는 이 확인 이후 진행.
- [완료][admin.js][kpop_universe.css][index.html] 영상 관리 패널 정리 — ①버튼 CSS 통일: `#vm-normal-btn`("선택-정상")·`#vm-coverclear-btn`("선택-원곡제외")에 CSS 규칙 자체가 없어서(vm-toolbar 안 다른 버튼들은 다 있는데 이 둘만 브라우저 기본 버튼 스타일로 표시되고 있었음) `#vm-indiv-btn`("선택-개별")과 동일한 블루 스타일 적용해 통일. ②검색 결과 줄에 그룹만 보이던 걸 멤버/콜라보/원곡 태깅 정보까지 한 줄로 요약해 보여주도록 `_vmTagsLine()` 신설(members→"멤버:", with_members+with_groups→"함께:", cover_of_members+cover_of_groups→"원곡:", 값 있는 항목만 " · "로 구분) — `.vm-item` 렌더에 `.vm-tags` 줄 추가, 이 정보를 안 담고 있던 4개 조회 쿼리(전체 탭 3자+ ilike/1~2자 avsCache/검수 탭/무관·숨김 탭)에 전부 필드 추가해서 어느 경로로 들어와도 빠짐없이 보이게 함. 썸네일 화질은 그대로 유지(변경 요청 없었음, mqdefault 그대로). 헤드리스로 가짜 데이터 주입 후 렌더 검증 — 태그 줄 정상 조합("멤버: 지수, 로제 · 함께: 카리나(에스파), 에스파 · 원곡: 아이유(솔로)"), 태그 없는 영상은 줄 자체가 안 생김(조건부 렌더 확인), 버튼 5개 computed style 전부 일치, 콘솔 에러 0건. `_ADMIN_JS_VER` 2026-08-18a→2026-08-18b.
- [완료][index.html][sw.js] CDN 스크립트(three.js/OrbitControls/CSS2DRenderer/supabase-js) 로드 실패 시 로딩 화면 영구 멈춤 수정 — fable이 별도 헤드리스 감사(가오픈 전 점검)로 발견+패치한 걸 검토 후 반영. three.js가 최상위에서 바로 `new THREE.Scene()`을 실행하는 구조라 CDN 차단(회사망/광고차단/CDN 순단)이면 로딩 화면("Here is Our K-Pop Universe..!")에서 에러 안내도 재시도 버튼도 없이 영원히 멈췄음(JSON fetch 쪽엔 이미 재시도가 있었지만 스크립트 로드는 그보다 앞단이라 해당 안 됨). 2중 방어 추가 — ①원본 CDN(cdnjs/jsdelivr) 실패 시 제3 CDN(unpkg)에서 한 번 더 로드 시도(`document.write` 동기 로드로 순서 보장), ②그래도 실패하면 메인 스크립트 최상단 KPU_LIB_GUARD가 기존 "우주를 불러오지 못했어요" 화면과 같은 UI(에러 문구+다시 시도 버튼, KO/EN 자동)로 전환하고 실행 중단 + GA `cdn_load_error` 이벤트 기록. fable이 준 패치 파일(index2.html)이 이 세션에서 그 사이 커밋된 다른 수정 2건(애니버서리 순서 재배열, admin.js 캐시버스팅)이 반영 안 된 시점 스냅샷이라 통째로 덮어쓰지 않고 새로 추가된 가드 블록만 diff로 골라내 현재 index.html에 이식 — 헤드리스로 diff 검증(그 두 수정 외 divergence 없음 확인). sw.js CACHE_VERSION v3→v4(index.html 변경이라 재방문 유저 캐시 무효화 필요). 미검증 1건(fable 보고) — "원본 CDN만 죽고 unpkg는 살아있는" 중간 시나리오는 배포 후 개발자도구에서 cdnjs 요청만 차단해 별도 확인 필요.
- [완료][index.html] admin.js 캐시버스팅 누락 버그 수정 — "HTML 엔티티 정리" 타임아웃을 admin.js에서 고쳤는데(위 항목) GitHub 재업로드까지 했다는데도 사용자가 계속 같은 타임아웃을 겪는다고 제보. 원인 확인 — `_loadAdminScript()`가 `s.src='admin.js'`로 쿼리 파라미터 없이 로드하고 있어서, 브라우저 디스크캐시나 GitHub Pages 앞단 CDN(Fastly)이 재배포 이후에도 옛 admin.js를 계속 서빙할 수 있는 구조였음(코드 자체 재검증 결과 admin.js 최신 파일엔 이미 고친 순수 페이지네이션 방식이 정상 들어있음 — 배포 코드 문제가 아니라 캐시 문제로 결론). `_ADMIN_JS_VER` 문자열 신설해 `admin.js?v=버전`으로 로드하도록 변경 — sw.js의 CACHE_VERSION과 같은 방식으로, admin.js 내용을 바꿀 때마다 이 버전 문자열도 같이 올려야 함(PRINCIPLES.md에 추가 필요).
- [완료][mc_history.json] 6대 음악방송(엠카운트다운/뮤직뱅크/인기가요/음악중심/쇼챔피언/더쇼) MC(엠씨) 역대 계보 데이터 신설 — `{show, memberKo, groupKo, start, end}` 스키마의 독립 JSON 파일, groups/artists/connections.json과 같은 급의 정적 데이터. 각 방송 나무위키 페이지를 fork로 전수 조사한 원본(방송별 기수/이름/재임기간)을 실제 DB 멤버명(artists.json)과 매칭 — 이름만으로 유일하게 매칭되는 케이스, 괄호 힌트(그룹명/영문명)로 동명이인 중 좁혀지는 케이스, 그래도 남는 동명이인은 그룹 데뷔연도/해체연도로 후보를 걸러내는 자동 로직까지 적용해 255건 확정(비아이돌·매칭불가 이름은 전부 제외). 자동 로직으로도 안 풀리는 예외 몇 건은 수동 처리 — 소희(2007 원더걸스·2024 라이즈로 동명이인이 실제 두 명), 김규빈(제로베이스원 vs 앤더블 — 앤더블 데뷔일이 해당 MC 임기 시작보다 늦어 시간상 불가능하다고 판단해 제로베이스원으로 확정), 나나(WOOAH — 매칭 로직이 멤버 영문명만 보고 그룹 영문명 힌트를 못 읽던 버그를 수동 확인으로 우회), 쇼챔피언 4대 도영·재현(NCT 정식 데뷔 전 MC 발탁 — 사용자가 실제로 있었던 일이라 확인해줌, 통상적인 데뷔연도 필터로는 걸러졌을 케이스), 뮤직뱅크 14대 지성(배우 지성이라 비아이돌로 제외, 사용자 확인). 아직 안 한 것 — 이 데이터를 이용한 영상 자동 태깅 개선(MC 진행 클립 vs 무대 클립 구분)과 멤버카드 MC 경력 노출(표기 방식 미정)은 둘 다 향후 과제로 남겨둠, 이번엔 데이터 파일만 신설.
- [완료][admin.js] "HTML 엔티티 깨진 제목/설명 정리(일회용)" 버튼이 "조회 실패: canceling statement due to statement timeout"로 계속 실패하던 버그 수정 — 서버에 `title.ilike.%&#%` 등 OR로 묶은 게 원인이었음. `%&#%`가 2글자 패턴이라 pg_trgm 인덱스가 트라이그램을 못 뽑아 인덱스를 못 타고(3글자 미만은 인덱스 가속 자체가 안 됨), description은 애초에 트라이그램 인덱스가 없어서, OR 조건 중 하나라도 인덱스를 못 타면 전체 테이블(실측 353,910행)을 순차스캔하게 됨 — 바로 위 "전체 영상 검색(admin)" 기능이 예전에 겪었던 "원곡" 스캔 타임아웃과 똑같은 원인. 그때 고친 방식 그대로 재사용 — 서버 필터 없이 순수 id 페이지네이션(1000개씩)으로 전량 가져와 클라이언트에서 엔티티 여부 판별. 실측으로 1페이지(1000행) 0.33초 확인 — 전체 훑는 데 몇 분 걸리겠지만 타임아웃 없이 끝까지 돎.
- [완료][index.html] 탐험 패널 "Today's Anniversary" 줄 순서 변경 — 그룹 데뷔 N주년이 멤버 생일보다 앞(왼쪽)에 오도록 `_buildFeedAnniversaries` 안 debut/bday 두 forEach 블록 순서를 맞바꿈(_appendAnnivStripItem이 매번 list 끝에 이어붙이는 방식이라 호출 순서=화면 순서). 사용자 확인 — 이 기능(Today's Anniversary)엔 그룹 데뷔주년/멤버생일 두 종류만 있고 다른 기념일 로직 없음(마이페이지 즐겨찾기 "기념일 아카이브"는 별개 기능, 거긴 연결 기념일까지 3종류).
- [완료][index.html] "주간 직캠 TOP 20" → "주간 개인 직캠 TOP 20"(EN: Weekly Solo Fancam TOP 20)로 개편 — 그룹 전체 무대/포메이션 영상 말고 개인 직캠만 노출되게 필터 추가. `_buildFeedWeeklyTopCams` 쿼리에 `members` 필드 추가해서 `(v.members||[]).length===1`(정확히 한 명만 태깅) 조건을 기존 `_FANCAM_TITLE_RE` 제목 필터에 AND로 결합. 실제 DB 샘플 조회로 검증 — K-Choreo/MIX직캠(그룹 전체 포메이션)은 members가 빈 배열, 진짜 개인 직캠은 그 한 명만 담겨있는 패턴 확인 후 적용. 최근 7일 실데이터로도 필터 적용 후 53개 후보(그룹당 3개 캡 적용 후) 확보 — 카드 유지에 필요한 최소 3개는 여유 있게 통과. **이 세션부터 index.html이 유일한 정식 파일**이라 별도 동기화 단계 없이 여기서 바로 끝(아래 구조변경 항목 참고).
- [완료][index.html][kpop_universe.html][manifest.json][sw.js][PRINCIPLES.md] **구조 변경: index.html이 유일한 정식 앱 파일이 됨.** 원래 index.html은 kpop_universe.html의 수동 복사본이라 매번 동기화해야 했고 실제로 몇 번 깜빡해서 옛 버전이 노출된 사고가 있었음(오늘 세션에도 2건 발견+수정) — 사용자가 "수동 복사 대신 kpop_universe.html이 index.html로 리다이렉트하게 하자"고 제안했는데, 확인해보니 sitemap.xml·og:url이 전부 루트(index.html)를 정식 URL로 선언해놔서 그 방향대로 하면 대부분 트래픽(검색·공유 유입)이 매번 이중 리다이렉트를 겪고 SEO에도 불리했음. manifest.json의 start_url이 `./kpop_universe.html`로 별도 지정돼있던 게 두 파일이 각자 존재해야 했던 진짜 이유였음을 발견 — 이걸 `./`로 바꿔서 kpop_universe.html을 더 이상 필요 없게 만들고, **반대 방향**으로 kpop_universe.html을 `location.replace('./')`하는 얇은 리다이렉트 파일로 전환(옛 PWA 바로가기/북마크 하위호환용). sw.js도 같이 손봐야 했음 — 오프라인 캐시 폴백 대상이 하드코딩으로 `./kpop_universe.html`이었던 걸 `./`로 변경, PRECACHE_URLS에 `./`·`./index.html` 추가, CACHE_VERSION v2→v3. 헤드리스 크롬으로 실제 리다이렉트 체인(kpop_universe.html 접속 → `/`로 이동 → 앱 정상 로드, GROUPS 249개·콘솔 에러 0건) 검증 완료. **앞으로 코드 작업은 항상 index.html에 할 것 — kpop_universe.html은 절대 직접 수정 금지**(PRINCIPLES.md에 명시).
- [완료][kpop_universe.html] 모바일에서 행성(구체)이 가끔 일시적으로 세로로 찌부/늘어나 보이는 버그 수정 — three.js camera/renderer resize 핸들러가 디바운스 없이 매 resize 이벤트마다 즉시 window.innerWidth/innerHeight로 camera.aspect를 갱신하고 있었음. 모바일(특히 iOS Safari)은 주소창 접힘/펼침·화면회전 도중 resize가 짧은 간격으로 여러 번 발생하는데 그 사이 너비/높이가 순간적으로 안 맞는 값을 보고할 수 있어(2026-08-18, 사용자 제보), 이미 검색창 리사이즈에 쓰던 것과 같은 디바운스(150ms) 패턴 적용. 헤드리스 크롬으로 CDP Emulation.setDeviceMetricsOverride를 30ms 간격 3연타(390×700→390×760→390×800)로 흔들어 실제 주소창 접힘과 유사한 지터를 재현+검증 — 디바운스 중엔 camera.aspect가 중간값 없이 이전 값 그대로 유지되다가 마지막 안정된 크기(390×800)로만 한 번에 갱신되는 것 확인.
- [완료][admin.js][kpop_universe.html] 영상 제목/설명에 "&#39;" 등 HTML 엔티티 그대로 노출되던 버그 수정 — YouTube API가 title/description을 종종 HTML 이스케이프해서 내려주는데(어퍼스트로피→&#39;, &→&amp; 등) 동기화 코드가 그대로 저장, 화면은 안전하게 textContent로만 찍다 보니 엔티티 텍스트 자체가 그대로 보였음. `_decodeHtmlEntities()`(textarea innerHTML→value 트릭, 모든 named/numeric 엔티티 커버) 신설해 동기화 3개 캡처 지점(채널 훑기 `_ytFetchNewVideos`/과거 백필 search API/URL 수동추가)에 전부 적용. 기존에 이미 오염된 채로 저장된 행 정리용으로 "HTML 엔티티 깨진 제목/설명 정리(일회용)" 버튼 신설(tags_manual 행 보호, title_norm도 같이 재계산). 헤드리스 브라우저로 디코딩 로직 자체는 검증 완료(정상 "&" 포함 제목은 안 건드림 확인) — 실제 DB 백필 실행은 관리자가 버튼 눌러야 함.
- [완료][kpop_universe.html] 구글 로그인 "한 번에 안 되고 다시 시도해야 됨" 제보 — 원인 두 가지 발견해 수정. ①buildUniverse()의 최초 데이터 로드(groups/artists/connections.json fetch)에 타임아웃/재시도가 전혀 없어서, 구글 인증 화면 갔다 리디렉트로 돌아오는 순간(모바일에서 특히)처럼 네트워크가 잠깐 불안정한 때와 겹치면 그대로 throw → "우주를 불러오지 못했어요" 화면 — 카드그리드 로딩에 이미 쓰던 조용한 자동재시도(`_withTimeout` factory+retries, 8초×3회)를 여기도 적용. ②createClient()의 detectSessionInUrl이 URL의 `?code=`를 세션으로 교환하는 게 비동기(Supabase 토큰엔드포인트 왕복)라, 리디렉트 직후 곧바로 `getSession()`을 부르면 아직 교환 전이라 예전 익명 세션이 잡히는 레이스 — "구글 로그인 콜백은 감지됐지만 세션이 익명 상태로 남음" 경고가 뜨던 게 바로 이 증상. 짧게(350ms×최대6회) 재확인하는 루프 추가. 헤드리스 크롬으로 정상 로딩 경로 회귀 없음 확인(GROUPS 249개 정상 로드, 콘솔 에러 0건) — 단 실제 구글 OAuth 리디렉트 레이스 자체는 이 환경에서 재현/검증 불가.
- [완료][kpop_universe.html] 디스코그래피 영어모드 "N집" 서수 처리 — 위 재정정 직후 사용자가 "Full 2집"처럼 "집"이 번역 안 된 채 남는 걸 발견, 영어 모드에서만 서수+타입 순서로 재배치("정규 2집"→"2nd Full", "미니 5집"→"5th Mini"). `_ordinal(n)` 헬퍼 신설(11/12/13 예외 포함 표준 서수 접미사 로직), 정규식으로 "N집" 패턴 파싱 후 재조합. 한국어 모드는 그대로 유지("Mini 5집"처럼 Mini만 원래 영단어라 안 어색함).
- [완료][kpop_universe.html] 디스코그래피 앨범 타입 표기 재정정 — 오늘 먼저 "미니"→"EP" 통일 요청받아 처리했는데(위 항목 참고) 사용자가 번복, "EP"→"Mini"가 맞다고 정정 + "정규"도 언어별 전환 신규 요청. 최종 규칙: All/Mini/Single은 Single·All처럼 언어 무관 항상 영문(탭 세그먼트 라벨용 `_discogBucketLabel()` 신설로 기존 정적 객체 `_DISCOG_BUCKET_LABEL` 대체), "정규"만 한국어 모드에선 원문 유지·영어 모드에서만 "Full". `_discogTypeLabel()`도 같은 규칙으로 수정 — 원본 데이터에 "미니 N집" 말고 단독 "EP" 문자열도 있어서(`_discogBucket`의 `/^(미니|EP)/` 버킷 판정과 동일 패턴 적용) 같이 안 잡았으면 "EP" 문구가 남을 뻔한 걸 검증 중 발견해 수정.
- [진행중][kpop_universe.html] 그룹/멤버 카드 무한스크롤 "점세개 안 뜸" 재발 — 8/14에 두 차례(releaseLock 뷰포트 재확인 + 4개 컨테이너 scroll 리스너 보완) 고쳤는데도 사용자가 계속 재발 제보. 로컬에 헤드리스 크롬(CDP, playwright 없이 Node 24 내장 WebSocket/fetch로 직접 구현)으로 실제 재현 시도 — 두 기존 수정 모두 코드상 정상 존재 확인, 스크롤 시뮬레이션으로도 재현 실패(여러 페이지 정상 연속 로드됨). 기존 두 수정의 근본 원인 설명(iOS Safari fixed-position 컨테이너에서 IntersectionObserver 재발화 누락)이 특정 브라우저 엔진 이슈라 Windows Chrome 환경에서 애초에 재현이 안 될 가능성 높음. 어느 이벤트(intersect/scroll)에도 의존하지 않는 3중 안전망으로 setInterval(700ms) 폴링 추가 — moreWrap 위치를 주기적으로 직접 재확인해서 tryLoadMore 호출(hasMore/appendLock으로 이미 막혀있어 평소엔 no-op). 추가하면서 카드 닫힌 뒤에도 이 폴링이 계속 도는 잠재 문제 발견(closeCards가 컨트롤러 상태/리스너를 안 지움 — 기존 scroll 리스너도 같은 결함 있었으나 실제 스크롤이 일어나야만 발화해서 안 드러났던 것) — offsetParent 체크로 카드 닫히면 폴링이 조용히 멈추도록 보완, 헤드리스로 재검증(열림 상태 스크롤 시 정상 페이지네이션, 닫은 뒤 3.5초간 요청 0건) 완료. 사용자가 "여전히 발생"이라 아직 [진행중]으로 남김 — 실제 발생 기기/브라우저 확인 필요.
- [진행중][kpop_universe.html] 위 무한스크롤 건 후속 — 사용자 확인 결과 데스크톱 사파리·모바일 사파리 둘 다에서 발생(안드로이드는 미확인). Windows Chrome으론 재현이 안 되고 실제 발생 환경(사파리)을 테스트할 방법이 없어서, 추측 대신 실측하기로 함 — `tryLoadMore(ko,source)`에 트리거 출처(observer/scroll/poll/releaseLock) 태그 추가해 `_gaEvent('infscroll_trigger',{source})`로 GA에 남김. 이제 실사용 데이터가 쌓이면 IntersectionObserver가 실전에서 얼마나 자주 놓치는지, poll(3중 안전망)이 실제로 얼마나 자주 구제하는지 확인 가능. (구현 중 스스로 발견: scroll 리스너 등록을 인라인 화살표 함수로 바꾸면서 _cleanupScroll의 removeEventListener 대상이 어긋나는 실수를 커밋 전에 잡아 별도 참조로 수정.)
- [완료][kpop_universe.html] 탐험 패널 "Today's Anniversary" 줄 가로 스크롤 안 되던 버그 수정 — #feed-anniv에 .hscroll-drag 클래스+_enableDragScroll 누락(다른 스트립엔 다 있었는데 여기만 빠짐, #feed-overlay가 메인 &lt;script&gt;보다 뒤에 있는 정적 HTML이라 스크립트 최상단에서 바로 바인딩하면 안 돼서 _buildFeedAnniversaries() 안에서 지연 바인딩). 근본 원인은 더 넓은 문제였음 — 모바일 카드 바텀시트 스와이프-닫기(_wireSheetSwipe)와 탐험 패널 스와이프-닫기(_wireFeedSwipeDismiss) 둘 다 .hscroll-drag 영역 제외 로직이 없어서, 콘텐츠가 맨 위일 때 가로 스크롤 시도가 세로 방향 손떨림만으로도 시트 닫기/펼치기로 오인식되던 상태였음(#tt의 멤버 스와이프 제외 로직엔 있었는데 이 둘엔 없었음). 두 함수 모두에 같은 제외 체크 추가 — 이제 .hscroll-drag 붙은 줄(카테고리 칩·태그 줄·디스코그라피 스트립·기념일 스트립 등)은 모바일 바텀시트/탐험 패널 안 어디에 있든 좌우 스크롤이 세로 제스처에 안 뺏김.
- [완료][kpop_universe.html] 디스코그래피 앨범 타입 표기 "미니"→"EP" 통일 — groups.json 원본 type 값("미니"/"미니 1집"~"미니 8집")은 안 건드리고(멜론 스크래핑 진행 중이라 동시수정 위험) `_discogTypeLabel()` 신설해 표시 단계에서만 치환(그룹카드 디스코그래피 스트립 배지+상세 팝업 2곳 적용). "미니 5집"→"EP 5집"처럼 뒤에 "집" 붙는 건 그대로 유지("정규 N집"과 같은 패턴이라 안 건드림).
- [완료][kpop_universe.html][kpop_universe.css][admin.js] 어드민 CSS 통일 + 신규 버튼 2종 — ①어드민 팝업 패널(vm/hnn/gp/fbv) 스타일 감사 결과 vm-panel이 가장 오래돼서 배경색·radius·shadow·헤더 폰트굵기·탭 스타일(개별 테두리 필박스)이 나머지 3개(hnn/gp/fbv, 공유 배경+무테두리 세그먼트 방식)랑 어긋나 있던 것 확인 후 값 전체를 gp/fbv 기준으로 맞춤(overflow:hidden 누락도 같이 보완). fbv-close도 지난 세션에 만들 때 살짝 다르게 넣었던 걸 gp-close 기준으로 재수정. HTML/JS(탭 전환 바인딩)는 안 건드리고 CSS 값만 교체. ②영상관리패널에 "선택-정상" 버튼 신설 — 기존 vm-apply-btn은 탭마다 동작이 고정(전체탭은 무관처리만 가능)이라 이미 플래그 섞인 항목들을 한번에 정상으로 되돌릴 방법이 없었음, vm-indiv-btn과 동일 패턴으로 추가. ③그룹/멤버 카드 영상 그리드에 어드민 전용 "전체선택" 버튼 신설 — state.allVids(현재 탭/검색 기준으로 로드된 목록, 필터 바뀔 때마다 리셋되는 값이라 항상 "지금 노출된 것"과 일치) 전체를 한 번에 _selIds에 담음. vm-toolbar/.gc-ch-hd에 flex-wrap:wrap 추가해 버튼 늘어나도 자동 줄바꿈되게 함.
- [완료][index.html] index.html이 kpop_universe.html과 2군데 어긋나 있던 것 발견+동기화 — 8/12 리액션 키 구분자 버그수정(스페이스→null byte)이 index.html엔 반영 안 됨. index.html은 항상 kpop_universe.html과 byte-for-byte 동일해야 함(PRINCIPLES.md에 경고 추가). 이번 세션 작업 끝나고 다시 전체 동기화 완료.
- [완료][kpop_universe.html][index.html] 영어모드(EN) 미번역 텍스트 전수 수정 — 사용자 제보(설정 "N세대" 버튼, 소속사명)로 시작해 전체 감사 후 우선순위대로 전부 수정: ①탐험 피드 카드 제목/부제(포맷모아보기·세대별직캠·주간TOP20·연도별TOP100·데뷔연도모아보기) ②앱 로드 실패 화면 ③피드백 폼 전체(data-i18n 신규 부여) ④컬렉션 관리(새로만들기/이름수정/삭제/공개전환/한줄평/영상개수, `_colVidCountText` 헬퍼 신설) ⑤다중선택 버튼("선택"/"취소"/"N개 선택됨", `_selCountText` 헬퍼 신설) ⑥공유링크 복사 토스트 ⑦기타(가족 라벨·기타앨범포함·즐겨찾기 전체보기·다른아티스트의 커버·콘서트 예매정보/다녀왔어요/한줄후기·음방1위 트로피·N명이 좋아해요·X계정 연결 alert). `applyLang()`에 `data-i18n-title` 속성 신규 지원 추가(title 툴팁용, 기존 data-i18n/data-i18n-ph와 동일 패턴). 설정 "N세대" 버튼은 아이콘이 같은 버튼에 섞여있어 `_updateGenButtonsLang()` 별도 함수로 처리, 이 과정에서 `_openGenInfo` let 선언이 파일 뒷부분에 있어 초기 `applyLang()` 호출 시 TDZ ReferenceError 나던 것 발견해 앞으로 옮겨 수정. 관리자 전용 UI(어드민 검수센터·연결카드 태깅 등)는 의도적으로 미번역 유지.
- [완료][kpop_universe.html][index.html] 소속사(agency) 영문 표기 정확도 개선 — 기존엔 "…엔터테인먼트"→"Ent." 단순 접미사 치환뿐이라 순수 한글 브랜드명(스윙·큐브·젤리피쉬 등)은 여전히 한글로 남았음. groups.json의 147개 소속사명 중 한글 포함 110개 전체를 나무위키/웹검색으로 실제 공식 영문 표기 확인해 `_AGENCY_EN` 매핑 테이블로 추가(`_dispAgency`가 테이블 우선 조회, 없으면 기존 접미사 치환으로 폴백). 판타지오(Fantagio, "Fantasio" 아님)·플레디스(Pledis)·웨이크원(WAKEONE)처럼 음차 추측과 실제 표기가 다른 사례 다수 확인. `포켓돌스튜디오 / 엠이오` 1건은 영문 자료 확인 못해 추정치로 남김(코드에 UNVERIFIED 표시).
- [완료][kpop_universe.html] 신규 유저 프로필(별) 첫 생성 시 기본 컬렉션 "Bookmarks" 자동 생성 — 별도 북마크 버튼이 없어 컬렉션이 그 역할을 대신하는데, 유저가 직접 만들어야만 시작할 수 있던 문제. saveProfile()에서 `_isNewProfile&&!collections.length` 조건일 때만 생성(기존 유저 소급 미적용, 익명 상태에서 이미 만든 컬렉션 있으면 건너뜀). 이름은 언어 설정 무관 고정 "Bookmarks"(사용자 결정)
- [완료][kpop_universe.html][kpop_universe.css][admin.js] 피드백 조회 패널 신설 — 기존엔 사용자가 보낸 피드백이 `feedback` 테이블에 저장만 되고 관리자가 볼 UI가 없었음. 설정 패널 어드민 섹션에 "피드백 보기" 버튼 추가, 그룹 우선순위 패널(gp-)과 같은 톤으로 카테고리별 탭(전체/버그/제안/기타)+목록+삭제 기능만 구현(읽음 상태 추적은 없음, 처리 끝나면 삭제로 정리하는 방식). `feedback` 테이블에 `id`·`created_at` 컬럼 있다고 가정하고 짰으니 실제 스키마와 다르면 확인 필요.

## 2026-08-15

- [완료][kpop_universe.html][kpop_universe.css] 몰입형 쇼츠 모드 UI 항상 표시 — 닫기·리액션·툴바 아이콘 기본 숨김(lb-ui-hidden) 제거, 탭-캡(탭→UI토글) 기능 제거
- [완료][kpop_universe.html] 탐험 버튼으로 새 카드 열릴 때 스크롤 위치 초기화 버그 수정 — display:none 중 scrollTop=0이 Safari에서 무시되던 문제, display:block 직후 재설정으로 보완(openMobSheet 첫 오픈 + 스택 푸시 2곳)
- [완료][kpop_universe.html][kpop_universe.css] 라이트박스 쇼츠 모드 UI 정리 — ① 닫기 버튼 아이콘 항상 chevron(↓)으로 통일(✕ 제거, 모드 무관하게 동일); ② 줄 세개 버튼(lb-shorts-list-btn) 완전 제거; ③ rotate 버튼 양방향 토글로 통합 — 세로(쇼츠)→가로(browse), 가로→세로(shorts) 자유 전환; _lbSwitchToShorts() 신설
- [완료][groups.json][artists.json] 그룹 추가: 아이칠린(ICHILLIN', 2021, KM엔터, 걸그룹 7인), 엔싸인(n.SSign, 2023, nCH엔터, 보이그룹 7인, 청춘스타 출신)
- [완료][kpop_universe.html] 동기화 무관 키워드 추가 — 스프링피버·유뷰녀 킬러 (_JUNK_TITLE_KEYWORDS_GLOBAL)
- [완료][kpop_universe.html] 빈 탭 숨김 존재 체크 쿼리 버그 수정 — cover는 category='cover' 대신 title_norm ilike(cover·커버·원곡) + content_formats 체크로 수정, variety에 제목 키워드(라디오스타·아는형님 등) 추가, show에 content_formats.cs.{show} 추가 → buildBaseQuery 실제 필터와 일치

## 2026-08-14

- [완료][kpop_universe.html] live·cover 탭 content_formats 크로스탭 지원 — content_formats에 'live'/'cover' 수동 태깅 시 해당 탭에 복수 노출; _pickFeaturedVidFromDB·buildBaseQuery·patchItem 3곳 동기화
- [완료][kpop_universe.html] 빈 탭 자동 숨김 — 카드 오픈 시 all 탭 제외 각 탭에 limit(1) 병렬 존재 확인, 영상 없으면 탭 제거·현재 필터가 해당 탭이면 all로 리셋; fan 탭은 기존 별도 처리 유지
- [완료][kpop_universe.html] 무한스크롤 점 세개 재발화 누락 보완 — scroll 리스너를 mob-card-stack-inner·mob-sheet-inner·side-panel·member-panel 4곳에 추가(IntersectionObserver가 iOS Safari 등에서 fixed 컨테이너 안에서 재발화 놓치는 경우 대비); 이전 releaseLock 뷰포트 체크와 병행
- [완료][kpop_universe.html] 모바일 카드 내 영상 스크롤 감도 상향 — _wireSlowScroll factor 0.85→0.92, coast decay 0.87→0.90 (한 번 터치에 더 긴 거리 스크롤+관성 여운 증가)
- [완료][kpop_universe.html] 멤버 카드 콘서트 분리 — 그룹 멤버는 멤버명 기반 솔로 이벤트만(_loadMemberConcertRow 신설), 솔로 아티스트는 기존대로; DB에 groups=['멤버명'] 추가 즉시 자동 표시
- [완료][kpop_universe.html] Cover 탭 제외 키워드 추가 — 무빙 커버·moving cover·original contents (대표영상쿼리·목록쿼리·patchItem 3곳 동기화)
- [완료][kpop_universe.html] 예능 탭 제목 키워드 분류 추가 — 라디오스타·아는형님·아는 형님·놀라운토요일·전참시 (buildBaseQuery 2곳+patchItem 3곳 동기화, patchItem에서 content_formats 체크도 누락분 추가)
- [완료][kpop_universe.css][kpop_universe.html] Collections 헤더에 인라인 컬렉션 수 표시 — "Collections · n개" 형식, cnt 스팬 별도 스타일(letter-spacing·uppercase 해제, 약간 연하게)
- [완료][kpop_universe.css] 콘서트 표시줄 폰트 11.5px→10.5px 축소, padding-top 10px→7px 축소 / gc-collection-row margin-top 9→7px·padding-top 10→8px 조정(아래 선 간격 감소)
- [완료][kpop_universe.css] 틱톡↔나무위키 아이콘 간격 보정 재수정 — 이전 -3px는 방향이 반대(gap 증가), -11px로 교체해 시각적으로 다른 아이콘 쌍과 균등하게 맞춤
- [완료][kpop_universe.css][kpop_universe.html] 대표(featured) 쇼츠 표시 크기 확대 — 42% 폭 제한 제거, 2열 전체 폭으로 9:16 비율 최대 크기 표시 / 버튼 위치 오버라이드 제거(기본값으로 복귀)
- [완료][kpop_universe.html] Shorts 탭 그리드에 간헐적 와이드 쇼츠 추가 — _groupGcVids에 widenSomeShorts 파라미터, 10% 확률로 단독 2열 폭 카드 삽입(normal 영상과 동일한 방식)

- [완료][kpop_universe.html] 무한스크롤 점 세개 안 나오는 버그 수정 — appendLock 해제 시점에 moreWrap이 이미 뷰포트 안이면 IntersectionObserver가 재발화 안 하던 문제, releaseLock에서 직접 viewport 체크 후 tryLoadMore 호출로 보완
- [완료][kpop_universe.html] Live 탭에 제목 기반 예외 추가 — '리무진서비스'/'리무진 서비스' 포함 영상 category 무관하게 live 탭으로 포함 (buildBaseQuery·_pickFeaturedVidFromDB·patchItem 3곳 동기화)
- [완료][kpop_universe.html][admin.js] variety/show 탭 분류를 category 단일값 → content_formats 배열 기반으로 전환 — 동기화 시 tier 자동 태깅, 수동 저장 시 기존 코너명 태그 보존하며 장르 태그만 교체, 쿼리는 category OR content_formats OR 병행(기존 레거시 행 누락 방지)
- [완료][kpop_universe.html] 어드민 영상 포맷 편집 select에 예능·드라마/영화 옵션 추가
- [완료][kpop_universe.html] Cover 탭 제외 키워드 추가 — discover/recovery/undercover 및 한글판(디스커버·리커버·언더커버) 3곳(대표영상쿼리·목록쿼리·클라이언트 필터) 동기화
- [완료][kpop_universe.css] 멤버 이름 레이블(10명 이하 그룹 웹) 폰트 9.5px→8.5px 축소, 투명도 0.55→0.82로 높이고 text-shadow를 group-label과 동일한 currentColor 이중 글로우 방식으로 변경
- [완료][kpop_universe.html] Cover 탭 제외 키워드 추가 — original ver / cover story / 커버 촬영 / uncover / choom original / the original (목록쿼리·대표영상쿼리·patchItem 클라이언트 필터 3곳 동기화)
- [완료][admin.js] 영상 태그 저장 후 그리드 갱신 딜레이 500ms → 100ms (단일/일괄 저장 모두)
- [완료][kpop_universe.html] 어드민 계정 GPU 렌더링 15fps로 제한 — 일반 유저는 30fps 유지, 로그인 전/일반 계정은 그대로
- [완료][kpop_universe.html][kpop_universe.css] 공개 컬렉션 칩에 삭제 버튼(×) 추가 — mine인 칩에만 hover 시 표시, 클릭 시 confirm 후 `public_collections` + 로컬에서 동시 삭제
- [완료][kpop_universe.html] 어드민 로그인 시 본인이 만든 공개 컬렉션 안 보이는 버그 수정 — `_loadCollectionRow`의 `pub` 필터를 `owner_id !== uid` → `!mineIds.has(r.id)`로 변경; 자기 uid 공개 컬렉션이 `user_data`에 없으면 mine/pub 양쪽에서 제외되던 문제

- [완료][kpop_universe.css][kpop_universe.html] 멤버 카드 소속 그룹 폰트 조정 — 유닛은 메인 그룹(12px)보다 1pt 작게(11px), 영어 텍스트는 추가 1pt 감소(그룹 11px·유닛 10px), `tg-en` 클래스로 한글 미포함 텍스트 자동 감지
- [완료][kpop_universe.html] WHO'S YOUR BIAS? 검색창 디자인 수정 — 위치 아래로(top 58→76px), 보라빛 제거·더 투명한 물방울 느낌(배경 0.06, blur saturate 140%, border 0.5px, 그림자 축소, border-radius 28px)
- [완료][kpop_universe.html] 탭바 위 떠있던 `#search-wrap` 자동오픈(신규 유저 모바일) 제거 — 중복 검색창이었음
- [완료][kpop_universe.html] "WHO'S YOUR BIAS?" 검색창 개편 — 비로그인 모바일에만 표시, 버튼이 아닌 바로 열린 입력창으로 변경, 탐험·별표 탭 클릭 시 / 검색결과 선택 시 자동 숨김
- [완료][admin.js] admin.js 전체 코드 검토 (파일 수정 없음) — 매칭 예외 목록 하드코딩 증식·`_m2ParseTitle` 복잡도 누적·sweep 함수 중복·two-step tags_manual 저장 취약점 등 개선 포인트 정리해 보고
- [완료] CHANGELOG.md 신설 — 오늘부터 세션마다 기록 습관화
- [완료][admin.js] 어드민 영상관리 검색: 결과 제한 200→1000개로 상향, 1~2글자 검색 대소문자 구분되던 버그 수정(_titleNorm 정규화 누락)
- [완료][kpop_universe.html] 재생 모드 업로드일 표시 — 탐험 피드 스포트라이트 카드 5종에 published_at 누락되어 있던 것 수정
- [완료][groups.json] disbanded 그룹 41개 전수 해체연도/날짜 리서치+반영 (39개 날짜 채움, 이달의소녀·카드는 "해체 아님" 확인되어 플래그 해제)
- [완료][kpop_universe.html] 영상 동기화 시 해체 그룹의 해체일 이후 영상은 수집 안 하도록 컷오프 로직 추가(`_disbandCutoffDate`)
- [완료][groups.json][artists.json] 그룹 추가: 배틀(2006, 배틀신화 출신), 스피카(2012~2017)
- [완료][kpop_universe.html] 해체 그룹·탈퇴 멤버의 소속사(agency) 표기 흐리게 처리(`.tag-chip-past`)
- [완료][groups.json][kpop_universe.html] JX(JYJ) 그룹 행성 제거 → 동방신기 소속 유닛으로 전환 — 전원 active:false인데 disbanded 아니라 행성에 별 없던 버그 처리
- [완료][groups.json] 서바이벌쇼 24개 전수조사 — projectShow 태그 11개 그룹 신규 추가(I-LAND→엔하이픈 등)
- [완료][kpop_universe.html] 온보딩 "Who's Your Bias?" 검색바 — 반투명·페이드인·텍스트 글로우, 최초 1회 타자 입력 모션 추가
- [완료][kpop_universe.html] 멤버 카드 그룹칩 줄(#tg-row) 웹 휠/드래그 스크롤 안 되던 버그 수정 — `_enableDragScroll` 미연결이 원인
- [완료][kpop_universe.html] _PROJECT_UNITS 68개 서브유닛 일괄 추가 (EXO-CBX·SC, 인피니트H·F, BTOB-BLUE·4U, VIXX LR, JJ Project, Jus2, NU'EST W, 이달의소녀 3종, MISAMO, 3RACHA 등 총 68개)
- [완료][artists.json] soloDiscography 추가: 빅스 전원·앰버·레이·디오(엑소)·아이즈원 4명·이찬혁 등
- [완료][artists.json] 나무위키 링크 45명 신규 추가 (라이즈·아이브·빅스·엔시티 드림·에프엑스·엑소 등)
- [완료][artists.json] 인스타그램 링크 추가 — 트리플에스 7명·사쿠라(르세라핌)·리센느 4명 등; 베이비몬스터/보이넥스트도어/엔하이픈/트레저 등 소속사 정책상 없음 확인
- [완료][artists.json] 제로베이스원 누락 멤버 4명 추가 (장하오·리키·김규빈·한유진) — artists.json 총 1,585명

## 2026-08-09~13

- [완료][artists.json] unitDiscography 필드 신설 — groups.json 246개 그룹 전수 서브유닛 조사·80개+ 유닛 멤버별 개별 적용 (NCT U는 앨범별 참여 멤버 분리)
- [완료][artists.json] soloDiscography 대량 추가 (슈퍼주니어 전원·씨스타·애프터스쿨·샤이니 키 등 총 80+명)
- [완료][kpop_universe.html] _PROJECT_UNITS 3개 추가 (아이린&슬기, SM THE BALLAD, NCT U)

